# GATE-K8REACH registry — every UNAOS_* knob with no K8_FEATS arm, and why.
# NA <reason> = deliberately absent from the Pi bare-metal image.
# TODO <owner/note> = grandfathered at seeding, nobody has ruled yet.
# `scripts/k8-reach.py --evidence <KNOB>` prints the sites behind a row.
#
# AN `NA` ROW CITES THE COMMAND THAT JUSTIFIED IT, not a reason string alone (pi 7,
# 2026-09-06). A human first pass inherits the classifier's blind spots exactly: the
# arch-neutral file (`nvidia-kepler`'s site in `video/wm.rs`) and the x86-gated callee
# (`rastmc`) are the cases a reader also mis-sorts, because the path and the cfg both
# look decisive and neither is. `NA  x86 only` is a second classifier with no proof and
# worse recall; `NA  --evidence: 11 sites, 0 Pi-live (all under drivers/gpu, x86 mod tree)`
# is checkable by the next seat.
UNAOS_AHCI   NA   --evidence: ahci 17 sites, 0 Pi-live, 0 negated — 16 classified X86 and 1 PROSE (the module doc quoting its own gate). Every code site is the SAME conjunction, `all(target_arch = "x86_64", feature = "ahci")`: the `pub mod ahci` declaration in drivers/mod.rs, the AHCI registry block at the tail of drivers/block.rs, and the one enumeration hook appended in arch/x86_64/pci.rs. There is no arch-neutral consumer to mis-sort (the `nvidia-kepler`-in-`video/wm.rs` trap this file's header names) and no x86-gated callee reached from a Pi-live caller (the `rastmc` trap): drivers/ahci.rs is not lexed on aarch64 at all, so a K8_FEATS arm would arm nothing. Not merely absent from the Pi image but UNBUILDABLE there as written — the enumeration seam is `arch::pci` config space, which the aarch64 tree does not provide. The FILE is deliberately arch-neutral in name and shape (AHCI exists on aarch64 boards), so if an aarch64 board ever grows an ABAR route this row is the place that has to change. Added 2026-09-15 with the driver's first rung (rmbp-ledger B89).
UNAOS_AHCI_WRITE   NA   --evidence: ahci-write 26 sites, 0 Pi-live, 0 negated, implies ahci — every one is inside the same `target_arch = "x86_64"` conjunction the `UNAOS_AHCI` row above documents, and `ahci-write` implies `ahci` in Cargo.toml, so a K8_FEATS arm for it would arm its parent too and still reach no aarch64 code. The three code homes: the `write_block_at` tail of drivers/ahci.rs (a file the aarch64 tree never lexes — its `pub mod` is `#[cfg]`-erased), the `WriteGrant` + `write_sectors_granted` tail of drivers/block.rs (`all(target_arch = "x86_64", feature = "ahci-write")` on every item), and the SATA leg of install/partition.rs, which is `installdemo`-gated at its `pub mod` declaration and therefore not in a Pi image at all — K8_FEATS is `baremetal,skip_xhci` plus witness/logts/bootlog and has never carried `installdemo`. The two arms in install/mod.rs are single `#[cfg(all(target_arch = "x86_64", ...))]` match arms in a file aarch64 does compile, so they are the only sites in an aarch64-lexed file and both vanish. Not merely absent from the Pi image but UNBUILDABLE there as written, for the reason the parent row gives: the enumeration seam is `arch::pci` config space. If an aarch64 board ever grows an ABAR route, this row and its parent change together. Added 2026-09-15 with the driver's SATA write half (rmbp-ledger B89).
UNAOS_HDA   NA   --evidence: hda 4 sites, 0 Pi-live, 0 negated — 3 classified X86 and 1 PROSE (the module doc quoting its own gate). Every code site is the SAME conjunction, `all(target_arch = "x86_64", feature = "hda")`: the `pub mod hda` declaration in drivers/mod.rs:62, the enumeration hook appended to `PciScanner::enumerate_buses` at drivers/pci.rs:24, and the `audio_inventory` class-0x04 census at drivers/pci.rs:406. There is no arch-neutral consumer to mis-sort (the `nvidia-kepler`-in-`video/wm.rs` trap this file's header names) and no x86-gated callee reached from a Pi-live caller (the `rastmc` trap). ⚠ NOTE THE DIFFERENCE FROM THE `UNAOS_AHCI` ROW ABOVE, because it is the reason both pci.rs sites carry the arch in their OWN attribute rather than leaning on the module gate: drivers/ahci.rs is not lexed on aarch64 at all, but drivers/pci.rs IS — it is the shared PCI walker both arches compile — so a feature-only cfg there would have made an aarch64 build with `hda` enabled call into a module its own `#[cfg]` had deleted. That is the exact shape the `gen7` entry in arroyo's arm_features records a review catching. Not merely absent from the Pi image but UNBUILDABLE there as written: the enumeration seam is `arch::pci` config space, which the aarch64 tree does not provide, and the BCM2711 has no HD Audio controller to address. The FILE is deliberately arch-neutral in name and shape (HDA exists on other machines, and the Pi's HDMI/PWM audio is the sibling ROADMAP §6 still owes), so if an aarch64 board ever grows an HDA route this row is the place that has to change. Added 2026-09-17 with the driver's first arc (rmbp-ledger B127).
UNAOS_HDATONE   NA   --evidence: hda-tone 39 sites, 0 Pi-live, 0 negated, implies hda — every single one is inside drivers/hda.rs, a file the aarch64 tree never lexes because its `pub mod` is `#[cfg]`-erased by the parent row's conjunction, so a K8_FEATS arm for it would arm its parent too and still reach no aarch64 code. The 39 are the arc-2-only register and verb constants (34), the `cmd16` 4-bit-verb form, `elapsed_ms`, `mod tone`, the `run_tone` definition and its one call site — all gated `feature = "hda-tone"` ALONE rather than conjoined with the arch, which is correct and is the difference from the parent row: they live inside a file that is already arch-gated at its declaration, so the arch appears once, where it can be checked, instead of 39 times where it cannot. Not merely absent from the Pi image but UNBUILDABLE there, for the reason the parent row gives. If an aarch64 board ever grows an HDA route, this row and its parent change together. Added 2026-09-17 with the driver's tone arc (rmbp-ledger B127).
UNAOS_HDASIE   NA   --evidence: hda-sie 4 sites, 0 Pi-live, 0 negated, implies hda-tone — every one inside drivers/hda.rs (the two file-tail fns, the `impl Audit` variant, the same-line `sie_restore` call), a file the aarch64 tree never lexes (drivers/mod.rs gates the module on x86_64 + `hda`); the knob is the x86 HDA controller's INTCTL.SIE latch experiment (rmbp-ledger B207)
UNAOS_BAR1EXP              TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_BAR1WEDGE   NA   --evidence: bar1wedge 21 sites 0 Pi-live 0 NEG [implies: nvidia-kepler]. Every site but one is in drivers/gpu/pcihealth.rs; the exception is the SECSTA2 call statement appended to arch/x86_64/pci.rs, which carries the full all(target_arch = "x86_64", feature = "bar1wedge") conjunction in its own attribute (the AHCI hook shape), so a Pi compile still emits nothing and every one is x86-qualified: the two call statements sit inside `#[cfg(target_arch = "x86_64")]` functions (census, rp_at_wedge) and all sixteen tail items carry `all(target_arch = "x86_64", feature = "bar1wedge")` in the attribute itself, so a Pi compile emits nothing for this knob and there is nothing for a K8_FEATS arm to arm. The subject is a PCIe root port's config space reached through ACPI MCFG ECAM — the BCM2711 publishes no MCFG and this kernel's aarch64 half has no CF8/ECAM accessor at all, so the rung is not merely unbuilt on the Pi, it has no hardware to address there. Registered 2026-09-15 by the BAR1WEDGE executor (rmbp, ledger A1).
UNAOS_BCMARECON            TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_BCMAS1               TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_BEAM   NA   --evidence: beam 12 sites 11 Pi-live 1 NEG [implies: jd1dc]; Cargo.toml `beam = ["jd1dc"]` (the guard and the aperture are that rung's; UNAOS_JD1DC is the TODO row above). Ten of the eleven Pi-live sites are in arch/aarch64/display_tegra.rs, a module declared `#[cfg(feature = "tegra")] pub mod display_tegra` (arch/aarch64/mod.rs:111) — a Pi image never lexes the file; the eleventh is arch/aarch64/mod.rs:562, conjoined `all(feature = "tegra", feature = "beam")`, whose `not(...)` twin is the NEG. The consumer, video/beam.rs, is unconditional and arch-neutral on purpose (it is the MECHANISM, not a knob site) and folds to a no-op wherever `arch::scanout_beam()` is the constant `None` — which on the Pi it is: the BCM2711 HVS publishes no raster-line register this kernel reads, so there is nothing for the knob to arm there. The register it locks is Tegra234 nvdisplay `NV_PDISP_RG_DPCA` under the display@ BPMP power guard.
UNAOS_BOTFAULT             TODO  seeded 2026-09-06; 6 Pi-live cfg site(s)
UNAOS_BOTRING64            TODO  seeded 2026-09-06; 4 Pi-live cfg site(s)
UNAOS_BOTWEDGE             TODO  seeded 2026-09-06; 3 Pi-live cfg site(s)
UNAOS_BSPRUN               TODO  seeded 2026-09-06; 5 Pi-live cfg site(s)
UNAOS_BSPTICK              TODO  seeded 2026-09-06; 8 Pi-live cfg site(s)
UNAOS_BT                   TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_BTC                  TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_BTDIR                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_DEADMAN              TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_DESKCASCADE          TODO  seeded 2026-09-06; 417 Pi-live cfg site(s)
UNAOS_EHCICONFIG           TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_EHCISCOUT            TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_GA10B_PROBE1         TODO  seeded 2026-09-06; 2 Pi-live cfg site(s)
UNAOS_GMUX_IGD             TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_HCRONST              TODO  seeded 2026-09-06; 3 Pi-live cfg site(s)
UNAOS_HOLOCRON             TODO  seeded 2026-09-06; 6 Pi-live cfg site(s)
UNAOS_INSTALLDEMO          TODO  seeded 2026-09-06; 4 Pi-live cfg site(s)
UNAOS_INSTALL_TARGET_SD    TODO  seeded 2026-09-06; 99 Pi-live cfg site(s)
UNAOS_INSTGUI              TODO  seeded 2026-09-06; 4 Pi-live cfg site(s)
UNAOS_IRQSTORAGE           TODO  seeded 2026-09-06; 1 Pi-live cfg site(s)
UNAOS_IVB                  TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_IVB3D                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_IVB3D_R8   NA   --evidence: gen7r8 2 sites, 0 Pi-live, 0 negated [implies: gen7, intel-ivb]; both sites are in drivers/gpu/gen7.rs, which carries a file-level `#![cfg(target_arch = "x86_64")]` (gen7.rs:122), so a Pi image never lexes the file. The rung drives the Ivy Bridge IGD's BCS blitter through BAR0 of PCI 0:2.0 — there is no such device on a BCM2711 — and its one call site is the tail of `blit()` (gen7.rs:5663), reached only from `igpu::init` under `all(target_arch = "x86_64", feature = "gen7")` (igpu.rs:660). Its parent UNAOS_IVB3D is the TODO row above.
UNAOS_IOAPIC   NA   --evidence, run at this commit and quoted verbatim: `ioapic: 3 site(s), 0 Pi-live, 0 negated` — all three classified X86, and the classifier is right for a reason stronger than the path. THE SUBJECT IS AN x86 INTERRUPT CONTROLLER AND NOTHING ELSE: the Intel 82093AA I/O APIC, reached through an ACPI MADT the BCM2711 does not publish, programming redirection entries that deliver to a LOCAL APIC the Pi does not have. aarch64 routes its interrupts through the GIC, which this feature does not touch and structurally could not. The three sites: the `pub mod ioapic` declaration at arch/x86_64/mod.rs:283 — a directory the aarch64 tree never compiles, so the module is not merely unarmed on the Pi, it is not lexed — and the two LINE-NEUTRAL `#[cfg]`-erased appends in arch/x86_64/acpi.rs (:169 the census call, :300 the MADT catch-all arm), which live in the same x86-only directory. There is no arch-neutral consumer to mis-sort (the `nvidia-kepler`-in-`video/wm.rs` trap this file's header names) and no x86-gated callee reached from a Pi-live caller (the `rastmc` trap): the 0 Pi-live count is not a classifier opinion, it is the absence of any site in a file aarch64 lexes at all. SECOND, INDEPENDENT REASON, the one that would still hold if the module were ever made arch-neutral: `kernel8()` builds its OWN curated list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and never reads the `_feats` knob-mapping block this knob writes to, so `UNAOS_IOAPIC=1 ./arroyo kernel8` cannot change a byte of a Pi image. Commands: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment inside kernel8(), none from _feats); `grep -n 'UNAOS_IOAPIC' unaos/arroyo` (one non-comment line). Arroyo's `arm_features` also strips `ioapic` from every aarch64 build for the same reason it strips `hda` and `ahci`, so the aarch64 media stay byte-identical (`./arroyo knoboff ioapic` exit 0 on both arches). If an aarch64 board ever grows an I/O APIC — it will not; that is what the GIC is — this row is the place that has to change. Registered 2026-09-22 by the IOAPIC executor (rmbp-ledger B147).
UNAOS_JD1DC                TODO  seeded 2026-09-06; 19 Pi-live cfg site(s)
UNAOS_KDISP_HOLD           TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_KEPLER               TODO  seeded 2026-09-06; 1 Pi-live cfg site(s)
UNAOS_KEPLER_CE            TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_KEPLER_FIFO          TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_KEPLER_TAKEOVER      TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_MTRAW                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_NET4                 TODO  seeded 2026-09-06; 50 Pi-live cfg site(s)
UNAOS_NOASPM               TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_NOEHCIHID            TODO  seeded 2026-09-06; 2 Pi-live cfg site(s)
UNAOS_NOKBDWIT             TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_NOPORTSW             TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_NOSDHCBLK            TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_NOSDWRITE   NA   --evidence: sdwrite 24 sites 21 Pi-live 1 NEG. DELIBERATELY absent from K8_FEATS, and the Pi-live count is the reason rather than an oversight. `sdwrite` (A60, DEFAULT ON in the general knob map) lifts the SdMmc write veto; its tegra-gated half cannot reach a Pi image at all, but its OTHER half is the arch-neutral posture FORWARD at fs/vfs.rs:1805 and fs/bootdisk.rs:1372 — `NativeBackend::write_veto` stops asserting `None` and forwards the block layer's answer for the handle the shared unafs mount rides. On the Pi that handle is `Global`, so arming this knob on kernel8 would make `/`'s `rw=` (and the shell's pre-mutation refusal) follow FRGUARD's `default_writable()` instead of a flat yes. That is arguably more honest and it is a CHANGE TO THE PI'S BEHAVIOUR, which is the pi seat's call and not this arc's (R39: no seat proofs another seat's board; orin cannot run `kernel8-test`). Absent from K8_FEATS the Pi image is byte-identical to baseline and the knob is inert there BY CONSTRUCTION. Registered 2026-09-12 by the SDWRITE2 executor (orin, ledger A60), which names the open question in that row.
UNAOS_NOSMOLNET            TODO  seeded 2026-09-06; 4 Pi-live cfg site(s)
UNAOS_NOTEGRASMP           TODO  seeded 2026-09-06; 242 Pi-live cfg site(s)
UNAOS_ORINCLICK            TODO  seeded 2026-09-06; 32 Pi-live cfg site(s)
UNAOS_ORINCONWIN           TODO  seeded 2026-09-06; 420 Pi-live cfg site(s)
UNAOS_ORINDEFER            TODO  seeded 2026-09-06; 25 Pi-live cfg site(s)
UNAOS_ORINDESK             TODO  seeded 2026-09-06; 10 Pi-live cfg site(s)
UNAOS_ORINEL1AP            TODO  seeded 2026-09-06; 274 Pi-live cfg site(s)
UNAOS_ORINFACE             TODO  seeded 2026-09-06; 5 Pi-live cfg site(s)
UNAOS_ORINFURN             TODO  seeded 2026-09-06; 447 Pi-live cfg site(s)
UNAOS_ORININPUT            TODO  seeded 2026-09-06; 276 Pi-live cfg site(s)
UNAOS_ORINLADDER           TODO  seeded 2026-09-06; 504 Pi-live cfg site(s)
UNAOS_ORINRENDER           TODO  seeded 2026-09-06; 413 Pi-live cfg site(s)
UNAOS_ORINRX               TODO  seeded 2026-09-06; 249 Pi-live cfg site(s)
UNAOS_ORINTENANT           TODO  seeded 2026-09-06; 298 Pi-live cfg site(s)
UNAOS_ORINVPAR             TODO  seeded 2026-09-06; 11 Pi-live cfg site(s)
UNAOS_ORINWDT              TODO  seeded 2026-09-06; 3 Pi-live cfg site(s)
UNAOS_PCICENSUS            TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_PCIE2                TODO  seeded 2026-09-06; 16 Pi-live cfg site(s)
UNAOS_PCIE3                TODO  seeded 2026-09-06; 42 Pi-live cfg site(s)
UNAOS_PCIEPROBE            TODO  seeded 2026-09-06; 11 Pi-live cfg site(s)
UNAOS_PCILINK              TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_PFWIRE_SELFTEST      TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_PI                   TODO  seeded 2026-09-06; 133 Pi-live cfg site(s)
UNAOS_RAST                 TODO  seeded 2026-09-06; 30 Pi-live cfg site(s)
UNAOS_RASTMC               TODO  seeded 2026-09-06; 1 Pi-live cfg site(s)
UNAOS_RTPI                 TODO  seeded 2026-09-06; 2 Pi-live cfg site(s)
UNAOS_RTWIT                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_SCHED_DEMO           TODO  seeded 2026-09-06; 3 Pi-live cfg site(s)
UNAOS_SDMMC                TODO  seeded 2026-09-06; 47 Pi-live cfg site(s)
UNAOS_SDMMC_ARM            TODO  seeded 2026-09-06; 57 Pi-live cfg site(s)
UNAOS_SDWRITE     NA   --evidence: sdwritefx 2 sites, both in fs/bootdisk.rs (the `bind` call site and the fixture) [implies: sdwrite, sdmmc]. The knob arms the METAL fixture — create / write / read-back / delete `/SDWRITE.TXT` on the bound root — whose purpose is to prove the Tegra234 microSD write on an Orin bench boot. Absent from K8_FEATS for the reason the UNAOS_NOSDWRITE row above gives (it implies `sdwrite`, so arming it on kernel8 would carry that knob's Pi-live forward with it), and additionally because a fixture that WRITES A FILE to an operator's card has no business arriving on a board whose seat has not asked for it. Registered 2026-09-12 by the SDWRITE2 executor (orin, ledger A60).
UNAOS_SELFHOST             TODO  seeded 2026-09-06; 2 Pi-live cfg site(s)
UNAOS_SELFUP               TODO  seeded 2026-09-06; 3 Pi-live cfg site(s)
UNAOS_SIMMER               TODO  seeded 2026-09-06; 4 Pi-live cfg site(s)
UNAOS_SKIP_XHCI            TODO  seeded 2026-09-06; 3 Pi-live cfg site(s)
UNAOS_SMC                  TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_SMCWALK              TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_SMPMARK              TODO  seeded 2026-09-06; 3 Pi-live cfg site(s)
UNAOS_SUPSTATE             TODO  seeded 2026-09-06; 45 Pi-live cfg site(s)
UNAOS_TEGRA                TODO  seeded 2026-09-06; 242 Pi-live cfg site(s)
UNAOS_TEGRADESK            TODO  seeded 2026-09-06; 413 Pi-live cfg site(s)
UNAOS_TEGRASMP             TODO  seeded 2026-09-06; 242 Pi-live cfg site(s)
UNAOS_TEGRA_EL0            TODO  seeded 2026-09-06; 262 Pi-live cfg site(s)
UNAOS_THERM                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_USBDEBUG             TODO  seeded 2026-09-06; 28 Pi-live cfg site(s)
UNAOS_VIDEOBENCH           TODO  seeded 2026-09-06; 1 Pi-live cfg site(s)
UNAOS_VIDEOCAP             TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_VNET                 TODO  seeded 2026-09-06; 7 Pi-live cfg site(s)
UNAOS_VROM                 TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_VSYNCPACE            TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_VUGRAS               TODO  seeded 2026-09-06; 1 Pi-live cfg site(s)
UNAOS_WC                   TODO  seeded 2026-09-06; 24 Pi-live cfg site(s)
UNAOS_WCDVALVE             TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_WCG_PAYGO            TODO  seeded 2026-09-06; 129 Pi-live cfg site(s)
UNAOS_WEDGEINJ             TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_WIFI                 TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_WIFI2                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_WIFI3                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_WIFI4   NA   --evidence: wifi4 21 sites, 0 Pi-live, 0 negated [implies: wifi, wifi2, wifi3]. Twenty are `#[cfg(feature = "wifi4")]`/`#[cfg(all(feature = "wifi3", not(feature = "wifi4")))]` attributes in crates/kernel/src/wifi/bringup.rs and the twenty-first is PROSE (the section header quoting its own gate). There is no second file and no arch-neutral consumer: `src/wifi/` is declared `#[cfg(all(feature = "wifi", target_arch = "x86_64"))]` in lib.rs and `bringup.rs` is `#[cfg(feature = "wifi2")]` inside it, so an aarch64 compile never lexes the file — neither the `nvidia-kepler`-in-`video/wm.rs` trap (arch-neutral file) nor the `rastmc` trap (x86-gated callee reached from a Pi-live caller) applies, because there is no aarch64-lexed site of any kind. A K8_FEATS arm would arm nothing. Not merely absent from the Pi image but UNBUILDABLE there as written: the whole arc reaches its device through `arch::pci` config space (cfg:0x80, the bcma BAR0 window selector) and the aarch64 tree provides no config-space accessor; the subject is a PCIe BCM4331 in a 2012 rMBP and there is no such part on a Pi. Same class and same argument as the four wifi rows above, which are TODO only because they predate this file's NA-with-evidence rule. Added 2026-09-17 with the §S5 PHY/radio rung (rmbp-ledger B124).
UNAOS_WIFIVAL              TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_WXNRO                TODO  seeded 2026-09-06; no Pi-live cfg site
UNAOS_XCARVE               TODO  seeded 2026-09-06; 245 Pi-live cfg site(s)
UNAOS_XCARVE_CRCRQ         TODO  seeded 2026-09-06; 241 Pi-live cfg site(s)
UNAOS_XCARVE_RELINK        TODO  seeded 2026-09-06; 240 Pi-live cfg site(s)
UNAOS_XCARVE_SCRUB         TODO  seeded 2026-09-06; 245 Pi-live cfg site(s)
UNAOS_XUSBFW               TODO  seeded 2026-09-06; 241 Pi-live cfg site(s)

# ---- rows for knobs that are NOT in THIS branch's `_feats` map -------------------------
# Measured and written by orin 17 (2026-09-06) against `hw-jetson` 963516c8, handed over ccd
# with the `--evidence` output behind each. They are DEFERRED here and become reds on the
# trunk if the knob has still not arrived; see the STALE note in k8-reach.py.
UNAOS_GA10B_PROBE2   NA   --evidence: ga10bprobe2 19 sites 19 Pi-live; Cargo.toml:2025 `ga10bprobe2 = ["tegra"]` — the knob arms `tegra` transitively, so a Pi image would build the wrong SoC; the payload module is gated `all(feature = "tegra", any(ga10bprobe1, ga10bprobe2, ga10bprobe3))` (arch/aarch64/mod.rs:110) and its call site is inside the tegra boot path (main.rs:2254), so all 19 sites are empty without `tegra`. GA10B is Tegra234 iGPU silicon; the Pi 4's GPU is VideoCore VI (`v3d`, already armed in K8_FEATS).
UNAOS_GA10B_PROBE3   NA   --evidence: ga10bprobe3 56 sites 56 Pi-live, ga10bprobe3b 9 sites 8 Pi-live; Cargo.toml:2048 `ga10bprobe3 = ["tegra"]` and :2065 `ga10bprobe3b = ["ga10bprobe3"]` — same tegra closure as PROBE2, same module gate (arch/aarch64/mod.rs:110) and same tegra-path call site (main.rs:2254). Rungs 3/3b live in arch/aarch64/ga10b_probe.rs (62 of the 65 sites); no Pi hardware answers a GA10B BAR.
UNAOS_GA10B_PROBE4   NA   --evidence: ga10bprobe4a 51 sites 50 Pi-live, ga10bprobe4b 10 sites 9 Pi-live, ga10bprobe4c 43 sites 43 Pi-live, ga10bprobe4d 10 sites 10 Pi-live (orin 27, 2026-09-12: values 3 and 4 added); same tegra closure as PROBE3: Cargo.toml `ga10bprobe4a = ["tegra"]`, `ga10bprobe4b = ["ga10bprobe4a"]`, `ga10bprobe4c = ["ga10bprobe4b"]`, `ga10bprobe4d = ["ga10bprobe4c"]`, same module gate (arch/aarch64/mod.rs) and same tegra-path call site (main.rs); rungs 4a/4b/4c and the deferred arm live in arch/aarch64/ga10b_probe.rs, their DMA window seater in arch/aarch64/mmu_tegra.rs, and 4d's one hook in power.rs (`psci_call`, cfg ga10bprobe4d, aarch64 non-pi); no Pi hardware answers a GA10B BAR.
UNAOS_GA10B_PROBE5   NA   --evidence: ga10bprobe5 3 sites 3 Pi-live (the arch-neutral LOADER `arch/aarch64/ga10b_fw.rs`, Cargo.toml `ga10bprobe5 = []`); its sub-knobs ga10bprobe5a/5e/5b/5d are added by a `case` map the classifier does not parse, and each `= ["ga10bprobe5a"...]` closes over `ga10bprobe4a = ["tegra"]` — the same tegra closure as PROBE2/3/4 (orin-0912b, 2026-09-12). NOT in kernel8()`\s curated K8_FEATS: the loader module compiles arch-neutrally but its only run sites are `fixture_service` (witness+ga10bprobe5, and every Pi media image is witness-FREE) and `ga10b_ignite::ga10bprobe5_run` (tegra); no Pi hardware answers a GA10B BAR, so setting the knob and flashing kernel8 changes nothing.
UNAOS_NET5   NA   --evidence: net5 30 sites 29 Pi-live, net4 9 sites 8 Pi-live, pcie3 28 sites 26 Pi-live, pcie2 17 sites 16 Pi-live; the Cargo closure net5->net4->pcie3->pcie2 pulls NO `tegra` (Cargo.toml:1000/980/965/951), so unlike the other six this one WOULD compile into a Pi image — and reach nothing: the armed entry `net4_bringup` is `all(feature = "net4", feature = "tegra")` (main.rs:2357), and the only non-tegra entry (main.rs:351) sits inside the QEMU-virt block `all(target_arch = "aarch64", not(feature = "pi"), not(feature = "tegra"))` (main.rs:309) which a kernel8 image compiles out because `baremetal = ["pi", "aarch64_el0"]` (Cargo.toml:225). Arming it would add arch/aarch64/rtl8168_tegra.rs + pcie_probe.rs + mmu_tegra.rs to the image with zero call sites. Hardware: the RTL8168 hangs off Tegra234 PCIe C5; the Pi 4's PCIe carries the VL805 xHCI and its NIC is the on-SoC GENET (UNAOS_GENET is already armed in K8_FEATS).
UNAOS_SDMMCWRITE   NA   --evidence: sdmmcwrite 17 sites 17 Pi-live, sdmmc 50 sites 48 Pi-live; same closure as SDMMCROOT — every site is in the `tegra`-gated `mod metal` (sdmmc_tegra.rs:127), the export is `all(feature = "tegra", feature = "sdmmcwrite")` (sdmmc_tegra.rs:125) and the call site is `all(feature = "sdmmcwrite", feature = "tegra")` (main.rs:2371), so a Pi image reaches none of the 17. NOT registered on a "the Pi cannot write" ground: since U9 drivers/block.rs:1198 routes SD writes to `crate::drivers::emmc2::write_block_512` (implemented at drivers/emmc2.rs:798), so the Pi card path DOES write — the refusal claim in the emmc2.rs header (lines 6-7) is stale prose. The gap-#3 probe targets a Tegra234 microSD slot the Pi does not have.
UNAOS_TCUPROBE   NA   --evidence: tcuprobe 2 sites 2 Pi-live, tegra 270 sites 259 Pi-live; the knob arms `tegra` outright in both arroyo (`_feats="${_feats}tcuprobe,tegra,"`, arroyo:1328) and Cargo (`tcuprobe = ["tegra"]`, Cargo.toml:2658) — a Pi image carrying it would build the wrong SoC (boot_tegra/mmu_tegra/smmu_tegra, tegra GIC and serial). Both sites are tegra-conjoined: the module `all(feature = "tegra", feature = "tcuprobe") pub mod hsp_tegra` (arch/aarch64/mod.rs:110) and the arm `hsp_tegra::tcuprobe_arm` (main.rs:2495). The Tegra234 HSP shared mailbox / TCU has no BCM2711 counterpart.
UNAOS_TCURX   NA   --evidence: tcurx 32 sites 32 Pi-live, tcuprobe 2 sites 2 Pi-live, orinrx 10 sites 10 Pi-live, tegra 270 sites 259 Pi-live; arms `tegra` in arroyo (`_feats="${_feats}tcurx,tcuprobe,orinrx,tegra,"`, arroyo:1342) and in Cargo (`tcurx = ["tegra", "orinrx", "tcuprobe"]`, Cargo.toml:2697) — same wrong-SoC bar as TCUPROBE. Its consumer half is the Tegra234 UART/HSP RX drain (arch/aarch64/serial.rs, arch/aarch64/hsp_tegra.rs, xusb_tegra.rs:1936); the Pi's console is the BCM2711 PL011/mini-UART, a different driver entirely.
UNAOS_VIRT_EL0             NA    --evidence at orin 17's ccf93304: virt_el0 29 sites, 14 Pi-live, 11 NEG [implies: aarch64_el0]; a QEMU-virt-only board term implying nothing a Pi image wants, and the majority of its sites are `not(any(baremetal, tegra_el0, virt_el0))` exclusions — an exclusion term is not reach (the NEG verdict exists because this knob's first evidence run said 25 Pi-live)
UNAOS_VIRT_TICK   NA   --evidence (rmbp 18, 2026-09-10, at the hw-jetson fold): virt_tick 28 sites, 24 Pi-live, 4 NEG [implies: aarch64_el0, virt_el0]. It IMPLIES `virt_el0`, which is already NA one row above as a QEMU-virt-only board term — a knob cannot reach a Pi image with a term the image never carries. Same exclusion-term caveat as VIRT_EL0: most Pi-live hits are `any(feature = "tegra", feature = "virt_tick")` alternations in `arch/aarch64/exceptions.rs` where the Pi takes the `not(...)` arm.
UNAOS_NOBSPRUN    NA   --evidence: bsprun 10 sites, 9 Pi-live, 1 NEG — but reach is decided at the PRODUCER, not the consumer, and `grep -n 'UNAOS_NOBSP' unaos/arroyo` finds FOUR live reads, all Jetson-only: arroyo:880-881 are `[ -n "${UNAOS_TEGRA:-}" ]`-guarded, and arroyo:5252/5257 are inside `esp_jetson()` (the Jetson media builder). kernel8() enters NEITHER path and never puts `bsprun` in the feature list at all, so the knob cannot change a byte of the Pi image. Ruled NA by orin 25 (owner) 2026-09-10; the two esp_jetson sites are rmbp 18's addition to their reason, which said UNAOS_TEGRA-guarded ONLY — that is incomplete, and esp_jetson's own comment says why: it forces `tegra` WITHOUT setting the env var the mapping line keys on. `sched.rs:10465` is not a counter-example: it is a `compile_error!` rejecting bsprun-without-bsptick on every arch, which is why it carries no tegra term.
UNAOS_NOBSPTICK   NA   --evidence: same producer family as UNAOS_NOBSPRUN (the tool resolves both to `bsprun`: 10 sites, 9 Pi-live, 1 NEG). Same four Jetson-only reads, same command: `grep -n 'UNAOS_NOBSP' unaos/arroyo`. Ruled NA by orin 25 (owner) 2026-09-10, reason completed by rmbp 18 with the two `esp_jetson()` sites.
UNAOS_APSRUN      NA   --evidence: ONE live read, arroyo:1402, writing `_feats` (`apsrun,tegrasmp,tegra`). `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:6249, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block. So a knob that writes `_feats` cannot change a byte of a kernel8 image regardless of its guards. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). Ruled NA by orin 25 (owner) 2026-09-10.
UNAOS_NOAPSRUN    NA   --evidence: TWO live reads: arroyo:1403 is UNAOS_TEGRA-guarded and writes `_feats`; arroyo:5627 is inside `esp_jetson()` (opens 5608), the Jetson media builder — the same unguarded-inside-esp_jetson shape rmbp 18 caught me missing on UNAOS_NOBSPRUN. kernel8() enters neither. `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:6249, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block. So a knob that writes `_feats` cannot change a byte of a kernel8 image regardless of its guards. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). Ruled NA by orin 25 (owner) 2026-09-10.
UNAOS_NOUSBLUN    NA   --evidence, re-run at this commit and quoted verbatim: `nousblun: 12 site(s), 1 Pi-live, 11 negated`, the one PI-LIVE being `drivers/xhci/mod.rs:12169  #[cfg(feature = "nousblun")]` — the census-SKIPPED witness inside `bring_up_storage` (the line number is as of this commit and drifts; re-run `scripts/k8-reach.py --evidence UNAOS_NOUSBLUN` rather than trusting it). ONE live read in the producer, arroyo:1453, writing `_feats` (`nousblun`). `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:6608, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats); `grep -n 'UNAOS_NOUSBLUN' unaos/arroyo` (one non-comment line, :1453). SECOND REASON, independent of the first and the one that would still hold if the mapping block were ever wired into kernel8: all 12 cfg sites are in `drivers/xhci/mod.rs`, and `K8_FEATS` carries `skip_xhci` unconditionally — a Pi image built by `kernel8()` does not bring up xHCI at all unless the operator arms `UNAOS_PIUSB`, which extends K8_FEATS by its own arm and still never adds `nousblun`, so the PI-LIVE site is unreachable in an image whose feature list cannot contain the feature. Ruled NA by orin 27 (executor, USBLUN-M3) 2026-09-12; counts and PI-LIVE site corrected in M4 after the second addendum review (G3) found the row saying nine sites where the tool said twelve.
UNAOS_FACET       NA   --evidence: ONE live read, arroyo:1225, writing `_feats` (`facet`). `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:6249, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block. So a knob that writes `_feats` cannot change a byte of a kernel8 image regardless of its guards. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). Ruled NA by orin 25 (owner) 2026-09-10.
UNAOS_SNTP6       NA   --evidence, re-run at this commit and quoted verbatim: `sntp6: 7 site(s), 7 Pi-live, 0 negated` — and the honest reading is that the 7 split into two halves with different reasons, neither of which is reachable on a Pi image today. (1) THE CLIENT half is ONE site, `lib.rs:49`, whose cfg is `all(feature = "sntp6", feature = "net6", target_arch = "aarch64")` — so it inherits the UNAOS_NET6 row above verbatim: `net6::register_nic` has exactly two callers (`arch/aarch64/virtio_net.rs` under `vnet`, `arch/aarch64/rtl8168_tegra.rs` under `net4`), neither feature is in K8_FEATS, `genet.rs` registers no `NicOps`, and with no adapter `net6::ipv4()`/`net6::gateway()` both answer None, which is exactly the guard `net_sntp_client::service_tick()` returns on. The client would compile into a Pi image and make not one attempt. (2) THE MENU-BAR half is the other 6 sites (`video/menubar.rs` :827, :1311, :1735, :1740, :1758 and `selftest.rs`:457) and it is REAL Pi code, not a classifier artifact — `video::menubar` is compiled on the BCM2711 panel (`any(all(x86_64, wc), all(aarch64, desktop_firmware))`, PI-DESK). It is still unreachable from a `kernel8` image because `desktop_firmware` is not in K8_FEATS either (`UNAOS_PIDESK` is the pi lane's knob and its curation is the pi lane's call), and because `menubar::ENABLED` starts false on both arches. So arming UNAOS_SNTP6 on a Pi would add code and change no behaviour, which is the exact shape SR1 forbids advertising as reachable. WHAT WOULD CHANGE THIS ROW: `sntp6` in K8_FEATS beside `desktop_firmware` gives the Pi bar the `:: BARCLOCK:` witness over the clock PI-NET-16 already anchors there — a genuinely useful pi-lane fold and a pi-lane decision; the client half additionally needs the `genet` `NicOps` adapter the UNAOS_NET6 row above already names. Seeded 2026-09-13 by orin-0912b ORINTIME (orin-ledger A69).
UNAOS_NET6        NA   --evidence, re-run at this commit and quoted verbatim: `net6: 51 site(s), 43 Pi-live, 7 negated` — and the 43 are REAL, not a classifier artifact: `net6 = ["dep:smoltcp"]` (Cargo.toml) pulls NO board term, and its sites are in `syscall.rs` / `shell.rs` / `net_phy.rs`, every one of which a `kernel8` image does compile. So unlike UNAOS_NET5 (which at least stops at a `tegra`-gated entry point) this knob WOULD compile into a Pi image — the whole socket family, the three shell-verb bodies and the persistent stack — and REACH NOTHING, because the surface is driven entirely by a registered device adapter and the Pi has none. `net6::register_nic` has exactly TWO callers, `arch/aarch64/virtio_net.rs` (`#[cfg(feature = "vnet")]`, QEMU virt) and `arch/aarch64/rtl8168_tegra.rs` (`#[cfg(feature = "net4")]`, Tegra234 PCIe C5), and neither feature is in K8_FEATS; the Pi's NIC is the on-SoC BCM2711 GENET (`UNAOS_GENET`, already armed there) and `genet.rs` registers no `NicOps`. With no adapter, `net6::init()` prints `stack NOT up — no NIC adapter registered` and every verb and every one of the seven syscalls fails closed. Arming it on a Pi would therefore add code and change no behaviour, which is the exact shape SR1 forbids advertising as reachable. WHAT WOULD CHANGE THIS ROW: a `genet` device adapter (four function pointers, the `virtio_net.rs` shape) plus `net6` in K8_FEATS — a pi-lane fold, not a jetson one, and the reason the surface was written adapter-shaped in the first place.
UNAOS_FTDIRX      NA   --evidence, re-run at this commit and quoted verbatim: `ftdirx: 7 site(s), 7 Pi-live, 0 negated` — one in `drivers/xhci/ftdi.rs` (the tail `ftdirx` module) and six in `drivers/xhci/mod.rs` (the completion claim at :4368, three teardown drops at :6278/:6486/:14658, the service call at :13362, and the tail `impl XhciController` at :15866; line numbers drift — re-run `scripts/k8-reach.py --evidence UNAOS_FTDIRX` rather than trusting them). ONE live read in the producer, arroyo, writing `_feats` (`ftdirx`). TWO INDEPENDENT REASONS, the same pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats); `grep -n 'UNAOS_FTDIRX' unaos/arroyo` (one non-comment line). SECOND, and it would still hold if the mapping block were ever wired into kernel8: every site is in the xHCI driver, `K8_FEATS` carries `skip_xhci` unconditionally, and even `UNAOS_PIUSB` (which extends K8_FEATS by its own arm) never adds `ftdirx` — so the PI-LIVE sites are unreachable in an image whose feature list cannot contain the feature. THIRD, the physical one: the code drives an FTDI FT232 USB-serial cable, which is the 2012 rMBP's ONLY console because that board has no 16550; a Pi has a PL011 on its header and no FTDI to enumerate, so there is nothing for the knob to arm there even with xHCI up. Ruled NA by the rmbp FTDIRX executor (rmbp-ledger A9) 2026-09-15.
UNAOS_KEPLER_USERD_SNOOP  NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-userdsnoop: 2 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler]` — both in `drivers/gpu/kepler.rs` (the pre-init arm and its witness-conditional restore); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_USERD_SNOOP` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-userdsnoop`). THREE INDEPENDENT REASONS, the first two being the pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:7111, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes either file, which is why the tool reports 0 Pi-live. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — an MMIO experiment against NVIDIA PFIFO/PDISPLAY registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp SHUTRESTORE executor (rmbp-queue GPU LADDERS, R19 restoration) 2026-09-15.
UNAOS_KEPLER_PFIFO_FLUSH  NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-pfifoflush: 1 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler]` — in `drivers/gpu/kepler.rs` (the 0x70000 trigger + bounded BUSY poll); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_PFIFO_FLUSH` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-pfifoflush`). THREE INDEPENDENT REASONS, the first two being the pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:7111, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes either file, which is why the tool reports 0 Pi-live. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — an MMIO experiment against NVIDIA PFIFO/PDISPLAY registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp SHUTRESTORE executor (rmbp-queue GPU LADDERS, R19 restoration) 2026-09-15.
UNAOS_KEPLER_CTRL_ADDR    NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-ctrladdr: 1 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler]` — in `drivers/gpu/kepler.rs` (the 3 PBDMA x 4 TARGET audit); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_CTRL_ADDR` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-ctrladdr`). THREE INDEPENDENT REASONS, the first two being the pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:7111, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes either file, which is why the tool reports 0 Pi-live. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — an MMIO experiment against NVIDIA PFIFO/PDISPLAY registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp SHUTRESTORE executor (rmbp-queue GPU LADDERS, R19 restoration) 2026-09-15.
UNAOS_KEPLER_REPOINT      NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-repoint: 2 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler]` — both in `drivers/gpu/kepler_display.rs` (the call inside `takeover_display` and the `repoint_surface` definition); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_REPOINT` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-repoint`). THREE INDEPENDENT REASONS, the first two being the pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:7111, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes either file, which is why the tool reports 0 Pi-live. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — an MMIO experiment against NVIDIA PFIFO/PDISPLAY registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp SHUTRESTORE executor (rmbp-queue GPU LADDERS, R19 restoration) 2026-09-15.
UNAOS_KEPLER_LATCH_ARM    NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-latcharm: 2 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler]` — both in `drivers/gpu/kepler_display.rs` (the call and the `latch_arm_update` definition); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_LATCH_ARM` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-latcharm`). THREE INDEPENDENT REASONS, the first two being the pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:7111, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes either file, which is why the tool reports 0 Pi-live. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — an MMIO experiment against NVIDIA PFIFO/PDISPLAY registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp SHUTRESTORE executor (rmbp-queue GPU LADDERS, R19 restoration) 2026-09-15.
UNAOS_KEPLER_VBLANK       NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-vblank: 7 site(s), 0 Pi-live, 0 negated  [implies: beam, jd1dc, nvidia-kepler]` — four in `drivers/gpu/` (the `pub mod kepler_vblank` declaration, the PMC statement in kepler.rs, and beam_probe's arm + scanout_beam's edge feed in kepler_display.rs) and THREE IN `video/beam.rs`, which is the case this file's header warns about and the reason this row spells it out rather than saying "x86 only". `video/beam.rs` IS lexed on aarch64 — it is the arch-neutral MECHANISM, declared `pub mod beam` with no gate — so a feature-only cfg there would have been a genuine Pi-live site. All three carry `target_arch = "x86_64"` IN THEIR OWN ATTRIBUTE (the AHCI/HDA hook shape): the `VB_WIDE_DENOM` const, the rung-2 wait arm inside `hold`, and the `witness`-gated fixture call. The tool classifies all three X86 for that reason, and a Pi compile emits nothing for any of them. Line numbers drift; re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_VBLANK` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-vblank`). THE SAME THREE INDEPENDENT REASONS the five UNAOS_KEPLER_* rows above establish. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares the kepler modules under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes them. It also implies `beam`, whose own NA row records that `arch::scanout_beam()` is the constant `None` on the Pi (the BCM2711 HVS publishes no raster-line register this kernel reads), so even the arch-neutral consumer folds to a no-op there. THIRD, the physical one: the subject is the GK107's PDISPLAY `HEAD_STAT.VERT` vblank counter and the PMC interrupt-source word of a discrete PCIe NVIDIA part. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the KVBLANK executor (rmbp-ledger B145, rmbp-queue GPU LADDERS) 2026-09-22.
UNAOS_KEPLER_PITCH_LADDER NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-pitchladder: 2 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler]` — both in `drivers/gpu/kepler_display.rs` (the call and the `pitch_ladders` definition); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_PITCH_LADDER` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-pitchladder`). THREE INDEPENDENT REASONS, the first two being the pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:7111, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes either file, which is why the tool reports 0 Pi-live. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — an MMIO experiment against NVIDIA PFIFO/PDISPLAY registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp SHUTRESTORE executor (rmbp-queue GPU LADDERS, R19 restoration) 2026-09-15.
UNAOS_KEPLER_GOP_OVERLAP  NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-gopoverlap: 2 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler]` — both in `drivers/gpu/kepler_display.rs` (the call beside the `fb-draw cover=` line and the `gop_overlap_probe` definition); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_GOP_OVERLAP` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-gopoverlap`). THREE INDEPENDENT REASONS, the first two being the pair UNAOS_NOUSBLUN's row establishes for this exact file. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"` at arroyo:7111, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies `nvidia-kepler` (Cargo.toml), `nvidia-kepler` is in no K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes either file, which is why the tool reports 0 Pi-live. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — an MMIO experiment against NVIDIA PFIFO/PDISPLAY registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp SHUTRESTORE executor (rmbp-queue GPU LADDERS, R19 restoration) 2026-09-15.
UNAOS_KEPLER_KFBIND       NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-kfbind: 3 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler, nvidia-kepler-fifo]` — the `pub mod kepler_fifo` declaration in `drivers/gpu/mod.rs` and the rung's TWO call sites in `drivers/gpu/kepler.rs` (phase A immediately pre-submit, phase B immediately below the DISCRIMINATOR loop); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_KFBIND` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-kfbind`). THREE INDEPENDENT REASONS, the same three the seven SHUTRESTORE rows above establish for this same file family, with the SECOND strengthened by this feature's extra implication. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies BOTH `nvidia-kepler` and `nvidia-kepler-fifo` (Cargo.toml), NEITHER is in any K8_FEATS arm, and the module is declared `#[cfg(feature = "nvidia-kepler-kfbind")] pub mod kepler_fifo;` in `drivers/gpu/mod.rs` beside `kepler`/`kepler_display`/`kepler_ce`, which are all `nvidia-kepler`-gated — a Pi image never lexes any of them, which is why the tool reports 0 Pi-live. The two call sites are additionally inside `kepler::init`'s `nvidia-kepler-fifo` leg, so even a hypothetical Pi build carrying the module could not reach them. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — a PTOP device-info sweep and PBDMA PRI reads against NVIDIA host-engine registers on a discrete PCIe GPU, plus a BAR1 read of that GPU's channel control area. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp KFBIND executor (rmbp-queue GPU LADDERS; shut-out register §2 rung KF27) 2026-09-15.
UNAOS_KEPLER_KFCTXBIND    NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-kfctxbind: 4 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler, nvidia-kepler-fifo]` — the `pub mod kepler_fifo` declaration in `drivers/gpu/mod.rs` (whose gate this rung widened to `all(nvidia-kepler, nvidia-kepler-fifo, any(nvidia-kepler-kfbind, nvidia-kepler-kfctxbind))` — the `any` because KF28 deliberately does NOT imply KF27's feature, and the two parents spelled out because GATE-FC2 takes no required atom from an `any(...)` and convicted the short form as a declaration wider than its four call sites (`decl=target_arch = "x86_64" ... all-under=nvidia-kepler + nvidia-kepler-fifo -> FINDING`); both features imply both parents, so the `all(...)` admits exactly the same configurations — KEPLER-METAL-LOG.md's KF28 entry forbids flying the two rungs on the same boot, so the dependency is a RUNTIME gate and never a Cargo one), the `pub mod ctxbind` declaration inside `drivers/gpu/kepler_fifo.rs`, and the rung's TWO call sites in `drivers/gpu/kepler.rs` (phase A below KF18's CHAN_CUR/CHAN_NEXT bind and strictly pre-submit, phase B below KF27's phase B); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_KFCTXBIND` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-kfctxbind`). THREE INDEPENDENT REASONS, the same three the KFBIND row directly above establishes for this same file, with the SECOND adjusted for this feature's own implication set. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies BOTH `nvidia-kepler` and `nvidia-kepler-fifo` (Cargo.toml), NEITHER is in any K8_FEATS arm, and the module it lives in is declared beside `kepler`/`kepler_display`/`kepler_ce` in `drivers/gpu/mod.rs`, which are all `nvidia-kepler`-gated — a Pi image never lexes any of them, which is why the tool reports 0 Pi-live. Both call sites are additionally inside `kepler::init`'s `nvidia-kepler-fifo` leg, so even a hypothetical Pi build carrying the module could not reach them. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — a FECS falcon census, a PTOP/PBDMA PRI sweep, a PFIFO channel-table read and a BAR1 read of that GPU's instance block and channel control area, all against NVIDIA host-engine registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp KFCTXBIND executor (rmbp-queue GPU LADDERS row KFBIND-CTXBIND; shut-out register §2 rung KF28) 2026-09-15.
UNAOS_KEPLER_KDHEAD       NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-kdhead: 21 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler, nvidia-kepler-takeover]` — every one of the twenty-one is in `drivers/gpu/kepler_display.rs`: the ONE call site folded onto the existing `panel_console_resume` statement line beside BEAMX86's, the single cfg'd census-publication statement inside `beam_probe`, and nineteen items in the rung's own tail block (its constants, statics, `KdheadBlock`/`KdheadGeom`, `kdhead_publish_census`, both `kdhead_bracket` arms, `kdhead_answered`, `kdhead_decode`, `kdhead_probe` and three `const _` address asserts); line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_KDHEAD` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-kdhead`). THREE INDEPENDENT REASONS, the same three the seven SHUTRESTORE rows and the two KF rows above establish for this same file family, with the SECOND strengthened by this feature's extra implication. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies BOTH `nvidia-kepler` and `nvidia-kepler-takeover` (Cargo.toml), NEITHER is in any K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes the file, which is why the tool reports 0 Pi-live. The call site is additionally inside `kepler_display::takeover_display`, below that function's trace-only early return, so even a hypothetical Pi build carrying the module could not reach it. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — read-only MMIO reads of NVIDIA PDISPLAY `HEAD_STAT`, the EVO core-channel state and the 0x640000 method mirror on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp KDHEAD executor (rmbp-queue GPU LADDERS row KDHEAD; shut-out register §1 rung KD14) 2026-09-15.
UNAOS_KEPLER_CTRLBIND     NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-ctrlbind: 6 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler, nvidia-kepler-ctrladdr]` — every one of the six is in `drivers/gpu/kepler.rs`, all inside `kepler::init`: the entry capture of PFIFO_CHAN[1] and its `ctrlbind begin` print, the two `skipped reason=` arms of the KF9 audit loop (`ctrladdr-absent`, `ctrladdr-readonly`), the per-TARGET rung block itself, and the `chan-restored` unwind below the loop; line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_CTRLBIND` rather than trusting them. The SEAM the rung calls, `kepler::try_bind_and_witness`, is deliberately NOT a site: it is ungated, `init` calls it on every kepler boot, and it is the block this rung was carved out of — so the feature adds call sites, never mechanism. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-ctrlbind`). THREE INDEPENDENT REASONS, the same three the seven SHUTRESTORE rows and the three KF/KD rows above establish for this same file, with the SECOND strengthened by this feature's extra implication. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies BOTH `nvidia-kepler` and `nvidia-kepler-ctrladdr` (Cargo.toml), NEITHER is in any K8_FEATS arm, and `drivers/gpu/mod.rs` declares `kepler`/`kepler_display` under `#[cfg(feature = "nvidia-kepler")]` — a Pi image never lexes the file, which is why the tool reports 0 Pi-live. All six sites are additionally inside `kepler::init`'s `cfg!(feature = "nvidia-kepler-fifo")` leg and inside the `nvidia-kepler-ctrladdr` audit's own region, so even a hypothetical Pi build carrying the module could not reach them. THIRD, the physical one: this is a probe rung on the 2012 rMBP's GK107 (GeForce GT 650M) — it writes NVIDIA PBDMA CTRL_ADDR TARGET encodings and re-runs a PFIFO channel bind (PFIFO_CHAN[1] +0x00/+0x04) with PFIFO error and scheduler status read back, all against host-engine registers on a discrete PCIe GPU. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is nothing for the knob to arm there even if both reasons above were lifted. Ruled NA by the rmbp CTRLBIND executor (rmbp-queue GPU LADDERS row SHUTRESTORE-CTRLADDR-BRINGUP; shut-out register §2 rung KF9b) 2026-09-17.
UNAOS_KEPLER_KFUNWEDGE    NA   --evidence, re-run at this commit and quoted verbatim: `nvidia-kepler-kfunwedge: 4 site(s), 0 Pi-live, 0 negated  [implies: nvidia-kepler, nvidia-kepler-fifo]` — the `pub mod unwedge` declaration inside `drivers/gpu/kepler_fifo.rs`, the rung's ONE call site in `drivers/gpu/kepler.rs` (immediately above the TERMINAL POKE block, which is the §5.4 ordering rule taken literally: this rung's stimulus IS the poison offset, so it fires after every proven read of the boot has completed), the `fecs_poison_ledger` accessor at the tail of the same file, and the `pub mod kepler_fifo` declaration in `drivers/gpu/mod.rs` — whose gate this rung widened a second time, to `all(nvidia-kepler, nvidia-kepler-fifo, any(nvidia-kepler-kfbind, nvidia-kepler-kfctxbind, nvidia-kepler-kfunwedge))`. The third `any` atom is there because KFUNWEDGE deliberately implies NEITHER sibling: it is a SACRIFICIAL rung that fires the `0x409504` poison on purpose, so a boot carrying KF27 or KF28 with it would collect their verdicts over a wedged unit, and its dependency on KF27's base ladder is a RUNTIME gate (the ladder is re-run read-only) exactly as KF28's is. Omitting the atom makes `UNAOS_KEPLER_KFUNWEDGE=1` alone the same `E0433` the KFCTXBIND row above measured; the two parents stay spelled out because GATE-FC2 takes no required atom from an `any(...)` and all three features imply both, so the `all(...)` admits exactly the configurations the bare `any(...)` would. Line numbers drift, so re-run `scripts/k8-reach.py --evidence UNAOS_KEPLER_KFUNWEDGE` rather than trusting them. ONE live read in the producer, arroyo, writing `_feats` (`nvidia-kepler-kfunwedge`). THREE INDEPENDENT REASONS, the same three the KFBIND and KFCTXBIND rows directly above establish for this same file, with the second adjusted for this feature's own implication set. FIRST: `kernel8()` builds its OWN curated feature list — `local K8_FEATS="baremetal,skip_xhci"`, extended only by its own explicit arms — and NEVER reads the `_feats` knob-mapping block, so a knob that writes `_feats` cannot change a byte of a kernel8 image. Command: `grep -n 'K8_FEATS=' unaos/arroyo` (every assignment is inside kernel8(), none from _feats). SECOND, and it would still hold if the mapping block were ever wired into kernel8: the feature implies BOTH `nvidia-kepler` and `nvidia-kepler-fifo` (Cargo.toml), NEITHER is in any K8_FEATS arm, and the module it lives in is declared beside `kepler`/`kepler_display`/`kepler_ce` in `drivers/gpu/mod.rs`, which are all `nvidia-kepler`-gated — a Pi image never lexes any of them, which is why the tool reports 0 Pi-live. The call site is additionally inside `kepler::init`'s `nvidia-kepler-fifo` leg, so even a hypothetical Pi build carrying the module could not reach it. THIRD, the physical one, and for this rung it is the loudest of the three: the stimulus is a host MMIO read of `0x409504` — `WRCMD_CMD` in the FECS falcon of the 2012 rMBP's GK107 (GeForce GT 650M) — followed by a W1C of the NVIDIA PRI-ring fault latches at `0x1100` and `0x120128`. A Pi 4 has a BCM2711 with a VideoCore VI and no NVIDIA part on any bus, so there is no falcon to wedge, no PRI ring to clear and nothing whatever for the knob to arm there even if both reasons above were lifted — and a K8_FEATS arm would be an arm for a SACRIFICIAL experiment on hardware that cannot perform it. Ruled NA by the rmbp KFUNWEDGE executor (rmbp-queue GPU LADDERS row KFUNWEDGE; shut-out register §2 rung KF29) 2026-09-15.
UNAOS_UVC                 NA   --evidence, run at this commit and quoted verbatim: `uvc: 2 site(s), 0 Pi-live, 0 negated  [implies: ehciconfig, ehcihid, ehciscout]` / `X86  drivers/ehci/mod.rs:4588  #[cfg(feature = "uvc")] { let _b = self.data_buf; crate::drivers::uvc::probe(...` / `X86  drivers/mod.rs:76  #[cfg(all(target_arch = "x86_64", feature = "uvc"))]`. TWO sites and both are x86 by construction, which is the strongest shape this file has (the `UNAOS_HDA` row's shape, one tier tighter). Site 2 is the `pub mod uvc` DECLARATION, whose conjunction names `target_arch = "x86_64"` explicitly, so an aarch64 image never lexes drivers/uvc.rs at all and no literal, constant or `panic::Location` of it can reach kernel8.img. Site 1 is the only call, folded into `configure_hid` — a method of `drivers/ehci/mod.rs`, whose own declaration is `#[cfg(all(target_arch = "x86_64", feature = "ehcihid"))] pub mod ehci;` (drivers/mod.rs) — so the bare `feature = "uvc"` there is x86-gated by the module tree above it, which is why the classifier reads it X86 rather than Pi-live. The implication set the evidence prints is Cargo's `uvc = ["ehcihid"]` plus the pre-existing `ehcihid`/`ehciscout`/`ehciconfig` family, every member of which this same file already classes x86-only. SUBSTANTIVE, not bookkeeping: the driver reads USB Video Class descriptors off a host controller the Pi bare-metal image has no driver for, so a K8_FEATS arm for it would arm nothing and say so to nobody. If a Pi USB host controller ever grows a UVC call site this row is wrong and must be re-derived, not edited — the file is arch-neutral in shape (a pure descriptor parser plus a transfer closure), which is exactly the condition that would make that happen. Added 2026-09-22 by CAMERA1 (rmbp-ledger B143).
