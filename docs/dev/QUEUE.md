# QUEUE — the trunk queue: every job that does not need a specific board (R45)

One file, on `main`, kept current AS WORK HAPPENS, never at close. A job lives here if it can be
gated on QEMU or by compile alone; a job lives in a track queue (`docs/dev/OS/<track>-queue.md`)
only if it needs that board's metal or that board's own files. A job that has both halves is CUT:
the shared half is a row here, the metal half is a row there, each citing the other (R45: "isolate
down to the specific metal requirement as far as possible so as to maximize shared code").
Every row cites its ledger id — the ledger holds the finding, this file holds the ORDER. `✓` =
verified in this tree this session · `·` = inherited, not re-checked. Re-derive every sha.
Created 2026-09-12T00:3xZ (UTC; 2026-09-11 local) by orin 27 from the union of the four branches' ledgers (80 non-landed shared rows,
27 open orin rows, 93 open rmbp A/B rows, 8 open pi rows — counted by awk over the status column of each
branch's file at its tip that hour; the rmbp figure was first written as 88 from a narrower filter and
corrected by the landing review — the commands live in each track queue). rmbp 19's verdict stands: "88 open rows is a graveyard, not a queue" — so this
file lists JOBS, ranked, and a ledger row that is a record rather than a job stays in the ledger.

## STATE — 2026-09-12T03:5xZ (orin 27)
✓ main 0aa3afe6 = the jetson landing (parents d28e701e + 6ed2be1a; three review panels; landing legs on 5acf6d16: check tegra/plain strict rc=0, UNAOS_WC=1 test rc=0, test-arm rc=0; ledger-check rc=0 on the result; `UNAOS_LEDGER_STRICT=1 UNAOS_K8REACH_STRICT=1 ./arroyo check` on main 0aa3afe6 rc=0). Before it: rmbp 267bd49d and pi e10d9e4e landings.
✓ ALL THREE TRACKS LEVEL WITH MAIN after this commit (hw-jetson, hw-pi4, hw-rmbp fast-forwarded). R45's property holds: any track can merge to main at any time.
✓ Push line (Peter): `git push origin main hw-jetson hw-pi4 hw-rmbp exec-orin26-unafsroot exec-orin26-tear exec-orin27-ga10b4c exec-orin27-cmd8 exec-orin27-usblun exec-orin27-closefold exec-orin24-fold exec-orin25-shotverb`
✓ Orin bench: render13 (`render13-20260912T0259Z-f4b4bf3`, KELF max=0x34f820) written to the old 32 GB card; the UEFI does not list our MBR card in the native slot (GPT card image is the job, orin-queue) so it flies from the USB reader; waker armed on the loader line.

✓ 2026-09-24 LATER (cloud session): + B103, WCLANE, B96, B94 (GATE-LINENEUTRAL), B101, B64 (K8REACH's option_env! half), B208 (PIDESKCOND), B209 (QUIETKILL), SO22, the DRAGSTALL rule; strict check on the tip 86 legs OK, k8 reachability green, ledger 242, container red only.
✓ 2026-09-24 LATE (cloud session): after the sync the branch also carries SO54, SO55, **SO56 USBNET**, SMALLFIX (B70/B74/S32), SO22, HDASIE (B207) and the queue hygiene rows below; strict check on 9d7ffd3e 86 legs OK, ledger 242, container reds only. The level lines in the platform queues' TRACKSYNC blocks still apply (every track tip is an ancestor).
✓ TRACKSYNC 2026-09-24: hw-rmbp + hw-pi4 + hw-jetson are one tree at `claude/optimistic-ramanujan-r3qyu5` (merge 2dda36e8; main 131d8958 is an ancestor). R45's property holds again once Peter levels the three tracks with the `--ff-only` lines in each platform queue's TRACKSYNC block; the land into main is PLAYBOOK §7 (panel, `--no-ff`) when he says. Each platform's own boot 13 is written at the head of its queue.
· 2026-09-24 (cloud session, focus rmbp) SHARED FILES TOUCHED ON `claude/optimistic-ramanujan-r3qyu5` (rmbp-ledger B198, LOGIN14): `fs/users.rs` (an unset-credential row state, root's row, `set_password`, `passwd`), `video/login.rs` (the set-password form), `shell.rs` + `midden_core` (`passwd`), `crates/kernel/Cargo.toml` + `Cargo.lock` (`x86_64` 0.15.5 — 0.15.4 does not compile on nightlies from 2026-08-31 on). Every board with `login` gets root's set-password alert at its first boot-to-root; the aarch64 `virt` lane prints `:: LOGIN-ROOTPW: -> SKIP` where no screen is built. Owed to pi/jetson: their own `test-arm`/`kernel8-test` reading of the `[login] root password …` lines.
· 2026-09-24 (same branch) TOOLCHAIN, every board: `crates/bootloader/src/main.rs` defines `wcslen` (rmbp-ledger B205) — nightlies from 2026-08-31 emit a call to it from the `uefi` crate's UTF-16 length loops and the UEFI targets have no libc; `x86_64` 0.15.4 does not compile on those nightlies and 0.15.5 does not compile before 2026-07 (the rmbp-queue STATE has the measured window). Any box that runs `rustup update` needs both; the jetson's `esp-jetson` builds the aarch64-unknown-uefi bootloader from the same file.
## 1. THE DESKTOP — glass defects Peter has seen, all in shared `video/` (fix once, every board)
· ✓ RESCUED  DESKRESCUE  (hw-rmbp, 2026-09-22, branch `exec-rmbp-deskrescue` off `2fc9c71c`) — the six shared desktop fixes BRANCHCENSUS found stranded on no track are folded, in the order the rescue rows required (SCRSHOT after LOGINBOOT, R54). Seven commits, each `git cherry-pick -x` so the original author, message and Co-Authored-By stand, with conflict resolutions appended to the bodies: **BOOTFOCUS `39f3c967`** (← `fdcba15d`; LEDGER SO14 ticked open -> fixed-unflown, one field) · **LOGINBOOT `b7d3b990`** (← `f14f6505`) · **SCRSHOT `246ac847`** (← `c8d66657`; ids A72-A74 -> A85-A87) · **SESSGATE `edc39e77`** (← `9f365744`; ids A72-A74 -> A89-A91, and SO36's own cross-ref `ledger A72` -> `A89` with them) · **PTRLOST `3504fd33` + `5aba6bdf`** (← `f58eeed3` + `26646c22`; ids A74/A75 as that commit's own message sets them) · **BLITSTALL `68c09141`** (← `4a4a55b1`). NO STOP — all six arcs landed. GATES: `./arroyo check` 157 ✅ legs, both arches, on every commit; the ONLY findings are `stale queue-citation registration` lines, which are the gate NOTICING the rescued rows now exist — 11 are ids this series resolves (SO43 SO44 SO46 A74 A75 A85-A87 A89-A91) and 13 more came from other seats landing on hw-jetson and hw-rmbp DURING this session. ⚠ THE PRUNE OF `QUEUECITE_REG` (`unaos/scripts/ledger-check.sh:641-648`) IS THE SEAT'S AT THE FOLD — `ledger-check.sh` is not an executor's file. END RUNS: `UNAOS_WC=1 UNAOS_QUARRY=1 UNAOS_QEMU_FULL=1 ./arroyo test 150` rc=0, MBENCH PASS 6/6 required witnesses, 0 forbidden, reached its last fixture at serial.log:2379 (NOT a wall truncation); `./arroyo test-arm` rc=0, complete. The rescued instruments fire on the x86 wire: `:: PTRLOST: window=1`, `[ptrown] … paint=comp`, `:: BLITWIRE: loop_us=378852 bytes=12214800 raw_bus=32`. KNOB-OFF: BOOTFOCUS `deskcascade` 0 · LOGINBOOT `login` 0 · SESSGATE `login` 0 · PTRLOST `wc` 0 · BLITSTALL `wc` 0 — all byte-identical both arches, controls fired. ⚠ **SCRSHOT `login` EXIT 1 (MOVED), BOTH ARCHES, AND IT IS THE ARC'S POINT, NOT A DEFECT**: the knob-off image SHRINKS (x86 1600824 -> 1585456, arm 1618440 -> 1610932) because R54 makes a capture with no session a one-line refusal and `fs/users.rs` is `#[cfg(feature = "login")]`, so the destination lookup, the 8.3 mapping and the encode become unreachable. `login` is NOT a default feature, so **on every board today Print Screen now refuses instead of writing a file** (`:: PRTSCR: no user session … NOTHING WRITTEN`) — the rescue row warned of exactly this. It stays a live regression until the login screen is armed by default; that switch is not this series' to throw. ⚠ TWO REAL LEDGER DEFECTS FIXED IN BLITSTALL's own SO46 row, both predating the arms that catch them (LEDGERGATES landed 2026-09-15, the commit is 2026-09-13): status `diagnosed-unflown` is not in the enum -> `open — diagnosed-unflown: …`, and `bytes_pp=3053700` is a BYTE COUNT the sha scan read as a commit -> the field is lifted out of the backticks in both wire quotes, every number unchanged. The gate arm is blunt: ANY 7-or-8-digit decimal in a quoted wire line reads as a sha. ⚠ `docs/dev/exec-branches.txt` IS NOT TOUCHED and the six refs stay LOST, by that file's own contract: FOLDED-BY-HAND's field 4 "must resolve here AND be an ancestor of a track", and an executor branch is not one until it lands. The file also does not exist at `2fc9c71c` (it was added to hw-rmbp later), so creating it here would fabricate a whole-file add that conflicts with the real registry. THE SEAT SETS, AT THE FOLD, eleven lines to `FOLDED-BY-HAND <fold-sha> 2026-09-22`: `refs/heads/exec-orin17-bootfocus` (no origin line exists) and `refs/heads/` + `refs/remotes/origin/` for each of `exec-orin-loginboot`, `exec-orin-scrshot`, `exec-orin-sessgate`, `exec-orin-ptrlost`, `exec-orin-blitstall`. ⚠ ONE STALE COMMENT LEFT FOR THE SEAT: hw-rmbp's LOCKFIX-B1 doc at the head of the x86 `syscall.rs` tail block still says "Nothing follows this function"; SESSGATE's fixture now does. Evidence: the commit bodies; build logs at `~/unaos-bench/scratch/rmbp-0915/deskrescue-logs/`.
· NEW  APPMENU  **an app cannot publish a menu on ANY board — the ABI has no menu verb** (MENUDROP, hw-rmbp 2026-09-17: `crates/una-abi/src/lib.rs` ends at `SYS_CPUPULSE = 49`; `BUS_VERB_MENU_PUBLISH` is only the design ledger at `video/menubar.rs:1611`). The `View` menu Peter knows from Pulse is the KERNEL pulse window's (`video/pulsewin.rs:172-183`, published `:526`), every runtime call site `desktop_firmware`-gated (`UNAOS_PIDESK`/`UNAOS_TEGRADESK`), so on x86 the app's title shows and no View follows. Two arcs, Peter's call: (a) a menu verb in una-abi + the bar's per-window menu path taking it (apps own their menus, every board); (b) the kernel pulse window on x86 (cosmetic, board-split). One line each in x86-witness.spec once flown.
· NEW 2026-09-12 (render13 on the Orin, Peter on the glass — shared code, every board): (a) launched programs are titled "Application" in the taskbar and menubar — `[winmenu] app-menu owner=N name=Application kind=default` for every QUARRY-LAUNCH'd ELF; only Shell is `from=declared`. R36: declared name, else the PROGRAM name; the launch path knows the ELF path and the app_name registry (wintitle) is not fed for EL0 launches. (b) R49: the console and the shell are APPS with pinned taskbar tiles — quit on close, relaunch from the tile through the same path any app takes; retire the console "route" (`[dock] console-reopen … route=declined -> DECLINE`) and the shell re-mint (`[realdesk] shell-remint … MINTED`). R50: Quarry is the Finder, always there, permanent tile (already in quarry.md since the day it was named). (c) after the shell re-mint the POINTER VANISHED — `[comp2] … sprite_us=1`, the sprite is no longer composed; goes with (b). (d) mousing/dragging not smooth: `[comp2] rollup pass_us≈2000 max_us=375000..588000` — half-second compositor passes; correlate the spike passes with `[wc-w]`, `[pstrip] gapmax`, `[wc-h] longpres`, the cross-core compositor gate (A46) before touching anything. Branches cut on hw-jetson at 2f6adcc4 for (a) exec-orin27-appname, (b)+(c) exec-orin27-conreopen, (d) exec-orin27-dragstall — briefs are the lines above plus the wire at ~/unaos-bench/scratch/orin27/render13-wire-partial.txt.
✓ DONE 2026-09-23 (e) and (f) (WCDMEM, branch `exec-rmbp-wcdmem` off `1ba7f63b`; the branch tip is the sha — the seat fills it at the fold; rmbp-ledger B199). Both halves were ALREADY FIXED on hw-rmbp and hw-jetson when this row was briefed, and neither landing ticked it: (e) by WCDFLOOD `0fc32a4c` (LEDGER SO30 — the SKIP latched per window id in `wcd_oom_say`, every skip counted into `[comp2] wcd_skips=`, fixture `:: WCDLATCH:`), (f) by SERDRAIN `9c0263a0` (LEDGER SO31 part 3 — `serial_ring::power_drain` before every PSCI SMC in `power.rs`, reboot, shutdown and both crystal verbs; the x86 S5 port by S5DRAIN, SO39). The aarch64 witness is `[pwrshutoff] ring drained lines=<n> bytes=<n>`, printed by the SAME function that prints x86's `… mirror=ok`. The shapes differ by one field, because `mirror=` reports the x86 FTDI mirror sink and aarch64's `_print` has only the UART (`serial_ring.rs` `power_drain`). WCDMEM adds the per-window half of (e)'s rollup, which `wcd_skips=` (a span total over all windows) could not give: `[wc-d] skip-rollup win=<id> reason=no-memory passes=<n> since_ms=<t>`, printed from `comp2_emit` after each `[comp2]` line and only for a window whose count moved. Its fixture arm follows WCDLATCH: `[wc-d] latch-check forced=16 printed=1 rolled=16 win=30 reroll=0 -> PASS`, REQUIREd in `x86-wc.spec`. The Orin wire has not scored either half: no aarch64 QEMU verb composites, and no aarch64 QEMU lane exits through SYSTEM_OFF (`test-arm` runs `-no-shutdown`). Was: · NEW 2026-09-12T15:1xZ (render13 boot 1 wire, orin-ledger A55/A54 context; shared `video/wm.rs` and `power.rs`): (e) `[wc-d] verify win=1 -> SKIP (no memory for WxH source snapshot)` (wm.rs:7877) fires once per compositor pass while a window whose snapshot cannot be allocated exists — 46,161 of 67,185 lines on one boot, and the serial staging ring dropped 5,331 lines in 192 `[serial] dropped` events under it. A per-pass witness must latch (print once, then count into a rollup). (f) the shutdown path (`power.rs` `platform_shutdown` / `crystal_shutdown` → `psci_call`) prints through the same staging ring and drains nothing before the SMC: on a flooded ring the last lines before SYSTEM_OFF are lost with the power. Drain the ring (bounded wait) before the SMC on every power verb. (g) ✓ DONE 2026-09-15 (STALEBANNER, branch `exec-rmbp-smallfix2` off `94b137a8`) — the banner was stale on the ORIN and accidentally RIGHT on the Pi, and one literal now covers both boards. `[pidesk]` (`video/desktop_firmware.rs:356`) and `[orinfurn]` (`main.rs:8562` — not `:8547`, the citation had drifted 15 lines) both print `Restart and Shut Down psci_wired={} — where a secure monitor answers the smc they ACT, crystal::fire -> power::crystal_restart / power::crystal_shutdown = PSCI SYSTEM_RESET / SYSTEM_OFF, and where none does they print their honest unimplemented lines`, with Sleep named as the ONE remaining stub (no S3 suspend path). The field is `!cfg!(feature = "pi")` and not a flat “both are wired” sentence, because `Verb::real` (`crystal.rs:135`) is arch-TRUE and the two boards genuinely differ: the Orin's ATF/BL31 answers the smc so `crystal.rs:675`/`:693` ACT, while the Pi runs bare-metal at EL2 with nothing behind its `smc` and keeps the honest stubs `crystal.rs` spells out — the flat literal would have replaced a stale line with a FALSE one. Both edits are line-neutral (1/1 and 2/2). ⚠ NO RUNTIME WITNESS FROM THIS SEAT, measured not assumed: the literal is aarch64-only (`video/mod.rs:657` gates the module on `all(aarch64, desktop_firmware)`; the `[orinfurn]` block is `orinfurn`-gated), so it reaches no x86 wire and no x86 ELF — `LC_ALL=C grep -a -o -F 'no PSCI wiring' target/x86_64-unaos/release/unaos-kernel` was **0 BEFORE the change as well**, with the control `:: XHCIHUB: hub slot=` at 1, so the x86-artifact proof this row was briefed with could never have fired. Type-checked by `check`'s `arm-pi`, `arm-tegra-desk` and `arm-tegra-seam` legs and certified in an aarch64 ELF by grep; the WIRE proof is owed to `kernel8-test` (pi) and an Orin boot, neither of which is rmbp's to run (R39).
· ✓ FLOWN PASS  SO3  no application menu, no Quit anywhere — an application menu exists and its Quit works, render8 2026-09-06 (LEDGER SO3 `flown — PASS`); this line re-derived from the ledger 2026-09-24. What remains is APPMENU below (an APP publishing its own menu through the ABI), Peter's call.
· ✓ FIXED IN TREE  SO2 / SO4  window-menu drop-down misplaced + wrong typeface; crystal drop-down 12 px inset — BOTH were already fixed at `298208ed` and the GLASSFIX brief that re-opened them was STALE (it described the pre-fix state). SO2: `b768331a` (MENUBAR2) anchors the drop-down at the title's GLYPH column (`winmenu.rs` `layout_open_rect`, `s.text_x(k)`, not the press box) and re-sources `CELL_W`/`CELL_H`/`FACE`/`BOLD` from `menubar::BAR_*`, so the bar titles and the drop-down rows draw one face by construction (const-asserted at the file foot against `crystal::DROP_CELL_*`). SO4: `06ffdaf8` (the render9 re-cut, R25) makes `crystal::menu_rect` return a literal `mx = 0` and binds the mark's `_cx` to `_`, so the dropdown goes flush and the crystal STAYS at its `strip::PAD` — the two are independently settable, which is the whole ruling. FLOWN on render14 (`docs/dev/evidence/orin28/render14-boot1-desktop-menubar.log`: `[menubar] crystal menu=170x156+0+34 anchor=panel-left menu_x=0 glyph_x=12 gap_left=0 glyph_gap_left=12`), and Peter raised neither at the glass on render13 or render14. The one thing that WAS owed — SO2's typeface half, which no capture could score because `winmenu::selftest` cleans up before the camera fires — is now PAID: `winmenu::shotmenu_selftest` (behind `witness`) holds the menu down across the QMP screendump, and the capture measures dx=0, dy=0 and identical ink coverage (0 of 900 px over 0.25 alpha, 174 vs 174 ink px) between the bar's caption and the drop-down's `About Glass` row. Evidence + scorer: `docs/dev/evidence/rmbp-0915/glassfix/GLASSFIX.md`
· ⚠ BLOCKED — GRANT NEEDED  SO5  pointer sprite changes size over the desktop backdrop — MEASURED, and the fix is ONE LINE IN A FILE NO video/ BRIEF MAY TOUCH. There is no per-surface cursor scale to fix: `video::cursor` derives its block scale from the PANEL (`cursor.rs:1646`, `block_scale` = `Metrics::for_height(fb.height).scale`), so it is one size on every surface by construction. The defect is TWO SPRITES — `video::cursor` at `(BASE_CELL+1)*s` = 9·s in the FRONT buffer and `pal::cursor` at `9*(metrics().scale+1)` = 9·(s+1) in the back buffer — and the desktop present's occluder subtraction picks which one reaches glass: over a window the back buffer is subtracted and only 9·s lands, over the backdrop nothing subtracts it and 9·(s+1) does. **Nothing grows; a bigger sprite stops being covered.** NOT LIVE ON x86: `pal::cursor::SPRITE_OWNS_PAINT` is `cfg!(target_arch = "x86_64")` (`pal.rs:301`), so `pal::cursor::paint` never runs here and the x86 desktop already satisfies the rule — MEASURED on the glass, `:: GLASSFIX2: sprite same=1 backdrop=9x9 window=9x9 owns_paint=1 compositor=9x9 backbuffer=18x18 -> PASS ::`, with the go-red reading `same=0 backdrop=18x18 window=9x9 -> FAIL` from removing exactly that condition. LIVE on aarch64, where the constant is false and both painters are on one panel. **THE GRANT: `crates/kernel/src/pal.rs` `mod cursor` `sprite_scale` (line 382-384), `pal.metrics().scale + 1` -> `pal.metrics().scale`.** Converge DOWN — the SO5 row's older `WAS:` text says converge UP and its CURRENT status overrules it by measurement: the 9·s arrow is the one orin 17 photographed and must not move. Two things the grant prices in: `sprite_scale`'s doc states the `+1` as INTENT ("one step above the text scale so the cursor reads at a glance"), which is Peter's taste call; and the better end state is CURSOR-13 — `SPRITE_OWNS_PAINT` true on aarch64, retiring the back-buffer sprite, which drags the Pi/Orin pump's save-under bracket with it. Scored by `wm::glassfix2_selftest` the moment either lands. Evidence + the exact diff: `docs/dev/evidence/rmbp-0915/glassfix2/GLASSFIX2.md`
· ✓ DROPPED BY DESIGN  SO1 / SO10 / SO13 / SO17  console route lost on close; shell re-open resolves to the console; no `pin_console`; re-minted console comes back empty — R49 (APPPIN, orin 27): the reopen mechanism these rows tracked is DELETED, the console and the shell are pinned apps (LEDGER rows read `dropped — by design`, see SO27); line re-derived 2026-09-24.
· ✓ FIXED IN TREE (unflown)  SO9 / SO14  a shell Enter opens a file when Quarry is open-unfocused; boot-opened Quarry holds the keyboard — SO9FIX's focus gate (`live.rs:2318`) and the R24 Esc retraction are at the tip (re-derived by code 2026-09-24; the fold sha is not an ancestor, it landed re-cut); SO14 fixed-unflown. Flies with the next desktop boot on any board.
· ✓ DROPPED  SO11  chrome drawn at a different width from its surface — DOES NOT REPRODUCE, and the code says it cannot. orin 17 PIXELS: the ~300 px title bar was OCCLUSION, not chrome — a console closed, re-minted by the dock and therefore raised ABOVE the pulse window on SCREEN3; over the pulse window's own span the chrome measures 1280 light px against a 1280 px surface on SCREEN0/1/2. In the tree, `wm::outer_box` (`video/wm.rs:14866`) is ALREADY the single expression (`bw = cw + 2*BORDER`, `cw = r.w * r.scale`) and every chrome consumer derives from it — the painter's `strip_w = bw - 2*BORDER` (`wm.rs:20452`) and the drag-handle hit test (`wm.rs:15950`) — so `chrome_w == surface_w` holds identically. The `FIT|MISFIT` witness the brief asked for would therefore print `FIT` and nothing else, and its go-red leg is unreachable without vandalising `outer_box`; the tripwire was DECLINED rather than forgotten (seat's call, recorded in `docs/dev/evidence/rmbp-0915/glassfix/GLASSFIX.md`). Same misreading class as SO4 under R25 — furniture in front of the thing being measured
· ✓ FIXED IN TREE  SO12 / S15  boot cascade overlaps the pulse title bar; pulse overlaps the console on both aarch64 boards — BOTH fixed by `9b1a0605` (CASCADEFIT, orin 17 fold 6), which is an ancestor of `c1e71690` (`git merge-base --is-ancestor 9b1a0605 HEAD` rc=0), and BOTH re-measured at HEAD on the board the rows are written about: `UNAOS_PIDESK=1 ./arroyo kernel8-test` prints `[deskcascade] fit console=570x220+35+0 pulse=436x164+10+230 overlap_rows=0 -> FIT` at QEMU raspi4b's 640x480, against CASCADEFIT's own recorded A/B at that panel of 164 overlapping rows -> 0 with the pulse box unmoved. The mechanism is a fix IN PLACE (LAWS §6): `pulsewin::boot_keepout_top` (`pulsewin.rs:998`) publishes the pulse's prospective box top less one `wm::BORDER*2` gutter, `fbcon::console_work_bottom` (`fbcon.rs:3137`) caps the console's work-area bottom at it, and the console's HEIGHT shrinks while its width and its `x` never move. **The brief's premise about the OTHER population was wrong and the correction is worth keeping: `wm::place` is a FLOW TILER, not a cascade** — rows left to right at `theme::GAP`, wrapping (`wm.rs:24052`ff) — so two TILED boxes cannot share a pixel at all, which is stronger than a fixed-step offset, and TILEFIT's clamp arm walks a clamped row UP until its box is distinct (`[wm] tile-fit … alias=none -> DISTINCT`, 47/47 on the gate run). The windows that CAN collide are the ones `place` skips (`!r.used || r.compat || r.pinned`), i.e. the pinned pulse monitor — which is exactly why this pair needed a keep-out and not a cascade step. Standing fixture over BOTH populations: `wm::glassfix2_selftest` mints four rows through the real `create`, lets the tiler place them, counts ordered pairs whose box covers another's title band, and carries an ARMED CONTROL (it walks one row onto another's title bar through the real `move_to` and asserts the scan saw it) — `cascade overlaps=0 minted=4/4 n=4 control=1 pulse_over_console=0 -> PASS`. On x86 the pair has no subject and the wire says so rather than skipping: `pulsewin::ever_armed()` is false on a `desktop_uefi` desktop, so `console=win0 pulse=win0`. Evidence: `docs/dev/evidence/rmbp-0915/glassfix2/GLASSFIX2.md`
· ✓ FLOWN PASS  SR2 (=A36)  Print Screen wedges the machine for the whole capture, every board — PRTSCR3 (sliced capture) flew PASS on render8 (A36: `prtscr3: armed=5 capturing=4 ok=4 slices=32`); the row's claim is closed 2026-09-24. A36 stays open for the SECOND loss that flight found (owner rmbp).
· ✓ FIXED IN TREE (unflown)  SO21  dock tiles and windows "all crazy mixed up" — DOCKID landed (LEDGER `fixed-unflown`: tile identity is `(win id, generation)` for apps and the OWNER for the pinned furniture), DOCKID2 (B165) closed its two follow-on defects; line re-derived 2026-09-24.
· ✓ DROPPED 2026-09-24  S32  four furniture `rollup` functions have no callers — DOES NOT REPRODUCE: each is called from its module's selftest (`rollup("selftest")` in dock/crystal/winmenu/menubar, `pulsewin::rollup("pulsequit")`); measured by grep at the tip, LEDGER row updated.
· ✓ DONE 2026-09-24 (SMALLFIX, cloud session; fixture + go-red in x86-wc.spec, evidence `docs/dev/evidence/rmbp-0924/smallfix/SMALLFIX.md`)  B70  `video/prtscr.rs`'s refusal counter is blind to the event it names — `REFUSALS.fetch_add` has ONE site (`:395`, the terminal `Err` arm) while the two door checks raise the refusal by calling `Refusal::InFlight.report()` directly at `:327`/`:359`, so `[kbdpoll] refused` reads 0 on a boot that prints the refusal by name. Fix shape: a `refuse(r)` helper that counts then reports, scoped KEYSTROKE-ONLY (`selftest_once`'s arms must NOT count, or the `census()` triple breaks asymmetrically). rmbp-ledger B70
· ✓ DONE 2026-09-24 (SMALLFIX, cloud session — `strip::settle` pays the debt on the next paint pass, dock + menubar call it; fixture + go-red in x86-wc.spec, evidence `docs/dev/evidence/rmbp-0924/smallfix/SMALLFIX.md`)  B74  the TEARSCOPE FIX ARM: `strip::erase_rect` can DECLINE and the return is discarded — `video/dock.rs:853` and `video/menubar.rs:1020` call `strip::vacate(...)` as a statement, so the debt is forgotten and the uncovered pixels tear. The instrument landed (`scope=bar`, `bar_decline`); the pre-fix baseline is a COUNT of `[strip] vacate … owed=yes` lines. rmbp-ledger B74 (→ A5)
· ✓ FIXED-UNFLOWN 2026-09-24 (CLOSESCOPE + SHOTVERB, rmbp-ledger B212, cloud session: `wc_close_click(win, owner)` on both arches so `[wm] close-scope` names the pressed window; the verb calls `Shot::report_ok`; pins + go-red in x86-wc.spec; `docs/dev/evidence/rmbp-0924/closescope/CLOSESCOPE.md`) — was: x86 `[wm] close-scope win=0` on the close-box path; `screenshot` verb prints OK with no device (shell.rs, two-line fix via Shot::report_ok; exec-orin25-shotverb is a non-compiling preserved edit)

## 2. INPUT AND USB — shared xHCI/EHCI
· ✓ RESCUED  LUN2  (hw-rmbp 2026-09-22, STORGPU fold 3bbeb754: `exec-orin-lun2` bf45f7cb cherry-picked — a declining LUN answers, and a FAT32 label is not read at the wrong sector; registry line FOLDED-BY-HAND) — its ledger row is orin's to write; until then the citation is a registered grandfather entry, not an id this row names.
· ✓ FIXED-UNFLOWN (re-derived 2026-09-24 from rmbp-ledger B44 `fixed-unflown` — XHCINTD, `KBD_INFLIGHT=4` Normal TRBs outstanding with the dup guard over the set, `drivers/xhci/mod.rs`; flies on the next rMBP/Orin boot) — was: A41 / B44  keyboard report loss: SET_IDLE 0 + one outstanding interrupt-IN TRB (both arches; the N-TD fix is the real one)
· S1 / S2  ✓ DONE (XHCIHUB, `exec-rmbp-hubfix`, fixed-unflown) — ONE endpoint-context defect and one missing store, both in `drivers/xhci/mod.rs`, both ungated. S1: `configure_hub_interrupt_ep` wrote the status-change endpoint's mps into DW0 bits 31:24, which are **Max ESIT Payload Hi**, and left Max ESIT Payload Lo (DW4 31:16) zero — the hub asked for `mps << 16` bytes per service interval, which is exactly why the HS hub returns 8 (Bandwidth Error) and the SS hub 17 (Parameter Error). S2: `enumerate_downstream` printed vid/pid and never stored them, so `MOUSE-1` read zeros — and `bot_ident`'s anonymous guard then charged the hub-downstream SD reader nothing for 84 pump timeouts. ⚠ **S1 does not reproduce on QEMU** (QEMU validates neither budget nor Max ESIT Payload), so its witness scores the SUBMITTED context dwords and the metal verdict is owed to the Orin bench; S2 reproduces and go-reds on QEMU. Standing sibling: ✓ DONE too — HIDESIT (`exec-rmbp-hidesit`, fixed-unflown) fixed the identical `(mps << 24)` / bare-`mps` arithmetic at BOTH endpoints of `configure_hid_endpoints`, and its baseline **disproves this row's own excuse for it**: the HID devices on the XHCIKBD leg are `speed=3` HIGH SPEED on ROOT PORTS with no TT anywhere, and the 524288-byte request survived only because QEMU validates no periodic budget at all. A real xHC refuses that with code 8, so root-port HS HID could not configure on metal before this. See `docs/dev/OS/07_USB_STORAGE/usb_xhci.md` §35 and §35.4
  ✓ 2026-09-15 (HIDPTR, branch `exec-rmbp-smallfix2` off `94b137a8`) — HUBFIX's ONE unscored clause is scored. `evts=` on the `:: XHCIHUB:` line was REPORTED and not verdicted because nothing moved the QEMU tablet and `scripts/qmp_type.py` was key-only. That script now has a `--pointer` mode (QMP `input-send-event` with `InputEventKind` `abs`/`rel` carrying an `InputMoveEvent` — `{axis, value}`, value 0..0x7FFF absolute or a delta — and `btn` carrying an `InputBtnEvent`; `qapi/ui.json`), and `arroyo`'s x86 `test` arms a pointer typist on the `UNAOS_XHCIHUB=1` leg: 12 abs moves + one left click, 50 ms apart, gated on the marker `:: MOUSE-1: HID pointer detected`. ⚠ THE MARKER IS NOT XHCIKBD'S, and that is the whole of the timing: `xhcihub_score` prints ONCE at the first report OR `XHCIHUB_SETTLE_MS`=8000 after the hub's status-change Configure-Endpoint, whichever is first (baseline serial.log — MOUSE-1 :1020, statchg :1021, the scorer :1113), so a typist waiting for the desktop and the boot battery the way XHCIKBD's does would inject into an ALREADY-SCORED fixture and `evts=0` would still be the wire's answer. The scorer is in `arroyo`, NOT the kernel (`drivers/xhci/mod.rs` untouched): only the harness knows whether a typist ran, and a kernel clause demanding `evts>0` would red every boot without one. Rule: typist armed ⇒ `evts>0`, plus a no-`:: XHCIHUB:`-line arm and a no-`evts=`-field arm, each with its own named red. Go-red knob `UNAOS_XHCIHUB_NOPTR=1` (host-side, `arroyo` only — no kernel feature, no fourth place to keep in step) disarms the typist and nothing else. ⚠ OWED, and it needs the file this brief could not touch: the `⚠ evts IS REPORTED, NOT SCORED` note at `drivers/xhci/mod.rs:~16345` is now stale and says the clause “is REPORTED rather than taken”. One clause of one comment, on a line HIDESIT held
· ✓ FIXED IN TREE (re-derived 2026-09-24: STORSLOT — `drivers/xhci/mod.rs` holds `StorageRecord`s index-addressed, record 0 the primary, `storage_ix` answers "the record for slot N"; `docs/dev/OS/07_USB_STORAGE/usb_xhci.md` §38; two readers on one boot is the metal proof still owed) — was: USB mass storage holds ONE device: `drivers/xhci/mod.rs` keeps a single `storage_slot`, overwritten by whichever mass-storage device configures LAST (`Endpoints Configured … Storage ready`), and every render12 boot configured exactly one even with two readers attached (S1 class: hub enumeration). Two readers on the bench = a coin flip for which one is the disk. Fix shape: per-slot storage state and the registry array below; until then ONE reader per boot (Peter's bench, 2026-09-12)
· USB mass storage: the multi-LUN census and first-present selection are DONE (USBLUN, orin-ledger A56, on hw-jetson for the next landing). LEFT: the block registry holds ONE USB disk (`drivers/block.rs` `USB_BLOCK_DEVICE`/`BLOCK_DEVICE` are single slots; `fs/bootdisk.rs` walks them) so a reader with two cards publishes one — a registry design change: a small array with per-device handles, unpublish keyed on (slot, lun), the bootdisk walk iterating it
· S30  ✓ DONE 2026-09-15 (PTRPRESS, branch `exec-rmbp-ptrpress` off `c516ea5b` — re-derive the sha at the land) — **the pointer path's accounting was absent for a reason a grep could not see: the decode is a LEVEL diff, and a level cannot represent a lost edge.** `drivers/xhci/mod.rs`' pointer branch diffs `data_data[0]` against `mouse_prev_buttons` and pushes `Event::Button` on a change, so a report that never reaches the decoder leaves the level unchanged and prints nothing anywhere. A lost RELEASE leaves the level stuck DOWN forever (SET_IDLE 0: a boot pointer reports only on a change and NEVER resends); a lost press+release PAIR swallows the click whole — Peter at the glass, "clicks not registering". **THE HOLE IS MEASURED:** `queue_mouse_read` arms ONE Normal TRB and is the LAST statement of that branch, so the endpoint is dark from the retirement until software re-arms — PRTSCLOST's keyboard argument, on the endpoint XHCINTD did not repair. On `UNAOS_XHCIHUB=1 UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 90`, with no injected fault, that window measured **`armgap_us=668983`** and only **20 of 30** button edges injected 20 ms apart reached the decoder. SHIPPED: five ungated relaxed counters folded onto the counters line (`MOUSE_ARMGAP_MAX` — the latch the pointer never had; `MOUSE_RESTATED_COUNT`; `MOUSE_PRESS_LOST`; `MOUSE_RELEASE_ORPHAN`; `MOUSE_PRESS_RECOVERED`), the predecessor's report bytes folded onto `mouse_prev_buttons`, `KBD_RESTATED_COUNT`'s conviction applied to the pointer, and the ungated repair — on a restated DOWN and on both error re-arms, synthesise the release the device can never resend. The swallowed-pair half is COUNTED and deliberately NOT synthesised: inventing a click nobody made is the worse failure. ⚠ **THE REAL FIX IS THE NEXT RUNG AND IS NAMED WITH ITS NUMBERS: the N-TD pointer endpoint** — `kbd_retire_set`/`kbd_free_buf`/`kbd_top_up` mirrored onto `mouse_*` (armed set on the slot, 64-byte-stride pool inside the existing pointer buffer, arm-into-a-free-buffer, set-membership in the dup guard, the retired buffer decoded, the halt-recovery clear). Its gate is already engineered by XHCIKBD's arithmetic: QEMU's HID device queues 16 undelivered events, so a burst of 19 into a dark window loses 3 at depth 1 and 0 at depth 4. ⚠ **QEMU CANNOT PRODUCE THE DEFECT, and the fixture says so rather than pretending:** the emulated tablet BUFFERS what the window would lose and coalesces whole press+release PAIRS on overflow, so `edges` drops while `restated` stays 0 and `held` stays consistent. The leg therefore INJECTS the fault the way XHCIKBD injects its stall — a one-shot 900 ms hold in the completion branch plus ONE swallowed release report, both behind `witness`, which is OFF for every boot and media verb — and `holed=1` is the control a PASS requires. Both polarities, one tree, one command: repair in `press_lost=1 release_orphan=0 recovered=1 restated=1 held=0x00 holed=1 -> PASS`; synthesis reverted `press_lost=1 recovered=0 -> FAIL`. The stuck click itself is metal's to score. See `docs/dev/OS/07_USB_STORAGE/usb_xhci.md` §37
✓ DONE 2026-09-24 (USBNET, cloud session; shared ledger **SO56**) — **a USB Ethernet link on the shared xHCI path, CDC-ECM front-end, the x86 smoltcp surface over it with no stack edit** (`drivers/xhci/usbnet.rs`, `UNAOS_USBNET=1`; QEMU `usb-net`, RNDIS-first handled). Dongle-only leg green: DHCP lease from the link's netdev, ICMP 4/4, UDP DNS, ring-3 SOCK-2/SOCK-4; `x86-usbnet.spec` 9/9; go-red. **AX88179 front-end BUILT on the same link, UNFLOWN** (Peter's two `0b95:1790` dongles; `kind=ax88179` on the wire; register map per the part / the Linux `ax88179_178a` driver; QEMU has no model — the bench boot confirms every constant). **OWED: (a) that dongle boot** on the rMBP (flight 13 can carry `UNAOS_USBNET=1`: with no dongle the knob is inert, with one it prints `:: USBNET: candidate kind=ax88179 …` and either `up … -> PASS` or the step that refused); **(b) aarch64 flight** — the `NicOps` registration into net6 compiles and is unflown (a Pi or Orin with a dongle); **(c)** ring-3 SOCK-3 over the link (`done=0` in the 120 s container wall, where the connect is refused anyway). Evidence `docs/dev/evidence/rmbp-0924/usbnet/USBNET.md`; doc network_stack.md §10.
✓ DONE 2026-09-24 (USBNET finding; shared ledger **SO55**) — **the xHCI enumerator read 64 bytes of every configuration descriptor and walked wTotalLength**; configurations longer than 64 bytes were silently cut (a CDC Ethernet device's are 67 and 80). Now 256 (the buffer). Default-leg enumerations unchanged (USBNET.md §SO55). Noted, not fixed: the stock dispatch tests byte 4 as a device class on the configuration descriptor's completion too (bNumInterfaces 8/9 would misroute).
· S29  no board owns a serial RECEIVE path (Orin: SPE/TCU mailbox; x86: FTDI bulk IN never driven — the x86 half is rmbp's)
✓ RESCUED  STORGPU  2026-09-22 (`exec-rmbp-storgpu` **484f142c**, `git cherry-pick -x` of `exec-orin-lun2` **bf45f7cb**) — **LUN2 IS ON A TRACK AGAIN** (the declining-LUN and FAT32-shape pair, ledger A72/A73)**.** BRANCHCENSUS judged it LOST (shared `drivers/xhci/mod.rs` + `fs/fat.rs` + `fs/bootdisk.rs` + `usb_xhci.md`, reachable from no track branch); it is now an ancestor of hw-rmbp's executor line, ids A72/A73 KEPT exactly as the LUN2-RESCUE row instructs. ⚠ THE ROW ITSELF IS NOT IN THIS FILE ON THIS BRANCH: `LUN2-RESCUE` was written on `main` at 131d8958, and this branch's base (hw-rmbp 2fc9c71c) predates the BRANCHCENSUS merge 490259e3, so the row was read from `main` and is annotated here instead — the seat that merges main into hw-rmbp should expect this line and the `main` row to meet, and should strike the `NEW` on the `main` row rather than re-picking. Three conflicts, all tail-append-vs-tail-append (AHCIBOOT/RBTDRAIN/STORWAIT2 vs BOTRESIDUE/FAT32SHAPE), all resolved keeping BOTH blocks; union line+brace arithmetic exact on all three against the merge-base f164b6fd. One file beyond the pick and the gate named it: `unaos/scripts/ledger-check.sh`'s QUEUECITE_REG went RED as STALE on A72/A73 the moment the rows landed (exactly as its own comment predicts), so both entries were struck — 35 → 33, two ids nearer the zero that list must reach. Gates: `./arroyo check` rc=0 (99 legs), `ledger-check.sh` rc=0, `./arroyo knoboff nousblun 2fc9c71c` EXIT 1 and CORRECTLY so — the three fixes are UNCONDITIONAL shared-code repairs the nousblun build compiles too (x86 1600824 → 1601040, +216 bytes; arm 1618440 → 1618440, size unmoved), which is the same verdict and the same reasoning the original commit recorded against `f164b6fd`.

## 3. FILESYSTEM AND NAMESPACE
· NEW  VFSWIT  **every per-mount `[vfs]` witness on x86 has been dead since X86BIND** (CARDROOT, hw-rmbp 2026-09-17): `bind`'s announce latch is consumed by the FIRST mount table (serial ~line 156, before any disk enumerates), so `[vfs] root mount` / `[vfs] volume mounted` / `[vfs] data mount` read 0 on boots where all four prefixes bound; the wire that carries the fact is `:: volid: mount …`, which rides a fixture. Fix = latch on the first table that BOUND A ROOT; changes `fs/bootdisk.rs::bind` for every board; go-red = the latch back on the first table. Owed beside it: an x86 lane that drives the STOR-1 witnesses (`S7`/`DIRNS` emit 0 lines on `test-fat`).
✓ FIXED IN TREE  VFSWIT  (exec-rmbp-vfswit off `2fc9c71c`; LEDGER SR18) — the latch now falls on the first table that BOUND A ROOT: `bind` takes `announce` from `s.root.is_some() && !MOUNTS_ANNOUNCED.swap(true, …)`, ONE clause in `fs/bootdisk.rs`, no other file touched in the kernel. The condition is the ROOT and deliberately not "this table has anything to say" (`|| !s.others.is_empty()`), because `survey()` re-walks until a root is found (SO38 — this run's own wire says `[vfs] resurvey n=2 … bound_on_pass=3`) and latching on a table that carries home-soil disks but no root would lose the root/boot/apps/data lines one table later; what it gives up is stated in `docs/dev/OS/09_FILESYSTEM/layout.md` §1.6.8. MEASURED `UNAOS_WC=1 UNAOS_QUARRY=1 UNAOS_QEMU_FULL=1 ./arroyo test-fat sf 200` rc=0, `completion=complete` `complete_line=2319`: `[vfs] root mount` / `[vfs] boot mount` / `[vfs] apps mount` / `[vfs] volume mounted` = **1 each** (0 each before), `[vfs] data mount` 0 because the sf image carries no `DATA/` — four witness lines for the four prefixes the same boot's `:: X86BIND: … mounts=4 layout=true -> PASS ::` reports, counted with `awk -v p='[vfs] root mount' 'index($0,p)' target/serial.log | wc -l`. The pins are `scripts/specs/x86-fat.spec` (which `test-fat sf` replays AS A STEP of the verb, so its rc is the verb's): 40/40 required witnesses, where the same spec asked 36 before. GO-RED: the latch back on the first table → all five read 0 and `mbench.py --replay` answers `MBENCH FAIL — 36/40`, rc=1, `FIRST-SHORTFALL x86-fat.spec:339`. `./arroyo test-arm` rc=0 and the aarch64 wire is UNCHANGED — the virt lane never reaches a filesystem verb, so its `[vfs]` block is the same 2 fixture lines before and after and the whole 658-line capture diffs to zero once numbers are normalised; the Pi and the Orin bind a root on their first table, where both latches answer the same. Image: aarch64 `kernel.elf` 2,142,776 → 2,142,808 B, x86_64 3,027,608 → 3,025,136 B. STILL OPEN beside it, and not this arc's: the x86 lane that drives the STOR-1 witnesses (`S7`/`DIRNS` emit 0 lines on `test-fat`).
· SO20  `SYS_OPEN` has no directory namespace (every EL0 file is pinned to the volume root) — the shared EL0 ABI
✓ DONE 2026-09-15 (DIRNS, exec-rmbp-dirns off 5e85a7a9) — `SYS_OPEN` takes a PATH on both arches through ONE shared resolver, `fs/vfs.rs`'s `el0_locate` (+ `el0_mkdir`/`el0_rmdir`/`el0_root_leaf`). It is in `fs/` and not in either `syscall.rs` because the two arches reach it from different CONTEXTS: aarch64 from `sys_open` itself, x86 from the storage SERVICE TASK, since that arch's IF-masked handler cannot mount or walk the FAT (`irqstorage.rs`'s `Stat` doc says so). aarch64: LOGIN M2's `login` cfg deleted at its three fold sites — path-taking open is the DEFAULT, `MAX_NAME` 40, and the knob-off aarch64 image MOVES by design (a default flipped). x86: `irqstorage::NAME_MAX` 12→40 with `BlockRequest.name`, every file op resolves a path through the shared resolver, `MkDir`/`RmDir` ops added, `O_CREAT` on `open_dynamic_ondisk`, `MAX_NAME` 12→40 (`FILE_DYNNAME` 624→2080 bytes, +1456 .bss — measured, not estimated). A BARE LEAF resolves `(parent=0, leaf)` and therefore walks `find_located`/`create_in_root` exactly, which is the whole compatibility argument. `..` is REFUSED, never normalised (`-EINVAL`, announced once per boot, counted for its life). Wire: `:: DIRNS: … -> PASS ::` on each arch; go-red pins `el0_walk`'s parent to the root → `nested=FAIL`. Doc: `vfs.md` §13.9; `docs/SECURITY.md`.
· NEW 2026-09-15 (DIRNS, rmbp, MEASURED — the brief's own gate commands did not compile the paths they gate) — the arc was briefed with `UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 90` for x86, which arms `witness` (arroyo:49) and NOTHING ELSE: `irqstorage` needs `UNAOS_IRQSTORAGE=1` (arroyo:273), so the entire dynamic on-disk open arm — the only x86 path that can reach a directory — is `#[cfg]`'d out of the build the gate runs, and `s4_sync_storage()` is the `false` stub. The aarch64 leg had the mirror problem before this arc: `login` needs `UNAOS_LOGIN=1` (arroyo:581), so `kernel8-test` compiled the root-pinned open. Third instance of this class in one day (§5's `esp-jetson` spelling row; NSGEN's `UNAOS_QUARRY` row): a gate that compiles and passes while the thing it names is not in the image. The standing fix is the one NSGEN asked for — a brief's gate command is derived from the knobs its code is `#[cfg]`'d on, and that derivation is mechanical enough to be a script. Owner: the next gates arc.
· NEW 2026-09-15 (DIRNS, rmbp, MEASURED — `irqstorage` had NO banner-cert row, so the knob could not be gated on media at all) — `UNAOS_IRQSTORAGE=1 ./arroyo esp-x86` (hence `test-fat`) exited **2, NO VERDICT**, before QEMU: `scripts/banner-cert.sh` had no row for `irqstorage`, and the gate treats an unregistered banner feature as un-certified rather than as a pass — correctly. That is why the STOR-1 knob-on witnesses (S7-openany, S8) are OPTIONAL in `x86-fat.spec` and why x86's LIVE-storage path had no media gate in its life. DIRNS added the row (token `:: bx-blockreq: no block device`, a literal in `drivers/xhci/irqstorage.rs`; control is the module gate itself — `#[cfg(all(target_arch = "x86_64", feature = "irqstorage"))] pub mod irqstorage;`, so knob-off the file is never lexed; measured 1 hit knob-on, 0 for a known-absent control on the same artifact). WORTH KEEPING: the registry is a HEREDOC, so a `#` comment between rows becomes a row with an empty token and reds the whole gate ("token for '# ...' is 0 bytes") — comments go above `bc_table()`. Owner: whoever next arms a knob on media; the general job is a check that every `_feats` name has a banner-cert row, which is the `sdwrite`/`ehcihid` class one level up.
· NEW 2026-09-15 (DIRNS, rmbp, MEASURED TWICE — a fixture's PLACEMENT is part of its cost, and a self-consistent fixture proves nothing) — two lessons the DIRNS gates paid for. (a) PLACEMENT: parked mid-u7-chain beside `rmdir_unafs_witness`, the aarch64 DIRNS fixture cost `kernel8-test` three witnesses across two runs (ERET-SCRUB first-entry, U6b, the u7fix park margin) and ~5 300 guest lines of the 300 s wall, while the base commit ran 126/126 on the same box — its FAT directory I/O contended for the EL0 mount loan and for wall time with fixtures still running 1 900 log lines earlier. Moved DEAD LAST in the chain: 126/126, DIRNS PASS. The missing witnesses were all EARLIER in the transcript than the fixture, which is what made this look impossible and is exactly why it needed the base measurement rather than a theory. (b) SELF-CONSISTENCY: the first go-red (resolver pinned to the root) left `nested=ok` — the fixture's SETUP walks the same resolver its CHECK does, so a namespace-less build created the file in the root and read it back from the root. Only the leg carrying an explicit "and NOT in the root" control fired. **A fixture whose setup and assertion share the mechanism under test measures nothing about that mechanism**, and every mutation-based go-red should be read for that shape before it is trusted. Owner: standing, for every fixture author.
· NEW 2026-09-15 (DIRNS, rmbp, measured — a hole this arc OPENED and closed in the same commit, recorded because the next path-ABI arc can reopen it) — **x86 keys its EL0 owner ACL by a byte comparison against constant strings**, so widening `SYS_OPEN` to paths made `"/OWNED.BIN"` stop being byte-equal to `"OWNED.BIN"` while still naming the same on-disk file: it would have missed the `U10_NAMES` owner check and then resolved through the dynamic arm as an arbitrary PUBLIC file — a private file readable by respelling its name. Closed by collapsing a single-component path to its bare leaf (`fs::vfs::el0_root_leaf`) at the head of `sys_open`, BEFORE any table lookup, and asserted by the x86 fixture's `collapse` leg. The general shape, which is the part worth keeping: **an identity key that is a STRING is a key that a namespace change can silently re-spell.** aarch64 was immune only because its key is the entry's `(dir_lba, dir_off)` slot. Every remaining x86 name-keyed table (`OWNED_FILES`, `DYN_DELETED_G`, `OPENF_*`) has the same exposure to the next spelling that is added; the re-key is LEDGER SO35.
✓ DONE 2026-09-15T18:57Z (RMDIR, exec-rmbp-rmdir off 27716175) — SO18: the UnaFS crate has a `rmdir` and `NativeBackend` implements `remove_dir` over it, so every volume in the namespace can remove a directory, not only FAT. `UnaFS::rmdir` is `unlink`'s twin (same CoW transaction, same block/inode/catalog release) with the one question `unlink` could not answer — is it EMPTY — read through `ls` and never off `size`, because an emptied directory carries a non-zero size (the serialized empty vector) and a `size == 0` test would refuse it. New `FileSystemError::DirectoryNotEmpty` keeps ENOTEMPTY distinguishable from EISDIR. The adapter authorizes FIRST through the same `native_write_authz` `unlink` uses (U6 owner/grants, RIGHT_WRITE), maps file->`NotADirectory`, root->`IsADirectory`, non-empty->`Backend("not-empty")`. Host KATs ×2 in `libs/fs/unafs/tests/mutation_logic.rs` (rc=0, 12 passed); wire fixture `rmdir_unafs_witness` (witness-gated file-tail append in `fs/vfs.rs`, LINE-NEUTRAL call in the u7 chain) prints one `:: RMDIR-UNAFS: … -> PASS ::` and re-reads after `force_remount` so the claim is about the CARD. Doc: `vfs.md` §13.8.
· NEW  SO18's own brief was wrong about the terrain, and the measurement is worth keeping: the ONLY QEMU leg that mounts a UnaFS volume is `kernel8-test` (raspi4b; `arroyo kernel8` stages a 4 MB unafs volume as MBR p2 type 0x7f via `make-pi-img.sh`). `test-arm` boots QEMU `virt` with a pflash ESP and a USB stick and has NO SD, so every unafs witness honest-skips there; and no x86 leg can host one at all because `NativeBackend` is `#[cfg(target_arch = "aarch64")]`. There is likewise NO FAT rmdir witness on the x86 `test-fat` leg — `grep -i 'rmdir|remove_dir|FATDIRS|FATGROW' unaos/scripts/specs/x86-fat.spec unaos/scripts/specs/x86-witness.spec` is empty; the FAT directory-removal witnesses are FATDIRS and FATGROW, both aarch64, both on the same `kernel8-test` leg. Anything that wants a native-volume fixture is asking for a Pi QEMU run.
· NEW  SO18's fix moved one assertion, and the move is the arc working: `shell.rs`'s `vfsroute_native_witness` leg 3 drove `fs_rmdir` at the FILE `/VFSR1.TXT` and required `-ENOTSUP` — the CAPABILITY refusal, which no longer exists anywhere. FIXED in the same branch (second commit, seat-authorised): the leg now requires `-ENOTDIR` and the name still listed, which keeps its ROUTING claim two-sided (a `rmdir` riding fat.rs would answer `-ENOENT` about the boot partition). The three doc comments that stated the old residual — `fs_rmdir`, the RELICS `mkdir` leg, `vfsroute_native_witness` — are corrected. LEFT AS A ROW, deliberately: the VFSROUTE transcript still has no POSITIVE `mkdir`+`rmdir` pair. It now CAN have one (that was the residual's whole condition), but it is ~6 lines, not a one-line addition, and every existing leg there is two-sided about routing rather than about mutation. The positive pair is carried on the wire meanwhile by `fs/vfs.rs`'s `rmdir_unafs_witness` (`:: RMDIR-UNAFS: … -> PASS ::`), which creates, refuses non-empty/file/root/stranger, removes, and re-reads after a genuine `force_remount`.
· SO19  `screenshot` still writes FAT-direct, bypassing the mount table
✓ DONE 2026-09-15T21:xxZ (NSGEN, exec-rmbp-vidsmall off 31c7e5cb) — SR3: quarry's cache-invalidation stamp now advances for every NAMESPACE mutation, not only USB arrivals. The seam is `fs/vfs.rs`: all six `MountTable` mutators (`create`/`write`/`truncate`/`unlink`/`rename`/`remove_dir`) wrap their existing resolve-then-dispatch line in `fs::ns_bump`, which advances ONE arch-neutral `AtomicU64` (`fs::NS_GEN`, `fs/mod.rs` — in `fs/`, never in a driver) iff the mutation SUCCEEDED. Above the dispatch, so every backend inherits it and none implements anything; six line-for-line replacements, so `fs/vfs.rs`'s knob-off `panic::Location` numbering is untouched (`git diff -U0` = six `@@ -N +N @@` + one file-tail append). `video/quarry/live.rs::volume_gen` ADDS the two monotone numbers on aarch64 (`usb_publish_gen().wrapping_add(fs::ns_gen())`) so the USB event still advances the stamp through the same function, and reads `fs::ns_gen()` alone on x86, where the arm was a literal `0` — i.e. x86's cache could never invalidate at all. Wire: `:: QUARRYSTAMP: … -> PASS ::` (`stamp_selftest`, witness, chained from BOTH `selftest` and `door_selftest` so no router file another lane owns needed a line): +1 per mutation, 0 for a listing (mock volume AND the live `vfs_mount_table()`), 0 for each refusal shape (`-EEXIST`, denied principal). Go-red MEASURED (not predicted): delete the `ns_bump` at `MountTable::create` -> `:: QUARRYSTAMP: before=0 after=3 mutations=3 … mkdir=true/false create=true/false … -> FAIL ::`, run rc=1; reverted. Gates: `./arroyo check` rc=0; ONE run `UNAOS_WC=1 UNAOS_QUARRY=1 UNAOS_QEMU_FULL=1 ./arroyo test-fat sf 200` rc=0. Doc: `vfs.md` §15.
· NEW 2026-09-15 (NSGEN, rmbp, measured) — **the brief's own gate command does not compile the module it gates.** `UNAOS_WC=1 … ./arroyo test-fat` leaves `quarry` OFF: `wc` does not imply it (`Cargo.toml`'s `quarry = []` is implied only by `facet` and `deskcascade`), `video/mod.rs:684` gates `pub mod quarry` on `any(all(x86_64, wc), all(aarch64, desktop_firmware))` AND the call sites carry `feature = "quarry"`, so `crystal::selftest`'s tail calls nothing and any quarry fixture vanishes silently with no SKIP line. `UNAOS_QUARRY=1` is required beside `UNAOS_WC=1` for every x86 quarry gate. Same class as the `esp-jetson` spelling row in §5: a gate that compiles and passes while the thing it names is not in the image.
· NEW 2026-09-15 (NSGEN, rmbp, measured; SR3's OWN SECOND DEFECT, LEFT OPEN) — `unpublish_usb_geometry` (`drivers/block.rs`) still never advances `USB_PUBLISH_GEN`: its body holds zero `fetch_add` and LOADS the counter only to compare against `captured_gen`, so a stick REMOVAL invalidates nothing and quarry serves a listing of a volume that is gone until the next arrival. SR3's fix covers every mutation UnaOS itself performs; a retraction performed by a human hand is still unheard. One `fetch_add` in that function, plus a fixture; `drivers/block.rs` was outside the VIDSMALL brief's file set and another executor held it that session. **✓ DONE 2026-09-15 (BLOCKSMALL/USBUNPUB, `exec-rmbp-blocksmall`, parent `5e85a7a9` — re-derive the sha at the land).** ONE `fetch_add(1, AcqRel)` in `unpublish_usb_geometry`, folded onto the line the attach-edge drop already occupied so the file stays line-neutral. **Placement is the whole of the fix and is not the obvious one:** it sits AFTER the `departing` unwrap and after both handle clears, not at the head — a generation is a CHANGE, and each of the three arms above that point (a `captured_gen` a newer publish superseded, the slot-0 sentinel, no registry match) returns `false` having altered nothing; and a reader that observes the new generation must observe the emptied registry behind it. Bumping at the tail cannot make a retraction refuse itself: every caller captures its generation before its teardown begins, the two sweep loops in `drivers/xhci/mod.rs` (`:6258`, `:14654`) re-read `usb_publish_gen()` inline per iteration, and the rescue ladder (`:11147`) makes one call. Fixture: QEMU can hot-unplug nothing on either headless leg (the removal witness says so in its own comment — that is why this shipped uncounted), so `usbunpub_selftest` (`witness`, `drivers/block.rs` file tail, chained from `fs/bootdisk.rs::unafsroot_selftest`) drives a synthetic disk at slot `0x7f` through the REAL `publish_usb_geometry_lun` / `unpublish_usb_geometry` — the real registry, the real counter, the real slot-matching rule — from the heap-up line, where the registry is still empty and the pair can disturb no boot volume; `BLOCK_DEVICE` and `USB_STORAGE_READY` are snapshot and restored anyway. FOUR legs, and the two QUIET ones are load-bearing: publish +1; a retraction earned against a SUPERSEDED generation refused AND quiet; the retraction proper +1; a NO-OP retraction refused AND quiet. A `fetch_add` at the HEAD of the function passes leg 3 and fails legs 2 and 4, which is exactly what those legs are for. Wire `:: USBUNPUB: gen_before=… gen_after=… publish_step=… stale_refused=… retracted=… retract_step=… noop_refused=… -> PASS ::`. Go-red MEASURED, not predicted: delete the `fetch_add` -> `gen_before=0 gen_after=1 … retract_step=false … -> FAIL`, run rc=**1**, against the green `gen_before=0 gen_after=2 … retract_step=true … -> PASS`, run rc=**0**; reverted. The two mutations are ISOLATED — the USBUNPUB go-red leaves `:: LBA32: … -> PASS ::` standing and the LBA32 go-red leaves this line standing. Doc: `docs/dev/OS/07_USB_STORAGE/usb_xhci.md` §36.1. NO ledger id was taken: this row IS the finding and is closed by the sha.
✓ DONE 2026-09-15 (QUARRYX86, exec-rmbp-quarryx86 off 6f45030c; opened by NSGEN/VIDSMALL as `· NEW`, MEASURED BY RUN — this settles the question SR3 left explicitly undetermined and said only a run could answer) — **on x86, Quarry's tree is empty because `video/quarry/live.rs::mount_prefixes` is still the `#[cfg(not(target_arch = "aarch64"))]` empty stub, and that stub is now STALE: x86 HAS a namespace.** Capture (`UNAOS_WC=1 UNAOS_QUARRY=1 ./arroyo test-fat sf 200`): `:: QUARRYSTAMP: … live_mounts=3 …` — `shell::vfs_mount_table()` binds `/`, `/boot` and `/apps` over the boot FAT on x86 (VFSROUTE/BOOTROOT/LAYOUT) — beside `[quarry] open volumes mounts=[] roots=[] tree-rows=0` and `[quarry] open census ERROR cwd=/ no volumes mounted` from the same boot. So the tree CANNOT be non-empty on x86 today, `collect_cached` is never reached there, and the file manager opens on an empty volume list on a machine that has three mounts. The two shims that cause it are `collect` (-> `shell::vfs_ls_collect`) and `mount_prefixes` (-> `shell::vfs_mount_table`), both `#[cfg(target_arch = "aarch64")]` with empty x86 twins, and live.rs's own header already names the exit: "the day the x86 VFS adoption lands these two shims collapse into one". That day has passed — the header's cited authority (`vfs.md` §12.4, "x86 is unchanged by design … that arch has no mount table to route through") was superseded by §13.3 "x86 has a namespace now". Deleting the two arch gates is a behaviour change to the file manager on x86 (real listings, real launch paths) and is its own arc, NOT part of SR3; SR3's stamp is correct and live on x86 the moment they go. **FIXED 2026-09-15 by QUARRYX86 (`exec-rmbp-quarryx86`, parent `6f45030c`), and the count was THREE shims, not two.** `volume_gen` is the third: SR3 had left its x86 arm reading `fs::ns_gen()` alone, with a comment saying `usb_publish_gen` was not added "because the block-layer epoch stays behind the arch gate this file has always carried" — a citation to the very banner this arc deletes, and never a hardware reason (LAWS §3). `usb_publish_gen` (`drivers/block.rs:937`) carries no `cfg` and `drivers::block` is compiled on both arches, so all three are now ONE arch-neutral body each and `video/quarry/live.rs`'s VFS seam contains no `target_arch` at all (R16). NO new accessor and NO new lock were needed: `MountTable::prefixes()` (`fs/vfs.rs:500`) already existed and is already what `vfs_ls_collect` asks, and `shell::vfs_mount_table()` BUILDS a fresh owned table per call and returns it by value, so the prefixes are read off a local with nothing to guard — `fs/vfs.rs` is not edited by this arc. The aarch64 bodies are unchanged and nothing was folded in: the USB / `/volumes` mounts are bound by `fs::bootdisk::bind` inside `vfs_mount_table`'s own aarch64 arm, so they arrive as ordinary prefixes and aarch64's list is the same set it listed before. Wire: `:: QUARRYVOL: mounts=3 table=3 rows=7 seam=fs::vfs::MountTable::prefixes model=["/", "/apps", "/boot"] live=["/", "/apps", "/boot"] roots=1 same_list=true nonempty=true rows_ge_mounts=true cwd=/ err=None match=true -> PASS ::` (`video/quarry/live.rs::vol_selftest`, `witness`+`quarry`), chained from BOTH family arms beside `stamp_selftest` so no router file another lane owns needed a line; `table` is read by calling `vfs_mount_table().prefixes()` directly rather than through `mount_prefixes`, so the two sides of the comparison do not share the code under test. x86 before: `[quarry] open volumes mounts=[] roots=[] tree-rows=0`; after: `mounts=["/", "/apps", "/boot"] roots=["/"] tree-rows=7`, with the list pane carrying 18 entries (6 dirs `APPS/ B43/ EFI/ SUBDIR/ apps/ boot/`, 12 files including `KERNEL.ELF*(2889760)`) where it printed `census ERROR cwd=/ no volumes mounted` before. Docs: `vfs.md` §12.4 marks the shim retired, `quarry.md` §3.1 is rewritten from "the one `target_arch`" to its deletion. Gates: `./arroyo check` rc=0 both arches; ONE run `UNAOS_WC=1 UNAOS_QUARRY=1 UNAOS_QEMU_FULL=1 ./arroyo test-fat sf 200` rc=0 carrying QUARRYVOL PASS beside VIDSMALL's QUARRYSTAMP and QUARRYDOOR, both still PASS; go-red (restore the `Vec::new()` twin) -> `mounts=0 table=3 … match=false -> FAIL`, reverted.
✓ DONE 2026-09-15 (opened by QUARRYX86 as `· NEW` — the FOURTH shim on the same dead citation, deliberately not taken there; FIXED by SMALLFIX3, second commit on `exec-rmbp-smallfix3` off `dbfbff85` — re-derive the sha at the land) — **double-click still cannot launch a program from Quarry on x86, and the reason is one more `vfs.md` §12.4 shim: `video/quarry/live.rs::launch`'s `cfg(not(target_arch = "aarch64"))` arm.** It prints `[quarry] launch DECLINE path=… reason=no-vfs-on-this-arch (vfs.md 12.4)` and returns that sentence to the path bar, on the premise "x86 has no VFS mount table (`fs/vfs.rs` gates both backends to aarch64)" — the same premise QUARRYX86 measured dead (VFSROUTE made `shell::vfs_mount_table` arch-neutral; §13.3 superseded §12.4). It is now provably reachable: the same boot that prints this DECLINE prints `[quarry] open volumes mounts=["/", "/apps", "/boot"] roots=["/"] tree-rows=7` and a census listing `KERNEL.ELF*(2889760)` and the `APPS/` tree the launcher wants, so the path resolves. And the file's OWN note already says the spawn half is not what is missing (`live.rs`, rmbp-7 QUARRY, on `reap_jobs`): "x86 has `arch::syscall::spawn_user_image_bg` and `bg_poll` with the same signatures … What is missing on x86 is the mount table". That sentence is the whole of the remaining work and it is now false. NOT done by QUARRYX86 on purpose: its brief scopes the LISTING (`mounts=` / `tree-rows=`), and spawning an EL0 image read off the boot FAT on x86 is a different risk class that deserves its own fixture — the aarch64 arm reads through `collect`, checks `uslots::USER_REGION_SIZE` and enters the job table, and each of those wants a witness on the new arch rather than inheritance by deletion. The shape is the same as QUARRYX86's: delete the x86 arm, let the (currently aarch64-gated) real body become arch-neutral, and keep the THIRD arm (ringless aarch64) as the only remaining `cfg` — its `REFUSED reason=no-el0-layer` is a real fact about a build with no rings, not a board twin. Fixture shape: double-click `/apps/VUG.ELF` from the x86 door fixture and assert a pid in the job table. Owner: the next quarry arc. **DONE, and the shape was exactly the one predicted.** The `cfg(not(target_arch = "aarch64"))` arm is deleted and x86 takes the aarch64 body; the ringless arm (`aarch64` + `desktop_firmware`, none of `baremetal`/`tegra_el0`/`virt_el0`) stays as the ONLY `cfg` left, because `REFUSED reason=no-el0-layer` is a fact about a build with no rings rather than a board twin. Nothing was folded in and nothing invented: `arch/x86_64/syscall.rs:16937` is `spawn_user_image_bg` with the merged body's exact signature, and `video/desktop_uefi.rs:858` has been driving it through the same `wm::app_name_arm(wm::owner_of_launch(…), path)` seam on every `wc` boot. TWO `cfg`s remain INSIDE the one body and both are hardware facts with the reason stated (LAWS §3), copied verbatim from `shell::read_el0_image` — this tree's other launcher, which carries the identical pair for the identical cause: the read ceiling is the ring-3 window each arch MAPS (`uslots::USER_REGION_SIZE` / `arch::syscall::user_window_size()`) and the expected `e_machine` is the instruction set each CPU DECODES (183 / 62). The aarch64 refusal text is byte-for-byte unchanged (`WANT_NAME` renders `"aarch64"` there). The merged predicate is `reap_jobs`'s character for character — the two functions are the two halves of one job table. Fixture: `launch_selftest` (`witness`+`quarry`), chained from BOTH battery arms beside `vol_selftest`, driving `launch` directly on `vol_selftest`'s principle that the thing under test must not also be the thing measuring; it kills the job it starts and reaps the row, so no job, window or ASID survives into the fixtures after it, and a board with no program source mounted SKIPs naming every candidate. Doc: `quarry.md` §3.2. Go-red: restore the deleted x86 arm -> `jobs=0->0 line="no VFS mount table on this arch yet (vfs.md 12.4)" started=false -> FAIL`.
· NEW 2026-09-15 (NSGEN, rmbp, measured) — **no x86 fixture may mutate the FAT volume without joining a roster, and that is a real bound on x86 fs gates.** `fs/fat.rs`'s X86 FAT-MUTATOR ROSTER is the whole consistency argument on x86 (there is no in-`fat.rs` mutation lock there — `with_fat_lock`/`with_dir_lock` are `#[inline(always)]` passthroughs), and `FatBackend`'s VFSX86 header makes joining it an explicit precondition for any new x86 consumer of the VFS write surface. So SR3's fixture drives the seam over an in-RAM volume (`vfs::nsgen_mock_table`) instead of the boot disk: the claim is about `MountTable`, which is above the backend dispatch, and a battery-time mutation of the very FAT image the same run reads STAT.ELF / VUG.ELF / PULSE.ELF off of is not a free experiment. Anyone who wants a LIVE-volume x86 fs fixture owes a roster row in `fs/fat.rs` first.
· ✓ FIXED-UNFLOWN 2026-09-24 (B102, cloud session: `scratch_ladder -> bool`, Gate 3 runs only on `true`, else `Gate 3 REFUSED … No write.`; three polarities type-checked; the Pi bench flies it) — was: B102  the Pi installer's "Gate 2" is a printed diagnostic wearing a gate's name: `install/pi.rs:166` calls `scratch_ladder(&t, &sec0)` then `install_to_pi(t, &sec0)` unconditionally, `:197` declares no return type, and `:290` then prints `[2] write path armed … all satisfied` — so a capture can carry `ladder REFUSED … No write.` and a positive claim that the skipped gate passed. Default-off path (`piinstall_confirm`), ORDERED BEHIND the stranger guard (rmbp-ledger B91). rmbp-ledger B102
· ~~`read_block_at` `(lba*512) as u32` truncation above 4 GiB (flagged by sdv1, unledgered — ledger it first)~~ **✓ LEDGERED AS SR15 AND PARTIAL 2026-09-15 (BLOCKSMALL/LBA32, `exec-rmbp-blocksmall`, parent `5e85a7a9` — re-derive the sha at the land). READ SR15 BEFORE ACTING ON THIS ROW: as written it pointed at a defect ANOTHER TRACK HAD ALREADY FIXED.** `read_block_at` + `(lba*512) as u32` + 4 GiB is `arch/aarch64/sdmmc_tegra.rs`, and orin ledger **A57** — whose own text ends "flagged earlier by sdv1 and never ledgered until now" — folded all six of that file's sites onto `sd_block_arg` and shipped the SDARG fixture (folded `ee5e2109`). So this row was a STALE DUPLICATE of a closed track row, and the honest response was to SWEEP the class rather than fix the name. **WHAT THE SWEEP FOUND, and it is a real defect in SHARED code:** `drivers/block.rs` narrowed a `u64` LBA with `lba as u32` at EIGHT xHCI entry points (`storage_read10` / `storage_write10` and their `_on` twins, single-block and counted). Different ceiling from A57 and the difference matters — READ(10)'s CDB carries a SECTOR number, not a byte offset, so these top out at `u32::MAX` SECTORS = 2 TiB, 4096x further out than A57's 4 GiB, and no byte offset is formed in `block.rs` at all. FIXED: one helper, `read10_lba32`, refuses with a witness and `BlockError::BadLba` instead of wrapping onto LBA 0 (the boot sector); all eight call it, line for line in place; ungated; fixture `lba32_selftest` with two ceiling positives and three negative controls; go-red measured on the HELPER (`u32::try_from` back to `Ok(lba as u32)` -> `refused=0/3 -> FAIL`, rc=1) — and the fixture's bound is stated in SR15 rather than oversold: it cannot see a single CALL SITE reverted, because the `lba >= dev.num_blocks` bound refuses an out-of-range LBA with the same error one layer earlier, so no behavioural leg can distinguish them. **✓ THE PARTIAL IS CLOSED 2026-09-15 (EMMC2LBA + LBA32GATE, `exec-rmbp-smallfix4`, parent `1c7c1733` — re-derive the sha at the land).** What was left was `drivers/emmc2.rs:736` and `:805` building the BLOCK-ADDRESSED card argument as a bare `lba as u32`, missing the `lba > u32::MAX` refusal that `sd_block_arg` and `sdhc.rs::lba_arg` both carry (the byte-addressed arm there was already checked). Both now go through one tail helper, `card_block_arg`, in `sd_block_arg`'s shape, with a named refusal on the wire and the `emmc2lba_selftest` fixture (four ceiling positives, four negative controls) executed by `kernel8-test` — QEMU `raspi4b` mounts the SD through emmc2, so the fixture's host is also the mount proof. **AND THE ROW'S OWN CLOSING SENTENCE IS BUILT:** "a grep gate over the call shape would make it mechanical" is now **GATE-LBA32** (`unaos/scripts/lba32-check.sh` + `scripts/lba32.registry`, one probe in `./arroyo check` beside fc2's, `docs/dev/STRUCTURAL_GATES.md` §GATE-LBA32) — because every fixture in this class is a known-answer test on the HELPER and cannot see a CALL SITE reverted, which is the one mutation that matters and the gate's recorded go-red. Census at the landing: 41 files, 562 `as u32`, 1 LBA-named, 1 registered, 0 unregistered. **Unflown on Pi metal — the Pi track owns that flight, `docs/dev/OS/pi-queue.md`.** Everything else swept is CLEAN and SR15 names each so nobody re-runs it: `fs/fat.rs` (by construction — `parse_bpb:1537` refuses a volume past `u32::MAX + 1` sectors), `fs/bootdisk.rs` (hits=0), `drivers/ahci.rs` (`u64` end to end into 48-bit READ DMA EXT), the xHCI CBW/CDB builder (`xhci/mod.rs:11921`/`:11936` is a block COUNT, not an LBA), `drivers/sdhc.rs` (`lba_arg`, `u32::try_from` on both arms).

## 4. ONE OS — platform-split families and board names in shared code (R16, R4, S6)
✓ DONE 2026-09-24 (WINTITLE leftovers, cloud session; LEDGER **SO22** fixed-unflown) — the two x86 launchers that minted UNNAMED windows (`winx8_launcher`, `pulsew_launcher`) name their program at spawn through `wm::app_name_arm(owner_of_launch(slot), path)`, and the name DIES WITH THE SLOT on both arches (`free_user_space_by_cr3` / `teardown_user_slot`). Measured on the wc leg: before, every later slot-0 window wore "VUG" (dock labels gen 27..32); after, `Application` for unnamed blobs, `VUG` and `PULSE` for the programs; WINX-8 carries `name_gone`; x86-wc.spec 41/41. Evidence `docs/dev/evidence/rmbp-0924/so22/SO22.md`.
· ✓ M1+M2 DONE 2026-09-22 (BUSX86, rmbp, `exec-rmbp-busx86` off `878e1818`, M1 `cb6635be`; ledger **SR20** / rmbp-ledger **B153**) — **THE SMessage BUS WAS A BOARD-SPLIT OF THE PROGRAM STORY, NOT OF A DRIVER, AND THE MODULE SAID SO IN ITS OWN LAST LINE** ("aarch64-only … zero x86 surface"). `SYS_MSEND`/`SYS_MRECV` lived only in `arch/aarch64/`, so the rMBP desktop's three EL0 programs had no bus to speak on. The ABI numbers were never the split — 19/20 have been in `una-abi` since ABIFREEZE. M1 lifts the v1 CODEC to `crate::bus` (one declaration in `lib.rs`, both arches, goldens asserted on both every boot); M2 lands the two syscalls, the bounded mailboxes and the in-kernel `ls`/`cat`/`cp` on x86. **FAMILY SIZE FOR THE WIRE: 2 -> 1.** For the TRANSPORT it stays 2 and always will, which is the part worth writing down: the two arches key their file ACLs on different things — aarch64 `(dir_lba, dir_off)` + an IMAGE_SHA256 `PrincipalRecord`, x86 a `U10_NAMES` name-id + `(row, SLOT_GEN)` — so "the same principal stamping" is a claim about the FRAME (one constant on both) and not about the identity behind it, and a merged ACL is NOT what this row is asking for. OWED (M3): the ring-3 midden twin on x86 and the EQUIVALENCE witness proper — denied-via-bus == denied-via-syscall with a byte-same errno — which cannot be made kernel-side on either arch, because the direct leg is `SYS_OPEN` and only a ring-3 program can drive both legs and compare. Also owed: the BANDY-2 write side on x86 (it decodes and answers `-ENOSYS` in a well-formed reply today) and the descriptor-free created-name create that would retire bus `cp`'s handle cost — both are STOR-1's namespace work, not the bus's.
· S7  `render_service` ×3 (`render_service` Pi / `x86_render_service` / `orin_render_service`) — convergence arc ✓ M1 DONE 2026-09-22 (RENDCONV, branch `exec-rmbp-rendconv`, parent `cab94b9e`; LEDGER B144 + SR19): the waiting axis and the census are lifted into `RenderWait` and the Pi member is a shim over the shared `render_pass<W>`, 421 lines unchanged and line-neutral. Steps 2-3 (x86, Orin) OWED and re-scoped — SR19 measures the family at seven non-constant axes, not the design's two, so the order and the cost in S7-CONVERGENCE §4/§6 are priced against a family that no longer exists.
· ✓ STRUCK 2026-09-24 (re-derived: SO27 APPPIN removed the `SHELL_REOPEN` latch and all three drains; `arch-families.sh` lists no such family; LEDGER S4/SR6 dropped) — was: SR6 / S4  `SHELL_REOPEN` drained three ways, two names break R16
· ~~`input_service`/`x86_input_service`, `usb_pump`/`x86_usb_pump`~~ ✓ DONE 2026-09-24 (ONEOS5, LEDGER SR21, cloud session: ONE name each, two cfg-exclusive definitions, table 6 → 4) — with `render_service` ×3 (S7) these WERE the THREE real convergence rows left; S7 is the one remaining. ~~`start_secondaries`/`start_secondaries_tegra`~~ ✓ DONE 2026-09-16 (ONEOS3, `exec-rmbp-oneos3`, parent `be09d558` — re-derive the sha at the land): ONE `start_secondaries`, two cfg-EXCLUSIVE definitions, the shared handshake extracted into five `#[inline(always)]` helpers; the table reads 7 → 6 families. ~~`release`/`pi_release`~~, ~~`shell_remint`/`tegra_shell_remint`~~, ~~`run_bsp`/`run_bsp_tegra`~~, ~~`on_block`/`pi_on_block`~~ ✓ STRUCK 2026-09-15 (ONEOS2 — two are not families in the table at all, one is a caller/callee pair, one is an affix collision; see the `· NEW` rows below) — the GATE-FAMILY table (`unaos/scripts/arch-families.sh`), one family per arc. ~~`rast_demo_maybe` ×2~~ ✓ DONE 2026-09-15T19:18Z (ONEOS, exec-rmbp-oneos 3dab65ac): ONE `rast_demo_maybe`, both boards' fragments behind their existing cfgs inside it, behaviour unchanged on both boards (witness census 1:1 vs 27716175); the table reads 8 → 7 families
· NEW 2026-09-15T19:18Z (ONEOS, rmbp, `exec-rmbp-oneos` 3dab65ac) — **two of the seven rows left in the GATE-FAMILY table are FALSE FAMILIES and are not convergence work.** (1) `on_block`/`pi_on_block`: `on_block` is a trait method of the tar walker (`unaos/crates/kernel/src/selfhost/tar.rs:110`, `fn on_block(&mut self) -> Result<(), TarError>`); `pi_on_block` is the sleeping-`Mutex` PRIORITY-INHERITANCE hook (`unaos/crates/kernel/src/arch/x86_64/sched.rs:4615`, `#[cfg(feature = "rtpi")] fn pi_on_block(&self) -> *mut Task`). `pi_` there is `rtpi`, NOT the Pi board — exactly the `orin_ladder_arm` collision `arch-families.sh`'s own header warns about, one affix later, and the gate cannot know it. The owed work is an R16 rename inside `rtpi` (`pi_on_block` / `pi_current` / `pi_held_set_waits` / `pi_acquire_after_block` → a `prio_`-style affix), never a convergence — or the affix set learns that `pi_` on x86_64 is not a board. (2) `fault_handler` ×2: `aarch64_fault_handler` (`arch/aarch64/exceptions.rs:614`, `(kind: u64) -> u64`, heals and returns) and `tegra_fault_handler` (`arch/aarch64/mmu_tegra.rs:1494`, `(esr, far, elr, idx, spsr) -> !`, prints and halts) are two different `#[unsafe(no_mangle)] extern "C"` landing symbols for two different vector tables — not one job written twice, and renaming either is a vector-table edit, not a rename. So the honest count of ONE OS convergence work left in that table is FIVE rows, not seven. Found while converging `rast_demo_maybe`; nothing was struck from the list on this reading alone. Owner: the next ONEOS arc
· NEW 2026-09-16T00:58Z (ONEOS2, rmbp, `exec-rmbp-oneos2` 2c937f30) — **the GATE-FAMILY table re-measured row by row at `ca5b4ff5`: SEVEN rows, and only FOUR of them are convergence work.** ONEOS's reading found two false rows; this one is the whole table with each member's `file:line`, and it finds a THIRD false row plus TWO §4 entries that are not rows at all. REAL (4): (a) `input_service`/`x86_input_service` — `main.rs:5100` `#[cfg(all(aarch64, baremetal))]` (PL011 RX drain, interrupt-driven + poll-nap fallback) and `main.rs:6121` `#[cfg(x86_64)]` (the INPUT-UNGATE `pal::EVENT_QUEUE` producer with owed-motion/owed-event backpressure). The two bodies share NO statement — the convergence here is the R16 rename to one `input_service` with the two cfg-exclusive definitions as siblings, the `desktop_owns_backdrop` idiom already three-deep in this same file (`main.rs:6225/6229/6248`), not a body merge. (b) `usb_pump`/`x86_usb_pump` — `main.rs:5776` `#[cfg(all(aarch64, baremetal))]` (PIUSB-26, `sleep_ticks(1)` ≈ 4 ms at 250 Hz + a rate-limited `[piusb26]` cost line) and `main.rs:5880` `#[cfg(x86_64)]` (SCHED-X86 device-service: the xHCI WEDGE-8 loan, storage, FAT/flight-recorder/boot-ledger pumps, NIC drain). Same shape, same verdict. (c) `start_secondaries`/`start_secondaries_tegra` — `smp_virt.rs:559` and `smp_virt.rs:861` `#[cfg(feature = "tegrasmp")]`; 330 diff lines, neither calls the other, both AP bring-up over the shared `__secondary_rust_virt` tail. A real family and the only one whose shared half is worth extracting. (NB the gate collapses a THIRD `start_secondaries` into this row: `smp.rs:507` `pub fn start_secondaries()` (spin-table release, no args) is a different job in a different module and both modules are unconditional `pub mod` on aarch64 — a name-only collision the gate cannot see.) (d) `render_service` ×3 — S7's arc, design at `docs/dev/evidence/orin14/S7-CONVERGENCE.md`. FALSE (3): `fault_handler` ×2 and `on_block`/`pi_on_block` as ONEOS recorded, PLUS **`run_bsp`/`run_bsp_tegra`, which is a CALLER/CALLEE pair — SR9's shape exactly, one affix later.** `run_bsp` (`arch/aarch64/sched.rs:6495`) is a FOUR-LINE body, `mark_online(cpu); run(cpu)`; `run_bsp_tegra` (`:10524`, `#[cfg(all(feature = "tegra", feature = "bsprun"))]`) is an ORIN-BSPRUN prologue — banner, `el0_refusal_rollup()`, `load_witness_emit()`, `SCHED_ACTIVE.store(true)`, `bsprun_hosting_witness` — whose LAST STATEMENT is `run_bsp(cpu)`. It is not the Tegra copy of `run_bsp`; it is the Tegra prologue TO `run_bsp`, and extracting a shared part would extract the callee. Owed there is an R16 rename of the prologue, never a convergence. NOT ROWS AT ALL (2, both named in the list above and both now struck): **`release`/`pi_release`** — there is NO `fn release` anywhere in `unaos/crates/kernel/src` (`grep -rn 'fn release\b'` = zero), so the gate never grouped it; `pi_release` is `arch/x86_64/sched.rs:4689`, `#[cfg(feature = "rtpi")] fn pi_release(&self)`, the priority-inheritance lock release beside `pi_current`/`pi_held_add`/`pi_held_remove`/`pi_held_set_waits`/`pi_boost_reset`/`pi_donate`/`pi_bump_running`/`pi_relocate_scan`/`pi_acquire_uncontended`/`pi_on_block`/`pi_acquire_after_block` — the SAME `pi_` = `rtpi` collision ONEOS named on `pi_on_block`, and the eleven-symbol `prio_`-style rename is one job, not eleven rows. **`shell_remint`/`tegra_shell_remint`** — `tegra_shell_remint` no longer exists: orin's APPPIN deleted both copies at `e0b5d31d` (ancestor of this tip), leaving only `video/wm.rs:10083 pub fn shell_remint`. SR9's rename half is therefore CLOSED by another track; SR9's gate half (a caller/callee discriminator) is open and just produced its second false family — see `run_bsp` above. **FILE-SET STOP (why this arc converged nothing).** The two main.rs families' CODE is entirely in `main.rs` — both are private `fn`s of the binary crate, so no other crate can call them and every reference outside `main.rs` is PROSE. But the prose is wide: `x86_input_service` is named in 8 further `.rs` files (`video/wm.rs` ×4, `arch/x86_64/syscall.rs` ×3, `arch/x86_64/smp.rs` ×2, `drivers/xhci/mod.rs`, `drivers/xhci/ftdi.rs`, `deadman.rs`, `arch/x86_64/sched.rs`, `arch/x86_64/interrupts.rs`) and `x86_usb_pump` in 12 (`fs/fat.rs` ×6, `video/wm.rs` ×4, `arch/x86_64/smp.rs` ×4, `arch/x86_64/syscall.rs` ×3, `drivers/xhci/mod.rs` ×2, `video/desktop_uefi.rs`, `shell.rs`, `flight_recorder.rs`, `drivers/ehci/mod.rs`, `deadman.rs`, `bootpace.rs`, `arch/x86_64/sched.rs`) — 40 token-only edits in 13 files, THREE of which (`video/wm.rs`, `arch/x86_64/syscall.rs`, `shell.rs`) are held by live executors (GLASSFIX2, DIRNS, X86BIND) and the rest outside this brief's file set. ONEOS's standard was zero stale references ("the two retired names survive in exactly one place on purpose"); a rename that leaves 40 comments naming functions that no longer exist fails it. So the convergence was NOT made. The next ONEOS2 brief needs `main.rs` PLUS those 13 files, or it must wait for the three holders to fold. Owner: the next ONEOS arc
· NEW 2026-09-16 (ONEOS3, rmbp, `exec-rmbp-oneos3`, parent `be09d558` — re-derive the sha at the land) — **the `start_secondaries` row is CONVERGED, and the measurement says it was never the 330-line body merge the table implied.** The two members (`arch/aarch64/smp_virt.rs`, `start_secondaries` at `:559` / `start_secondaries_tegra` at `:861` on `be09d558`) are 281 + 176 lines with 89 identical lines between them (57 non-trivial) — but EVERY shared `serial_println!` carries a different literal (the Orin copy infixes `ORIN-SMP-3`), and the virt copy guards three loops with a `startable[]` mask the Orin copy must not have, so the run extractable without changing a witness or a board's behaviour is the HANDSHAKE, not the body: `publish_secondary_ctx` (6 statements), `publish_index_table` (3), `await_core_ready` (2), `ping_core` (5), `await_bsp_ping` (3), all `#[inline(always)]`, each called at the position its caller already ran it. **THREE axes keep the two definitions apart and no parameter spans them:** the PRESENCE ORACLE (GIC redistributor walk + PSCI `AFFINITY_INFO` phantom gate, versus the DTB `/cpus` node ALONE — RIDER 1 FORBIDS `AFFINITY_INFO` on Orin), the POSITION of the publication (virt publishes the EL2 regime BEFORE it enumerates; Orin enumerates first and STOPs single-core if `/cpus` names nothing, so it cannot publish until it knows it will start anyone), and the arity (`ram_gib_mask`, which only the DTB oracle needs). So the shape is the `desktop_owns_backdrop` idiom ONEOS2 prescribed: ONE subsystem name (R16), TWO cfg-exclusive sibling definitions (`not(tegrasmp)` / `tegrasmp`). **The cfg exclusivity is NEW and it is the arc's real finding:** the virt definition carried NO cfg at all, so every tegra image compiled it although `main.rs:329`'s call site is `not(feature = "tegra")` — gating it `not(tegrasmp)` made the compiler prove it, which it did by reding `report_conduit` and `psci_affinity_info` as never-used on the tegra arm. Those two are now gated with their only caller. **Third copy still standing:** `smp_virt.rs::probe_publish_real_path` (`tegra`+`smpprobe`) is a hand copy of the same publication its own doc calls a "line-for-line" mirror; it takes a `&[u32]` slice and guards the BSP store on `n_cores > 0`, so folding it onto `publish_secondary_ctx`/`publish_index_table` would change that guard — left alone deliberately, owed as a follow-up. GATES: `arch-families.sh` 7 → 6 (rc=1 with exactly ONE removed row and no `+` line, then `--update` rc=0); `./arroyo check` rc=0; `UNAOS_GICV3=1 ./arroyo test-arm 40` rc=0 with all 29 `[AARCH64 SMP]` witnesses set-equal before/after (`3/3 secondaries online`, both SGI directions OK, both heartbeats PASS, CAPSTONE COMPLETE) — the only deltas are the stub's link address and the cross-core print race; go-red (starve the virt core count) took `3/3` to `1/1` and was reverted. Census 1:1 over all 30 witness-literal lines. ~~**16 prose references in 7 files the brief did not name are now STALE** and are owed one token-only sweep: `arch/aarch64/boot_tegra.rs` ×2, `arch/aarch64/smpprobe.rs` ×3, `arch/aarch64/timer.rs` ×1, `fs/fat.rs` ×1, `rast_demo.rs` ×1, `unaos/arroyo` ×5, `unaos/crates/kernel/Cargo.toml` ×3.~~ ✓ SWEPT 2026-09-15 (ONEOS4/PROSESWEEP, rmbp, `exec-rmbp-oneos4`, parent `b591f379` — re-derive the sha at the land): all 7 files, comment lines only, one token substitution `start_secondaries_tegra` → `start_secondaries`. **The 16 was a LINE count and the OCCURRENCE count is 17** — `fs/fat.rs:1069` is one line of the row-10 concurrency table carrying the token TWICE (the call-order clause and the anchor-construct list); every other file's two counts agree. The proof is whole-file, not eyeball: each base file was normalised through the SAME substitution and `diff`ed against the edited file — IDENTICAL on all 7, so nothing but the token moved; `wc -l` before == after on all 7 (3362 / 704 / 1849 / 885 / 5350 / 658 / 10066), so no `panic::Location` shifted and the knob-off fold measures clean; `grep -rn start_secondaries_tegra unaos/crates unaos/arroyo` 16 → 0; `./arroyo check` rc=0. `docs/dev/OS/01_BOOT_HAL/arch_arm64.md` needed NO edit — ONEOS3 had already converged all 16 of its mentions. `docs/env-knobs.md:7356`, `unaos/scripts/orin-smp8-bench.md` (×2) and `review/unaos-schednext-LANDING.md` (×3) keep the old name ON PURPOSE — they are flight/landing records and one is a verbatim quotation of the bench card; the hits left in `docs/dev/QUEUE.md`, `docs/dev/LEDGER.md` and `docs/dev/OS/rmbp-queue.md` are this arc's own history rows and are records for the same reason.
· NEW 2026-09-15 (ONEOS4/THIRDCOPY, rmbp, `exec-rmbp-oneos4`, parent `b591f379` — re-derive the sha at the land) — **ONEOS3's "third copy" MEASURED, and its own doc comment is wrong on the one line that matters: `probe_publish_real_path` is NOT a line-for-line mirror, it is the mirror MINUS the APTEXT cache clean.** SPEC for the fold, no code changed by this arc. **Subject:** `arch/aarch64/smp_virt.rs::probe_publish_real_path` (`:1113`–`:1130` at `b591f379`, `cfg(all(feature = "tegra", feature = "smpprobe"))`, 17 lines) against the two helpers ONEOS3 extracted, `publish_secondary_ctx` (`:584`) and `publish_index_table` (`:601`), as the `tegrasmp` `start_secondaries` calls them at `:998`/`:999`. **SHARED — five statements byte-identical in the same order, and this is what a fold may take:** `gic::enable_sgi(IPI_SGI)`; `capture_secondary_ctx()`; `cache::clean_range(SEC_CTX …)`; `cache::clean_invalidate_range(SECONDARY_STACKS …)`; `N_CORES_PUB.store(n_cores as u32, Release)`. **FOUR LINES DIFFER, and only one of them is a behaviour:** (1) the BSP store — helper `BSP_AFFINITY.store(bsp_aff, Release)` unconditional on a scalar the caller read from `gic::this_affinity()`, probe `if n_cores > 0 { BSP_AFFINITY.store(aff_by_index[0], Release) }` on element 0 of the caller's slice. TWO differences on one statement: the VALUE SOURCE (the caller's table, not the live MPIDR — they agree only because `smp6_aff_table` puts the BSP at index 0) and the GUARD. **What the `n_cores > 0` guard actually guards is INDEXING AN EMPTY SLICE, not a machine state** — and it is unreachable-false at both existing call sites: `smpprobe.rs:1550` and `:1770` each pass `&aff_by_index[..n_cores]` behind an `if n_cores < 2 { … return; }`, so `n_cores >= 2` always. It is dead by CALLER, not by type, so deleting it is only safe under a signature that cannot be handed an empty input. (2) the index-table loop — same stores, same order, differing only in parameter TYPE: `&[u32; MAX_CORES]` + `n_cores` indexed, versus `&[u32]` iterated `.take(n_cores).enumerate()`. A slice does not coerce to the array, so this is the arity axis, not a behaviour. (3) the probe RETURNS `_secondary_start_virt` as a PA; the helpers return `()` — stays in the probe. (4) **THE ABSENCE, and it is the finding: the APTEXT PoC clean of the AP entry text is on `:999` INSIDE the tegra `start_secondaries` and has no counterpart anywhere in the probe path.** `population = grep -n 'clean_range\|clean_invalidate_range\|cache::' arch/aarch64/smpprobe.rs`, hits = 18, none of them the `_secondary_start_virt`/`__secondary_rust_virt` text range — so both real-entry legs issue `CPU_ON` into code the BSP may still hold dirty above the PoC, which is precisely the failure orin-ledger A15 charges with 5 of 23 dark boots and render3 2/2. **THEREFORE THE FOLD IS NOT A CLEANUP, IT IS A FIX PLUS A CLEANUP, and they must be separable:** routing the probe through the shared publication either ADDS the APTEXT clean to the smpprobe legs (a real behaviour change on a leg whose whole purpose is to reproduce the SMP-3 park — it could MASK the park and must be flown as its own rung, not folded silently) or deliberately keeps the clean out of the shared helper and leaves the probe's hole documented. **SHAPE that keeps the diff honest:** keep `publish_index_table`'s array signature (so the two existing call sites emit what they emit today), give the probe a thin slice→scalar adapter that does its own `len().min(MAX_CORES)` and passes `aff_by_index[0]` to `publish_secondary_ctx`, and make the APTEXT clean its own named `#[inline(always)]` helper called explicitly by each path that wakes a core — so the probe's omission becomes a visible call site rather than an absence. **FIXTURE, and the honest part is that no QEMU gate reaches this code:** `smpprobe` is `tegra`-gated and `arroyo test-arm` is aarch64 `virt`, so the behavioural leg is Orin metal (sel=21 single, sel=23 rapid). Cheapest real proof, costing no extra flight: an `smpprobe`-gated PARITY selftest that runs inside sel=21 BEFORE its `CPU_ON` — publish through the folded path, snapshot `BSP_AFFINITY`/`AFF_BY_INDEX[0..n]`/`N_CORES_PUB`, publish through a retained copy of the pre-fold sequence, compare, print one `:: tegra: SMPPROBE-6 publish parity OK|MISMATCH ::` line; it wakes no core, so a red is a compile-or-logic red and never a park. Plus the two compile legs already in KERNEL_CFG_MATRIX that carry `tegra,tegrasmp,smpprobe`, and a go-red (perturb one store) to prove the parity line can fail. **KNOB-OFF POLARITY:** `smpprobe` OFF is the shipped jetson image AND every virt/Pi image, and the fold touches helpers compiled into all of them — so the gate is byte-identity of the `smpprobe`-off images, measured `llvm-objcopy -O binary` + `cmp`, not asserted. Two things break it if unwatched: any change to `publish_index_table`'s signature (it reshapes the loop at `:695` and `:999`), and any line-count change in `smp_virt.rs` moving `panic::Location` constants below it — the APTEXT comment on `:999` records that exact 8-byte lesson, so the fold is LINE-NEUTRAL or it is measured. Owner rmbp; blocked on an Orin bench slot for the sel=21 rung. Class S6.
· S6  the 50 board-named symbols/tokens in shared files (rmbp 19's count; scour per R4), and the 28 board-named shared scripts
· ✓ FIXED-UNFLOWN 2026-09-24 (B55, cloud session: the seven comments name the capability `aarch64_el0`; `k8-reach.py --impliers` derives the list; STALE-ENUM red in k8-reach, first catch `any(sdmmc_arm, sdmmcwrite)` missing `sdwrite`; GATES.md §B55) — was: B55  ELEVEN stale board-term restatements in `unaos/arroyo` (7 sites still read `any(baremetal, tegra_el0)` on this tip) — stale BEFORE JV1, since `arch/aarch64/mod.rs:46` gates on the CAPABILITY `aarch64_el0`, and falsified outright by `virt_el0` as a third implier. One line-neutral sweep carrying the grep that finds them; scope grew to replacing the hand enumeration with a DERIVED one (`k8-reach.py`'s `cargo_implications` already computes the closure). rmbp-ledger B55
· B6  cross-arch splash: `splash.rs` stays, its call sites go x86-gated (Peter asked by name). rmbp-ledger B6
· ~~S3 / S5 / S21  `flight_recorder` x86-only; unconditional `pub mod` with single-arch consumers (FC-2 shape, fourth instance)~~ ✓ DONE 2026-09-15 (FC2CHECK, rmbp, `exec-rmbp-fc2check`): **S5's structural check is BUILT and wired** — GATE-FC2, `unaos/scripts/fc2-check.sh` + `unaos/scripts/fc2.registry`, called from `check_both` beside GATE-ROOTS/GATE-FAMILY/GATE-APPEND, documented in `docs/dev/STRUCTURAL_GATES.md` §GATE-FC2 with its go-red. It walks both crate roots' module trees, gives every file the conjunction of the declaration cfgs above it, resolves every `::`-path and brace-expanded `use` to an absolute module path, and reds when a referenced module's every reference sits under a predicate the declaration lacks. **All three instances the rows name were ALREADY GATED at `d6b3c9a7`** — `flight_recorder` since its own landing commit `77c61e3a` (`lib.rs:142`), `dock` (`video/mod.rs:103`), `pulsewin` (`video/mod.rs:665`) — so the rows were stale, and S21 is not an instance at all (there is no socket `mod`; the socket surface is an inline tail module of `net_phy.rs`, whose declaration is already gated). Census: **162 declarations / 162 referenced / 4 findings, none of them a named instance.** FIXED on their declaration lines, line-neutral: `install::pi` and `install::clone` (`install/mod.rs:44`/`:51` — Cargo implication carries features but never `target_arch`, so `piinstall ⇒ baremetal ⇒ pi` left both compiled into an x86 image whose only consumers are in `all(aarch64, baremetal)` bodies). REGISTERED with their measurements, each because the declaration carries a ruling this gate may not overturn: **`splash`** (refs=3, all x86 — but rmbp-ledger **B6** "Cross-arch splash: `splash.rs` stays, call sites x86-gated" is open and Peter asked for it by name; B6's seat rules, and the fix when ruled is one folded line at `lib.rs:189`), **`rtpi`** (refs=6, all x86; the decl's own comment at `lib.rs:226-232` deliberately declares it feature-only so an aarch64 `rtpi` build gets the shim — R1's seat rules), and **`video::desktop_firmware`** (refs=13, all under `aarch64_el0`; whether a ringless `UNAOS_DESKTOP_FIRMWARE` image is a thing is a product call owned by hw-pi4/hw-jetson). S3 and S21 stay OPEN on their surviving substance — neither aarch64 board leaves an on-card record, and the socket ABI's aarch64 gap — with the structural clause struck from each and the measurement recorded
✓ DROPPED 2026-09-15T21:xxZ (VIDSMALL, exec-rmbp-vidsmall off 31c7e5cb) — SR8 asked for a `cfg` on `BLIT_NET_CORE` and there is no `cfg` that covers its writers, so the row is answered by measurement rather than fixed. It is `[AtomicI64; 8]` (`wm.rs:10571`; signed is load-bearing under DEBTCLEAR). Writers `BlitGuard::enter` (`:10773`) and `impl Drop for BlitGuard` (`:10810`) carry NO attribute and are reached from `composite_inner` (`:5949`, enclosing `fn` `:5421`, no attribute) — THE composite pass, on every board and every feature set, which is DRAGFIX M1's stated decision ("the net has to exist on every build") and not an oversight. Reader `blit_samecore_futile` (`:11324`) is unconditional too, called from `impl DrainBarrier` at `:11803`/`:11851`. Per board: x86 writes from the composite and reads from the drain + the wedge legs; Pi and Orin write from the composite and read from the drain + the COMPGATE census (`blit_net_snapshot` `:27483` -> `wcg.rs:1808` `blitnet=`). So the 64 bytes of `.bss` are touched everywhere and the knob-off image cannot shrink; the one `wm.rs` edit is a line-neutral append to an existing doc line, so this row moves ZERO bytes. The census now lives on the static's own doc so nobody re-derives it. LEFT, and NOT this id: the literal `8` is still board geometry in kernel source. `percpu::NUM_CPUS` is aarch64-local and `gdt::MAX_CPUS` x86-local; the only arch-neutral `8` of this shape in the tree is `ui_status::PSTRIP_MAX_CPUS` (`ui_status.rs:407`), a DISPLAY bound and not a capacity bound — so the honest fix is a new shared max-cores constant in a new home, a different arc in different files.

## 5. GATES AND TOOLING — shared `arroyo`, scripts, specs
✓ DONE 2026-09-24 (same session, arroyo `test_kernel8`: the machine list is asked FIRST — `qemu-system-aarch64 -machine help`, awk on the first field — and an absent `raspi4b` is its own NO VERDICT exit 4 naming the QEMU version and "QEMU 9.0 added it", before any build; measured here: the new line prints and rc=4 in under a second) — was: **`kernel8-test` calls an absent machine type a HARNESS FLAKE.** On QEMU 8.2.2 (no `raspi4b`, added in 9.0) `qemu-system-aarch64: unsupported machine type` is retried on three ports and reported as `HARNESS FLAKE — QEMU never came up`; a flake is something a retry can change and this is not one. The verb should read QEMU's stderr for `unsupported machine type` and say `raspi4b absent (QEMU ≥ 9.0 needed)` with exit 4, the same NO VERDICT it already gives. Not fixed here (a verb change, not this session's focus); `docs/dev/evidence/rmbp-0924/tracksync/REHEARSAL.md` §2.
✓ DONE 2026-09-24 (TRACKSYNC, cloud session; shared ledger **SO54**) — **`loginst` had no banner-cert row**: the Pi's boot-13 rehearsal (`UNAOS_PIDESK=1 UNAOS_WC=1 UNAOS_LOGIN=1 UNAOS_LOGINST=1 ./arroyo kernel8-test`) stopped at `feature=loginst … -> UNREGISTERED`, `noverdict=1`, exit 2, before QEMU ran; x86 `test` never runs banner-cert so no x86 gate could see it. One row added (`loginst|:: LOGIN: users+session|-|measured`; 5 hits in the Pi and virt login artifacts, 0 in a loginst-off x86 kernel); the same artifact re-certified `hits=5 -> OK`, exit 0. Evidence `docs/dev/evidence/rmbp-0924/tracksync/REHEARSAL.md`.
✓ DONE 2026-09-17  BRANCHCENSUS  **206 of ~430 executor branches have a tip on NO track branch — JUDGED, all 206 lines, 177 rows after deduping local/origin pairs at one sha, none UNJUDGED. Table: `docs/dev/evidence/branch-census-0917/VERDICTS.md`.** COUNTS: **123 FOLDED-BY-HAND** (85 of them byte-identical by patch-id — the seat's line probe was measuring context drift, not loss: 14 of the 26 low-probe rows are verbatim folds) · 19 SUPERSEDED · 7 DOCS-SUPERSEDED · 5 WIP-SURVIVOR (all five re-done, cited) · **19 LOST + 4 DOCS-LOST = 23 rows, 21 distinct pieces of work, 19 of them SHARED code or shared docs.** THE ANSWER TO PETER'S QUESTION: the loss is almost entirely the SHARED layer built under one board's focus — only 4 of 23 are board-only — and it is not a slow leak: **14 of the 23 are ONE DAY, 2026-09-13**, the orin session that cut nine executors under the R56 cap, had all nine report DONE and gated, and ran out of weekly credit between the last report and the fold. That session DID write a full handoff (branch table, ledger-id collision resolution, six-item OWED-AT-FOLD list) — on `hw-jetson`'s copy of `docs/dev/OS/orin-queue.md`. `main`'s copy of that same file is still at 2026-09-12 and names none of the fourteen. OLDEST LOST: `exec-orin17-bootfocus` fdcba15d, 2026-09-06 — 122 lines of `main.rs` whose finding, SO14, is STILL OPEN in `docs/dev/LEDGER.md:67` with *click the shell before typing* as its workaround. FONTS2X ad62cf09 is LOST→IN-FOLD (hw-rmbp, QUARRYCLICK) — not re-homed here. JOBS: 16 `-RESCUE` bullets in this file (§1, §2, §5), 3 in `docs/dev/OS/orin-queue.md`, 1 in `docs/dev/OS/rmbp-queue.md`. ON THE RULE CANDIDATE: the proposed close-out rule would have made 21 of 23 VISIBLE and PREVENTED none, and it misses the two failures that actually happened — (a) it does not say WHERE the close-out is written, and the 2026-09-13 session obeyed its substance perfectly on a track branch the trunk never reads; (b) it keys on a branch NAME, and MATRIXPAR is two different tips (`ce0d992b` on origin, `5c1f13d0` local) under one name. **What catches them is a gate, not a discipline:** teach `unaos/scripts/ledger-check.sh` (or a sibling `branch-check.sh`) to walk every `refs/heads/exec-*` and `refs/remotes/origin/exec-*` PER REF, RED when a tip is an ancestor of none of main/hw-rmbp/hw-jetson/hw-pi4 and older than a stated window, with a registered-exception line in the shape GATE-LEDGER already uses. A rule a seat must remember fails exactly when the seat runs out of credit, which is when these fourteen were lost. Owner: unassigned — this is the one job the census itself did not do.
✓ DONE 2026-09-22  BRANCHGATE  (`exec-rmbp-branchgate`, parent `2fc9c71c` — re-derive the sha at the land; one new script + docs, NO kernel bytes: `git diff --stat 2fc9c71c HEAD -- unaos/crates unaos/arroyo` is EMPTY): **THE ROW ABOVE ASKED FOR A GATE RATHER THAN A DISCIPLINE, AND THIS IS IT — an executor branch is now CLOSED BY A LINE IN GIT, and an unaccounted tip REDS `./arroyo check`.** `unaos/scripts/branch-census.sh` (GATE-BRANCH, called by `ledger-check.sh` so no seat has to remember a second gate) walks every `refs/heads/exec-*` and every `refs/remotes/origin/exec-*` PER REF — 548 today — and REDs on a tip that is an ancestor of no track branch (main · hw-rmbp · hw-jetson · hw-pi4, local and origin) and is named by no line in the new registry `docs/dev/exec-branches.txt`. PER REF, never per name, because MATRIXPAR was two different tips under one name. THE REGISTRY IS SEEDED with all 177 judged rows of the census = **206 lines, which is every unreached exec ref in the repo**, five fields each (`<ref> <tip-sha> <state> <fold-sha-or-owner> <date>`, states IN-FLIGHT · FOLDED-BY-HAND · SUPERSEDED · WIP-SURVIVOR · LOST), plus 9 IN-FLIGHT lines for today's rescue executors. IT KEEPS ITSELF HONEST IN BOTH DIRECTIONS: a closed line whose ref has moved is STALE-RED (the verdict is about a sha that is no longer there), an IN-FLIGHT line may only move FORWARD off its recorded cut and REDs at 14 days, a FOLDED-BY-HAND sha that is on no track REDs (a fold nobody can fetch is not a fold), and **a LOST line whose ref later lands on a track REDs until somebody writes the fold sha** — so the allowlist is falsifiable by its own entries coming true. GO-RED, all executed and reverted: the seeded file → `owed=41 red=0` rc 0, `UNAOS_BRANCH_STRICT=1` → `red=41` (41 REFS = the census's 23 LOST ROWS: a row naming a local and an origin ref is two refs, the MATRIXPAR point restated by the gate), then `exec-orin-fonts2x` (+origin) marked **FOLDED-BY-HAND 582278cd** — QUARRYCLICK's real carry onto hw-rmbp — → `red=39` and FONTS2X names itself in no finding. Eleven more mutations, one per red class, each RED and each reverted; the registry moved out of the tree → `NOT ARMED`, rc 0, and it says so; the verdict neutered on a scratch copy of the script → rc 2 NO VERDICT, never a green. 0.5 s wall for 548 refs (the first cut was 9.4 s; one `git rev-list` over the 5,322 track commits replaced ~600 `merge-base` calls), printed in the census line every run. **DEFAULT POSTURE, and it is a choice: LOST is OWED, not RED.** A LOST line is a judged, owned debt that names its queue and PRINTS ON EVERY RUN; the silent kind — a tip nobody has judged — is red out of the box. Redding 39 already-written-down refs by default would paint every seat's `check` red for work that is already in a queue, which is how a gate teaches people to skim it; `UNAOS_BRANCH_STRICT=1` arms them, and arming strict by ancestry (trunk enforces, tracks defer — GATE-LEDGER's own shape) is the next cut once the queues have worked the debt down. **OWED NOW: 39 refs = 22 rows still LOST**, every one of them a `· NEW …-RESCUE` row in this file or in an OS queue, and the gate prints all 39 with ref, tip and subject on every `./arroyo check`. LAWS §2 carries the rule, NOMINATED — Peter's word pending. SR17 in `docs/dev/LEDGER.md`.
✓ DONE 2026-09-16 (TESTROOTS, `exec-rmbp-testroots` off `11ca67f1` — re-derive the sha at the land; scripts + `arroyo` + docs only, NO kernel bytes): **THE SWEEP FOREMAN-ROOT'S OWN ROW SAID WAS OWED — AND THE TREE WAS RUNNING 2 OF ITS 59 TEST SUITES.** FOREMAN-ROOT closed the unnamed-test root for ONE file and wrote that "the sweep of the rest of that class (every `#[test]`, `tests/*.rs` and script suite no verb names) is owed". MEASURED FIRST, over the whole repo at `11ca67f1` and before a line was written: **59 suites, 2 run by anything at all** — `midden_core::unit` (GATE-CORE) and `foreman::agreement` (GATE-FOREMAN) — **57 named by nothing**, among them all 19 `unafs` suites (the filesystem's whole KAT and logic battery), `net::unit`'s 29 TCP assertions in a crate the kernel LINKS, `rast::golden` (the "same scene ⇒ same pixels on both arches" oracle), `bandy::smessage_kats` (97), `helm::rover_invariants` (13), `mbench.py --self-test` (38 scenarios — the scorer EVERY bench verdict in this tree quotes, runnable by hand for months and run by no gate), and `tools/foreman/tests/preflight.rs`, **the sibling of the file FOREMAN-ROOT fixed, in the same directory, still unrun after that cut**. That last one is the argument for a gate rather than a fix, in one line. TWO NEW LEGS. **GATE-TESTROOTS** (`unaos/scripts/test-roots.sh`, GATE-ROOTS'/GATE-SPECROOTS' shape at the third layer): every crate with a `#[cfg(test)]`, every `tests/*.rs`, every script suite must be run by a `cargo test` line of `arroyo`'s CODE, or declare a `# RUN-BY:` header, or be REGISTERED BY NAME in `unaos/scripts/tests.registry` — comments stripped, backslash continuations joined, and **the target filter honoured**, because `-p foreman --test agreement` runs agreement and NOT preflight and a parser that read `-p foreman` alone would have declared this class closed while preflight stayed unrun. **GATE-HOSTTESTS**: one leg, two `cargo test` invocations plus mbench's self-test, on an EXPLICIT `-p` list — not `--workspace`, because `--workspace` cannot pass on a bench without the native -devel packages (a BUILD failure takes the whole leg, not a skip) and because an explicit list is the RATCHET a new crate's suite reds against. RESULT: **49 GATED, 10 REGISTERED, 0 ORPHAN; 75 `test result: ok` lines, 533 assertions across the 47 suites that carry any, and mbench's 38 scenarios now run inside `check`**, for 66 s warm / 164 s when the harnesses rebuild (quoted in the leg; under the ~2 min that would need a strict knob). Go-red proven three ways and reverted: an EMPTY `tools/sentinel/tests/orphan.rs` reds by name, the same file under `crates/rast/` correctly resolves GATED (the leg really does run it — the pair is what shows the resolver answers the question rather than a proxy), dropping `-p lux` reds at `lux::decode`, and `TESTROOTS_REGISTRY=/dev/null` reds naming all ten. **THREE FINDINGS HANDED ON, none fixed here (each its own cut, all in `tests.registry` with the command that measured it):** (a) `vaire::unit` is GREEN at `cargo test -p vaire` and **RED 63/64 the moment TMPDIR is pinned inside the tree as LAWS §Scratch requires** — `repo::tests::plan_names_an_unreadable_source_instead_of_failing` makes a tempdir that is deliberately not a git repo, and a tempdir inside this worktree IS inside one, so git discovery walks up and the assertion gets `Clone` where it wants `SourceUnreadable`; the test is right about the behaviour and wrong about how it obtains a non-repo, and 63 real assertions are hostage to it. (b) FIVE crates' suites (7 in all — `resonance`×3, `stria`, `phonolite`, `aether`, `una`) cannot BUILD without alsa / OpenSSL / GTK -devel headers; `aether/src/engine_tests.rs` alone is 62 `#[test]` fns over the cascade, the net layer and the renderer that have never been observed green OR red here. (c) `run_test.sh` at the repo root asserts nothing, returns `cat`'s exit status, can never fail, and writes `/tmp` — a name that reads as coverage and is none. **THE TRUNK LESSON: a gate that only MEASURES is satisfiable by registering everything, and a runner with no gate is a list that rots — the two legs are one instrument and neither is worth landing alone.** Section in `docs/dev/STRUCTURAL_GATES.md` (GATE-TESTROOTS + GATE-HOSTTESTS) with invariant, controls, go-red and the shrink rule on the registry.
✓ DONE 2026-09-16 (FOREMAN-ROOT, `exec-rmbp-foremanroot` off `f8f8ce8c` — re-derive the sha at the land) — **A CHECKED-IN TEST THAT NO GATE INVOKES IS THE THIRD UNNAMED-ROOT SHAPE, AND IT HAD BEEN RED FOR WEEKS.** `tools/foreman/tests/agreement.rs` asserts that `tools/foreman` (a Rust re-implementation of `unaos/scripts/mbench.py`'s spec grammar and verdict table) agrees with mbench byte-for-byte — exit code AND rendered table — and `foreman` was named in NO line of `unaos/arroyo`, so `./arroyo check` never ran it. Measured at `f8f8ce8c` with one bench capture staged at `unaos/target/serial.log`: `cargo test -p foreman --test agreement` rc=101, **red on 21/21 pairs**, on three strings mbench had learned and foreman never did — QEMU-FAST's `[mode unknown: …]` verdict-line suffix (the three-valued capture-mode read every consumer must treat as NOT-FULL), and SPECRUN's ` (pinned at <spec>:<line>)` coordinate and the `FIRST-SHORTFALL <spec>:<line>` line, which `test`/`test-fat`'s own tails quote verbatim so the verb and the table can never disagree about which pin came up short. All three taught (mbench stays the source of truth; foreman follows), and the leg named: **GATE-FOREMAN**, a `check_both` leg on the GATE-CORE pattern, documented in `docs/dev/STRUCTURAL_GATES.md` with its invariant, control, go-red and update path. Now `agreement: 21 (log, spec) pairs — exit code and verdict table identical`, rc=0. Go-red proven twice by mutation and reverted: dropping the mode suffix reds 21/21, dropping the pin reds 14/21 — the two counts differ because the suffix is on every verdict line and the pin only on a row that came up short, which is the distinction the pin exists to make. **THE TRUNK LESSON, and it is the generalisation rather than the fix: GATE-ROOTS closes the unnamed root for BINARIES and GATE-SPECROOTS for SPECS; a TEST nobody invokes is the same defect one layer up, and strictly worse than no test, because it reads as coverage in the tree while catching nothing. The sweep that is owed is the rest of the class — every `#[test]`, `tests/*.rs` and script suite in this repo that no verb names.** ⚠ **AND THE ALTERNATIVE THE NEXT SEAT SHOULD WEIGH: NOTHING AND NOBODY RUNS `foreman`.** Measured unfiltered, not sampled — `grep -rl foreman` over the whole bench directory with scratch worktrees excluded returns **5 files and zero invocations**: `tools/media-writer.sh` and its two dated backups cite `tools/foreman/src/verdict.rs` for their exit-code contract and call nothing, one 2026-08-18 capture header uses "foreman" as an executor's ROLE name, and the `flash/rmbp` MANIFEST hit is inside a commit-log field. No verb in `arroyo`, no playbook line, no MANIFEST command, no README. Its ONE live claim on this tree is its preflight: `foreman` refuses look-around, which is why LAWS §5 requires every `.spec` here to be look-around free and why pi's specs are deliberately kept that way. So R16's "two tables, one job" case is real and open — but (a)+(b) were done regardless, because a red unrun test is the defect either way and a retirement is Peter's call, not an executor's.
✓ DONE 2026-09-16 (QUEUEGATE, `exec-rmbp-queuegate`, parent f8f8ce8c — re-derive the sha at the land; scripts + docs only, NO kernel bytes): **THE FOLD SHAPES THAT COST A SEAT A DAY WERE INVISIBLE TO EVERY GATE, AND THE COMMON CAUSE IS THAT A CLEAN AUTO-MERGE IS NOT A CORRECT ONE.** One fold of 12 branches produced six defects and `ledger-check.sh` was rc=0 at every step: a STATE line DUPLICATED as a prefix-variant (1878e035); three METAL rows spliced away by a helper that grepped both copies into one 64 KB line (X86BIND + AHCIFLY at fb29c268, AHCIWRITE at a84c8f32, rebuilt from history at 60c1954d); a merge committed WITH conflict markers because the resolving chain did not stop on its own failure (b7d39af5); ledger rows whose status text landed AFTER the final pipe, which the FIELD COUNT check cannot see when the count happens to match (`git show 754f9107:docs/dev/OS/rmbp-ledger.md | sed -n '21p;123p'`); a `.rs` union three closing braces short from git's common-suffix dedup; and an `arroyo` with a lost `return 1`/`fi` from the same dedup. FIVE GATES NOW, each with a control and a go-red by mutation on a scratch copy: GATE-LEDGER's FOLD SHAPES block adds STATE-LINE UNIQUENESS (whole-file prefix test — 7 of the last 80 hw-rmbp commits red, all one defect; 0 on all eight track heads), ROW CONTINUITY (armed only at a fold, rows in BOTH parents, `DROPPED <id>` escapes — 3 findings over 60 merges, all real; the wider rule found 6 and 3 were rewords, dropped) and TRAILING TEXT; `./arroyo check` gains GATE-BRACES (199 `.rs` under `crates/`, parser validated over 402) and GATE-SELFSYNTAX (`bash -n` over `arroyo` + 22 scripts, **13 of which are named by no leg of `check`** — the media builders and the bench-connect scripts, so a defect there surfaces at the bench with a board attached). ⚠ TWO HONEST RESIDUALS, both measured and both in `docs/dev/STRUCTURAL_GATES.md`: the `arroyo` half of GATE-SELFSYNTAX is a TAUTOLOGY on today's file (bash's loader already refuses it whole, rc=2, because the `case` dispatch is the last command — it stops being one the moment an early `exit` is added); and GATE-BRACES runs at the TOP of `check_both`, not beside GATE-APPEND where it is written up, because `set -e` at `unaos/arroyo:24` ends the script on a failing compile leg and a brace gate placed below could never fire for its own defect class. LAWS §3 Folds is the rule; every other seat's fold gets the same three checks the next time it merges.
✓ DONE 2026-09-15 (opened by QUARRYX86 as `· NEW` — it BLOCKED that executor's only gate command and every other one arming `UNAOS_WC=1`; FIXED by SMALLFIX3, first commit on `exec-rmbp-smallfix3` off `dbfbff85` — re-derive the sha at the land) — **`banner-cert` reds every `esp-x86` / `test-fat` build that arms `UNAOS_WC=1`, and the cause is the ROW, not the build: the `wc` witness string it greps for is inside a function the linker drops.** `UNAOS_WC=1 UNAOS_QUARRY=1 UNAOS_QEMU_FULL=1 ./arroyo test-fat sf 200` on `6f45030c` exits 1 at `arroyo:6140` with `feature=wc witness=[wc-x] activate DECLINE reason=fb-not-ready latch=released hits=0 -> MISSING` and `❌ banner-cert: the banner and the artifact DISAGREE`, before QEMU is ever started. The row is `scripts/banner-cert.sh:94` and the string lives at `video/desktop_uefi.rs:388`, inside `activate`. MEASURED on the produced ELF (`target/x86_64_esp/kernel.elf`, `LC_ALL=C grep -a -c -F`): `"[wc-x]"` = 26 hits, so `wc` IS compiled and printing; `"wc-x] activate"` = 0 and `"activate REFUSE reason=already-active"` = 0, so EVERY string in that one function is absent — the whole of `desktop_uefi::activate` is eliminated in this configuration, not the feature. So this is a MISSING that means "the certifying string was chosen from a function this build does not keep", which is a different fact from the banner lie the gate was built for (§5's `esp-jetson` spelling row) and is indistinguishable from it in the output. It is NOT a regression from VIDSMALL's green run: BANNERCERT (`06eafc01`, `94870154`) is not an ancestor of `3291384b`, so VIDSMALL's identical command never met this gate. Two honest shapes, unchosen here because `scripts/banner-cert.sh` and `video/desktop_uefi.rs` are outside QUARRYX86's file set: (a) re-key the `wc` row on a string the artifact actually keeps — 26 candidates — which fixes the gate and leaves the second question open; or (b) ask why `desktop_uefi::activate` is unreachable on the x86 panel path when `wc` is armed, which is the more interesting question and may be a real finding about the UEFI activation seam. WORKAROUND USED (stated so the next executor does not re-derive it): `esp_x86` writes `target/x86_64_esp/kernel.elf` and `target/x86_64_data/` BEFORE the gate runs, so the packaged media survives the exit; `bash scripts/make-fat-img.sh sf` then `UNAOS_FATIMG=sf ./arroyo test <secs>` is `test_fat`'s body minus the already-completed `esp_x86` step and gives the same boot. Owner: whoever holds `scripts/banner-cert.sh` next (BANNERCERT2 is cut and is the natural home). **FIXED by SMALLFIX3 with shape (a), and the diagnosis is SHARPER than the row above: the row was not rotted, it was MEASURED CORRECT ON THE WRONG BUILD.** `activate` has exactly ONE caller — the Kepler takeover, and `main.rs:1141` says so in prose — and the row was seeded against the rmbp flight-7 knob line, which arms `UNAOS_KEPLER_TAKEOVER=1`. Re-measured this arc on TWO x86 artifacts off the same tip: flight-7 keeps the function (`wc-x] activate` = 6, the row's token = 1) and a no-kepler `UNAOS_WC=1 UNAOS_QUARRY=1` build drops it (both 0), while `[wc-x]` = 26 in BOTH — the control that proves the feature was compiled and printing either way, so this was never a banner lie. Re-seeded on `[wc-x] desktop-app DECLINE reason=no-storage name=/` from `desktop_uefi::desktop_app_service`, whose call site (`main.rs:5999`) is gated `#[cfg(feature = "wc")]` and nothing more, so the literal exists on every build that arms the knob and none that does not; `cond` stays `-`, state `measured(1)`, and it measures 1 on BOTH artifacts. Two new recipe steps in `scripts/banner-cert.sh` (7a/7b): a row's token belongs in the path the feature's OWN knob makes live with nothing else armed, and a row measured only on a rich knob line is measured on the configuration least likely to expose this. Question (b) — why `activate` is unreachable when `wc` is armed — is ANSWERED and is not a finding: it is reachable exactly when its one caller is, and on x86 the compositor's ignition IS the Kepler takeover (CLAUDE.md says so), which QEMU has none of. **THE 48-ROW AUDIT THIS ARC OWED: no second row has that shape.** All 28 flight-7 banner features certify (27 with hits>=1; the 28th, `unaos_ivb`, is an `@boot ` row certifying in `EFI/BOOT/BOOTX64.EFI`), and the 20 aarch64-only rows measure 0 on both x86 artifacts and ride no x86 banner. ⚠ **ONE FINDING FROM THE AUDIT, REGISTERED NOT FIXED, AND IT RUNS THE OTHER WAY: `witness` IS NOT CERTIFIED ON THE FLIGHT-7 LINE AND ITS TOKEN IS THERE.** Its cond `!baremetal,!bootlog,!usbdebug,!tegra` (BANNERCERT2, copied from `main.rs:79`'s `cfg_attr`) makes the flight-7 line print `-> UNVERIFIABLE (its literal needs 'NOT usbdebug')` and grep nothing — but the literal measures **1 hit** on that artifact, because the usbdebug terminal loop that would delete the post-GUI tail is itself compiled out on x86+`wc` (`main.rs:1155`). So the cond over-refuses on exactly the configuration the rMBP flies: coverage narrower than the table claims, not a red and not a MISSING. The fix needs a cond term meaning "`usbdebug` only kills this literal when NOT (x86 and `wc`)" — an arch-aware conjunction the cond grammar does not have, and adding one is a gate-language change, not a row re-seed. ~~**OWED: whoever next extends the cond grammar.**~~ ✓ **DONE 2026-09-15 (CERTCOND, this commit on `exec-rmbp-certcond`, parent `bc9cf442` — re-derive the sha at the land): the grammar gained the term, the row is re-seeded, and the flight-7 cert reads `ok=28 … unverifiable=0`.** The new term is `!a@except:<t>+<t>…` — `a` makes the literal unreachable EXCEPT on a build satisfying EVERY term after `@except:`, each term an ARCH (`x86_64`/`aarch64`) or a FEATURE, `+` meaning AND because the spec is ONE configuration copied out of the source's `not(all(…))`. **THE ARCH IS READ FROM THE ARTIFACT'S OWN ELF HEADER** (`e_machine`, two little-endian bytes at offset `0x12`; `0x3E` x86-64, `0xB7` aarch64), never from an argument, so a cond can be wrong about the SOURCE — which a human can check — and can never be wrong about the BYTES UNDER TEST. A flat `kernel8.img` has no header: the caller may name the arch in a new optional argument 3, and the census line prints which of the two happened on every run; with neither, the arch is `unknown`, arch terms are UNSATISFIED and the row stays UNVERIFIABLE rather than guessed. The `witness` row is now `!baremetal,!bootlog,!usbdebug@except:x86_64+wc`,`!tegra` state `measured(1)`, and on the flight-7 line it reads `hits=1 -> OK`; summary before → after on the same command: `ok=27 … unverifiable=1` → `ok=28 missing/leak=0 registered-divergences=0 unverifiable=0`. ⚠ **A BARE ARCH TERM (`!usbdebug@aarch64`, "only kills it on aarch64") WAS THE OBVIOUS SPELLING AND IS WRONG IN THE REDDENING DIRECTION** — `main.rs:~1155` gates the loop on `all(feature = "usbdebug", not(all(target_arch = "x86_64", feature = "wc")))`, so it still compiles on x86_64 WITHOUT `wc` (the knob's original purpose), and a bare-arch term would call that build's `witness` row checkable, find the literal correctly absent and print a FALSE MISSING where today's over-refusal is only a loud silence (LAWS §5, wrong-strict is worse than wrong-lenient). **AND THE RULE THAT CAME OUT OF IT, now step 6b of the script's recipe: a `cfg_attr` IS A LINT DIRECTIVE, NOT THE GATE.** `main.rs:79` is an `allow(unreachable_code)` and is deliberately coarse; the gate is each feature's own early-exit, and one of its four names is narrower than it says. A negated term is seeded from the `#[cfg]` on the code that does the deleting; a `cfg_attr` or a comment is a POINTER to that code, never the source. GO-RED, three mutations on the SAME artifact with no rebuild: (a) wrong arch in the exception (`@except:aarch64+wc`) returns the row to UNVERIFIABLE with the census naming *"the artifact's arch is x86_64, not aarch64"*, rc **0** — deliberately not a red, because an UNVERIFIABLE has never been one in this gate and must not become one; (b) an unparseable cond (`@onlyon:`) is `❌ NO VERDICT` naming the row and the term BEFORE any verdict is printed, rc **2**; (c) the control, the `sdwrite` shape with one letter off a token, still rc **1**. Three more probes on the arch READER, because a new input channel never fed a wrong value is an unchecked one: a FLAT copy of the same image (`llvm-objcopy -O binary`) with no arch argument reads `arch=unknown` and the row declines (UNVERIFIABLE, rc 0 — the conservative direction on purpose); the same flat image with `x86_64` as argument 3 certifies `hits=1 -> OK`; and the ELF with a LYING `aarch64` argument prints `⚠ argument 3 says 'aarch64' and the ELF header says 'x86_64' — the HEADER wins` and certifies anyway. That last one is why the arch is read and not passed. **THE SWEEP THIS OWED: `witness` was the ONLY over-refusing row, and after the fix there is none.** All 48 measured against the SAME flight-7 ELF this arc built and cross-read against that build's banner: the EIGHT other rows whose cond goes unsatisfied there all measure 0 hits and are the `tegra` family (`bsptick`, `bsprun`, `sdmmc`, `ga10bprobe5a`) and the `baremetal` family (`smp7`, `vugpar`, `genet`, `nettest`), none of them on an x86 banner — the OFF side working, not the same defect. All 28 rows the banner DOES name measure >= 1, `unaos_ivb` included at 1 hit in `EFI/BOOT/BOOTX64.EFI` where its `@boot ` token routes it. ~~⚠ NOT TAKEN, one line outside this arc's file set: `arroyo`'s `kernel8` call site passes two arguments, so the flat image certifies with `arch=unknown`.~~ ✓ **DONE 2026-09-15 (PTRPRESS-K8ARCH, branch `exec-rmbp-ptrpress` off `c516ea5b` — re-derive the sha at the land): `kernel8()` passes `aarch64` as argument 3 and the census line on a real `kernel8` build now reads `arch=aarch64 (FLAT image (no ELF header); the caller named this arch in argument 3)` with the cert still rc=0.** The other two call sites pass ELFs and are right to pass nothing — the script reads `e_machine` at 0x12 there and an argument cannot lie about the bytes. Nothing regressed before this (the only arch-qualified term is on `witness`, whose cond hits `!baremetal` first on every Pi build), so this is the hole closed BEFORE a Pi row needs it rather than after. Section: `docs/dev/STRUCTURAL_GATES.md` GATE-BANNERCERT. **AND A SECOND ROW OF THE SAME SHAPE, FOUND THE SAME DAY AND FIXED IN THE SAME COMMIT — `smolnet`** (datum from SMALLFIX, measured twice; recorded HERE rather than as its own row so the two do not duplicate at the land, because they are one defect class, one gate and one commit): `UNAOS_IVB=1 ./arroyo esp-x86` with no other knobs exits 1 with `feature=smolnet witness=:: SOCK-3: no free address-space slot hits=0 -> MISSING`. The literal is in `arch/x86_64/syscall.rs::sock3_launcher`, correctly gated `all(feature = "smolnet", target_arch = "x86_64")` — but its only callers (`main.rs:902`, `:909`) are `all(target_arch = "x86_64", feature = "witness", feature = "smolnet")`, and `witness` is OFF for every media verb, so a DEFAULT-ON feature that rides every x86 media banner had its certifying string in a function no media build links. `smoltcp` = 137 hits on that same ELF is the control proving the feature was compiled. Re-seeded on `:: SOCK-1: smoltcp icmp echo` from `smolnet::witness_tick`, whose caller `drivers/e1000.rs:1194` (inside `service_net`) carries that same smolnet+arch gate and nothing else. BOTH POLARITIES MEASURED: 1 hit on three x86 artifacts (the witness-free `UNAOS_IVB=1` build, flight-7, and the no-kepler `UNAOS_WC=1` build); 0 hits on an `esp-arm` artifact where `arm_features` strips `smolnet`, with `smoltcp` = 0 there as the corroborating control. Two rows, one shape, one day — which is what made 7(a)/7(b) rules in the script's recipe rather than a note about `wc`.
· ✓ REGISTERED (re-derived 2026-09-24: `docs/dev/FIXTURE_FLAKES.md` §1d carries DOCKID with its mechanism, FLAKEFIX rmbp-ledger B150, measured 2026-09-22) — was: NEW 2026-09-15 (PARTINSTALL, `exec-rmbp-partinstall`, executor finding — measured, not inferred): **DOCKID is FLAKY on the x86 `test` leg and the flake register does not know it.** `:: DOCKID: tiles=5 closed=win2 reopened=win2 recycle=true order=true set=FALSE furniture=true count=true/5 pins=true/2 press=yes :: FAIL ::` reddened ONE run of four with a byte-identical command (`UNAOS_WC=1 UNAOS_INSTALLDEMO=1 UNAOS_AHCI=1 UNAOS_PART_DISK=… UNAOS_QEMU_FULL=1 ./arroyo test 120`) on a tree whose only changes were under `install/` — no video path, no shared `video/` file, no arch or asm change. It PASSED on the other three runs of the same command and on the immediate lone re-run that LAWS §5 prescribes, so the population is 1 red in 4, not 1 in 1. **`set=` is the ONE sub-assertion that moved**; every other field (`recycle`/`order`/`furniture`/`count`/`pins`/`press`) held at its passing value in the red run, which is the useful half of this row: whoever picks it up is looking for a race in the SET half of the dock's tile bookkeeping, not in the whole fixture. **Why this matters beyond one afternoon:** a fixture that reds 25% of the time on an unrelated diff trains every executor to re-run and move on, and the next seat cannot tell a real dock regression from this. It also costs a full QEMU wall each time it fires. TWO JOBS, and the first is cheap: (a) reproduce it by looping the x86 `test` leg on an UNCHANGED tree and counting — if it reds there too, it is unconditional and belongs in `docs/dev/FIXTURE_FLAKES.md` with its rate; (b) if it does NOT red on an unchanged tree, the trigger is load or layout and (a)'s count is the control that says so. **Deliberately NOT filed in FIXTURE_FLAKES.md by this executor**: that register is for measured flakes with a rate, and 1-in-4 from one afternoon's four runs is an observation, not a rate — the seat files it once (a) has a number. Evidence is the PARTINSTALL commit's own report (4 runs of the command above, 1 red); no capture is cited here because the logs are scratch and a queue row may not point at scratch — reproduce with (a), which is the measurement this row actually wants. **✓ (a) DONE 2026-09-16 (FLAKERATE, `exec-rmbp-flakerate`, tree `da2a9abc`) — DOCKID DID NOT RED: 0 in 4.** Four `UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 90` runs, sequential, one worktree, UNCHANGED tree, at 1-min load 7.90 / 15.55 / 15.18 / 15.09 with a **20.57** peak observed in-run, printed the IDENTICAL line every time: `:: DOCKID: tiles=5 closed=win2 reopened=win2 recycle=true order=true **set=true** furniture=true count=true/5 pins=true/2 press=yes :: PASS ::` — the `set=` field this row correctly isolated as the one that moved held at its passing value in all four. So **(b) is the branch that fires: the trigger is load or layout, not the fixture being unconditionally flaky**, and (a)'s count is the control that says so. The control is armed, not vacuous: the same four captures carry 3 reproducible `-> FAIL` lines each (the ruled X86BIND default-leg condition), so the leg can and does go red. **SCOPE, stated because it bounds the claim: my command is NOT PARTINSTALL's.** Theirs was `UNAOS_WC=1 UNAOS_INSTALLDEMO=1 UNAOS_AHCI=1 UNAOS_PART_DISK=… UNAOS_QEMU_FULL=1 ./arroyo test 120`; mine drops `INSTALLDEMO`/`AHCI`/`PART_DISK` and shortens the wall. 0-in-4 therefore refutes 'unconditional on the x86 `test` leg' and does NOT refute 'flaky under the install-demo/AHCI configuration' — whoever takes the dock race next should re-loop PARTINSTALL's exact command. **Still deliberately NOT filed in FIXTURE_FLAKES.md**, and now for the opposite reason: there is no rate to file. The x86 leg's 0-in-4 is recorded as that file's CONTROL instead (Class 5's closing section), where it also carries 0-in-4 for `[dmgovlp]`, `[ptrdead]`, SOCK-4, DMG-REFUSE, SERWIT-1, PWRDRAIN, S5DRAIN, SINKDRAIN, DOCK and APPPIN at the same loads. Full table and quotations: `docs/dev/evidence/rmbp-0915/flakerate/FLAKERATE.md` §2 and §4.**
✓ DONE 2026-09-15T23:xxZ (SINKDRAIN, exec-rmbp-sinkdrain 091a11a6: `power_drain` now writes `_print`'s sink set via `serial_ring::sink_write`; `:: SINKDRAIN: staged=8 drained=8 on_cable=8 -> PASS ::`, the tally on the cable) — was: · NEW 2026-09-15 (RBTDRAIN, rmbp executor finding, MEASURED on the cable, not reasoned): `serial_ring::power_drain` EMPTIES THE STAGING RING INTO A PORT THE rMBP DOES NOT HAVE. It writes both the drained lines and its own `[<family>] ring drained lines=N bytes=M` tally through `crate::arch::serial::raw_write_str` — on x86 the 16550 at 0x3F8 alone — and never through `_print`, whose sink SET also includes the FTDI mirror ring that is this laptop's only console. So PWRDRAIN (SO31) and S5DRAIN (SO39) are correct everywhere there is a UART and are a no-op for the reader on the one board whose ledger row (`rmbp-ledger` A3) is about losing the wire: a line DEFERRED into the staging ring under contention is CONSUMED by `power_drain` into the dead port before any FTDI flush can see it. The evidence is one line apart in the same fixture run: with the FTDI flush wired, the cable's tail carries `[pwrreboot] reboot verb invoked …`, `… x86 mechanism: FADT RESET_REG ladder …` and `[pwrreboot] ftdi flushed bytes=210 transfers=4 exhausted=0`, while `[pwrreboot] ring drained lines=0 bytes=0` appears ONLY in `target/serial.log` (`tail -c 600 ~/unaos-bench/scratch/rmbp-0915/rbtdrain-logs/cable-green.log | tr -d '\000'` vs `awk 'index($0,"[pwrreboot]")' target/serial.log`). RBTDRAIN (rmbp `exec-rmbp-rbtdrain`) carries everything `_print` reached the mirror ring with — every line on a quiet path, and the verbs' own announces on any path — and deliberately does NOT touch this: the fix is a SHARED-FILE change to `serial_ring.rs`, giving `power_drain` a sink that is `_print`'s SET rather than one arch port, and it must not silently re-enter `_print`'s locks on a power path (the PWRDRAIN reason `raw_write_str` was chosen in the first place). ⚠ NOT rMBP-only in principle: any board whose console is not the arch's raw port has it. Fixture to write with the fix: PWRDRAIN's existing `pwrdrain_selftest` proves the POLICY (a full drain empties the ring) and says nothing about WHICH SINKS received it — that second half is what has never been asserted. Doc: `docs/dev/OS/02_KERNEL_CORE/serial_transport.md` §RBTDRAIN
· ✓ LANDED (re-derived 2026-09-24 after TRACKSYNC — the tracks are one tree: `acpi_power.rs::poweroff` flushes as its first statement, LEDGER SO39/SO40 `fixed-unflown`; **SO41 stays OPEN**, its `mirror_service` call on the bsprun path is the next serial arc's) — was: FIXED ON hw-jetson, NOT YET LANDED 2026-09-12T2x:xxZ: both SERDRAIN findings are FIXED on `exec-orin-pwrdrain2` (S5DRAIN 30e736d6, WITNESS-GATING 3b784ed1), folded to hw-jetson daf38b01 as SO39 and SO40 — the x86 S5 flush went to the PORT (`poweroff()`'s own first statement) so no caller can skip it, and the three serial-ring fixtures are behind `witness`, returning 14,143 B ≈ 1.228 s of UART per boot. The two `· NEW` rows that stood here are REMOVED rather than left to duplicate at the land; hw-jetson carries them as ledger rows. **NEW AND OPEN — SO41, found while proving the second one:** `mirror_service` is NEVER CALLED on the Jetson (its two aarch64 call sites are `main.rs:4425`, `baremetal`-gated, and `main.rs:1779`, the shared BSP loop the Orin's `sched::run_bsp(0)` handoff at `main.rs:1494` diverges before). So `[mirror] N line(s) dropped` and `:: SERWIT-2 ::` cannot appear on a Jetson capture and a tap losing lines there is silent by construction. The fix is one call on the tegra/`bsprun` path. **Do not land it casually:** the Orin flight line carries `UNAOS_WITNESS=1`, so that call also arms the three ring fixtures on the card and spends the 1.228 s back — land it with that decided, and consider gating the fixtures on something narrower than `witness`. Owner: the next serial arc.
· ✓ LANDED (re-derived 2026-09-24 after TRACKSYNC: `knoboff` refuses a bare merge HEAD with exit 2 naming both parents, arroyo:11847, and holds an `flock` per caller scratch, :12240) — was: FIXED ON hw-jetson, NOT YET LANDED 2026-09-12T2x:xxZ: both `knoboff` findings this session (the NET6 one — the default baseline `HEAD~1` is the FIRST parent, so a merge tree was compared against its own pre-merge tip; and the GA10B4E/SO34 one — one shared scratch directory, so concurrent runs corrupted each other's verdict) are FIXED on `exec-orin-knoboff` 1c8397fa, folded to hw-jetson a2f9f4ee: a bare merge HEAD now exits 2 and prints both parents with their re-run commands (refusal, not a guessed parent — the right parent depends on merge direction and LAWS §3 sanctions both), and every caller gets its own keyed scratch with an flock and a kept run directory on failure. The two `· NEW` rows that stood here are REMOVED rather than left to duplicate: hw-jetson carries them as `✓ DONE` and they arrive here when it lands.
✓ DONE 2026-09-12T16:xxZ (orin session, on hw-jetson): `make-pi-img.sh` FATCLUST review objections answered — the guard is now `unaos/scripts/fat-clusters.sh`, which parses the WRITTEN BPB and asserts FAT32 by cluster count (go-red proven on the refused render13 image: `clusters=32440 kind=FAT16 -> FAIL`; green on card-v2 `256030`; NOT-A-BPB control rc=2), mkfs stderr is no longer captured, the numbers are corrected (256,030 at 127 MiB; 110,874 at 55 MiB), and A58 says fs/fat.rs MISREADS (FAT16 by count, no refusal). Lands with hw-jetson.
✓ DONE 2026-09-15 (LEDGERGATES, `exec-rmbp-ledgergates`, parent 2051470a — re-derive the sha at the land) — the refusal is in, over the queues AND the ledgers AND RULINGS (9 files in this tree, the count printed in the gate's census). Go-red by mutation with one marker shape per file in one run: rc=1, each named with its file, its line and the marker text. Reverted, rc=0.
· ✓ WRITTEN 2026-09-24 (FIXTURE_FLAKES.md, the placement-rule entry before "Adding an entry") — was: NEW 2026-09-12 (DRAGSTALL, executor finding, measured): the x86 witness ladder's "no fixture between them may lose an event" rule extends to SERIAL PRINTS — a line-neutral fixture appended at the head of `dmgovlp_selftest` cost that fixture its drag leg (`drag_evt=0 … -> FAIL`; baseline `drag_evt=5 -> PASS`); moved to `physwit_once`'s site it is green. Write it in FIXTURE_FLAKES.md or LAWS §5. Also: `./arroyo test`'s 20 s default does not reach the x86 witness ladder on a loaded host — a brief that gates on a ladder fixture needs `./arroyo test 90`.
· NEW 2026-09-13 (orin session, seat finding; Peter asked "i'm curious why sntp doesn't 'just work'" and then "that sounds like a hack"): **SNTP IS DRIVEN FROM A NIC DRIVER, AND THERE ARE THREE CLIENTS.** The wire format was factored out correctly — `net_sntp.rs` is shared and arch-neutral, `clock.rs` is shared, and FAT mtimes already derive from the clock (CLOCK-3 `f98fbfa4`) — but the DRIVING of it was stapled to a driver: the ONLY caller of `smolnet::witness_tick_sntp` is a statement inside `drivers/e1000.rs:1184 service_net`, guarded `all(feature = "smolnet", target_arch = "x86_64")`, beside its DNS and v6 twins. The Pi therefore did not inherit a client, it got a SECOND copy on genet (`941db4b2` PI-NET-16, later adopting the shared parser at `69e4627e`). The Jetson has NONE — `lib.rs:46-48` says in as many words that aarch64 "has no consumer yet" — which is why the menu bar's clock (`video/menubar.rs:49-50`, `CLOCK_GLYPHS`, and the honesty rule at :131-137 that draws NO clock while `clock::try_unix_now()` is None) has never drawn a single time on that board. Normal SNTP is a userspace daemon on a socket; an in-kernel client is ordinary for this stage, but the per-driver coupling is not, and it has now produced exactly the failure it was always going to produce. **A fourth client is being written** (orin `exec-orin-orintime`, arch-neutral on the NET6 socket surface, explicitly NOT hung off rtl8168's tick, its driving seam named and justified in its commit). **THE ARC THIS ROW IS FOR:** ONE client, three drivers under it. What blocks it today is that `pub mod net6` is gated `#[cfg(all(feature = "net6", target_arch = "aarch64"))]` (`net_phy.rs:797`) and only `rtl8168_tegra.rs:5733` and `virtio_net.rs:643` call `register_nic`, while x86 talks to smolnet directly; nothing in net6 looks intrinsically arch-specific (smoltcp behind a `NicOps` trait), so bringing e1000 onto it and retiring the other two clients is plausible but touches x86 boot and is its own arc. ORINTIME is reporting the file:line cost of that move WITHOUT attempting it. Peter rules on whether it is worth doing. Owner: unassigned.
· NEW 2026-09-13 (orin session, seat; measured, fixed on hw-jetson 2e48d721 — the FIX is orin-local, the LESSON and the GATE GAP are not): **A FEATURE GATE KEYED ON ONE SPELLING OF A VERB, AND THE BUILD LOG LIED ABOUT IT.** `unaos/arroyo:1147` armed the GA10B rung-5 ignition with `[ "${1:-}" = "esp-jetson" ]` — a single string equality — while the dispatcher accepts SIX spellings across the two jetson verbs (`esp-jetson|esp_jetson|jetson`, `esp-jetson-img|esp_jetson_img|jetson-img`, the second of which is a bare call to the first). Five of six therefore built a DIFFERENT feature set than asked, dropping the rung while keeping its parent feature, and the second step of a two-step chain OVERWROTE the first's kernel. **THE GENERAL HAZARD, which is not orin's:** the `⚡ kernel features:` banner printed the features the build BELIEVED it had and would have passed a reviewer reading the log; the artifact was empty. A card cut from it boots, prints nothing, and looks like a clean flight — a spent power cycle with clean-looking evidence. THE ONLY THING THAT CAUGHT IT was `LC_ALL=C grep -a -o -F` on the BUILT ELF (LAWS' artifact-certification rule), which is now worth its own line: **a build banner is not evidence that a feature is in the image; only the artifact is.** The correct pattern is already in this file's own tree — the `UNAOS_WITNESS` gate at `arroyo:49` enumerates its aliases in a `case`. **OWED, SHARED:** ✓ (a) DONE 2026-09-15 (rmbp BANNERCERT, this commit on `exec-rmbp-bannercert`, parent 9a7b09a4 — re-derive the sha at the land) — MEASURED, not read: `grep -n -E '"\$\{1:-\}" = |"\$1" = ' unaos/arroyo` at 9a7b09a4 returns exactly TWO lines and NEITHER is a live verb gate — `:1147`, which is the ⚠ prose quoting the already-fixed defect (the live gate beside it is a `case` over all six jetson spellings), and `:8507`, `if [ "$1" = "--build" ]`, a function-local argument test inside `battery`, not a verb gate at all. No single-spelling verb gate survives in this file; the measurement is recorded as a trailing note on :8507 itself. `esp-x86`/`esp-arm`/`kernel8*`/`test-*` reach their functions through the dispatcher's `case` and key on nothing else. ✓ (b) DONE 2026-09-15 (same sha) — `unaos/scripts/banner-cert.sh` plus ONE call at the end of `esp_x86`: for every feature the `⚡ kernel features:` line names, the produced `target/x86_64_esp/kernel.elf` must carry that feature's certifying string (`LC_ALL=C grep -a -o -F`), and five features the banner does NOT name must be absent from it. MISSING or LEAK = rc 1; a banner feature with no row in the script's token table = rc 2, a loud NO VERDICT, never a silent skip. Registry: 44 token rows — 28 measured against an armed x86 flight-7 artifact, 15 aarch64 rows `unmeasured-here`, 1 (`wedge2`) a NOWITNESS row carrying its reason. GO-RED proven by mutation (a wrong token for `nvidia-kepler-fifo` reds the verb, rc 1; an unknown banner feature gives rc 2); the gate's section is `docs/dev/STRUCTURAL_GATES.md` GATE-BANNERCERT. **AND IT CAUGHT A LIVE ONE ON ITS FIRST ARMED RUN, which is a NEW finding and is why the gate landed carrying one registered divergence:** 27 of 28 flight-7 banner features certified in the artifact and **`sdwrite` did not**. `arroyo:1992` appends `sdwrite` to `$_feats` unconditionally so the banner names it on EVERY verb, but the x86 MEDIA kernel is compiled by `builder/src/main.rs`, which composes its OWN feature list from env knobs and has no `sdwrite` entry (`grep -c sdwrite builder/src/main.rs` = 0). One `esp-x86` log prints both lists and they differ by exactly that name — arroyo's `⚡ kernel features:` ends `…,gmux_igd,sdwrite` (28), the builder's own `   kernel features:` ends `…,gmux_igd,smolnet` (27) — and in the ELF `SDWRITE-POSTURE` has 0 hits while `:: USBREG`, printed from the same `witness`-gated `unafsroot_selftest`, has 3. This is the `rastmc` defect `builder/src/main.rs:136-143` already records in its own comment ("printed in the feature banner, `strings` on the ELF had no `RAST-MC` in it`"), a SECOND time, on a different knob — the argument for a standing gate rather than a remembered grep. **FIXED IN THE SAME ARC, second commit on `exec-rmbp-bannercert`, the child of 06eafc01 — re-derive the sha at the land** — ruling: arroyo is right, `sdwrite` is meant to ride every image, so `builder/src/main.rs` gained `if std::env::var("UNAOS_NOSDWRITE").is_err() { feats.push("sdwrite"); }` beside its siblings in the knob-mapping block, with the file's own comment idiom recording that BANNERCERT caught it. The cert then reads **28/28** with `SDWRITE-POSTURE` hits>0, and the registered-divergence table is EMPTY (the mechanism stays; the row came out in the commit that fixed it, which is a row's whole intended lifetime). Consequence worth stating plainly: every x86 media image cut since A60 landed shipped without `sdwrite` while the build log claimed it. ⚠ THE CLASS IS NOT CLOSED: any knob in arroyo's map that `builder/src/main.rs` does not read is a banner lie on `esp-x86`/`vm-image`/`x86`, and this is its THIRD instance (rastmc, s42/INSTGUI+GMUX-IGD, now sdwrite). The gate now names them one at a time as they are armed, which is the point; a sweep of arroyo's map against the builder's list would name them all at once and is worth a row of its own. ✓ SWEEP DONE 2026-09-15 (rmbp BANNERCERT2, this commit on `exec-rmbp-bannercert2`, parent e06c4a61 — re-derive the sha at the land): `unaos/scripts/knob-parity.sh`, wired into `./arroyo check` as an EXTENSION of the existing `knob→builder wiring` probe — the same function, the same `return 1`, so `check` keeps ONE knob-wiring verdict instead of two that can disagree about which is authoritative; the script is its own file only so it can be run and gone red by hand. It needs no build. Three vocabularies, COUNTED BEFORE QUANTIFIED: (a) **146** names arroyo can append to `$_feats` — both idioms, plus the `case`-arm appends (`ga10bprobe5a`), the `&& { … }` compound ones (`bsprun`) and `esp_jetson`'s in-function forcing, which the old probe's `[ -n "${UNAOS_X:-}" ]` sed pattern sees NONE of; (b) **82** `feats.push("…")` names in `builder/src/main.rs`; (c) **185** features declared in `crates/kernel/Cargo.toml`. Differences: **a\b = 64** (every one knob-gated, most of them aarch64-only — `tegra`, the ga10b ladder, the whole orin family — where the build invokes cargo directly and the builder is not in the path at all: printed, quantified, NOT a defect and NOT red, because deciding which of those belongs in the builder needs the cfg matrix, which is the older probe's job), **a\c = 0**, **b\c = 0**. THE RED is the sdwrite class and nothing else: a name in (a) that is DEFAULT-ON (top-level, no positive knob — `[ -z "${UNAOS_NO…:-}" ]` or unguarded, so it rides every x86 media banner with NO environment set) and absent from (b). 5 of the 146 are default-on (`ehcihid`, `kbdwit`, `sdhcblk`, `smolnet`, `sdwrite`); all 5 are in the builder today, so the red set is EMPTY. GO-RED, and its first run is the lesson: commenting out the builder's `feats.push("sdwrite")` line left the script GREEN, because a plain `grep feats.push` matches the COMMENTED-OUT line — this gate's own failure mode seen from the inside, a name present in the file and compiled into nothing. The extractor now truncates every line at its first `//`; the drill then prints `❌ knob-parity: sdwrite — default-on in arroyo (arroyo:2000,2009 …) and NOT pushed by builder/src/main.rs` and exits **1**, and restoring the line gives rc 0 with an empty `git diff` on that file. Six control probes (`wc`/`sdwrite`/`tegra` in (a), `smolnet` in the default-on SUBSET, `wc` in (b), `witness` in (c)) exit **2** with NO verdict if a parser breaks — and `sdwrite` is deliberately NOT a builder-side control, so the drill produces a finding rather than a broken gate. Section: `docs/dev/STRUCTURAL_GATES.md` GATE-KNOBPARITY. ✓ (c) DONE 2026-09-15 (rmbp BANNERCERT2, the same commit) — **ALL THREE remaining media verbs are wired, and the 15 `unmeasured-here` aarch64 rows are measured against real artifacts.** `esp_arm` and `esp_jetson` certify `target/aarch64_esp/kernel.elf` through one helper, `banner_cert_arm`; `kernel8` certifies **`kernel8.img`, the FLAT binary**, not the ELF it was objcopy'd from — the ELF carries non-allocatable sections (debug info, symbol and string tables) whose bytes the VideoCore ROM never loads, so a token found only there would certify a string that is not in the booted image, while `-O binary` emits exactly the allocatable sections the Pi executes: same question, strictly stronger. `esp_jetson` certifies INSIDE the function rather than at a call site, so `esp-jetson-img` (a bare call to it), all six verb spellings and `jetson_card_image` get the same verdict on the same bytes — the shape of the original :1147 incident. **THE CLAIM UNDER TEST ON AARCH64 IS `$(arm_features)`, NOT `$_feats`:** the aarch64 kernel is compiled from the stripped list (20 x86-only names removed for media byte-identity) and `build_kernel_aarch64` prints it as `⚡ aarch64 effective features:` whenever it differs; feeding the unstripped banner would red every aarch64 build on a documented, measured, byte-identity-enforced strip. Measured: `esp-arm` (no knobs) · `esp-jetson` with the orin flight line read from the newest staged MANIFEST (`# KNOBS: UNAOS_GA10B_PROBE5=4`) · `kernel8` with LAWS §9's Pi desktop line of record. Table now **48 rows — 47 measured, 0 `unmeasured-here`, 1 NOWITNESS (`wedge2`)**, every aarch64 row carrying its hit count in a new `measured(N)` state form, and it gained four rows the banners needed and did not have (`apsrun` 2 hits, `bsptick` 1, `bsprun` 1, `ga10bprobe5a` 13). **THE `unmeasured-here` COLUMN IS NOW EMPTY** — every one of the 15 rows this clause owed, plus the four new ones, is proven against a real artifact. **TWO FINDINGS, and they are different animals.** (1) **A WRONG ROW, not a lie:** `ga10bprobe5` was seeded on the 64-hex-character vendor digest at `ga10b_fw.rs:75`, which is the argument to `const fn hx(s: &str) -> [u8; 32]` — evaluated at COMPILE TIME, so only the 32 decoded bytes ever reach an image and that token measured 0 on a build that carried the feature and would have measured 0 forever. Worse, the row had no `cond`: `ga10b_fw`'s two callers are independently gated (`ga10b_ignite` under `ga10bprobe5a`, the QEMU fixture at `main.rs:1749` under `witness`) and with neither compiled in the linker garbage-collects the whole module and its `.rodata` with it — a feature can be in the cargo feature set, compile, and still put NOTHING in the artifact because nothing calls it. Re-seeded on `[ga10bfw] window_need=` with `cond = ga10bprobe5a+witness` (a new OR-group form for exactly this shape), and a new `ga10bprobe5a` row on `[ga10bprobe5a] -> REFUSED reason=`; measured 1 and 13 hits on a `UNAOS_GA10B_PROBE5=1 esp-jetson` artifact. Both traps are now numbered steps 4 and 5 of the script's seeding recipe. ⚠ Worth a seat's eye: `UNAOS_GA10B_PROBE5=4` has **no case arm in this tree** (the arms are 1/1d, 2/2d, 5/5d, 7/7d), so the flight line off the newest orin MANIFEST — which was staged from a NEWER orin branch that does have a `4` arm — builds `ga10bprobe5` with no ignition and therefore a dead-stripped module here. That is a tree-skew, not a defect, but a flight line is only meaningful against the tree it was cut from. (2) **A SECOND WRONG ROW, of a kind worth its own rule:** `witness` red-lined `kernel8` with 0 hits on a Pi desktop image that carries the entire witness battery. Its token sits in `kernel_main`'s post-GUI tail, and `main.rs:79` states in its own `cfg_attr` that `baremetal`, `bootlog`, `usbdebug` and `tegra` EACH make that tail unreachable — the compiler is told so and deletes it. So a feature can be compiled in, present, and working, and its literal still absent because the CONFIGURATION removed the code it lives in. `cond` gained a negated term (`!a` = this must be OFF) and the row now reads `!baremetal,!bootlog,!usbdebug,!tegra`, copied from that `cfg_attr` rather than guessed; the Pi build prints UNVERIFIABLE naming the reason instead of a false MISSING. ⚠ Owed, small: `witness` is still certified on x86 ONLY. A Pi-side token for it (a literal from a fixture the baremetal path actually reaches) would close that, and costs one `esp-x86` re-measure to swap safely — not free inside this arc's gate floor. (3) **A REAL LIE, found and FIXED — see the `· NEW` row below, which is ✓ not owed.** Owner of what remains: the Pi-side `witness` token above, and nothing else — the registered-divergence table is EMPTY again and the token table has no `unmeasured-here` row left.
· NEW 2026-09-15 (rmbp BANNERCERT2 executor finding, measured on the first armed aarch64 run — `./arroyo esp-arm` with NO knobs at all, the most default build this tree has; REGISTERED, not fixed, because the fix is not this seat's to take): **EVERY AARCH64 MEDIA BANNER NAMES `ehcihid`, AND NO AARCH64 ARTIFACT CAN CARRY ONE BYTE OF IT.** `crates/kernel/src/drivers/mod.rs:9` gates the whole module `#[cfg(all(target_arch = "x86_64", feature = "ehcihid"))]`, so on aarch64 the feature emits nothing: on this arc's `esp-arm` ELF the row's token has 0 hits AND `LC_ALL=C grep -a -o -F 'EHCI-HID'` has 0 — an absent feature, not a rotted token. **HOW IT REACHES THE BANNER:** `arroyo:337` appends it DEFAULT-ON (`[ -z "${UNAOS_NOEHCIHID:-}" ]`), and `arm_features` (`arroyo:2206`) — the function that strips TWENTY x86-only names from the aarch64 cargo line precisely so aarch64 media stay byte-identical whether or not the x86 track armed them — strips its own TWIN `kbdwit` at :2218 and does NOT strip `ehcihid`. Same class as the `sdwrite` lie above, seen from the other arch: a default-on name on a banner that the artifact structurally cannot answer for. **THE FIX IS ONE LINE** — `f="${f//,ehcihid,/,}"` beside the kbdwit strip — **and it is not free**, which is why this executor registered it in `bc_registered` rather than taking it: removing a name from the cargo feature set shifts `-Cmetadata`, so it RE-HASHES EVERY AARCH64 MEDIA IMAGE once. That is a one-time sha256 break on the Pi and Orin flight cards and on every MANIFEST that records one — a call for the boards' seats, exactly as each of the other twenty strips was a call when it landed. Registered means the cert prints it as a NAMED, COUNTED divergence that does not red the verb and MUST REACH ZERO; an unregistered MISSING still reds. **OWED:** the aarch64 seats (orin + pi), one line in `arm_features` plus the sha-break acknowledgement, then delete the `bc_registered` row in the same commit (register, fix, delete is a row's whole intended lifetime). Until then every `esp-arm`/`esp-jetson`/`kernel8` run prints `⚠ banner-cert: 1 REGISTERED DIVERGENCE(S)`.
· NEW 2026-09-13 (orin session, RASTWIN executor finding, measured twice): **`./arroyo test`'s VERDICT IS LOAD-DEPENDENT — the same command on the same tree gives rc=1 on an idle box and rc=0 under load.** Measured: idle, the rast demo took 3,553 ms and the boot reached `dmgovlp`, rc=1; under load the demo took 6,662 ms, the boot did NOT reach it, rc=0. So the gate passes BECAUSE it ran out of wall before it got to the failing fixture. That is the worst possible polarity for a gate — a loaded box, which is exactly what a session with several executors produces, hides reds. Relatedly and separately measured: true baseline `b8f5ded9` with `UNAOS_RAST=1` at a 40 s wall exits **1 with five fault lines (DOCK, DOCKID, WINMENU, PULSEQUIT, APPPIN)**, and the SAME tree with `rast` OFF shows those five passing — i.e. the panel-owning rasterizer demo was breaking five desktop fixtures, which is a real defect that only a rast-armed run could ever have shown and which no fold gate this session would have caught (none carried `UNAOS_RAST`). ⚠ THIS IS THE THIRD INSTANCE TODAY of a gate that passes while proving nothing, after the arroyo feature-drop and the NET6 certification leg built without its own knobs. OWED: (a) a wall long enough to reach the ladder, or a gate that REFUSES when it did not reach its last fixture rather than reporting rc=0 — a truncated run must never be a pass; (b) decide whether `test` should carry `rast` in any standing gate, given it demonstrably perturbs the desktop fixtures. **✓ (a) DONE 2026-09-15 (TESTTRUNC, exec-rmbp-testtrunc, parent 27716175): the x86 `test` leg now carries a POSITIVE completion requirement — `scripts/specs/x86-test.spec` declares one COMPLETE marker (the zeolite block, the last fixture of the ladder, measured off a default boot and a UNAOS_WC=1 boot that end at the same place), `qemu_await.py --settled` reads it off the finished capture, and a run that did not reach it prints `-> TRUNCATED` and exits 1 instead of 0. The SECOND clause of (a) was deliberately NOT taken: no wall was lengthened and none was shortened — the point is that a short wall is now VISIBLE rather than silent, which is what lets a seat choose the wall knowing what it costs. Go-red: `./arroyo test 8` rc=1 truncated against `./arroyo test 120` rc=0 complete_line=1984. (b) is UNTOUCHED and still owned by nobody.**  Note the pre-existing `[dmgovlp] narrow=0/12` red reproduces with `rast` off and belongs to the drag-stall lane, not here. Owner: unassigned.
· NEW 2026-09-15T1x:xxZ (LOCKFIX, rmbp seat — measured as the go-red of rmbp-ledger B1's own fixture): **THE SIBLING OF THE ROW ABOVE, AND THE WALL IS NOT THE MECHANISM. `./arroyo test` exits 0 when an x86 WITNESS FIXTURE WEDGES and takes every leg after it off the wire.** Measured on one tree, two runs, same command (`UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 90`), the only delta being one line of `arch/x86_64/syscall.rs::click_pointer_pos`: FIXED → **rc=0, 15 `:: PASS ::`**, `:: LOCKFIX-B1: … :: PASS ::` and `:: APPPIN: … :: PASS ::` both on the wire. MUTATED back to the blocking `WRITER.lock()` → **rc=0 again, 13 `:: PASS ::`**, and the ladder stops dead one line after `[ptrdead] … -> PASS`: `LOCKFIX-B1`, `WMDIRECT`, `DMGOVLP`, `VUGRES` and `APPPIN` are ALL ABSENT (`LC_ALL=C grep -a -c -F`, each 0 against 1 or the green run's presence). The battery task had deadlocked on a non-reentrant spin `Mutex` — the boot-8 wedge itself — and the run still printed `✅ Test run complete` and returned 0, because the verb's completion checker is satisfied by OTHER tasks' output (the xHCI/BOT rollups follow the hole and score the run). So this is NOT the load/wall polarity of the row above: the run did not time out, it COMPLETED, with five fixtures silently deleted. **The two rows share one owed fix and it is the one already written above: a gate must REFUSE when it did not reach its last fixture.** This row adds the second reason that fix is needed and the cheaper instrument for it — the wall is a proxy, the LEG COUNT is the measurement: assert the x86 ladder's tail witness is present (`APPPIN` is the declared last leg of `witness_battery`), or assert a leg count, and a wedged fixture reds instead of vanishing. Go-red for that check already exists and is free: revert `click_pointer_pos` to `crate::video::WRITER.lock().info()`. Evidence: `~/unaos-bench/scratch/rmbp-0915/lockfix-logs/g2-test90.log` (green) and `g3-gored.log` + `serial-GORED.log` (wedged). Owner: unassigned — fold into the row above's (a). **✓ DONE 2026-09-15 (LADDERTAIL, exec-rmbp-laddertail, parent 31c7e5cb) — ALREADY FIXED BY TESTTRUNC, AND THE CHECK THAT PROVES IT IS THE ONE THIS ROW ASKED FOR. No new directive was written; the claim was MEASURED instead. THIS ROW'S OWN wedged capture, replayed at 31c7e5cb against the UNCHANGED `scripts/specs/x86-test.spec`, is REFUSED: `qemu_await.py --settled … serial-GORED.log` → `SETTLED status=truncated … stopped_at=1652` rc=**3**, and `./arroyo mbench --replay … --platform x86` → `✂️ MBENCH TRUNCATED (INCONCLUSIVE)` rc=**3**; both healthy captures taken on this tree replay `✅ MBENCH PASS` rc=0 (`UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 120` rc=0 `completion=complete complete_line=1980`, 2148 lines; default boot rc=0 `complete_line=1370`, 1531 lines). **WHY THE ROW READ AS OPEN:** the wedged run exited 0 because that worktree PREDATED TESTTRUNC — `g3-gored.log` says so in as many words (`⚡ test: no completion signal declared for this verb — full wall 112.0s`). The wedge did not "complete with five fixtures deleted"; it took zeolite off the wire too (`LC_ALL=C grep -a -c` for `zeolite` in `serial-GORED.log` = **0**), so the marker never landed. **WHY THE MARKER COVERS THE LADDER, measured on both boots at 31c7e5cb:** the marker is emitted AFTER the ladder's last verdict — default, `:: SOCK-4: … :: PASS ::` @1351 vs marker @1370; `UNAOS_WC=1`, @1969 vs @1980 — and the two are one task's output in that order, so NO wall reaches the marker while cutting a fixture. **THE INSTRUMENT THIS ROW PROPOSED IS THE WRONG ONE, twice over.** (i) `APPPIN` cannot be the tail assertion: it is WC-only — present @1887 under `UNAOS_WC=1`, ABSENT from the default boot — so asserting it would red every healthy default run. (ii) A `REQUIRE` in `x86-test.spec` was measurably INERT when this row was ticked: `qemu_await.py`'s `settled()` answered on `matcher.markers()` alone and never called `Matcher.complete()`. Probed with one unsatisfiable REQUIRE over the same healthy capture — `--settled` → `status=complete … rc=0` (invisible), `mbench --replay` → `❌ FAIL — 0/1 required witnesses` rc=1 (scored). That half is FIXED in this branch's second commit; see RESIDUAL below. **CORRECTION TO THIS ROW'S OWN EVIDENCE:** `WMDIRECT`, `DMGOVLP` and `VUGRES` are named here as fixtures the wedge deleted, but they appear in NEITHER capture — `LC_ALL=C grep -a -c` = 0 in `g2-test90.log` (the GREEN run) as well as in the mutant. They do not exist on that tree; only `LOCKFIX-B1` and `APPPIN` were actually taken off the wire. **RESIDUAL — ✓ ALSO DONE, same branch, second commit (the seat answered the STOP and authorised it).** `settled()` now gates on `Matcher.complete()` instead of on `matcher.markers()` alone, so REQUIRE/COUNT reach `./arroyo test`'s verdict as `qemu_await.py`'s own docstring already claimed — code and documentation no longer disagree, which is what made the inert-REQUIRE trap readable as a working check. Go-red, replay-only, no QEMU: the unsatisfiable-REQUIRE probe over the healthy WC capture moved `status=complete complete_at=1980` rc=0 → `status=truncated reason=short-witnesses:1 stopped_at=2148` rc=**3**, while the UNCHANGED spec over that same capture stays `complete complete_at=1980` rc=**0** and the wedged capture stays `truncated stopped_at=1652` rc=**3**. A hole in the ladder and a short wall stay DISTINGUISHABLE (`reason=` present vs absent, short patterns named on stderr). Blast radius nil and checked rather than assumed: `x86_test_completion` (`arroyo:3194`) is the tree's ONLY `--settled` caller and `x86-test.spec` its only spec, 0 REQUIRE/COUNT, so every current verdict is byte-identical; `mbench.py --self-test` 35/35. Still NO REQUIRE was added to the spec — the ordering already covers the ladder — and `[ptrdead] backlog` must stay unasserted if one ever is (Class-3 flake, `docs/dev/FIXTURE_FLAKES.md`). Evidence: `~/unaos-bench/scratch/rmbp-0915/laddertail-logs/`. Gate detail in `docs/dev/STRUCTURAL_GATES.md` GATE-TESTTRUNC, second control.**
✓ DONE 2026-09-12T2x:xxZ (KNOBOFF executor, on `exec-orin-knoboff`) — the NET6 finding: `./arroyo knoboff <feature>` defaulted its baseline to `HEAD~1`, which on a MERGE commit is the FIRST parent, so an executor who merged the track in and re-gated compared against their OWN pre-merge tip across every commit of the track (measured on exec-orin-net6: plain run exit 1, x86 1,210,312 bytes differ; second-parent run exit 0 with the control fired). FIXED BY REFUSAL, not by a guess: a merge HEAD with no baseline NAMED now returns NO VERDICT (exit 2) and prints HEAD, every parent with its subject, and the two commands to re-run — `knoboff <feat> <parent2>` for the executor's trunk-into-track merge, `knoboff <feat> <parent1>` for a landing's arc-into-trunk merge. Which parent is the tree-without-the-arc depends on the merge DIRECTION and both directions are sanctioned (LAWS §3), so no default can be right in both; exit 2 is never a pass, so the refusal cannot read as green, while a wrong `1` sends an executor hunting a line shift that is not there. An explicitly named baseline is honoured on a merge HEAD as everywhere else and the no-argument form is untouched for every non-merge run. Proven on a throwaway merge commit built in the executor's worktree: pre-fix arroyo exit 1, fixed arroyo exit 2 with both parents printed, fixed arroyo + parent 2 exit 0 with the control fired. Ledger: SO34's sibling.
✓ DONE 2026-09-12T2x:xxZ (KNOBOFF executor, same commit) — the GA10B4E/SO34 finding: knoboff hardcoded ONE shared scratch directory for every caller on the bench (`~/unaos-bench/scratch/knoboff`: one worktree, one CARGO_TARGET_DIR, one set of `flat-*.bin`), so two concurrent invocations corrupted each other's verdict. The scratch is now PER CALLER, keyed on a digest of the caller's worktree path (`caller-<basename>-<hash12>/` holds the throwaway worktree and the target dir — two executors are two keys, and the same executor's later runs keep their incremental cache, which is why the key is the caller and not the pid), with this run's log, patch and six flat images in a `run-<stamp>-<pid>/` inside it: removed on a clean PASS, KEPT with its path printed on every other outcome. The one remaining same-key collision — one worktree, two concurrent runs — is held by an `flock` on the caller directory and the second caller WAITS and says so on the wire, never a silent share. Measured both ways: two concurrent runs against the PRE-FIX arroyo (from git, with only its `SCR=` root redirected into this executor's scratch so no live executor was disturbed) versus the same two against the fixed one. Ledger SO34 ticked fixed-unflown.
✓✓ **DONE 2026-09-13 (GATEHYG executor, on `exec-orin-gatehyg`) — A TRUNCATED RUN IS NEVER A PASS (orin-ledger A72; LEDGER S8's truncation half).** The same tree and the same `./arroyo test` gave rc=1 idle and rc=0 under load — the loaded run ran out of wall before `[dmgovlp]` — because `scan_serial_faults` is NEGATIVE-ONLY and cannot tell a clean capture from a short one. `unaos/arroyo` now declares the x86 ladder's TERMINAL MARKER (`:: PULSE-W:`, the last line `pulsew_launcher` prints on every path, or `:: zeolite:` which is strictly after it) plus an ORDERED CENSUS of ten rungs, and `ladder_tail_check` REFUSES with exit 5 — NO VERDICT, not a pass and not a regression — when the capture ends before the tail, naming the furthest rung it did reach. WHY A MARKER AND NOT A COUNT: truncation only ever removes a SUFFIX, so a terminal marker cannot be truncated into looking present, while a count (or "the log is non-empty", which is what we had) is satisfiable by a short run. GO-RED/GO-GREEN both measured on the same tree and quoted in the commit; the census prints `10/10` on a green so a renamed family is a number in the log, not a silently weaker gate. WALL COST 8 ms on a 2,089-line capture. `test-arm` is wired and NOT exempt but has NO declared marker yet — it prints the UNDECLARED line (rc 3), reported and never silently green; declaring the aarch64 virt tail is owed. ✓ **2026-09-23 (ARMUSERS, `exec-rmbp-armusers`, rmbp-ledger B188): `test-arm` now DECLARES its marker, one row per GIC regime because the verb boots two ladders — `LADDER_TAIL_test_arm_gicv3=':: CAPSTONE COMPLETE'`, `LADDER_TAIL_test_arm_gicv2=':: BOT: tx single='` (the topology summary's last line, the loop's 2000th pass) — and a mid-ladder `./arroyo test-arm 13` that the old verb passed now exits 5 naming `[usbw] write lba=` as its furthest rung.**
✓✓ **DONE 2026-09-13 (same commit) — THE BANNER AND THE ARTIFACT MUST AGREE (orin-ledger A73).** `⚡ kernel features:` states what cargo was handed; only `LC_ALL=C grep -a -o -F` on the BUILT ELF states what is in the image, and a DONE gate that said "build `UNAOS_TEGRA=1`, certify `:: NET6:`" passed on a build carrying no `net6` at all — the family CORRECTLY absent, the leg proving nothing. `banner_artifact_check` now runs on every kernel ELF `build_kernel_x86_64`/`build_kernel_aarch64` produce, from a DECLARED `<feature> <arch> <family>` map, with a present-control (`UNAOS_BUILD_STAMP`) and an absent-control (`NOTATOKEN-UNAOS`) on every run — a broken instrument gives NO verdict rather than a green. ARMED-and-missing is a hard red; an UNMAPPED banner feature is NAMED in one line, never skipped; UNARMED-but-present is a WARNING, because Cargo and not `arroyo`'s string is the enumerator of what a build enabled (`ga10bprobe5a = ["ga10bprobe5","ga10bprobe4a"]`, so a `ga10bprobe4a` row would red an honest build — wrong-strict, LAWS §5). Rows landed: `ga10bprobe5a`, `ga10bprobe5`, `net6`, `sdwrite`, all aarch64-scoped and all measured both polarities. THE ARCH COLUMN WAS PAID FOR BY THE FIRST RUN: the x86 default list carries `sdwrite` while its family is printed from `arch/aarch64`, so an unscoped row reddened an honest `./arroyo test`. LEFT: the x86_64 half of the map (the check says so out loud on every x86 build), `kernel8()`'s curated `$K8_FEATS` banner, and the 18 features whose family `crates/kernel/Cargo.toml` already names in prose (`grep -c 'Witness famil'`, counted before the claim) — one measurement each, not a transcription. WALL COST 43 ms per 2 MB ELF.
✓✓ **DONE 2026-09-13 (same commit) — A FEATURE GATE KEYS ON THE BUILD TARGET, NOT ONE SPELLING OF ITS NAME, AND IT IS NOW MECHANICAL (orin-ledger A74).** 2e48d721 fixed `arroyo:1147` by hand after it cost a power cycle; nothing could have caught the next one. `unaos/scripts/verb-alias-closure.sh` parses the dispatcher's alias groups and reds on any `$1`-keyed guard — multi-line `case` arm, inline one-line `case … esac`, or `[ "$1" = "x" ]` — that names a SUBSET of a group; wired into `./arroyo check`, exit 2 = NO VERDICT. GO-RED on the real pre-fix file (`git show 2e48d721^:unaos/arroyo` → `:1147 … MISSING: esp_jetson,jetson`, rc=1) and GO-GREEN on this tree (`groups=24 guards=3 violations=0`, rc=0); the `case`-arm path proven separately by mutating the `UNAOS_WITNESS` arm at :49 (4 violations). SWEEP RESULT (deliverable 3, ALL arches): `arroyo` has exactly FOUR top-level `${1` sites — `unaos/arroyo:49` (UNAOS_WITNESS, 9 spellings, CLOSED), `:1147` (the GA10B metal gate, 6 spellings, CLOSED at 2e48d721), `:2157` (the kernel8-family banner suppression, 8 spellings, CLOSED) and the dispatcher at `:9282`. No `esp-x86`, `esp-arm`, `kernel8*` or `test-*` guard is open; :1147 was the sole offender in the whole file, not just in the Orin block. WALL COST 40 ms.
✓✓ **DONE 2026-09-13 (same commit) — A FIXTURE ON AN ENTRY POINT THE GATE NEVER RUNS IS A DEAD GATE (orin-ledger A75).** The fourth member of the day's family and the worst: the other three are "the gate ran and proved nothing", this is "the gate never ran and nothing said so". Found by executor LUN2, which had `crates/kernel/src/selftest.rs` in its FILES list, DECLINED it, and said why. THE TRUTH, MEASURED on a healthy 2,089-line `UNAOS_QEMU_FULL=1 UNAOS_WC=1 ./arroyo test 90` capture: **NOT REACHED** — `selftest.rs::run()` (`:428`), the in-OS `tste` suite, sole caller `shell.rs:5727` (the `tste` shell verb), so its thirteen legs (`test_sched_introspection` :522, `test_heap_roundtrip` :556, `test_video_geometry` :626, `video::witness::run` at :469, `run_sync_section` :823, `run_storage_section` :900) are dead to BOTH QEMU verbs — `:: TSTE: suite start ::` and `:: TSTE: suite complete ::` are 0 hits, and both are printed unconditionally by `run()`. **THE TRAP** — the same capture carries 23 `:: TSTE:` lines, every one of them a BOOT-PATH fixture in `shell.rs` (`:2896`, `:3010`, `:3156`, `:3342`) borrowing the same wire shape for the boot-replay ring — exactly two files print that token at all, `selftest.rs` (6 sites) and `shell.rs` (20), counted with `grep -rn 'serial_println!(\":: TSTE:'`; `main.rs:491` only mentions the shape in a comment, so a reader who greps the capture for `TSTE` concludes the suite ran. **ALSO REACHED BUT NOT AN ENTRY POINT** — `selftest::capture()` (`:230`) is a passive TAP called only from the two serial print paths (`arch/x86_64/serial.rs:81,202`, `arch/aarch64/serial.rs:194,271`); a fixture cannot land there. GATE: `unaos/scripts/fixture-reachable.sh`, wired into `./arroyo check`, freezes that roster (accounted-for, not forbidden — the `k8-reach.registry` shape) and checks the declaration itself still holds (exactly one caller, in `shell.rs`; zero callers = exit 2 NO VERDICT, its control probe). Go-red measured three ways on a mutated copy: a 14th leg named `lun2.newfixture` → rc=1 by name; a second caller of `selftest::run` → rc=1; the entry point renamed away → rc=2. Go-green on this tree: `callers=1 legs_found=13 legs_declared=13 -> PASS`. WALL COST 60 ms. NOT BUILT, on purpose: the universal "every `-> PASS` in the ELF must appear in some capture" — knob-, arch- and skip-gated fixtures are absent from healthy captures by design, so it is wrong-strict (LAWS §5).
· ✓ DONE 2026-09-24 (LEDGER SR22, cloud session: `_board` tags on every leg, `UNAOS_BATTERY_BOARD` or the track branch picks the board, skips are said and counted, `UNAOS_BATTERY_DRY=1` prints the plan; GATES.md §R39) — was: R39 battery selector: `battery()` runs the landing seat's OWN-BOARD legs — the selector is not written
✓ DONE 2026-09-15 (LEDGERGATES, `exec-rmbp-ledgergates`, parent 2051470a — re-derive the sha at the land) — SR13: GATE-LEDGER's strict trigger is decided from CONTENT, not from a ref name. Strict arms when HEAD's sha is contained in the trunk ref (`git merge-base --is-ancestor HEAD main`, then `origin/main`) or by `UNAOS_LEDGER_STRICT=1`, and EVERY run prints `strict=by-env|by-ancestry|off` with its reason. The direction is the safety argument and it is the one the row demanded: head-contained-in-trunk ARMS, trunk-contained-in-head (a post-fold track tip) does NOT. Proven on two throwaway detached worktrees at this tree's own shas: trunk sha -> `strict=by-ancestry` rc=0 where the pre-fix script printed no posture at all, track sha -> `strict=off` rc=0, and `UNAOS_LEDGER_STRICT=1` from the same track worktree -> `strict=by-env`. An unresolvable `UNAOS_LEDGER_TRUNK` is now printed as the reason strict is off, so the rename door is loud
· NEW 2026-09-15 (LEDGERGATES, `exec-rmbp-ledgergates`, parent 2051470a — re-derive the sha at the land), and it is the first thing the new queue-citation check found: **`docs/dev/OS/orin-queue.md` cites THIRTY-FIVE ledger ids that exist in no ledger file on ANY head.** Measured over the nine refs the gate already enumerates (`refs/heads/hw-*`, `main` and their `origin/` twins), hw-jetson — orin's own branch — included, so this is not a cross-branch artefact: the rows were never written. 202 of that file's 373 citations point at them. That is SO6's shape at scale, which is what SR12 is about. They are REGISTERED in `ledger-check.sh`'s `QUEUECITE_REG` rather than red, because a gate reding another seat's file on the day it ships is the mistake that file's own ABSENCE note records paying for once already; the registration is printed on every run as a census line, must reach zero, and goes RED the moment one of the ids starts resolving anywhere — which is what will happen on hw-jetson as orin writes each row. Owner: orin, to write the rows or to strike the citations
· SR1  a `UNAOS_*` knob with no `K8_FEATS` arm is unreachable for every Pi image, silently — the CLASS needs a gate. **The gate exists (GATE-K8REACH) and it bit this session (2026-09-24): `UNAOS_USBNET` and `UNAOS_HDASIE` landed with no arm and no registry row; `k8-reach.py` named both. USBNET got its `K8_FEATS` arm (a Pi with a dongle is the target), HDASIE an `NA` row with `--evidence` (x86 HDA only). 163 knobs: 16 armed, 147 registered — 102 still TODO.**
· SR9  GATE-FAMILY groups by NAME and cannot tell a per-platform COPY from a CALLER
✓ DONE 2026-09-15 (GATEFIX, `exec-rmbp-gatefix`, parent 2946ea30 — re-derive the sha at the land) — SR10. `_step`'s `logfile:verdictfile` legs now CLEAR the shared verdict path and its `.run` sidecar before the command and COPY the capture to `<step>.serial.log` (sidecar beside it, never appended into it) afterwards, judging the copy; the pi4 leg's verdict therefore comes from a file that leg leaves behind, and the summary's PASS tally moves onto the same kept copy. The BACKWARDS half was not in the row's diagnosis and is closed too: bytes the leg's own command never wrote can no longer reach its verdict. Go-red by a scratch harness that extracts the real `_step` and drives it with stubs (no QEMU, no build) — a fault line appended to the shared path after leg-A's verdict flips leg-B red and destroys leg-A's judged bytes at the base (harness exit 1) and does neither after the fix (exit 0), with a control leg whose own run emits a fault line still red in both. `docs/dev/STRUCTURAL_GATES.md` §GATE-BATTERY-EVIDENCE
✓ DONE 2026-09-15 (LEDGERGATES, `exec-rmbp-ledgergates`, parent 2051470a — re-derive the sha at the land) — SR11 and SR12. **SR11**: one general escape for quoted material at the OUTPUT boundary, which is the shape SR11 itself prescribed instead of a third input-side special case. Every emitted line is rewritten for the harness fault-scan family (arroyo's FAULT_PATTERNS) and for the markdown cell delimiter, and the diagnostics stopped separating the status enum and the owner set with the delimiter they are about. Measured on one injected row: pre-fix script 2 fault-matching output lines and 1 pipe-carrying line, post-fix 0 and 0, same tree, same mutation. **SR12**: a deferring row must name an owner (a track) and an expiry (a date, a sha this repo resolves, or a blocking id) or the gate reds naming the missing half; the grandfather list is the field-count mechanism and is EMPTY, measured — this tree carries zero deferrals in either posture. NOT CLOSED by either: the authoring hazard (a raw pipe in a cell is still refused, B63 stands) and peer-head resolution for ledger cross-refs, which is implemented for queue citations instead
· S8  `scan_serial_faults` passes on a MISSING log; `test`/`test-arm` are negative-only — **✓ PARTLY DONE 2026-09-15 (TESTTRUNC, exec-rmbp-testtrunc): the missing-log half is CLOSED on both arches (the scan now names the path and returns 1; proved in a probe harness, with a clean log still rc=0 and a `-> FAIL` log still rc=1, so the pattern list did not move). The negative-only half is closed for `test` (see the TRUNCATED row) and OPEN for `test-arm`, which wants an aarch64-virt spec first.**
✓ DONE 2026-09-15 (GATEFIX, `exec-rmbp-gatefix`, parent 2946ea30 — re-derive the sha at the land) — SO6, and the answer is that it is ALREADY FIXED and was never proven. Measured at the base: the probe greens from the repo root and from `unaos/` with the same line and the same parsed map (142 rows both ways), because `981463ea` anchored it to `$WORKSPACE_DIR` plus `basename` of the invoked path, absolute by construction. **So no code moved here** — inventing a second fix for a defect that is not in the tree is the failure this row is about, run backwards. What was missing is the drill, and it is now recorded: a scratch MIRROR of the tree (sources symlinked, the builder file a writable copy, a harness copy of arroyo with the cfg-leg cargo line stubbed and a `cfgonly` dispatch arm — the shape STRUCTURAL_GATES' own standard section prescribes) gives three states from BOTH directories, identical in each: clean rc=0; the builder's knob read renamed away rc=1 naming `UNAOS_WC->wc`; the map line broken rc=1 with the control refusing a verdict. The tree is never mutated and no cargo runs. `docs/dev/STRUCTURAL_GATES.md` §GATE-KNOBBUILD
· SO7 / B26  `kernel8-test` (MBENCH) reds intermittently under host load — quiet-box obligation
· NEW 2026-09-16 (FLAKERATE, `exec-rmbp-flakerate`, rmbp executor — six runs, measured, ADDRESSED TO LAWS §5's Flakes paragraph; the seat carries it, this executor did not edit LAWS): **THERE IS NO LOAD THRESHOLD, AND THAT IS THE RESULT. A 1-min load number does not predict the verdict on either leg, so the quiet-box rule cannot be written as a number.** The arc was sent to turn today's load reds into rates with their load beside them and to state the load above which the x86 `test` verdict is unreliable. The rates exist; the threshold does not. **x86, 4 runs of `UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 90`, one tree (`da2a9abc`), sequential in one worktree, at 1-min load 7.90 / 15.55 / 15.18 / 15.09 with a 20.57 peak observed in-run: the four captures are IDENTICAL** — 119 distinct witness tags (`diff` of the four sorted tag lists: no output, three times), 78 `-> PASS`, 22 `:: PASS ::`, 3 `-> FAIL`, all four reaching the completion marker. Not four passes — four identical samples. **0 flakes in 4 for every fixture today's reports named**: DOCKID, `[dmgovlp]` (`adopt_stretch=4/4`, `max_ms` 4-13), `[ptrdead]` (`fpop12=0 fpop3=0`), PWRDRAIN, S5DRAIN, SINKDRAIN, SERWIT-1, DOCK, SOCK-4, DMG-REFUSE, APPPIN — each PRESENT in all four captures, so the zero is a fact about the data and not about the pattern (LAWS §5). **Pi, 2 runs of `UNAOS_QEMU_FULL=1 ./arroyo kernel8-test 300` (pi's leg under R39, run here because the population being measured is the SHARED harness's): the reds landed on the LOWER-load run** — 1-min load 13.35 red (124/126 REQUIREs, 6 forbidden hits), 13.84 green (125/126, 0 forbidden hits). Two runs cannot set a threshold but they are enough to falsify the ordering. **THE RULE PROPOSED, in the paragraph's own voice: 'quote the load, never reason from it — the discriminator is in the line, not in `uptime`'** (`moved=` and `maxus=` for `[wc-*]`, `fpop12=` for `[ptrdead]`, `max_ms=`/`drag_evt=` for `[dmgovlp]`). **SECOND CLAUSE, and it is the one with teeth: A COMPLETED RUN IS NOT A COMPLETE CAPTURE.** `kernel8-test` printed *the end-of-run marker was seen — the run completed, so a missing witness here is a GENUINE regression* for `:: U5: capabilities … -> PASS ::`, a witness whose OWN BANNER is on the wire five lines earlier in the same capture — the fixture ran and the verdict line was lost. Same shape, 2 in 2, for `:: ERET-SCRUB: first-entry …` (absent in BOTH its PASS and FAIL forms, `first-entry`/`TPIDR`/`FPSR` all 0 hits, no torn fragment, while the SAME task's next `serial_println!` — `arch/aarch64/syscall.rs:6541`, the two `if/else`s at `:6550-:6559` and `:6560-:6569`, all four arms printing, no early return between them — landed both times). **A missing REQUIRE on a completed run is a CAPTURE question before it is a regression**, and `SERWIT-2 -> PASS` does not clear it: SERWIT-2 conserves over the four MIRROR taps, and the primary PL011 capture the battery is scored from is not one of them (both captures carry SERWIT-2 PASS *and* `[mirror] fbcon: 8 line(s) dropped`). **THIRD, a free one for the seat: a `FAIL` the battery cannot see.** In the Pi run MBENCH scored `0 forbidden hit(s)`, the capture carries `:: PWRDRAIN: FAIL — filled=64 lines=12 bytes=816 want_bytes=4352 residue=0 dropped=0 ::` — `mbench.py:136`'s `DEFAULT_FORBIDS` are `-> FAIL`, `FAIL ::`, `PANIC`, and `FAIL — ` is none of them. Scan for the bare token; controlled over the four x86 captures, where `FAIL` count equals `-> FAIL` count, so it is a Pi emitter's spelling and not a tree-wide hole. **NO RUN WAS TRUNCATED** — all six reached their marker, so the 2026-09-13 load-dependent-verdict row's case did not arise here and nothing is recorded against it; at these loads 90 s still reaches the end of the x86 ladder. NOTHING IN THE KERNEL WAS TOUCHED (this arc measures). Evidence: `docs/dev/evidence/rmbp-0915/flakerate/FLAKERATE.md` (six-run table with load headers, the rate table, every verdict line verbatim); `docs/dev/FIXTURE_FLAKES.md` Class 5 (5a/5b/5c/5d plus the x86 control section). Owner: the seat, for LAWS §5.
· ✓ INSTRUMENTED 2026-09-24 (TASKEXIT, rmbp-ledger B211, cloud session: the per-task `[taskexit] tid name core reason` line at `exit()`/`retire_killed`, witness-gated, kernel threads only; REQUIRED on the virt login lane, PENDING in pi4-regression.spec — the pi seat's next kernel8-test reads the stolen verdict's own row; the fix half (pin the verdict) was already in tree) — was: NEW 2026-09-16 (SMPBALK8, `exec-rmbp-smpbalk8` off `3e4b7aa1`, rmbp executor; ONE line of `arch/aarch64/syscall.rs`, aarch64 only — pi runs `kernel8-test`, R39): **THE TWO ERET-SCRUB REQUIREs GO ABSENT WHEN THE VERDICT TASK IS STOLEN `c2->c0` — FIXED BY PINNING THE VERDICT TO ITS LAUNCHER'S CORE, AND THE REASON A STOLEN KERNEL TASK PRINTS NOTHING ON CORE 0 IS STILL OPEN.** Row: LEDGER `SR16` (measurements, the steal rule with file:line, the load mechanism, the cost). FOR THE PI SEAT: (1) count `awk 'index($0,"[smpbal] steal '\''eret-verdict'\''")' unaos/target/serial-pi.log` on your next `kernel8-test` — 0 is the fix's wire shape, and both `:: ERET-SCRUB: … -> PASS ::` lines present; (2) the open half — a `CPU_AUTO` kernel task stolen onto core 0 left core 0's run queue with no witness (`[spin6]`, `[redzone]`, `[wedge4]`, `[skill]` all 0 for it) and `u4-launch`, caller-pinned on core 2, went silent once in three runs (`11ca67f1`, load 1.04) though both its EL0 fixtures exited — a kernel poller in `wait_while_secs` → `yield_now` vanishing is a scheduler question, not an ERET one; it needs an instrument on the dispatch/exit path (a per-task `[taskexit] tid name core reason` line at `exit()`/`retire_killed`, witness-gated) before anyone re-places CPU_AUTO verdicts. Evidence: `docs/dev/evidence/rmbp-0915/smpbalk8/SMPBALK8.md`.
· SR7  three QEMU verbs have no completion signal (QEMU-FAST cannot shorten them) — **✓ DONE FOR `test` 2026-09-15 (FASTTEST, exec-rmbp-fasttest): the builder plumbing TESTTRUNC left owed is in. `builder/src/main.rs`'s blind `thread::sleep(secs)` is now `qemu_test_wall`, running the SAME `scripts/qemu_await.py` over the SAME `x86-test.spec` from the process that holds the QEMU pid, so the run ends at completion + grace; `arroyo` passes the spec and grace beside `UNAOS_TEST_SECS` and reads the builder's one `AWAIT` line back out of `<log>.await` for the `.run` sidecar. Measured `UNAOS_WC=1 ./arroyo test 120`: complete at +27.2 s, QEMU wall 47.3 s of 120 (rc=0, `mode=fast`), where every run before paid 120 s. `UNAOS_QEMU_FULL=1 ./arroyo test 60` still pays 60.2 s and `./arroyo test 8` is still rc=1 TRUNCATED. STILL OPEN: `test-arm` and `install-pi`, which have no completion source at all — `test-arm` wants an aarch64-virt spec first (S8).**
· B95  the x86 spec files are run by no `arroyo` verb; `orin-specscore.py` and `mbench --self-test` likewise — **✓ MOSTLY DONE 2026-09-15 (SPECRUN, row at the tail of this section): `x86-fat.spec` is now REPLAYED by `test-fat sf`, `x86-test.spec` by `test`, and every one of the 12 specs DECLARES its runner — 4 gated by a verb, 4 bench, 3 knob-leg, 1 script — under a new `./arroyo check` leg (`scripts/spec-roots.sh`) that reds by name on a spec nobody runs. WHAT REMAINS IS EXACTLY THIS ROW'S ORIGINAL COMPLAINT AND NOTHING ELSE: the invocations still NAME PATHS (`X86_TEST_SPEC`, `X86_FAT_SPEC`, the two pi4 paths), so the discovery fix this row asks for now has five named-path call sites to replace instead of four — but they are no longer the difference between a spec that runs and one that does not, and `spec-roots.sh` is the enumeration a discovery fix would build on.**
· S16  106 ordering invariants, 99 in comments enforced by nothing — check them in code, one file per arc
· ✓ LANDED (re-derived 2026-09-24 from LEDGER S13 `landed`: `scheduler.md` §2.z carries the fact and its commands; the probe is by design a high-water checkpoint of `u7_launcher`'s chain, `witness`-gated)  S13  `[u7stk]` probe has no reachable caller outside `u7_launcher`
· ✓ DONE 2026-09-24 (GATE-VERBS, cloud session, branch `claude/optimistic-ramanujan-r3qyu5`: `unaos/scripts/verb-roots.sh` in `./arroyo check`, both set differences, controls, go-red recorded in STRUCTURAL_GATES.md) — B47  `HOST_VERBS` <-> the dispatch match's arms is a HAND-MAINTAINED invariant across two files and nothing asserts it in EITHER direction — `umv` and `urmattr` answered "Unknown command" for their entire existence while `shell.rs:4305` claimed the review had checked all 78 spellings. The instance is restored; the class needs a gate that takes BOTH set differences, the way GATE-KNOB does over `[features]`. rmbp-ledger B47
· ✓ DONE 2026-09-24 (cloud session: the option_env! half of GATE-K8REACH — named/dual/unnamed, `ENV` registry status, the specimen DMAWIN and three NET4 knobs registered with their sites; GATES.md §B64)  B64  GATE-K8REACH's DOMAIN gap: `k8-reach.py` builds its universe from `_feats=` lines in `arroyo`, so a knob delivered by `option_env!` and armed by NO `_feats` line in any polarity is not enumerated at all — it can never be reported UNREGISTERED. Specimen: `UNAOS_DMAWIN` (read at `arch/aarch64/rtl8168_tegra.rs`, `grep -c UNAOS_DMAWIN unaos/arroyo` = 0); siblings `UNAOS_FBW`/`UNAOS_FBH`. One pass: enumerate `option_env!` reads as a second universe and rule each PROSE / ARMED-BY-COMMAND / ARMED-BY-NOTHING. SR1's class through the other door. rmbp-ledger B64
· ✓ FIXED IN TREE (re-derived 2026-09-24: `UNAOS_CHECK_P=N` is the MATRIXPAR lever, 10 sites in arroyo, named on every `check` header; the default stays serial in the shared dir by design)  B92  `./arroyo check`'s 56-leg cfg matrix shares the default `target/` with every other verb, so "warm" never means the same thing twice; MATRIXPAR's measured win is cold 387.2 -> 152.5 s (2.54x) from parallelism with per-slot dirs as the ENABLER, and the 81x is WITHDRAWN. The accepted design (`1dcd5644`: unset = one worker in the default dir, `=N` an opt-in cold lever with its CPU price in the banner, a red that names its leg, a summary asserting legs-run == legs-listed) has NEVER reached this tree — `grep -c UNAOS_CHECK_P unaos/arroyo` = 0. rmbp-ledger B92, B99
· ✓ DONE 2026-09-24 (cloud session: `scripts/line-neutral.sh`, GATE-LINENEUTRAL in STRUCTURAL_GATES.md, PLAYBOOK §2; measured on this session's own commits — GATES.md §B94)  B94  a mechanical LINE-NEUTRALITY check, the twin of `append-position.sh`: under R26 a comment-only `.rs` commit may skip its gate, but `panic::Location` embeds the source line (`arroyo:6866`, measured at `:341` as 32 bytes of `.data.rel.ro`), and legs in this tree ASSERT byte-identity against a baseline (`arroyo:6137`, `:1124`, `:3229`). The safe rule is by FILE TYPE plus counted line-neutrality, never by intent. rmbp-ledger B94
· ✓ DONE 2026-09-24 (cloud session; `check-roots.sh [<unaos dir>]`, header names the tree, bad/extra arguments exit 2 — GATES.md)  B96  `unaos/scripts/check-roots.sh` derives its root from `BASH_SOURCE` and its usage line takes NO argument, so it always scans its own tree and silently ignores a path handed to it — a cross-tree GATE-ROOTS green means nothing. Decide whether it should take a `[repo-root]` like `ledger-check.sh` does; until then only the root-taking gates can be offered as a cross-tree support service. rmbp-ledger B96
· ✓ DONE 2026-09-24 (cloud session: three whole tokens in the emitter's order with gaps; the emitter names the spec; nine-line proof old vs new; the lookahead first cut was refused by GATE-FOREMAN — specs may not use look-around — GATES.md §B101)  B101  `[pstrip] rollup`'s FORBID (`pi4-regression.spec:1462`) depends on FOUR FIELDS STAYING ADJACENT and the emitter (`ui_status.rs:1244`) says so NOWHERE — proven by execution: the rule fires on the live line, still fires with `paced=` appended at the TAIL, and goes SILENT with `paced=` inserted mid-chain, which is the edit the spec's own commentary twice records as owed. Fix is the contract comment at the emitter in `video/wcg.rs:1495`'s FORM (naming the consuming spec, the load-bearing key order, the tail-only rule), NOT a change to the FORBID. rmbp-ledger B101
· ✓ DONE 2026-09-24 (cloud session; the closing line is now the six verdicts' sum, INCOMPLETE is a declared ladder tail so the failure is a RED not a truncation; go-red by forcing `join` FAIL — GATES.md)  B103  `arch/aarch64/sched.rs:8333` prints `:: CAPSTONE COMPLETE — all 6 sync primitives verified in one boot ::` UNCONDITIONALLY, three lines after six `cap_report(name, bool)` calls that only print — nothing aggregates them — and `cap_report("join", true)` passes a literal, so the line overstates by one on every boot. `mbench.py`'s `DEFAULT_FORBIDS` catches a real `FAIL ::` so the GATE is protected; the SPEC ROW that requires the banner contributes nothing, and `pi4-regression.spec:287` carries the same weak row beside its sound `:286` COUNT. Fix: count the six verdicts on the Jetson specs the way pi already does, and make COMPLETE carry the conjunction it claims or stop claiming it. rmbp-ledger B103
✓ DONE 2026-09-12T16:xxZ: `media-writer.sh` C15-FAT32-BY-COUNT on the `--image` p1 and on the `--src` target volume (probe_medium prints VOL_CLUSTERS/VOL_FATKIND; selftest fixture `spc8.img` = the A58 shape, red arms in both modes). Bench tool, un-versioned (B97 stands): backup `media-writer.sh.bak-c15-<UTC>` beside it.
· `media-writer.sh --src`: no preflight that the staged tree FITS the target FAT, and a failed copy leaves the card mounted with a partial file (orin 27, 2026-09-12: a 640 MiB card image staged inside the tree filled the 127 MiB volume) — add C14 fits-the-volume and unmount-on-failure
· scorer STAMP-MATCH leg (wire `sha=` == card elf stamp); `media-writer.sh` is un-versioned (B97)
· SR5  R24/R25/R26 double-booked across seats and the gate cannot see it (seat-prefix RULINGS ids)
✓ DONE 2026-09-15 (LEDGERGATES, `exec-rmbp-ledgergates`, parent 2051470a — re-derive the sha at the land) — the four queue files are gated. `ledger-check.sh` now scans this file and the three track queues for exactly two things, and LAWS §3 Queues names the gate instead of saying "warning only". (a) NO CONFLICT MARKER, extended past the queues to the ledgers and RULINGS as the 2026-09-12 row below asked — go-red by mutation, one marker shape per file, all three named with file and line. (b) EVERY LEDGER ID A QUEUE ROW CITES EXISTS. The pattern was MEASURED before it was chosen and the measurement dropped a prefix: the proposed `E` matched only `error[E0080]`, rustc's diagnostic code, twice, while the three real E ids are cited by no queue row — two false findings and zero true ones, so E is out. The surviving population at this tip is 590 citations, 185 distinct. Three verdicts, which is SR12's split where it was affordable: resolves here -> OK; resolves on an enumerated head -> DEFERRED-KEEPABLE, printed with the ref that has it (5 of these on trunk today, all the jetson rows this file's own §5 says are not yet landed); resolves NOWHERE -> RED. Go-red by mutation both ways, plus the registration's own falsifier

· ✓ DONE 2026-09-16 (REPRO, exec-rmbp-repro off 56103466; opened the same day as `· NEW` by SMALLFIX, rmbp executor finding, MEASURED twice each side): **A BYTE-IDENTITY CLAIM IS ONLY AS GOOD AS ITS CACHE STATE.** `kernel8.img` from the identical pristine tree with the identical banner hashed `9d3103c3…` on a COLD target dir (deps + the `include_bytes!` user_blob rebuilt) and `0b6f2381…` on a WARM one, 2638 bytes apart; warm→warm and cold→cold each reproduce. So a before/after sha256 across a cold boundary proves nothing, and every byte-identity claim in the tree proven by two Pi builds across one is worth re-reading. `knoboff` builds both images in ONE directory (good) but does not ASSERT the deps were already warm; the gate should refuse a verdict when the baseline build compiled anything but the kernel crate. **FIXED 2026-09-16 by REPRO (`exec-rmbp-repro`, parent `56103466`) as GATE-KNOBOFF — `unaos/arroyo`'s `knoboff` verb only, no kernel code.** `_knoboff_flat` gained a per-build census of cargo's own `Compiling <crate>` lines (captured to its own file, because the run log is shared by six builds and a line in it cannot be attributed to the build whose verdict depends on it), and every run now prints `knoboff: warm=<yes|no> compiled_baseline=[…|…] compiled_tree=[…|…]` before the verdict. `_knoboff_scored_pair` warms first and scores second: it builds, reads that census, and if the pass was anything but exactly one `unaos-kernel` compile it discards those images, re-dirties the crate roots and scores a SECOND build with the deps now warm — so the extra compile is paid on cold and no-op runs and on no others. A scored build that compiles a foreign crate, or compiles NOTHING, is `2 — NO VERDICT: cache not warm (rebuilt: …)`, never 0 or 1. **The no-op half is the half nobody was looking for and it is worse than the cold half:** cargo fingerprints on mtime and `git reset --hard` rewrites only the files that differ, so a docs-only diff made the second `./arroyo knoboff deadman` of a warm key finish in **1.87 s having invoked no compiler at all**, comparing three images objcopied out of .elf files the PREVIOUS run had left behind — one of them the cold-built baseline — and print PASS. The `include_bytes!`'d `unaos/target/user_blob.bin` (`arch/aarch64/syscall.rs:3327`) is not a cargo output and no phase rebuilds it, so its sha256 is recorded before each scored build and a difference is exit 2 naming the file (`absent` on both sides under the default `baremetal`-gated sets; live under `UNAOS_TEGRA_EL0`). GO-RED five states, MEASURED: (a1) warm-up retry removed + fresh key = genuinely cold -> exit 2, census named 43 crates on the baseline and `<none>` on the tree; (a2) same key, mutation removed -> exit 0 `warm=yes`; (b1) dependency touch injected in the scratch worktree (`crates/net/src/lib.rs` mtime +2 h) -> exit 0, the retry ABSORBS it — the must-PASS fixture, an arc that edits a dep gets a verdict and not a refusal; (b2) `Compiling` parser mutated -> exit 2 naming `unaos-kernel-mutant`; (c) `xhcikbd` d78241fa vs d863eab2 -> exit 1, `warm=yes`, control fired both arches, x86 1246172 B / arm 1120995 B differing, both sizes changed. Known-identical pair (docs-only 56103466 vs 707c293d) -> exit 0. The scored images are now the same bytes from a cold key, a warm key and a third caller key on one box (x86 `1262919d…`/1537216, arm `243e2e99…`/1608416) where the same source pair scored `6c88e301…` cold before. Cost before/after on the same box state: COLD 115.61 s -> 165.57 s (+49.96 s = the one extra kernel-only compile per arch), WARM 1.87 s -> 95.23 s. Standing limit, not a bug: an arc whose diff makes a non-kernel workspace crate recompile on BOTH passes gets NO VERDICT rather than a wrong one. Gates: `bash -n unaos/arroyo` rc=0; `./arroyo check` rc=0; `bash unaos/scripts/ledger-check.sh` rc=0. LAWS §5 carries one clause ("a `knoboff` verdict is trusted only with `warm=yes`"); the section is `docs/dev/STRUCTURAL_GATES.md` GATE-KNOBOFF.

· ✓ DONE 2026-09-16 (BATTFAT, `exec-rmbp-battfat` off `a1e50849`; `unaos/arroyo` `_step` only — arroyo-only change, no kernel byte moves, so no knoboff run, R38 floor): **A BATTERY VERDICT THAT SAYS ONLY `❌` AND A PATH CANNOT BE AUDITED — AND THAT IS HOW A LEG THAT COULD NOT GO GREEN RODE ALONG UNSEEN.** This was opened as the mirror of TESTTRUNC and GATEFIX (SR10) — a RED leg scored PASS — and the measurement says the opposite, and something worse. MEASURED at `a1e50849` by lifting `_step` (`arroyo:9191-9260`) into a harness and feeding it the seat's own fold-gate capture: the red `test-fat sf 300` log scored `❌` already, twice over (`rc=1` from `test_fat_drop_retry`, AND the `FAULT_PATTERNS` scan), so **no red leg was passing**. What was actually true is that `x86 test-fat sf 300` **could not go GREEN for any tree**. `scripts/banner-cert.sh` — landed the same day by BANNERCERT — prints one `feature=<f> witness=<token> hits=<n> -> <verdict>` / `control=<f> …` line per registered witness, and a witness TOKEN is verbatim kernel text: the `selfhost` and `holocron` control probes both carry `-> FAIL` INSIDE the token. banner-cert runs inside `esp_x86`, whose stdout is the step's own capture, so `_step`'s fault scan was matching the HARNESS QUOTING a pattern, not a kernel PRINTING one. Proven on a real green run, not a synthetic one: `UNAOS_NOSDHCI=1 UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test-fat sf 120` → rc=0, 3327 lines, `U11m2 … -> PASS` present, and EXACTLY two `FAULT_PATTERNS` lines in the whole log, both of them banner-cert's control table — the `a1e50849` `_step` scores that capture `❌`, the new one `✅`. **The two defects are one defect.** The `❌` line named a path and stopped, so a false red and a true red printed the same sentence and nothing in the summary could tell them apart; a verdict that cannot be audited from the summary is precisely how an always-red leg survives, and it is the same LAWS §5 shape as a gate that cannot fire. FIX, in `_step` only: the four rules that red a judged step — (a) the command's rc, (b) a fault line in the judged capture, (c) the expected pattern absent, (d) the run TRUNCATED — each append their OWN NAME to the verdict (`❌ x86 test-fat sf 300  [rc=1; fault line]  (log: …)`), and the fault rule quotes the first offending line beneath it (`↳ first fault line: …`, CR-stripped, capped at 200 chars, backslashes doubled because `results` is rendered with `echo -e`); the fault scan skips the `^(feature|control)=… witness=… hits=` table. The absent-capture case is now SPELLED OUT instead of inherited: the scan this replaces was `awk … "$vf" || ok=0`, and awk's rc 2 on an unopenable file was silently doing double duty as SR10's "a run that produced NO capture reds, because a scan with no input cannot go red". A `$( … )` capture cannot borrow that — under this script's `set -e` the failing substitution would abort the whole battery mid-leg, and a `2>/dev/null` would hide the reason while doing it — so the absence is TESTED, and unlike before it is NAMED: `❌ pi4 kernel8-test 60 (mbench)  [no capture at …/k8.serial.log]`, measured on a step whose separate verdict file the command never writes. Skipping costs NO coverage — banner-cert's own verdict is its exit status and every call site is `|| exit $?`, so a real missing witness or leak reds the step through rule (a), not through its quoted text. This is the same distinction `--build` already draws for compiler output and the mbench leg already draws for its FORBID-pattern verdict table, now one rule in one place. The verdict FAMILIES are deliberately NOT enumerated in the scan (no `:: TSTE:|:: X86BIND:|WINX-|PULSE-W|MISSION|CAPSTONE` list): a gate keyed on a spelling set is the defect this file keeps re-learning, and the next family would be born invisible to it — the anchor is the shape of the QUOTER, which is ONE script. TESTTRUNC's rule is UNTOUCHED: `test_x86_64` and `test_kernel8` still decide truncation and exit nonzero, so (a) already reds a short boot and (d) adds only the NAME, anchored on the three verdict spellings those verbs emit (`-> TRUNCATED`, `MBENCH TRUNCATED`, `boot was TRUNCATED`) and never the bare word, which appears in their prose. GO-RED, every state on a REAL log: red fold-gate capture → `❌ … [rc=1; fault line]` + `↳ first fault line: :: TSTE: fatverb.writegate -> FAIL …`; real green capture → `✅` where `a1e50849` gave `❌`; green minus its `U11m2` line → `[pattern absent (U11m2.*-> PASS)]`; green plus one `-> TRUNCATED` verdict line → `[TRUNCATED]`; all four at once → all four named in order. Gates: `bash -n arroyo` rc=0, `./arroyo check` rc=0; `blog_dir`'s BATTERYCWD anchoring, every step's pattern, and the summary's `PASS lines (fat+pi)` awk are untouched. **OPEN, and this row cannot close it:** the same quoted-witness exposure reached every other esp-x86-backed judged leg (`x86 test 25 (MISSION)` and the four `cat test-fat.log` re-greps) and is fixed by this one rule — but NOTHING asserts that a battery leg CAN still go green. The falsifier this needs is a control probe over `_step` itself: feed it a known-clean capture and require `✅`, the way `knoboff` refuses a verdict when its control cannot fire. ✓ **THAT CLAUSE IS CLOSED BY BATTCTRL (`exec-rmbp-battctrl` off `bf299a37`, which CONTAINS this row's `b9522dd5`) — A BATTERY STEP NOW PROVES ITS JUDGE CAN GO GREEN BEFORE ANY VERDICT COUNTS.** `unaos/arroyo` (`battery`, one new `battery_control_probe`) plus a new checked-in fixture `unaos/scripts/specs/battery-control.capture` (76 lines, documented at its head) — arroyo-and-fixture only, no kernel source touched, so no byte moves and no knoboff run, R38 floor. THE PROBE runs ONCE at the top of `battery()`, below `_step`'s definition and above every call site, and costs two `cat`s of a 76-line file — no QEMU, no build, no knob: **(A)** the fixture as it stands must score `✅`; **(B)** the same fixture with one `:: TSTE: x -> FAIL` appended must score `❌` **naming `fault line`** — not merely go red, because a red for the wrong RULE is a judge broken in a way the summary would misreport. Either control wrong and the battery prints `battery: CONTROL FAILED — the judge cannot tell green from red; no verdict` and exits nonzero having run **no step** — which is the hour of QEMU behind a blind judge that this row's incident actually spent, every run, for days. It is `knoboff`'s shape on purpose (knoboff asserts `warm=yes` and forces the scored compile rather than print PASS off stale ELFs), and `battery_control_probe` shadows `battery()`'s `results` and `rc` with its own locals — bash is dynamically scoped, so the probe's two verdict lines cannot leak into the summary and its red cannot leak into the exit status; it reports through its return value alone, and shadowing rather than save-and-restore means an early `return` cannot leak one either. **(C), and this is the half that makes the fix permanent — BATTFAT's defect could not recur, but a step BORN unprovable could:** every judged `_step` call site's pattern must match a line in the fixture, and the call sites are read out of `arroyo` ITSELF (`awk -F'"'`, the quoted fields landing on the even indices, RANGED to `^battery() {`…`^}` so the probe's own two `_step` calls are not read as judged steps — a probe must not be an input to itself) rather than from a maintained list beside them: a list of patterns is the same "gate keyed on a spelling set" this file keeps re-learning, and it would go stale on the first step added. A step added without a control line stops the battery and names itself — `battery: step arm virt v3 (CAPSTONE 6/6) pattern CAPSTONE COMPLETE has no control line — add one to the fixture`, then `CONTROL INCOMPLETE … no verdict` — printed **before** the two probes run. All 9 judged patterns have a line today (`MISSION SUCCESS` ×2, `U11m2.*-> PASS`, `usbw. write lba=.* ok` ×2, the three ELF-off-FAT verdicts, `CAPSTONE COMPLETE`); the three `--build` steps and the rc-only pi4 step are correctly skipped, the first because its verdict file is compiler output this judge does not scan, the second because an rc-only step asserts nothing for a capture to carry. **(2)** the summary's FIRST line now carries the state — `════════════ BATTERY SUMMARY (0s) control=ok ════════════` — so a pasted capture of a battery run states that its judge was proven; `control` is DERIVED and never asserted (every other path returns above it), and it is on the header rather than appended at the end because a truncated paste keeps the header. MEASURED, the judge run IN ISOLATION on three real captures (every function extracted from the tree's own `arroyo` by line range, never retyped, the real `battery()` carried into the harness as a definition that is never called so the coverage pass has real call sites to read): the 2026-09-15 green fold-gate capture → `✅`; the red fold-gate capture → `❌ [fault line]` + `↳ :: TSTE: fatverb.writegate -> FAIL …`; the synthetic truncated capture → `❌ [TRUNCATED]`, and that one is the interesting one — its `did not reach MISSION SUCCESS in 120s` prose SATISFIES the pattern rule, so only rule (d) can red it. GO-RED, the incident itself: reintroduce BATTFAT's defect (delete `$0 ~ q { next }` from `_step`'s fault scan) → the clean fixture reds with `↳ first fault line: control=selfhost witness=:: SELFHOST: … -> FAIL :: hits=0 -> OK` → `CONTROL FAILED`, i.e. this row's lost days are now caught in two `cat`s before the first boot. SECOND go-red, on the other half: delete the line that sets `ok=0` on a fault line → (A) stays green and **(B)** reds with "the judge greens a tree it has a finding against". Both reverted. FOURTH state measured: the fixture minus its `CAPSTONE COMPLETE` line → `CONTROL INCOMPLETE`, naming the step and the pattern, before either probe. Gates: `bash -n unaos/arroyo` rc=0, `./arroyo check` rc=0. INVARIANTS HELD: no step's own pattern or command changed, `blog_dir`'s BATTERYCWD anchoring untouched, and `_step`'s four rules are untouched — the probe DRIVES them, it does not amend them. `docs/dev/PLAYBOOK.md` is unchanged because it names no battery output shape (`grep -n 'control=' docs/dev/PLAYBOOK.md` → no hit; the file does not mention `battery` at all).

· ✓ DONE 2026-09-15 (SPECRUN, `exec-rmbp-specrun` off `b555badd`; `unaos/arroyo` + `unaos/scripts/mbench.py` + new `unaos/scripts/spec-roots.sh` + `unaos/scripts/specs/*.spec` — arroyo/scripts/specs only, no kernel byte moves, so no knoboff run, R38 floor; opened by STORWAIT's STOP 2): **A REPLAY SPEC NO GATE COMMAND RUNS IS A SILENT LANDMINE — IT CANNOT CATCH A REGRESSION, AND IT STILL BENDS THE KERNEL AROUND ITSELF.** MEASURED at `b555badd`: `grep -n 'specs/' unaos/arroyo` named `x86-fat.spec` — 32 pinned witness lines, one of them (`x86-fat.spec:156`) pinning the `[fatverb] storage witness` serial line VERBATIM — in TWO FULL-LINE COMMENTS and in NO LINE OF CODE, so no run ever replayed it. Both halves of the cost are real and the second is the one that is easy to miss: a kernel change that reddened those 32 pins shipped green, AND STORWAIT, respecting a pin it could not change, added a SECOND `storage settle:` line to the kernel rather than edit the pinned one — a kernel carrying two spellings of one witness, bought by a file no command reads. **CENSUS FIRST, against this seat's fold-gate captures** (the SERIAL REGION of `g2-test-fat-sf.log`, 2684 lines, and of `g2-test-x86-wc.log`, 2228 — the arroyo stdout log is NOT the capture: banner-cert's control rows quote `-> FAIL` inside a witness token and replaying the whole file scores the harness, BATTFAT's finding one level down): **x86-fat.spec 32/32 pinned lines still MATCH — zero stale, nothing to re-pin and nothing to drop**; x86-test.spec's one COMPLETE marker matches both. The other six x86 specs are metal or knob-leg specs and match a QEMU capture only in part BY CONSTRUCTION (x86-wc 8/8, x86-witness 26/42, round6-rmbp 29/49, rmbp-boot 10/12, x86-holocron 0/8, x86-wifival 0/11) — that is not staleness, and each now says so in its own file rather than being papered over. **THE FIX IS THE VERB, NOT A NEW GATE.** `x86_spec_replay` is the last step of `test_x86_64`, so `./arroyo test` replays `x86-test.spec` and `./arroyo test-fat sf` (and `UNAOS_FATIMG=sf ./arroyo test`) replays `x86-fat.spec` against the same `target/serial.log` the fault scan just read, through `mbench --replay` — the single verdict authority for the spec path since ad8b52aa — and mbench's rc is the verb's rc. **ORDER IS THE TRUNCATION HONESTY**: fault scan, then TESTTRUNC's MEASURED end-of-run marker, THEN the replay — and the replay REFUSES outright (rc=1) on any completion verdict but `complete`, written as a guard and not as a comment, because `x86-fat.spec` declares no COMPLETE of its own and `32/32 pinned lines` over half a boot would be the most convincing false green in this tree. `mbench.py` now emits `FIRST-SHORTFALL <spec>:<line> <kind> <pattern>` on a FAIL (and ` (pinned at <spec>:<line>)` on the short row) — ONE line the verb quotes verbatim, so the table and the verb can never disagree about WHICH pin is short; a green table is byte-identical to before. **AND THE CLASS, in GATE-ROOTS' shape** ("a binary no check leg names is never type-checked"): `./arroyo check` grows `scripts/spec-roots.sh`, which reds BY NAME on any spec under `scripts/specs/` that is neither named in arroyo's CODE (full-line comments stripped — the prose mentions are exactly what made x86-fat.spec look run) nor carrying a `# RUN-BY: <who> — <command>` header naming the bench / knob-leg / script command that does. A `RUN-BY: verb:` claim is cross-checked against arroyo, so a file cannot talk its way into "gated"; controls run on synthetic input first (a comment-only mention must NOT resolve gated, a code mention MUST) plus one fact about this tree (`pi4-regression.spec` resolves GATED), or exit 2 with NO verdict. All 12 specs now declare a runner. **⚠ TWO GAPS NAMED RATHER THAN CLOSED**, both for the same reason this arc exists — a gate nobody has watched go green is not a gate: `test-fat part|gpt|p16` gets NO replay (x86-fat.spec was measured on `sf` only, and there is no green capture for the other three on this seat) and `test-selfhost` gets none (SOURCE-ALONG changes the capture; that lane already scores its own `SELFHOST: src verified … -> PASS ::`). Both print a skip line naming the reason instead of passing quietly. The CONTRACT — *a pinned line is changed TOGETHER with the kernel line it pins, in the same commit; re-pinned naming the arc, or dropped with the reason, never worked around by teaching the kernel a second spelling* — is in all 12 spec files and in the verb's red. **⚠ IT IS AT EACH FILE'S TAIL, NOT ITS HEAD, and that is measured too**: ledger rows, queue rows and kernel comments cite pinned lines POSITIONALLY (`x86-fat.spec:156` from `crates/kernel/src/shell.rs:4933`, `:238`/`:242` in rmbp-ledger B71, `pi4-regression.spec:1549` in PI5, `jetson-sync1.spec:1839` in B39, `round6-rmbp.spec:6` in env-knobs), so a header insert moves every one of them silently — rmbp-ledger PI5 already names tail-append as this repo's safe form. All 12 spec edits are pure appends; `git diff` shows zero line moves and `x86-fat.spec:156` is still the `[fatverb] storage witness` REQUIRE. **THE LESSON FOR EVERY SEAT: a spec file is a ROOT, like a binary — it is nobody's dependency, so unless a command names it, nothing evaluates it, and it is then strictly worse than absent.** Check `ls scripts/specs/` against `grep -n 'specs/' arroyo` on your own track's specs (`jetson-jd5.spec`, `jetson-sync1.spec`, `pi4-barename.spec`, `pi4-regression.spec` are all declared here but only the two pi4 ones are GATED by a verb).

✓ RESCUED  DOCSRESCUE  2026-09-22 (`exec-rmbp-docsrescue` off `2fc9c71c`; docs only, `git diff --stat 2fc9c71c HEAD -- unaos/` EMPTY; `ledger-check.sh` rc=0 after each of the three commits) — **the three DOCS-LOST arcs BRANCHCENSUS found are back, one cherry-pick each, every one of them conflicted and none of them dropped.** **LAWSNOM** `exec-orin21-lawsnom` bf93acf9+f86cf448 (2 commits, no squash — the second is the self-caught correction that names the arc) and **LAWSNOM2** `exec-orin22-lawsnom2` 66523c05: both conflicted WHOLE-FILE, because LAWS.md was re-consolidated 2026-09-09 into numbered §0-§9 and not one of the five sections they edit ("Verification", "Code and history", "Direction", "Arcs and executors", "Coordination") exists under that name any more. Resolved by taking HEAD's file whole inside the conflict and judging each nominated entry on its own against the consolidated text. LANDED, 9 entries, verbatim, each marked at its end `(nominated orin 21/22, rescued 2026-09-22 — Peter's word pending)` — RULINGS.md NOT touched, a nomination is not a ruling: §3 "Boot cold, boot dumb, presume nothing about the machine" and "A clean merge is where composition defects hide"; §4 "The brief is the only channel into a running executor"; §5 the rmbp 15/16 arming split, base-scoped counts, "Legibility outcompetes soundness", "Derive from a different end and compare", "Require a PROPERTY, never a LIMITATION", "The early-stop terminus kills everything below it", "Wire beats comment". DROPPED AS ALREADY LAW, 2 of 11: "A check is trusted only when its corpus can produce more than one outcome" (headline verbatim in §5's "A check that cannot fire is an absent one") and "a matrix leg's feature list is not a build verb's forced set" (§5 "Cite the declaration site, not the symbol", rmbp 16, same day — the consolidation got it from rmbp's copy). DROPPED AS UNLANDABLE, 1 — **the only content loss in this arc, and it is Peter's to rule on**: LAWSNOM2's `## Direction` heading and its preamble *"Design laws — they bind what the code may assume, not how a session runs. Everything else in this file is subordinate to them."* That is a PRECEDENCE ORDER over the whole rulebook, and it cannot be stated without the section; a `## 10. Direction` after §9 "Standing facts" would put the section that claims to outrank the file at the bottom of it, and renumbering LAWS.md is not a rescue's warrant. The rule itself landed in §3; its rank did not. **LEDGERROWS** `exec-orin21-ledger` fc1a2d52: four conflict regions, all ID COLLISIONS — all fourteen ids were re-used for different findings in the two weeks the branch sat unmerged. Renumbered uniformly, A46-A55 -> **A96-A105** (+50) and SO22-SO25 -> **SO50-SO53** (+28), with every cross-reference renumbered with them (the four `(→ SO4x)` pointer rows, the prose block, S13's new A101 cite, A28's new A105 cite) and no pre-existing row touched. **The next free ids in both files are UNUSABLE and this is the finding to keep:** GATE-LEDGER's `QUEUECITE_REG` registers 35 ids — an A-range of 30 and an SO-range of 7 plus two shared ids, all read off `unaos/scripts/ledger-check.sh` and deliberately NOT re-typed here, because writing them in this file is itself a citation the gate scores — that `docs/dev/OS/orin-queue.md` cites and no ledger on any head defines. That registration goes RED AS STALE the moment one of them starts resolving (`ledger-check.sh:667`), so landing a rescued row at the bottom of that range would have reded the gate AND pointed live citations at content that is not what they cite. Those 35 are the 2026-09-13 session's rows, still unwritten; **any future rescue lands ABOVE the top of that range, read from `QUEUECITE_REG` and never guessed — and this row was itself reded once for naming the range's endpoints in prose (rc=1, six `citation to nothing` findings), which is the go-red proof that the rule is enforced.** 399 rows, was 385; census still "grandfathered ids 35 (+0 registered id(s) not cited in this tree)". Three ticks landed on text byte-identical to their 2026-09-08 base (S13, A28, B4). Status stays `open` for all fourteen and that is still correct by the row's own prose: they name shas that live only on `exec-orin20-*`/`exec-orin21-*`, which are still on no track. NO STOPs — every hunk found its target. TWO DIVERGENCES FROM THIS FILE'S OWN RESCUE ROWS, taken openly because the executor brief overrode them: §5's LAWSNOM row asks for the BOOTIDLIVE example to be re-typed out, and the brief said land verbatim — so `aec2c604` survives in two entries as the historical measurement (dead by DIRECTION is a fact about the code, not about the 2026-09-07 reading); §5's LEDGERROWS row asks to drop the rows DISPOSITION.md killed as class (A), and the brief said renumber-and-land — so A96, A99 and A100 are in, and A100 is the measured instance the rescued "clean merge" rule cites, which would otherwise cite nothing. If those strikes are wanted they are named here and they are three lines. ALSO FLAGGED FOR PETER, kept both sides rather than edited: two rescued entries (the arming split, the terminus rule) prove arming/reachability with `strings`, while §5's Logs bullet now rules *"Artifact certification uses `LC_ALL=C grep -a -o -F`, never `strings` (counts moved 320 to 322)"* — a later ruling the 2026-09-08 nominations predate; and "The brief is the only channel" rules (1) and (2) name a peer round, lane grants and peer blockers, which R46 and the one-session direction retired, while its headline and rule (3) (a `BRIEF-AMENDMENT.md` re-read at every commit boundary) are untouched by that. Branch shas: 5779128b (LAWSNOM, +77 -1 LAWS.md) · 704df6bd (LAWSNOM correction, +7 -5) · 704b7e19 (LAWSNOM2, +127) · 1740880f (LEDGERROWS, +28 -3 over LEDGER.md + orin-ledger.md) — re-derive at the land. Closes the LAWSNOM-RESCUE, LAWSNOM2-RESCUE and LEDGERROWS-RESCUE rows above.
✓ RESCUED 2026-09-22  TOOLRESCUE  (`exec-rmbp-toolrescue`, off `2fc9c71c`) — **5 of 6 BRANCHCENSUS tooling rescues folded, 1 STOPPED.** NETVERB-RESCUE `557afe47`+`23bddcf8` -> **2974ca72**, **0c414917** (clean; knoboff net6 rc=1 MOVED, which the commit itself pre-justifies — the `dns` registration is deliberately NOT net6-gated, ONE OS means the word exists on every build; both images same size, x86 1600824, arm 1618440). WIREHYG-RESCUE `9c42a93c` -> **32e76100** (orin-ledger A59/A61 conflict: A59 took WIREHYG's row, which appends `via=cache age_ms=N` per SO47; A61 kept hw-rmbp's, which carries a later 2026-09-15 addition. ids A72/A73 -> **A83/A84** per this row, in the 2 ledger rows and all 6 cross-refs. knoboff net6 rc=0 byte-identical). ⚠ THE RENUMBER WAS NOT BOOKKEEPING: `exec-rmbp-storgpu` merged LUN2+KFUNWEDGE onto hw-rmbp mid-session (`3bbeb754`) and took A72/A73 for itself. Two seats reached for the same two ids on the same day; the QUEUE's pre-assigned deconfliction is what kept them apart. SNTPCLIENT-RESCUE `ad549988` -> **00a60ee4** — and **THE OWED SEAM IS LANDED**. The trunk §5 row's 'one client, three drivers' shape is UNCHANGED (net6 still aarch64-only at `net_phy.rs:797`, `register_nic` still 2 callers, `witness_tick_sntp` still only `e1000.rs:1219`), so no contradiction and no STOP. ORINTIME recorded one statement as OWED because NETVERB held net_phy.rs that session; this fold held both, so it is transcribed verbatim onto `net6::init()`'s return line (`net_phy.rs:1041`, folded onto `ok`, file line-neutral at 2110). The Jetson's clock no longer needs `tste` typed at the console. selftest.rs conflict was two same-line appends (FONTAA + sntp6) kept BOTH, line-neutral at 1148. knoboff sntp6 rc=0 byte-identical. DRAGMETAL-RESCUE `75c32d5e` -> **29d8aa2a** (4 conflicts, all two seats appending to one tail, BOTH kept. ⚠ THE TAIL UNION IS NOT A CONCATENATION: both parents end with the same `);`/`}`/`}`, so git's common-suffix dedup hoists it OUT of the conflict and the naive paste left the first block unterminated — measured, GATE-BRACES serial_ring.rs +2, wm.rs +1. Re-derived under `merge.conflictStyle=zdiff3`, which proves base=0 lines, and each block was given its own suffix copy; 201/201 .rs files balance. Ordering call: SERWIRE runs after SINKDRAIN because it zeroes its odometer ON ENTRY, so it inherits none of sinkdrain's traffic, while the reverse order breaks serwire's own rule. knoboff wc rc=0 byte-identical). MATRIXPAR-RESCUE -> **cf09c1fa**, **962e7c96**, **734d6380** — took the LOCAL tip `5c1f13d0` series per this row; origin `ce0d992b` carries the same title and 116 of the lines but its WHY is orin 21's thrash hypothesis, which `e90c69ed` re-measured, did NOT reproduce, and replaced with a measured table (cold 387.2->152.5 s at P=6, 2.54x; warm ~3 s either way; 2.4 GB disk, 2.56x CPU) plus a 'not measured, so not claimed' note. Nothing unique lost. Conflict: hw-rmbp's GATE-CENSUS and the P-wide wave both expanded one base line — census first (it only prints), wave second, one shared `cd`. knoboff rc=0 byte-identical (arroyo-only, UNGATED, images unmoved: x86 1600824, arm 1618440). ⛔ **GATEHYG-RESCUE `78ca893a` NOT FOLDED — and the reason is a REAL DEFECT its own gate caught on first contact.** Its `scripts/fixture-reachable.sh` reds on this tree: `DEADFIXTURE selftest.rs::run callers=1 legs_found=14 legs_declared=13 -> FAIL`, naming `video.font.aa`, which FONTS2X (`582278cd`) added to the interactive-only `tste` suite (`selftest.rs:457`) — a leg that ships in every image and is run by NEITHER `test` NOR `test-arm`. `selftest.rs:1013` itself prescribes the cure as one `verdict("video.font.aa", …)` line in shell.rs's boot-fixture block. ⚠ THAT ONE LINE DOES NOT GREEN THE GATE, measured against the script: LEG B scrapes `selftest.rs` (`found=$(sed … "$ST" | grep -o …)`), not shell.rs, so adding a shell.rs verdict leaves `legs_found` at 14; and `test_font_aa` is private (`selftest.rs:1034`), so shell.rs cannot even call it without a `pub`. The gate names exactly two cures and the fold must pick one: MOVE the leg (drop the `report(…)` from `selftest.rs::run`, make the fixture `pub`, add the shell.rs verdict) or DECLARE it (add `video.font.aa` to the script's DECLARED with the reason, the way k8-reach.registry accounts for an unarmed knob). Both change content this fold did not write, so both need an owner. GATEHYG's other three parts are un-conflicted and still live (banner truth, target-keyed feature gate, GATE-VERBALIAS which passes rc=0 standalone); its §1 LADDER-TAIL is SUPERSEDED on x86 by TESTTRUNC/GATEFIX (`X86_TEST_VERDICT` + `x86_spec_replay`, rc=1 not rc=5) but still live on `test-arm`, where `test_aarch64` passes an empty spec to `qemu_wait_or_complete` and has no completeness verdict at all. Owner: unassigned — re-cut as its own arc. GATES: `./arroyo check` before each commit, every compile/spec/hygiene leg ✅ (157–158) and the ONLY finding `stale queue-citation registration` for ids the picks resolved (SO47, A69, SO45, A83, A84; the A72/A73 + A66/A67/A68/A80/A82/A95 lines are sibling seats landing on hw-rmbp/hw-jetson, not this branch). `ledger-check.sh` not run separately — denied to this seat; its output is inside each check log. END RUN `UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 150` **rc=0, completion marker REACHED at serial.log line 2332, wall 210.2 s, log clean, every pinned line of x86-default.spec present, 26 ✅ / 0 ❌**. ⚠ ONE CHECK WAS VOID AND RE-RUN: the bench overlay hit 100% in a window ending ~16:45Z and `check-matrixpar.log` died on `No space left on device` (8 aarch64 legs, incl. `arm-virt-sntp6`, which had compiled ✅ in the two preceding checks) — NOT a code defect; re-run clean at 17:02Z as `check-matrixpar2.log`. The void log is kept renamed `VOID-diskfull-*`. Logs: `~/unaos-bench/scratch/rmbp-0915/toolrescue-logs/`.

✓ RESCUED 2026-09-22 (second series)  TOOLRESCUE/GATEHYG  (`exec-rmbp-toolrescue`) — **GATEHYG-RESCUE `78ca893a` IS FOLDED, and the row above that says it was not is SUPERSEDED by this one.** `23ec7262` FONTAA leg move + `075f7b67` GATEHYG. The arc was blocked by its OWN new gate, which is the best evidence it works: `fixture-reachable.sh` named TWO legs sitting in the interactive `tste` suite, shipped in every image and run by no gate. **`video.font.aa` took the first cure and was MOVED** — `report(…)` dropped from `selftest.rs::run`, `test_font_aa`/`Outcome` widened to `pub(crate)`, and one witness-gated `font_aa_witness()` folded onto `layout_witness();` in `fatverb_storage_witness`, body at the FILE TAIL. Both hunks same-line/tail because shell.rs and selftest.rs compile into every image and one added line moves every `panic::Location` beneath it (LEDGER P7, the rule shell.rs states at its own `install` arm); selftest.rs holds at 1148 lines. **`net.sntp` took the second cure and was DECLARED**, with the reason at the roster: its fixture is `sntp6`+`net6`+aarch64-gated and NO headless verb in this tree arms `sntp6`, so moving it would green the gate while leaving the leg as unrun as it is today — laundering the finding rather than closing it. ⚠ That entry must not outlive the day a gate arms `sntp6`. GATE, measured both directions: `DEADFIXTURE selftest.rs::run callers=1 legs_found=14 legs_declared=14 -> PASS` rc=0; go-red with the `report()` put back `legs_found=15 legs_declared=14 -> FAIL` rc=1; reverted, PASS rc=0, selftest.rs unchanged. WIRE: `:: TSTE: video.font.aa -> PASS ::` now appears in the capture — the leg has a verdict instead of a call nothing reaches. CONFLICTS: §1 LADDER-TAIL is SUPERSEDED on x86 (TESTTRUNC/GATEFIX landed a spec-declared completion marker, rc=1 not rc=5, plus the positive `x86_spec_replay`), so HEAD was taken — keeping both would have put the WEAKER checker first and weakened the landed gate; but `ladder_tail_check` IS KEPT at its `test-arm` site, real coverage this tree lacks since `test_aarch64` passes an empty spec and has no completeness verdict at all. GATE-APPEND both-intents. QUEUE §5 HEAD (all four incoming open-form rows already carry their ✓ DONE closures here). ⚠ **KNOBOFF IS EXIT 1 AND THAT IS THE HONEST RESULT, not a miss.** ISOLATED: `knoboff wc 23ec7262` (GATEHYG alone) is **exit 0, byte-identical both arches** — it adds no code, only `arroyo`, two scripts and docs. The mover is the LEG MOVE, and it moves the image BY CONSTRUCTION: the fixture was called unconditionally from `selftest::run` and is now called from a `witness`-gated site, so no knob makes the two images equal — a fixture that actually RUNS costs bytes, which is the point of moving it. Measured: x86 1600824 -> 1620256 (+19,432 B), arm 1618440 -> 1620048 (+1,608 B). The tempting explanation was REFUTED rather than assumed: `#[inline(always)]` on `test_font_aa`, to restore the `Face` const-propagation the FONTAA note prices at ~98 KB of Chrome atlas, changed the sizes by ZERO bytes on both arches, so that trap is NOT what this is; the change was reverted rather than kept for a mechanism it does not buy. The residual x86/arm asymmetry is unexplained and is NOT claimed. END RUN `UNAOS_WC=1 UNAOS_QEMU_FULL=1 ./arroyo test 150` **rc=0, completion marker REACHED at serial.log line 2292, wall 150.7 s, log clean, 26 ✅ / 0 ❌**. `./arroyo check` 160 legs ✅ (two more than before — the two new gates, both green: `✅ verb alias closure`, `✅ fixture reachability`), rc=1 on the registered stale-queue-citation class only.

· ✓ DONE 2026-09-24 (PIDESKCOND, rmbp-ledger **B208**: a fourth cond term `@arch:<arch>`, the row scoped to aarch64, UNVERIFIABLE-by-name on x86 instead of NO VERDICT; proven on both artifacts, GATES.md) — was: NEW 2026-09-22 (rmbp, NEUTRAL M1 finding, `docs/dev/evidence/rmbp-0915/neutral/m1-bannercert-pidesk-x86-RED.txt`): **THE `pidesk` ROW IN `unaos/scripts/banner-cert.sh` HAS NEVER FIRED ON x86** — `UNAOS_WC=1 UNAOS_PIDESK=1 ./arroyo esp-x86` exits 2 NO VERDICT at 4d8d047d and at the rename tip alike, because its rename-INVARIANT control (`activate DECLINE reason=no-panel`) is also 0: no `desktop_firmware` string reaches an x86 image at all, so the row's `cond=-` (every arch) is wrong. Gate-map fix: scope the row to the arch that compiles the module, and add the present-control that proves the row can fire (LAWS §5, a check that cannot fire is an absent one).
· ✓ DONE 2026-09-24 (JS1-GREEN, orin-ledger A106, cloud session: the capture's three pre-orin-26 lines rewritten to the ORIN-CORE0 spellings; 27/27 on both tools, go-red ×3; `docs/dev/evidence/rmbp-0924/js1/JS1-GREEN.md`) — was: NEW 2026-09-22 (rmbp, NEUTRAL M1 finding; JETSON's to run): **`unaos/scripts/specs/jetson-sync1.spec` IS RED AGAINST ITS OWN SYNTHETIC GREEN CAPTURE** at 4d8d047d and at the rename tip alike — 24/27, first shortfall at `:675 TEGRA-EL0` — which is the one thing that capture exists to disprove. The rename moved both spellings (`pending 7/32 → 5/32` was the only signal that a plain-token sed had missed the regex-escaped rules); the residual 3 are pre-existing. A spec whose synthetic capture cannot pass it is a gate that certifies nothing (LAWS §5).

· ✓ DONE 2026-09-24 (QUIETKILL, rmbp-ledger **B209**: `qemu_await.py --quiet` — after the grace the kill waits for a quiet, newline-terminated tail, bounded by the cap; synthetic proof + control in GATES.md; the next bench `kernel8-test` measures it live) — was: NEW 2026-09-22 (rmbp, gate9a on cd642fd8, `~/unaos-bench/scratch/rmbp-0915/logs/foldgate/g9r-kernel8-test.log`): **THE HARNESS'S FAST-COMPLETION KILL CAN TRUNCATE A COMPLETE RUN** — `kernel8-test 300` read `126/126 required witnesses … [fast: completion +127.2s grace 20s wall 147.8s]` and then `✂️ MBENCH TRUNCATED (INCONCLUSIVE) … capture ends MID-LINE (no terminating newline) — the writer was killed in the middle of a write`, rc=3. The kill fired after the completion marker and the 20 s grace, on a box at load 34, while the guest was still printing `[click2] depth gui_chan=0 (se`; the truncation rule (LAWS §5, a truncated run is never a pass) then refused a boot that had printed every witness and its terminal marker. Fix in `unaos/arroyo`/`qemu_await.py`: after the completion marker, the kill waits for the capture to go quiet (no byte for N ms, N ≥ the mirror's rollup period) or for a newline at the tail, before it fires; the mid-line check stays. SPLASHGATE named the same family (`--settled` folding short-witness completion into truncated). Until fixed, an INCONCLUSIVE with all witnesses present and the completion marker seen is re-run, never scored.

· ✓ DONE 2026-09-24 (FORBID-UNREACHABLE, rmbp-ledger B210, cloud session: mbench and foreman count every spec FORBID's longest literal in the artifact named by `--artifact` or the fresh sidecar's `artifact=`; 0 → ◦ UNREACHABLE row + summary count, advisory; arroyo writes `artifact=` on test/test-arm/kernel8-test/install-pi; GATES.md §B210. The rename-table half — per-file expected counts that die on a miscount — stays a NEUTRAL2 tool, not folded) — was: NEW 2026-09-22 (rmbp, NEUTRAL2 go-red, rmbp-ledger B160): **A FORBID ON A RENAMED TOKEN READS GREEN WITH 0 HITS — A RENAME BATCH CAN PASS A FORBID-ONLY GATE HAVING BROKEN IT.** With the base spec held against the renamed tree, the REQUIRE went 6/6 → 5/6 and named itself; the FORBID on the same old spelling stayed ✅ with 0 hits, indistinguishable from a FORBID that passed. Fix in `unaos/scripts/mbench.py`: a FORBID whose token has ZERO hits in the artifact (`LC_ALL=C grep -a -o -F` of its literal prefix on the ELF under test) is reported `⚠ UNREACHABLE`, not ✅ — a check that cannot fire is an absent one (LAWS §5); the present-control is the REQUIRE beside it. Second spelling found by NEUTRAL2: the DROPPED `:: ` prefix (`jetson-sync1.spec` writes `FORBID TEGRA-SD:` bare; two docs hand the operator `awk 'index($0,"PINSTALL:")'`) — a rename table must enumerate per-file expected counts and die on a miscount (NEUTRAL2's table does; make it the standing tool).
· ✓ DONE 2026-09-24 (WCLANE, cloud session: `x86_spec_replay` now picks `X86_WC_SPEC` for `UNAOS_WC=1 UNAOS_QUARRY=1 UNAOS_FTDIRX=1` with no medium/lane knob — the leg the spec names as its gate; measured on the next wc run, GATES.md) — was: NEW 2026-09-22 (rmbp, MENUFIRST, rmbp-ledger B156): **`x86-wc.spec` IS NOT IN THE HARNESS'S AUTOMATIC REPLAY SET** — `grep -n 'x86-wc.spec' unaos/arroyo` is empty, so MENUDROP (B121), MENUSTAT (B148), MENUFIRST (B156) and every `[dmgovlp]`/`:: DOCKID:` pin there are hand-scored today; the scored set is what the harness invokes (LAWS §5). Fix in `unaos/arroyo`: the wc lane (`UNAOS_WC=1 … ./arroyo test`) replays `x86-wc.spec` the way the default lane replays `x86-default.spec`, with LADDER-TAIL's terminal marker; go-red by deleting one REQUIRE.

## 6. NEEDS PETER (not jobs until he rules)
· ✓ RULED R67 2026-09-24 ("no need for root to do screenshots"): the refusal STANDS for root — design, not a bug; nothing to build. Was: ROOTSHOT (flight 13 §4, 2026-09-24): Print Screen as root is REFUSED — `:: PRTSCR: no user session (reason=no-session) — a capture belongs to a user's own Desktop folder … NOTHING WRITTEN`. R54's rule predates R63 naming root. Does root get a Desktop (`/home/root/Desktop`, created like a user's), or does the refusal stand for root? Not a bug until Peter says which.
· ✓ RULED R66 2026-09-24 ("please turn the volume down on the test tone"): boot 14 flies the tone at the 4096 default (rmbp-ledger B215); a louder level is a per-build `UNAOS_HDA_AMP=<n>` choice, never the default. The `:: HDA-PCM:` read-back on boot 14 says whether the sample layout was the "sandpaper" mechanism at any level.
· IOAPIC3 (flight 13 §6, B191 answered on the metal): Apple's firmware leaves every PIRQ_ROUT at 0x80 (IRQEN clear) and D29IR routes the EHCI's INTA to PIRQG; the router REFUSES with `reason=pirq-disabled`. The next rung WRITES a chipset register (clear bit 7 of PIRQG_ROUT and choose the IRQ, or route I/O APIC input 16+6 directly) — a chipset write on the rMBP is Peter's go.
· S27  138 prune-candidate origin refs from the 2026-07-25 triage, never OK'd (408 remote-tracking refs today)
· "one file is the rulebook, EVERYTHING ELSE DELETED": the deletion half never started (R20, destructive)
· D1 loader `SetMode` to the widest console mode (knob-gated, one power cycle)
· B7  vug arbiter placement (kernel-side recommended)
· B86  the most expensive rule in the corpus is REMEMBERED, not GATED: no gate, hook or assert anywhere inspects a spawn's `model:` before it costs money (a case-insensitive `fable` search over `unaos/scripts`, `unaos/arroyo` and `.claude` returns documentation only). A PreToolUse hook that refuses an `Agent` call resolving to Fable fails CLOSED. It edits HARNESS CONFIGURATION, which no seat changes on its own initiative and no peer can authorise — Peter's call. rmbp-ledger B86
· B99  `origin/exec-orin22-matrixpar` points at `ce0d992b`, the PRE-AMEND object, abandoned within the same second and carrying BOTH designs the two seats rejected; the real tip `5c1f13d0` is on no remote and cannot be pushed to that ref (non-fast-forward; a force-push is a STOP tripwire). Agreed remedy: publish under a fresh name (`5c1f13d0:refs/heads/exec-orin22-matrixpar2`, the corpus's own `<slug>2` convention) and RETIRE the old ref — the delete is Peter's alone. Until then two refs carry the name and origin's MATRIXPAR must not be folded. rmbp-ledger B99
· `:: tegra:` ×42 in `unaos/crates/kernel/src/main.rs` (S6/R16, NEUTRAL-TABLE §2, rmbp 2026-09-22): the last colon-prefix family naming a board in a shared file is NOT a rename — those lines are the Tegra bring-up narrative around `tegra_early_stop` (the `!` terminus at main.rs), and the honest fix relocates the terminus and its narrative into `arch/aarch64/`, a seam decision. NEUTRAL2 renames every other colon family and leaves these; the count at its tip is in its row.
· The xHCI hub 100 ms debounce settle (BOOTWAITS, rmbp-ledger B152, 2026-09-22): flight 11's boot waits were re-measured and the settle is the only remaining cut; CCSTRIM already refused it on this evidence (the T_ATTDB cliff is asymmetric — 80 ms prize vs +100 ms penalty on a one-port sample — and the trim must survive the uncalibrated-TSC fallback, already 100→~74 ms). Overturning a metal-proven constant with a written cost model is Peter's call, not an executor's.
· `:: TEGRA-EL0:` ×2 in `main.rs` (NEUTRAL2, rmbp-ledger B160, 2026-09-22): NOT renamed, and NEUTRAL-TABLE §2's row is wrong twice — `:: EL0:` is taken (`arch/aarch64/syscall.rs` emits it 26 times), and the board tag is DELIBERATE: `mmu_tegra_el0.rs:115-117` routes six witnesses through `const EL0TAG = "TEGRA-EL0"/"VIRT-EL0"` so a QEMU capture and an Orin capture are told apart by the same awk (`arch_arm64.md:10052-10069`); renaming only the shared half splits the PASS/FAIL pair across `main.rs:7146` and `syscall.rs:23835/23838`. Peter's call: rename all four sites incl. arch to a free spelling (`:: EL0HELLO:`), or rule the family a MACHINE-IDENTIFYING field by design (R16's stated-hardware-reason exception). Until ruled: left as is.
