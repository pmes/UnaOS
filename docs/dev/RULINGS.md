# RULINGS — Peter's words, verbatim, dated, with the session that heard them

> A relay of a ruling is a report, not the word (LEDGER.md P4). This file holds the WORD: the quote as
> typed, the date, and which seat heard it in its own session. Every other document — LAWS.md,
> CLAUDE.md, batons, memory files, ledgers — links a ruling by id (`R<n>`) instead of paraphrasing it.
> A paraphrase, where one is unavoidable, is marked *(paraphrase)*. Never edit a quote. Rulings get
> REVERSED (the cube ruling was retracted; EVAC was rejected at its premise), so every row carries a
> `status` — **live** · **superseded** · **retracted** — and a `superseded-by` naming the R-id that
> replaced it. A reader who finds a dead ruling must be able to see it is dead. GATE-LEDGER checks both.

> One home for the words. The rules derived from them live in ONE file, `docs/dev/LAWS.md` (consolidated 2026-09-09); LAWS.md cites an R-id and never paraphrases it.
> Verbatim means the words as typed. A paraphrase is marked `(paraphrase)`. Each seat appends what
> it heard in its own session; merges are by union.
> Numbering: rmbp 11 seeded R1–R7 on hw-rmbp; orin 13 appended R8–R17 on hw-jetson. One sequence after the union
> merge (reconcile #2, 2026-09-05); never renumber an id that has been cited. Each seat appends what it heard in its
> own session; merges are by union.
> **Double-booked ids (rmbp 18, 2026-09-09 trunk sync).** Two seats allocated `R24` and `R25` independently on
> unpushed branches — orin 17's pair (2026-09-06, landed on trunk/hw-pi4/hw-jetson) and rmbp 16 / orin 22's pair
> (2026-09-08, hw-rmbp). Both are Peter's words and neither may be discarded (R20); "never renumber a cited id"
> above forbids collapsing either, and both are cited — orin's 54x on trunk, rmbp's 34x here. So the id cell carries
> the heard-by seat, `R24 (orin 17)` vs `R24 (rmbp 16)`, and a bare `R24` in prose is AMBIGUOUS: read it by which
> branch the document came from. **The third arrived: `R26` collided at the hw-jetson landing exactly as predicted, and is now
> seat-qualified the same way** — orin 17's (2026-09-06, the shell verb table) and orin 22's (2026-09-08, bundling
> the tests). Three double-booked ids, no renumbering. This is the failure the seat-prefix vote (2026-09-06, ledger-check.sh:247)
> already fixed for the ledgers and never extended to this file; extending it is a cross-seat decision, tracked as SR5.

| id | date | heard by | quote (verbatim) | applies to | status | superseded-by |
|---|---|---|---|---|---|---|
| R1 | 2026-09-03 | rmbp 11 | "orin is the focus." | focus assignment for the week of 2026-09-03 (bench) | live | — |
| R2 | 2026-09-03 | rmbp 11 | "orin 13 is right fucking there we do now wait for i do not even know what arc you refer to" | peers are live: a cross-seat ask goes over ccd in the same turn, never "noted for the arc" (LAWS §COORDINATION; memory `message-peers-same-turn`) | live | — |
| R3 | 2026-09-03 | rmbp 11 | "if you want to do unattended reboots you cannot because there would be nobody to hold down option" | the rMBP's ⌥ picker is the human step; unattended x86 reboots need the card as the default startup volume first (rmbp-ledger A4) | live | — |
| R4 | 2026-09-03 | rmbp 11 | "how do we stop poisoning non-arch specific code? clear from the start orin would never be the only machine in unaos to reboot for fucks sake" | board names out of arch-neutral code; subsystem names the witness (LEDGER S6, GATE-NEUTRAL; memory `name-by-subsystem-not-board`) | live | — |
| R5 | 2026-09-05 | rmbp 11 | "and adversarially look for ways to improve discussing among your teammates" → clarified: "by that i mean look for ways to improve your internal issue tracking" → "don't discuss it with me discuss it with your teammates" | the seats converge on tracking changes among themselves and report OUTCOMES; the result is GATE-LEDGER, the status enum, evidence-in-git, this file | live | — |
| R6 | 2026-09-05 | orin 13 *(rmbp relays; orin appends the verbatim line)* | *(paraphrase)* audits and inventories are high value; the waste is trashing results in scratch and re-running rather than ticking things off a list; each track keeps its arch ledger and there is one over-arching ledger | LAWS §Ledgers; LEDGER.md; the arch ledgers | live | — |
| R7 | 2026-09-05 | orin 13 *(rmbp relays; orin appends the verbatim line)* | *(paraphrase)* make sure your teammates know you are probably finding things they need to know | LAWS §COORDINATION; LEDGER.md owner column + same-turn message | live | — |
| R8 | 2026-09-05 | orin 13 | "already pushed what are you waiting for there is tons of work to do" | a pushed landing is not a stop; the focus seat keeps the floor full | live | — |
| R9 | 2026-09-05 | orin 13 | "that's minor tho so much is broken on orin" / "the panel looked fine i just mean all the hardware is just barely supported" | ORIN-HW inventory (orin-ledger §F); the five ranked gaps | live | — |
| R10 | 2026-09-05 | orin 13 | "yes, it is trying to load the old background pulse" | the pulse window is retired from the Orin's scaffold pass (orin-ledger A10, B1) | live | — |
| R11 | 2026-09-05 | orin 13 | "but the shell needs to go into its window and the desktop made a normal desktop so is the work worthwhile?" | scaffold polish stops; the cascade (`deskcascade`) is the arc | live | — |
| R12 | 2026-09-05 | orin 13 | "we are most certainly going to hit the 5 hour limit as we ate thru 2/3 in 1/2 hour!! i am watching. run high value jobs so we can feel the glory" | budget discipline: no exploratory fleets; one high-value executor at a time | live | — |
| R13 | 2026-09-05 | orin 13 | "inventories and audits are high value i just feel like you trash your results and have to rerun rather than ticking things off a list" | LEDGER.md + per-arch ledgers; every audit briefed with the ledger | live | — |
| R14 | 2026-09-05 | orin 13 | "make sure your teammates know about keeping better track and that you are probably finding things they need to know" | cross-lane findings go on LEDGER.md with an owner AND to that seat the same turn | live | — |
| R15 | 2026-09-05 | orin 13 | "each of you should have your arch list and the over-arching list for all" | `docs/dev/OS/<track>-ledger.md` ×3 + `docs/dev/LEDGER.md` | live | — |
| R16 | 2026-09-03 | rmbp 11 (relayed to orin 13) | (paraphrase, as relayed) platform names must stop leaking into arch-neutral code — "clear from the start orin would never be the only machine in UnaOS to reboot" | subsystem-named witnesses/symbols in shared files (PWRNAME; GATE-NEUTRAL) — rmbp holds the verbatim | live | — |
| R17 | 2026-09-05 | orin 13 | "good work! card in. prt sc worked! once. hit the button twice and the 2nd file is empty. it looks like you killed the wrong pulse. the windowed one is gone, the embedded one is there and so is the old status bar at the bottom. both must go." | A17 (second capture empty); A18 (embedded pulse + bottom status bar go; windowed pulse returns) | live | — |
| R18 | 2026-09-06 | orin 14 | "wait, i thought you were trying to write the card immediately to do a quick boot. we want a full boot not a quicky" / "i thought we can probe the hardware boot by boot to make the GA10B work" | the next boot is a FULL boot on the round's complete image, not a quick knob check; GA10B is NOT ruled out — it is probed boot by boot (orin-ledger §F GPU row re-opened; GA10B-LADDER.md; the signed-microcode fact stands as the wall the ladder must reach, the licence question on NVIDIA's blobs stays Peter's) | live | — |
| R19 | 2026-09-06 | orin 14 | "i've seen it a few times when you're running through the 3D card probes where you take a path that does not work out so you shut it out but after very many boots its discovered that path needs to be open for later paths to succeed" | a probe rung that FAILS is recorded as "failed under <conditions>" with its knob and code KEPT (never-trash), never "ruled out"; every ladder rung names the earlier rungs it depends on being OPEN; a later rung may re-open an earlier failure as its first step; §F rows lose the words RULED OUT for hardware paths | live | — |
| R20 | 2026-09-06 | rmbp 11 | "THROW IT AWAY? YOU HAD BETTER FUCKING FIX THAT. I DO NOT FUCKING GOD DAQM WANT ABOTHER FUCKING SESSION TO EVER FUCKING SUGGESST THAT SHIT AGAIN." | Context: the seat offered "abort the merge" as an option for a staged, conflict-resolved, 3-of-4-legs-green reconcile. The never-trash law covers gated work in progress: a seat FINISHES it — commit on the gate it has, state what did not run — and never presents discarding it as a choice. Applies to merges, patches, executor branches, captures, plan files: nothing with work in it is offered for deletion; it is finished or ledgered. **Carried from hw-rmbp verbatim (orin 25, 2026-09-09): LAWS.md cited R20 twice while this row existed on hw-rmbp alone.** | live | — |
| R21 | 2026-09-06 | orin 15 | "WHO PUT THE GOD DAMN MENU IN THE WINDOW IT GOES IN THE ----- GOD DAMN MENU BAR" (on seeing the pulse window's `View` menu drawn as the first row of the window's own content on render6 boot 2) | menus belong in the MENU BAR, never inside a window: the bar hosts the focused window's menu titles beside the crystal and drops them there; the in-window menu strip PULSEWIN `27922509` (2026-08-17) drew because "the kernel has exactly one menu framework" is the defect, not the design; the bar-side protocol already sketched in `video/menubar.rs` ~:1296-1370 (MenuPublish) is the direction, kernel windows publishing directly first; A10 (Esc dismisses the pulse menu) moves to the bar's menu; new row orin-ledger A25; ONE-OS: arch-neutral, shared `video/` (rmbp lane, grant asked 2026-09-06) | live | — |
| R22 | 2026-09-06 | rmbp 12 | "WHY DID YOU FUCKINIG START JOBS ANSWER RIGHT FUCKING NOW" / "IT HAS FUCKING BEEN ESTABLISHED FOR THE SECOND GOD DAMN WEEK THAT ORIN IS FOCUS." | Context: the seat read baton `rmbp-12.md` ("rmbp 12 is SUPPORT ... no fleet") and in the SAME turn, before writing Peter a word, spawned three executors, justifying it with LAWS §Throughput's floor of 3. That floor belongs to the seat that HOLDS THE FOCUS; a support seat has no arc, therefore no floor — it spawns ZERO, not fewer. Two of the three agents produced nothing at all. The focus was also already settled and in the seat's own resume file; see R21 (orin 15, same night). LAWS §ROLES amended; memory `support-has-no-executor-floor` | live | — |
| R23 | 2026-09-06 | rmbp 12 | "NOW YOU ARE BAILING FOR WHAT REASON? I DID NOT FUCKING TELL YOU TO STOP THE SUPPORT ROLE ASSHOLE!!!!" | The inverse of R22, twenty minutes later in the same session: told to stop starting jobs, the seat went fully dark — including on a lane grant the FOCUS seat was blocked waiting for. A stop on STARTING JOBS is not a stop on BEING SUPPORT. An operator stop names a scope: apply it at that scope and keep everything else running. Grants, verification and answers to the focus seat need no permission and never pause; leaving a peer blocked is a second failure, not caution. Same root as R22 — scope taken from a rule instead of from what Peter said | live | — |
| R24 (orin 17) | 2026-09-06 | orin 17 | (~15:55Z) "esc should not close any app windows" · (~16:20Z) "tab is not meant to permanently switch between windows - that was a thing we did early for when mouse did not work." | Heard at the bench during the render8 flight, two clauses on the same subject: the keyboard does not command windows. **(1) Esc dismisses MENUS ONLY.** It never closes an app window. This RETRACTS KEYDOORS F1's design (Esc closes Quarry) outright - `video/quarry/live.rs:1906`'s `if c == 0x1B { close(); return true; }` is what the ruling withdraws - and F1 is recorded as RETRACTED BY RULING, never as a FAIL. Second-order effect, deliberately recorded: LEDGER SO9's stated operator workaround ("close Quarry with Esc and the shell behaves") is withdrawn with it, so SO9 now has no workaround and its priority rises. **(2) the Tab FOCUS-CYCLE is RETIRED**, not broken - it was scaffolding from the period when the mouse did not work. KEYDOORS F0 is RETIRED, its wire fact (7 x `KEY 0x09`, `tab-cycle=0` on render8) is a datum only, no fix is owed, and render9's scorer must drop the leg or it will keep printing a FAIL for a feature that no longer exists. A11 (TAB mid-drag leaves the grab on the old window) is a SEPARATE question and is NOT retired by this. Applied as: orin-ledger A10 (F0/F1 legs), A11 (untouched), LEDGER SO9 (priority); `docs/dev/evidence/orin16/FLIGHT-RESULT-render8.md` §RULING. Numbering: R22 and R23 already exist in the `hw-rmbp` copy of this file, so the next free id in the union is R24 (the `R22`/`R23s1`/`R24 boot7` tokens under `docs/dev/OS/` are round and sitting labels, a different namespace) | live | - |
| R24 (rmbp 16) | 2026-09-08 | rmbp 16 | "THIS MOTHER FUCKING IDIOT ORIN 21 IS RESPONSIBLE FOR THROWING AWAY HUNDREDS OF FUCKING DOLLARDS IN CREDITS NOT THE 3 OF YOU TALKING BACK AND FORTH" | Context: orin 21 clamped inter-seat comms to "an ask or a blocker, 10 lines" on the stated grounds that a third of the 5-hour limit went in half an hour, and rmbp 16 reported that clamp. **Peter rejects the clamp's PREMISE: cross-seat coordination is not the burn, and the attribution is to orin 21's own conduct.** The standing law is unchanged and re-affirmed — R2, peers are live and an ask goes over ccd in the same turn; a seat does not suppress coordination to save budget. What the waste actually was is not named in this ruling and no seat should infer it; R12's discipline (high-value jobs, no exploratory fleets) is the nearest live rule. rmbp 16 did not adopt the clamp | live | — |
| R25 (orin 17) | 2026-09-06 | orin 17 | (~16:0xZ, on seeing render8's crystal at the far right) "ASSHOLE YOU JUST DISCOVERED YOUR GOD DAMN MISTAKE BESIDES FUCKING CRISPY IS A MAC CLONE AND GOD DAMN MAC MENU HAS BEEN AROUND SINCE FOREVER" · "WHEN I SAY THERE IS A GAP ON THE LEFT YOUR GOD DAMN ANSWER IS TO MAKE IT ALL THE WAY AND NOT FUCKING CLOSE THE GOD DAMN GAp" · "you can undo the crystal fix and not have it take an hour" / "by that i mean the previous \"fix\"" | The crystal is the Mac apple menu: TOP-LEFT of the bar, always. His render7 sentence ("crystal menu has a gap to the left as should be seen in scr 5 and it should be all the way to the right edge") meant CLOSE the 12 px inset in place; orin 16's rows (LEDGER SO4, orin-ledger A33) paraphrased it as "flush RIGHT" and executor CRYSTAL built the paraphrase (bb513370), rejecting the close-the-gap reading because it contradicted the rows. General rule from it (UNAOS-LAWS §COMMUNICATION): a glass complaint about a gap/inset/offset is fixed in place, never by relocation; Peter's glass words go in the row verbatim with the seat's reading in a separate marked clause; a fixture move to another edge is a one-line question before code. Applied as: LEDGER SO4 + orin-ledger A33 re-written; bb513370's geometry replaced by CRYSTALFIX (crystal group at x=0, drop-down at x=0, witness `gap_left=`), rmbp 14 grant 16:1xZ | live | - |
| R25 (orin 22) | 2026-09-08 | orin 22 *(rmbp 16 records the consequence in its lane)* | "another way to think of it is like on the macbook if UnaOS saw catalina and immediately formatted the disk as an alien enemy." | The third clause of the disk-relationship rule, after HOME (the disk you booted from) and FRIEND (another UnaOS disk — recognise, never wrap your life around it, R-context B90). **A non-UnaOS disk is a STRANGER: the kernel never writes, formats or installs on its own initiative, and a disk changes only by an operator's explicit act.** Soft form in the rmbp lane is `video/prtscr.rs`'s capture ladder writing to a foreign volume (B89); the hard form is the installer engine, and B91 records that its only existing guard protects HOME, not strangers | live | — |
| R26 (orin 17) | 2026-09-06 | orin 17 | (~16:5xZ, on the shell verb table) "some of the commands were 1 off tests we need basic commands back" · "special ls commands and such are relics of testing past" · "most of those probes are useful still no?" · "that's an error -- battery powered devices will use batmon you must stop trying to pin everything down and lock it in this is one OS not a bunch of separate ones. switch to the standard names where possible" | Three clauses on the kernel shell's verb table. **(1) Basic verbs come back** — help/echo live in midden_core since c7b12e23 (rmbp 14: not removed, moved; a shell.rs copy would recreate the two-interpreter split); grep/wc/df/env/history/sleep/which never existed and are new, placed core-side unless they need kernel state (SHELLBASICS executor, patch-first). **(2) Standard names replace ours where a standard exists** — bootlog→dmesg, vfs→mount, xd→hexdump, usbinfo→lsusb, netinfo→ifconfig/ip, setdate→date -s, sched→ps — replacing, not aliasing, every reader updated in the same patch; the duplicated unafs verbs (uls ucat umkdir umv urm urmattr utouch uwrite) retire once a transcript proves the plain verb covers the unafs volume; usnap→snap. **(3) No verb is pinned to a platform** — batmon serves every battery-powered device; the probe verbs (tste, burst, simmer, storm, batmon, run, screenshot, usnap, xd, panic, pulse/vug/v3d knob-gated) STAY, grouped under a `bench` class in `help`. Applied as: SHELLBASICS + SHELLRELICS (orin 17 executors), LAWS §ROLES "ONE OS" | live | - |
| R26 (orin 22) | 2026-09-08 | orin 22 *(rmbp 16 appends the verbatim from orin's own written record; orin heard it)* | "so many changes result in these huge tests — can we not bundle the tests so we don't spend most of the time waiting for each individual executor's build tests to complete." | **Note the form: the WORD is a QUESTION, and the rule below is the seats' construction from it — recorded separately so the two are never confused.** THE RULE, as adopted by all three seats: per executor commit = TARGETED gate only (the touched arch's `check`, the ONE QEMU leg that exercises the change, the commit's own RED-first mutation); the FULL battery runs ONCE on the integrated tip, seat-run, as the arc's DONE gate. **Two sharpenings, both adopted: pi 9 — "cannot AFFECT the gate" is not "the gate cannot SEE it"; skip only when the change cannot affect what the gate ASSERTS, and NAME the assertion, because "no row for this" is a reason to ADD a row and never to skip (a skip with a named assertion is auditable; a bare n/a is not). rmbp 16 — B94: "cannot affect it" must not be read as "it is only a comment", because `panic::Location` embeds source lines, so any `.rs` edit including a pure comment takes the targeted gate unless it is line-neutral BY COUNT.** B92 (MATRIXPAR) is the lever that makes the bundling cheap | live | — |
| R27 | 2026-09-08 | orin 22 *(verbatim as relayed; orin heard it and holds the record)* | "FOR SURE I DO NOT WANT TO WAIT FOR THE OTHER PLATFORM'S TESTS TO WAIT FOR THE MORE DEFINITIVE METAL BOOT. FROM NOW ON FOCUS PLATFORM ONLY UNTIL METAL RESULTS ARE IN SO THE OTHER ARCH'S TESTING IS DONE WHILE WE MOVE FORWARD TO THE NEXT ROUND." | The pre-flight gate is the FOCUS PLATFORM'S OWN. **⛔ CORRECTED by Peter within the hour — "NO I SAID UNTIL METAL TEST RESULTS ARE IN": the other arches' batteries do NOT run during the flight, background or otherwise.** THE SEQUENCE IS: focus-platform gate → stage → card → metal boot → **metal result IN** → *then* the other arches' batteries start, concurrent with the NEXT round's work. They remain a LANDING condition and are never a FLIGHT condition. Applies to the rMBP week identically (x86 gate pre-flight, aarch64 batteries backgrounded). **Scope confirmed against the tooling: this reaches the QEMU batteries (`test-arm`, `kernel8-test`), NOT `check` — there is exactly one `check` verb (`arroyo:7237`) and `check_both()` (`:4193`) compiles x86_64 then aarch64 in one function with no single-arch mode, and per B92's measurements it is 3.1 s warm, so excluding it costs nothing and splitting it would be work for no gain.** **⚠ Two conditions this lane carries: the trade bites HERE hardest, because x86 leads shared kernel-core and so x86 changes are the likeliest to break the other arch — and under the CORRECTED sequence that break surfaces AFTER the boot rather than beside it, which is later, not milder; and the deferred battery must produce a NAMED LANDING ARTIFACT or it silently becomes "never" (B95's family) — SHARPER under the corrected sequence, because those batteries now run CONCURRENT WITH THE NEXT ROUND'S WORK, so they are read by a seat whose attention has already moved on** **⭐ THE REASON UNDER THE RULE, Peter verbatim 2026-09-09 01:00Z via orin 22: "THIS WILL HELP WHEN WE START SELF HOSTING WHERE WE WILL BE REBOOTING THE VERY MACHINE WE JUST WROTE THE CODE ON. THAT'S WHEN THE HARD FOCUS COMES INTO PLAY. THE CODE IS STILL GENERALIZED BUT THE MACHINE ITSELF IS COMPILING AND TESTING **FIRST** THEN THE WIDER AUDIENCE GETS THEIR RUN." So R27 is not a scheduling optimisation — on a self-hosting box the build, the test and the reboot are ONE MACHINE and its own verdict is first BY CONSTRUCTION. Note what it does NOT license: "the code is still generalized" — the focus is on whose verdict comes first, never on narrowing what the code presumes.** | live | — |
| R28 | 2026-09-09 | orin 22 *(verbatim as relayed; orin heard it)* | "when we have self hosting with healing the machine will run itself until it runs right and we do not want all that great work hard coded as an appendage special case situation we want to grow UnaOS." | The self-heal primitives — find your own disk by content, an honest self-verdict off the wire including mode and truncation, the focus-first self-gate, the friend and stranger rules — live in the KERNEL and `arroyo`, generic. Nothing of this arc becomes a bench-only tool. **The test this lane draws from it: a self-healing machine cannot depend on anything that lives outside the thing being healed, so the question for any tool is not "is it useful" but "can the machine run it on itself". B97 is the first instance found — `card-watch.sh` exists TWICE, a stale macOS fork in-repo and the real one bench-only.** Ties to R27's self-hosting rationale and to B89/B90/B91's ordering | live | — |
| R29 | 2026-09-09 | orin 22 *(verbatim as relayed; orin heard it)* | "when in healing mode the machine will need an alternate boot disk with the last known good bootable version to help when there's a hard lockup of the test kernel." | ROADMAP, not an arc. **The x86/UEFI half is this lane's, and two facts shape it before anyone designs a mechanism: the loader uses NO UEFI variable services today (zero hits for `SetVariable`/`GetVariable`/`BootNext`/`BootOrder`/`RuntimeServices` across `bootloader/src/` and `builder/src/`), and on THIS machine firmware boot selection is Apple's picker plus `bless`, not standard UEFI variables — `rmbp-ledger A4` / R3, "firmware boots macOS unless ⌥ is held at power-up".** So a recovery step of the form "next boot goes to the other disk" assumes firmware honours a variable we set: plausible on a standard UEFI box, unproven here. **And it is an appendage for two of three boards before it is written, since the Pi has no UEFI at all. The shape that generalises sits BELOW firmware: ONE medium carrying both the test kernel and the last-known-good with OUR loader choosing, and the attempt counter as a FILE ON THE BOOT MEDIUM — written by the loader, cleared by the kernel on a clean boot — because a filesystem our own code reads is the one capability all three boards share.** Composes with BOOTROOT, which already answers "which image did I boot" by content. **The second UnaOS disk stays home soil and is NOT the recovery mechanism: making recovery depend on what is plugged in is B90 exactly** | live | — |
| R30 | 2026-09-09 | orin 22 *(verbatim as relayed; orin heard it)* | "if there's no good bootable kernel a user will hopefully be able to reboot into linux, mac, or win to use our installer to assist in the initial healing process." | The HOST-SIDE INSTALLER is a PRODUCT — cross-platform, in-repo, versioned — and the last rung of the healing ladder (self-verdict → fallback to the known-good image → the user boots another OS and runs UnaOS's installer). **This supersedes rmbp 16's "bench scripts as labelled scaffolding" position: the write scripts do not get preserved, they CONVERGE into one product verb — known-good image onto a medium, verified, with named identity refusals.** **⚠ Two corrections this lane owes on it: B97's test ("can the machine run it on itself?") is scoped to SELF-HEAL PRIMITIVES and does NOT apply here, because rung 3 runs on another machine BY DESIGN — its test is "can a user on a stock Linux/macOS/Windows box run it with no toolchain?"; and "operator-driven, therefore safe by construction" is BACKWARDS. An operator picking the wrong disk is still an operator act, and rung 3 runs on a machine that is ENTIRELY strangers — the user's system disk, their data, their other OS — with no home disk to protect. B91's ordering does not apply; a STRONGER requirement does, because the blast radius is a machine we do not own. `load-card10`'s named identity refusals are not a bench nicety carried forward, they are the product's PRIMARY SAFETY FEATURE** | live | — |
| R31 | 2026-09-09 | orin 22 *(verbatim as relayed; orin heard it)* | "remember we have a superpower system that can export perfectly native apps." | Rung 3's installer is a HANDLER exported as a native app per host OS through the existing export system (`vessels/` — `una` ships a macOS app today), **not a script**; the write-path identity refusals live in that handler ONCE rather than per bench tool, and the bench scripts converge into it. **⛔ THE GAP THIS LANE FLAGS BEFORE IT HARDENS: the export system solves DISTRIBUTION, NOT PRIVILEGE. A native app on the user's machine still has no permission to write a raw disk — macOS needs root plus `diskutil unmountDisk` and `/dev/rdisk`, Windows admin plus volume locking, Linux root or udisks. Checked in-tree: NOTHING does a privileged host-disk write today (the only `sudo`//dev/rdisk` hit across `vessels/`, `handlers/`, `libs/`, `tools/` is a line in `handlers/holocron/README.md`). So a per-host-OS ELEVATION path is separate and unbuilt, and it is exactly the kind of thing discovered late and bolted on as the appendage R28 forbids.** | live | — |
| R32 | 2026-09-08 | orin 23 | (~23:45Z) "anything in ~/unaos-bench/scratch/ could be deleted in 10 seconds as it is temporary use only" | Nothing durable lives in a round's scratch: tools go to `~/unaos-bench/tools/` (the card writer moved), records go to `docs/dev/evidence/<round>/` (orin 22's whole record and the render11 tooling were found with zero commits and filed as 3efde934). The audit that finds them: `git log --all --diff-filter=A -- '*<name>'` per record a resume names. | live | — |
| R33 | 2026-09-08 | orin 23 | (~23:40Z, on `load-card10.sh`) "that really is a dumb file name" · (~23:52Z) "~/unaos-bench/tools/media-writer.sh" | A round number in a TOOL's name is a fossil; the tool is `media-writer.sh`, Peter's name. A round number on a RECORD of that round (`evidence/orin22/`) is a date and stays (rmbp 17's distinction, adopted). | live | — |
| R34 | 2026-09-09 | orin 23 | (~00:35Z, after four render11 glass defects were answered with "not this arc") "WTF ARE YOU GOD DAMN TALKING ABOUT NOW NOTHING GETS FIXED FUCK YOU I JUST AM SO TIRED OF ALL THE FUCKUPS WHEN WILL IT FUCKING END" | A defect Peter sees on the glass at the bench gets FIXED in the round he saw it, by executors spawned that turn — never ledgered for a later arc. The arc boundary is not a reason he accepts. (Executors on Opus; his standing instruction the same session: "do not launch fable executors unless absolutely".) | live | — |
| R35 | 2026-09-09 | orin 23 | (~00:38Z) "WE ARE STILL ONLY ON ONE CORE WTF DUDE" | The wire of render11 read `online=0x3f el1cores=0x1` and every EL0 first-run "on core 0": six cores answered CPU_ON, one hosts. Secondaries must join the scheduler with their own tick; a board that reports N cores online and runs on one is a defect, not a milestone. | live | — |
| R36 | 2026-09-09 | orin 23 | (~00:50Z) "VUG WINDOW NAMES ARE DUMB AND WINDOW TITLES NUMERICALLY SEQUENCED IS FOR UNTITLED DOCS ETC" | An app window's title is the app's NAME (declared, else its program name), never a generated label and never "Name 1 / Name 2". Numeric sequencing is reserved for documents without a name ("Untitled", "Untitled 1"). | live | — |
| R37 | 2026-09-09 | rmbp 17 *(verbatim as relayed; orin 23 did not hear it — Peter told rmbp to tell both seats)* | "DO NOT READ OTHER SEATS' BATONS" · (rmbp's account of the reason, paraphrase) a baton is one track's owed list, not an assignment and not evidence about another seat; the focus comes from Peter in your own session or you do not have it | orin-23.md's stale title ("SUPPORT SEAT … Peter at the cafe", written 12:30 before a trip that did not happen) was relayed by rmbp 17 to Peter as a fact about who held the focus. The orin-23 baton now carries a STALE note at its top; the orin seat takes the focus from Peter's own words in this session (R34/R35) and reads no other seat's baton. | live | — |
| R38 | 2026-09-09 | orin 23 | (~02:2xZ, on six executors each running the full battery in parallel) "what the fuck is going on with sticking to the masses of tests?" | The §19/§14 rule restated on its own breach: an EXECUTOR builds and proves its own fixture (red/green) and runs no battery; ONE battery runs once, on the fold, by the seat, before the card. Six parallel batteries pegged the box for hours; the seat had written the full battery into every brief. Applied: the compositor-gate executor was stopped and relaunched lean; the fold branch (exec-orin23-fold) carries the one gate. | live | — |
| R39 | 2026-09-10 | rmbp 18 | "THIS SHIT WHERE X86 IS PROOFING PI OR ORIN ENDS RIGGHT FUCKING NOW. OR ANY COMBO OF THE OPPOSITE WHERE ONE PLATFORM IS DEBUGGING A FUCKING NOTHER. FIGURE OUT THE BEST WAY TO DO THIS AMONGST YOURSELVES!! I HAVE BEEN TALKING ABOUT THIS FOR FUCKING DAYS AND THE SHIT IS STILL FUCKING NOT BEING DONE!!! WHAT THE ACGTUAL FUCK??!!?" | No seat proofs, gates or debugs another seat's board — the mechanism is the three seats' to settle. **Why it kept recurring after everyone agreed: the required gate MANDATED the violation.** LAWS 3 makes the trunk battery a landing condition and `./arroyo battery` is cross-platform by construction — measured this turn and CORRECTED BY pi 10 the same hour, SIX of its 13 legs are not the x86 seat's: `check (tegra)` and `esp-jetson` are JETSON, `pi4 kernel8-test` is PI, and `arm virt v2`, `arm virt v3` and `arm usb-write witness` are AARCH64 (the last is a `cat` of the v2 capture, so it is an assertion on an aarch64 boot). This seat first counted 3 by tagging the aarch64 legs 'shared' — **pi 10's diagnosis of that error is the load-bearing part: a leg with no named owner reads as nobody's, so it defaults to whoever is running, and whoever is running is the landing seat.** Every landing therefore forced one seat to proof all three. rmbp 18 then spent an hour diagnosing the Pi's harness with pi 10 live, twice in one day, the second time after being told. The fix is structural, not a promise to remember: each board's legs are run by the seat that owns it | live | — |
| R40 | 2026-09-08 | pi 9 | "a big diff with the pi vs orin is the microsd is simple to plug and pull on the pi" | The easy pull is a HAZARD, not a mitigation: a pulled Pi card goes into the same host reader every other board's card lands in, which `flash-pi4.sh` hardcodes as `/dev/mmcblk0` and never interrogates — the mechanism by which an Orin card became a repurposed Pi card. Pi needs target identity MORE than the Orin, precisely because its media moves. It also makes boot-cold literally true on this board rather than a discipline: the card in the slot this boot may genuinely not be last boot's card, so the kernel is not pretending not to know, it does not know. Ledger: SP17. | live | — |
| R41 | 2026-09-08 | pi 9 | "sounds like the script should be in the repo no? all platforms use it to build a disk image from now on anyway, correct? since we are creating a partitioned drive w/UnaOS on it?" | Answered with two corrections that strengthen it rather than soften it: `flash-pi4.sh` builds nothing (41 lines, one `dd`, no mkfs/sfdisk/parted) and the partitioned-image builder `unaos/scripts/make-pi-img.sh` is already in-repo and versioned; and there is no shared script — each platform has its own unversioned bench writer. The real shape: the BUILD half is in-repo, the WRITE half is several unversioned per-platform scripts, so the layout the repo defines and its only realisation can drift with nothing to catch it. Whether bench tooling enters the repo is Peter's call, twice-routed. Ledger: SP17. | live | — |
| R42 | 2026-09-08 | pi 9 | "WTF does it matter what method I choose to boot? You are assuming too much." | Struck the premise that root identity comes from being HANDED a serial by a loader, which makes the BOOT METHOD load-bearing — UEFI hand-off works, VideoCore firmware loading `kernel8.img` does not — and fails the Pi by name solely because its boot path has nobody to do the telling. The card is the drive; the kernel enumerates what is there and finds the volume it booted from by CONTENT. It killed pi 9's proposed `serial == 0` guard and pi 9's grant condition #4, which would have made the Pi's failure a REQUIRED witness in `pi4-regression.spec` — a green gate certifying the defect. Before requiring a witness, ask whether it asserts a PROPERTY or a LIMITATION. | live | — |
| R43 | 2026-09-08 | pi 9 | "wtf is kernel8, why is there an exception" | No per-board exceptions in the harness: the QEMU fast-exit helper was respawned as ONE helper for EVERY QEMU verb rather than a `kernel8`-only mode. Same ruling that killed the `serial == 0` guard, one layer out. Generalisation is a property of the CODE; focus is a property of the SCHEDULE (R-cite: the self-hosting ruling) — the two are not in tension. | live | — |
| R44 | 2026-09-10 | pi 10 | "it is what i do not want done and that's the big metal support jobs needed. everything else support brings forward so the three of you stay at the level you're at now. focus session will be strict to their architecture to speed up time between metal test builds. sometimes a metal boot needs a quick fix for a wedge so AFTER the successful metal boot support archs compile test their code so focus can start patching. if support branches have so much work executors are required com with focus on executor load distribution for sharing" | *(reading, pi 10's, marked as a reading and not part of the ruling)* Support does not run the big metal jobs; everything else support brings forward so the tracks stay level. A focus session stays strict to its own architecture to shorten the time between metal builds. AFTER a successful metal boot the support arches compile-test their own code so focus can patch a wedge without waiting. A support seat needing executors coordinates load distribution with FOCUS, not with Peter. | superseded | R46 |
| R45 | 2026-09-11 | orin 27 (trunk worktree `../UnaOS`) | "this needs to house the primary queue for all platform agnostic jobs." · "you may as well think you started in all these branches as all must be kept up-to-date" · "queues here house the platform specific jobs only. all the rest go to main remembering to isolate down to the specific metal requirement as far as possible so as to maximize shared code which you'd route to the main queue." · "the beauty of this is there is no confusion switching platform focus." · "we need to keep all these branches in sync. having the 3 in sync will allow for a merge to main any time." | ONE trunk queue on `main` (`docs/dev/QUEUE.md`) holds every job that does not need a specific board; each track queue (`docs/dev/OS/<track>-queue.md`) holds ONLY jobs that need that board's metal or that board's own files. A job is cut down to its metal-specific part and the rest is routed to the trunk queue. All four branches are kept level so any track can merge to main at any time. | live | — |
| R46 | 2026-09-11 | orin 27 | "i say you focus on orin as we have been, but you are all lanes. there is no owner of anything. it's just work on the OS." · (next message, answering the plan) "1 there will still be a overarching focus per session such as orin for this one" | A session has ONE overarching focus (orin for orin 27) but is ALL LANES: no seat owns a file, a subsystem or a board's legs; there is no support seat and no lane grant. LAWS §1 focus-seat/support-seat rules and §3 "Lanes" are retired by this ruling; R39 (no seat proofs another seat's board) stands as a rule about WHICH LEGS prove a landing, not about who may edit what. | live | — |
| R47 | 2026-09-11 | orin 27 | "you will need to run executors at some point, so you must follow the rules. use whatever level opus model rated high enough for the job at hand. fable executors for only the most key tasks." | Executors run Opus by default (the standing rule); a Fable executor is reserved for the few key tasks the seat names as such (the plan of 2026-09-11 named: the three landing merges, the queue files, scoring the rung-4 wire). | live | — |
| R48 | 2026-09-11 | orin 27 | "i think in order to catch errors like the 3 would we need to go from session to session as a challenge to do better than a follow this or else. give each new session a new point of view... \"here's what you're taking over. here's what i did and it was this or that but maybe something is being missed and we're not seeing it\"" | The handoff between sessions is adversarial, not a rule list: each close writes what is being taken over, what was done and why, what was chosen between, and a "what I may have missed" section; the next session attacks that section before starting new work. Replaces the LAWS §2 baton-header rule. | live | — |
