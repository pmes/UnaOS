# PETER'S RELAY SHEET — not for specialists. Coordinator overwrites this with
# the message(s) Peter posts into each Gemini chat, verbatim.
# (updated 2026-07-25, after s25boot1: fw surface is LINEAR pitch 0x4000 (!); falcon base likely wrong; pulls 16 + 23 briefed)

## → kepler-display session

Display: s25 verdict — YOUR RECON ENDED THE GUESSING. The mirror was rock-stable across both passes, and the head-0 ISO surface cluster decodes completely: 0x460=00000200 (offset>>8 → fw surface at VRAM+0x20000, our >>8 pointer convention confirmed), 0x468=07080B40 (SET_SIZE 1800×2880 exact), 0x46C=01004000 (SET_STORAGE: bit24 LAYOUT=PITCH, pitch>>8=0x40 → LINEAR, 16384 bytes/row), 0x470=0000CF00 (format 0xCF). The scanout is NOT block-linear — every seam and checkerboard since s19 was aliasing of linear-16384 against our assumed layouts (your s18 "pitch≠11520" read was right all along). Pull 16 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-display-pull16-linear-16k.md`: ONE cycle, linear fill, rows strided 16384 bytes (2880×4 ruler + black padding), lin-step markers, latch/restore unchanged, recon dumps gated off. Seam-free + solid white column = the mapping war is over. Proposal first. PUSH OWED reminder stands.

## → kepler-fence session

Fence: s25 verdict — your pulse was electrically clean (off rb shows bit 12 clear, on rb shows it set) and changed nothing: ports still BADF1000 on every access. That pattern — dead on ALL accesses including control readbacks, unmoved by enable state — is the signature of a register that DOESN'T EXIST, not a gate. The spec's §2 base (0x400180) is likely wrong for GK107: on this family the PGRAPH context-switch Falcons live at 0x409000 (FECS) and 0x41A000 (GPCCS), register file offset from the unit base (CPUCTL +0x100, IMEMC +0x180, DMEMC +0x1C0). The spec doc now carries a caution note. Pull 23 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-fence-pull23-falcon-base-recon.md`: READ-ONLY two-pass dense recon of base+0x000..0x1FC at both candidate bases (fal-base markers + per-base verdict line), zero writes, verify the bases against envytools hwdocs and cite in your proposal. Real values at either base = the Falcon is found and pull 24 moves the sentinel probe there. Proposal first. PUSH OWED reminder stands.
