# PETER'S RELAY SHEET — not for specialists. Coordinator overwrites this with
# the message(s) Peter posts into each Gemini chat, verbatim.
# (updated 2026-07-25, after s28boot1: GOP overlap found — display results were confounded; ucode blocker named; pulls 19 + 26 briefed)

## → kepler-display session

Display: s28 verdict — your probe worked perfectly and it caught something big, though not what either of us expected. The overlap check fired on its first boot: `gop-overlap=YES-RESULT-VOID surf2=01600000+01C20000 gop=00020000+01C20000`. Our scratch surface sits INSIDE the firmware's GOP framebuffer — which is itself 1800 rows × 16384 B, exactly the geometry we thought we'd "discovered" — starting 1400 rows in. So the model is: our row r lands at panel row 1400+r, only rows 0–399 are on screen, in the bottom 22% starting 77.8% down. Peter's photo matches to the pixel: bands begin ~77–78% down, ~25 bands visible (400 rows / 16), and your diagonal ramp is straight and truncated exactly where r=399 predicts. That diagonal is a real measurement: the row map is 1:1 with NO vertical scaling, so hypotheses (a) scaling and (b) short scan window are both refuted — the geometry is purely the 1400-row base offset.

The uncomfortable implication, stated plainly: we may have been painting the firmware's own scanout surface all along, with the EVO latch doing nothing. That would put s17 "first pixels" and everything after it in question. Pull 19 settles it with one variable — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-display-pull19-relocate-decisive.md`: move surf2 to 0x4000000, clear of the GOP window, change nothing else, keep both photo points. Include the VRAM headroom arithmetic in your proposal. If the pattern still appears, the latch is real and we go to a console next. If it doesn't, then we already have a working framebuffer on this machine by writing linear/16384 into the GOP FB — the capability we were chasing, reached through a different door. Either answer is worth the boot. Commit ALL docs+code, no push. Report "PUSH OWED: n". (I run all builds and gates.)

## → kepler-fence session

Fence: s28 verdict — everything up to execution now works, and the blocker is named by data you already collected. Both images verified byte-exact in IMEM; `tlb page0=01000000` means the page-pad made page 0 usable; CPUCTL bit 6 is clear so writing 0x100 directly was right. But cpuctl went 00000010 → 00000012 and mailbox0 never left the A5A50000 seed: per rnndb that's START_TRIGGER latched (bit 1) with STOPPED still set (bit 4) — the trigger took and the core refused to run.

Your own post-sweep says why: **DMACTL (base+0x10C) = 0x00000001 — REQUIRE_CTX is SET.** The Falcon wants a bound context before it will execute; the scrub bits are clear, consistent with your IMEM writes landing. Nouveau clears exactly this bit on the no-context path. Pull 26 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-fence-pull26-dmactl-require-ctx.md`: print DMACTL pre, mask-clear bit 0, print post, REFUSED marker + skip if it won't clear, then re-run image A exactly as landed. One new write. If mailbox0 moves off the seed, that is the first UnaOS-authored code ever executed on GPU silicon. If it still won't run with DMACTL clear, the next step is a READ-ONLY recon of the engine reset/context group (0x3C0, and the 0x048/0x054/0x480 aperture registers) — propose that, not blind writes. Commit ALL docs+code, no push. Report "PUSH OWED: n". (I run all builds and gates.)
