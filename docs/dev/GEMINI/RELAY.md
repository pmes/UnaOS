# PETER'S RELAY SHEET — not for specialists. Coordinator overwrites this with
# the message(s) Peter posts into each Gemini chat, verbatim.
# (updated 2026-07-25, after s26boot1: ⭐⭐ MAPPING SOLVED + ⭐ FALCONS FOUND; pulls 17 + 24 briefed)

## → kepler-display session

Display: s26 verdict — ⭐⭐ THE MAPPING IS SOLVED. Your linear pitch-16384 cycle photographed SEAM-FREE: solid white left column, clean unbroken 64-row bands, crisp black separators. The s25 mirror decode was the truth; block-linear is retired for good. One variable remains in the entire display story: vertical placement — about one band-cycle of rows displayed at the bottom quarter of the panel, horizontal perfect, so the scan start sits at a fixed measurable row offset from our pointer. Pull 17 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-display-pull17-row-offset.md`: one cycle, black surface with five 8-px marker stripes at rows 0/448/896/1344/1792 (white/red/green/blue/magenta), row-cal markers, same latch. The photo names the offset; pull 18 re-points and the full panel is ours, then the lane graduates to the real framebuffer console. Implement, commit ALL docs+code, no push — I run all builds and gates at land-review. Proposal first. PUSH OWED reminder stands.

## → kepler-fence session

Fence: s26 verdict — ⭐ THE FALCONS ARE FOUND. Your base recon read REAL registers at BOTH candidates: FECS 0x409000 and GPCCS 0x41A000, cpuctl=00000010 each (a live state bit — halted), imemc/dmemc true zeros. The old 0x400180 base is formally dead; first non-poison Falcon reads of the campaign. Pull 24 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-fence-pull24-falcon-port-probe-real-base.md`: your pull-21 sentinel probe retargeted to both real bases (IMEMC/IMEMD at +0x180/184, DMEMC/DMEMD at +0x1C0/1C4), AINCW bit 24 for writes, AINCR bit 25 for readback (the s24 lesson), zero execution, everything landed stays as baseline. Sentinels back = the upload path is proven and pull 25 is your first from-scratch microcode (cleanroom notice binding). Implement, commit ALL docs+code, no push — I run all builds and gates at land-review. Proposal first. PUSH OWED reminder stands.
