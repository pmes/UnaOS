# PETER'S RELAY SHEET — not for specialists. Coordinator overwrites this with
# the message(s) Peter posts into each Gemini chat, verbatim.
# (updated 2026-07-23, after s14: surface pointer candidate found; pull-5 briefed)

# (updated 2026-07-24, after s19: block-linear decode; pulls 10 + 17 briefed)

## → kepler-display session

Display: s19 verdict — your ruler decoded the scramble: every panel fact (full 8-color cycle compressed ~8× into the band, 16-px dash checkerboard, vanished white column, s17's clean solid green) fits ONE mechanism — the scanout window reads the surface BLOCK-LINEAR (GOB 64B×8 rows) while we fill linear. Pull 10 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-display-pull10-swizzle.md`: same ruler, same two writes, but the fill goes through a linear→GOB address transform. Clean stripes + solid white column on the next photo = tiling PROVEN. Proposal first. PUSH OWED reminder stands.

## → kepler-fence session

Fence: s19 verdict — beacons NONE-SEEN (the window is not a mirror of our channel structures) and pass1→pass2 changed ZERO words: stable in-boot, differs across boots. Pull 17 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-fence-pull17-latch-correlation.md`: add a PRE-takeover mirror-hdr dump and print latch-deltas vs pass0 — if the display UPDATE perturbs the window, it's core-channel processing state and your USERD road runs through the proven method path. Read-only. Proposal first. PUSH OWED reminder stands.
