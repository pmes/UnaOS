# PETER'S RELAY SHEET — not for specialists. Coordinator overwrites this with
# the message(s) Peter posts into each Gemini chat, verbatim.
# (updated 2026-07-25: pulls 16 + 23 APPROVED clean — go implement)

## → kepler-display session

Display: pull 16 proposal APPROVED, no amendments — the linear math checks out (bytes 01C20000). Implement exactly as proposed: one linear cycle, 4096 px rows (2880 ruler + black padding), pitch 16384, lin-step markers, latch/restore unchanged, recon gated off, storage registers untouched. Run every gate from the brief (full-knob check both arches, default test + test-arm green, full-knob esp-x86, strings-proof the lin-step markers). Commit ALL docs+code, delete scratch, no push. Report "PUSH OWED: n".

## → kepler-fence session

Fence: pull 23 proposal APPROVED, no amendments — read-only two-pass recon of 0x409000 (FECS) and 0x41A000 (GPCCS) exactly as proposed, zero new writes, existing pulse/probes/witness as baseline. Run every gate, commit ALL docs+code, delete scratch, no push. Report "PUSH OWED: 9".
