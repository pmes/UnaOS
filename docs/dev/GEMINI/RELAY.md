# PETER'S RELAY SHEET — not for specialists. Coordinator overwrites this with
# the message(s) Peter posts into each Gemini chat, verbatim.
# (updated 2026-07-25, after s27boot1: upload path proven; row-cal photo in; pulls 18 + 25 briefed)

## → kepler-display session

Display: s27 verdict — your row-cal cycle ran perfectly, and the photo is a sharp result: ONLY the white stripe (rows 0–7) is visible, a single line ~62–66% down the panel; red@448, green@896, blue@1344, magenta@1792 are ALL absent. Two hard deductions: (1) simple 1:1-with-offset is REFUTED — at 1:1 red would sit ~600 panel rows below the white line, on-screen, and it isn't there; (2) arithmetic bound — our pointer is only 352 rows (×16384 B) above VRAM 0, so a 1:1 scan putting row 0 at panel ~1190 is impossible anyway. Open hypotheses, strongest first: (a) vertical scaling (fw scan mode smaller than native, lines doubled — note the 07080B40 repeats at 0x4B8–0x4C8, a viewport/raster cluster), (b) scan window covering fewer memory rows than the panel shows, (c) pointer-latch granularity/truncation. Pull 18 is briefed — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-display-pull18-placement-model.md`: YOU design the discriminating pattern (one latch cycle, pm-step markers), and your proposal must include a falsification table — per hypothesis, what the photo shows if it's true. Optional read-only channel: re-dump the 0x460–0x470 + 0x4B8–0x4C8 cluster while holding, armed-vs-written. Commit ALL docs+code, no push. Report "PUSH OWED: n". (I run all builds and gates.)

## → kepler-fence session

Fence: s27 verdict — ⭐ ALL SIXTEEN SENTINELS BACK. imem AND dmem, FECS AND GPCCS, control readbacks real (rb=01000000). The ucode upload path is PROVEN; K-GPU-4 milestone 1 complete. Pull 25 is briefed and it's the one the whole arc has been building to — git pull, read `docs/dev/GEMINI/video/Kepler/BRIEF-kepler-fence-pull25-first-ucode.md`: author the first from-scratch Falcon microcode. Smallest possible program (aim ≤16 words): write 0xF00DFACE to FALCON_MAILBOX0 (base+0x040) via iowr, then EXIT. FECS ONLY. Host side: upload via IMEMC/IMEMD with the IMEMT per-256B-block tag discipline, readback-verify BEFORE execution (verify fails = STOP, no CPUCTL write, honest null), BOOTVEC=0, CPUCTL=2, bounded poll, read MAILBOX0. Your proposal MUST include the full annotated assembly listing with per-instruction citations to envytools Falcon ISA docs — approval is contingent on verifiable encodings. CLEANROOM notice binding: every byte authored in-repo, no blob, no vendor-firmware disassembly. mailbox0=F00DFACE = the first UnaOS-authored code ever executed on the GPU. Commit ALL docs+code, no push. Report "PUSH OWED: n". (I run all builds and gates.)
