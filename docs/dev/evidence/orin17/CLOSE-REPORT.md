# orin 17 — CLOSE REPORT (2026-09-06, opened 15:38Z, closed ~19:0xZ)

> **STATUS:** render8 FLEW and was scored; orin 15+16 LANDED on trunk (`main c7407753`, pushed, battery green, acks pi 7 + rmbp 14); sixteen fixes/arcs folded onto `hw-jetson` behind seven union gates; three items hold on branches (SHELLRELICS+VFSROUTE for fold 8, XHCINTD+DUPGUARD for the rMBP flight); render9 prepared (script, knobs, 39 questions, scorer with can-fire proofs) but NOT built — Peter: "once the jobs are in shut down immediately and have orin 18 build the boot disk". Baton: `~/.claude/plans/unaos/batons/orin-18.md`. Round ledger: `~/unaos-bench/scratch/orin17/QUEUE.md` (bench-side).

## 1. The flight (render8, 15:42–15:57Z)
`docs/dev/evidence/orin16/FLIGHT-RESULT-render8.md` and `PIXELS-render8.md`. PASS: A15 (5/5 CPU_ON), A24 rungs 3+3b (MAILBOX-HELD; rung-4 datums opt_wpr=1, bcr lock=0, clk 236 NOT-IN-BPMP-TABLE), A21 tick + BSPRUN HOSTING, A28 (root on the card), A16 paced 5/5 (burst 3/5 — mechanism found and fixed, RXBURST), A37, A27, A8, A25, A17, SR2/A36, A10+SO2+SO3, V-3, A19 pixels, A31 placement. FAIL/new: A30 (two defects, fixed), SO10 (shell tile re-minted the console, fixed), SO11 refuted → SO12 (cascade covered the pulse title bar, fixed), SO13 (no pin_console, fixed), SO14 (boot-opened Quarry holds the keyboard — by design, sequence clicks first), SO15 (umv/urmattr orphaned — rmbp B47), SO16 (wcgseam silence carried no information — census added), SO17 (re-minted console black — fixed), A41 (xHCI keyboard report loss: SET_IDLE 0 + one TRB — instrumented; fix accepted on branch). A34 NOT EXERCISED (power-off). Rulings heard: R24 (Esc menus-only; Tab focus-cycle retired), R25 (the crystal is the Mac menu, top-LEFT; "close the gap" — SO4's right-edge move was the seat's misreading, re-cut), R26 (shell: basics back, standard names replace ours, no platform pinning, probes stay).

## 2. The landing
`docs/dev/evidence/orin16/LANDING-REPORT-4.md` FINAL: preview LANDABLE (0 conflicts, merged tree byte-identical to hw-jetson by tree object — pi 7 — and by `merge-tree` — rmbp 14), F-1 grants reconciled (11 granted, 7 orin-region class confirmed by enclosing item, accca478 retroactive, adb3b1cd x86 hunk on sight, F-4 acked), `--no-ff` merge `c7407753` at 16:42Z after a fresh ls-remote, trunk battery green by exit code (check 0, x86 ptrdead PASS, test-arm 0, kernel8 0, kernel8-test ×2 119/119 SO7 0, ledger 0). ABSENT on trunk, not green: GATE-APPEND, K8REACH, the ledger STRICT split (rmbp's J1).

## 3. Folds on hw-jetson (gates 1–7; every code fold GATE-APPENDed by rmbp 14)
See the baton's STATE block for the sixteen shas. Gate 5 (1fc27809) went red on ONE forbidden hit — `[wc-h] rollup win=2 … torn=1` (SO7/B26 host-load family, nine executor gates concurrent) — and re-ran green as gate 6 on identical Pi bytes (video files are outside the knob-off set); recorded, not tidied. Gate 7 added the fresh-worktree armed-virt leg (the JV1 BLOBATOMIC hole: a warm tree had hidden a compile break in the newly-armed configuration). Two conflict resolutions were unions (main.rs tail blocks — verified by symbol count; fbcon.rs same-line statements + tail blocks — first two attempts committed markers and were amended out before any gate).

## 4. Lessons folded into UNAOS-LAWS.md (rules live once; no new memory files)
"run lean" = the seat's context, never executor count · a glass complaint is fixed in place, never by relocation, and Peter's words go in the row verbatim · a byte-identity claim names its RECIPE (kernel8-test arms witness) and, with genet, its HEAD · a cfg-widen compiles the newly-armed configuration in a FRESH tree · an ask describes the DIFF · the seat whose branch carries the text fixes the text · a peer ack is written into the row in the same commit · ticked rows cite fold shas.

## 5. Open at close
SHELLRELICS `dc683c40` + VFSROUTE (in flight at close; fold 8 for orin 18, on Peter's rule that `ls` names no filesystem) · XHCINTD `0019ec7a` + DUPGUARD `28899d5c` accepted, awaiting the rMBP flight · BOOTFOCUS `fdcba15d` unrequested (wip) · render9 build/stage/card/flight (orin 18) · owed list in the baton.
