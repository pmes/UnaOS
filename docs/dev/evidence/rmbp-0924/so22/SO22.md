# SO22 — WINTITLE's two leftovers: the unnamed x86 launchers, and the name that outlived its slot (2026-09-24)

Cloud session, QEMU 8.2.2 TCG (`UNAOS_QEMU_MACHINE=pc-q35-8.2`). Gate = x86-wc.spec's line:
`UNAOS_WC=1 UNAOS_QUARRY=1 UNAOS_FTDIRX=1 UNAOS_QEMU_FULL=1 ./arroyo test 240`, replayed with mbench.
Every run's rc=1 is the container's `:: SOCK-3:`; the spec replay is the verdict. Serial read with awk.

## Before the fix (the SMALLFIX ON run, same leg, one commit earlier) — slot 0's dock labels
```
[dock] tile add win=1 gen=1 owner=0x1 seq=1 label=Application
[dock] tile add win=1 gen=6 owner=0x1 seq=9 label=cr-a
[dock] tile add win=1 gen=15 owner=0x1 seq=23 label=VUG
[dock] tile add win=1 gen=27 owner=0x1 seq=45 label=VUG
[dock] tile add win=1 gen=28 owner=0x1 seq=46 label=VUG
[dock] tile add win=1 gen=29 owner=0x1 seq=47 label=VUG
[dock] tile add win=1 gen=30 owner=0x1 seq=48 label=VUG
[dock] tile add win=1 gen=31 owner=0x1 seq=51 label=VUG
[dock] tile add win=1 gen=32 owner=0x1 seq=52 label=VUG
```
gen 27..32 are six DIFFERENT programs on slot 0 (WINX-2's STAT.ELF, WINX-3's synthesized ELF, WINX-7's
thread blob, …, WINX-8's VUG.ELF, PULSE-W's PULSE.ELF) and every one wore "VUG": the name armed for
owner 1 by an earlier VUG launch was never forgotten when its slot was freed, so each recycled slot
inherited it — SO22's second half, on the wire. WINX-8 and PULSE-W themselves armed no name (first half).

## After the fix — ON run, spec 41/41 (+3 pins)
```
[dock] tile add win=1 gen=1 owner=0x1 seq=1 label=Application
[dock] tile add win=1 gen=6 owner=0x1 seq=9 label=cr-a
[dock] tile add win=1 gen=15 owner=0x1 seq=23 label=VUG
[dock] tile add win=1 gen=27 owner=0x1 seq=45 label=Application
[dock] tile add win=1 gen=28 owner=0x1 seq=46 label=Application
[dock] tile add win=1 gen=29 owner=0x1 seq=47 label=Application
[dock] tile add win=1 gen=30 owner=0x1 seq=48 label=Application
[dock] tile add win=1 gen=31 owner=0x1 seq=51 label=VUG
[dock] tile add win=1 gen=32 owner=0x1 seq=52 label=PULSE
:: WINX-8: VUG.ELF end-to-end — loaded (entry 0x100000000b0) + windowed + 3 presents with 2 ring-3 thread(s), killed + row reaped by the kill (free 9->10/10, exited 0->0), teardown clean -> PASS ::
════════════ MBENCH VERDICT — x86-wc.spec vs /tmp/claude-0/-home-user-UnaOS/823e4b66-c41d-5863-8c5e-22382d4804ac/s
  ✅ MBENCH PASS — 41/41 required witnesses, 0 forbidden hit(s), 3064 lines scanned [mode unknown: no run sidecar]
```
gen 27..30 read `Application` (unnamed test blobs, honestly), gen 31 `VUG` (winx8, named at spawn), gen 32
`PULSE` (pulsew). WINX-8's PASS line now requires `name_gone` — the name armed at spawn is gone once the
kill has released the slot (bounded 2 s wait: the address space is released by refcount a little after
the proc row and the window row; the first attempt read the flag at the window's death and got
`name_gone=false` on a correct tree, which is why the wait exists).

## Go-red — both mutations in one build (pulsew's arm removed; the x86 forget removed)
```
[dock] tile add win=1 gen=27 owner=0x1 seq=44 label=VUG
[dock] tile add win=1 gen=28 owner=0x1 seq=45 label=VUG
[dock] tile add win=1 gen=29 owner=0x1 seq=46 label=VUG
[dock] tile add win=1 gen=30 owner=0x1 seq=47 label=VUG
[dock] tile add win=1 gen=31 owner=0x1 seq=50 label=VUG
[dock] tile add win=1 gen=32 owner=0x1 seq=51 label=VUG
:: WINX-8: VUG.ELF end-to-end FAIL — windowed=true presents=3 threads=2 killed=true (verdict="killed, row reaped") row_gone=true row_freed=true (free 9->10/10 exited 0->0) cleared=true name_gone=false (want true/>=3/-/true/true/true/true/true) ::
  ❌ FORBID hit @ line 2569: :: WINX-8: VUG.ELF end-to-end FAIL — windowed=true presents=3 threads=2 killed=true (verdict="killed, row re
════════════ MBENCH VERDICT — x86-wc.spec vs /tmp/claude-0/-home-user-UnaOS/823e4b66-c41d-5863-8c5e-22382d4804ac/s
  ❌ REQUIRE    \[dock\] tile add win=\d+ gen=\d+ owner=0x1 seq=\d+ label=PULSE
  ❌ REQUIRE    :: WINX-8: VUG\.ELF end-to-end — loaded \(entry 0x[0-9a-fA-F]+\) \+ windowed \+ [0-9]+ presents with 2 ring-3 thread\(s\)
  ❌ FORBID     :: WINX-8: .* FAIL
  ❌ MBENCH FAIL — 39/41 required witnesses, 1 forbidden hit(s), 3075 lines scanned [mode unknown: no run sidecar]
```
Six VUGs again, no PULSE, `name_gone=false`. Source restored afterwards (grep GO-RED MUTATION = 0).

## aarch64 half — compile-proven only
`app_name_forget(asid)` at both `teardown_user_slot`s (boot.rs, mmu_tegra_el0.rs); arm-virt and arm-tegra
legs type-check rc=0. No QEMU leg here runs a windowed program to a slot teardown on aarch64; it flies
with the next desktop boot on the Pi or the Orin.
