# Orin ledger — every finding, ticked once

> Arch ledger for the Jetson Orin track. Cross-arch and shared-seam items live in
> `docs/dev/LEDGER.md` (ids `S<n>`, `P<n>`); rows below that cross reference it by id.

> Rule (SECURITY.md's, applied to this track): **an arc that fixes, flies, or drops a ledger item
> ticks it here in the same commit.** Every audit, inventory, or capture review folds its findings
> into this table the turn it lands. Before spawning any audit, brief it with this file: report
> only what is NEW or CHANGED. A finding re-derived because it was not here is the waste
> (Peter, 2026-09-05).

Status: **open** · **fixed-unflown** (in tree, not on metal) · **flown** (scored on the wire) ·
**dropped** (ruled out, with the ruling) · **relayed** (another lane owns it).
Sources: `od` = `docs/dev/OS/08_VIDEO/orin-desktop.md` · `AC` = `docs/dev/evidence/orin12/ARCH-CONFORMANCE.md` ·
`OB` = `docs/dev/evidence/orin13/ORIN-BROKEN.md` · `RA` = `docs/dev/evidence/orin13/RENDER2-AUDIT.md` ·
`SD` = `docs/dev/evidence/orin12/SERIAL-DEVLOOP.md` · `FR` = `docs/dev/evidence/orin13/FLIGHT-RESULT.md`.

## A. What the operator sees broken (ranked by panel impact, OB table 1, 2026-09-05)

| id | item | owner | flies-on | status | evidence | closed by |
|---|---|---|---|---|---|---|
| A1 | No desktop furniture (bar, dock, crystal); clicks land on nothing. §5.2 stop-line refuses `desktop_firmware::activate()` until the boot-core stack through the cascade is measured | orin | Orin | flown — render3b 2026-09-05: `[deskcascade] -> CASCADED windows=1 bar=1 owns_pixels=1 route=ROUTED`; menubar ENABLED+PAINTED, crystal LIVE, dock live, console windowed at (307,158); boot-stack post-cascade `hw=15472 headroom=17296` (the §5.2 number). Knob `deskcascade` still default-off · render4: `hw=15584 headroom=17184` (+112 B for DESKSCENE's frames; `docs/dev/evidence/orin14/FLIGHT-RESULT-render4.md`) · render6 ×2: `hw=15552 headroom=17216` on both boots | `od` §5.2, §3.12; OB#1 | CASCADE executor (knob `deskcascade` + boot-stack probe) |
| A2 | No EL0 program owns a window; `bg` refused; only the boot core drops to EL1 | orin | Orin | open — Peter's SMP D1–D5 ruling | `sched.rs` EL0-EL1CORE; OB#2 | — |
| A3 | Keystrokes ignore focus; every key goes to the shell | orin | Orin | open — design (desktop pseudo-owner | `xusb_tegra.rs` header; `main.rs` ~:2916; OB#3 | — |
| A4 | Serial RX dead; every retry is a card write + power cycle | orin | Orin | flown — render3b 2026-09-05: `[serialrx] lsr=0x00000200 base=0x0c280000 -> RX-LIVE` (excerpt :498); `tste\r` over the FIFO → `KEY 's'`, `KEY 't'`, `KEY 0x0d` (:586-588), `[serialrx] rx=3 (+3)` (:618) — 3 of 5 bytes delivered. The loss mechanism is A16's and is UNDETERMINED: the "overran the 1-byte holding register between polls" reading this row carried was refuted by REVIEW3 M2 (LEDGERTICK 2026-09-05, `awk '/polls=/' render3b-boot1.log`: 22.75 M → 23.07 M polls in one ~1 s sweep ≈ 325k polls/s, far faster than the 87 µs byte time) · A16 is now DETERMINED (render4): competing reader, not overrun | `docs/dev/evidence/orin13/render3b-boot1.log`; `SD`; FR q7 | ORINRX `9cb779bd` (knob `orinrx`, LSR witness) |
| A5 | Strip + pulse window paint once and freeze (`presents=2`); core 0 load a structural 100% because the capstone loop never folded idle | orin | Orin | flown — render3b: `SCHED: load c0=85%`, `[pstrip] rollup redraws=24 srcdelta=23`, `presents=29` and climbing | FR; OB#5 | LOADSAMPLER `341ca707` |
| A6 | One core, no preemption; five APs run zero work | orin | Orin | open — `bsprun`/`bsptick` unflown | OB#6 | — |
| A7 (→ S4) | Dock minimise round-trip never proven; aarch64 shell tile dead (`SHELL_REOPEN` drained only on x86) | rmbp | Orin | open | OB#7; AC | dock drain is video/ (rmbp lane) |
| A8 | No file browser (`quarry` compiles, cascade-gated) | orin | Orin | open — render6 ×2 2026-09-06: still 0 `quarry` lines (Peter: "what about quarry?"); the ONLY opener is `video/desktop_firmware.rs:394` `super::quarry::open()` inside `activate()`, and the cascade runs `activate=false` (`[deskcascade] -> CASCADED … activate=false`), so nothing ever asks it to open — QUARRY executor (orin 15) adds the opener on the cascade route. WAS: A1 flew (render3b CASCADED) and quarry still did not open: 0 `quarry` lines on the wire (LEDGERTICK 2026-09-05, `awk '/quarry/' render3b-boot1.log` counts 0) and no `quarry::open` call on the terminus line (`grep -n quarry main.rs` = comments only). The blocker is no longer §5.2; it is the missing opener | OB#8; `docs/dev/evidence/orin13/render3b-boot1.log` | — |
| A9 | Print Screen produces nothing: service call never reached the terminus | orin | Orin | flown — render3b, Peter pressed Print Screen: `:: PRTSCR: SCREEN0.PNG 1920x1200 (6.9 MB) -> OK ::`, valid PNG on the card (the first screenshot of the Orin desktop). Second press: A17 | OB#9. The press is NOT in the git excerpt: `docs/dev/evidence/orin13/render3b-boot1.log` (640 lines) ends at the `rx=3` census — LEDGERTICK 2026-09-05, `awk '/PRTSCR/' render3b-boot1.log` counts 0; the three `PRTSCR:` lines (two `capture armed`, one `SCREEN0.PNG 1920x1200 … -> OK`) sit ~500 lines later in the same boot's raw capture (same `KELF max=0x2d3488` anchor). Excerpt extension owed | PRTSCR-ORIN `2000608a` was INSIDE the line's trailing `//` comment — compiled nothing, check green (found at the ORINRX fold); live from PRTSCRLIVE `60f7ec5e`. Needs `UNAOS_HOLOCRON=1` |
| A10 | `<Esc>` cannot dismiss the pulse window's menu | orin | Orin | open — CONFIRMED on metal, render6 boot 2 2026-09-06: with the pulse menu open, Esc (`KEY 0x1b`) ×3 did nothing; the menu closed only on the next content click (`[pulsewin] menu dismiss reason=content` ×3, each immediately before a press; `docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`). Folds into A25 (R21: the menu moves to the bar; the bar's menu takes Esc). WAS: returns with the pulse window (DESKSCENE `8cbfaadf` restores `pulsewin::service()`); Esc dismissal unverified — AC#20's finding stands until the next flight presses Esc on the open menu and scores it | OB#10; AC#20 | — |
| A11 | TAB mid-drag leaves the grab on the old window (x86 cancels) | orin | Orin | open | OB#11; AC#7 | aarch64 `wc_focus_key` |
| A12 | No DHCP lease (NO-OFFER → static fallback); default jetson image carries no `net4` | orin | Orin | fixed-unflown — NET4A `a45af90e`: RX ring + 32 buffers below 4 GiB (lowest carveout-clean L2-split 2 MiB block in `[0x80000000, 0x100000000)`, Normal-NC, no inbound-iATU alias in the path; fail-open to the boot7h high window + alias, said as `below4g=0`); first metal question: `[net4F] buffers-written(count=4)` — the `[net4F] distinct buffers-written(count=N) … across the first 4 RX completion(s)` line, on the 4th completion and at window close. Needs `UNAOS_NET4=1` on the build line. NET-4o's boot-19 "no clean low span" was the EMPTY derived-`dma-ranges` set, never a scan of low DRAM (`docs/dev/evidence/orin14/NET4A.md` §2) | OB#12; NET-4A; `docs/dev/evidence/orin14/NET4A.md` | — |
| A13 (→ S3) | No `flight_recorder` on aarch64 (x86-only service) | rmbp | Orin | open — FC-2 shape | OB#13; `main.rs` ~:1747 | shared file; rmbp's FC-2 gate |
| A14 | No Wi-Fi | orin | Orin | open — out of the north star | OB#14 | — |
| A15 | **Boot dies at secondary-core power-on, one boot in five.** After `ORIN-SMP-3 enumerated core 5` the next step is PSCI `CPU_ON` of AP 1; on 5 of 23 Orin boots in orin.log (boots 2, 3, 14, 15, 23) and on render3's only boot, the wire shows EL3 `Exception reason=1 syndrome=0x82000010` (instruction abort from a lower EL, synchronous external abort) then `RAS Uncorrectable Error in IOB` (CBB Interface Error, ADDR=0x8000000000000200) + ACI FillWrite, then `Powering off core`. None of render3's commits touch the SMP kick-off; render2 passed the same line with `CPU_ON AP 1 -> SUCCESS (entry=0x25aca06c8)`. arch_arm64.md ~:6237 names the signature: `:P::R1:` then RAS with no `:A:` = the SMC returned cleanly and the AP died in its MMU-off window before the far side of `enable_mmu_virt` | orin | Orin bench | flown — 4 passes of the APTEXT layout: render3b + render4 2026-09-05, render6 boot 1 + boot 2 2026-09-06 (`docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`) (`CPU_ON AP n -> SUCCESS` ×5 both, 0 EL3 aborts, 0 power-offs; `docs/dev/evidence/orin14/FLIGHT-RESULT-render4.md`); every further boot still ticks the count. WAS: fixed-unflown — 1 pass: render3b brought 5/5 APs online (`CPU_ON AP n -> SUCCESS` ×5) on a layout that died 2/2 without APTEXT `fef6a184`; closes as flown after several passes | `docs/dev/evidence/orin13/render3-boot1.log`; arch_arm64.md ~:551 | — |
| A16 | **Serial RX lost 2 of 5 injected bytes (`rx=3`): mechanism UNDETERMINED.** REVIEW3 M2 refutes overrun-between-polls (the census shows ~325k polls/s, 3 µs/poll, vs 87 µs/byte) and warns the FCR write is probably wrong: UARTC is the SPE/TCU's combined-UART port (ORIN-SUPSOUND note), so a second reader may be draining the same RBR, and resetting the FIFOs resets the co-owner's too. The pump's synchronous `KEY` echo (~2.3 ms under the port lock, IRQs masked) does not fit a 1-byte register either (would give `s`,`\r`). Discriminators SHIPPED (RXDISCRIM): `note_lsr` counts LSR bit 1 (OVRF) per poll → `ovrf=` on the `[serialrx] rx=` census line; one read of IIR (`2 << 2`) on the RX-LIVE line → `iir=0x.. fifo=on/off`; `~/unaos-bench/tools/inject-paced.sh <fifo> <string> [ms]` (0 = the render3b burst, 50 = paced); scorer + decision table in `docs/dev/evidence/orin14/A16-SCORE.md`. All inside the `serialrx` tail mod, no FCR/IER/MCR write | orin | Orin bench | flown — render4 2026-09-05, MECHANISM DETERMINED: competing reader (the SPE/TCU firmware drains the same RBR). render6 boot 1 2026-09-06 CONFIRMED with the bytes in hand: UARTC delivered s,t,CR and the TCU RX mailbox holds `t`,`e` (A22, word `0x82006574`); paced 0/5 while the mailbox stayed full and unconsumed (`docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`).  FIX DESIGNED (TCURX, orin 14 executor commit — the seat folds the sha at landing; GATE-LEDGER refuses a sha that is not yet on a track head): the CCPLEX does not own UARTC RX — the board's own UEFI reads its console input from the TCU RX mailbox (HSP top0 shared mailbox 0 @ `0x03C10000`, TX aon SM1 @ `0x0C168000`; edk2-nvidia, BSD-2-Clause-Patent, Kconfig defaults) and never touches UARTC. Fix path = stop polling UARTC's RBR and read the TCU RX mailbox word (bit 31 = full, [25:24] = byte count, [23:0] = up to 3 bytes), consuming by the write-back the protocol defines; no UART register write, no IRQ. Alternative (needs the bench cable moved to the 40-pin header + BPMP clock/reset/pinmux) = UARTA/UARTE, Peter's call. Probe shipped as A21 (`tcuprobe`); design + decision table `docs/dev/evidence/orin14/TCURX-DESIGN.md`. `iir=0xc1 fifo=on`, `ovrf=0` on both legs; burst 3/5 (s,t,CR), paced 50 ms/byte 1/5 (s) — pacing made it WORSE, which only a competitor predicts; overrun refuted. An FCR write would not have fixed it. Fix owed: either stop the SPE reading UARTC (BPMP/TCU ownership — a §F question) or move the console to a UART the SPE does not own (`docs/dev/evidence/orin14/FLIGHT-RESULT-render4.md`). WAS: fixed-unflown — discriminators RXDISCRIM `fe6fe3b5`; mechanism still undetermined until render4 flies burst + paced and the scorer's table rules (overrun / timing-dependent / competing reader) · TCURX2 fold (orin 15): · **fix rung 1 FLOWN (render6 2026-09-06T01:28Z, A22 `tcuprobe`): the mailbox has the missing bytes.** Burst `tste\r` → UARTC delivered `s`,`t`,`\r` (`[serialrx] rx=3 (+3) … ovrf=0`) while the read-only probe printed `[tcu] rx-mbox raw=0x82006574 full=1 nbytes=2 data=[74 65 00] … full-edges=1 changes=1` and STAYED full all boot (nothing consumes) — bit31 set, [25:24]=2, byte0=`0x74` `'t'`, byte1=`0x65` `'e'`, EXACTLY the two bytes UARTC lost. The paced leg delivered 0 to UARTC and did not change the word (consistent with, not proof of, the §4 UNKNOWN "the SPE holds while the RX mailbox is full"; rung 2's paced leg is the test) · **fix rung 2 fixed-unflown — TCURX2 (orin 15 executor commit on branch `exec-orin15-tcurx2`; the seat folds the sha at landing — GATE-LEDGER refuses a sha that is not yet on a track head), knob `tcurx` (`UNAOS_TCURX=1`, implies `tegra`+`orinrx`+`tcuprobe`), DEFAULT OFF.** `serialrx::drain` gains a SECOND source — `hsp_tegra::rx_mbox_take()` reads the RX mailbox word and, on bit 31, takes byte 0 and writes the word back with the count decremented, the remaining bytes shifted down and the whole word zeroed when none remain (TCURX-DESIGN §4; the zero write is what frees the slot). Per R19 the UARTC RBR poll is KEPT, not replaced, so both sources are scored on one boot. Entire added write surface = that one 32-bit store to the RX mailbox word; no UART register, no doorbell, no IRQ; `main.rs`/`mod.rs` untouched. Gates: `./arroyo check` exit 0 (49 legs incl. `✅ arm-tegra-tcurx`, GATE-KNOB 158/157 clean), `./arroyo test-arm 60` exit 0, armed `esp-jetson` exit 0 with `tcurx` in the banner and `[tcurx] took` + `mbox=` present in `kernel.elf`, knob-off jetson loadable image sha256 `fd2fb251790ce9eb66804f776cacfc971ee22703fe2a8281079e278557ee6aa0` and `kernel8.img` sha256 `d73a8981d65bd24e254567934f0f2d21b3307b4a761408618d576623e2669fb0` identical before/after. EXPECTED WIRE on render7 (render6 line + `UNAOS_TCURX=1`, burst AND paced `tste\r`): `[tcurx] took=0x74 't' left=1 word=0x81000065 <- raw=0x82006574 …` then `took=0x65 'e' left=0 word=0x00000000`, 5 `KEY` lines per leg, `mbox=` climbing on `[serialrx] rx=`, and `[tcu] rx-mbox … full=0` at rest instead of the stuck `full=1` ; `docs/dev/evidence/orin14/TCURX-DESIGN.md`; `docs/dev/evidence/orin15/TCURX2.md` ; TCURX2 executor (orin 15, knob `tcurx`) | `docs/dev/evidence/orin13/render3b-boot1.log` (tail: the KEY echoes and `[serialrx] rx=3`); `docs/dev/evidence/orin14/A16-SCORE.md` | RXDISCRIM executor (orin 14) |
| A17 | **Second Print Screen press produces an EMPTY file and no verdict.** Two `capture armed` lines on the wire, one `-> OK` (SCREEN0.PNG); SCREEN1.PNG exists at 0 bytes with neither OK nor a refusal printed. The 6.9 MB write over USB BOT takes seconds; the second request was serviced while the first was in flight or its verdict path was skipped — read `prtscr::service`/`capture` for the re-entry and the create-before-encode order | orin | Orin bench | flown — render4 2026-09-05: two presses → `SCREEN0.PNG … -> capturing` → `-> OK`, `SCREEN1.PNG … -> capturing` → `-> OK`; both 6.9 MB valid PNGs on the card, different bytes (`docs/dev/evidence/orin14/FLIGHT-RESULT-render4.md`). render6 boot 2 2026-09-06: four fast presses → `SCREEN1.PNG`, `SCREEN2.PNG` both `-> OK`; the two presses that landed during an in-flight capture printed NO refusal (`armed=4 capturing=2 ok=2 refusals=0`) — the `Refusal::InFlight` verdict line is not on the wire, a gap against "every failure path prints a refusal" (rmbp grant condition d); open, patch shape as A17-prtscr.patch (`docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`). WAS: fixed-unflown — PRTSCR2 `f0db58bf` (prtscr.rs per `A17-prtscr.patch` + screenshot.md §9; rmbp 11 reviewed+approved 2026-09-05; two-QMP-chord `test-fat sf 200` proof: SCREEN0/SCREEN1 both valid 1280x800 PNGs of 3.07 MB each, capturing→OK ×2). diagnosed and fix staged (A17/PRTSCR2). NOT a re-entry and NOT a skipped verdict: the second press was decoded inside capture #1's BOT write (`xhci/mod.rs:3654` shared event drain -> `:4931 request()`), capture #2 ran on the next sweep, and the board lost power 7.28 s later (`orin.log` 12:41:55.477 vs raw.log NUL 12:42:02.755) inside a ~7.9 s capture (`[pstrip] gapmax=7894ms`) — `write_grow` publishes size LAST (`fat.rs:3030`), so the 0-byte entry is the interrupted-write residue. Fix: `-> capturing` line before the entry can exist + `IN_FLIGHT` door (`Refusal::InFlight`) + `service` defers behind a verb capture. Source is rmbp's lane: carried as `A17-prtscr.patch` until `GRANT prtscr.rs`; becomes fixed-unflown when applied as code, flown on render4 (two presses -> `SCREEN0 -> OK`, `SCREEN1 -> OK`) | `docs/dev/evidence/orin14/A17-PRTSCR.md`; `~/unaos-bench/capture/line-acm0/raw.log` 109385-109395; `docs/dev/evidence/orin14/A17-prtscr.patch` | — |
| A18 | **Peter (2026-09-05, R17): the windowed pulse was the WRONG one to kill.** On the cascaded desktop the EMBEDDED pulse (the per-core lamps/breath drawn by the strip) and the OLD STATUS BAR at the bottom (`ui_status` strip, the scaffold render pass's only product) are still on the panel; **both must go**. The windowed pulse (`pulsewin`, retired in CASCADE a20839c6) is the one that belongs. Orin 14: remove the strip + embedded pulse from the cascaded scene, restore the windowed pulse; the render pass then presents the desktop scene, not the strip | orin | Orin bench | flown — render4 2026-09-05: `[realdesk] backdrop=desktop-scene retired=pulse-band,status-line bottom_reserved=104->64`, `[pulsewin] open win=2 box=1290x212 at (10,914)`, census `strip=retired pulsewin=2`, `presents=1` (`docs/dev/evidence/orin14/FLIGHT-RESULT-render4.md`). render6 ×2 2026-09-06 PASS, same lines. WAS: fixed-unflown — `8cbfaadf` DESKSCENE: `orin_render_service` calls `retire_desktop_chrome` at its head when the cascade's readback holds (`bar=1` AND `route=ROUTED`; `orin_desk_scene_up`), so `ui_status::draw` is a no-op, `chrome_h` shrinks 104->64 and `tick` never presents; `pulsewin::service()` is back after `tick` (the Pi's fold) so the ARMED window opens on pass 1; the pass presents the backdrop seed once. Expected wire: `[realdesk] … bottom_reserved=104->64`, `[pulsewin] open … box=1290x212 at (10,914)`, `[orinrender] census … strip=retired pulsewin=2`. Evidence: `docs/dev/evidence/orin14/A18-DESKSCENE.md` | `docs/dev/RULINGS.md` R17; A10 (the retirement to undo) | DESKSCENE `8cbfaadf` |
| A19 | **Pre-cascade shell text survives the desktop-clear.** The render3b screenshot (SCREEN0.PNG, the card's first capture; harvested to the bench scratch, unversioned) shows four lines of the pre-cascade panel console (`UnaOS Jetson Orin Nano … architect@unaos:~$`) at the panel's top-left, under the menubar, after `activate`'s desktop-clear and 29 presents — the clear/composite never covers that band (DESKSCENE executor, 2026-09-05; outside its brief) | orin | Orin bench | flown — render6 2026-09-06 (both boots): `[realdesk] band-cleared x=0 y=34 w=1920 h=1166 bg=2d2b55 shell=win=3 surf=960x466 box=970x510 at (515,402)` + `[realdesk] shell-present win=3 outcome=Composited`; pixels `A19-pngband.py SCREEN0.PNG` → band `non-bg=0/60200 (0.0%)` PASS (render4 read 855/15050) — `docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`. WAS: fixed-unflown — A19FIX `6f56eff8` 2026-09-05: NOT residue and NOT the occluder walk — case (b): `jd2_console_pump`'s phase 2 paints the PANEL **after** the cascade (render4-boot1.log order: `[deskcascade] -> CASCADED` :479 → `JD2 — EL1 console pump live` :497 → `JD4 — console OWNS the panel` :591); `Console::draw` clears its back buffer to `Console::BG` (console.rs:162, == `wm::DESKTOP_BG`) and draws the banner + prompt, and `Screen::flush`'s occluder walk withholds the windows/bar/dock, so only the TEXT reached the glass (SCREEN0.PNG decoded: 2826/60200 non-bg in x 0..700 y 34..120, four 8-px glyph rows at y 46/58/70/82, x 12..495; the shell repaints it on every key). Fix (main.rs tegra region only, REALDESK-SHELLWIN tail block): on the cascaded scene the shell mints its own `wm` row from the pump that owns the drain (`Screen::direct` + `present_outcome_owned`, the Pi's SHELLWIN shape) and the panel Screen is seeded `DESKTOP_BG`; jd2's stack is 32 KiB on the knob. Wire: `[realdesk] band-cleared x=0 y=34 w=1920 h=1166 bg=2d2b55 shell=win=3 …`, `[realdesk] shell-present win=3 outcome=Composited`, `[u7stk] at=jd2-console:shell-present`. render5 scorer: `docs/dev/evidence/orin14/A19-pngband.py SCREEN0.PNG` → band must read `non-bg=0/60200`, verdict PASS | `docs/dev/evidence/orin14/A19-BAND.md`, `docs/dev/evidence/orin14/A18-DESKSCENE.md` | — |
| A20 | **Clicks do not land on the cascaded desktop (Peter, render4 2026-09-05: "keys ok but clicks not landing").** The render arm line prints `click=0`: `UNAOS_ORINCLICK` is not in the flight knob line, frozen at render3b's; the boot-mouse enumerates (xHCI slot 4, `SET_PROTOCOL(boot) OK`) but nothing routes its presses. A recipe gap, not a regression of A1 | orin | Orin bench | flown — render6 boot 2 2026-09-06: clicks ROUTED and CONSUMED (`[orinclick] census … press=10 rel=10 consumed=9 -> ROUTING`, `[clickroute] press hit furniture asid=… win=3 … consume -> shell focus`, `MOUSE-1: 3040 reports`). BUT boot 1 of the SAME image: the pointer was DEAD all boot (`-> ARMED` then every census `btn=0 press=0 -> IDLE-NO-CLICKS`, `MOUSE-1` stuck at 1 report, `[cursor3] offers=0`) — an intermittent pointer-enumeration/poll death, not the knob; CLICKDEAD executor (orin 15) owns the mechanism (compare rmbp-ledger A6 `kernel=true desktop=false nofab=true`). Drag is A27. `docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`. WAS: fixed-unflown — recipe: add `UNAOS_ORINCLICK=1` (composes: `orinclick`'s block makes only `wm` reads — `hit_test`/`count`/`compat_live`/`close_box_hit`, `display_tegra.rs:1081-1530` — and both `main.rs` call sites (:2979, :3014) are in the pump's phase-2 loop, which runs on core 0 after terminus item 9 `tegra_desk_cascade` (`main.rs:2717`), so the cascade's `table-not-empty` floor never sees a click row; the router `syscall.rs:14290` reaches `wm::hit_test` (:14300) and, with `desktop_firmware` implied by `deskcascade` (`Cargo.toml:2367`), the crystal/dock/pulsewin arm (:14299), the chrome arm (:14400) and the kernel-furniture arm (:14401) — the cascade's console window is `wm::create_at(KERNEL_OWNER_CONSOLE, …)` (`fbcon.rs:1979`), hittable per `wm.rs:3039`; the §6.1 ordering-rule decline lives only in `orin_conwin` (`display_tegra.rs:2631`, cfg `orinconwin`) and `tegra_desk_arm` (`main.rs:7404`, cfg `tegradesk`), neither in the line nor implied; `arm-tegra-render` (`arroyo:3106`) already type-checks the union). No code change. Expected wire: `[orinrender] arm … click=1`, `[orinclick] arm … rows=2 … -> ARMED`, on a press `[clickroute] press hit furniture asid=… win=1 …` (or `press chrome win=1`) then `[orinclick] edge=press … hit=yes win=1 … -> CONSUMED`, census `-> ROUTING`. A10's Esc test rides render5. Evidence: `docs/dev/evidence/orin14/A20-ORINCLICK.md` | `docs/dev/evidence/orin14/FLIGHT-RESULT-render4.md`; `docs/dev/evidence/orin14/A20-ORINCLICK.md` | — (no commit needed; the recipe is the fix) |
| A21 | **One core, no tick after the drop (gap #1, first question).** The boot core takes exactly one EL1 interrupt in its life (the IRQEL-RT one-shot, `render4-boot1.log:408`) and then runs the terminus with CNTP off; `[wcpar] cores=1`, `SCHED-BAL 0 steals`. `bsptick` (timer.rs tail) exists unflown: a standing 250 Hz CNTP tick at EL1 across `run_capstone_boot_core`, `on_tick` only, no preemption | orin | Orin bench | fixed-unflown — tick1 image staged from `2a04fb4a` at `~/unaos-bench/flash/orin/tick1-20260906T0013Z-…/` (full dir name, executor sha7 suffix, in TICK1-FLIGHT.md §B) (BSPTICK 2026-09-05; render5's knob line + `UNAOS_BSPTICK=1`, a SEPARATE image — Peter's SMP D1–D5 ruling is pending, so it is never folded into render5). First metal question: does `[orinbsptick] tick N` count past 1 (N = 250, 500, … at ~1 Hz, EL1, cpu 0) while `[orinrender] census passes=` keeps climbing and nothing prints `=== AARCH64 EXCEPTION`? Read pass (TICK1-FLIGHT.md §A) found no compose defect with deskcascade/orinrender/orinrx/orinclick; the one unobserved thing is the SECOND EL1 delivery. Arc 2 (`bsprun`) waits on this verdict and the `sched.rs` grant | `docs/dev/evidence/orin14/TICK1-FLIGHT.md` (§C scorer: PASS / NO / ARM-MISS / TICK-DIED / PUMP-STALL / FAIL-EL2 / EXCEPTION) | — |
| A23 | **On-die SDMMC write unproven (gap #3).** The Tegra SDHCI at `sdmmc1 @ 0x03400000` reaches card identification and sector-0 reads under `sdmmc`, but no CMD24 has ever been issued on it with the vendor pad block DISABLED (the FWALL SError conviction) — so self-update, the flight recorder and an unafs root all wait on one answer: does a single-block write to a scratch sector read back, at 1-bit default speed? | orin | Orin bench | fixed-unflown — sdmmcwrite 33dc7811; first metal question: CMD24 then CMD17 read-back at 1-bit default speed on sdmmc1 @ 0x03400000 — knobs `UNAOS_SDMMC=1 UNAOS_SDMMCWRITE=1`; the scratch sector is chosen from the sector-0 table the census already reads (the top of the post-MBR gap on the bench card), printed BEFORE the write and refused (named) if none is provable; scorer `awk '/\[sdmmc\] write/'`, PASS = `[sdmmc] write lba=… -> OK (512/512 match`; a second boot's `prior … = witness(tick=…)` is the persistence proof | `docs/dev/evidence/orin14/SDMMCWRITE.md` | — |
| A22 | **TCU RX mailbox probe (`tcuprobe`, read-only).** Does the SPE forward console input into the HSP shared mailbox the CCPLEX is meant to read (A16's fix path), or does nothing arrive there? `arch/aarch64/hsp_tegra.rs` resolves the `tcu` node's `mboxes` + the HSP nodes' `reg`/`#mbox-cells` from the live DTB at the console pump's arm and prints `[tcu] hsp top0=… aon=… tx-mbox=… rx-mbox=…`; bounds the index by `HSP_INT_DIMENSIONING` (+0x380); then a boot-core sampler task reads the RX mailbox word every pass and prints `[tcu] rx-mbox raw=0x… full=<0/1> nbytes=… data=[…] … -> FULL-NEVER / FULL-SEEN / FULL-NOW` once a second. No HSP/UART write, no IRQ enable; the parked byte is never consumed. Facts from the DTB, the public DT bindings and BSD-licensed edk2-nvidia — no GPL driver read (D3) | orin | Orin bench | flown — render6 boot 1 2026-09-06, TCURX-DESIGN §7 ROW 1: `[tcu] hsp top0=0x3c00000 aon=0xc150000 tx-mbox=1 rx-mbox=0`; after the burst `[tcu] rx-mbox raw=0x82006574 full=1 nbytes=2 data=[74 65 00] … full-edges=1 changes=1` and it stays full for the rest of the boot (nobody consumes) — those are the two bytes UARTC lost. Boot 2 (keyboard only, nothing injected): `FULL-NEVER changes=0`, consistent. The SPE forwards console RX into the mailbox and parks it; fix = rung 2, consume in the drain (TCURX2 executor, orin 15). Open datum: with the mailbox full, the paced leg reached neither UARTC nor the mailbox (§4 UNKNOWN, hold-or-drop). `docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`. WAS: fixed-unflown — tcuprobe, TCURX `5936f239`; flies on render5/6 with `UNAOS_TCUPROBE=1` beside `UNAOS_ORINRX=1`, burst + paced injection as render4; decision table in the design doc §7 | `docs/dev/evidence/orin14/TCURX-DESIGN.md` | TCURX executor (orin 14) |
| A24 | **GA10B rung 2 (Peter 2026-09-06, R18: "probe the hardware boot by boot").** `ga10bprobe2`: after the `gpu@` DTB walk, MRQ_PG GET_STATE pre-state → SET_STATE ON only if off → explicit readback → MRQ_CLK IS_ENABLED/ENABLE/IS_ENABLED per DTB clock → ONE BAR0 read (NV_PMC_BOOT_0, BAR0+0) behind the readback of ON → symmetric restore → RETURNS (full boot). Rung 1 (o3d) found the rail already ON and five PRI reads sane, so the new data are the clock census, BPMP's permission (every err), and the chip id | orin | Orin bench | fixed-unflown — `a5a62ffb`; flight: `UNAOS_TEGRA=1 UNAOS_TEGRA_EL0=1 UNAOS_WITNESS=1 UNAOS_ORINRENDER=1 UNAOS_DESKCASCADE=1 UNAOS_ORINRX=1 UNAOS_HOLOCRON=1 UNAOS_ORINCLICK=1 UNAOS_GA10B_PROBE2=1 ./arroyo esp-jetson` (NO `UNAOS_GA10B_PROBE1` — rung 1 would power the board off behind rung 2); PASS = `[ga10bprobe2] pg=0x1 clk=<n>/<t> boot0=0x… -> POWERED chipset=0x17b` and the desktop comes up behind `rung 2 complete — RETURNING`; every other arm (UNPOWERED/REFUSED/last-line-is-an-announce) is a datum the ladder names | `docs/dev/evidence/orin14/GA10B-LADDER.md` §Rung 2 | — |
| A25 | **The pulse window draws its `View` menu INSIDE the window (Peter 2026-09-06, R21: it goes in the menu bar).** PULSEWIN `27922509` put a one-title menu strip in the window's first content row because the bar had no per-window menu path (`video/pulsewin.rs` header §"The menu, stated plainly"); the bar's MenuPublish protocol is designed in `video/menubar.rs` ~:1296-1370 and unbuilt. Fix: the bar hosts the focused window's menu titles right of the crystal, drops them with the SHARD primitive, Esc dismisses; kernel windows (pulsewin first) publish directly, ring-3 later over the bus; pulsewin stops drawing its strip (its content row returns to the lamps) | rmbp (`video/menubar.rs`, `pulsewin.rs`, `wm.rs`) — orin drafts the patch for the grant | Orin bench (also Pi, x86 — ONE OS) | open — MENUBAR executor (orin 15) drafting `docs/dev/evidence/orin15/MENUBAR-WINMENU.patch` + design; grant asked of rmbp 12 2026-09-06; A10 folds into this row's Esc | `docs/dev/RULINGS.md` R21; `docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md` | — |
| A27 | **Click-and-drag grabs a window and never moves it (Peter, render6 boot 2, 2026-09-06: "click and drag does not work but clicks are").** Wire: `[clickroute] press chrome win=3 … -> drag` → `[wm-act] drag-begin win=3 owner=0xffffff02 at (1156,431) -> grabbed` → `[wm-act] drag-end win=3 … at (520,441) -> no-move` (×4, windows 1/2/3). NOTE (pi 7): begin prints the POINTER, end prints `DRAG_LAST` = the window ORIGIN seeded at begin — the two coordinates are not comparable; the discriminator is `DRAG_MOVES == 0` (`wm::drag_report`), and the pacer cannot eat the first report (rmbp 12). MECHANISM (orin 15, measured at fb5d0d8a; pi 7 and rmbp 12 verified independently): pointer motion reaches `wm::drag_motion` on aarch64 ONLY through `wm::drag_route_tail` (`drag_motion_paced`), whose three callers are `route_input_to_active_el0` (EL0-app drain only) and the PI's `render_service` (two sites); `orin_render_service` and `jd2_console_pump` contain zero calls, so the Orin has no motion feeder on its path. NOT a parity row: the Pi places on metal (boot12: 3 `-> placed`). Cite `sha:symbol`, not line numbers | orin (`jd2_console_pump`, tegra cfg region of `main.rs`) | Orin bench | fixed-unflown — DRAGDEAD `4dc88314` (orin 15, 2026-09-06): the jd2 pump feeds pointer motion to the grab through `orin_drag_steer(pw, ph)` → `wm::drag_motion_paced` (the next statement inside `drag_route_tail`, which itself takes the panel lock and is the wrong entry on this input path — LOCKFIX `7847ceea`); two line-neutral folds in `main.rs` (8868 lines before and after, P7 index proofs) + a tail append in `display_tegra.rs`; `sha256(kernel8.img)` identical before/after (the value TCURX2.md records for the knob-off Pi image); check 0, test-arm 0, armed build 0, tokens `[dragroute] wired|arm|end` 1 each with two absent controls 0 (grep needs `-F` for a bracketed token). Expected wire: `[dragroute] wired panel=1920x1200 desktop_firmware=1 -> READY`, per gesture `[dragroute] end win=N via=release fed=… applied=… -> STEERED` beside `[wm-act] drag-end … -> placed`. The "(EL0-app drain only)" premise holds: the Orin refuses every EL0 app (EL0-EL1CORE), so no app-owned window exists to drag. WAS: open — fix (a): one `drag_route_tail(ev)` call folded LINE-NEUTRALLY onto an existing statement at the jd2 drain (the ORINRX `serialrx::drain()` fold is the pattern; P7 position proof; `sha256(kernel8.img)` identical before/after or pi re-accepts with the reason) — DRAGDEAD executor (orin 15); fix (b) S7 step 2 (the Orin adopts the shared body) subsumes it (rmbp-ledger B11). Expected wire: `drag-end … -> placed` | `docs/dev/evidence/orin15/FLIGHT-RESULT-render6.md`; `docs/dev/evidence/orin15/render6-boot2.log`; rmbp-ledger B11 | — |

## B. Capture findings (RA, render2 boot 2026-09-05)

| id | item | owner | flies-on | status | evidence | closed by |
|---|---|---|---|---|---|---|
| B1 | Pulse window overlaps the console's bottom 4 rows (prompt row); `[wc-h] win=3 span=64 band=yes` | orin | Orin | open — pulse window returns (R17/A18, DESKSCENE `8cbfaadf`); measured from code at 1920x1200: 993x64 px / 4 rows with the strip live (prompt row inside); after `chrome_h` 104->64 the pulse box is 1290x212 at (10,914) vs the console box 1305x780 at (307,158) = 993x24 px of the box, 19 px of text (prompt row 45 fully + 3 px of row 44); the overlapping part is the pulse window's frame + title bar, its content starts 15 px below the console (`docs/dev/evidence/orin14/A18-DESKSCENE.md`; S15 measurement `60fe2747`, `docs/dev/evidence/orin14/S15-OVERLAP-ORIN.md`). orin13's `pulseoccl-fbcon.patch` (video/, rmbp) is the fix and is NOT applied | RA N1; → S15 | — (was CASCADE via A10; A10 reversed by R17) |
| B2 (→ S1) | Both USB hubs fail status-change endpoint configure (codes 17 / 8); hot-plug behind hubs dead all boot. rmbp's read: interval/ESIT math in the endpoint context | rmbp | Orin | open — owner rmbp; to rmbp 11 (ledgered rmbp-12 P5) | RA N2; `xhci/mod.rs` ~:13465; reproduced on render3b (LEDGERTICK 2026-09-05: `xHCI: HUB slot 1 status-change Configure-Endpoint code 17` :233, `HUB slot 3 … code 8` :298 of `docs/dev/evidence/orin13/render3b-boot1.log`) | rmbp; fix flies on this bench |
| B3 (→ S2) | `MOUSE-1` prints `vid:pid=0000:0000` for hub-attached pointers | rmbp | Orin | open — owner rmbp; to rmbp 11 | RA N3; reproduced on render3b (LEDGERTICK 2026-09-05: `MOUSE-1: HID pointer detected vid:pid=0000:0000` :284 and :297 of `docs/dev/evidence/orin13/render3b-boot1.log`) | rmbp |
| B4 | Three power-ons that session, two dark boots of the foreign volume `0xabfbdefa` (old loader); the firmware can still pick it | orin | bench | open — bench: find and wipe the medium carrying the old loader | RA §8; FR | Peter |
| B5 (→ S6) | `PIUSB` witness family prints on the Orin (shared USB-storage driver, Pi-named) | rmbp | Orin | open — GATE-NEUTRAL census item | RA §6; still prints on render3b (LEDGERTICK 2026-09-05: 5 `PIUSB:` lines, e.g. `[piusb25] storage enumerated: slot 2` :350 of `docs/dev/evidence/orin13/render3b-boot1.log`) | rmbp's GATE-NEUTRAL |

## C. Architecture-conformance findings still open on the Orin (AC, 2026-09-02)

| id | item | owner | flies-on | status | closed by |
|---|---|---|---|---|---|
| C1 (→ S9) | AC#1 knob→leg coverage check cannot fail on this branch | rmbp | Orin | open — until trunk carries KNOBLEG `647f485a` | rmbp landing |
| C2 | AC#7 = A11 | orin | Orin | open | — |
| C3 | AC#8/#9 layering vocabulary (`user-*` crates, "Ring 3" vs userspace) | orin | Orin | open — doc | — |
| C4 (→ S8) | AC#12 `scan_serial_faults` passes on a missing log (negative-only suites) | rmbp | Orin | open — arroyo (shared | GATE-BLINDNESS (rmbp set) |
| C5 | AC#13 GATE-BOOTLOADER hole: `unaos_ivb` bootloader leg (was cited as AC#14 — #14 is the mbench-spec replay finding; LEDGERTICK 2026-09-05 read the AC table) | rmbp | Orin | fixed-unflown — gate, no flight; the leg is GATE-ROOTS `e1bff790` leg 4 on hw-rmbp (`x86_64-unknown-uefi --features unaos_ivb`), NOT an ancestor of hw-jetson `6cc8de8c` (`git merge-base --is-ancestor e1bff790 6cc8de8c` = no; at 6cc8de8c `unaos_ivb` appears only on the kernel `x86-all` leg, arroyo:2702); lands with the rmbp landing | rmbp landing |
| C6 | AC#20 = A10 | orin | Orin | dropped | — |
| C7 | AC#21 (see AC) | orin | Orin | open | — |
| C8 | The six render-pass defects (AC on ORINRENDER) | orin | Orin | flown — render2, all six scored | `a5a66fc1` `7ffd2122` `01739a93` `8085c9c8`; FR |
| C9 (→ S11) | `sys_cap_revoke` leaks the fd on aarch64 | orin | Orin | fixed-unflown — QEMU kernel8-test 119/119 reaches it | CAPREVOKE `06858185` |
| C10 | The carveout exclusion set carries two bounded GUESS windows that no DTB node or readable register confirms: the 0xbe QUIRK widened to 96 MiB (XCARVE-8 `81b2a415`) and the GiB-9 undeclared gap `[0x26c400000,0x279e00000)` up to the fb carveout (XCARVE-11 `8154bc1c`) — ~200 MiB of DRAM withheld from the heap on a guess; MC-register probing is EL3-crash class (XCARVE-8 record). Five July briefs pointed at it, nothing tracked it (P5 sweep) | orin | Orin | open — recorded fact; re-opens as a defect the moment a SNOC Carveout RAS lands OUTSIDE the set (the label is refutable by construction); no flight owed until then | `docs/dev/evidence/orin14/P5-SWEEP.md` rows 11, 13, 24, 25, 32; `docs/dev/OS/01_BOOT_HAL/arch_arm64.md` §JETSON-XCARVE; §F row "Memory" | — |

## D. Decisions argued, awaiting Peter

| id | item | recommendation | source |
|---|---|---|---|
| D1 | Loader `SetMode` to the widest console mode: the 80-col wrap occurs ONLY on F11-menu boots (7/39); auto-boots inherit 240x56 | do it narrowly, knob-gated, before/after witness; one power cycle to test | `docs/dev/evidence/orin13/CONSOLEMODE-ARGUMENT.md` |
| D2 (→ S7) | `render_service` size-3 family: convergence arc (lift the waiting + input-ownership axes) | owed; ledger entry has an expiry | merge `be3b027e` body |
| D3 | GA10B: "licensing/bunker ruling pending Peter" was STALE — Peter ruled 2026-08-25 (`docs/MANIFESTO/CLEAN_ROOM_POLICY.md` §6: quarantined Group-A fact extraction over GPL `nvgpu` is admissible; facts only, provenance per fact, terms review before import). The remaining wall is a hardware fact, not a licence: the GA10B microcode is signed and encrypted with boot-ROM verification (`od` §4). Nothing is pending from Peter; a P10 phantom obligation carried from the orin 5 resume through ORIN-HW and this seat's own words twice on 2026-09-05 | dropped — ruled 2026-08-25; the arc, if ever, is a hardware question | CLEAN_ROOM_POLICY.md §6; `od` §4 |

## E. Landed but unflown (OB table 2) — each with the one question its flight answers
Reconciled against render3b (LEDGERTICK 2026-09-05; every line number is
`docs/dev/evidence/orin13/render3b-boot1.log`, counted with `awk`; the render3b image carried
`witness,ehcihid,holocron,tegra,tegra_el0,tegrasmp,orinrender,desktop_firmware,orinrx,deskcascade`):

- TABKEY `ac9c0701` — **not on the render3b wire**: 0 `KEY 0x09`, 0 `tab-cycle` (nobody pressed TAB).
- `orintenant` (rung 6) — **not on the wire**: 0 `[orintenant]` lines; the knob was not in the image.
- `orinladder` (a) glyphs-on-glass — **not on the wire**: 0 `[oringlass]`. (b) minimise round-trip —
  **not on the wire**: `[dock] live passes=41 … presses=0 raises=0 unhides=0` (:538).
- `orinfurn` bar — 0 `[orinfurn]` lines (`furn=0`), but **its question was answered by the CASCADE path
  instead** (A1): `[pidesk] menubar ENABLED panel=1920x1200 rect=Some((0, 0, 1920, 34))` (:460),
  `[pidesk] menubar PAINTED owns_pixels=true` (:466), and a crystal press consumed by the band:
  `SHARD-MENU: crystal_press=open via=crystal-glyph menu=170x121+12+34 items=4` (:469). The `orinfurn`
  half-rung is superseded by `deskcascade`.
- `tegradesk` floors — **not on the wire**: 0 `[deskseam]`; the cascade seam passed its own floors
  (`[deskcascade] arming cascade panel=1920x1200x4 stage=4194304 table=0` :415) and never printed
  `REFUSE reason=stop-line-5.2` because `deskcascade` crosses §5.2 by construction.
- the five §3.13 terminus instruments — PANELOWN **answered**: `[panel-owner] panel-ownership-handover
  from=owner-fbcon-panel to=owner-console-window site=fbcon::panel_console_window_open` (:431, and
  `to=owner-gui-screen site=fbcon::detach` :560). ORIN-STKDEPTH **not on the wire** (0 `[orinstkdepth]`;
  the §5.2 number came from the boot-core probe instead: `[u7stk] at=boot-core:post-cascade … hw=15472
  headroom=17296` :478). PANELREFUSE, supstate, and the `orinconwin` `live=` read-back — **not on the
  wire** (0 lines each; `conwin=0` on this image, the console was windowed by the cascade route:
  `[wc-x] console-window win=1 … at (307,158) cols=185 rows=46` :444).
- `bsprun`/`bsptick` — **not on the wire**: 0 lines; `SCHED-BAL: work-stealing witness — 0 steals total
  across 5 online cores, 0 core(s) ran work` (:487), `[wcpar] cores=1` (:547) — A6 stands.
- REDZONE absorber — 0 `[redzone]` lines on render3b as on render2: still "never fired", not "held".
- `reboot` verb (PSCI `SYSTEM_RESET`) — **not on the wire**: 0 lines; never fired.
- NET-4 fix — **not on the wire**: the image carried no `net4`; the one `[net4B]` line (:126) is
  `mmu_tegra.rs`'s ungated DMA-window reservation, not the NIC.
- window-body persistence after occluder subtraction — **not answered**: the only click on the wire
  is the crystal (`SHARD-MENU … crystal_press=open` :469 / `dismiss reason=outside` :472), on the bar,
  not a post-click probe of win=1; `[wcn] win=1 … att=37 comp=37` (:542) counts recomposites, not a
  body read-back.
- PRTSCR-ORIN (A9) — **flown** (raw capture only; see A9's evidence cell).
- LOADSAMPLER (A5) — **flown**: `[pstrip] rollup samples=39 redraws=24 skipped=15 srcdelta=23` (:577),
  `[orinrender] census … presents=29` (:584), `SCHED: load c0=85%` (tail).

## F. Hardware support inventory
Measured 2026-09-05 (ORINHW; evidence files scratch (build logs only), capture
`render2-clean.txt`). The shipped Orin image carries ten features; PCIe, NIC, SDMMC, watchdog, rast,
selfup, tenants, EL1-AP and the periodic tick exist only on type-check legs or behind one-flight knobs.
Corrections surfaced while measuring: the 48 MiB heap is the Pi's limit, the Orin maps 8 GiB; the BT
radio IS on the Orin's USB bus (13d3:3549, no driver); audio/RTC/GPIO/I2C/SPI are absent on all
three boards; PSCI SYSTEM_OFF has flown, SYSTEM_RESET has not.

| Subsystem | Orin status | Pi 4 status | x86 rMBP status | Evidence | What it would take | Lane |
|---|---|---|---|---|---|---|
| **CPU cores / SMP** | **WORKS ON METAL, 1 of 6 cores does work.** All 6 A78AE cores enumerated from DTB `/cpus`, 5 APs brought online via PSCI `CPU_ON`, SGIs both directions; then the boot core alone drops EL2→EL1 and runs the terminus cooperatively. APs stay at EL2 inside `sched::run` with nothing placeable: `ONLINE_MASK[0]` is false and cores 1–5 are filtered out as non-EL1, so steals = 0 by construction. `[wcpar] cores=1` in every census. Under the `rastmc` knob the 5 APs *did* render (`RAST-MC: pipeline width 5`), so the AP path executes work when something is staged. **Periodic EL1 tick: fixed-unflown — tick1 image staged from `2a04fb4a` (BSPTICK 2026-09-05, `~/unaos-bench/flash/orin/tick1-20260906T0013Z-…/` (full dir name, executor sha7 suffix, in TICK1-FLIGHT.md §B), render5's knob line + `UNAOS_BSPTICK=1`, a SEPARATE image, never folded into render5); first metal question: does `[orinbsptick] tick N` count past 1 at EL1 on cpu 0 (N = 250, 500, … at ~1 Hz) while `[orinrender] census passes=` keeps climbing and no `=== AARCH64 EXCEPTION` prints — scorer + verdict table in `docs/dev/evidence/orin14/TICK1-FLIGHT.md` §C (PASS / NO / ARM-MISS / TICK-DIED / PUMP-STALL / FAIL-EL2 / EXCEPTION). Read pass found no compose defect (§A): vectors installed post-drop, DAIF unmasked by the arm, `hlt()` stays the JD3 poll-spin, every masked span is save/restore.** | **WORKS ON METAL, 4 cores.** GICv2, each AP arms its own 250 Hz tick, `SCHED_ACTIVE`+`SCHED_GO` flipped in `start_aps`; live steals in the captures (`[smpbal] steal 'el0-thread-w' c2->c3`). | **WORKS ON METAL, 8 cores.** x2APIC, APs run the scheduler and `mark_online`; `[wcpar] cores=8` in flight 5. | r2:405-431 (SMP-3 CPU_ON ×5, SGI OK), r2:432-434 (JM6 drop), r2:498-504 (SCHED-BAL 0 steals across 5 online cores), r2:153 `[wcpar] cores=1`; orin.log `RAST-MC: pipeline width 5 (render cores [1..5])` ×7; `smp_virt.rs:821,253,294,317,366,380`; `sched.rs:3142-3172,3218,5908-5912,6040-6053`; Pi `smp.rs:300-329`, `sched.rs:9534`, boot12.log `[smpbal] steal`; x86 `smp.rs:744`, `sched.rs:5954-5958`, flight5 `[wcpar] cores=8` | **M.** Hard fact: the APs sit at EL2 and the sched's EL1 filter excludes them; `orinel1ap` (drops ONE AP to EL1, `boot_tegra.rs:322-380`) exists unshipped, and only `CPU_AUTO` spawns are steal-eligible (`sched.rs:378`). Needs: all APs at EL1 + `mark_online` for core 0 + something spawned `CPU_AUTO`. Ruling on record: "zero steal-eligible tasks on a default boot; EVAC ruled unendorsable; do not hack it" (jetson resume). | jetson (`boot_tegra.rs`, `smp_virt.rs`); `sched.rs` is rmbp-owned |
| **Timer tick & preemption** | **STUB at EL1 (one-shot only).** EL2 boot core: 250 Hz CNTP PPI 30 live, IRQs taken, WFI idle. After JM6 the EL1 core gets exactly ONE CNTP tick (`el1_oneshot_proof`, self-disarming) — "the cooperative scheduler gains NO periodic tick". `timer_preempt` exists but its GICv3 caller is gated `all(tegra,bsprun)`; `bsptick` (periodic EL1 tick) and `bsprun` (preemptive run) are unshipped. Consequence: "JD3 timerless mechanism" — `arch::hlt` poll-spins instead of WFI so the console/USB pump keeps moving. | **WORKS ON METAL.** Per-core periodic tick "for scheduler preemption", post-EOI `timer_preempt` on every PPI. | **WORKS ON METAL.** LAPIC periodic vector 0x20 → `sched::timer_preempt()`. | r2:116-122 (GIC init, timer armed, timer LIVE), r2:437-442 (IRQEL-RT one-shot at EL1), r2:495 "running the full M4 CAPSTONE cooperatively"; `timer.rs:390,434-472,479-546,666-757`; `gic.rs:937-940,966-972,981`; `sched.rs:299-300,5143-5144,10125-10134`; `boot_tegra.rs:128,136,289-290,372`; `display_tegra.rs:1232-1233`; Pi `timer.rs:38,116-127`, `gic.rs:937-940`; x86 `apic.rs:168-195`, `interrupts.rs:468` | **S–M.** Hard fact: the EL1 IRQ path is proven (one tick taken, banked vectors live) — what is missing is re-arming CNTP in `on_tick` at EL1 and flipping `SCHED_ACTIVE`; the `bsptick`/`bsprun` legs already type-check it. Metal risk is the JM6 latch (`HCR_EL2 IMO=0 FMO=0`, `CNTHCTL_EL2=0x3`) and the pump's `hlt()` waits that assume no IRQ. | jetson (`timer.rs`, `boot_tegra.rs`, `gic.rs` tegra arm); `sched.rs` rmbp-owned |
| **Interrupts (GICv3)** | **WORKS ON METAL for PPI 30 + SGI 0/15 only; zero SPIs.** GICD 0xf400000 / GICR 0xf440000, 992 INTIDs. `enable_spi` has ONE call site kernel-wide (Pi PL011 RX, SPI 153) and it sits past `tegra_early_stop` — unreachable on tegra. Every device (xHCI, UART, NIC) is polled; xHCI `IMAN.IE` is set but "interrupts taken so far: 0". | **WORKS ON METAL.** GICv2; PPIs + IPI_RESCHED + PL011 RX SPI 153 with RX-timeout IRQ. VL805 xHCI still polled. | **WORKS ON METAL.** x2APIC, MSI-X handler acks in xHCI; ISRARM "INERT on this hardware — no IOAPIC to route INTx", storage polled by default. | r2:116-117 (GIC init, self-SGI), r2:383 "xHCI interrupts taken so far: 0"; `gic.rs:187,530-545,616,645`; `main.rs:1394`; `serial.rs:397`; `xusb_tegra.rs:5,1646`; `drivers/xhci/mod.rs:936,2082,5270-5278`; Pi `serial.rs:373-417`; rmbp resume "ISRARM is INERT" | **M.** Hard fact: no device SPI has ever been routed on the Orin; the DTB `interrupts` cells for `usb@3610000`/`serial@` are read by nobody. First SPI needs the EL1 tick row above (an IRQ into a core that expects none). | jetson |
| **Serial UART — TX** | **WORKS ON METAL.** UARTC @0x0C28_0000 (AON cluster, normally SPE/TCU-owned), polled 16550, bounded THRE wait; every capture line is this path. Base marked "TO VERIFY ON THE BOARD" in source but 100 % of the wire evidence rides it. | **WORKS ON METAL.** PL011. | **WORKS ON METAL.** COM1 (FTDI mirror ring). | every `r2` line; `serial.rs:28-53,66-77`; Pi `serial.rs:269-281` | — | jetson |
| **Serial UART — RX** | **WORKS ON METAL behind the `orinrx` knob (render3b 2026-09-05), lossy — supersedes the STUB reading below.** ORINRX `9cb779bd` added `serialrx::drain` on both pump phases (`main.rs:2854,2915`) and `orinrx = []` (`Cargo.toml:2476`); render3b: `[serialrx] lsr=0x00000200 -> RX-LIVE`, `tste\r` → 3 of 5 bytes reached the shell (A4); the loss mechanism is undetermined (A16). Default-off, so the SHIPPED image still has no RX. Pre-render3b reading, kept for the record: `tegra::read_byte` returned `None` unless `tegra_guard::ready()` (DARKWIN-GUARD); render2 question 7 delivered nothing. (LEDGERTICK: `grep -n orinrx Cargo.toml` = :2476; `awk '/serialrx/' render3b-boot1.log` = 23 lines.) | **WORKS ON METAL.** PL011 RXFE poll + SPI 153 RX interrupt; the Pi shell is driven over serial. | **WORKS ON METAL.** COM1 `poll_input` seam. | `serial.rs:81-93,284,471`; `stage-render2.sh` question 7; `capture/line-acm0/marks.txt` "render2 second-question byte 'x' injected"; r2 shows no echo; jetson resume "ORINRX backburner" | **S.** The first flight is taken: LSR raw = `0x200` (RX-LIVE), bytes arrive. Remaining: A16's discriminators (LSR.OVRF count per poll, IIR[7:6] once, paced injection) before any FCR write — the SPE/TCU may be the second reader of the same RBR (`serial.rs:34-38`). | jetson |
| **USB host (XUSB xHCI)** | **WORKS ON METAL (shipped), polled, pre-drop only for enumeration.** Firmware-inherited Falcon (fw 80.05 read over BAR2, not loaded by us; `xusbfw` unshipped), BPMP PG 12/10 ON + 9 clocks enabled via IVC, xHCI v1.20, 8 root ports (USB 3.1 ×4, USB 2.0 ×4), inherited slots evicted (JB9i), interrupter programmed, controller started. Enumeration/hub/storage service runs in the 60 s EL2 window; after the drop the JD2 pump calls `poll_events` only → **no hot-plug of hubs/storage after boot** (HID reports still decode). | **WORKS ON METAL (knob `piusb`).** BCM2711 PCIe RC + VL805 xHCI (1106:3483) over PCIe, same shared driver in polled-aarch64 mode; header ATTENDED-METAL-UNVERIFIED. | **WORKS ON METAL.** Panther Point xHCI (PCI, MSI-X) + EHCI for the internal keyboard/trackpad. | r2:5-8 (JB8 Falcon), r2:33-43 (JB1a/JB1b IVC PING), r2:95-112 (JB1c PG/CLK, XUSB ALIVE, port survey), r2:169-187 (attach, RS=1), r2:211-219 (JB9i eviction); `xusb_tegra.rs:94-98,1791-1858,1887`; `main.rs:2787-2789,2852-2853,2913-2914`; Pi `piusb.rs:4-29`, boot12.log "PIUSB: M1: LINK UP"; x86 `xhci/mod.rs:2389-2409`, flight5 EHCI-HID lines | **M** for IRQ + post-boot hot-plug (same hard fact as the tick row: the post-drop core has no timer, so `service_*` waits that ride `hlt()` cannot run). | jetson (`xusb_tegra.rs`); `drivers/xhci/` rmbp-owned |
| **USB HID (keyboard / mouse)** | **WORKS ON METAL (shipped).** Boot mouse 1c4f:0034 and keyboard+abs-pointer 1c4f:0002 behind a HS hub (0bda:5489): HID endpoints configured, SET_PROTOCOL(boot), set-idle, "keyboard ARMED (slot 5)", "HID: 1 keyboard(s), 2 pointer(s) armed"; `[orinclick]` router ARMED, clicks routed on boot7g (rung 3 CLOSED). Print Screen key edge decodes but its service is `holocron`-gated (see Screenshot row). | **WORKS ON METAL.** Same driver via VL805; uvug/typematic proven on boot12. | **WORKS ON METAL.** EHCI-HID internal keyboard + trackpad (`KEY:`/`trackpad click` lines). | r2:302-340 (HID EPs, SET_PROTOCOL, keyboard ARMED), r2:389 (HID armed), r2:582 `[orinclick] … ARMED`; orin-desktop.md §3.8.1 (boot7g click ROUTES); `xhci/mod.rs:4386-4402`; flight5 `EHCI-HID: KEY:` | — (hot-plug after boot needs the USB row) | jetson / rmbp |
| **USB mass storage (BOT)** | **WORKS ON METAL (shipped) — and it is the ONLY block device.** SuperSpeed hub 0bda:0489 → 0bda:0326 "USB3.0 CRW -SD" card reader at speed 5, bulk EPs configured, TUR/INQUIRY/READ CAPACITY, READ(10) LBA0 passed; `BLOCK_DEVICE` published, "mass storage ready (slot 2); panel shell ls/cat live". USB3 SS/SSP speeds (PSI 4/5, Gen2 case added for Tegra234). Hubs to 5 tiers. Classes with drivers: 0x08 storage, 0x09 hub, HID boot kbd/mouse, 0xFF FTDI; everything else "no driver … releasing port". | **WORKS ON METAL.** `piusb27: mounted FAT32 1938 MiB as /fs/usb` (boot12). | **WORKS ON METAL.** xHCI storage path; flight 5 booted with "no mass-storage device enumerated" (the rMBP boots from its SD reader instead). | r2:243-262 (SS hub + storage), r2:368-378 (STORAGE BRING-UP, Disk 30959 MiB, READ(10) Passed), r2:390 (JD3 mass storage ready); `xhci/mod.rs:548-557,2825-2842,4374-4409,4526,6832-6840`; `drivers/block.rs:704-712,1175-1200`; boot12.log `piusb27` | — | rmbp (`drivers/xhci`) |
| **SD card — on-die SDMMC (mmc@3400000)** | **WORKS ON METAL (knob `UNAOS_SDMMC=1`), READ-ONLY, not the block source.** Tegra234 SDHCI @0x03400000 censused: CAPABILITIES base-clk 204 MHz ADMA2, card detect, SRST, 3.3 V, 400 kHz ID clock, CMD0/8/55/ACMD41 (3 rounds, CCS=1)/2/3/9/7/16, RCA 0xaaaa, 1-bit default speed, CSD v2 62,333,952 blocks (SS32G 12/2014), sector 0 read = MBR — "READ-ONLY; no card write". Vendor pad block DISABLED after an SError conviction (FWALL). `BlockHandle::TegraSd` is read-only in every cfg and `program_source` never returns it; `TEGRA-UNAFS: native unafs volume MOUNTED read-only on TegraSd`. mmc@3460000 (eMMC slot) is `status=disabled` — the Nano module has no eMMC. Not on the shipped image (`sdmmc`,`sdmmc_arm` absent). | **WORKS ON METAL, read + write.** `emmc2.rs` PIO CMD17 reads + U9 CMD24 writes; boot12 "M6g: FAT mounted from SD (Fat32)", "U9: real File writes … PASS"; the Pi boots and writes its own card. | **WORKS ON METAL, read-only + one extent.** `drivers/sdhc.rs` PCIe SDHCI card reader; RO except the reserved flight-recorder extent (`sdhc4c` guard — widening is a STOP tripwire). | orin.log `SDMMC:` ladder (M1 ×6, M2 identified ×2, DONE ×2, `TEGRA-UNAFS … MOUNTED read-only` ×2); `sdmmc_tegra.rs:26-31,43,70,290,408,1462`; `drivers/block.rs:439-444,1748`; `fs/fat.rs:697-701`; Cargo.toml `install_target=["sdmmc_arm"]:1018`; Pi `emmc2.rs:4-15`, `block.rs:1197-1200`; x86 `sdhc.rs:4-20` | **M.** Hard fact: the 4-bit/HS negotiation is unwritten, and the controller's vendor pad block is convicted EL3/SError-fatal when touched with clocks on — any DMA (ADMA2) mode must be designed around that. The CMD24 single-block write is now IN TREE as the knob-gated `sdmmcwrite` probe (A23, fixed-unflown: one witness block to a proven-free scratch sector + CMD17 read-back, vendor block untouched) — its first flight answers gap #3's question. Then `program_source` must be allowed to return `TegraSd` (today constant-refused). | jetson (`sdmmc_tegra.rs`); `block.rs`/`fat.rs` rmbp-owned |
| **Filesystem — FAT read/write, unafs** | **WORKS ON METAL over USB BOT.** Read: shell `ls`/`cat` on the boot volume (FAT serial 0xde001a13). Write: `default_writable()` is constant-true on tegra → `write_sector` → BOT WRITE(10); July 15 metal: "U10: file growth — open-RW+grow-write+readback OK … both FAT copies" PASS, FATMOVE PASS. unafs: native volume located (slot 2 type 0x7f) and mounted read-only on TegraSd under the sdmmc knob; on the shipped image `uls`/`ls /` answer "no unafs volume (NoVolume)" and `run /vug.elf` → "backend error: unafs-mount". | **WORKS ON METAL.** FAT on SD + USB, U9 real writes PASS. | **WORKS ON METAL.** FAT on the SD reader; PNG screenshots land on the card (flight 5); writes limited by the sdhc4c set. | `jetson-serial-2026-07-15-smp3bench.log` (U10, FATMOVE, K4-write); orin.log `PART: unafs span check … fits=yes magic=ok` ×2, `ls: /: backend error: unafs-mount`; `drivers/block.rs:394-400,1089-1091`; `fs/fat.rs:768-790,886-902,1559-1562,2851,3034`; `shell.rs:368,1364`; boot12 U9 lines | **S** to mount unafs read-write on the USB volume on the shipped image (it is a knob/ordering matter); the SD-resident root is the SDMMC row. | rmbp (`fs/`), jetson for the tegra mount site |
| **Screenshot to card (prtscr)** | **FLOWN behind `UNAOS_HOLOCRON=1` (render3b 2026-09-05, A9): one press → `PRTSCR: SCREEN0.PNG 1920x1200 … -> OK`, a valid PNG on the card; the second press wrote a 0-byte `SCREEN1.PNG` with no verdict (A17). Still UNSHIPPED by default:** the `main.rs:3014` site is `#[cfg(feature = "holocron")]` and the jetson default feature line carries no `holocron` (`arroyo:1532`); `2000608a` sat inside a trailing `//` comment until PRTSCRLIVE `60f7ec5e`. The ungated `screenshot` shell verb writes `SCREEN<n>.PNG` via the FAT path. | **WORKS ON METAL** (Pi holocron path per its track). | **WORKS ON METAL.** Flight 5 wrote SCREEN3/4.PNG on the cmd-shift-3/4 chord. | `main.rs:3014`; `video/prtscr.rs:333,426`; `shell.rs:3536-3546`; `drivers/ehci/mod.rs:16195-16197`; jetson resume "Print Screen on the Orin = one service() call + UNAOS_HOLOCRON=1 + verify the FAT handle"; rmbp resume flight 5/6 | **S.** Ship `holocron` on the jetson default line (or drop the gate), and fix A17's second-press re-entry (`prtscr::service`/`capture` create-before-encode order). | jetson |
| **PCIe** | **WORKS ON METAL (knobs `pcieprobe`/`pcie2`/`pcie3`), one RC only.** DTB census: 8 RC nodes, three `status=okay` (`pcie@140a0000` domain 8, `pcie@14100000`, `pcie@14160000`). The code enumerates ONLY the first `pcie@` node (`pcie@140a0000`): ECAM mapped, RP 10de:229c, "LinkCap max(gen3,x2) LinkStatus cur(gen1,x1) DLL-active=1 => LINK UP", downstream 10ec:8168 found, BARs sized and restored, `appl` LTSSM enable written. The two other okay RCs — by Tegra234 numbering C1 (M.2 Key-E) and C4 (M.2 Key-M, x4) — are never touched. Absent from the shipped image (zero `PCIE` lines in r2). | **WORKS ON METAL (knob `piusb`).** BCM2711 RC brought up for the VL805 (PERST, link training, LINK UP 121 ms). | **WORKS ON METAL.** x86 PCI enumeration, `pcicensus`, `pcilink`, `vromprobe` are all x86-only. | `jetson-serial-2026-07-17-r21b-sitting.log` (8 controllers, status per node), `jetson-serial-2026-07-18-r22-net4.log` (PCIE2 LINK UP, PCIE3 DEVICE FOUND); `pcie_probe.rs:45-58,431,736-769,911,931,1215,1282`; `arroyo:1424-1454,4526-4539`; boot12 `PIUSB: M1: LINK UP`; `drivers/mod.rs:39`, Cargo.toml:1068 | **M** to enumerate the other two RCs (the DesignWare bring-up is written once and reusable; the BPMP PG/CLK ids for C1/C4 come from the DTB the same way XUSB's did). | jetson |
| **NVMe** | **ABSENT.** No NVMe driver anywhere, for any board. The C4 RC that would host the devkit's Key-M slot is never enumerated. | **ABSENT** (no slot). | **ABSENT** (rMBP has SATA/AHCI — also no driver; "no SD, AHCI, SATA or NVMe driver on this arch"). | `grep -ri nvme` → 11 prose hits, 0 `.rs`: `unaos/README.md:69` (roadmap box), `builder/src/main.rs:620`, `arroyo:4260`, `drivers/pci.rs:73`, `unaos/docs/dev/OS/10_INSTALL/orin-unafs-root.md:58` ("M.2 NVMe slot is unbrought-up hardware") | **L.** Hard fact: needs a PCIe driver for C4 that exists nowhere + an NVMe queue driver that exists nowhere + a 64-bit-DMA-capable block path (the RTL8168 needed an iATU alias because the heap sits above 4 GiB; NVMe PRPs are 64-bit so that part is easier). | jetson (RC) + rmbp (`block.rs`) |
| **Ethernet** | **WORKS ON METAL (knob `net4`) up to TX; RX defective; never leased.** The devkit's RJ45 is a Realtek RTL8168/8111 on PCIe (the Tegra EQOS/MGBE is unrouted; UEFI "Skipping GBE UPHY config"; no EQOS driver in the tree). `rtl8168_tegra.rs` (4614 lines): iATU in/out, C+ rings Normal-NC, MAC 4c:bb:47:25:49:c8, smoltcp 0.13 bound, DHCP DISCOVER left the NIC; "no lease within 5000 ms (13.5 M polls) — falling back to static 192.168.1.2/24"; RX by class `arp=1 dhcp=0`; NET-4A convicted: "tag-proven single-address latch: 4 consecutive completions wrote ONLY buffer 17 — NIC/RC-internal". NET-4A is an **Orin** finding (boot7h, 2026-08-25). No ping, no ARP reply proven. `vnet` is QEMU virtio-net only; `smolnet` is stripped from all aarch64 media. | **WORKS ON METAL (knob `genet`) to link + polling.** `genet.rs` (5932 lines): MDIO, BCM54213PE autoneg, RGMII OOB, rings; boot12 `PI-GENET: [net19] poll cost …`. DHCP on the Pi is UNPROVEN ("earlier pi DHCP claim was a grep false-positive on 'release' — net-dhcp is UNVERIFIABLE"). | **ABSENT on metal.** `e1000.rs` is QEMU-only; rMBP Broadcom NIC declined: ":: x86_64 PCI: non-Intel NIC (0x14e4) — no e1000 driver, skipping ::". | orin.log:14403-14529 (`PCIE4:` NIC, MAC, TX DISCOVER, no-lease, NO-OFFER, NET-4A); `rtl8168_tegra.rs:4104,4272-4288`; `mod.rs:157,176-177`; `arroyo:1596,1797-1799`; jetson resume :338,:356; Pi `genet.rs:10,649-657`, boot12 `PI-GENET`, pi4 resume :234-235; rmbp resume :307 | **M.** Hard fact: the RX ring's descriptor address reuse is inside the NIC/RC (buffer-17 latch) with the SMMU globally off and DMA riding a self-programmed inbound iATU alias (bus 0x68000000 → DRAM 0x268000000, because the RTL8168 `addr` field is 32-bit). The first fix candidate is placing rings + buffers below 4 GiB (no alias) — the heap-guard's "NO dma-ranges … degraded" finding is the upstream cause. **fixed-unflown — NET4A `a45af90e`: RX ring + 32 buffers below 4 GiB** (`mmu_tegra::seat_net4a_low_nc` + `install_nc_window`, driver `init_rings` prefers the low window; `[net4F] rx-ring phys=… buffers=32 below4g=1`); **first metal question: `[net4F] buffers-written(count=4)`** — scorer in `docs/dev/evidence/orin14/NET4A.md` §6. | jetson |
| **WiFi (M.2 Key-E)** | **ABSENT for aarch64.** Every `wifi*`/`bcmarecon` module is `#[cfg(all(feature=…, target_arch="x86_64"))]`; arroyo strips wifi/bt from aarch64 media. The Key-E card's PCIe half sits on an RC the tree never enumerates. | **ABSENT** (no aarch64 arm; Pi's SDIO WLAN untouched). | **WORKS ON METAL to recon only.** `bcma.rs` AXI-backplane decode of the BCM4331 (0x4331 at 04:00.0); "UNMEASURED: whether the rMBP's WiFi path can carry IP". | `lib.rs:65-66`; `drivers/mod.rs:19-20`; `arch/x86_64/pci.rs:875`; `arroyo:611,630`; rmbp resume :308-312 | **L.** Hard fact: needs the C1 RC brought up (PCIe row) + a Realtek/whatever-the-card-is WLAN driver + firmware load that exists nowhere; the card identity is not even measured on PCIe (only its BT half is, on USB). | jetson + rmbp |
| **Bluetooth** | **ABSENT (device seen, no driver).** The Key-E card's BT half enumerates on USB root port 7 (FS) as 13d3:3549 class 0xE0/01/01 — "no driver for device class 0xe0 (slot 6, 13d3:3549); releasing port". The x86 BT stack lives in `drivers/ehci` (`bt=["ehcihid"]`), compiled to nothing on aarch64 even though `ehcihid` is on the shipped list. | **ABSENT.** | **WORKS ON METAL.** BT over EHCI: HCI_Create_Connection → PAGING a peer (rmbp 9: "BT CLOSED ON HARDWARE"). | r2:363-366; `xhci/mod.rs:4515-4526`; Cargo.toml:597; `drivers/mod.rs:9-10`; flight5 `bt-c1: HCI_Create_Connection … PAGING` | **M.** Hard fact: the HCI transport exists only as an EHCI (x86) transfer path; on the Orin it would ride xHCI bulk/interrupt EPs — a transport port of the existing HCI layer, not a new stack. | rmbp (`drivers/ehci` BT layer) + jetson (xHCI class 0xE0 claim) |
| **Display — scanout** | **WORKS ON METAL (shipped), inherited only.** JD1 reads the DTB `simple-framebuffer` (0x279e00000, 0x960000, 1920x1200 x8r8g8b8 stride 8192), maps it Normal-WB, fbcon + the window compositor paint with the CPU and clean to PoC; the DCE keeps scanning. Composited windows, chrome-on-glass 6/6 probes, console-as-window ROUTED/LIVE, pulse window, ~30 fps software cube (knob). "GOP fb addr=0x0" — the UEFI GOP itself is not used, the DTB node is. | **WORKS ON METAL.** Mailbox framebuffer (firmware allocates and sets the mode); 640x480 on QEMU, 1920x1200 on the bench. | **WORKS ON METAL.** Kepler BAR1 write-combining linear panel (firmware mode kept); copy-engine 2D blit metal-measured. | r2:24-30 (JM7 GOP 0x0, JD1 simple-fb, scanout sane, panel LIVE), r2:130-161 (wc-a/wc-g/orinwm1/orinchrome MATCH), r2:488 `[orinconwin] … ROUTED`; `display_tegra.rs:17-42,95`; Pi `mailbox.rs:4-11,346`; x86 `kepler_display.rs:27-35`, `kepler_ce.rs:16-17` | — | jetson |
| **Display — engine (mode set / vblank / page flip / cursor plane / DP-HDMI link)** | **RULED OUT (orin-desktop.md §4) for this ladder; nothing exists.** No mode set, vblank, flip, double-buffer, HW cursor or link programming in `display_tegra.rs` (0 hits). `jd1dc` (default-off, read-only, BPMP-PG-guarded) flew boot7e/7f: aperture 0x13800000 IS NV_PDISP class NVC67D (Ampere ga10x display, 2 heads, 2 SORs, 4 windows, core channel EFI-owned) — and the first *window* register read at 0x13802e00 was **EL3-fatal**; window sweep gated off. §4: "Mode-set, vsync and multi-head are DC-programming work that does not exist and is not required … until someone proves a safe non-powergated vblank source." | **ABSENT** beyond the firmware mailbox mode set; no vsync/flip tag used. | **ABSENT** beyond a single surface-repoint experiment (`nvidia-kepler-takeover`). | orin.log `JD1-DC-MODEL … MODEL-VERDICT=NVDISPLAY-CLASS-C670` ×9, `JD1-DC CENSUS heads=0 windows=0 … reads=5 writes=0` ×7; orin-desktop.md:3016-3055; `display_tegra.rs:56,687,774-800,1695` | **L / RULED OUT.** Hard fact: a correctly-bounded read inside the DTB-declared aperture is EL3-fatal on this silicon (§4, boot7f), and the permissive register reference (open-gpu-doc, nv-kernel-display-driver-source) documents a different generation's offsets. | jetson |
| **EDID** | **WORKS ON METAL, witness only.** The bootloader reads it from the UEFI EDID protocols (`read_edid`), parses the native timing, carries it in `BootInfo::edid_block`; the kernel prints "edid present=1 hdr=OK sum=OK native=1920x1200 pclk_khz=154120 ext=1 len=256". No consumer — "`edid_block()` is the accessor a mode-set is". | unknown — not measured (Pi geometry comes from the firmware mailbox). | **WORKS ON METAL** via the same bootloader path (UEFI). | r2:13; `bootloader/src/main.rs:50,112,501-503`; `video/mod.rs:536-537,577,639` | — (only matters once the engine row exists) | jetson |
| **GPU (GA10B)** | **open — Peter 2026-09-06 (RULINGS R18): probe boot by boot; ladder in `docs/dev/evidence/orin14/GA10B-LADDER.md`; rung 2 fixed-unflown `a5a62ffb`** (`ga10bprobe2`, sibling knob: BPMP power/clock on the `gpu@` domain + ONE PMC_BOOT_0 read behind an explicit pg=ON readback, symmetric restore, RETURNS — A24). The hardware fact the old RULED OUT rested on stands as rung 4's wall: the microcode is signed and encrypted with boot-ROM-enforced verification (§4; rung 1 measured `opt_priv_sec_en=1`). Rung 1 flown: one read-only flight (`ga10bprobe1`, knob, ends in PSCI SYSTEM_OFF): MRQ_PG GET_STATE + BAR0 GSP falcon/fuse/priscv reads, zero writes. The desktop uses the CPU + the `rast` software rasterizer (knob, unshipped): 90 frames/2970 ms = 30.3 fps at 320x240 on the 1920x1200 panel; `rastmc` spread it over 5 render cores. Licensing: RULED 2026-08-25 (CLEAN_ROOM_POLICY.md §6); the "pending" line this row carried was the D3 phantom. The rung-4 blob question (staging NVIDIA's signed images) is the one still Peter's. | **COMPILED, UNFLOWN as a working path.** V3D 4.2 driver scaffolding, ~25 experiment knobs; the bin-wall / VPM write path is unresolved ("exhausted every ARM-reachable bin-wall hypothesis"). | **WORKS ON METAL, 2D only.** GK107 copy-engine blit ("present_us ~73 % of the blit"); no 3D. | orin.log `[ga10bprobe1] rung 1 complete … zero MMIO writes … SYSTEM_OFF`, `RAST: 90 frames in 2970 ms — 30.303 fps` ×11, `RAST-MC: pipeline width 5` ×7; `ga10b_probe.rs:4-60`; orin-desktop.md:3016-3055; Cargo.toml:1703,1716,1738; Pi `v3d.rs:17-38`, Cargo.toml:1169-1619; x86 `kepler_ce.rs:16-17` | **open — R18 ladder, one rung per boot.** Rung 2 flies next (full boot, `UNAOS_GA10B_PROBE2=1`, without PROBE1); rungs 3/3b designed read-only/first-write; rung 4 = the signed-blob gate (Peter); rungs 5–6 need a Group A facts pass.; the record of every prior probe and every shut-out (R19) is `docs/dev/evidence/orin14/GA10B-HISTORY.md` | jetson |
| **Audio** | **ABSENT.** | **ABSENT.** | **ABSENT.** | `grep -rniE '\b(audio | hda | i2s | codec | sound | alsa)\b' --include=*.rs` → 35 prose hits, no module; no `drivers/audio`. The DTB `display@` reg-names list an `hdacodec` aperture (orin.log JD1-DC-REG) — the HDMI/DP audio codec sits inside the nvdisplay block that §4 forbids touching. | **L.** Hard fact: on the Orin the only audio hardware is the nvdisplay HDA codec (inside the EL3-fatal aperture) and I2S on the 40-pin header (needs a codec board + BPMP clocks). | jetson |
| **Thermal / fan** | **WORKS ON METAL for the fan (shipped), no thermal read.** JB0: MRQ_CLK enable PWM3 (clk 107), MRQ_RESET deassert (70), one fixed CSR write ≈40 % duty ("fan ON … PASS", metal-proven 2026-07-06). No SOCTHERM read, no MRQ_THERMAL (only the "a future arc could" comment); BL31/BPMP run their own hardware thermal net independent of us (`initialized soctherm/thermal_mrq` are BPMP-firmware lines). `thermprobe` is x86-only (Apple SMC). | **ABSENT.** | **WORKS ON METAL (knob `thermprobe`).** Apple SMC SP78 temps / fpe2 RPM (`bench_ride.rs`). | r2:91-94 (JB0 fan ON PASS); `bpmp_tegra.rs:407-466`; `main.rs:2244`; Cargo.toml:931-935; `drivers/mod.rs:39`; orin.log BPMP `initialized soctherm` ×24 | **S** for a Tj readout (one more MRQ, `MRQ_THERMAL`, over the proven IVC channel); **M** for a duty loop. | jetson |
| **RTC / time-of-day** | **ABSENT.** "The boards have NO RTC the kernel reads (§JD16)"; FAT timestamps and holocron carry no wall time. No Tegra RTC @0xc2a0000, no MRQ. | **ABSENT.** | **ABSENT** (no CMOS RTC read). | `clock.rs:3`; `fs/fat.rs:116,139`; `fs/holocron.rs:62`; `grep -rniE '\brtc\b | wallclock | cmos'` → statements of absence only | **S.** Hard fact: on the Orin the AON RTC is a plain MMIO block (needs the base verified against the DTB `rtc@` node and the same "is it powergated?" guard as everything else); alternatively NTP once the network row lands. | jetson |
| **Watchdog** | **WORKS ON METAL (knob `orinwdt`).** TKE WDT0 @0x0219_0000: `boot_arm()` programs a system-POR on the 5th expiry (60 s × 5 = 300 s), nothing pets it, `boot_ok_disarm()` at the EL1 terminus. Captures: "[orinreboot] wdt ARMED — POR reset in 300s" ×4, "wdt DISARMED — boot reached the EL1 terminus" ×2. Not on the shipped image. | **ABSENT** (BCM2711 PM/WDOG path "is the pi lane's" — not written). | **ABSENT.** | orin.log `[orinreboot] wdt ARMED/DISARMED`; `wdt_tegra.rs:4-41`; Cargo.toml:809; `main.rs:2102,2717`; `power.rs:124` | **S** to ship (it is one knob); **M** for a runtime pet from a periodic tick (needs the tick row). | jetson |
| **Power / reset (PSCI)** | **SYSTEM_OFF WORKS ON METAL; SYSTEM_RESET COMPILED, UNFLOWN.** `power.rs` is PSCI-only on `aarch64 && !pi`: the ga10bprobe1 flight ended in "[orinshutoff] PSCI SYSTEM_OFF (0x84000008) via SMC" and the board went dark (MARK o3d "dark=success"). `reboot()` → SYSTEM_RESET 0x84000009 has never been issued on the wire; a returning call parks in `hlt`. The WDT POR above is the only reset mechanism that has fired. | **STUB.** "[orinreboot] no reboot mechanism wired on this platform (Pi: no PSCI; the BCM2711 PM/WDOG path is the pi lane's) — parking in hlt"; same for shutdown. | **Shutdown WORKS (ACPI S5 via `acpi_power::poweroff`); reboot STUB** ("FADT RESET_REG slot is the rmbp lane's — parking in hlt"); flight 6 measured a reset by other means. | orin.log `[orinshutoff] PSCI SYSTEM_OFF`, marks.txt `MARK o3d … ends SYSTEM_OFF (dark=success)`; `power.rs:38,46,57-71,90-112,121-134,142-157`; `arch/x86_64/acpi_power.rs:345` | **S** (one attended flight of `reboot` to prove SYSTEM_RESET on this BL31 v2.13). | jetson |
| **GPIO / I2C / SPI / PWM (40-pin header)** | **ABSENT.** No GPIO, I2C or SPI driver on any board; the fan PWM CSR write is the only PWM write in the tree. | **ABSENT.** | **ABSENT.** | `grep -rniE '\bgpio\b | \bi2c\b | \bspi\b'` → one comment (`xusb_tegra.rs:29`), a future AUX-I2C note (`video/mod.rs:575`), GIC-SPI hits; `bpmp_tegra.rs:460` | **M** each. Hard fact: every header controller (GPIO, I2C, SPI) is BPMP-clocked/reset — the IVC path exists; the pinmux is UEFI's and untouched. | jetson |
| **BPMP / IVC (clocks, resets, power gates)** | **WORKS ON METAL (shipped).** HSP @0x3c00000 dimensioned (nSM=8), doorbell 19, SYSRAM tx/rx queues, "IVC channel ESTABLISHED", MRQ_PING 0xab5466 PASS on every boot (×31 in orin.log). MRQs implemented: PING, CLK IS_ENABLED/ENABLE, RESET DEASSERT, PG GET_STATE/SET_STATE. Used for XUSB (PG 12/10, 9 clocks), fan, SDMMC clock proofs, display PG guard. No MRQ_THERMAL / EMC / DVFS / RTC / UPHY. | **n/a** (no coprocessor; VideoCore mailbox is the Pi's equivalent — WORKS ON METAL). | **n/a** (Apple SMC via `smc`/`thermprobe` knobs). | r2:33-43 (JB1a/JB1b), r2:44-45 (JB5 PG), r2:77-90 (JB7 CLK census), r2:95-108 (JB1c); `bpmp_tegra.rs:17-36,55-68,158,183,473-570` | — (the channel is the leverage point for every other Tegra row: fan loop, RTC, PCIe C1/C4, header I/O) | jetson |
| **Memory (RAM mapped, heap)** | **WORKS ON METAL.** `RAM-GiB-mask=0x3fc` = GiB 2..9 → all 8 GiB of DRAM mapped in 1 GiB L1 blocks (GiB 0 = device window); 6 SNOC-firewalled carveouts UNMAPPED (XCARVE-6, three from DTB `/reserved-memory`, three QUIRK guesses); heap = **48 MiB** at 0x2683ca000 chosen by `select_heap_region` as the highest window clear of 165 carveout ranges — degraded because the RC has no `dma-ranges` (RAS-2 heuristic); 2 MiB Normal-NC DMA window carved below it. **48 MiB is not the board's limit — it is the Pi's**: `allocator.rs:40-42` sets aarch64 (Pi *and* Orin) to 48 MiB because the Pi's heap region is hand-placed at 32 MiB, 64 MiB long, and the two builds are kept byte-identical. Compositor stage 4 MiB + back buffers come out of that 48 MiB. | **WORKS ON METAL.** 48 MiB heap at the hand-placed region. | **WORKS ON METAL.** 256 MiB heap. | r2:14-22 (mmu regs, XCARVE-6), r2:124-128 (HEAP-GUARD, DMA-WINDOW STOP, net4B window); `mmu_tegra.rs:259-275,714-734,1561-1596,1648,1715-1719,1786-1839`; `allocator.rs:36-42`; `fdt_tegra.rs:808,839` | **S** to give the Orin a board-sized heap (a `tegra` arm on `HEAP_SIZE`, byte-identity for the Pi preserved by the cfg); the carveout map and DMA-window derivation are the real work (**M**) and gate NVMe/NIC ring placement. | jetson (`mmu_tegra.rs`); `allocator.rs` shared |
| **MMU / WXN / page permissions** | **WORKS ON METAL with WXN OFF at EL1, deliberately.** EL1 landing `SCTLR_EL1=0x30D01805` (M,C,I; bit 19 WXN=0). `mmu_tegra.rs:50`: setting PXN or SCTLR_EL1.WXN made every RAM GiB privileged-execute-never = "the JM6 metal dark". Protection is per-descriptor: EL0 user window in an unused GiB entry, code page EL0-RX/EL1-RO proven by AT probe ("EL0-read OK, EL1-write denied"). `wxnro` is x86-only. | Same `SCTLR_EL1_VAL` (shared `boot.rs:166`) → WXN OFF; per-descriptor EL0 protections via `aarch64_el0`. | **WORKS ON METAL.** EFER.NXE + CR4.SMEP on ("SMEP on", per-core NXE census), WXN-x86 classifier PASS, `wxnro` W-clear knob. | r2:434 (SCTLR_EL1), r2:443-445 (user window, AT probe); `boot.rs:161-166,322-329`; `mmu_tegra.rs:50,477-483,844-861`; `mmu_tegra_el0.rs:19-42`; Cargo.toml:167-170; `arch/x86_64/mod.rs:34-54`; flight5 `SMEP on`, `WXAUDIT-0 … PASS` | **M**, and a STOP-tripwire class of change: WXN needs a split map (RX kernel text, RW data) at 1 GiB block granularity today — i.e. L2/L3 tables for the kernel image before the bit can be set. | jetson (tegra map); `boot.rs` shared |
| **SMMU / IOMMU** | **WORKS ON METAL as a census (shipped); bypass in effect.** Three `nvidia,tegra234-smmu` instances (0x08000000/0x10000000/0x12000000) dumped (sGFSR, CB0 SCTLR/FSR/FAR, MC ERR_STATUS). The SMMU is **globally off** (`CLIENTPD=1`); the stream-mapping writers (6, under `all(net4,tegra)`) are no-ops on this firmware. PCIe DMA rides the DesignWare inbound iATU the NIC driver programs itself. | **n/a** (no SMMU on BCM2711). | **n/a** (no VT-d code). | r2:162-167,392-396 (JB3); orin.log `[net4w] verdict … bypass (CLIENTPD=1, SMMU globally off)` ×10; `smmu_tegra.rs:426,581,661`; `rtl8168_tegra.rs:4272-4288`; `mod.rs:100-101` | — (no protection is lost that the firmware ever granted; a translating SMMU would be **L** and is not needed for any row above) | jetson |
| **EL0 userspace** | **WORKS ON METAL (shipped), one blob on one core.** `tegra_el0`: user window at VA 0x7800000000 (8 slots), 51-byte `el0-hello` blob spawned at EL0 on the boot core, `SVC EC=0x15` taken, "hello from EL0", round-trip PASS, `[el0stkhw]` headroom 12688. **Only core 0 is at EL1**, so any background EL0 task is refused: `bg /fat/vug.elf` → "no core is at EL1 to host a background EL0 task on this platform (EL0-EL1CORE)"; `run /vug.elf` → "backend error: unafs-mount" (no unafs on the shipped image). | **WORKS ON METAL.** `aarch64_el0` + `vugpar`: VUG/STAT tenants run and are stolen across cores (`steal 'el0-thread-w'`). | **WORKS ON METAL.** Real ELFs from `vessels/`, preemptive. | r2:443-448, r2:511-514 (`SVC`, `hello from EL0`, `TEGRA-EL0 round-trip PASS`); orin.log `BGRUN: bg /fat/vug.elf — rejected (… EL0-EL1CORE)` ×5; `main.rs:7014-7030`; Cargo.toml:826-833; boot12 `[smpbal] steal 'el0-thread-w'` | **M** (= the SMP row: an EL1 AP to host background EL0 tenants, or the terminus core yielding to them) + unafs mounted on the shipped image (**S**). | jetson |
| **EL0 window tenants (rung 6: VUG/STAT/PULSE ELFs in windows)** | **COMPILED, UNFLOWN.** `orintenant` landed 2026-08-25 (§3.10 "UNFLOWN"), default-off, census only: every metal boot that carried it reports `[orintenant] … rows=0 bound=0 creates=0 … -> IDLE-NO-TENANTS` (×~50 lines across boots) — nothing has ever created a tenant window, for the EL0 reason above. Pulse window (rung 5, `render2`) is a kernel-drawn window, not a tenant. | **WORKS ON METAL** (pidesk: uvug tenants, drag, typematic — boot10/12). | **WORKS ON METAL.** | orin.log `[orintenant] census … IDLE-NO-TENANTS`; r2:528 `[pulsewin] open win=3`; orin-desktop.md:1577,3240; `display_tegra.rs:2555,2858,2896-2966,3124` | **M** — gated entirely on the EL0-host row. First tenant needs a core at EL1 that is not the terminus, plus a mounted volume to load the ELF from. | jetson |
| **Self-update** | **COMPILED, UNFLOWN.** `selfup_tegra.rs` (`all(tegra,selfup)`, default-off): mounts the boot volume = the USB stick, S0–S6 (SHA-256 verify, UPK1 parse, staged write + read-back hash, ordered flip loader-before-kernel, cleanup, warm-reboot hook). The header calls itself scaffolding (payload staged by the bench card loop, S4 residual window); nothing marks it flown; the warm reboot it ends in is the unflown SYSTEM_RESET. | **WORKS ON METAL (installer).** `piinstall → piinstall_arm → piinstall_confirm` writes the card. | **WORKS ON METAL (`installdemo`, QEMU scratch disk); `install_target=["sdmmc_arm"]` is the Orin destructive-confirm gate.** | `selfup_tegra.rs:26-56,110,122`; Cargo.toml:1018,1962; `main.rs:2517`; Pi `install/pi.rs:37,165-172` | **S** to fly (a knob + a payload on the stick); **M** to make it meaningful (a root volume that is not the removable USB stick — the SDMMC row). | jetson |
| **Flight recorder** | **ABSENT (x86-only by design).** "Two halves, both x86-only"; capture taps are on `arch/x86_64/serial.rs` only; the service gate is `#[cfg(target_arch = "x86_64")]`. The Orin's flight record is the butler's serial capture on the host. | **ABSENT.** | **WORKS ON METAL.** SD-card extent under the `sdhc4c` permit. | `flight_recorder.rs:19-24`; `arch/x86_64/serial.rs:82,202`; `main.rs:1258-1259,1745,5993`; `serial_ring.rs:1369,1379` | **M.** Hard fact: needs a writable non-removable extent on the board (SDMMC write row) and an aarch64 serial tap; the ring/sink code is arch-neutral. | rmbp (owner) + jetson |

## The five gaps that would most change what the board can DO — ranked

1. **One core, no tick (SMP + periodic EL1 timer + preemption).** Six cores are online and proven
   (PSCI CPU_ON, SGIs, per-AP ticks, and under `rastmc` they rendered), yet the shipped image does
   everything on core 0 cooperatively with no timer after the drop; the APs idle at EL2 and the
   scheduler's EL1 filter makes them unplaceable, so the render pass, the console pump, USB
   servicing and any EL0 tenant all share one poll-spinning core, and `bg` is refused outright.
   The pieces exist unshipped (`bsptick`, `bsprun`, `orinel1ap`), and the EL1 IRQ path is already
   proven by the one-shot. **First metal question:** with `UNAOS_BSPTICK=1`, does the boot core's
   CNTP PPI 30 keep re-arming at EL1 across the JM6 latch (`HCR_EL2 IMO=0/FMO=0`,
   `CNTHCTL_EL2=0x3`) — i.e. does `[bsptick]` count past 1 while the JD2 pump still progresses
   without its `hlt()` poll-spin? (Lane: jetson for `timer.rs`/`boot_tegra.rs`; `sched.rs`
   changes need the rmbp grant; Peter's SMP D1–D5 ruling is still pending per the resume.)

2. **Network RX (NET-4A).** The RTL8168 is found, linked, MAC'd, bound to smoltcp, and its
   DISCOVER leaves the wire — but RX completions latch a single buffer (17) and no OFFER is ever
   delivered; the Orin has never held a lease, answered an ARP, or been pinged. Everything downstream
   (NTP for the RTC row, remote logging, a network install path) waits on this. **First metal
   question:** with the RX ring and its 32 buffers placed *below 4 GiB* (no inbound-iATU alias,
   which the 32-bit `addr` field forced), do four consecutive RX completions land in four distinct
   buffers (`[net4F] buffers-written(count=4)`)? If yes, NET-4A was the alias; if no, it is the
   NIC/RC and the next question is the RC's inbound iATU region ordering. (Lane: jetson.)

3. **A block device that is not a USB stick (on-die SDMMC write, then NVMe).** The only storage
   today is BOT over a USB3 card reader; the Tegra SDHCI reaches card identification and sector-0
   reads read-only under a knob, and `program_source` refuses `TegraSd` by construction. Self-update,
   the flight recorder, an unafs root and "future sessions can write the files and warm reboot"
   (Peter's mission ruling) all need a writable non-removable volume. NVMe is the fuller answer
   but is L (no PCIe driver for C4, no NVMe driver, anywhere). **First metal question:** on
   `mmc@3400000` with the vendor pad block left DISABLED (the SError conviction), does a CMD24
   single-block write to a scratch sector followed by CMD17 read-back return the written bytes,
   at 1-bit default speed? (Lane: jetson for `sdmmc_tegra.rs`; `block.rs`/`fat.rs` need the rmbp
   grant to return `TegraSd` as a program source.)
   **In tree (orin 14, A23):** the `sdmmcwrite` probe asks exactly this — `UNAOS_SDMMC=1 UNAOS_SDMMCWRITE=1`,
   one CMD24 to the top of the post-MBR gap, printed before the write, CMD17 read-back over all 512 bytes;
   `docs/dev/evidence/orin14/SDMMCWRITE.md` carries the wire and the scorer. Unflown.

4. **Serial RX (ORINRX).** TX is the whole instrument; RX is guarded to `None` and the render2
   injection delivered nothing. Without it every flight is attended-only: no remote shell, no
   scripted bench, no "type at the board" from the seat — the operational multiplier for every
   other row. It is S-sized. **First metal question:** after DARKWIN-GUARD `ready()`, with one
   byte injected over the butler FIFO, does a raw read of `LSR` at 0x0C280000+5 show `DR=1`
   (and `RBR` the byte) — or is the FIFO being drained by the SPE/TCU first, which would move
   the fix to a different UART (the `serial.rs:45` fallback bases)? (Lane: jetson.)
   **Answered on render3b (2026-09-05, A4):** LSR raw reads `0x200` → RX-LIVE, and 3 of 5 injected
   bytes reached the shell behind `orinrx`; the open question moved to A16 (which reader drops the
   other two, and whether an FCR write would help or harm the SPE co-owner).

5. **USB interrupts + post-boot hot-plug.** The xHCI is polled with `IMAN.IE` set and zero
   interrupts taken, and after the EL2→EL1 drop only `poll_events` runs — hubs, storage and new
   devices attached after the 60 s window are never serviced, and the pump's `hlt()` waits are
   why (they assume no timer on the EL1 core). This is the first *device* SPI the Orin would ever
   route and it depends on gap 1. **First metal question:** with the DTB `interrupts` cell of
   `usb@3610000` enabled at the GICD and unmasked on the EL1 core, does one port-status-change
   event raise `IRQ_COUNT` above 0 without an EL3 abort or a stuck `EHB`? (Lane: jetson for the
   GIC arm; `drivers/xhci/` is rmbp-owned.)

Not in the five, stated so they are not read as forgotten: **GPU** (was RULED OUT by §4 and the
signed-microcode fact; Peter's R18 2026-09-06 reopened it as a boot-by-boot ladder — `GA10B-LADDER.md`, rung 2 fixed-unflown, §F row); **display engine / vblank** (RULED OUT for
the ladder; EL3-fatal aperture); **WiFi** (L; card identity not even measured on PCIe); **audio /
GPIO / I2C / SPI / RTC** (each S–M and each behind the proven BPMP channel, but none changes what
the board does before 1–5 do); **watchdog / SYSTEM_RESET / prtscr / heap size** (each S, each a
one-knob or one-constant change worth taking on the next flight rather than an arc).

