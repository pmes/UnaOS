# ORIN — WHAT IS IN FLIGHT AND WHAT IS OWED

One file, fixed path, not per-round. Kept current AS WORK HAPPENS, never "at close":
a list written only at close is never written at all by a session that does not reach one,
and a session ends when the work is done or when it is not worth continuing.
`✓` = verified in this tree.  `·` = inherited, NOT re-checked.  Re-derive every sha before acting.
Last touched 2026-09-12T15:1xZ: render13 boot 2 FLOWN — rung 4a/4b/4c PASS from the shutdown path, board off; BEAM LOCKED; nothing left to test on this card (Peter).

## STATE — 2026-09-12T2x:xxZ (session resumed after the usage reset; Peter: "continue from where you left off")
✓ hw-jetson 4fea9e59 = origin (Peter pushed the close); main 198376dd = origin. Board dark; butler pid 27246 holds /dev/ttyACM0; no waker.
✓ NET6 FOLDED 0175479e (executor d48292af merge + 5f58a44c; five gates rc=0 on the merged tree: check 94 legs, tegra check, test-arm full `:: NET6: fixture: 3/3` + el0 socket family PASS, knoboff net4 vs 2c4e7a73 exit 0, esp-jetson rc=0 with `:: NET6:` 1 / `-> is-at` 1 / ` seq=` 3 / ` rtt_ms=` 2 / `-> A ` 1 on kernel.elf, `:: NET7:` 0). CARD: no extra knob — UNAOS_NET4=1 UNAOS_NET5=1 append net6 (banner shows `net6`). Wire: `:: NET6: stack UP over rtl8168: … lease-owner=smoltcp-dhcpv4 [dhcp] sockets=4 ::`; shell `ping 10.42.0.1` → four `seq=N rtt_ms=… -> REPLY` + `4/4 replies`; `arp 10.42.0.1 -> is-at …`; `dns <name> -> A …`. Descriptor-17 row is A64 (A60 stays the veto lift). SHARED FINDING filed on the trunk queue §5 (main 0c109841): knoboff's default baseline is HEAD~1 = the FIRST parent on a merge — every merge-tree knoboff this session names the second parent; the five running executors were told.
✓ BENCH at open: butler host pid 27246 GONE and /dev/ttyACM0 ABSENT on the host (board dark; the ACM port is the board's). Before the card boots: `flatpak-spawn --host python3 ~/unaos-bench/tools/line-butler.py /dev/ttyACM0 ~/unaos-bench/capture/line-acm0` once the device is back, then mark, then waker.
✓ USBREG FOLDED 49465717 (executor 12efacec merge + 8af2acb7 prose fix; five gates rc=0 on both trees, full-wall captures; leg 7 `:: HOMESOIL: usbreg disks=2 … sources=usb+usb1 aliased=- … PASS` both arches, go-red by mutation proven). No card knob. Artifact `:: USBREG: publish` 1, `:: USBLUN: census slot=` 1. Wire with two cards in the reader: `:: USBLUN: census … luns=2 present=2`, two `xHCI: Disk` + two `:: USBREG: publish slot=N lun=0|1 ix=0|1 … disks=1|2`, two `/volumes/<LABEL>` mounts, `[vfs] root … aliased=-`. `storage_slot` still single (hub second reader refused). A60 OPEN NOW: seams BlockSource::UsbN, fat::live_sources, usb_disk_present/count/info_ix, same_device + units_agree; the veto is write_block_tegra_sd (block.rs:1973) + write_blocks_tegra_sd (:1989), dispatched :2427/:2455.
✓ SDWRITE2 = A60 LAUNCHED (Opus, exec-orin-sdwrite2 cut at 49465717, worktree wt-sdwrite2, brief briefs/SDWRITE2.md = the A60 row's five parts + the metal fixture `UNAOS_SDWRITE=1` → `:: SDWRITE: root=<source> wrote=1 readback=match deleted=1 -> PASS ::`; grants main.rs one line-neutral call, sdmmc_tegra.rs the write_block_512 twin). Folds after the six; its card knob joins the line if it lands in time, else it is its own boot.
✓ SERDRAIN FOLDED 447cfc58 (executor 96e76643 merge + 9c0263a0 deliverable 3; five gates rc=0 twice; `PWRDRAIN lines=64 bytes=4352 residue=0 PASS`, DRAINCAP/SERWIT-1B/SERWIT-1 PASS; go-red both isolated; esp-jetson `ring drained` 1, 0 at the base). SO31 fixed-unflown (seat adds the sha at fold: 9c0263a0). No card knob. Wire on Shut Down: `[crystal] verb=shutdown -> PSCI SYSTEM_OFF` … `[pwrshutoff] ring drained lines=N bytes=M` then the SMC; no such line = the machine died before it. REPORTED NOT MADE: (a) x86 crystal.rs/instgui.rs call acpi_power::poweroff() directly — that route still drains nothing (trunk, shared; filed §5 below); (b) the three ring fixtures (draincap, bpress, pwrdrain) each fill 64 lines at the first mirror_service on EVERY image incl. esp-jetson — 12,928 B ≈ 1.1 s of UART per boot; SEAT DECISION: not a card blocker (boot-time, before the desktop), but they belong under `witness` like every other fixture — filed on the trunk queue §5, owner the next serial arc.
✓ GA10B5A FOLDED 234a3e8c (executor 422ae733 merge, M2 b5b465d2, M3 49a93ff4; six gates rc=0 on the tip: strict checks 97 legs, PROBE5=1 test-arm LOADED total=40192 + REFUSED reason=digest, knoboff vs 4fea9e59 exit 0, esp-jetson `[ga10bfw]` 14 / `[ga10bprobe5a]` 84 armed and 0 unarmed, scorer selftest rc=0 with 5a red on both flown logs; no vendor byte in the tree). KNOB LINES for the GPU boots: 5a raw `UNAOS_GA10B_PROBE5=1`, 5a shift8 `=5`, deferred `=1d`/`=5d` (needs the power.rs 5d hook — one cfg'd statement beside the ga10bprobe4d call in psci_call — REPORTED NOT MADE; 5a flies immediate and attended per the R53 decisions, so the hook is not needed for the boot), 5b `=2`/`=7`. WRITES for dmabuf_pa 0x80200000: raw fmccode_lo=0x80200000 fmcdata_lo=0x80207000 pkcparam_lo=0x80209500; shift8 0x00802000/0x00802070/0x00802095; bcr_dmacfg=0x80000002, bcr_ctrl=0x111, cpuctl=0x1. STAGING: copy the triple + LICENCE.txt into target/aarch64_esp/GA10B/ before stage-orin.sh (it cp -R's and hashes everything; no edit needed). Seat at fold: the design row was A64 (taken by NET6's descriptor-17) → A65, references in the RUNG6 brief, arroyo, Cargo.toml, ga10b_ignite.rs renumbered.
✓ GA10B4E FOLDED 902bfc3f (executor 2bda5c65 merge + eed8d16e; gates on the tip: strict checks rc=0 69 legs, knoboff ga10bprobe4a vs 4fea9e59 exit 0, esp-jetson =7 rc=0 `shift=8` 9 / `[ga10bprobe4d]` 8 / `DEFERRED RUN` 1 / `rung 4e DEFERRED` 1, =5 and =6 rc=0, =2 both 0, =2/=4 loadable images identical, scorer 22/22). STEP 2 DONE: `UNAOS_GA10B_PROBE4=7` = 4d's feature set + ga10bprobe4e (no third path). FOLD REPAIRS by the seat (four same-anchor conflicts with GA10B5A, all unions): scorer-ga10b4.sh lost its 5a dispatch behind a duplicated `*)` arm and carried the old 4c selftest block twice — fixed; selftest 27/27 PASS, render13-boot2 4a/4b/4c rc=0, 4e/5a red as they must. arroyo and Cargo.toml unions (PROBE4 =5/=6/=7 lines + the PROBE5 block; ga10bprobe4e/4f + ga10bprobe5* features) are NOT yet compiled — the fold gate is the proof. CARD KNOB LINE: render13's fifteen with `UNAOS_GA10B_PROBE4=7` for `=4`, plus `UNAOS_LOGIN=1`. Shut Down wire: `[pwrshutoff] shutdown verb` → `[ga10bprobe4d] DEFERRED RUN` → 4a → `[ga10bprobe4e] rung 4e ARMED` → `about-to-WRITE priscv_bcr_fmccode_lo … val=0x00802000 raw_pa=0x80200000 shift=8` → `BCR-ALLHELD` → 4b `shift=8 -> BROM-VERDICT-…` → 4c → `CENSUS-COMPLETE` → `[pwrshutoff] ring drained` → SYSTEM_OFF. Early banner says `DEFERRED ARM (UNAOS_GA10B_PROBE4=4)` by design (that string lives in the =4 image) followed by the `=7` line.
✓ FABLE CREDITS OUT 2026-09-12T2x:xxZ (Peter: "We ran out of fable credits"). The LOGIN executor (Fable) died on HTTP 429 mid-M3; GA10B5A (Fable) had already finished and folded. Everything from here is OPUS, and commits from this seat now end `Co-Authored-By: Claude Opus 5`. LOGIN RELAUNCHED on Opus with briefs/LOGIN-resume2.md.
✓ LOGIN M2 DONE and gated on the branch: exec-orin-login 63503c4f (merge hw-jetson 2c4e7a73) then e16e04aa — `/home/<user>` created at first login on the EL0 FAT volume (`[users] home=/home/una created volume=el0-fat`), the aarch64 EL0 `sys_open` takes a `/`-separated path knob-on (SO20 first ABI step, LINE-NEUTRAL, knob-off verbatim), the home ACL proof on BOTH arches (`[users] home-acl … owner_ok=true same_user_live_refused=true same_user_admitted=true anon_refused=true`), and the fixtures moved onto their OWN knob `UNAOS_LOGINST=1` so a shipping login boot carries no fixture credential (`:: LOGIN:` = 0, `[users] load` 3, `[users] home=` 2 on the card artifact). Gates rc=0 on the merged tree; knoboff login vs 2c4e7a73 exit 0 after the midden_core HOST_VERBS rows moved behind the crate's new `login` feature. SO35 filed (a path-opened owner row is in-boot only; a FAT home has no owner); SO32 ticked. M3 (the login SCREEN, video/login.rs, 412 lines) was in flight UNCOMMITTED when the model limit hit — the relaunched executor decides whether to finish or rebuild it.
✓ FOLD PRE-CHECK on the five-fold tip fe370e20: `UNAOS_TEGRA=1 UNAOS_LEDGER_STRICT=1 UNAOS_K8REACH_STRICT=1 ./arroyo check` rc=0 — the GA10B4E/GA10B5A union of unaos/arroyo and Cargo.toml COMPILES, and ledger-check is green at 348 rows. The full fold gate (PLAYBOOK §3, chain script ~/unaos-bench/scratch/orin-0912b/fold-aio/chain.sh) runs once LOGIN and SDWRITE2 are in.
✓ SDWRITE2 = A60 FOLDED 80efd737 (executor 7e6ad01d off 49465717; six gates rc=0: strict checks 72 legs, test-arm full `:: SDWRITE-POSTURE: posture=on map=yes/no sources global=rw usb=rw card=absent PASS` with go-red, test full WC, knoboff sdwrite vs 49465717 exit 0, esp-jetson `:: SDWRITE:` 2 / `rw=` 6 armed and 0 unarmed, UNAOS_NOSDWRITE=1 gives `WRITE refused` 1). A60 → fixed-unflown. CARD KNOB: + `UNAOS_SDWRITE=1`. Wire: `[vfs] root mount / = native unafs volume source=tegra-sd rw=yes`, `TEGRA-SD: WRITE admitted at the block layer`, `:: SDWRITE: root=tegra-sd wrote=1 readback=match deleted=1 -> PASS ::`. CARD SAFETY on the boot: the fixture writes ONLY /SDWRITE.TXT and deletes it — any other changed file is a defect, and `step=delete` FAIL leaves SDWRITE.TXT behind. Seat ruled (R53) on both reported decisions: the separate `sdwritefx` fixture feature stands (a default-on feature cannot be armed by its own knob), and the two k8-reach.registry NA rows stand (the K8_FEATS arm would make the Pi root posture follow another board — R39).
✓ SIX FOLDS IN, TIP 80efd737 TYPE-CHECKS BOTH POLARITIES: `UNAOS_TEGRA=1 … ./arroyo check` rc=0 and plain strict rc=0, 0 red legs, GATE-LEDGER 348 rows (logs fold-aio/pre2-*). Only LOGIN M3/M4 is outstanding before the full fold gate.
✓ LOGIN FOLDED 80070464 — ALL SEVEN ARCS ARE IN (M1 0a04c01b, M2 e16e04aa, M3 46980b25 the screen, M4 9faaeb3c Log Out). Gates rc=0 on each milestone; knoboff login vs 2c4e7a73 exit 0 both arches — the whole arc M1-M4 moves no knob-off byte. Witnesses `:: LOGIN-SCREEN: … esc_kept=true wrong_kept=true opened=true passes_through=true logout=true back_after_logout=true second_login=true logins=2 -> PASS ::` and `:: LOGIN-LOGOUT: row=6/7 label=Log Out … session_closed=true screen_up=true -> PASS ::`, each go-red proven. CARD LINE CORRECTION from the executor: the screen witnesses exist only on the FLIGHT line (a bare `UNAOS_TEGRA=1 UNAOS_LOGIN=1 esp-jetson` builds NO desktop and therefore no screen) — and `UNAOS_LOGINST=1` NEVER goes on the card (the fixtures write a known credential to the medium). Card artifact: `Log in to UnaOS` 1, `Create the first user` 1, `Log Out` 1, `[login] screen open` 3, `[login] session open` 1, `[users] home=` 2, and `:: LOGIN:`/`:: LOGIN-SCREEN:`/`:: LOGIN-LOGOUT:` all 0. FOLD REPAIRS by the seat: four conflicts — LEDGER.md (both sides took SO33; USBREG keeps it, LOGIN's bootdisk-NONE-survey row renumbered SO38), unaos/arroyo (the `arm-virt-el0` leg gains `login,loginst` AND the NET6 `arm-virt-net6` leg stays), syscall.rs (two file-tail blocks, NET6 then LOGIN, both kept), lib.rs (the Sha256 cfg_attr line takes BOTH `login` and `ga10bprobe5`). Markers 0, GATE-LEDGER 354 rows.
✓ CARD BLOCKER FOUND AND BEING FIXED — LOGINCLOSE (Opus, exec-orin-loginclose at 80070464, briefs/LOGINCLOSE.md): the login window carries a wm CLOSE BOX; clicking it clears `login::WIN` through `winid_holders_clear` while `FORM.state` stays Open and `console_present_suspend(true)` stays set — no screen, no console, every keystroke swallowed. ONE MOUSE CLICK on the first thing Peter sees. Preferred fix: create the screen with no control cluster; fallback: the holder-close path calls `take_down()`. SO36 ticks. THE FOLD GATE WAITS FOR IT.
✓ SO37 filed by LOGIN M4 (open): Log Out does not stop what the session started — a program keeps the principal stamped at its launch, so a still-running program of the logged-out user can still open that user's files. Every ACL row is exactly what the launch wrote, so it is not a hole, but it is not the Mac model either; the fix is a session epoch checked in SYS_OPEN, an ABI change, a later arc.
✓ CARD KNOB LINE, FINAL (render13's fifteen, PROBE4 4→7, plus two): `UNAOS_TEGRA=1 UNAOS_TEGRA_EL0=1 UNAOS_WITNESS=1 UNAOS_ORINRENDER=1 UNAOS_DESKCASCADE=1 UNAOS_ORINRX=1 UNAOS_HOLOCRON=1 UNAOS_ORINCLICK=1 UNAOS_TCUPROBE=1 UNAOS_TCURX=1 UNAOS_BSPTICK=1 UNAOS_BSPRUN=1 UNAOS_NET4=1 UNAOS_NET5=1 UNAOS_GA10B_PROBE4=7 UNAOS_BEAM=1 UNAOS_LOGIN=1 UNAOS_SDWRITE=1`. NEVER UNAOS_LOGINST=1, NEVER UNAOS_NET6=1 (net4+net5 append it).
✓ MORE WORK LAUNCHED while Peter is out (2026-09-12T23:xxZ; Peter: "I'm still out is it possible you can find more jobs to run"). FOUR executors off 80070464, all Opus, all with disjoint file sets from LOGINCLOSE: PULSEQUIT (exec-orin-pulsequit, ledger A30 — closing the pulse window reopens it: the close-box RACE in pulsewin.rs close() and the QUIT BYPASS in winmenu.rs that calls raw wm::close; the row names the fix shape, both causes go-red separately) · PWRDRAIN2 (exec-orin-pwrdrain2, the two SERDRAIN rows on trunk §5 — the x86 direct acpi_power::poweroff route drains nothing, and the three ring fixtures ship unconditional on every image) · KNOBOFF (exec-orin-knoboff, SO34 + the two trunk §5 rows — the gate's wrong merge baseline and its single shared scratch dir, both to be proven from files with a real merge and a real concurrent pair). A49 was considered and REJECTED as an executor job: its instrument and fix are already in tree and only metal closes it.
✓ SEAT WORK, done: (a) FIXTURE_FLAKES §1c banked its 2nd and 3rd sightings (LOGIN M4 and SDWRITE2, both on the six-executor box, both clean on an idle re-run) — the starvation reading is CONFIRMED on §1c's own terms: `max_ms=0` is the outlier it asked for and `drag_evt=0` says the synthetic drag got no pass at all; filed the shape worth fixing (a leg whose stimulus never ran should REFUSE, not FAIL) for the next x86 compositor arc. (b) `~/unaos-bench/tools/stage-orin.sh` gained the optional 8th argument `<extra-dir>:<dest>` (backup `.bak-withdir-20260912T232805Z`) so the GA10B triple + LICENCE.txt reach the CARD without ever entering the repo OR the build tree (R52): each file verified byte-for-byte, a sha256 row per file in the MANIFEST, `STAGED-EXTRA GA10B/ files=N from=…` on the wire. Selftested: no-extra path unchanged; with-extra stages and hashes; refuses a nested dest, a missing dir and an empty dir with rc=2 and stages nothing. The 5a line will be `stage-orin.sh target/aarch64_esp <prefix> <sha40> hw-jetson "<KNOBS>" "<EFF>" "<HDR>" ~/unaos-bench/flash/orin/ga10b-fw-36.4.3:GA10B`. The bench flash MANIFEST was left exactly as found (33 images, 334 file shas, validate rc=0).
✓ KNOBOFF FOLDED a2f9f4ee (executor 1c8397fa off 80070464; tool change only, nine hunks inside knoboff(), no kernel source, gates rc=0 and `knoboff wc/login 80070464` exit 0 before AND after the commit with the control firing — which is itself the proof that no kernel image moved). A bare merge HEAD now EXITS 2 and prints both parents with their re-run commands; the executor chose refusal over defaulting to HEAD^2 and the reason holds: the parent that carries the tree-without-the-arc depends on merge DIRECTION, LAWS §3 sanctions both directions, so a default is confidently wrong half the time while an exit 2 can never read as green. Scratch is now keyed per caller with an flock, and a failing run KEEPS its directory and prints the path. Both fixes proven by execution, not assertion: a real throwaway merge (pre-fix rc=1 on the TRACK's delta, fixed rc=2, fixed-with-parent-named rc=0) and a real concurrent pair (pre-fix one wrong verdict with `index.lock: File exists` in the shared log, fixed both right, the second printing that it WAITED). Not executed and stated: the two-different-worktrees collision (the executor has one worktree; the same-worktree case, harder because the paths are equal, is the one it ran). SEAT RECONCILED the queue duplication the executor flagged: main 93ff5c1d drops its two `· NEW` rows for one pointer line, because hw-jetson now carries them as `✓ DONE` and they arrive when it lands.
✓ LOGINCLOSE FOLDED 6fee8b3a — THE CARD BLOCKER IS GONE, and the brief's premise was half wrong: there IS no close box. `login::open` mints the screen with `wm::create_at(0, ..)` (owner asid 0, the shell/desktop band), which wm.rs:15457 gives no control cluster and wm.rs:3011 skips in hit_test — so the routers never ask the close question about that row. That safety was ONE undocumented load-bearing argument; it is now measured by a fixture leg with an armed control row minted through the same call with only the owner changed. The STRANDING was real and is healed: `heal_if_row_gone()` at the head of every key offer restores WIN/FORM.state/console if anything closes the row by another road. Shape (b) was rejected with reason (TABLE re-entrancy on the input path = the reopen-latch shape R49 retired). Eight gates rc=0, knoboff byte-identical both arches, both new legs go-red. ON THE CARD `[login] row closed under an open screen` must NEVER print — one on the glass is a finding naming a route to shut. SO36's furniture half stays open for the next arc (the gate belongs at video/strip.rs:779 press_route, not in login.rs).
✓ A25 CORRECTED BY THE SEAT to fixed-unflown (a94db49a) — the row's head still read "NOT LANDED, NOT FLOWN" but the MENUOWN fix IS in this tree: the drafted three were RE-CUT and the re-cuts (02132f62, b956cc6c, 43ed98bd) are ancestors. Read in the files, not off the commit names: ONE reduction in `Model::read` with `menu_owner` taken from `cap_owner`, `MENUS_X0` reduced to `TITLE_X0` as the no-caption fallback, titles laid out from the app title box's right edge. NO EXECUTOR NEEDED — what closes A25 is a CAPTURE, so it joins the card's WATCH LIST: open console, then quarry, then pulse, and look at the bar each time — the bar must show the FOCUSED app's menus and the gap must not vary with the caption's length. Twice the ledger gate refused the correction and was right both times (a fixed-unflown row's shas must be ancestors); the drafted shas are now written `exec-orin20-fold:<sha>`, the declared-prefix form ledger-check.sh:339 already skips. The seat nearly filed that as a gate gap and read the code first — it is not one.
✓ PULSEQUIT FOLDED 76850c05 — and A30's TWO FIXES WERE ALREADY IN THE TREE (a84464fe, an ancestor): the disarm IS the first statement of pulsewin::close() and the Quit arm DOES route through it. The executor re-derived that before editing and changed no line of either fix. What was missing was the gate, and that is what this commit is: a close census (LAST_SEAL bit0 `disarm_before_swap`, sampled BETWEEN the disarm and the swap) plus winmenu::pulsequit_selftest driving both stimuli through real seams. The go-red is the interesting part — moving the disarm back below the swap keeps EVERY end-state leg true and only bit0 convicts, which is exactly why the bench flight could convict this code and a naive end-state test could not. Six gates rc=0, knoboff byte-identical. SCOPE: the census reaches the card, the FIXTURE does not (crystal::selftest is x86-ladder and dead-stripped on aarch64), and the re-mint-by-render-pass half is gated on the Orin only because ui_status::tick never arms on the x86 test path — printed as `remint_live=false`, not hidden.
✓ QUITLEAK LAUNCHED (Opus, exec-orin-quitleak at 76850c05, briefs/QUITLEAK.md) = the finding PULSEQUIT was not allowed to fix: quarry (quarry/live.rs) and instgui (instgui.rs) still take a bare `wm::close(win)` on their app-menu Quit. Neither reopens (no ARMED latch) but each leaks its own teardown — quarry a Vec, and INSTGUI leaves `fbcon::console_present_suspend(false)` uncalled, i.e. THE CONSOLE STAYS SUSPENDED. That is the same stranding class LOGINCLOSE just healed, reachable from the app menu. Job: the public win()/close() pair each module needs, the Quit arm routed to them with the cfg'd knob-off twin, and a per-app gate with a separate go-red each. A29's holder registry is NOT the fix — it clears an id cell and cannot free a Vec or resume presents.
✓ PWRDRAIN2 FOLDED daf38b01 (S5DRAIN 30e736d6 + WITNESS-GATING 3b784ed1; all gates rc=0 on both, ledger-check 357 rows; arroyo untouched). D1: the x86 S5 flush is at the PORT — `poweroff()`'s own first statement — so crystal.rs, instgui.rs and power.rs cannot skip it; go-red RUN (`:: S5DRAIN: FAIL — filled=64 lines=3 bytes=201 want_bytes=4288 residue=61 ::`, 201 B exactly the predicted arithmetic). D2: the three ring fixtures are behind `witness`, returning 14,143 B ≈ 1.228 s of UART per boot; esp-x86 and kernel8 fills 1 → 0, loadable images −41,864 B and −133,172 B. knoboff exit 1 on both is the STATED direction, with the aarch64 image byte-identical on D1 proving the shared-file folds were line-neutral.
✓ SO41 OPEN, and it is the real find — `mirror_service` IS NEVER CALLED ON THE JETSON. Its two aarch64 call sites are main.rs:4425 (baremetal-gated) and main.rs:1779 (the shared BSP loop), and the Orin's `sched::run_bsp(0)` handoff at main.rs:1494 diverges before that loop. Proven two-sided: seven fixture strings absent while controls hit, and `llvm-nm` showing the `_DONE` .bss statics with NO function symbol for mirror_service or any selftest. Consequence: `[mirror] N line(s) dropped` and `:: SERWIT-2 ::` CANNOT APPEAR on a Jetson capture — that tap is silent by construction on this board. NOTE for reading render13's wire: the drops we DID see there (`[serial] dropped 5331 lines in 192 events`) come from the ring's own counter, a different instrument, which does work here.
✓ SEAT DECISION — SO41 IS NOT FIXED BEFORE THIS CARD, and the reason is the card itself: the flight line carries UNAOS_WITNESS=1, so adding the missing `mirror_service` call also ARMS the three ring fixtures on the flight image and spends 1.228 s of UART back — three 64-line ring fills at boot, on the very boot whose serial we are flying, in a track that has already paid once for a ring flood (SO29/DRAGSTALL). The instrument we would gain is not the one that caught render13's drops. It lands next session, deliberately, probably with the fixtures gated on something narrower than `witness`. Filed on the trunk queue §5 (main 90ad68d7) with that constraint written down.
✓ QUITLEAK FOLDED 56ec3d13 — quarry's app-menu Quit now runs quarry's own teardown (`:: APPQUIT: app=quarry … owner_close=true model_freed=1 surf_released=1 seal=3 PASS`, go-red leaves row_gone TRUE while every teardown bit goes to zero). Six gates rc=0, knoboff byte-identical both arches. INSTGUI: fixed but UNGATED, and the fixture found out why — the app menu CANNOT REACH that window: instgui mints `wm::create_at(0, ..)` and `wm::dock_addressable` requires `owner_asid != 0`, so dock_scan never emits the row and the bar lays out no app box (measured against the live window: `bar_named=false NOT-REACHABLE reason=owner0-is-not-dock-addressable z=58 shell_z=57 boxes=0`). The leg is a SKIP naming its own reason, never a FAIL for a gesture the product does not offer. So the stranding I called the serious one does NOT arrive by that door — and instgui is x86+wc only, so it is not on this card at all. The door that IS open is x86's `wc_close_furniture` (arch/x86_64/syscall.rs:6503), recorded as SO42 with `fbcon` needing a `console_present_suspended()` getter. SEAT AT THE FOLD: the executor wrote its row as SO39, which PWRDRAIN2 had already taken — renumbered SO42 here, and the A30 cross-reference fixed with it.
✓ ALL EXECUTORS DONE. hw-jetson carries twelve arcs: NET6 · USBREG · SERDRAIN · GA10B5A · GA10B4E · SDWRITE2(A60) · LOGIN · LOGINCLOSE · KNOBOFF · PULSEQUIT · PWRDRAIN2 · QUITLEAK, plus the A25 correction. THE FOLD GATE IS RUNNING on this tip (PLAYBOOK §3, six steps, chain ~/unaos-bench/scratch/orin-0912b/fold-aio/chain.sh, rc.txt fills in, DONE at the end).
✓✓ RENDER14 IS STAGED AND READY TO FLY — `render14-20260913T0135Z-43ff29b` on hw-jetson 43ff29bd.
   FOLD GATE 6/6 rc=0, ZERO reds in every log (1-check-tegra · 2-check-plain · 3-test-wc QEMU_FULL · 4-test-arm QEMU_FULL · 5-esp-jetson · 6-esp-jetson-img; logs ~/unaos-bench/scratch/orin-0912b/fold-aio/).
   ARTIFACT CERTIFIED by `LC_ALL=C grep -a -o -F` on kernel.elf (3,572,536 B, sha256 8fc76ebfa429…) — certify.sh in that dir, output certify.out. Present: `[dock] launch app=` 3, `from=program` 1, `wcd_skips=` 1, `DRAINCAP` 1, `ring drained` 1, `Log in to UnaOS` 1, `Create the first user` 1, `Log Out` 1, `[login] screen open` 3, `[login] session open` 1, `[users] home=` 2, `[login] row closed under an open screen` 1 (the heal, which must NEVER print on the glass), `:: NET6:` 1, ` rtt_ms=` 2, ` -> is-at ` 1, ` -> A ` 1, `lease-owner=` 2, `:: USBREG: publish` 1, `:: USBLUN: census slot=` 1, `:: SDWRITE:` 2, `rw=` 6, `shift=8` 9, `[ga10bprobe4d]` 8, `DEFERRED RUN` 1, `disarm_before_swap=` 1, `[quarry] closed win=` 1, `app-menu quit win=` 1, `BEAM VERDICT=` 3. ZERO, as they must be: `shell-remint`, `reopen requested`, `:: LOGIN:`, `:: LOGIN-SCREEN:`, `:: LOGIN-LOGOUT:`, `:: PULSEQUIT:`, `:: APPQUIT:`, `[draincap] fill`, `[bpress] fill`, `[pwrdrain] fill`, `instgui closed win=`, and the control token. The three `fill` zeros are PWRDRAIN2 working: no fixture credential and no fixture flood on the card.
   BANNER: witness,ehcihid,kbdwit,sdhcblk,holocron,login,smolnet,tegra,bsptick,bsprun,beam,ga10bprobe4a,4b,4c,4d,4e,orinclick,tegra_el0,tegrasmp,apsrun,orinrender,desktop_firmware,orinrx,tcuprobe,tcurx,deskcascade,net4,pcie3,pcie2,net6,net5,sdwrite,sdwritefx,sdmmc. max_vaddr=0x3fe708 (score `KELF min=0x0 max=0x3fe708` FIRST on the wire).
   CARD IMAGE `render14-20260913T0135Z-43ff29b.card.img` (671,088,640 B) beside the staged dir, sha in the flash MANIFEST, validate rc=0 (34 images, 345 file shas). FATCLUST clusters=256030 kind=FAT32 -> PASS on both the build image and the staged copy. CARDREADY-GEOM 1:0c:2048:260096,2:7f:262144:1048576.
   DRY RUNS: both writer modes refuse on `/dev/mmcblk0 does not exist` — the reader is not plugged in (Peter is out). Every check that CAN run without the card passed (C9-NOT-SYSTEM, C13-TARGET-QUIET, …); the refusals are C3/C4/C12/C15/C5/C7/C8, all "no target", which is the correct answer. RE-RUN BOTH DRY RUNS once the card is in, before writing.
   MARK appended (`MARK render14 43ff29b boot1 staged-armed …`). PLAYBOOK written to ~/unaos-bench/PLAYBOOK-orin.md, watch list first, with the two card lines filled in.
   WAKER NOT STARTED and the BUTLER IS NOT RUNNING: /dev/ttyACM0 does not exist (board dark, USB unplugged). The playbook tells Peter to start the butler BEFORE powering on; the waker is armed after that, against the then-current byte anchors.
## RENDER14 FLOWN 2026-09-13 — FIVE BOOTS, PETER AT THE GLASS. Capture ~/unaos-bench/scratch/orin-0912b/wire-r14.txt (29,807 lines from mark raw=46873780), split boot1..boot5; the Shut Down boot saved as docs/dev/evidence/orin28/render14-boot3-shutdown-rung4e.log (4,973 lines, KELF min=0x0 max=0x3fe708 in it). Every boot is THIS image.
## ⚠⚠ HANDOFF — 2026-09-13, WRITTEN AGAINST A CREDIT LIMIT (Peter: "don't stop anything but prepare for hitting the weekly credit limit 2% to go")
**THE QUEUE IS THE HANDOFF. NOTHING BELOW NEEDS THIS SESSION'S MEMORY.** No new executors were started after this warning; the nine live ones were left running, and any that finish after the seat stops will have their work sitting on their branch tips below, ungathered but committed.
**THE PUSH LINE — PETER PUSHES, THE SEAT NEVER DOES. Nothing below is on a remote.**
```
git push origin hw-jetson main exec-orin-loginboot exec-orin-orintime exec-orin-dragmetal exec-orin-netverb exec-orin-lun2 exec-orin-ptrlost exec-orin-rastwin exec-orin-blitstall exec-orin-scrshot exec-orin-wirehyg exec-orin-ga10b6 exec-orin-sessgate exec-orin-fonts2x exec-orin-gatehyg
```
**BRANCHES WITH UNLANDED WORK — every one is COMMITTED and GATED unless marked otherwise. hw-jetson is `b434626a`.**
| branch | tip | ahead | state |
|---|---|---|---|
| exec-orin-loginboot | f14f6505 | 1 | **DONE, gated.** SO43 fixed (login opens at boot); SO44 derived complete and handed to SESSGATE |
| exec-orin-orintime | ad549988 | 1 | **DONE, gated.** SNTP client (A69) — BUILT BUT INERT, needs the one-line seam below |
| exec-orin-dragmetal | 75c32d5e | 1 | **DONE, gated.** SO45 derivation — the serial ring ACQUITTED by arithmetic; SERWIRE instrument |
| exec-orin-netverb | 23bddcf8 | 3 | **DONE, gated.** SO47 closed — arp cache + dns registered |
| exec-orin-lun2 | bf45f7cb | 1 | **DONE, gated.** SO49 + the garbled label (A72/A73) |
| exec-orin-ptrlost | 26646c22 | 2 | **DONE, gated.** SO46 — the pointer over furniture (A74/A75) + its evidence |
| exec-orin-blitstall | 4a4a55b1 | 1 | **DONE, gated.** SO46 in LEDGER.md (shared) — the BLITWIRE instrument; see below |
| exec-orin-rastwin | **28bcd764** | 3 | **DONE, gated.** 3D in a window (A66) + the A67 probe repair; A68 REWRITTEN as a flake record |
| exec-orin-scrshot | c8d66657 | 1 | live — R54, refuse rather than fall back |
| exec-orin-wirehyg | 9e5bbb85 | 4 | live — retired/renamed wire strings and their readers |
| exec-orin-ga10b6 | f7ace14c | 1 | live — the next GPU boot (safety-scheduler triple) + the scorer miscount |
| exec-orin-sessgate | (at b/1b423dcf) | 0 | live, NOTHING COMMITTED YET — SO36+SO37+SO44, **this gates the bench under R55** |
| exec-orin-fonts2x | f164b6fd | 0 | live, nothing committed yet — SO48 |
| exec-orin-gatehyg | f164b6fd | 0 | live, nothing committed yet — the gates that pass without proving |
**OWED AT FOLD TIME, in order, each one line or small:**
  1. **THE SNTP SEAM.** ORINTIME's client is inert until this lands, and NETVERB (which owned the file) is now DONE, so it is unblocked: on `net_phy::net6::init()`'s existing return line, CODE BEFORE THE COMMENT — `#[cfg(feature = "sntp6")] crate::net_sntp_client::service_tick();`
  2. **`unaos/arroyo:2054`** — the net6 knob doc block is stale (missing `via=`/`age_ms=` and the `(server …)` suffix). WIREHYG has the replacement text and was forbidden to edit the file because three executors were in it.
  3. **`main.rs:7944`** (`tegra_desk_arm`, `tegradesk`) — the FOURTH `screen_open_once` site, same pre-fix `if activated` defect, latent only because both its stop-line consts are `false`. Same one-line change LOGINBOOT made at 9129.
  4. **`wm.rs:5626`** — add a second `hit |= reserved_hit;` immediately after the MENU-UNDER block, so a furniture overlap takes CURSOR-3's whole-sprite bracket instead of declining. PTRLOST's prescription; not required by its fix.
  5. **LEDGER ID COLLISION ALREADY LIVE:** BLITSTALL took **SO46 in `docs/dev/LEDGER.md`** (shared ledger) while PTRLOST's SO46 is the ORIN row — different files, NOT a collision, but a reader will trip. BLITSTALL deliberately avoided SO45 because DRAGMETAL's unmerged commit adds one. Resolve the numbering when both fold.
**THE R55 GATE IS STILL CLOSED.** No card flies until the multi-user elements land. SESSGATE is the one outstanding piece with an executor on it; PWHARD (key stretching) is unstarted and its scope is **Peter's to confirm** — the seat's reading of "all the multi-user elements" is in R55 and is explicitly a reading, not a ruling.
**BENCH:** board dark after the 5a flight. Butler holds /dev/ttyACM0. Waker armed on the 5a anchors — **RE-ANCHOR IT before the next boot or it fires on bytes already past** (the conf's own warning; `OWNER=orin-0912b`). Staged and unflown: `render15-5a-…` already flew; nothing else is staged.

## EXECUTOR ROSTER AND WAITING QUEUE — R56 cap = 9 concurrent, seat starts the next on every opening
LIVE (8): FONTS2X (SO48) · GATEHYG · SCRSHOT (R54) · SESSGATE (gates the bench) · BLITSTALL (SO45 continued) · GA10B6 (the next GPU boot) · WIREHYG · PTRLOST (2nd pass: id renumber A74/A75 + cut its evidence into git).
WAITING, IN ORDER — the seat starts the top one the moment a slot frees:
  1. PWHARD — ⚠ BLOCKED ON A FILE, not on a decision: `fs/users.rs` is SESSGATE's until it folds. Start it the moment SESSGATE reports, IF Peter's "all the multi-user elements" includes hardening. — key stretching on the credential (one SHA-256 per guess is the whole cost today, and the users.rs header names stretching + a hardware-backed secret as later arcs). Needed only if Peter's "all the multi-user elements" includes hardening; his word decides.
  2. The `main.rs:7944` fourth site (below) if SESSGATE does not absorb it.
  3. **THE SNTP SEAM STATEMENT** — one line, owed to ORINTIME's arc the moment NETVERB folds; see the ORINTIME entry.
DONE THIS SESSION: LOGINBOOT (SO43 fixed, SO44 derived) · RASTWIN pass 1 (3D in a window) · ORINTIME (the SNTP client — BUILT BUT INERT) · DRAGMETAL (SO45 derivation — the ring is ACQUITTED by arithmetic) · NETVERB pass 2 (SO47 CLOSED, dns registered) · LUN2 (SO49 + the garbled label — BOTH ROOT-CAUSED, neither was what the row said).

✓✓ **A67 REPAIRED, AND THE EXECUTOR WITHDREW A FALSE CAUSAL CLAIM BEFORE SHIPPING IT — RASTWIN (`exec-orin-rastwin` 28bcd764, parent 3d41de0d = the merge of 71dcb63a and f164b6fd).** Every `display_tegra.rs` hunk inside the `[orinrast]` block (4717-5053); `wm.rs` UNTOUCHED; `main.rs` one line changed for one, line-neutral, rast-ON tegra arm only. Gates 1-6 rc=0 incl. `cargo test -p rast`; `knoboff rast 3d41de0d` exit 0, both arches byte-identical, control fired.
**THE REPAIR: GEOMETRY IS ASKED FOR, NOT RESTATED.** `RG_DEMO_W/H` deleted; `rastwin_seat()` publishes what `wm::info` actually seated **including the `scale` multiply** — on 1920x1200 a 320x240 surface composites at 640x480 while `info.w/h` still read 320x240, so **a probe using those numbers would have read a QUARTER of the window**. Three populations now: MARGIN backdrop, MIDDLE non-backdrop, and **OUTSIDE zero backdrop — the inverted test A67 specified**. `RAST-NO-WINDOW` and `RAST-LEAKED-OUTSIDE` are both outside the passing set, and the leak is ordered ABOVE the row's own arms. The `post` sample moved into `run` on the last composited frame — at the terminus it would read the desktop and say `NO-RAST-INK` on every healthy boot — and latches one-shot. The window is now PARKED, not closed: it stays on the desktop, closable by the ordinary route.
⚠ **GATES 1-2 WENT RED FIRST, AND THE GATE WAS RIGHT: `GATE-LEDGER: A68 cites evidence outside git (unaos-bench/scratch)` — R32, correctly enforced.** Citation moved into `docs/dev/FIXTURE_FLAKES.md`; both re-ran rc=0. That is the same class as LUN2's and PTRLOST's evidence problems and the third time today the ledger gate caught a citation that could not be checked by a later reader.
⚠⚠ **IT NEARLY SHIPPED A FALSE CAUSAL CLAIM AND WITHDREW IT.** Parking was FIRST adopted because `dmgovlp` failed with the close and passed without it. A WIDER CENSUS KILLED THAT: `dmgovlp` produces BOTH outcomes with `rast` **OFF**, where its module is not linked and the image is byte-identical to baseline. Six-run table in `FIXTURE_FLAKES.md`. **So its own earlier A68 was also wrong — it had asserted a settled pre-existing red off a SINGLE SAMPLE.** A68 is rewritten as a flake record plus the still-valid gate finding (the 20 s wall decides whether fixtures are reached at all). **Parking now stands on design merit alone, with no causal claim attached.** This is the fourth `[dmgovlp]` flake sighting today and the second time an executor has corrected its own committed finding.

✓✓ **THE "SLOW BLIT" MAY BE THE INSTRUMENT ITSELF — BLITSTALL (`exec-orin-blitstall` 4a4a55b1, parent 4cfb6e99; took SO46 in the SHARED LEDGER.md, deliberately not SO45 because DRAGMETAL's unmerged commit adds one).** THIRD OVERTURNING OF THE DAY, and it lands on the number I handed it as a lead.
**`blit_us` IS NOT A BLIT RATE.** `comp2_emit` prints it as `loop_cyc - cache_cyc`, and `C2_LOOP_CYC` (wm.rs:6413) brackets the **WHOLE PER-WINDOW LOOP**. FIVE WITNESS-ONLY SITES RUN INSIDE THAT BRACKET — `wcg::end` (:6362, a SCAN-OUT READ-BACK), `stage_flush` (:6378), `band_flush` (:6382), `chromeband_fixture` (:6386), `verify_window` (:6398) — and only the last has a field of its own (`wcd_us`). So "the blit runs at a third of its own speed" was never a statement about the copy.
**THE PARTITION OVER render14 BOOT 1'S 38 ROLLUPS IS PERFECT:** the four rollups carrying a WC-G sample or `wcd_us>0` are the boot's FOUR SLOWEST (32.2 / 40.2 / 51.8 / 71.7 B/us); the 32 witness-free ones sit at 68.7–163.0, mean 109. **AND IT REPRODUCES ACROSS BOOTS TO THREE FIGURES:** boot 3 r1 reads `blit_us=94910 bytes_pp=3053700 wcd_us=41937` against boot 1's `94713 / 3053700 / 41944`. Contention and a mapping fault do not repeat like that.
**THE ARITHMETIC ON THE STALL ROLLUP:** bracket total `4 x 94713 = 378,852 us`, and `max_us=361130` is **95.3% of it**. Measured INSIDE it: `[wc-g] prof` four-phase sums = 55,036 us, `wcd_us` = 41,944 us — **96,980 us = 25.6%, none of it a pixel** — and that is a LOWER bound (it excludes 1,686 B of in-loop UART).
⚠ **AND IT REFUSED TO CALL IT SOLVED, which is why this is an instrument and not a fix.** Subtracting the MEASURED terms leaves 43 B/us (still 2.5x under healthy); subtracting the MODELLED full bracket leaves 90 B/us (inside the healthy band). Nothing charges the print sites, so THE WIRE CANNOT SEPARATE THEM — the mapping-attribute, alignment and memory-port candidates all live past that gap. The next boot decides: **`-> WITNESS-BOUND`** (expected `wit_pct≈64`, `net_bus≈90`) means the excess IS the instrument, SO45 closes, and there is no slow blit — only a wide bracket; **`-> COPY-BOUND`** with a small `wit_pct` means the copy really is slow and the hardware candidates survive WITH A MEASURED BUDGET to explain; `wit_us` near `loop_us` with `wit_n` near zero convicts the bracket itself.
Gates 1-5b rc=0, fixture PRESENT in the capture (`:: BLITWIRE: loop_us=378852 bytes=12214800 raw_bus=32 net_us_meas=281872 net_bus_meas=43 net_us_full=135519 net_bus_full=90 floor=64 -> PASS ::`), go-red by mutation quoted, DRAINCAP/SERWIT-1/SERWIT-1B/S5DRAIN/WCDLATCH all unmoved; knoboff wc exit 0 against BOTH f164b6fd and the true first parent 4cfb6e99. ⚠ It merged and found hw-jetson ALREADY UP TO DATE — **DRAGMETAL's 75c32d5e is still only on its own branch, so SERWIRE is NOT in this tree**; it touched no serial file and did not reopen the ring question. UART cost: ONE latched line, bounded 345 B, 166 B on render14's numbers; with SERWIRE merged the pair costs ≤669 B.

✓✓ **SO46 ROOT-CAUSED — THE POINTER IS LOST OVER THE FURNITURE, A THIRD REGION WITH NO PAINTER. PTRLOST (`exec-orin-ptrlost` f58eeed3; RENUMBERED to A74/A75 at 26646c22 after colliding with LUN2, which committed and reported first; done as a FOLLOW-UP COMMIT, not an amend, because LAWS §3 bans rewriting history — `git diff f58eeed3 HEAD -- unaos/` is EMPTY, so the gated tree is untouched and nothing needed re-gating).**
**`nohit` AT ITS DEFINITION (wm.rs:12490, `CUR12_NOHIT`):** *"the sprite was on the panel, but no live window above the shell met its box"*, bumped at :5719-5721 on `!hit`, where `hit` is built at :5572-5584 from WINDOW TABLE ROWS ALONE. The rollup's own prose — *"the operator simply was not pointing at a window. Not a defect"* — is TRUE over the backdrop and **FALSE over furniture**.
**THE ROW SET IS NOT WHAT CHANGED** (every `[wc-fv]` that boot reads `shell_z=0`, so `above_shell` excluded nothing, and `[wcn] rollup … wins=7` throughout). **WHAT CHANGED IS WHERE THE HAND HAS TO BE: under R49 a pinned app launches from its DOCK TILE.** Three regions, and one has no painter: over the BACKDROP the desktop present does not subtract the sprite and the back-buffer 18x18 arrow shows; over a WINDOW it subtracts and the compositor's 9x9 shows; over FURNITURE it SUBTRACTS (screen.rs:1617 strips, :1632 dropdown) and **neither paints**. The furniture arm sets `reserved_hit` at wm.rs:5692/:5700 — SEVENTY LINES AFTER the only OR-fold into `hit` at :5626, itself inside a `witness+baremetal` cfg ABSENT from an Orin build. `pal.rs`'s CURSOR-X86 note had written the window half down and shipped the fix FOR x86 ONLY; SHELLDESK later widened the SUBTRACTION onto aarch64 `desktop_firmware` without widening the OWNERSHIP. **NOT SO27** — `shell-remint` is 0 across the wire and APPPIN's own `nohit=0 :: PASS ::` still passes on this executor's gate-3 capture.
THE WIRE: four straight `[cursor12]` rollups at `nohit=3143/3158`, `7465/8032`, `6889/7717`, `906/1379`, ALL `planned=0`, while `[cursor] restore rect=9x9+765+1156 / +1036+1167 / +1195+1154` sit INSIDE `[strip] … tenant=dock rect=768x52+576+1136` — the lost pointer is over the dock — and `[flick2] down_max` climbs 1ms → 374 → 634 → **1883ms**.
FIX: `pal.rs` LINE-NEUTRAL, one line changed and none added — `SPRITE_OWNS_PAINT = cfg!(target_arch = "x86_64") || cfg!(feature = "desktop_firmware")`, so `repaint_on_move` repaints the front-buffer arrow on every pointer report EVERYWHERE; the virt/UEFI-console carve-out kept. Held by a **COMPILE-TIME** `const _: () = assert!(PTR_OWNER_INVARIANT, …)` — a build that subtracts the strips without owning the paint now FAILS TO COMPILE (go-red quoted: `error[E0080]` at screen.rs:2360, rc=101). Witness `[ptrown]`, edge-latched and capped at 12 (SO30), separating `row=no` from `row=yes composited=0` — "has a row, not composited" — plus a one-shot `:: PTRLOST: … :: PASS ::`. knoboff wc exit 0, both arches byte-identical, control fired.
⚠ **FOURTH CERTIFICATION-LEG DEFECT OF MINE TODAY.** My gate 5 (`UNAOS_TEGRA=1 ./arroyo esp-jetson`) builds `ehcihid,kbdwit,sdhcblk,smolnet,tegra,bsptick,bsprun,tegrasmp,apsrun,sdwrite` — NO `desktop_firmware`, NO `witness` — so the family is CORRECTLY absent and the leg certifies nothing. The executor added 5b with the render14 recipe and certified there: `[ptrown] at=(` 1, `:: PTRLOST: window=` 1, `lost_reachable=` 1, controls present `[cursor12] offer scope=` 1 / `[sprite] size=` 1, controls absent 0. Gate 4 (arm virt, no desktop_firmware) compiles the no-op twin and is correctly silent. THAT IS FOUR: arroyo's verb spelling, NET6, LOGINBOOT, this.
⚠ **HONEST LIMIT, stated by the executor and worth preserving:** the `nohit`-latched block co-located with the dock strip is MEASURED; that the arrow was INVISIBLE at those coordinates is Peter's report plus the two-sprite/subtraction mechanism, NOT a pixel capture. `-> furniture` on the next boot, with a pointer Peter can see, is what converts it.
⚠ REPORTED, NOT FIXED (wm.rs is another executor's): the `hit |= reserved_hit` fold at wm.rs:5626 runs BEFORE the dock/dropdown assignments at :5692/:5700. Exact prescription for whoever owns it next — add a second `hit |= reserved_hit;` immediately after the MENU-UNDER block, so a furniture overlap takes CURSOR-3's whole-sprite bracket instead of declining outright and `[cursor12] reserved=` becomes honest on the Orin. NOT required by this fix: the compositor sprite now paints there unconditionally.
⚠ PROCESS NOTE WORTH KEEPING: its first gate chain's `setsid` put the chain in its own PGID rather than the shell's `$!`, so its kill missed and two chains briefly raced in the worktree. It killed BOTH BY PGID — no `pkill -f`, no other executor touched — wiped the logs, and re-ran all five gates clean under an `flock`. Every rc it reported is from that single clean chain.

✓✓ **SO49 ROOT-CAUSED — AND IT WAS NEVER ABOUT THE LUN. LUN2 (`exec-orin-lun2` bf45f7cb, parent f164b6fd, ledger A72/A73).**
**`TransferError(13)` IS NOT xHCI COMPLETION CODE 13.** Code 13 is SHORT PACKET, which this driver treats as SUCCESS (mod.rs:8787, :8856). The `13` is the driver's OWN SYNTHETIC LABEL raised at **mod.rs:8931, the residue-disagree site** — and the transaction's real completion code WAS 13, which is exactly why `data_stalled` stayed false and the cross-check ran at all. **THE CSW REFUTES THE VERDICT:** `55 53 42 53 07 00 00 00 00 00 00 00 01` — `USBS`, tag 7 matched, **residue 0, bCSWStatus 01 (Failed)**. The device DECLINED READ CAPACITY(10), and reports residue 0 on a declined command rather than 8. The cross-check called that a phase fault. **So the shape is: LUN 0 holds a card, PASSES, and the check is silent; LUN 1 is the unit that must DECLINE — and the same check that agrees on every success disagrees on the honest failure.** Ruled out ON THE WIRE, not by reasoning: the CBW LUN field is correct (INQUIRY on LUN 1 returned the device's real strings), no uncleared stall (cc=13 is not a stall, and a stall sets `data_stalled` which SKIPS this check), no shared toggle, no Reset Recovery owed. The four-slot reader scores its empty slots `NO-MEDIUM` through this IDENTICAL code because it reports the conforming residue and this one reports 0. FIX: gate the FAULT on `bstatus == 0` — the check's own comment states its defect as `bStatus=0` — so the false positive goes and everything it was built for stays; the disagreement is still printed as `verdict=advisory-on-failed-csw`. USBREG's registry untouched.
**THE GARBLED LABEL WAS NEVER A LABEL, AND IT IS A58 WEARING A DIFFERENT HAT.** `label_raw=0002000000010006000000` is, byte for byte, the FAT32 BPB at offset 0x2B: BPB_FSVer's high byte, `BPB_RootClus`=2, `BPB_FSInfo`=1, `BPB_BkBootSec`=6, two reserved. **`BS_VolLab` on FAT32 lives at 0x47.** The mount line refutes itself twice: `FAT16 … reserved=32 … clusters=32440 rootdir@LBA2592 (0sec)` — `reserved=32` is the FAT32 reserved size, and `(0sec)` means `BPB_RootEntCnt` is ZERO, while FAT16 ALWAYS has a fixed root directory and a non-zero `BPB_FATSz16`. MBR type 0x0c. A small FAT32 volume (32,440 clusters, under the spec's 65,525) that the CLUSTER-COUNT RULE APPLIED ALONE called FAT16 — **which is precisely what A58 recorded as "fs/fat.rs MISREADS such a volume as FAT16, it does not refuse"** — so `BS_VolLab`, `BS_VolID`, `root_cluster` and the 2-vs-4-byte FAT entries were ALL read wrong. FIX: `fat::fat32_by_shape`, the structural signature Linux's `fs/fat` keys on, mid-file and line-neutral BY CONSTRUCTION (`@@ -1501,6 +1501,6 @@`); plus the refusal asked for — `sanitize_label` answers `Untitled` when not one byte survives the whitelist, keeping `altered` true so `label_raw=` still names the reason. A literal `__` label is KEPT: the refusal is about substitution, not the character.
⚠⚠ **A DEFECT IN MY OWN BRIEF, and the executor caught it before it cost anything.** I cited `docs/dev/evidence/orin28/render14-boot1-desktop-menubar.log` as the evidence. **IT DOES NOT CONTAIN THE QUOTED WIRE.** That log carries a DIFFERENT READER — `max_lun=3`, four LUNs, `present=1`, and its three empty slots scored CORRECTLY as `NO-MEDIUM reason=cap-sense:02/3a/00`. Verified by the seat: zero `cap-bot:TransferError` in it, and the same in orin28/boot3 and orin27/render13-boot2, the only three USBLUN files in git. The real measurement lived ONLY in the live bench capture, on the two-LUN `Generic- USB3.0 CRW -SD`. **AND THE EXECUTOR FLAGGED A TRAP BESIDE IT:** a near-identical string exists in `~/unaos-bench/scratch/orin27/usblun-logs/m2-gored-*-serial.log`, which is the A56 M2 GO-RED MUTATION on a QEMU stick — a reader who grabbed that would chase a control probe. **FIXED BY THE SEAT:** `docs/dev/evidence/orin28/render14-usblun-two-lun-reader.log` (1,053 lines, cut from the live capture and ANCHORED on its boot — `KELF min=0x0 max=0x3fe708`, the render14 identity; ledger-check REFUSED the first cut for carrying no anchor, and was right) now carries BOTH findings in git — `cap-bot:TransferError(13)` 1, `label_raw=0002000000010006` 1, `FAT16` 1, `max_lun=1` 2 — so A72/A73 are citable. **A BRIEF MUST CITE EVIDENCE THAT CONTAINS WHAT IT QUOTES; mine named a file I had not checked.**
⚠ **knoboff = 1 AND THE JUSTIFICATION IS THE RIGHT ONE:** control armed on both arches, x86 1529300→1529744 and arm 1595624→1597672, and the tool itself says "the SIZE changed, so this is code, not a line shift". All three fixes are unconditional and DELIBERATELY un-knobbed — *"a knob to keep turning an honest CHECK CONDITION into a transport error would be a knob to leave the bug in place"*, which is A56's own reasoning.
⚠ **AND A FINDING THAT INDICTS SEVERAL OF MY BRIEFS: `selftest.rs` WAS IN ITS FILE LIST AND IT DELIBERATELY DID NOT USE IT.** That file's `run()` is the INTERACTIVE `tste` suite, which a headless QEMU boot NEVER REACHES — **a leg added there would ship unexecuted and gate nothing.** It put the fixture on `homesoil_selftest` leg 9 riding `unafsroot_selftest`, the boot-path entry point `test`/`test-arm` actually execute, which is why it appears in both captures. I have been listing `selftest.rs` in brief after brief. Forwarded to GATEHYG as a fourth instance of "a gate that passes without proving anything".
Evidence: `docs/dev/evidence/orin28/render14-usblun-two-lun-reader.log`. Gates 1-5 rc=0, `test 90` used and the fixture VERIFIED PRESENT in both captures (`:: SO49: … :: PASS ::`), certification done on the WITNESS builds because `so49_selftest` is `#[cfg(feature = "witness")]` and esp-jetson is witness-free (so `SO49` = 0 there is CORRECT). Three go-red mutations, one per code change, each rc=1 — the label one reproduces the bench defect inside the fixture.

✓✓ **SO45's MECHANISM IS OVERTURNED — THE SERIAL RING IS ACQUITTED BY ARITHMETIC. DRAGMETAL (`exec-orin-dragmetal` 75c32d5e).** The brief asked which of two things was true; the answer was the one that inverts the question. **THE CAP IS ON THE PATH THE METAL TAKES** — every hop walked, no `cfg` a tegra flight build turns off: wm.rs:14451 `comp2_emit` → wm.rs:13462 `serial_println!` → arch/aarch64/serial.rs:284 → :177 `_print` → :203 `without_interrupts` → :224 `SERIAL_PORT.try_lock()` → :238 `serial_ring::drain_capped` → serial_ring.rs:251 `DRAIN_BYTE_BUDGET = 192`, UNGATED on both arches in every image. And `DRAINCAP_PAD` is a DIFFERENT OBJECT that has never executed on this board (witness-gated fixture padding, reached only from `mirror_service` — SO41); re-measured on the flight ELF, `[draincap] fill` = 0 with control `ring drained` = 1.
**THE ARITHMETIC.** 115200 8N1 = 86.805 us/B; `drain_into` tests strictly `<` before taking a line, so one drain pays at most `192 - 1 + widest line`: **22.5 ms** at SO31's 68 B line (SERDRAIN's prediction, confirmed), **29.0 ms** at render14's 143.1 B mean, **115.1 ms** at render14's widest 1135 B line — the worst single drain physically possible. Metal's `max_us = 361130` is 4,161 bytes = **21.7 budgets = 1.05 WHOLE RINGS. ONE CAPPED DRAIN CANNOT PRODUCE IT.** So SO29's MECHANISM is what metal disagrees with, not SERDRAIN's fix. Supporting, neither conclusive alone: render14 boot1 has ZERO `[serial] dropped` where render13 boot1 had 5,331; and render14 boot3 printed `[pwrshutoff] ring drained lines=1 bytes=163` — the whole ring held ONE LINE — on the boot reading `max_us=367166`.
**THE INSTRUMENT (SERWIRE), and its design idea is worth keeping:** a SPAN TOTAL UPPER-BOUNDS ANY SINGLE PASS INSIDE IT, so `drain_us < max_us` is arithmetic proof the pass did not spend its time in the drain — no per-pass bracket needed, which is what let it fit the one `wm.rs` site allowed. Uncapped spellings deliberately not charged; the fixture asserts both directions. ONE LINE, at most once per boot, bound 324 B and 163 B on render14's own numbers, latched above 100 ms; `[comp2]` not widened by a single byte (a per-rollup field would be SO30 one layer up). Gates 1-5b all rc=0 with DRAINCAP/SERWIT-1/SERWIT-1B/PWRDRAIN/S5DRAIN all still PASS and `-> FAIL` count 0 across both logs; knoboff wc exit 0 both arches; go-red quoted.
**→ THE NEW LEAD, AND IT IS STRONG.** On the very rollup carrying `max_us=361130`, `blit_us` is **94,713 us against a 119,516 us mean pass — 79% of the pass** — moving 3,053,700 B at **32 B/us against 109 B/us on the same boot's healthy rollups**. The same blit, same board, same boot, at a third of its own speed. Executor **BLITSTALL** is on it.
✓✓ **SO47 IS CLOSED — NETVERB pass 2 (`exec-orin-netverb` 23bddcf8, merged from hw-jetson 09ddb1c4).** The `dns` verb is registered and reachable. **THE `Avail` CALL WAS SETTLED BY A TEST, NOT BY TASTE** — and the executor found the file contradicting itself: `Avail::Aarch64` is empty BY TEST (`lib.rs:969-976` asserts no HOST_VERBS member matches it), not by convention, so the seat's suggestion FAILED 2 of 16 tests when tried. It recorded that contradiction AT the variant so nobody re-litigates it. `Always` is right for three independent reasons, the sharpest being that `Aarch64` is not even narrower (the arm is `net6 AND aarch64`) and on x86 it would make `dns` a TYPO rather than an absence — `resolve_exec` finds no `DNS.ELF` and the shell answers with the very 44-byte TerminalError this arc exists to delete. R26 clause 3 ruled exactly this shape before. `cargo test -p midden_core` 16/16.
⚠⚠ **AND IT CORRECTED ITS OWN PREVIOUS REPORT, which is the most valuable thing in it.** It had told us `-> NO REPLY ::` was RETIRED. **FALSE.** It left `.rodata` only because ARP's old format string was contiguous — **PING STILL EMITS THE EXACT BYTES** (`net_phy.rs:1365`, `" -> {} ::"` against `if received > 0 {"REPLY"} else {"NO REPLY"}`). Measured with a temporary fixture ping at an address slirp does not host, then reverted: `:: NET6: ping 10.0.2.99 0/1 replies over virtio-net peer ----------------- -> NO REPLY ::`. **THE HAZARD IS WORSE THAN "GONE" AND CUTS BOTH WAYS: a WIRE scorer still fires but now only for ping — one built to catch a failing ARP goes quietly green on exactly that failure — while an ARTIFACT scorer reads 0 and would RED a healthy build.** Score `arp`'s failure on `NO REPLY (cache miss,` — a token no other verb reaches. `SEND FAILED (socket unusable)` is genuinely gone from both channels. THE SWEEP: no executable scorer is keyed on either string (zero hits in any `.spec`, `mbench.py`, `foreman`, or bench tools; the one apparent hit at `pi4-regression.spec:1868` is the word *mis-**at**tributes*). Stale PROSE owed, outside its list: `docs/dev/OS/orin-ledger.md:90` (A59's go-red names both superseded shapes) and `unaos/arroyo:2054` (the net6 knob doc block).
⚠ knoboff MOVES (+128 x86, +64 arm) and the direction was justified BY ISOLATION rather than argument: two runs against two baselines showed the whole NET6 arc contributes ZERO and the entire move is one `(&str, Avail)` row plus a 3-byte string plus `.rodata` alignment. The registration is deliberately NOT net6-gated — ONE OS.
⚠ OWED in `shell.rs:6103-6122`, reported not made: its comment calls the `other =>` drift net "unreachable by construction… that set is empty today". This registration makes that FALSE.

✓ **THE JETSON HAS AN SNTP CLIENT — ORINTIME (`exec-orin-orintime` ad549988, parent b8f5ded9, ledger A69).** `net_sntp_client.rs` (466 lines, NEW), arch-neutral, sitting on the NET6 socket surface and using ONLY the shared parser and the shared clock — no second parser, no second civil renderer. Server selection is lease-resolver → `net6::dns(pool.ntp.org)` → gateway, `MAX_ATTEMPTS=3`, each attempt witnessed with the source that named it. Line-neutrality MEASURED not argued (lib.rs 2/2, selftest.rs 1/1, menubar.rs two 1-for-1 in-body replacements + 80 lines at the FILE TAIL); `clock.rs` and `net_sntp.rs` needed nothing and were not touched; net_phy/net_dns/smolnet READ, never edited. Artifact certified on the BUILT ELF (3,585,624 B): `[sntp6] attempt` 2, `sync COMPLETE anchored=yes source=` 1, `anchored=no attempts=` 1, `lease-resolver` 1, `dns-pool` 1, `no reply within budget` 1, `:: BARCLOCK: clock=set rect=` 1, `:: BARCLOCK: clock=unsynced rect=none glyphs=0` 1, **`:: MENUBAR:` 0 — the aarch64 invariant menubar.rs:400/:938 asserts is PRESERVED**, controls 0. knoboff net6 EXIT 0 both arches byte-identical (its first run exited **2** — `NO VERDICT — untracked source files under unaos/: net_sntp_client.rs` — quoted because a 2 is never a pass). Gate 1's first red was `❌ k8-reach UNREGISTERED: UNAOS_SNTP6` and was fixed with a REGISTRY ROW, not by silencing.
⚠ **BUILT BUT INERT — NOTHING PERIODIC CALLS IT, and the executor said so plainly rather than hiding it.** `service_tick()` names no driver, is latched, is guarded on `net6::ipv4()`/`gateway()` and is capped — the R54-adjacent design requirement (do not recreate the per-driver coupling this arc exists to remove) is MET. But every candidate driving seam was in a file it was forbidden to touch: `net_phy::net6::init()` :1010 (NETVERB's), both `net6_start` sites (the NIC drivers — the very coupling being removed), and `main.rs` (outside its brief, hard `panic::Location` constraint). It REJECTED the compositor on the merits (`recvfrom` pumps up to 400k iterations; clock.rs:322 forbids spinning there). **THE ONE STATEMENT THE SEAT FOLDS AFTER NETVERB LANDS**, on `net6::init()`'s existing return line, code BEFORE the comment: `#[cfg(feature = "sntp6")] crate::net_sntp_client::service_tick();` — in the waiting queue above. Until then the Orin's clock is reached by typing `tste` at the console.
⚠ **A CORRECTION TO THE SEAT'S FRAMING, and it is the right kind.** I told the executor the bar's clock witness was blind "on the Orin". Wrong scope: the WHOLE `:: MENUBAR:` census is x86-only, so it is blind on every aarch64 board. That is why a new `:: BARCLOCK:` family was the correct answer rather than extending the old one.
⚠ **THIRD `[dmgovlp]` FLAKE SIGHTING** — `FIXTURE_FLAKES.md` §1c asked the next sighting to record the fingerprint; red `drag_evt=0 drag_px=0 relay=0 narrow=0/12 adopt=0 max_ms=0` vs green `drag_evt=5 drag_px=38590 relay=3 narrow=3/12 adopt=25 max_ms=4`, at host load 6.35/8.89/9.14 with five executors building. `max_ms` is the outlier §1c named. That file was outside the executor's list — OWED to it, and it is the same load-dependence GATEHYG is chartered on.
✓ **THE CONSOLIDATION ARC IS NOW COSTED (the trunk-queue row Peter asked about).** The ONLY arch-specific code inside `mod net6` is `now_ms()`'s two `mrs cntpct_el0/cntfrq_el0` at net_phy.rs:892-901 — and `clock.rs` ALREADY abstracts exactly that for both arches (`monotonic()` :44/:57), so the fix is one call into `clock`, not a second asm arm. `smolnet.rs:81` already implements the same `RawNic` seam for the e1000, so an e1000 `NicOps` is ~20 lines. The real work is the LEASE seam: x86 keeps its own `CURRENT_GW`/`CURRENT_DNS` (smolnet.rs:523/:538) and its own `dhcp_acquire` (:559) instead of publishing through `net_phy::settled_config()`. Retirable afterwards: smolnet.rs:1604-1795 + e1000.rs:1219, and genet.rs:2999/:4057-4090/:5196. **THE BLOCKER WORTH NAMING: `smolnet` is the x86 DEFAULT stack, so this moves the shipped x86 image's `panic::Location`s — an rmbp-coordinated arc, NOT a jetson one.** Peter rules.

✓✓ **SO43 IS FIXED — LOGINBOOT (`exec-orin-loginboot` f14f6505, parent b8f5ded9).** The brief demanded the seam be identified before anything was changed, and the answer was not the one the row assumed. `screen_open_once` has **FOUR** sites, not three: 5342, 6380, 7944, 9129. **main.rs:6380 is not merely not-taken, it is NOT COMPILED on tegra** — it is inside `x86_render_service`, `cfg(target_arch = "x86_64")`. **Its aarch64 twin at 5342 is also absent**: inside `render_service`, `cfg(all(target_arch = "aarch64", feature = "baremetal"))`, and that is UNSATISFIABLE on a jetson image because `baremetal = ["pi", "aarch64_el0"]` while `pi` + `tegra` is a hard `compile_error!` (arch/aarch64/serial.rs:23). PROVEN OFF THE WIRE WITHOUT READING A CFG: `LC_ALL=C grep -a -o -F 'shellwin-pi'` on both render14 captures = **0** — `render_service`'s own witness, absent, because that task does not exist on this board. So 9129 is the ONE live site, and my brief's instruction to "use the predicate 5342/6380 already use" was pointing at two seams that do not exist here.
FIX: one named seam, `fs::users::screen_open_at_ignition(desktop_up, console_routed)` — takes both facts, PRINTS both, decides on the first. 9129 hands it `menubar::enabled()`, the readback of activate's one unconditional step and the same value the cascade's `bar=` reports. Line-neutral (main.rs `1 1`), appended before the first `//` (P7). `./arroyo knoboff login b8f5ded9` EXIT 0, both arches byte-identical, control fired.
GO-RED ON THE SEAM THE ORIN ACTUALLY TAKES, certified on the BUILT ELF of the render14 card knob line + `UNAOS_LOGIN=1`: `[login] ignition desktop_up=` = **1 with the fix, 0 before**, while `[deskcascade] -> CASCADED` stays 1 and `Log in to UnaOS` stays 1 in BOTH. **The screen was compiled into the flight image all along; nothing on the board ignited it.**
NEXT BOOT MUST SHOW: `[login] ignition desktop_up=true console_routed=false -> OPEN`, then ONE `[login] screen open window=<id>` BEFORE the CASCADED line (not after a Log Out), and `windows=3` where render14 read `windows=2`. `activate=false` stays false and no longer decides anything. `heals=` must not appear on a real boot. On a card with no `USERS.DAT` the caption reads `Create the first user`.
⚠ **SO44 STOPPED AT THE RIGHT PLACE AND IS HANDED ON COMPLETE.** The seam is `video/strip.rs:780 press_route`, in LOGINBOOT's explicit STOP list, so it did not touch it — it derived the whole change instead: a new FIRST term `users::screen_press(x,y)` OR-chained ahead of winmenu/crystal/dock, and `screen_press` as the press twin of `screen_key` delegating to `login::press_swallow` returning `is_open()` — TRUE for every press while the screen is up, inside the rectangle and out, so no window arm runs. It explicitly REJECTED special-casing `owner_asid == 0` in `hit_test` (that hands the screen a close box back, LOGINCLOSE's measured defect) and states this SAME statement closes SO36's furniture half. Now in SESSGATE's brief. Until it lands, a click on the login row still raises the row beneath it.
⚠ **A FOURTH SITE, LATENT: main.rs:7944** (`tegra_desk_arm`, `tegradesk`) carries the identical pre-fix `if activated { screen_open_once(); }`, harmless only because both its stop-line consts are `false` in source. Same one-line change as 9129. Left untouched, reported.
⚠ **THIRD BRIEF DEFECT OF THE SEAT'S TODAY, same class as the other two.** My gate 3 (`UNAOS_QEMU_FULL=1 UNAOS_WC=1 ./arroyo test`) arms NEITHER login knob — banner `witness,ehcihid,kbdwit,sdhcblk,smolnet,wc,sdwrite` — and the fixtures additionally need a FAT volume (`users::service` returns when `drivers::block::info()` is None), so `grep -a -c -F 'login'` on that capture is **0**. The executor added 3b (`+UNAOS_LOGIN=1 UNAOS_LOGINST=1 UNAOS_FATIMG=sf`), which is the line that carries the legs. A GATE MUST NAME THE KNOBS THAT COMPILE WHAT IT TESTS — that is now three for three today (arroyo verb spelling, the NET6 certification leg, this), and it is why GATEHYG exists.

✓ **R54 — SCREENSHOTS DO NOT GET A FALLBACK, AND MULTI-USER'S REAL POSTURE IS WRITTEN DOWN.** Peter asked "is this real multi-user then or what's the security like", and on the answer ruled: *"do not hack screenshots to make it work right before multi-user is in."*
**THE POSTURE, established from the code and not from intent, because he asked.** REAL: credentials are a 16-byte per-user salt + SHA-256 of `salt || password`, CRC32 per row, a `ver` migration byte, no plaintext on disk or wire (`fs/users.rs` header); and login did NOT invent a parallel scheme — `user:<name>` is stamped into the SAME per-slot principal the loader already stamped and the SAME `SYS_OPEN` owner/grants check already enforced. NOT REAL YET, each already a row: **SO37** Log Out does not stop what the session started (the boundary is the launch, not the clock — a still-running program keeps its launch-time stamp and can still open that user's files while the next user shares the glass; fix shape is a session epoch on the slot principal checked in SYS_OPEN); **SO36** input is not session-gated (the `session_open()` gate belongs at `strip.rs press_route` ahead of every window arm — and this is the same defect as **SO44** on the glass, the login window being a window that happens to be on top rather than a modal barrier); **no key stretching** (one SHA-256 per guess, unthrottled, offline — the module says so itself, and the credential file must be readable with NO session open, so it sits at the root of the root volume where anyone holding the card reads every name, salt and hash); **nothing encrypted at rest** (ownership is a runtime boundary only); and **SO43**, which dominates all of it — the login screen does not open at boot on this board, so today the Orin runs with no session at all.
**THE RULING APPLIED, same hour.** The SCRSHOT brief had offered its executor a choice between a shared-location fallback and a refusal when nobody is logged in. THE SEAT WITHDREW THAT CHOICE: destination resolves from the real session principal; with no session the capture is REFUSED with a named reason and ZERO BYTES WRITTEN. No shared directory, no volume-root landing place, no "temporary" path, and no knob-off fallback left in the tree — a knob-off hack is still a hack in the tree. The REFUSE path is a first-class outcome with its own fixture and go-red, not an error case. Correct-and-currently-inert beats hurried-and-wrong: the code goes live the moment SO43 lands and nothing has to be unwound. The executor is also asked to say plainly whether the arc is worth landing now or should sit until LOGINBOOT folds.
**THE GENERAL SHAPE, worth keeping past this arc:** a feature that depends on a boundary which is not finished must not be made to LOOK finished by routing around the missing boundary.
✓ **3D IS IN A WINDOW — RASTWIN (`exec-orin-rastwin` 71dcb63a, parent b8f5ded9; ledger A66/A67/A68).** The software rasterizer no longer owns the panel: it renders into a real `wm` row, owner `0xffffff61`, `hit=OK` against the row's own TITLE BAR (the strip `drag_begin` accepts), surface 320x240 scaled into a `650x524` box. `panel_px_written_outside_row=0` — no `fill_screen`/`put_pixel`/`flush` remains on the path. Damage 11,308 of a possible 21,360 rows (52.9%), widest band 146/240. Gates 1-6 all rc=0 including `cargo test -p rast` (7 goldens intact); `./arroyo knoboff rast b8f5ded9` EXIT 0 with BOTH arches byte-identical — the standing cross-arch proof of this track holds. Artifact certified on the BUILT ELF: `[rastwin]` 7, `RASTWIN: 3D-in-a-window` 1, and the control `software rasterizer, panel present` = 0, which proves REPLACEMENT rather than addition. Go-red by mutation (`RW_OWNER = 0` -> `hit=BAD -> FAIL`, rc=1, with `geom=OK dmg=OK` unchanged, so the leg isolates exactly the property it claims). `wm.rs`, `main.rs`, `display_tegra.rs`, `arroyo` and `selftest.rs` all UNTOUCHED — the `&mut Screen` signatures were kept precisely so the line-neutral call sites at main.rs:1613/7141 would not move.
⚠ **THE SEAT'S BRIEF WAS WRONG ABOUT `run_mc`, and the executor measured it rather than believing me.** My brief said `run_mc` "was built for the Orin and only ever reached x86's APs". FALSE. Every RAST-MC `#[cfg]` reads `any(all(feature="tegra", target_arch="aarch64"), all(feature="rastmc", target_arch="x86_64"))` — aarch64 FIRST and original — and `run_mc` is already CALLED on the Orin at main.rs:7141. `arroyo:311` describes RASTPORT ADDING x86, not RAST-MC arriving on aarch64; I read the comment backwards. What is actually missing is the MEASUREMENT: arroyo launches q35, raspi4b and virt and ZERO tegra machines, so the four-A78 ratio needs a metal boot and nothing else. Deliverable 2 is therefore correctly a finding, not a wire-up.
⚠ **A67 — ORIN-RASTGLASS IS NOW BLIND, by design and declared.** Its SURROUND arm requires the backdrop constant OUTSIDE the render box, which was only ever true while `run` owned the panel; on a windowed tegra boot it will latch `NO-RAST-INK`. That is the direction its own header declares acceptable ("blind, never falsely green") and NO GATE REDS FROM IT. ~~So on the next Orin boot a trailing `[orinrast] … -> NO-RAST-INK` is EXPECTED and is NOT evidence the cube failed to paint.~~ **⚠ RETIRED 2026-09-13 BY THE A67 REPAIR (`exec-orin-rastwin` 28bcd764) — THIS SENTENCE IS NOW WRONG AND WOULD MISLEAD A READER OF THE NEXT BOOT. `NO-RAST-INK` MEANS WHAT IT SAYS AGAIN.** The next boot should show `[orinrast] phase=post … -> CUBE-ON-GLASS` with `outside paper=0`, then `[orinrast] census … -> RAST-WINDOW-CLOSED` once the operator closes the window. `RAST-NO-WINDOW` means the demo seated no row (it names its own decline); `RAST-LEAKED-OUTSIDE` means the renderer wrote glass it does not own — a real defect, and the new claim this rung earns. Repair specified in A67 and rasterizer.md §4.1: sample inside the row's rect from `wm::info(id)` and invert the surround test to require NO backdrop pixel outside the row. `display_tegra.rs` was outside the grant and was not touched.
⚠ **THE `fbcon::detach()` AT main.rs:7138 STAYS, and the executor's reason is better than the brief's question.** RASTWIN removes RAST's second-writer problem, but what the detach guards is generic: an UNROUTED fbcon paints over ANY compositor content. Its existing `tegra_conwin_live()` guard already skips it exactly when the console is routed, so on a desktop boot it is already not taken. Removing it would be an unverifiable behavioural change on the one target with no QEMU model.
✓ **SO47 ANSWERED — BOTH VERBS, AND NEITHER CAUSE WAS THE ONE ON THE ROW.** NETVERB (`exec-orin-netverb` 557afe47, parent b8f5ded9; re-gate on the current tip owed before it folds).
**`arp` — the THIRD shape, and the row's two guesses are REFUTED BY MEASUREMENT, not set aside.** smoltcp 0.13.1 keeps `neighbor_cache` private to `InterfaceInner` and `NeighborCache::lookup` is `pub(crate)`, so the verb COULD NOT consult smoltcp's cache — and kept none of its own: every NET6 verb built a throwaway `Interface` with an empty cache (`net_phy.rs:1257`), so `arp`'s only truth was one ARP reply captured inside its own 2 s budget. Refutations: with the verb UNCHANGED the virt leg printed `arp 10.0.2.2 -> is-at 52:55:0a:00:02:02` after a 4/4 ping, so it is NOT waiting on a queue nothing feeds; and ping's first reply on boot4 took 524 ms against the same `VERB_BUDGET_MS = 2000`, so the budget is NOT short. What kills it on metal is A64 — six `[net5T] … verdict=NOWHERE — the payload never reached this DRAM at all` in twelve pops, several of them 60 bytes, i.e. ARP-reply-sized. Ping needs one of four echoes; `arp` needed one specific frame. The executor also REFUSED a tempting reading: zero `[net4F]`/`[net5T]` lines during the arp window is not a dead wire, both witnesses are capped (8 and 12 pops that boot) and were spent during the ping. FIX: an 8-entry/120 s neighbour table inside `mod net6`; `snoop_arp` learns every ARP sender on BOTH opcodes (a request carries sender IP+MAC too), refusing 0.0.0.0 and group-bit MACs; `arp` reads the table first and probes only on a miss.
**`dns` — THE VERB WAS NEVER REACHABLE, and the error was never a network error.** `len=44` is exactly `"Unknown command. Type 'help' for assistance."` — the midden fallthrough at `unaos/libs/sys/midden_core/src/lib.rs:512`. `HOST_VERBS`' `// network` group at :273-275 registers ifconfig/ping/arp/nc/curl and NOT dns, so `shell.rs:5594`'s `"dns"` arm has never once run. SEAT VERIFIED INDEPENDENTLY before granting the file: the string is 44 bytes to the character, `"dns"` appears nowhere else in that file, and the shell arm is real. So SO47's dns half was never a stack defect, a resolver defect, or a socket defect — the word was not a verb. ⚠ THE EXECUTOR PROPOSED `("dns", Avail::Always)`; the seat sent it back, because `Avail::Aarch64` sits in that same enum at :208-220 with NO MEMBERS and a comment saying it is kept for exactly "a verb whose ring arm genuinely does not compile off aarch64" and that a new member needs a reason written beside it — and `dns`'s arm is `cfg(all(feature = "net6", target_arch = "aarch64"))`. `Always` would advertise on every x86 build a verb that does not exist there. The call and its written reason are back with the executor. Landed regardless: `dns` is now a printing-free `dns_lookup` -> `DnsVerdict` -> ONE emission point, so "a witness on every path" is structural rather than remembered, `BIND FAILED` is split out of a `SEND FAILED` that conflated two failures, and a `DNS_WITNESS` counter backs the fixture.
⚠ **A BRIEF DEFECT OF THE SEAT'S, recorded so the next net brief does not repeat it:** my DONE GATE said `UNAOS_TEGRA=1 ./arroyo esp-jetson` and certify `:: NET6:` on the artifact. That build carries NO NIC KNOBS, so `net6` is absent and `:: NET6:` is CORRECTLY 0 — the leg certifies nothing and would have passed while proving nothing. The executor caught it, added leg 5b (`+UNAOS_NET4=1 UNAOS_NET5=1`, banner `…net4,pcie3,pcie2,net6,net5…`) and named `lease-owner=` 0 as the control proving the absence is structural. A CERTIFICATION LEG MUST NAME THE KNOBS THAT COMPILE THE THING IT CERTIFIES — today's second instance of the same class as the arroyo feature-drop above.
⚠ **RETIRED WIRE STRINGS, owed:** `-> NO REPLY ::` (A59's fragment) and `SEND FAILED (socket unusable)` NO LONGER EXIST. Anything scoring them goes quietly green on a boot that failed. The executor is sweeping scorers/specs for both.
✓✓✓ **RUNG 5a FLEW — THE ROM REFUSES NVIDIA'S OWN SIGNED FIRMWARE EXACTLY AS IT REFUSES OURS.** 2026-09-13, Peter: "5a flew". Card `render15-5a-20260913T2123Z-352d6fe`, identity confirmed: `KELF max=0x265208` = the staged `max_vaddr` exactly. Board dark by itself.
**THE LOADER WORKED PERFECTLY — this half is an unqualified PASS.** `[ga10bfw] -> LOADED total=40192 window=0x80200000..0x80209d00 sections=3 digests_ok=3 fmccode_off=0x0 fmcdata_off=0x7000 pkcparam_off=0x9500`. All three vendor files read from `/boot/GA10B/` through the VFS with ZERO MMIO, each sized and sha256-verified against the digest recorded in A61: acr-gsp.text 28,672 B `ceeae9ec…`, acr-gsp.data 9,472 B `e5cd6c6a…`, acr-gsp.manifest 2,048 B `03f3ecc6…`, every one `sha=match`. `window_need=0x9d00 window_have=0x200000`. The 8th-argument staging worked: vendor firmware reached the card from bench media, never entering the repo (R52).
**AND THE ROM SAID THE SAME THING IT HAS SAID FOUR TIMES NOW.** `[ga10bprobe5a] bcrheld=7/7 dmabuf_pa=0x80200000 form=raw lock_after=0 unreadable=0 -> BCR-ALLHELD`, `pre-ignition priscv_br_retcode = 0x00000000` (clean before), then **`br_retcode=0x00000002 br_result=0x2`** — byte for byte the verdict of our blob-free image (render13), of the shift-8 form (render14 4e), and of BRFETCH-false (render15 4f). Full verdict line: `lock_latched=1 post_lockdown=1 v1_readable=0 halted=1 oracles_agree=0 form=raw shift=0 image_sections=3 image_digests_ok=3`. `post-ignition hwcfg2 lockdown=1 (raw=0x0001a733) -> POSTLOCK-HELD`, unchanged across every flight. `DMABUF-UNTOUCHED sections_intact=3/3 tail_words_changed=0/514240` — and note what that does and does not say: nothing WROTE our window; whether anything READ it is not observable this way.
**WHAT THIS ELIMINATES, AND IT IS THE BIGGEST ELIMINATION OF THE LADDER.** Four boots have now varied: the PAYLOAD (ours vs NVIDIA's genuine signed triple), the ADDRESS ENCODING (raw vs `pa >> 8`), and the FETCH CONFIGURATION (BRFETCH true vs false). The ROM answered `0x00000002` to ALL of them. **So "the ROM refuses us because our image is not genuine" is REFUTED BY MEASUREMENT.** It refuses a real, correctly-placed, digest-verified NVIDIA image through this door in exactly the same way. The door is not rejecting the payload; it is not opening for anyone by this route.
**THE RUNG ANTICIPATED THIS AND SAYS WHAT IS NEXT, in its own words on the wire (F19):** *"a real signed image, digests verified in the window, addresses form=raw, and the ROM still says FAIL. Do not iterate images this session: record the identity, then decide between the other address form, the second triple (safety-scheduler) and the mapping inference (§4.1.2) — one boot each."* ⚠ NOTE THE FIRST OF THOSE IS ALREADY ANSWERED: 4e measured raw and shift-8 IDENTICAL, so `PROBE5=5` is NOT worth a power cycle on that reasoning alone. The live candidates are **the second triple (safety-scheduler, R53 Q7 already decided it is the next boot if acr-gsp is refused — IT WAS)** and **the mapping inference (rung-5 brief §4.1.2)**. R53 says nothing on the GPU line is Peter's except the boots.
**SCORER: `FAIL 5a` — and that is the rung working, not the rung broken.** Its PASS arm is `ACR-ACCEPTED`, which did not happen; `BROM-VERDICT-FAIL = 1`, `BCR-CTRL-REFUSED = 0`, `IGNITION-SKIPPED = 0`, `REFUSED reason= = 0`, `5a verdict lines = 1`, `SYSTEM_OFF = 1`. Per R19 a FAIL is A RECORDED RESULT — "failed under these conditions" — not a broken experiment. Every precondition the rung demanded was met before it spent the power cycle.
⚠ **A SCORER FINDING, small and worth fixing before the next GPU boot:** the second RED is `write_announces != write_results` — measured 37 announces against 38 results on this capture. One result line has no matching announce. That is an INSTRUMENT ACCOUNTING mismatch, not a rung failure, but the announce-before-write discipline is the thing that tells us WHICH write killed a boot if one ever does, so a miscount there is worth a look. Owner: next GPU arc.
Evidence `docs/dev/evidence/orin28/render15-5a-vendor-ignition.log` (1,697 lines from the mark). Mark `render15-5a … boot1 flown` in marks.txt. A63 (rung 5a as built) FLOWN; A61's cause list is now down to the two candidates above.

✓✓ **THE esp-jetson-img FEATURE-DROP IS FIXED, AND RUNG 5a IS STAGED WITH THE FIRMWARE.** 2026-09-13 (Peter: "go ahead. do not make up reasons to wait").
**ROOT CAUSE, one line, `unaos/arroyo:1147`:** `_p5_metal=""; { [ -n "${UNAOS_TEGRA:-}" ] || [ "${1:-}" = "esp-jetson" ]; } && _p5_metal=1` — the metal gate that arms `ga10bprobe5a` was a SINGLE STRING EQUALITY against ONE of the SIX verb spellings the dispatcher accepts (`esp-jetson|esp_jetson|jetson` at :9026, `esp-jetson-img|esp_jetson_img|jetson-img` at :9029 — and `esp_jetson_img()` is nothing but a call to `esp_jetson`, so the BUILD is identical and only the feature derivation differs). Five of six spellings silently dropped the ignition while still setting `ga10bprobe5`, and the build printed a feature banner naming a rung it was not building. The `UNAOS_WITNESS` gate at `:49` already enumerates its aliases in a `case` — this line was the outlier. FIXED: the guard is now a `case` over all six spellings, plus the `UNAOS_TEGRA` arm. A feature gate keys on the BUILD TARGET, never on one spelling of its name.
**GO-RED / GO-GREEN, both measured on the artifact, never on the diff.** BEFORE: `esp-jetson-img` banner `ehcihid,ga10bprobe5,sdwrite,tegra,…` and `LC_ALL=C grep -a -o -F` on the built ELF = `ga10b` 0, `ga10bprobe4a` 0. AFTER: banner `ehcihid,ga10bprobe5,ga10bprobe5a,sdwrite,tegra,…` (rc=0) and the SAME grep = `ga10b` 171, `ga10bprobe5a` 85, `[ga10bprobe5a]` 84, `[ga10bfw]` 14, `[ga10bprobe4a]` 50 (5a implies 4a, as Cargo declares), `VENDOR IGNITION` 1, control NOTATOKEN 0. **BLAST RADIUS CHECKED:** `:1147` is the ONLY `$1`-keyed guard in the whole feature-derivation block; PROBE4 carries none, which is why the render15-4f card was sound and its result stands.
**5a STAGED — `render15-5a-20260913T2123Z-352d6fe`**, kernel.elf sha256 4a70f7f5878c030c…, max_vaddr=0x265208, 28 files = the 9 base + **`GA10B/` 19 files staged from `~/unaos-bench/flash/orin/ga10b-fw-36.4.3` via stage-orin.sh's 8th argument, NOT from the build tree and never entering the repo (R52)**; MANIFEST carries a sha256 per file. stage validate rc=0 (36 images, 381 file shas). Card image FATCLUST `clusters=256030 kind=FAT32 bpb32=yes -> PASS`.
⚠ **THE 5a CARD LINE IS `--src`, NOT `--image`.** The `.card.img` is built from the build tree and does NOT contain `GA10B/`; the STAGED DIRECTORY does. `--image` lays a whole card, `--src` stages files INTO an existing volume — and Peter's card is already a valid UNAOS volume from the 4f write. So: `sudo ~/unaos-bench/tools/media-writer.sh --src render15-5a-20260913T2123Z-352d6fe --target /dev/mmcblk0 --write`. A `--image` write here would produce a card with no firmware on it and rung 5a would REFUSE at zero MMIO — which is the loader behaving correctly, and would look like a rung result. It is not one.
WHAT THE 5a WIRE MUST SHOW: `[ga10bfw] … -> LOADED total=` (the loader finding `/boot/GA10B/` through the VFS and verifying digests; `-> REFUSED reason=digest` or a missing-file REFUSE are real outcomes and each means the CARD, not the ROM) → `[ga10bprobe5a]` the vendor ignition → a `br_retcode=` verdict → SYSTEM_OFF. This is the boot that discriminates: 4e and 4f exhausted the BCR configuration space and the ROM answered 0x00000002 to all three, so 5a changes the one remaining variable — what we hand it.
✓✓ **RUNG 4f FLEW AND ANSWERED ITS QUESTION — THE BCR IS NOT THE WALL.** 2026-09-13, Peter on the button ("4f ran"), one power cycle, board dark by itself. Card `render15-4f-20260913T2041Z-b8f5ded`, identity confirmed on the wire: `KELF min=0x0 max=0x25c588` = the staged `max_vaddr` exactly. Scorer on this capture alone: **4a rc=0 PASS, 4b rc=0 PASS, 4f rc=0 PASS**, STOP-CHECK ok on both, `4c lines = 0 (0 is the expected =5/=6 shape)`.
**THE REGISTER TOOK THE ALTERNATE VALUE.** `[ga10bprobe4b] priscv_bcr_ctrl @0x668 wrote=0x00000011 read=0x00000011 held=1` — so `BCR-CTRL-REFUSED = 0`. The rung-5 brief's §6 Q5 door ("configure the BCR but do not have the boot ROM fetch through it") is OPEN, not barred: BRFETCH FALSE is a value this register accepts. That was a real possible outcome and it did not happen.
**AND THE ROM RETURNED THE SAME THING ANYWAY.** `[ga10bprobe4b] br_retcode=0x00000002 br_result=0x2 sample=1/16` -> `BROM-VERDICT-FAIL`. Byte for byte the verdict of the flown `0x111` form (render13 boot2) and of 4e's shift-8 form (render14 boot3). Also unchanged: `post-ignition hwcfg2 lockdown=1 (raw=0x0001a733)` and `gsp_falcon_cpuctl_v1 readable=0 (raw=0xbadf5620)` — the same 0xbadf5620 three prior flights read. `4a bcrheld=7/7 dmabuf_pa=0x80200000 brfetch=false lock_after=0 unreadable=0 -> BCR-ALLHELD`, `bcr_ctrl_before=0x00000110 bcr_ctrl_baseline_changed=0`.
**WHAT THIS ELIMINATES, BY MEASUREMENT AND NOT BY ARGUMENT.** Two boots have now varied the only two things rung 4 controls about the BCR — the ADDRESS ENCODING (4e: raw vs `pa >> 8`) and the FETCH CONFIGURATION (4f: BRFETCH true vs false) — and the ROM answered `0x00000002` in all three configurations. So the refusal is not a consequence of HOW WE SET UP THE BCR. Every remaining cause in A61 is about WHAT WE HAND THE ROM, not how we point it there. ⚠ STATED AS ELIMINATION, NOT AS CONCLUSION: this does not prove the cause is the image's signature. It proves the BCR configuration space rung 4 can reach is exhausted, and it makes rung 5a — which hands the ROM a GENUINE SIGNED VENDOR IMAGE — the experiment that discriminates, because 5a changes exactly the variable these two boots could not.
Evidence `docs/dev/evidence/orin28/render15-4f-brfetch.log` (1,121 lines from the boot mark). Mark `render15-4f … boot1 flown` in marks.txt. Board dark; cold boot next. A62's 4f half is flown.
✓ **RUNG 5a IS NOT FLYABLE YET — A BUILD DEFECT CAUGHT BY CERTIFYING THE ARTIFACT, and it would have flown as a silent no-op.** Staging 5a at this tip: `UNAOS_GA10B_PROBE5=1 ./arroyo esp-jetson` rc=0 builds `ehcihid,ga10bprobe5,ga10bprobe5a,sdwrite,tegra,…` — correct. Then `UNAOS_GA10B_PROBE5=1 ./arroyo esp-jetson-img` rc=0 REBUILDS with `ehcihid,ga10bprobe5,sdwrite,tegra,…` — **`ga10bprobe5a`, the ignition itself, is GONE** — and overwrites `target/aarch64_esp/kernel.elf`. The two steps of one chain do not agree on the feature set. The resulting artifact certifies EMPTY: `LC_ALL=C grep -a -o -F` on the built ELF gives `ga10b` 0 and `ga10bprobe4a` 0, against the staged 4f ELF's 79 and 42. ⚠ SEAT CORRECTION, same day: the first writing of this row also cited "even `sdwrite` 0" as a third control. That was not evidence — `sdwrite` is a Cargo FEATURE NAME and appears as a string in no build at all; it reads 0 on the KNOWN-GOOD 4f card too (its witnesses are `SDWRITE`/`:: SDWRITE:`, not the feature name). The finding stands on the two real controls; the third was a bad grep and is struck. A card cut from it would have booted, printed no rung, and looked like a clean flight. THE GATE THAT CAUGHT IT is artifact certification on the BUILT ELF; the feature banner in the build log says the opposite and would have passed a reviewer. Fix before 5a flies, and check whether esp-jetson-img has been silently re-deriving features on every previous card too. Owner: next GPU arc.
· (superseded, kept as the record) RUNG 4f CARD STAGED AND READY TO FLY — `render15-4f-20260913T2041Z-b8f5ded`** (2026-09-13, seat; Peter: "i meant the gpu"). Built at the hw-jetson tip b8f5ded9 with `UNAOS_GA10B_PROBE4=6 ./arroyo esp-jetson` rc=0 then `esp-jetson-img` rc=0 (logs ~/unaos-bench/scratch/orin-0912b/gpu-4f/). THIS IS A DEDICATED PROBE FLIGHT, NOT A GLASS BOOT: the effective feature line is `ehcihid,ga10bprobe4a,ga10bprobe4b,ga10bprobe4f,sdwrite,tegra,tegrasmp,apsrun,bsptick,bsprun,sdmmc` — no desktop, no witness, no render. It boots, runs 4a+4b with the ONE delta (`bcr_ctrl` written 0x00000011, BRFETCH FALSE, instead of 0x00000111), and ends in PSCI SYSTEM_OFF on every path. The board goes dark by itself; a dark board is the "ready for cold boot" signal. ARTIFACT CERTIFIED on the BUILT ELF (kernel.elf 2,105,096 B, sha256 8310a7a28dd7218f…, max_vaddr=0x25c588): `[ga10bprobe4a]` 42 · `[ga10bprobe4b]` 24 · `[ga10bprobe4f]` 1 · `brfetch=false` 5 · and the arm's documented ABSENCES all 0 — `[ga10bprobe4c]` 0, `[ga10bprobe4d]` 0, `[ga10bprobe4e]` 0 (the `=6` arm carries no 4c and is not deferred) · control NOTATOKEN 0. That 4f family count of 1 is CORRECT and not a miss: arroyo:1082-1086 defines `=5`/`=6` as "the `=2` wire plus one tag on the verdict lines", so only the ARMED line carries the family and the rest of the wire is 4a/4b lines tagged `brfetch=false`. Card image `~/unaos-bench/flash/orin/render15-4f-20260913T2041Z-b8f5ded.card.img` (671,088,640 B, sha256 dc33aed993473188…), FATCLUST `clusters=256030 kind=FAT32 bpb32=yes -> PASS`; stage validate rc=0 (35 images, 354 file shas). Writer go-red proven on a bogus target (`C12-IMAGE-FITS … refusing to write an image of unknown fit`, nothing written). CARD LINE: `sudo ~/unaos-bench/tools/media-writer.sh --image ~/unaos-bench/flash/orin/render15-4f-20260913T2041Z-b8f5ded.card.img --target /dev/mmcblk0 --write --erase-target`. WHAT THE WIRE MUST SHOW: `[ga10bprobe4f] rung 4f ARMED (UNAOS_GA10B_PROBE4=6)` → 4a `BCR-ALLHELD` with `brfetch=false` → 4b's ignition → a `br_retcode=` verdict → SYSTEM_OFF. `BCR-CTRL-REFUSED` is a REAL outcome, not a failure — the register may simply not take 0x011. Score with docs/dev/evidence/orin27/scorer-ga10b4.sh. NO THEORY IS OFFERED about what the ROM does with BRFETCH FALSE; this arm spends one power cycle to find out.
✓ NEXT AFTER 4f — RUNG 5a, the one that discriminates: `UNAOS_GA10B_PROBE5=1` (raw) or `=5` (shift-8). 4e already measured raw and shift-8 IDENTICAL, so that choice is now free and `=1` is the simpler record. 5a hands the ROM a GENUINE SIGNED VENDOR IMAGE, so it is what tells apart "the ROM refuses OUR image" from "the ROM refuses everything". It needs the firmware on the card beside the kernel and NEVER in the repo (R52): stage with stage-orin.sh's 8th argument `<extra-dir>:<dest>` from `~/unaos-bench/flash/orin/ga10b-fw-36.4.3/`. Not built yet — build it while 4f flies.
✓✓ RUNG 4e FLEW AND ANSWERED ITS QUESTION — scorer on boot3 alone: 4a rc=0, 4b rc=0, 4c rc=0, **4e rc=0 PASS**. `[ga10bprobe4d] DEFERRED RUN` fired from the shutdown verb, `shift arithmetic: writes_with_raw_pa=12 wrong=0`, `BCR-ALLHELD` with shift=8, `priscv_bcr_fmccode_lo wrote=0x00802000 read=0x00802000 raw_pa=0x80200000 held=1`. **VERDICT: `br_retcode=0x00000002 -> BROM-VERDICT-FAIL`, IDENTICAL to the raw-address form flown on render13.** So THE ADDRESS SHIFT WAS NOT THE WALL — A61 cause 5 is now measured and eliminated, not argued. Also `post-ignition census readable=21/30 changed=1 changed_ex_bcr_retcode=0`, `bcr post-ignition intact=8/8`, `DMABUF-UNTOUCHED words_changed=0/524288`, `CENSUS-COMPLETE`, `[pwrshutoff] ring drained lines=1 bytes=163`, SYSTEM_OFF, board dark. NEXT ON THE GPU LINE: 4f (BRFETCH, `=6`) and 5a (the real signed image, `=1`/`=5`) — the ROM refuses a real thing the same way it refuses ours, and 5a is what tells them apart.
✓ A60 FLOWN — THE ROOT IS WRITABLE ON METAL: `[vfs] root mount / = native unafs volume source=tegra-sd rw=yes`, `:: TEGRA-SD: WRITE admitted at the block layer`, and `:: SDWRITE: root=tegra-sd wrote=1 readback=match deleted=1 -> PASS ::` on EVERY boot. The card safety held: nothing but SDWRITE.TXT moved.
✗ **LOGIN NEVER OPENS AT BOOT — ROOT CAUSE FOUND, one line.** Peter: "no login sequence at all". Across five boots `[login] screen open` appears ONCE, and that once is after Log Out. The call is `main.rs:9129` `let activated = desktop_firmware::activate(); #[cfg(feature="login")] if activated { …screen_open_once(); }` — and THIS BOARD PRINTS `[deskcascade] -> CASCADED windows=2 bar=1 owns_pixels=1 route=ROUTED activate=false`. `activate()` returns `fbcon::console_is_routed()` only on its SECOND call (ACTIVATED already swapped) and `false` when the panel reads 0; on the Orin the desktop comes up perfectly with `activated == false`, so the screen is gated on a flag that is false exactly where it matters. The x86 ladder never caught it because there `activate()` is true. FIX: gate the open on the desktop EXISTING, not on that return — the same `desktop` predicate main.rs:5342/6380 already use — and prove it on the seam the Orin actually takes. SO43.
✗ **THE LOGIN WINDOW IS DISMISSED BY A CLICK, and LOGINCLOSE said it could not be.** Peter: "the login window appeared over the top of the gui and when i click it it went away". The wire says the row is `[wm] alloc win=5 gen=1 owner=0x0 title="Log in" from=declared` — owner 0, the band LOGINCLOSE measured as not dock-addressable and skipped by `hit_test`. So the measurement was right about the CLOSE BOX and wrong about the consequence: a click that hits nothing does not close the window, it lands BENEATH it, and the screen is left behind/underneath the desktop it no longer covers. The fixture asked "can the pointer close this row" and never asked "what does a click on this row DO". SO44.
✗ **THE DRAG STALL IS BACK AT FULL SIZE — DRAINCAP DID NOT FIX IT ON METAL.** Peter: "window dragging starts a bit sticky and has sticky moments here and there", plus a one-minute freeze. `[comp2] max_us` tops out at **361,130 / 359,500 / 348,593 µs** across boots 1/2/5 — the SAME 375-ms band render13 showed, against SERDRAIN's predicted 22.6 ms. Either the 192-byte cap is not on the path the metal takes, or SO29's mechanism (the ring drain) is not the whole cause. The DRAINCAP witness cannot adjudicate it here — it rides `mirror_service`, which SO41 proved never runs on this board. THIS IS THE ONE TO TAKE NEXT: instrument the stall where it happens, on the Orin, not on the x86 ladder. SO45.
✗ THE POINTER IS STILL LOST after a while — Peter lost it twice and rebooted both times ("went to open quarry the machine froze for a minute and when it came back the mouse cursor was gone"). ⚠ **SEAT CITATION CORRECTED 2026-09-13 (PTRLOST, correcting BOTH of us):** I wrote here that `[cursor12]` carries `nohit=186` at the end of boot2 and that boots 1/3/4/5 end in single digits. **`nohit=186` IS real and IS in boot 2 — but it is NOT the lost-pointer signature.** That rollup reads `passes=2294 nosprite=29 nohit=186 … planned=2079 … -> below-session`: 91% of the window PLANNED an overlay, and the verdict token is `below-session`, not a decline. The signature the arc actually rests on is the LATCHED block — `nohit` approaching `passes` with `planned=0` across four consecutive rollups — and that is in **BOOT 5** (`nohit=3143/3158`, `7465/8032`, `6889/7717`, `906/1379`). The executor had also told the seat `nohit=186` "appears nowhere in any capture", which was false — it had searched only `docs/dev/evidence/`, not the live capture — and corrected itself. SO46's headline number pointed at the wrong window; the FINDING stands, the CITATION did not. Boot ordinals are DERIVED, not assumed: the four `KELF max=0x3fe708` boots start at capture lines 326454, 331681, 336654 and 342308, and the already-committed `render14-boot3-shutdown-rung4e.log` pins the second as boot 3, which makes 342308 boot 5. Evidence now in git: `docs/dev/evidence/orin28/render14-boot5-ptrlost-nohit-latched.log`, a whole contiguous boot (14,096 lines) beginning on its anchor `KELF min=0x0 max=0x3fe708` — and that anchor is not decorative, it matches the render14 flash MANIFEST's `ELF max_vaddr=0x3fe708`, so the image identity is VERIFIED rather than assumed. Boot 2's capture is still NOT in git; the one line is quoted in A74 so a reader can find it in the same source, and it must be cut and committed if it is ever needed as evidence for a claim. APPPIN closed the shell-remint route (`shell-remint` = 0 on the whole wire, that fix held) — so this is a DIFFERENT road to the same place, and it is reached by opening apps, not by quitting the shell. SO46.
✗ `arp` AND `dns` DO NOT WORK; PING DOES. `:: NET6: ping 10.42.0.1 4/4 replies over rtl8168 peer 9c:69:d3:28:6e:f4 -> REPLY ::` — ping even LEARNS THE PEER MAC — and then `:: NET6: arp 10.42.0.1 -> NO REPLY ::` on the same boot, and `dns google.com` returns `:: [midden] cmd="dns google.com" -> TerminalError len=44 ::`. The lease names a resolver (`dns 10.42.0.1`). Two separate defects in the two verbs, neither in the stack under them. SO47.
✗ BLOCKY FONTS in the login and Quarry windows (Peter). The login box is `890x524` where the fixture measured `450x284` on its panel — very close to 2x, so these windows are being drawn through a 2x scale of a bitmap font on the 1920x1200 panel. SO48.
✓✓ **A25 IS CLOSED — FLOWN, on Peter's eyes** (2026-09-13): *"the windows and their names appeared synced up with the menu and task bars from my pov"*. Evidence docs/dev/evidence/orin28/render14-boot1-desktop-menubar.log. BOTH halves hold: the old defect's signature — a `menu_owner` that is neither 0 nor `cap_owner`, i.e. pulse's `View` under another app's caption — occurs ZERO times in 29,807 lines; and `items=app:Pulse@34+57,View@91+48` puts the tenant title at exactly the app box's right edge (34+57=91), which is this row's own stated predicate, to the pixel.
✗✗ **SEAT ERROR — I REOPENED A25 ON A MISREADING AND TOLD PETER HIS REJECTION STOOD.** I read `cap_owner=2 cap=Quarry menu_owner=0` off the wire as "wrong owner". It is not: `menu_owner = if winmenu::has_tree(cap_owner) { cap_owner } else { WIN_NONE }` (menubar.rs:826), and Console/Shell/Quarry publish no menu tree — `[winmenu] publish owner=3` is the ONLY publisher on the whole wire — so `0` there means "this app has no menus", the opposite of the defect. I had the derivation in front of me hours earlier, in the correction I myself wrote, and did not apply it when the field was 0. THE RULE THIS COSTS: before calling a witness field a defect, state what its value MEANS and where that meaning is defined — a count of 0 is a claim about the code, not about the glass. R25 cuts both ways: a row closes on the glass, and it does not reopen on a seat's reading either.
✗ TWO CARDS IN ONE READER: the second LUN FAILS. `:: USBLUN: lun=1/1 … capacity=none verdict=FAILED reason=cap-bot:TransferError(13) ::` after `max_lun=1 … the device declares 2 logical unit(s)`, so `census … present=1` and only one disk publishes. USBREG's registry is right; the READ_CAPACITY on LUN 1 errors. Peter also saw Quarry not resolve it. SO49.
· A garbled volume: `[vfs] volume mounted /volumes/________ source=global rw=yes label_raw=0002000000010006000000` — a label of raw bytes, not text. Worth a look with SO49.
· Peter: "this last boot went faster with two card readers plugged in", and "a long pause after gui with console then the others appear", "long pause on opening apps". The long pauses are almost certainly SO45; the faster-with-readers observation is unexplained and is a CLUE for it — record it, do not theorise.
✓ STEP 1 RUNNING: six re-gate executors launched at once on their own tips (SERDRAIN 52b694d4 Opus · LOGIN 0a04c01b Fable · NET6 bd3d0113 Opus · USBREG b9b0cab5 Opus · GA10B4E af9c0265 Opus · GA10B5A c6af430e Fable), briefs `~/unaos-bench/scratch/orin-0912b/briefs/<NAME>-resume.md` (the original brief appended). CHANGE OF SHAPE from the close plan: each executor FIRST merges hw-jetson 4fea9e59 into its branch (--no-ff, conflicts rebuilt from both sides by the side that wrote them, ledger union, markers 0, ledger-check rc=0) and gates the MERGED tree — so the seat's six folds (step 3) become near-trivial and the fold conflicts in wm.rs/block.rs/bootdisk.rs/serial are rebuilt by their authors, not by the seat blind. Grants decided under R53: NET6's lib.rs/Cargo.toml/k8-reach.registry + syscall.rs fixture placement; USBREG's stale USB_BLOCK_DEVICE prose (comments only). GA10B4E carries step 2 (rung 4e deferrable, PROBE4=7 proposed, one-hour box). GA10B5A carries M2 (5a wired, both address forms, deferrable) and M3 (RUNG6 brief, 5b). LOGIN carries M2 then M3 then M4. Pushed WIP tips are never amended: gate records go in the next commit bodies.

## CLOSE STATE — 2026-09-12T19:xxZ (spend wall; Peter: "6% left until we hit the 5 hour wall")
✓ hw-jetson 369130f5 (local, UNPUSHED since 9ec96894): the render13 close-out, C15, R51/R52/R53, the folds APPNAME ecd3bc79 · DRAGSTALL 4465eb20 · SDWRITE ee5e2109 · GA10B5 ec20ee6a · GA10B5B 186ac5d5 · APPPIN e0b5d31d, the family ledger d9e5be86, the WCDFLOOD cfg fix 83ee4653. main 198376dd (local, unpushed since b8e45320: the trunk-queue rows). Push line: `git push origin main hw-jetson exec-orin27-appname exec-orin27-dragstall exec-orin27-conreopen exec-orin-sdwrite exec-orin-ga10b5 exec-orin-ga10b5b exec-orin-login exec-orin-net6 exec-orin-usbreg exec-orin-ga10b4e exec-orin-serdrain exec-orin-ga10b5a`.
✓ **NEXT SESSION, IN ORDER (Peter 2026-09-12: "can we do an all in one boot again?" — yes; ONE card carries everything below; the render14 fold at 369130f5 is superseded by this bigger fold, do NOT stage it):**
  1. Re-gate each checkpointed branch on its own tip with its brief's DONE list (the rc lists in their WIP messages name what did not run): exec-orin-serdrain 52b694d4 (+ apply deliverable 3 from its scratch: the ring drain before the power SMC) · exec-orin-login 0a04c01b (gates 2-6; then M2 /home, M3 login screen, M4 Log Out from its scratch scripts — at least M2 before the card) · exec-orin-net6 bd3d0113 (five gates; grant its three out-of-list files) · exec-orin-usbreg b9b0cab5 (gates 1-5; leg 7 must read aliased=-) · exec-orin-ga10b4e af9c0265 (gates 3-4 with the =2 control) · exec-orin-ga10b5a c6af430e (M2: the pub(crate) one-liners in ga10b_probe.rs, the scorer 5a leg; M3 the RUNG6 brief). Executors may be resumed on their branches or the seat runs the gates itself; every fold conflict in wm.rs/block.rs/bootdisk.rs/serial is rebuilt from both sides, never resolved blind.
  2. Make rung 4e DEFERRABLE the way 4d is (GA10B-RUNG4-BRIEF §10.4: the knob value that arms 4a+4b for the shutdown path) so the glass boot ends in the GPU test: `UNAOS_GA10B_PROBE4=<deferred-4e>` — 4e writes the six addresses `pa >> 8`; Shut Down runs it and powers off. If 4e cannot be deferred in an hour, fly 4e as its own boot after the glass card.
  3. Fold all six into hw-jetson (--no-ff each, union on ledgers, markers = 0, ledger-check rc=0), then ONE fold gate (PLAYBOOK §3) with the knob line = render13's fifteen + UNAOS_LOGIN=1 + UNAOS_NET4=1 UNAOS_NET5=1 (already) + the deferred-4e value; certify the artifact (`[dock] launch app=`, `from=program`, `wcd_skips=`, `DRAINCAP`/`ring drained`, `:: LOGIN:`/`[users]`, `:: NET6:`, `USB_DISKS`/`UsbN` witnesses, `shift=8`, `shell-remint`=0), fat-clusters.sh on the card image, stage-orin.sh, dry runs, playbook (watch list: login screen → home folder → apps named → console/shell tiles → pointer alive after a shell quit+relaunch → drag smooth → two cards in the reader both mounted → ping/dns from the shell → Shut Down → 4e verdict → dark), mark, waker, one card line.
  4. Then the GPU boots: 4f (BRFETCH) and 5a (the real image; 5a needs the GA10B/ files + LICENCE.txt staged into the tree from ~/unaos-bench/flash/orin/ga10b-fw-36.4.3/, R52), one power cycle each.
✓ RENDER14 FOLD GATE GREEN on 369130f5 (SUPERSEDED by the all-in-one fold above; kept as the record that the glass fold compiles and runs) — 1-check-tegra rc=0 · 2-check-plain rc=0 · 3-test-wc (UNAOS_QEMU_FULL=1, 150 s) rc=0 · 4-test-arm rc=0 · 5-esp-jetson rc=0 · 6-esp-jetson-img rc=0 (logs ~/unaos-bench/scratch/orin-0912b/fold-render14/). ARTIFACT CERTIFIED (kernel.elf 3,402,032 B): `[dock] launch app=` 3 · `from=program` 1 · `wcd_skips=` 1 · `:: SDARG:` 1 · `BEAM VERDICT=` 3 · `shell-remint` 0 · `reopen requested` 0 · `[ga10bprobe3]` 0 · control NOTATOKEN 0 (`:: APPPIN:` 0 is expected: that fixture is x86-ladder only). Banner: witness,ehcihid,kbdwit,sdhcblk,holocron,smolnet,tegra,bsptick,bsprun,beam,orinclick,tegra_el0,tegrasmp,apsrun,orinrender,desktop_firmware,orinrx,tcuprobe,tcurx,deskcascade,net4,pcie3,pcie2 (no ga10bprobe4*: PROBE4 dropped from this line). Card image unaos/target/UnaOS-orin-card.img: FATCLUST p1_start=2048 bps=512 spc=1 rsvd=32 nfats=2 tot=260064 fatsz=2001 data=256030 clusters=256030 kind=FAT32 bpb32=yes -> PASS; CARDREADY-GEOM 1:0c:2048:260096,2:7f:262144:1048576. NOT STAGED (spend wall): next session stages with stage-orin.sh at 369130f5 (the build tree is target/aarch64_esp/ in ../UnaOS-orin — REBUILT by nothing since; do not run test-arm before staging), fat-clusters.sh on the card image, dry runs, playbook, mark, waker, one card line. (was: RUNNING DETACHED on 369130f5 (six steps, logs ~/unaos-bench/scratch/orin-0912b/fold-render14/, rc.txt fills in; `DONE` at the end). Next session: read rc.txt; if all six rc=0, certify the artifact (`LC_ALL=C grep -a -o -F` for `[dock] launch app=`, `from=program`, `wcd_skips=`, `shell-remint`=0), stage with stage-orin.sh (knob line in the RENDER14 line below), fat-clusters.sh on the card image, media-writer dry runs, playbook, mark, waker — then one card for Peter. If any step is red: the log names the leg; fix on hw-jetson, re-run the chain.
✓ EXECUTORS at the wall: told to WIP-commit on their branches and stop (LOGIN M1 built+proven, three granted files, M2-M4 not started; NET6; USBREG; GA10B4E rung 4e/4f; SERDRAIN; GA10B5A rung 5a). Each branch's tip is the handoff; a "WIP UNGATED:" commit has NOT run its gates — re-run them before folding. Their briefs: ~/unaos-bench/scratch/orin-0912b/briefs/*.md (bench scratch, not git).
✓ BENCH: board dark since the render13 boot-2 SYSTEM_OFF; butler host pid 27246 holds /dev/ttyACM0; no waker. Firmware set staged at ~/unaos-bench/flash/orin/ga10b-fw-36.4.3/ (R52).
✓ SERDRAIN at the wall: 6c2dc6f8 GATED (DRAINCAP: one ring drain capped at 192 B, 377.8 ms → 22.6 ms, SERWIT-1 PASS, go-red rc=1) + 52b694d4 WIP UNGATED (SERWIT-1B parity: aarch64 back-pressures instead of dropping; fixture written; its check was killed at 56 green legs). Deliverable 3 (drain before the power SMC) is a patch script in its scratch, unapplied. Its "foreign red" is the WCDFLOOD cfg already fixed on hw-jetson at 83ee4653. It ran a `pgrep -f "arroyo check"` kill early on — the render14 chain rc.txt says whether that hit.
✓ CHECKPOINTS at the wall: LOGIN 0a04c01b (M1, 14 files; gate 1 rc=0 on this tree, 83 legs; gates 2-6 green on the tree before it — rerun; M2-M4 drafted as scripts in its scratch, unapplied). NET6 bd3d0113 (shared smoltcp socket surface `net_phy.rs mod net6`, NicOps adapters for virtio-net and rtl8168, aarch64 SYS_SOCKET..SOCK_RECV, EL0 fixture PASS on virt, lease-owner=smoltcp-dhcpv4 named, A59 fixed-unflown, descriptor-17 row; gates green one tree behind — rerun all five, esp-jetson never ran; three files outside its list to rule on: lib.rs net_phy cfg, Cargo.toml net6, k8-reach.registry). GA10B4E af9c0265 (rungs 4e =5 and 4f =6; checks rc=0 on this tree; scorer 4e/4f legs go-red proven; knoboff and the =2/=4 byte-identity measured one tree behind — rerun gates 3-4 incl. the =2 control). USBREG and GA10B5A: not yet reported at close — their branch tips are the handoff.
✓ GA10B5A c6af430e (WIP UNGATED; parent 76482864): M1 firmware loader `ga10b_fw.rs` (knob UNAOS_GA10B_PROBE5=1; virt fixture rc=0: `-> LOADED total=40192`, digest mutation `-> REFUSED reason=digest`), A63; `ga10b_ignite.rs` = rung 5a DRAFT, cfg'd out, never compiled; its full check red only on the WCDFLOOD cfg already fixed at 83ee4653. FACTS gathered (for RUNG6 brief, unwritten): nvgpu's GSP-ACR door is MAILBOX0/1 descriptor + bcr_ctrl 0x11 + IMEM/DMEM port loads; nvgpu's BRFETCH-DMA door (PMU ns_bootstrap) writes the addresses >> 8 — so 5a flies the shift-8 form, and 4e's result decides it. Parked M2 pieces in its scratch (`m2-*.txt`). USBREG b9b0cab5 (WIP UNGATED; parent cc3ca3e8): `USB_DISKS: [Option<UsbDisk>; 4]` keyed (xHCI slot, LUN) replaces the single slot; per-LUN transfers; `BlockSource::UsbN(n)`, `live_sources()`; bootdisk walks every live source with a registry-unit dedupe and a HOMESOIL leg 7; SO33 fixed-unflown, usb_xhci.md §5k. One tree earlier: check rc=0 (92 legs), tegra check rc=0, esp-jetson rc=0, test/test-arm rc=1 from its own leg 7 catching a real untruthful `aliased=ambiguous:usb?usb1` witness; the one-line fix is in this sha and NOT re-gated (re-run reached 29/92 legs). Rerun gates 1-5, expect leg 7 `aliased=-` PASS. Still single: `storage_slot` (one reader, not two). Stale USB_BLOCK_DEVICE prose in emmc2.rs:708, shell.rs:5343, prtscr.rs (4 lines) — untouched, report. After it folds: A60 (the TegraSd write veto).
✓ WHAT I MAY HAVE MISSED (R48): (1) the wm.rs union of APPNAME+DRAGSTALL+APPPIN is proven only by the running chain — no human read the three-way; (2) the render14 knob line drops UNAOS_GA10B_PROBE4 — if Peter wants the shutdown rung re-proven with the ring drain, it is not on this card; (3) SERDRAIN's cap and APPPIN's console pump are unflown; (4) the A60 veto lift waits on USBREG, which is now WIP; (5) ledger-check accepted conflict markers once (trunk queue §5) — every fold this session was checked by hand after that.

## STATE — 2026-09-12T15:1xZ
✓ main b8e45320 = origin · hw-jetson 9ec96894 = origin, 14 ahead of main (FATCLUST 3225b5b0, A58, R49/R50, queue docs, LAWS/PLAYBOOK merges) · hw-pi4 e6e71c9f = origin · hw-rmbp c3890cf6 = origin. Every worktree clean. Nothing was owed a push at open (the retired baton's push line was already done).
✓ **render13 BOOT 2 FLOWN 2026-09-12T15:0xZ — RUNG 4a/4b/4c PASS FROM THE SHUTDOWN PATH, BOARD OFF.** Same card (native slot, `KELF min=0x0 max=0x34f820`, boot volume serial 0x8b482cca). Peter typed `shutdown` in the shell → `[pwrshutoff] shutdown verb invoked` → `[ga10bprobe4d] DEFERRED RUN` → 4a `bcrheld=7/7 -> BCR-ALLHELD` → `PRECENSUS-DONE` → 4b `br_retcode=0x00000002 -> BROM-VERDICT-FAIL` (the PASS) → 4c `POSTBCR-INTACT`, census 21/30 readable changed=1 (retcode only), `DMABUF-UNTOUCHED`, `CENSUS-COMPLETE` → `PSCI SYSTEM_OFF` → firmware "Shutdown state requested 0". `scorer-ga10b4.sh` 4a/4b/4c rc=0 PASS each. Evidence `docs/dev/evidence/orin27/render13-boot2.log` (3,049 lines from the boot-2 mark). A55 and A54 ticked flown. Marks: `boot2 armed` and `boot2 flown` in marks.txt. Board dark; cold boot next. Butler host pid 27246 holds /dev/ttyACM0; waker not running (nothing to wait for).
✓ BEAM: boot 2 `BEAM VERDICT=LOCKED head=0 half=lo @0x13806330 vtotal=1235 period_us=16667` with 52 `beam=` rollups (`torn=no -> BUFFERED`); boot 1 of the SAME image said NO-RASTER. The candidate search is not repeatable boot to boot — A54 carries the question. Peter saw no tearing on either boot.
✓ CORRECTION to the CLOSE STATE below: render13 boot 1 never reached Shut Down because the pointer was lost after the shell re-mint (the crystal menu was unreachable) and Peter powered the board off in the idle desktop at up≈740s; the wire ended mid-line. That was a power cut, not a shutdown.
✓ Peter (2026-09-12T15:0xZ): the DHCP lease worked on earlier boots and EVERY render12-fold GUI check (close leaves siblings, cursor sweep, dock tiles, Facet PNG) is confirmed fixed on the glass. The shutdown rung was the last test on this card. NOTHING IS LEFT TO FLY ON render13.
✓ NEW on the render13 boot-1 wire (shared code → filed on the trunk queue §1/§5 too): (1) `[wc-d] verify win=1 -> SKIP (no memory for 960x272 source snapshot)` (wm.rs:7877) prints once per compositor pass while such a window exists: 46,161 of 67,185 lines, and the serial staging ring dropped 5,331 lines in 192 events under it — a per-pass witness must latch, not repeat; (2) the shutdown path (power.rs `platform_shutdown`/`crystal_shutdown` → `psci_call`) prints through the same ring and drains nothing before the SMC, so on a flooded ring the rung's lines can die with the power (boot 2 was quiet: one 23-line drop before the verb, none in the rung); (3) the banner `crystal LIVE … Sleep/Restart/Shut Down print their honest unimplemented lines — no PSCI wiring on this track yet` (video/desktop_firmware.rs:356, main.rs:8547) is stale: video/crystal.rs:693 calls `power::crystal_shutdown` → PSCI SYSTEM_OFF.
✓ R51 (2026-09-12T15:2xZ, Peter): go on all three glass executors, plus 3D, net, the open hardware rows, and MULTI-USER as a line (login, home folder). EIGHT executors live off hw-jetson (three at a475fd6f, five at cc3ca3e8), briefs in the session scratch: APPPIN (Fable; exec-orin27-conreopen; R49/R50 + the lost pointer; SO27) · APPNAME (exec-orin27-appname; R36; SO28) · DRAGSTALL (exec-orin27-dragstall; the 375-588 ms passes + the wc-d latch; SO29/SO30) · LOGIN (Fable; exec-orin-login; a human user, login screen, /home/<user>, session principal, logout; SO32) · SDWRITE (exec-orin-sdwrite; lift the TegraSd write veto so `/` is rw, A57 refusal, a metal SDWRITE fixture; A60) · NET6 (exec-orin-net6; which stack leased, ping/arp/dns verbs, the EL0 socket family on the tegra NIC via virt, the descriptor-17 row; A59) · USBREG (exec-orin-usbreg; the block registry becomes an array keyed (slot, lun); SO33) · GA10B5 (docs only; exec-orin-ga10b5; the rung-5 brief: FMC/BCR layout from public sources, the blob-free fork, the L4T licence facts for Peter, A61). Fold order when they report: glass three first (one fold gate, one card: render14), then the rest as they land. Ledger ids reserved above; SO31 free.
✓ C15 DONE (2026-09-12T16:3xZ, this session, hw-jetson 93ba7fda): `unaos/scripts/fat-clusters.sh` asserts FAT32-by-count off the WRITTEN BPB inside make-pi-img.sh (red on the refused render13 image, green on card-v2 and on a fresh 128 MiB build); A58 corrected (256,030 / 110,874; fs/fat.rs misreads) and flown. Bench writer `~/unaos-bench/tools/media-writer.sh` gained C15-FAT32-BY-COUNT in --src and --image modes with the `spc8.img` fixture: selftest 45 passed, 0 failed (was 43/0); backup `media-writer.sh.bak-c15-<UTC>`. The FATCLUST review objections are answered; nothing blocks landing hw-jetson into main except the executors in flight.
✓ GA10B5 FOLDED ec20ee6a (docs only; A61 open): the rung-5 brief. Findings: no public source supports a blob-free path to any GA10B engine (nouveau's Tegra map stops at GV11B; Turing+ engines come up only through NVIDIA-signed firmware; GA10B has no display engine of its own; our die held priv-lockdown across the ignition); `DMABUF-UNTOUCHED` proves the ROM did not WRITE our window, not that it did not read it; `bcr_ctrl 0x110` decodes as BRFETCH TRUE, CORE_SELECT RISCV, VALID FALSE (MIT header), so 4b's delta was exactly "mark valid"; the MIT Hopper bootstrap writes the BCR DMA addresses `>> 8` where 4b wrote them raw. Executor GA10B4E (Opus, exec-orin-ga10b4e, A62) builds rung 4e (shift) and 4f (BRFETCH FALSE) as knob values 5 and 6 of UNAOS_GA10B_PROBE4; each is one boot ending in SYSTEM_OFF.
✓ APPNAME FOLDED ecd3bc79 (SO28, fixed-unflown): the launch seam already armed the name (6915c256); the child reached SYS_WIN_CREATE first about half the time (BG-SPREAD), so the arm is retroactive now (`app_name_adopt`). Next boot: `[winmenu] app-menu owner=N name=VUG from=program` for every launched ELF, `from=declared` for Console/Quarry/Pulse/Shell, no `name=Application`. knoboff wc exit 1 accepted: wm.rs is unconditional and R36 asks for a default-path change. SDWRITE: the veto lift STOPPED (the refusal is drivers/block.rs:1774 `write_block_tegra_sd`, unconditional; needs block.rs, bootdisk.rs, Cargo.toml, main.rs — USBREG's files); scoped to A57 + SDARG for this commit, A60 carries the five-part plan for after USBREG folds.
✓ DRAGSTALL FOLDED 4465eb20 (+7006857f: the fold committed three conflict markers in LEDGER.md — removed; ledger-check did NOT refuse them, a gate gap filed on the trunk queue §5). MECHANISM (SO29, open): `[comp2] max_us` measures the serial staging-ring DRAIN — `_print` runs IRQ-masked and the core that wins SERIAL_PORT drains up to 64 staged lines byte by byte at 86.8 us/byte (64 x 68 B = 377.8 ms; the observed band 375-618 ms is one drain wide); not the present (maxpresent 7.7 ms), not the sprite, not A46. The producer was the wc-d OOM SKIP line, once per pass (46,161 lines, ~36% of the wire) — LATCHED now (SO30, fixed-unflown; `wcd_skips=` on the rollup). Executor SERDRAIN (exec-orin-serdrain, SO31) takes the rest: cap one drain's BYTES at the point of use, aarch64 SERWIT-1B backpressure parity (x86-only today), and the drain before the power SMC (trunk queue item f).
✓ GA10B5B FOLDED: the ga10b firmware set is 17 files (public file list of a package whose upstream version string is NVIDIA's own, 36.4.3); the triple the ROM wants is INFERRED as acr-gsp.text/.data/.manifest (three files, placements ours at 256-byte alignment); the 2 MiB window is plausible, unproven (sizes need the bytes; P8 REFUSES image-too-large). Licence: 1.1(d) redistribution with the agreement reaching each recipient; 2.1 no translating out of architecture; 2.6 no circumventing authentication (5a hands the ROM a genuine image); 2.9 is why "staged beside, never linked" is the only GPL-3.0 shape. DOWNLOADED BY PETER 2026-09-12 (sha256 verified, 17 files, staged unmodified with the agreement at `~/unaos-bench/flash/orin/ga10b-fw-36.4.3/`; the acr-gsp triple is 28,672 / 9,472 / 2,048 B — A61 has the digests; the window question is closed for size). The line was: `curl -fL -O https://repo.download.nvidia.com/jetson/t234/pool/main/n/nvidia-l4t-firmware/nvidia-l4t-firmware_36.4.3-20250107174145_arm64.deb` — 2,295,852 bytes, SHA256 ec89d6a5c449bc6e7e89127bc7390bea7a113a69f92a4dc15462e9e943fad3ab (NVIDIA's Packages index); it carries lib/firmware/nvidia/ga10b/* AND the agreement (usr/share/doc/nvidia-l4t-firmware/copyright). DECIDED BY THE SESSION under R53 (2026-09-12T18:xxZ; Peter: "you already know our license complies why ask"): Q2 rung 5a flies attended like every rung boot before it, a power cut is the known cost; Q5 rung 4f flies in the same sitting after 4e (one boot each); Q7 if acr-gsp is refused the safety-scheduler triple is the next boot; Q8 vendor firmware stays on bench media only, the repository carries names and digests (the GPL-3 tree and R52 already say so). NOTHING on the GPU line is Peter's except the boots. Executor GA10B5A (Fable, exec-orin-ga10b5a, A63/A64) builds the loader, rung 5a and the ladder beyond (5b ACR→PMU, RUNG6 brief) in new modules beside GA10B4E's probe-file work.
✓ SDWRITE FOLDED ee5e2109 (A57 fixed-unflown: six sites fold onto `sd_block_arg`, a sector the card cannot express is REFUSED; SDARG fixture on virt, go-red proven; A60 OPEN = the veto-lift brief for after USBREG folds: drivers/block.rs:1774 `write_block_tegra_sd` is the refusal, plus fs/fat.rs write_veto arm, bootdisk fixture, Cargo/arroyo `sdwrite` default-on with the shipped-polarity matrix leg; also bind_root's native arm samples `src.write_veto()` not the NativeBackend it mounts — a second finding). A stock esp-jetson card prints no SDARG line by design (witness-free); `rw=no` on the root stays until A60.
✓ APPPIN FOLDED e0b5d31d (SO27 fixed-unflown; SO1/SO10/SO13/SO17 dropped by design): `dock::PinnedApp {Console, Shell}` + a launch post replace SHELL_REOPEN/CONSOLE_REOPEN/CONSOLE_WINDOWED; console tile permanent; both `tegra_shell_remint` copies, `console_reopen_service` and the board-named drains deleted; `wm::shell_remint` kept (wedge-rescue, not a dock route). Next boot: `[dock] launch app=shell by=console-pump win=N gen=G -> LAUNCHED` at boot; tile press `[dock] press … app=shell -> launch`; close `[dock] quit app=shell … -> TORN-DOWN`; relaunch a NEW gen; sprite over the relaunched shell `[cursor12] offer … -> planned|below-session`, never nohit; no `shell-remint` anywhere. Family ledger re-derived (d9e5be86). Fold defect found by the seat's strict check at ee5e2109 and fixed at 83ee4653 (WCDFLOOD selftest call cfg). RENDER14 = the glass fold (APPNAME + DRAGSTALL + APPPIN + SDWRITE's A57), knob line = render13's fifteen minus PROBE4 (the GPU boots are their own images): fold gate running on d9e5be86, logs ~/unaos-bench/scratch/orin-0912b/fold-render14/.
✓ NEXT (the render13 glass items, shared code, branches cut at 2f6adcc4 → fast-forward to the hw-jetson tip first): APPPIN (R49/R50: console and shell as pinned apps; the lost pointer goes with it), appname (R36), dragstall (`[comp2] max_us` 375–588 ms); then the FATCLUST objections (C15) so hw-jetson can land into main.
## CLOSE STATE — orin 27, 2026-09-12 ~04:3xZ
· ORIN-METAL, from render13 (Peter on the glass; the shared-code items are in docs/dev/QUEUE.md §1 and §5):
  – Shut Down NOT done: the deferred GPU rung (4a/4b/4c, `UNAOS_GA10B_PROBE4=4`) has not fired. Next: re-arm the waker on `DEFERRED RUN|BROM-VERDICT|CENSUS-COMPLETE|PSCI SYSTEM_OFF` (`sh ~/unaos-bench/tools/waker.sh`, fresh anchors), have Peter Shut Down (crystal, or `shutdown` in the shell — the pointer is gone), score with docs/dev/evidence/orin27/scorer-ga10b4.sh 4a|4b|4c, commit the capture as docs/dev/evidence/orin27/render13-boot1.log, tick A51/A55 and every flown row. Partial wire: ~/unaos-bench/scratch/orin27/render13-wire-partial.txt (raw.log from byte 40765064).
  – A49: bit18 is NOT the card-detect signal (OVERRIDDEN with the card seated) → the gate moves to the DTB `cd-gpios` GPIO; needs a Tegra GPIO driver (none under arch/aarch64).
  – A53: first native root, `rw=no` — tegra-sd is read-only; writes to `/` fail on a slot boot; the write path is its own arc.
  – A54 BEAM: `NO-RASTER` — none of the candidate registers behaved as a raster line counter (the BEAM RAW lines in the capture say which test each failed); the tearing fix is inert until a counter locks; Peter saw no tearing anyway.
  – A56: the multi-slot reader works (4 LUNs, data card at lun 0, mounted rw at /volumes/UNAOS-DATA). Two readers at once is still a coin flip (trunk queue).
  – A58: card-v2 (FATCLUST) BOOTS from the native slot; the guard/number objections are in the trunk queue §5. hw-jetson carries FATCLUST unlanded.
✓ render13 IS UP on the Orin (native slot, card-v2, KELF max=0x34f820); Shut Down NOT yet done → the deferred GPU rung (4a/4b/4c) has not fired. The waker of this session dies with it: the next session re-arms on `DEFERRED RUN|BROM-VERDICT|CENSUS-COMPLETE|PSCI SYSTEM_OFF` and scores with docs/dev/evidence/orin27/scorer-ga10b4.sh; the partial wire is ~/unaos-bench/scratch/orin27/render13-wire-partial.txt (30,962 lines, from raw.log byte 40765064).
✓ Glass (Peter): apps launched (core 1); some titles/menus say "Application" (A59 owed); console could not reopen after close; shell reopened but the POINTER VANISHED; no tearing (BEAM NO-RASTER — inert); mousing/dragging NOT smooth (`[comp2] max_us` up to 588 ms). R49/R50 rule the fix shape: console+shell = pinned APPS, Quarry = Finder.
✓ Branches CUT, executors NOT spawned (close = stop starting): exec-orin27-appname, exec-orin27-conreopen (→ APPPIN per R49), exec-orin27-dragstall, exec-orin27-cursorlost (unused) — all at 2f6adcc4. Briefs are in the baton.
⚠ FATCLUST (3225b5b0) review OBJECTED: the warning guard greps dosfstools ≤4.1 wording (this host is 4.2: "Number of clusters for 32 bit FAT is less then suggested minimum") so it never fires; `2>&1 |` now discards mkfs stderr; ">/dev/null hid it" is false (stderr was never redirected); A58 says 259,520/112k clusters but the truth is 256,030/110,874; fs/fat.rs MISREADS such a volume as FAT16, it does not refuse. Fix shape: assert the cluster count off the written BPB (the queued C15). Not landed to main; hw-jetson is 6 ahead of main because of it.


## STATE — re-derived 2026-09-12T01:0xZ by orin 27 (local), not relayed
✓ origin/main 084b79ac · origin/hw-jetson 277e2cab · origin/hw-rmbp 73d9d361 · origin/hw-pi4 153c78dd
✓ LOCAL, UNPUSHED: main 50d35cbf (R45-R48 + LAWS + docs/dev/QUEUE.md, the trunk queue) · hw-jetson 059db935
  (f2794266 rung 4 flown + the unafsroot fold) · hw-pi4 3c102d2a (pi-queue.md) · hw-rmbp dc1cc3a2 (rmbp-queue.md) ·
  exec-orin26-unafsroot 5984c69c (merged) · exec-orin27-ga10b4c 6ffb5211 DONE and FOLDED (hw-jetson 9567fdbf): rung 4c + the deferred Shut Down arm (=4), A55; knoboff EXIT 0; scorer 4c go-red proven · exec-orin27-closefold (cut at dff46786, Opus executor live: the x86 WC red — closemin fixture vs VUGTAB, SO26) · exec-orin26-tear 3e9d5625 DONE and FOLDED (hw-jetson 7ff0aa9d): BEAM — gates check/knoboff 0/esp-jetson/test-arm green; A54 · exec-orin27-cmd8 28c99b9b DONE and FOLDED (hw-jetson e813d0d2): card-detect gate before CMD0 (bit18 is the signal; bit16 lies), csd-v2/byte-addressing refusal, A57 truncation row (four sites, 4 GiB ceiling); knoboff EXIT 0. Caveat: bit18=1 with a seated card unproven — render13 prints all three bits · exec-orin27-usblun (cut at 0a22ae40, Opus executor live: the USB mass-storage path addresses LUN 0 only, so a multi-slot reader hides every slot but one — Get Max LUN census, publish the slot that holds a card).
  Push line: `git push origin main hw-jetson hw-pi4 hw-rmbp exec-orin26-unafsroot exec-orin26-tear exec-orin27-ga10b4c exec-orin27-cmd8 exec-orin27-usblun exec-orin27-closefold exec-orin24-fold exec-orin25-shotverb`
✓ TRUNK FOLDED: hw-jetson dff46786 = 895b5dda + main 8c38f753 (arroyo arm-pi union, .gitignore union, LEDGER keeps main's SR12; SR7 is the S33 re-id and stays). Fold gate on dff46786: check-tegra strict rc=0 · check-plain strict rc=0 · **`UNAOS_WC=1 ./arroyo test` rc=1 — `[closemin] close-scope … shell_arm_control=0 -> FAIL` (serial.log:1022): orin's closemin fixture (S34/SO25) meets pi's VUGTAB raise rewrite (d6ed3024, via trunk) — a fold defect, executor exec-orin27-closefold** · test-arm rc=0 · esp-jetson rc=0. Logs ~/unaos-bench/scratch/orin27/fold-dff46786/.
✓ Fold gate on 059db935 GREEN: `UNAOS_TEGRA=1 UNAOS_LEDGER_STRICT=1 UNAOS_K8REACH_STRICT=1 ./arroyo check` rc=0 · plain strict check rc=0 · `./arroyo esp-jetson` rc=0.
✓ A49: three readings in one night — "v1 card" (orin 24) → "driver CMD8 defect on a 2.00 card" (orin 27, from the card's registers) → "EMPTY native slot, card was in the USB reader" (Peter). The executor rewrites the row to the third. render13: native slot EMPTY must print the SKIP line; UNAOS-DATA card in the reader's second slot must appear in the USBLUN census.
✓ Landing order (R45): rmbp LANDED (main 267bd49d) → pi LANDED (main e10d9e4e; hw-pi4 and hw-rmbp level with main) → jetson (review panel running at f0dc8058; main folds into hw-jetson first — merge-tree shows conflicts in .gitignore, LEDGER.md, arroyo).
✓ R46: one focus (orin), ALL LANES, no owner, no support seat, no lane grants. R47: Opus executors, Fable for key tasks only. R48: adversarial handoff.

## RENDER13 — the next boot (line settled 2026-09-12)
✓ **RENDER13 FLEW 2026-09-12 ~04:0xZ from the NATIVE microSD slot** on the card-v2 image (FATCLUST) — boot volume serial 0x8b482cca, KELF max=0x34f820 = the staged identity. Wire so far (desktop up, Peter on the glass, Shut Down pending):
  · A49: `card-detect present=0 … bit18 CD-pin-level=0` with the card SEATED, then `CMD8 SEND_IF_COND ok` → `card-detect OVERRIDDEN — bit18=0 but CMD8 answered; bit18 is not this board's signal`. VERDICT: bit18 is NOT the detect signal on this board; the permanent gate belongs on the DTB `cd-gpios` GPIO (needs a Tegra GPIO driver — its own arc). SDCMD8b's probe-once shape saved the mounts.
  · A53 UNAFSROOT: `[vfs] root = boot volume serial=0x8b482cca source=tegra-sd … unafs=present` → `[vfs] root mount / = native unafs volume source=tegra-sd rw=no` · `/boot` and `/apps` = fat boot volume source=tegra-sd. FIRST native root on the Orin. rw=no because tegra-sd is read-only today.
  · A56 USBLUN: the multi-slot reader declares 4 LUNs — `lun=0/3 "USB SD Reader" 3970048x512 PRESENT` (the UNAOS-DATA card), CF/SM/MS `NO-MEDIUM reason=cap-sense:02/3a/00` — `publish lun=0/3`, `xHCI: Disk … (1938 MiB)`, `[vfs] volume mounted /volumes/UNAOS-DATA source=global rw=yes`. Works on metal.
  · A50/core0: `el0cpus=0x2 lastcpu=1` — the smoke program ran on core 1, not core 0.
  · A54 BEAM: `BEAM VERDICT=NO-RASTER — no candidate word behaved as a raster line counter`; `scanout_beam()` stays None, so the hold is inert and `torn=0` is the OLD duration predicate this boot. The raster register is not at the nouveau offset on this display; next step is the BEAM RAW lines (in the capture) against the facts file.
  · A55: `[ga10bprobe4d] DEFERRED ARM` printed; the rung waits for Shut Down.
  · No PANIC/SError/EXCEPTION/-> FAIL on the wire (32 pattern hits, all prose: `EXC_` register names, the panic-fallback instrument).

⚠ BENCH CHANGE (Peter 2026-09-12 ~03:2xZ): the Orin BOOTS FROM ITS NATIVE microSD SLOT; the boot card is the old 32 GB UNAOS-PI card (SanDisk SS32G 12/2014) re-imaged with UnaOS-orin-card.img (whole-card, Peter's go); the data card rides the multi-slot USB reader, which is then the ONLY USB storage (no two-reader coin flip). Consequences: `/`, `/boot`, `/apps` come from tegra-sd (READ-ONLY today — the wire says rw=); the empty-slot gate must not gate out a seated card (SDCMD8b: probe CMD8 once before SKIP; bit18 unproven with a card); the reader census sees the data card at whatever LUN its slot is.
✓ Knob line (render12's fifteen, minus PROBE3 per arroyo's rung-4 rule, plus the deferred rung 4 and BEAM):
  `UNAOS_TEGRA=1 UNAOS_TEGRA_EL0=1 UNAOS_WITNESS=1 UNAOS_ORINRENDER=1 UNAOS_DESKCASCADE=1 UNAOS_ORINRX=1 UNAOS_HOLOCRON=1 UNAOS_ORINCLICK=1 UNAOS_TCUPROBE=1 UNAOS_TCURX=1 UNAOS_BSPTICK=1 UNAOS_BSPRUN=1 UNAOS_NET4=1 UNAOS_NET5=1 UNAOS_GA10B_PROBE4=4 UNAOS_BEAM=1 ./arroyo esp-jetson` then `./arroyo esp-jetson-img` for the whole-card image; write with `media-writer.sh --image … --write --erase-target` (Peter's go 2026-09-12).
✓ Glass list: EL0 on all cores (core0), apps launch (el0mem), no tearing + `BEAM VERDICT=` (A54), `/` = native unafs volume (A53, needs the re-imaged card), wintitle/closemin/dockid/cursorbg/facet/netlease checks from the render12 fold, native slot EMPTY → `M2: no card in the native slot -> SKIP` (A49), UNAOS-DATA card in the reader's second slot → USBLUN census (A56); then Shut Down → 4a/4b/4c → board off (A55).
✓ SECOND FOLD GATE on f4b4bf3e GREEN (check tegra/plain strict, UNAOS_WC=1 test with closemin PASS, test-arm, esp-jetson, esp-jetson-img) → STAGED `render13-20260912T0259Z-f4b4bf3` (KELF max=0x34f820) with UnaOS-orin-card.img; playbook sent; mark written; waker armed. Second addendum review OBJECTED again on G1 (the census still passes bot_park_gate on every transfer and dead waits charge the park account → USBLUN-M4 in flight, LANDING-blocking, not flight-blocking: the boot is from the native slot and the reader carries only the data card), G2 (A49 v1-card corner, recorded), G3 registry wording, G4 builder comment placement, G5 the x86 test leg run by this seat under R46 (declared in the landing). The "census SKIPPED" literal appears x2 in the armed artifact (nousblun off) — ask M4 why.
✓ USBLUN-M3 69788718 FOLDED (a2915fae) and SDCMD8b 8d77809e FOLDED (cc23cdcd): the addendum objections are answered in code; the second fold gate and the render13 rebuild run on the commit after this one (logs ~/unaos-bench/scratch/orin27/fold-render13b/); the first staged render13 (…-2906747) is SUPERSEDED except for its card image, which Peter writes first.
⚠ (history) ADDENDUM REVIEW (orin 27, second panel) OBJECTED on USBLUN: F1 ep0_resync lacks the Running arm (blind Set TR Dequeue); F3 a LUN answering a transport error made the ladder surrender the whole device — LUN 0's disk lost (its own go-red wire); F2 every stick on every board now gets +1 control +2 bulk transactions with QEMU-only evidence → USBLUN-M3 in flight (fix arms, non-fatal census, `UNAOS_NOUSBLUN=1` opt-out). Non-blocking, recorded: F4 → A54 status cell; F5 the x86 `test` leg run by this seat under R46 is named as such in the landing; F7 bit18-with-card unproven (A49); F8 the `arm-tegra-sdmmcroot` matrix leg leaves trunk with the landing (deleted with the knob at b0536d83, 55 legs vs 47); F9 power.rs names ga10b_probe under a cfg that cannot arm off tegra — borderline R16, not over. render13 is NOT flown on the staged render13-20260912T0221Z-2906747: it carries M2 without M3 and is superseded once M3 folds.
✓ ALL FOUR EXECUTORS FOLDED: tear 7ff0aa9d · 4c 9567fdbf · cmd8 e813d0d2 · closefold 4cd676c2 (SO26) · usblun c055cfdc (A56). FOLD GATE on 29067473 GREEN: check-tegra strict rc=0 · check-plain strict rc=0 · `UNAOS_WC=1 ./arroyo test` rc=0 (closemin PASS with lid_buried=1 shell_arm_unbury=1) · test-arm rc=0 · render13 esp-jetson rc=0 (banner carries beam, ga10bprobe4a/b/c/d, apsrun, sdmmc; artifact: BEAM VERDICT= x3, [ga10bprobe4c] x24, [ga10bprobe4d] x7, HOMESOIL x7, USBLUN x11, "no card in the native slot" x1, [ga10bprobe3] 0) · esp-jetson-img rc=0 (640 MiB, CARDREADY-GEOM 1:0c:2048:260096,2:7f:262144:1048576). Logs ~/unaos-bench/scratch/orin27/fold-render13/. Staged as render13-<UTC>-2906747 with the card image beside it. STOP carried from cmd8: `bootdisk.rs tegra_sd_state` cannot say "absent (no card)" without a flag from sdmmc_tegra.rs — the SKIP line is the witness for now.

## RENDER13 WAS HELD — Peter 2026-09-11 (history)
✓ Specified as fold + core0 + el0slot + mem + tear + unafsroot. Measured: core0/el0slot/mem are at
  ZERO commits, tear is diagnosis-only, unafsroot incomplete. It would have fixed NONE of the four
  glass defects. Held rather than fly a number implying progress it does not have.
✓ orin 27 2026-09-12: RUNG 4 FLOWN, ONE BOOT (Peter 2026-09-11: combine 4a and 4b). Image ga10b4ab-20260912T0019Z-277e2ca at hw-jetson 277e2cab, KELF max=0x238368. 4a `bcrheld=7/7 -> BCR-ALLHELD`; 4b `br_retcode=0x00000002 -> BROM-VERDICT-FAIL` (the PASS), SYSTEM_OFF. Scorer PASS 4a exit 0, PASS 4b exit 0. Evidence docs/dev/evidence/orin27/ga10b4ab-boot1.log; A51 flown. Boot 1 (2026-09-11) REFUSED no-dma-window, fixed by 1629d449. Next rung is a clean-room question (what the ROM wants at fmccode/fmcdata/pkcparam), brief §7.
· (history) orin 26: Peter 2026-09-11 "do the 4 boots". Rung 4a/4b BUILT (exec-orin26-ga10b4 a39fd32d, A51), two images STAGED
  (`ga10b4a-20260911T1809Z-a39fd32` max_vaddr 0x23cae8 → `ga10b4b-20260911T1809Z-a39fd32` 0x23f120), card line dry-run
  refuses only on device readability (needs sudo) and geometry (`--expect-geom 1:0b:2048:63401984`, from render12's FLIGHTID).
  Butler on ttyACM0, MARK ga10b4a written, waker armed. Boot 1 = 4a; boot 2 = 4b only on `-> BCR-ALLHELD`.
✓ tear: DONE (BEAM, A54, folded 7ff0aa9d) · unafsroot: DONE (A53, folded 059db935) · core0/el0mem: folded 277e2cab. Trap for the next image: `./arroyo test-arm` REBUILDS target/aarch64_esp/kernel.elf — build esp-jetson LAST and grep the artifact before any QEMU leg.
· (history) Four defect executors spawned 2026-09-11 off the fold 63654d93 in ~/unaos-bench/scratch/orin26/{core0,el0mem,tear,unafsroot}
  on branches exec-orin26-*; tear carries 7b0a9804 merged, unafsroot carries a81f38c7 merged (one spec conflict, the executor resolves).

## THE FOLD'S FIRST GATE — seven findings, none of them code
✓ 3 malformed ledger rows (SR11: `raised=yes|no`, `src=scene\|flat`, a `|` where an em-dash belongs)
✓ 3 unregistered knobs, all orin's, all NA — `kernel8()` builds its own K8_FEATS and never reads
  the `_feats` block they write (arroyo:6249)
✓ 1 STALE registry row (UNAOS_SDMMCROOT) visible only with UNAOS_K8REACH_STRICT=1 forced — SR13,
  and it was orin 25's own cleared SP5 hit coming back: a feature lives in THREE places
  (declaration, cfg sites, registry) and only two were enumerated.

## NEEDS PETER
✓ **GPU ladder, the fork — RULED (R52, Peter 2026-09-12T17:0xZ: "1"):** the blob rule is amended for one case (LAWS §3 Licence): the GA10B firmware set may be staged unmodified on the Orin's boot media beside its agreement, loaded as data, never linked or committed. Rung 5 is designed for real (executor GA10B5B: enumerate the exact file set from PUBLIC package manifests — names, sizes, package URL, licence file — no download; fill GA10B-RUNG5-BRIEF.md §4.1 and §5). Still Peter's: the download itself (one line, filename, source, size), Q2 attended flight with a possible power cut, Q5 whether rung 4f (BRFETCH FALSE) flies after 4e.
· "One file is the rulebook, EVERYTHING ELSE DELETED" — the absorbing half ran at 3cfe28ad; the
  deletion half has never started. Destructive, R20, and the union is not yet correct.
· D1 (orin-ledger §D) loader `SetMode` to the widest console mode — knob-gated, one power cycle.
· D2 (→S7) `render_service` size-3 family convergence arc. Entry carries an expiry.
· S27 138 prune-candidate origin refs, never OK'd.

## THREE-WAY, AFTER THE PUSH — needs all three seats, nobody edits alone
✓ P14's sentence "no gate can fix this: a checker cannot see another branch" is FALSE of this script
  now: `ledger-check.sh:118` already enumerates `refs/remotes/origin/hw-*` and `origin/main`. rmbp 19
  found it, declined to edit a shared protocol row alone, pi agrees it is stale. **Orin seconds it.**

## ORIN'S OWN, OPEN
✓ `tegra_shell_remint` — R16 board-named AND named after a function it does not call
  (`wm::shell_remint` re-points a row; this mints via `create_at`). Rename is orin's; SR9's
  caller-vs-callee discriminator in GATE-FAMILY is rmbp's.
✓ **SO ids get no suffix form, ever** — a naming constraint, deliberately NOT a gate. The id
  extractor `([A-Z]+[0-9]+)` is unanchored on purpose (to admit `A36 (→ SR2)`), so `SO21b` silently
  aliases to `SO21` and the uniqueness check cannot tell them apart. Found by rmbp 19's mutation.
✓ SO6's row exists now (rmbp filed it under orin's granted id). Four days unwritten, not six weeks —
  the "six weeks" was S27's age, relayed onto the wrong finding.

## STARTED, NOT FINISHED
✓ exec-orin24-tear        7b0a9804  1 commit, diagnosis only. No instrument, no fix.
✓ exec-orin24-unafsroot   a81f38c7  3 commits, incomplete. Owes rmbp the main.rs /
                                    drivers/block.rs diffs BEFORE it moves.
✓ exec-orin24-fold        0cdb3654  holds sdv1 + evidence. NO GATE has ever run on it.
· exec-orin22-bootroot    600887c2  B98 clone-merge fix HALF-APPLIED, uncommitted, in an agent
                                    worktree. Landing-blocking for bootroot.

## CUT AND NEVER BEGUN — zero commits, all at 1b50376a
✓ exec-orin24-{core0, el0slot, mem, small, tools}

## DEFECTS OFF RENDER12 (flown 2026-09-09 · scorer11 6/6 · 0 faults, 0 panics, 3 boots)
✓ EL0 never leaves core 0 (`el0cpus=0x1` every sample)               → core0
✓ 7× `slot 4 backing allocation FAILED`; apps refuse to launch       → el0slot / mem
✓ tearing on the glass while every rollup reads `torn=0`             → tear
✓ `unafs=absent`; `/` does not mount UnaFS                           → unafsroot

## OWED FLIGHTS AND CONTROLS
· FRIEND-DIFF positive control — owed since render11. Needs the data card mounted AND root
  write-locked. Without it that leg has never been shown able to go red.
· Glass checks never evidenced: cursor sweep (no `restore src=` has fired), taskbar order across
  quarry open/close, EL0 placement off core 0 (blocked by core0/el0slot/mem).
· render13 = the fold + core0 + el0slot + mem + tear + unafsroot, one gate, one card line.
✓ GA10B rung 4: FLOWN 2026-09-12, PASS both rungs in one boot (A51). Nothing owed on this rung.

## SMALL FIXES — none started
· shell.rs `screenshot` prints OK with no device (shared core — ask rmbp).
· x86 `[wm] close-scope win=0` on the close-box path (rmbp flag).
· wintitle out-of-lane leftovers (SO22): `app_name_forget` from `teardown_user_slot`, two x86
  launchers minting unnamed windows, `b"orin"` title on the display_tegra test pattern.
· `read_block_at` `(lba*512) as u32` truncation above 4 GiB — flagged by sdv1, never ledgered.
· NIC descriptor-17 OPEN (R19); net5V REFETCH-WRONGSLOT=3 carried since render8.

## TOOLING / AUDIT
· scorer11 STAMP-MATCH leg (wire `sha=` == card elf stamp, WRONG-IMAGE exit 1).
· orin-specscore.py / mbench --self-test are run by NO verb (ARCH-CONFORMANCE 14).
· 28 board-named shared scripts — R4 scour, its own arc.
· media-writer.sh is un-versioned (rmbp B97).
· winmenu APP_OWNER unregistered WinId holder (pi 10's question) — prove or refute.

## BENCH FACTS THIS ROUND (orin 27, 2026-09-12)
· **The Orin UEFI does not list the re-imaged microSD (MBR: p1 0x0c FAT32 127 MiB + p2 0x7f UnaFS) as a bootable disk in the native slot** (Peter 2026-09-12 03:3xZ: "the orin does not see it as bootable disk"); the same firmware booted an MBR FAT32 card from the USB reader on the rung-4 flights, and the slot reads this very card (SS32G) at the kernel level. CAUSE FOUND (A58): the image's FAT32 volume had 32,440 clusters — FAT16 by the spec — so EDK2 refused it (Linux trusts the BPB and mounted it, so every writer check passed). FATCLUST (`make-pi-img.sh -s 1`, 3225b5b0) fixes the image; the GPT hypothesis is withdrawn. Writer gap → trunk queue: a FAT32-by-cluster-count check on the image.
· A whole-card image NEVER lives inside a `--src` staged tree: `media-writer.sh --src` copies the entire tree onto the 127 MiB FAT, so the 640 MiB `UnaOS-orin-card.img` filled the volume mid-copy ("No space left on device", 1 of 11 files differ, card left mounted). Stage it BESIDE the tree as `<name>.card.img` and list it in the top-level MANIFEST only. Writer gap to close (trunk queue): a C14 "the staged tree fits the target FAT" preflight, and an unmount-on-failure that does not leave a partial file behind.
· The idle Debug Probe port drips single NUL bytes: a GROWTH waker can false-fire on one byte; re-anchor and re-arm.

## STANDING — how this seat reads, after a day of getting it wrong
    git log --all --not HEAD -- <file>                    # what I lack: an enumeration, not a pick
    git ls-tree -r --name-only <ref> -- docs/ | comm -23 - <mine>   # files I don't know exist
    UNAOS_LEDGER_STRICT=1 ./arroyo check                  # a detached HEAD gets the WEAK gate
  Never `| head` on evidence you are about to quantify over. Never a number from memory.
  The population a gate checks is not the population a seat greps (pi 10, 2026-09-10).
