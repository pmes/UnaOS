# ORIN — WHAT IS IN FLIGHT AND WHAT IS OWED

One file, fixed path, not per-round. Kept current AS WORK HAPPENS, never "at close":
a list written only at close is never written at all by a session that does not reach one,
and a session ends when the work is done or when it is not worth continuing.
`✓` = verified in this tree.  `·` = inherited, NOT re-checked.  Re-derive every sha before acting.
Last touched 2026-09-12T15:1xZ: render13 boot 2 FLOWN — rung 4a/4b/4c PASS from the shutdown path, board off; BEAM LOCKED; nothing left to test on this card (Peter).

## CLOSE STATE — 2026-09-12T19:xxZ (spend wall; Peter: "6% left until we hit the 5 hour wall")
✓ hw-jetson 369130f5 (local, UNPUSHED since 9ec96894): the render13 close-out, C15, R51/R52/R53, the folds APPNAME ecd3bc79 · DRAGSTALL 4465eb20 · SDWRITE ee5e2109 · GA10B5 ec20ee6a · GA10B5B 186ac5d5 · APPPIN e0b5d31d, the family ledger d9e5be86, the WCDFLOOD cfg fix 83ee4653. main 198376dd (local, unpushed since b8e45320: the trunk-queue rows). Push line: `git push origin main hw-jetson exec-orin27-appname exec-orin27-dragstall exec-orin27-conreopen exec-orin-sdwrite exec-orin-ga10b5 exec-orin-ga10b5b exec-orin-login exec-orin-net6 exec-orin-usbreg exec-orin-ga10b4e exec-orin-serdrain exec-orin-ga10b5a`.
✓ RENDER14 fold gate RUNNING DETACHED on 369130f5 (six steps, logs ~/unaos-bench/scratch/orin-0912b/fold-render14/, rc.txt fills in; `DONE` at the end). Next session: read rc.txt; if all six rc=0, certify the artifact (`LC_ALL=C grep -a -o -F` for `[dock] launch app=`, `from=program`, `wcd_skips=`, `shell-remint`=0), stage with stage-orin.sh (knob line in the RENDER14 line below), fat-clusters.sh on the card image, media-writer dry runs, playbook, mark, waker — then one card for Peter. If any step is red: the log names the leg; fix on hw-jetson, re-run the chain.
✓ EXECUTORS at the wall: told to WIP-commit on their branches and stop (LOGIN M1 built+proven, three granted files, M2-M4 not started; NET6; USBREG; GA10B4E rung 4e/4f; SERDRAIN; GA10B5A rung 5a). Each branch's tip is the handoff; a "WIP UNGATED:" commit has NOT run its gates — re-run them before folding. Their briefs: ~/unaos-bench/scratch/orin-0912b/briefs/*.md (bench scratch, not git).
✓ BENCH: board dark since the render13 boot-2 SYSTEM_OFF; butler host pid 27246 holds /dev/ttyACM0; no waker. Firmware set staged at ~/unaos-bench/flash/orin/ga10b-fw-36.4.3/ (R52).
✓ WHAT I MAY HAVE MISSED (R48): (1) the wm.rs union of APPNAME+DRAGSTALL+APPPIN is proven only by the running chain — no human read the three-way; (2) the render14 knob line drops UNAOS_GA10B_PROBE4 — if Peter wants the shutdown rung re-proven with the ring drain, it is not on this card; (3) SERDRAIN's cap and APPPIN's console pump are unflown; (4) the A60 veto lift waits on USBREG, which is now WIP; (5) ledger-check accepted conflict markers once (trunk queue §5) — every fold this session was checked by hand after that.

## STATE — 2026-09-12T15:1xZ
✓ main b8e45320 = origin · hw-jetson 9ec96894 = origin, 14 ahead of main (FATCLUST 3225b5b0, A58, R49/R50, queue docs, LAWS/PLAYBOOK merges) · hw-pi4 e6e71c9f = origin · hw-rmbp c3890cf6 = origin. Every worktree clean. Nothing was owed a push at open (the retired baton's push line was already done).
✓ **render13 BOOT 2 FLOWN 2026-09-12T15:0xZ — RUNG 4a/4b/4c PASS FROM THE SHUTDOWN PATH, BOARD OFF.** Same card (native slot, `KELF min=0x0 max=0x34f820`, boot volume serial 0x8b482cca). Peter typed `shutdown` in the shell → `[pwrshutoff] shutdown verb invoked` → `[ga10bprobe4d] DEFERRED RUN` → 4a `bcrheld=7/7 -> BCR-ALLHELD` → `PRECENSUS-DONE` → 4b `br_retcode=0x00000002 -> BROM-VERDICT-FAIL` (the PASS) → 4c `POSTBCR-INTACT`, census 21/30 readable changed=1 (retcode only), `DMABUF-UNTOUCHED`, `CENSUS-COMPLETE` → `PSCI SYSTEM_OFF` → firmware "Shutdown state requested 0". `scorer-ga10b4.sh` 4a/4b/4c rc=0 PASS each. Evidence `docs/dev/evidence/orin27/render13-boot2.log` (3,049 lines from the boot-2 mark). A55 and A54 ticked flown. Marks: `boot2 armed` and `boot2 flown` in marks.txt. Board dark; cold boot next. Butler host pid 27246 holds /dev/ttyACM0; waker not running (nothing to wait for).
✓ BEAM: boot 2 `BEAM VERDICT=LOCKED head=0 half=lo @0x13806330 vtotal=1235 period_us=16667` with 52 `beam=` rollups (`torn=no -> BUFFERED`); boot 1 of the SAME image said NO-RASTER. The candidate search is not repeatable boot to boot — A54 carries the question. Peter saw no tearing on either boot.
✓ CORRECTION to the CLOSE STATE below: render13 boot 1 never reached Shut Down because the pointer was lost after the shell re-mint (the crystal menu was unreachable) and Peter powered the board off in the idle desktop at up≈740s; the wire ended mid-line. That was a power cut, not a shutdown.
✓ Peter (2026-09-12T15:0xZ): the DHCP lease worked on earlier boots and EVERY render12-fold GUI check (close leaves siblings, cursor sweep, dock tiles, Facet PNG) is confirmed fixed on the glass. The shutdown rung was the last test on this card. NOTHING IS LEFT TO FLY ON render13.
✓ NEW on the render13 boot-1 wire (shared code → filed on the trunk queue §1/§5 too): (1) `[wc-d] verify win=1 -> SKIP (no memory for 960x272 source snapshot)` (wm.rs:7877) prints once per compositor pass while such a window exists: 46,161 of 67,185 lines, and the serial staging ring dropped 5,331 lines in 192 events under it — a per-pass witness must latch, not repeat; (2) the shutdown path (power.rs `platform_shutdown`/`crystal_shutdown` → `psci_call`) prints through the same ring and drains nothing before the SMC, so on a flooded ring the rung's lines can die with the power (boot 2 was quiet: one 23-line drop before the verb, none in the rung); (3) the banner `crystal LIVE … Sleep/Restart/Shut Down print their honest unimplemented lines — no PSCI wiring on this track yet` (video/desktop_firmware.rs:356, main.rs:8547) is stale: video/crystal.rs:693 calls `power::crystal_shutdown` → PSCI SYSTEM_OFF.
✓ R51 (2026-09-12T15:2xZ, Peter): go on all three glass executors, plus 3D, net, the open hardware rows, and MULTI-USER as a line (login, home folder). EIGHT executors live off hw-jetson (three at a475fd6f, five at cc3ca3e8), briefs in the session scratch: APPPIN (Fable; exec-orin27-conreopen; R49/R50 + the lost pointer; SO27) · APPNAME (exec-orin27-appname; R36; SO28) · DRAGSTALL (exec-orin27-dragstall; the 375-588 ms passes + the wc-d latch; SO29/SO30) · LOGIN (Fable; exec-orin-login; a human user, login screen, /home/<user>, session principal, logout; SO32) · SDWRITE (exec-orin-sdwrite; lift the TegraSd write veto so `/` is rw, A57 refusal, a metal SDWRITE fixture; A60) · NET6 (exec-orin-net6; which stack leased, ping/arp/dns verbs, the EL0 socket family on the tegra NIC via virt, the descriptor-17 row; A59) · USBREG (exec-orin-usbreg; the block registry becomes an array keyed (slot, lun); SO33) · GA10B5 (docs only; exec-orin-ga10b5; the rung-5 brief: FMC/BCR layout from public sources, the blob-free fork, the L4T licence facts for Peter, A61). Fold order when they report: glass three first (one fold gate, one card: render14), then the rest as they land. Ledger ids reserved above; SO31 free.
✓ C15 DONE (2026-09-12T16:3xZ, this session, hw-jetson 93ba7fda): `unaos/scripts/fat-clusters.sh` asserts FAT32-by-count off the WRITTEN BPB inside make-pi-img.sh (red on the refused render13 image, green on card-v2 and on a fresh 128 MiB build); A58 corrected (256,030 / 110,874; fs/fat.rs misreads) and flown. Bench writer `~/unaos-bench/tools/media-writer.sh` gained C15-FAT32-BY-COUNT in --src and --image modes with the `spc8.img` fixture: selftest 45 passed, 0 failed (was 43/0); backup `media-writer.sh.bak-c15-<UTC>`. The FATCLUST review objections are answered; nothing blocks landing hw-jetson into main except the executors in flight.
✓ GA10B5 FOLDED ec20ee6a (docs only; A61 open): the rung-5 brief. Findings: no public source supports a blob-free path to any GA10B engine (nouveau's Tegra map stops at GV11B; Turing+ engines come up only through NVIDIA-signed firmware; GA10B has no display engine of its own; our die held priv-lockdown across the ignition); `DMABUF-UNTOUCHED` proves the ROM did not WRITE our window, not that it did not read it; `bcr_ctrl 0x110` decodes as BRFETCH TRUE, CORE_SELECT RISCV, VALID FALSE (MIT header), so 4b's delta was exactly "mark valid"; the MIT Hopper bootstrap writes the BCR DMA addresses `>> 8` where 4b wrote them raw. Executor GA10B4E (Opus, exec-orin-ga10b4e, A62) builds rung 4e (shift) and 4f (BRFETCH FALSE) as knob values 5 and 6 of UNAOS_GA10B_PROBE4; each is one boot ending in SYSTEM_OFF.
✓ APPNAME FOLDED ecd3bc79 (SO28, fixed-unflown): the launch seam already armed the name (6915c256); the child reached SYS_WIN_CREATE first about half the time (BG-SPREAD), so the arm is retroactive now (`app_name_adopt`). Next boot: `[winmenu] app-menu owner=N name=VUG from=program` for every launched ELF, `from=declared` for Console/Quarry/Pulse/Shell, no `name=Application`. knoboff wc exit 1 accepted: wm.rs is unconditional and R36 asks for a default-path change. SDWRITE: the veto lift STOPPED (the refusal is drivers/block.rs:1774 `write_block_tegra_sd`, unconditional; needs block.rs, bootdisk.rs, Cargo.toml, main.rs — USBREG's files); scoped to A57 + SDARG for this commit, A60 carries the five-part plan for after USBREG folds.
✓ DRAGSTALL FOLDED 4465eb20 (+7006857f: the fold committed three conflict markers in LEDGER.md — removed; ledger-check did NOT refuse them, a gate gap filed on the trunk queue §5). MECHANISM (SO29, open): `[comp2] max_us` measures the serial staging-ring DRAIN — `_print` runs IRQ-masked and the core that wins SERIAL_PORT drains up to 64 staged lines byte by byte at 86.8 us/byte (64 x 68 B = 377.8 ms; the observed band 375-618 ms is one drain wide); not the present (maxpresent 7.7 ms), not the sprite, not A46. The producer was the wc-d OOM SKIP line, once per pass (46,161 lines, ~36% of the wire) — LATCHED now (SO30, fixed-unflown; `wcd_skips=` on the rollup). Executor SERDRAIN (exec-orin-serdrain, SO31) takes the rest: cap one drain's BYTES at the point of use, aarch64 SERWIT-1B backpressure parity (x86-only today), and the drain before the power SMC (trunk queue item f).
✓ GA10B5B FOLDED: the ga10b firmware set is 17 files (public file list of a package whose upstream version string is NVIDIA's own, 36.4.3); the triple the ROM wants is INFERRED as acr-gsp.text/.data/.manifest (three files, placements ours at 256-byte alignment); the 2 MiB window is plausible, unproven (sizes need the bytes; P8 REFUSES image-too-large). Licence: 1.1(d) redistribution with the agreement reaching each recipient; 2.1 no translating out of architecture; 2.6 no circumventing authentication (5a hands the ROM a genuine image); 2.9 is why "staged beside, never linked" is the only GPL-3.0 shape. DOWNLOADED BY PETER 2026-09-12 (sha256 verified, 17 files, staged unmodified with the agreement at `~/unaos-bench/flash/orin/ga10b-fw-36.4.3/`; the acr-gsp triple is 28,672 / 9,472 / 2,048 B — A61 has the digests; the window question is closed for size). The line was: `curl -fL -O https://repo.download.nvidia.com/jetson/t234/pool/main/n/nvidia-l4t-firmware/nvidia-l4t-firmware_36.4.3-20250107174145_arm64.deb` — 2,295,852 bytes, SHA256 ec89d6a5c449bc6e7e89127bc7390bea7a113a69f92a4dc15462e9e943fad3ab (NVIDIA's Packages index); it carries lib/firmware/nvidia/ga10b/* AND the agreement (usr/share/doc/nvidia-l4t-firmware/copyright). DECIDED BY THE SESSION under R53 (2026-09-12T18:xxZ; Peter: "you already know our license complies why ask"): Q2 rung 5a flies attended like every rung boot before it, a power cut is the known cost; Q5 rung 4f flies in the same sitting after 4e (one boot each); Q7 if acr-gsp is refused the safety-scheduler triple is the next boot; Q8 vendor firmware stays on bench media only, the repository carries names and digests (the GPL-3 tree and R52 already say so). NOTHING on the GPU line is Peter's except the boots. Executor GA10B5A (Fable, exec-orin-ga10b5a, A63/A64) builds the loader, rung 5a and the ladder beyond (5b ACR→PMU, RUNG6 brief) in new modules beside GA10B4E's probe-file work.
✓ SDWRITE FOLDED ee5e2109 (A57 fixed-unflown: six sites fold onto `sd_block_arg`, a sector the card cannot express is REFUSED; SDARG fixture on virt, go-red proven; A60 OPEN = the veto-lift brief for after USBREG folds: drivers/block.rs:1774 `write_block_tegra_sd` is the refusal, plus fs/fat.rs write_veto arm, bootdisk fixture, Cargo/arroyo `sdwrite` default-on with the shipped-polarity matrix leg; also bind_root's native arm samples `src.write_veto()` not the NativeBackend it mounts — a second finding). A stock esp-jetson card prints no SDARG line by design (witness-free); `rw=no` on the root stays until A60.
✓ APPPIN FOLDED e0b5d31d (SO27 fixed-unflown; SO1/SO10/SO13/SO17 dropped by design): `dock::PinnedApp {Console, Shell}` + a launch post replace SHELL_REOPEN/CONSOLE_REOPEN/CONSOLE_WINDOWED; console tile permanent; both `tegra_shell_remint` copies, `console_reopen_service` and the board-named drains deleted; `wm::shell_remint` kept (wedge-rescue, not a dock route). Next boot: `[dock] launch app=shell by=console-pump win=N gen=G -> LAUNCHED` at boot; tile press `[dock] press … app=shell -> launch`; close `[dock] quit app=shell … -> TORN-DOWN`; relaunch a NEW gen; sprite over the relaunched shell `[cursor12] offer … -> planned|below-session`, never nohit; no `shell-remint` anywhere. Family ledger re-derived (d9e5be86). Fold defect found by the seat's strict check at ee5e2109 and fixed at 83ee4653 (WCDFLOOD selftest call cfg). RENDER14 = the glass fold (APPNAME + DRAGSTALL + APPPIN + SDWRITE's A57), knob line = render13's fifteen minus PROBE4 (the GPU boots are their own images): fold gate running on d9e5be86, logs ~/unaos-bench/scratch/orin-0912b/fold-render14/.
✓ NEXT (the render13 glass items, shared code, branches cut at 2f6adcc4 → fast-forward to the hw-jetson tip first): APPPIN (R49/R50: console and shell as pinned apps; the lost pointer goes with it), appname (R36), dragstall (`[comp2] max_us` 375–588 ms); then the FATCLUST objections (C15) so hw-jetson can land into main.
## CLOSE STATE — orin 27, 2026-09-12 ~04:3xZ
· ORIN-METAL, from render13 (Peter on the glass; the shared-code items are in docs/dev/QUEUE.md §1 and §5):
  – Shut Down NOT done: the deferred GPU rung (4a/4b/4c, `UNAOS_GA10B_PROBE4=4`) has not fired. Next: re-arm the waker on `DEFERRED RUN|BROM-VERDICT|CENSUS-COMPLETE|PSCI SYSTEM_OFF` (`sh ~/unaos-bench/tools/waker.sh`, fresh anchors), have Peter Shut Down (crystal, or `shutdown` in the shell — the pointer is gone), score with docs/dev/evidence/orin27/scorer-ga10b4.sh 4a|4b|4c, commit the capture as docs/dev/evidence/orin27/render13-boot1.log, tick A51/A55 and every flown row. Partial wire: ~/unaos-bench/scratch/orin27/render13-wire-partial.txt (raw.log from byte 40765064).
  – A49: bit18 is NOT the card-detect signal (OVERRIDDEN with the card seated) → the gate moves to the DTB `cd-gpios` GPIO; needs a Tegra GPIO driver (none under arch/aarch64).
  – A53: first native root, `rw=no` — tegra-sd is read-only; writes to `/` fail on a slot boot; the write path is its own arc.
  – A54 BEAM: `NO-RASTER` — none of the candidate registers behaved as a raster line counter (the BEAM RAW lines in the capture say which test each failed); the tearing fix is inert until a counter locks; Peter saw no tearing anyway.
  – A56: the multi-slot reader works (4 LUNs, data card at lun 0, mounted rw at /volumes/UNAOS-DATA). Two readers at once is still a coin flip (trunk queue).
  – A58: card-v2 (FATCLUST) BOOTS from the native slot; the guard/number objections are in the trunk queue §5. hw-jetson carries FATCLUST unlanded.
✓ render13 IS UP on the Orin (native slot, card-v2, KELF max=0x34f820); Shut Down NOT yet done → the deferred GPU rung (4a/4b/4c) has not fired. The waker of this session dies with it: the next session re-arms on `DEFERRED RUN|BROM-VERDICT|CENSUS-COMPLETE|PSCI SYSTEM_OFF` and scores with docs/dev/evidence/orin27/scorer-ga10b4.sh; the partial wire is ~/unaos-bench/scratch/orin27/render13-wire-partial.txt (30,962 lines, from raw.log byte 40765064).
✓ Glass (Peter): apps launched (core 1); some titles/menus say "Application" (A59 owed); console could not reopen after close; shell reopened but the POINTER VANISHED; no tearing (BEAM NO-RASTER — inert); mousing/dragging NOT smooth (`[comp2] max_us` up to 588 ms). R49/R50 rule the fix shape: console+shell = pinned APPS, Quarry = Finder.
✓ Branches CUT, executors NOT spawned (close = stop starting): exec-orin27-appname, exec-orin27-conreopen (→ APPPIN per R49), exec-orin27-dragstall, exec-orin27-cursorlost (unused) — all at 2f6adcc4. Briefs are in the baton.
⚠ FATCLUST (3225b5b0) review OBJECTED: the warning guard greps dosfstools ≤4.1 wording (this host is 4.2: "Number of clusters for 32 bit FAT is less then suggested minimum") so it never fires; `2>&1 |` now discards mkfs stderr; ">/dev/null hid it" is false (stderr was never redirected); A58 says 259,520/112k clusters but the truth is 256,030/110,874; fs/fat.rs MISREADS such a volume as FAT16, it does not refuse. Fix shape: assert the cluster count off the written BPB (the queued C15). Not landed to main; hw-jetson is 6 ahead of main because of it.


## STATE — re-derived 2026-09-12T01:0xZ by orin 27 (local), not relayed
✓ origin/main 084b79ac · origin/hw-jetson 277e2cab · origin/hw-rmbp 73d9d361 · origin/hw-pi4 153c78dd
✓ LOCAL, UNPUSHED: main 50d35cbf (R45-R48 + LAWS + docs/dev/QUEUE.md, the trunk queue) · hw-jetson 059db935
  (f2794266 rung 4 flown + the unafsroot fold) · hw-pi4 3c102d2a (pi-queue.md) · hw-rmbp dc1cc3a2 (rmbp-queue.md) ·
  exec-orin26-unafsroot 5984c69c (merged) · exec-orin27-ga10b4c 6ffb5211 DONE and FOLDED (hw-jetson 9567fdbf): rung 4c + the deferred Shut Down arm (=4), A55; knoboff EXIT 0; scorer 4c go-red proven · exec-orin27-closefold (cut at dff46786, Opus executor live: the x86 WC red — closemin fixture vs VUGTAB, SO26) · exec-orin26-tear 3e9d5625 DONE and FOLDED (hw-jetson 7ff0aa9d): BEAM — gates check/knoboff 0/esp-jetson/test-arm green; A54 · exec-orin27-cmd8 28c99b9b DONE and FOLDED (hw-jetson e813d0d2): card-detect gate before CMD0 (bit18 is the signal; bit16 lies), csd-v2/byte-addressing refusal, A57 truncation row (four sites, 4 GiB ceiling); knoboff EXIT 0. Caveat: bit18=1 with a seated card unproven — render13 prints all three bits · exec-orin27-usblun (cut at 0a22ae40, Opus executor live: the USB mass-storage path addresses LUN 0 only, so a multi-slot reader hides every slot but one — Get Max LUN census, publish the slot that holds a card).
  Push line: `git push origin main hw-jetson hw-pi4 hw-rmbp exec-orin26-unafsroot exec-orin26-tear exec-orin27-ga10b4c exec-orin27-cmd8 exec-orin27-usblun exec-orin27-closefold exec-orin24-fold exec-orin25-shotverb`
✓ TRUNK FOLDED: hw-jetson dff46786 = 895b5dda + main 8c38f753 (arroyo arm-pi union, .gitignore union, LEDGER keeps main's SR12; SR7 is the S33 re-id and stays). Fold gate on dff46786: check-tegra strict rc=0 · check-plain strict rc=0 · **`UNAOS_WC=1 ./arroyo test` rc=1 — `[closemin] close-scope … shell_arm_control=0 -> FAIL` (serial.log:1022): orin's closemin fixture (S34/SO25) meets pi's VUGTAB raise rewrite (d6ed3024, via trunk) — a fold defect, executor exec-orin27-closefold** · test-arm rc=0 · esp-jetson rc=0. Logs ~/unaos-bench/scratch/orin27/fold-dff46786/.
✓ Fold gate on 059db935 GREEN: `UNAOS_TEGRA=1 UNAOS_LEDGER_STRICT=1 UNAOS_K8REACH_STRICT=1 ./arroyo check` rc=0 · plain strict check rc=0 · `./arroyo esp-jetson` rc=0.
✓ A49: three readings in one night — "v1 card" (orin 24) → "driver CMD8 defect on a 2.00 card" (orin 27, from the card's registers) → "EMPTY native slot, card was in the USB reader" (Peter). The executor rewrites the row to the third. render13: native slot EMPTY must print the SKIP line; UNAOS-DATA card in the reader's second slot must appear in the USBLUN census.
✓ Landing order (R45): rmbp LANDED (main 267bd49d) → pi LANDED (main e10d9e4e; hw-pi4 and hw-rmbp level with main) → jetson (review panel running at f0dc8058; main folds into hw-jetson first — merge-tree shows conflicts in .gitignore, LEDGER.md, arroyo).
✓ R46: one focus (orin), ALL LANES, no owner, no support seat, no lane grants. R47: Opus executors, Fable for key tasks only. R48: adversarial handoff.

## RENDER13 — the next boot (line settled 2026-09-12)
✓ **RENDER13 FLEW 2026-09-12 ~04:0xZ from the NATIVE microSD slot** on the card-v2 image (FATCLUST) — boot volume serial 0x8b482cca, KELF max=0x34f820 = the staged identity. Wire so far (desktop up, Peter on the glass, Shut Down pending):
  · A49: `card-detect present=0 … bit18 CD-pin-level=0` with the card SEATED, then `CMD8 SEND_IF_COND ok` → `card-detect OVERRIDDEN — bit18=0 but CMD8 answered; bit18 is not this board's signal`. VERDICT: bit18 is NOT the detect signal on this board; the permanent gate belongs on the DTB `cd-gpios` GPIO (needs a Tegra GPIO driver — its own arc). SDCMD8b's probe-once shape saved the mounts.
  · A53 UNAFSROOT: `[vfs] root = boot volume serial=0x8b482cca source=tegra-sd … unafs=present` → `[vfs] root mount / = native unafs volume source=tegra-sd rw=no` · `/boot` and `/apps` = fat boot volume source=tegra-sd. FIRST native root on the Orin. rw=no because tegra-sd is read-only today.
  · A56 USBLUN: the multi-slot reader declares 4 LUNs — `lun=0/3 "USB SD Reader" 3970048x512 PRESENT` (the UNAOS-DATA card), CF/SM/MS `NO-MEDIUM reason=cap-sense:02/3a/00` — `publish lun=0/3`, `xHCI: Disk … (1938 MiB)`, `[vfs] volume mounted /volumes/UNAOS-DATA source=global rw=yes`. Works on metal.
  · A50/core0: `el0cpus=0x2 lastcpu=1` — the smoke program ran on core 1, not core 0.
  · A54 BEAM: `BEAM VERDICT=NO-RASTER — no candidate word behaved as a raster line counter`; `scanout_beam()` stays None, so the hold is inert and `torn=0` is the OLD duration predicate this boot. The raster register is not at the nouveau offset on this display; next step is the BEAM RAW lines (in the capture) against the facts file.
  · A55: `[ga10bprobe4d] DEFERRED ARM` printed; the rung waits for Shut Down.
  · No PANIC/SError/EXCEPTION/-> FAIL on the wire (32 pattern hits, all prose: `EXC_` register names, the panic-fallback instrument).

⚠ BENCH CHANGE (Peter 2026-09-12 ~03:2xZ): the Orin BOOTS FROM ITS NATIVE microSD SLOT; the boot card is the old 32 GB UNAOS-PI card (SanDisk SS32G 12/2014) re-imaged with UnaOS-orin-card.img (whole-card, Peter's go); the data card rides the multi-slot USB reader, which is then the ONLY USB storage (no two-reader coin flip). Consequences: `/`, `/boot`, `/apps` come from tegra-sd (READ-ONLY today — the wire says rw=); the empty-slot gate must not gate out a seated card (SDCMD8b: probe CMD8 once before SKIP; bit18 unproven with a card); the reader census sees the data card at whatever LUN its slot is.
✓ Knob line (render12's fifteen, minus PROBE3 per arroyo's rung-4 rule, plus the deferred rung 4 and BEAM):
  `UNAOS_TEGRA=1 UNAOS_TEGRA_EL0=1 UNAOS_WITNESS=1 UNAOS_ORINRENDER=1 UNAOS_DESKCASCADE=1 UNAOS_ORINRX=1 UNAOS_HOLOCRON=1 UNAOS_ORINCLICK=1 UNAOS_TCUPROBE=1 UNAOS_TCURX=1 UNAOS_BSPTICK=1 UNAOS_BSPRUN=1 UNAOS_NET4=1 UNAOS_NET5=1 UNAOS_GA10B_PROBE4=4 UNAOS_BEAM=1 ./arroyo esp-jetson` then `./arroyo esp-jetson-img` for the whole-card image; write with `media-writer.sh --image … --write --erase-target` (Peter's go 2026-09-12).
✓ Glass list: EL0 on all cores (core0), apps launch (el0mem), no tearing + `BEAM VERDICT=` (A54), `/` = native unafs volume (A53, needs the re-imaged card), wintitle/closemin/dockid/cursorbg/facet/netlease checks from the render12 fold, native slot EMPTY → `M2: no card in the native slot -> SKIP` (A49), UNAOS-DATA card in the reader's second slot → USBLUN census (A56); then Shut Down → 4a/4b/4c → board off (A55).
✓ SECOND FOLD GATE on f4b4bf3e GREEN (check tegra/plain strict, UNAOS_WC=1 test with closemin PASS, test-arm, esp-jetson, esp-jetson-img) → STAGED `render13-20260912T0259Z-f4b4bf3` (KELF max=0x34f820) with UnaOS-orin-card.img; playbook sent; mark written; waker armed. Second addendum review OBJECTED again on G1 (the census still passes bot_park_gate on every transfer and dead waits charge the park account → USBLUN-M4 in flight, LANDING-blocking, not flight-blocking: the boot is from the native slot and the reader carries only the data card), G2 (A49 v1-card corner, recorded), G3 registry wording, G4 builder comment placement, G5 the x86 test leg run by this seat under R46 (declared in the landing). The "census SKIPPED" literal appears x2 in the armed artifact (nousblun off) — ask M4 why.
✓ USBLUN-M3 69788718 FOLDED (a2915fae) and SDCMD8b 8d77809e FOLDED (cc23cdcd): the addendum objections are answered in code; the second fold gate and the render13 rebuild run on the commit after this one (logs ~/unaos-bench/scratch/orin27/fold-render13b/); the first staged render13 (…-2906747) is SUPERSEDED except for its card image, which Peter writes first.
⚠ (history) ADDENDUM REVIEW (orin 27, second panel) OBJECTED on USBLUN: F1 ep0_resync lacks the Running arm (blind Set TR Dequeue); F3 a LUN answering a transport error made the ladder surrender the whole device — LUN 0's disk lost (its own go-red wire); F2 every stick on every board now gets +1 control +2 bulk transactions with QEMU-only evidence → USBLUN-M3 in flight (fix arms, non-fatal census, `UNAOS_NOUSBLUN=1` opt-out). Non-blocking, recorded: F4 → A54 status cell; F5 the x86 `test` leg run by this seat under R46 is named as such in the landing; F7 bit18-with-card unproven (A49); F8 the `arm-tegra-sdmmcroot` matrix leg leaves trunk with the landing (deleted with the knob at b0536d83, 55 legs vs 47); F9 power.rs names ga10b_probe under a cfg that cannot arm off tegra — borderline R16, not over. render13 is NOT flown on the staged render13-20260912T0221Z-2906747: it carries M2 without M3 and is superseded once M3 folds.
✓ ALL FOUR EXECUTORS FOLDED: tear 7ff0aa9d · 4c 9567fdbf · cmd8 e813d0d2 · closefold 4cd676c2 (SO26) · usblun c055cfdc (A56). FOLD GATE on 29067473 GREEN: check-tegra strict rc=0 · check-plain strict rc=0 · `UNAOS_WC=1 ./arroyo test` rc=0 (closemin PASS with lid_buried=1 shell_arm_unbury=1) · test-arm rc=0 · render13 esp-jetson rc=0 (banner carries beam, ga10bprobe4a/b/c/d, apsrun, sdmmc; artifact: BEAM VERDICT= x3, [ga10bprobe4c] x24, [ga10bprobe4d] x7, HOMESOIL x7, USBLUN x11, "no card in the native slot" x1, [ga10bprobe3] 0) · esp-jetson-img rc=0 (640 MiB, CARDREADY-GEOM 1:0c:2048:260096,2:7f:262144:1048576). Logs ~/unaos-bench/scratch/orin27/fold-render13/. Staged as render13-<UTC>-2906747 with the card image beside it. STOP carried from cmd8: `bootdisk.rs tegra_sd_state` cannot say "absent (no card)" without a flag from sdmmc_tegra.rs — the SKIP line is the witness for now.

## RENDER13 WAS HELD — Peter 2026-09-11 (history)
✓ Specified as fold + core0 + el0slot + mem + tear + unafsroot. Measured: core0/el0slot/mem are at
  ZERO commits, tear is diagnosis-only, unafsroot incomplete. It would have fixed NONE of the four
  glass defects. Held rather than fly a number implying progress it does not have.
✓ orin 27 2026-09-12: RUNG 4 FLOWN, ONE BOOT (Peter 2026-09-11: combine 4a and 4b). Image ga10b4ab-20260912T0019Z-277e2ca at hw-jetson 277e2cab, KELF max=0x238368. 4a `bcrheld=7/7 -> BCR-ALLHELD`; 4b `br_retcode=0x00000002 -> BROM-VERDICT-FAIL` (the PASS), SYSTEM_OFF. Scorer PASS 4a exit 0, PASS 4b exit 0. Evidence docs/dev/evidence/orin27/ga10b4ab-boot1.log; A51 flown. Boot 1 (2026-09-11) REFUSED no-dma-window, fixed by 1629d449. Next rung is a clean-room question (what the ROM wants at fmccode/fmcdata/pkcparam), brief §7.
· (history) orin 26: Peter 2026-09-11 "do the 4 boots". Rung 4a/4b BUILT (exec-orin26-ga10b4 a39fd32d, A51), two images STAGED
  (`ga10b4a-20260911T1809Z-a39fd32` max_vaddr 0x23cae8 → `ga10b4b-20260911T1809Z-a39fd32` 0x23f120), card line dry-run
  refuses only on device readability (needs sudo) and geometry (`--expect-geom 1:0b:2048:63401984`, from render12's FLIGHTID).
  Butler on ttyACM0, MARK ga10b4a written, waker armed. Boot 1 = 4a; boot 2 = 4b only on `-> BCR-ALLHELD`.
✓ tear: DONE (BEAM, A54, folded 7ff0aa9d) · unafsroot: DONE (A53, folded 059db935) · core0/el0mem: folded 277e2cab. Trap for the next image: `./arroyo test-arm` REBUILDS target/aarch64_esp/kernel.elf — build esp-jetson LAST and grep the artifact before any QEMU leg.
· (history) Four defect executors spawned 2026-09-11 off the fold 63654d93 in ~/unaos-bench/scratch/orin26/{core0,el0mem,tear,unafsroot}
  on branches exec-orin26-*; tear carries 7b0a9804 merged, unafsroot carries a81f38c7 merged (one spec conflict, the executor resolves).

## THE FOLD'S FIRST GATE — seven findings, none of them code
✓ 3 malformed ledger rows (SR11: `raised=yes|no`, `src=scene\|flat`, a `|` where an em-dash belongs)
✓ 3 unregistered knobs, all orin's, all NA — `kernel8()` builds its own K8_FEATS and never reads
  the `_feats` block they write (arroyo:6249)
✓ 1 STALE registry row (UNAOS_SDMMCROOT) visible only with UNAOS_K8REACH_STRICT=1 forced — SR13,
  and it was orin 25's own cleared SP5 hit coming back: a feature lives in THREE places
  (declaration, cfg sites, registry) and only two were enumerated.

## NEEDS PETER
✓ **GPU ladder, the fork — RULED (R52, Peter 2026-09-12T17:0xZ: "1"):** the blob rule is amended for one case (LAWS §3 Licence): the GA10B firmware set may be staged unmodified on the Orin's boot media beside its agreement, loaded as data, never linked or committed. Rung 5 is designed for real (executor GA10B5B: enumerate the exact file set from PUBLIC package manifests — names, sizes, package URL, licence file — no download; fill GA10B-RUNG5-BRIEF.md §4.1 and §5). Still Peter's: the download itself (one line, filename, source, size), Q2 attended flight with a possible power cut, Q5 whether rung 4f (BRFETCH FALSE) flies after 4e.
· "One file is the rulebook, EVERYTHING ELSE DELETED" — the absorbing half ran at 3cfe28ad; the
  deletion half has never started. Destructive, R20, and the union is not yet correct.
· D1 (orin-ledger §D) loader `SetMode` to the widest console mode — knob-gated, one power cycle.
· D2 (→S7) `render_service` size-3 family convergence arc. Entry carries an expiry.
· S27 138 prune-candidate origin refs, never OK'd.

## THREE-WAY, AFTER THE PUSH — needs all three seats, nobody edits alone
✓ P14's sentence "no gate can fix this: a checker cannot see another branch" is FALSE of this script
  now: `ledger-check.sh:118` already enumerates `refs/remotes/origin/hw-*` and `origin/main`. rmbp 19
  found it, declined to edit a shared protocol row alone, pi agrees it is stale. **Orin seconds it.**

## ORIN'S OWN, OPEN
✓ `tegra_shell_remint` — R16 board-named AND named after a function it does not call
  (`wm::shell_remint` re-points a row; this mints via `create_at`). Rename is orin's; SR9's
  caller-vs-callee discriminator in GATE-FAMILY is rmbp's.
✓ **SO ids get no suffix form, ever** — a naming constraint, deliberately NOT a gate. The id
  extractor `([A-Z]+[0-9]+)` is unanchored on purpose (to admit `A36 (→ SR2)`), so `SO21b` silently
  aliases to `SO21` and the uniqueness check cannot tell them apart. Found by rmbp 19's mutation.
✓ SO6's row exists now (rmbp filed it under orin's granted id). Four days unwritten, not six weeks —
  the "six weeks" was S27's age, relayed onto the wrong finding.

## STARTED, NOT FINISHED
✓ exec-orin24-tear        7b0a9804  1 commit, diagnosis only. No instrument, no fix.
✓ exec-orin24-unafsroot   a81f38c7  3 commits, incomplete. Owes rmbp the main.rs /
                                    drivers/block.rs diffs BEFORE it moves.
✓ exec-orin24-fold        0cdb3654  holds sdv1 + evidence. NO GATE has ever run on it.
· exec-orin22-bootroot    600887c2  B98 clone-merge fix HALF-APPLIED, uncommitted, in an agent
                                    worktree. Landing-blocking for bootroot.

## CUT AND NEVER BEGUN — zero commits, all at 1b50376a
✓ exec-orin24-{core0, el0slot, mem, small, tools}

## DEFECTS OFF RENDER12 (flown 2026-09-09 · scorer11 6/6 · 0 faults, 0 panics, 3 boots)
✓ EL0 never leaves core 0 (`el0cpus=0x1` every sample)               → core0
✓ 7× `slot 4 backing allocation FAILED`; apps refuse to launch       → el0slot / mem
✓ tearing on the glass while every rollup reads `torn=0`             → tear
✓ `unafs=absent`; `/` does not mount UnaFS                           → unafsroot

## OWED FLIGHTS AND CONTROLS
· FRIEND-DIFF positive control — owed since render11. Needs the data card mounted AND root
  write-locked. Without it that leg has never been shown able to go red.
· Glass checks never evidenced: cursor sweep (no `restore src=` has fired), taskbar order across
  quarry open/close, EL0 placement off core 0 (blocked by core0/el0slot/mem).
· render13 = the fold + core0 + el0slot + mem + tear + unafsroot, one gate, one card line.
✓ GA10B rung 4: FLOWN 2026-09-12, PASS both rungs in one boot (A51). Nothing owed on this rung.

## SMALL FIXES — none started
· shell.rs `screenshot` prints OK with no device (shared core — ask rmbp).
· x86 `[wm] close-scope win=0` on the close-box path (rmbp flag).
· wintitle out-of-lane leftovers (SO22): `app_name_forget` from `teardown_user_slot`, two x86
  launchers minting unnamed windows, `b"orin"` title on the display_tegra test pattern.
· `read_block_at` `(lba*512) as u32` truncation above 4 GiB — flagged by sdv1, never ledgered.
· NIC descriptor-17 OPEN (R19); net5V REFETCH-WRONGSLOT=3 carried since render8.

## TOOLING / AUDIT
· scorer11 STAMP-MATCH leg (wire `sha=` == card elf stamp, WRONG-IMAGE exit 1).
· orin-specscore.py / mbench --self-test are run by NO verb (ARCH-CONFORMANCE 14).
· 28 board-named shared scripts — R4 scour, its own arc.
· media-writer.sh is un-versioned (rmbp B97).
· winmenu APP_OWNER unregistered WinId holder (pi 10's question) — prove or refute.

## BENCH FACTS THIS ROUND (orin 27, 2026-09-12)
· **The Orin UEFI does not list the re-imaged microSD (MBR: p1 0x0c FAT32 127 MiB + p2 0x7f UnaFS) as a bootable disk in the native slot** (Peter 2026-09-12 03:3xZ: "the orin does not see it as bootable disk"); the same firmware booted an MBR FAT32 card from the USB reader on the rung-4 flights, and the slot reads this very card (SS32G) at the kernel level. CAUSE FOUND (A58): the image's FAT32 volume had 32,440 clusters — FAT16 by the spec — so EDK2 refused it (Linux trusts the BPB and mounted it, so every writer check passed). FATCLUST (`make-pi-img.sh -s 1`, 3225b5b0) fixes the image; the GPT hypothesis is withdrawn. Writer gap → trunk queue: a FAT32-by-cluster-count check on the image.
· A whole-card image NEVER lives inside a `--src` staged tree: `media-writer.sh --src` copies the entire tree onto the 127 MiB FAT, so the 640 MiB `UnaOS-orin-card.img` filled the volume mid-copy ("No space left on device", 1 of 11 files differ, card left mounted). Stage it BESIDE the tree as `<name>.card.img` and list it in the top-level MANIFEST only. Writer gap to close (trunk queue): a C14 "the staged tree fits the target FAT" preflight, and an unmount-on-failure that does not leave a partial file behind.
· The idle Debug Probe port drips single NUL bytes: a GROWTH waker can false-fire on one byte; re-anchor and re-arm.

## STANDING — how this seat reads, after a day of getting it wrong
    git log --all --not HEAD -- <file>                    # what I lack: an enumeration, not a pick
    git ls-tree -r --name-only <ref> -- docs/ | comm -23 - <mine>   # files I don't know exist
    UNAOS_LEDGER_STRICT=1 ./arroyo check                  # a detached HEAD gets the WEAK gate
  Never `| head` on evidence you are about to quantify over. Never a number from memory.
  The population a gate checks is not the population a seat greps (pi 10, 2026-09-10).
