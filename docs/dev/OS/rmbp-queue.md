# RMBP — WHAT IS IN FLIGHT AND WHAT IS OWED (the x86 rMBP track queue, R45)

One file, fixed path, not per-round. Kept current AS WORK HAPPENS, never at close. Only jobs that need
the rMBP's metal or x86-only files live here (`arch/x86_64`, Kepler/gmux/EHCI/FTDI, the x86 specs,
`stage-x86.sh`); everything else is a row in the trunk queue `docs/dev/QUEUE.md` on `main`. Ledger:
`docs/dev/OS/rmbp-ledger.md` (A/B/E) and `docs/dev/LEDGER.md` (SR<n>). `✓` = verified in this tree ·
`·` = inherited, not re-checked. Re-derive every sha before acting.
Created 2026-09-12T00:35Z (2026-09-11 local; dates here are UTC) by orin 27 (all lanes, R46). rmbp 19's own measurement, quoted by Peter the same day:
"88 open rows is a graveyard, not a queue" and "16 items … exist only in a plans directory outside the
repo" — this file is the queue those two sentences asked for; the 16 baton items were not read (R37),
their three named subjects are rows here or in the trunk queue.

## STATE — 2026-09-15T18:3xZ (rmbp focus session)
✓ COUNT COMMAND for the rmbp-ledger A/B population (this block carried none before the GRAVEYARD sweep; LAWS §5 wants the unfiltered `| wc -l` first):
  `awk -F'|' 'NR>=17 && NR<142 && /^\| [AB][0-9]+ \|/' docs/dev/OS/rmbp-ledger.md | wc -l` = 115 rows (the whole population, sections A and B, B24's two-rows-on-one-line counted once)
  then by class: `awk -F'|' 'NR>=17 && NR<142 && /^\| [AB][0-9]+ \|/{s=$6; sub(/^ */,"",s); sub(/ —.*/,"",s); sub(/,.*/,"",s); sub(/ *$/,"",s); print s}' docs/dev/OS/rmbp-ledger.md | sort | uniq -c`
  GRAVEYARD sweep 2026-09-15: open 93 -> 46, landed 1 -> 42, dropped 0 -> 6, fixed-unflown 20 and flown 1 unchanged. Per-row table and proof commands: `docs/dev/evidence/rmbp-0915/GRAVEYARD-SWEEP.md`
✓ hw-rmbp 160176d2 = main bd17f887 + hw-jetson fd440602 + hw-pi4 e6e71c9f, all folded here on Peter's order ("sync with both leaving main"); code is hw-jetson's exactly (`git diff --stat hw-jetson hw-rmbp` = QUEUE.md + pi-queue.md only). One conflict, QUEUE.md §5, by union; ledger-check rc=0. UNPUSHED: 00d32fc3, 5c4aad2d, 160176d2 (+ the executor branches below at close). main untouched.
✓ Bench: rMBP on the bench, NOT booted, FTDI on /dev/ttyUSB0 held by line-butler pid 10585 → `~/unaos-bench/capture/line-usb0/raw.log` (first bytes of the next boot land there). A new-to-us 29 GB SD in the reader (`/dev/mmcblk0p1`, vfat, NO label; Peter: contents disposable) — it becomes the UNAOS-X86 card at the card step. No flight image built yet (Peter: no compile before development).
✓ Executors cut from 160176d2 this session (branches `exec-rmbp-*`, worktrees `~/unaos-bench/scratch/rmbp-0915/<name>`): FTDIRX (A9, the x86 half of S29), XHCIKBD (B45 + B44/A41, the key task, Fable), TESTTRUNC (trunk §5 truncated-run row + B95 + S8), BEAMX86 (A5); then on Peter's "keep nine executors going": LOCKFIX (B1), GRAVEYARD (the B18-B88 sweep), SHUTOUT (B10, the R19 register for the GPU/iGPU ladders), RMDIR (SO18), ONEOS (one R16 family). Nine live at 2026-09-15T18:5xZ. FOLDED so far: SHUTOUT 3df6a93a, GRAVEYARD 52e29dcc, TESTTRUNC 9a7b09a4, GPUDOCS ccfdf3de (+ 9e10b32f two gen7 lines), BEAMX86 6647d359 (UNAOS_BEAM=1 joins the flight line; knoboff beam/nvidia-kepler and the BEAMX86 artifact grep owed on the fold gate). GMUX-2 (A7, igpu.rs pre-switch reads the status port) cut from 6647d359. FTDIRX 4a0350a5 (UNAOS_FTDIRX=1 joins the flight line), LOCKFIX folded next. RBTDRAIN (A3) and SHUTRESTORE (the seven deleted Kepler rungs, R19) cut from 4a0350a5. FOLDED: XHCIKBD 61021a30 (the repair is ungated, every board), ONEOS afd5ce03, RMDIR f8359d11, GMUX-2 31c7e5cb. FOLD LESSON (XHCIKBD): two tail-appended blocks in xhci/mod.rs ended with the same three closing lines and git deduplicated them as a common suffix — the union was 3 braces / 4 lines short; caught by brace+line arithmetic against both parents over the base BEFORE the compile (do that on every .rs union). BANNERCERT finding, awaiting its second commit: `sdwrite` is in arroyo's banner on every x86 media build and NOT compiled by the builder (0 hits in builder/src/main.rs; SDWRITE-POSTURE 0 in the ELF) — every arroyo-mapped knob the builder does not read is a banner lie on esp-x86/vm-image today; the gate names them as they are armed. FASTTEST 298208ed (x86 `test` QEMU wall 120 -> 47 s; UNAOS_QEMU_FULL honest; TRUNCATED unmoved). MEASURED (ONEOS): with seven `arroyo check` runs concurrent, one check takes 25-45 min against a few minutes on a quiet box — the COMPILE is the contended step, not QEMU; nine executors = nine full type-checks of both arches in parallel; a shared compile cache across executor worktrees is the untried lever (measure under concurrent cargo before trusting it). BANNERCERT folded (28/28; sdwrite compiled; registry empty). GLASSFIX (SO2/SO4/SO11, glass defects in shared video/) cut from 298208ed; BANNERCERT2 (wire esp_arm/esp_jetson/kernel8, measure the 15 aarch64 tokens, extend the knob->builder probe to unconditional appends) cut from this fold. SHUTRESTORE folded (seven rungs behind UNAOS_KEPLER_{USERD_SNOOP,PFIFO_FLUSH,CTRL_ADDR,REPOINT,LATCH_ARM,PITCH_LADDER,GOP_OVERLAP}, all default OFF, one per flight). SINKDRAIN (RBTDRAIN's finding: power_drain writes through the raw 16550 path the rMBP lacks) cut from this fold. LADDERTAIL folded d6b3c9a7 (settled() now gates on complete(); zero REQUIREs — the marker's ORDER covers the ladder). FC2CHECK (S3/S5/S21 structural gate) cut from d6b3c9a7. OUTAGE 2026-09-15T20:4xZ: a DNS failure (EAI_AGAIN) terminated ALL NINE live executors mid-turn; every worktree kept its edits (dirty 0-11 files, no build processes left running), DNS back within minutes, all nine RESUMED from their own transcripts (Peter: "Try again"); AHCI was mid go-red revert — told to confirm the mutation is out before committing. BENCH 2026-09-15T20:1xZ: /dev/ttyUSB0 and /dev/mmcblk0 both GONE from the host (no /dev/serial/by-id, no card) — cable and reader unplugged; line-butler pid 10585 still alive on the vanished node (holds nothing; raw.log 63 B, no boot seen). RE-ARM when the cable returns: kill 10585 by pid, start the butler on the new node (`ls /dev/serial/by-id/usb-FTDI_*`), verify with `lsof -t <dev>`. The card is still BLANK (labelled UNAOS-X86, nothing written): no flight image exists yet; it is built LAST, after the fold gate. Seat error recorded: the FTDIRX fold was committed with ledger-check RED (a scratch path in A9's evidence) — the fold script printed rc and did not stop on it; fixed in the LOCKFIX fold commit. AHCI (B89 first rung, read-only, q35-gated) cut from ccfdf3de after Peter asked about installing to the internal SSD. Then FASTTEST (SR7, the builder's sleep wall), BANNERCERT (banner-vs-artifact gate, trunk §5 (b)), GPUDOCS (GEN7DOC + GMUX-1) cut from 9a7b09a4 to hold nine. GATE FLOOR RE-ASSERTED (Peter 2026-09-15: bundle changes, test once): every executor gates on `check` + at most ONE QEMU run for its own fixture; knoboff, the second walls, test-arm/test-fat and the flight build run ONCE on the fold — the seat's briefs had over-asked past R38. Host at nine executors compiling, 2026-09-15T19:1xZ: load/used/avail/procs = 8.43, 6.58, 6.29 7G used 14G avail 12 (for Peter's ceiling question: the 22 GB box is the limit, not the cores). gmux provenance (SHUTOUT STOP 4): seat decision, hardware fact — the port map, register encodings and the chip's handshake order are what the gmux requires; the Rust is its own code; Peter 2026-09-15: "is there not bound to be similarities in code? sounds like that's what this is, no?" — recorded here, not as a ruling. Fold gate, esp-x86 LAST, stage, card, playbook, mark, waker — after the fold.
· x86 legs (R39): `test`, `test-fat`, the ELF-off-FAT legs, the x86 usb-write witness — rmbp's.

## METAL — needs the rMBP on the bench
· A9  serial console is TX-only (FTDI bulk IN 0x81 never driven) — no typing over the wire (the x86 half of S29)
· A1  BAR1 wedge under paint bursts (`storm`): a core dies mid-blit
· NEW  BAR1WEDGE FLIGHT — **the A1 flight, and it is two boots under `storm`, not one.** Boot A is the EXPERIMENT: the rMBP line plus `UNAOS_BAR1EXP=uc UNAOS_BAR1WEDGE=1`. Boot B is its CONTROL: the same line with `UNAOS_BAR1WEDGE=1` and NO `UNAOS_BAR1EXP` — and boot B must WEDGE, or boot A proves only that the storm was weak. Both knobs on boot A or it is unscorable (the ~6.8x UC slowdown explains a quiet boot as readily as the theory does). Confirm `bar1exp-uc` AND `bar1wedge` in the `⚡ kernel features:` banner AND by `LC_ALL=C grep -a -o -F ':: BAR1WEDGE:'` on the artifact — s42's INSTGUI lesson, and this knob's entire product is witness lines, so a banner-only arm would read as a negative result about the hardware. THREE LINES at kepler init (`:: BAR1WEDGE: rung=first-stall …`, `[pcih] bar1wedge cto rp …`, `[pcih] bar1wedge sticky-cleared at-arm …`), then one `[pcih] wedge-sample n=…` per tripwire crossing beside the unchanged `[pcih] rp-at-wedge`. **READ `n=1` — that is the first stall.** Absence controls: on boot A `fb-wc` ABSENT and `:: x86 bar1exp: UC arm ARMED` PRESENT, the reverse on boot B; `[pcih] rp-boot` and `[pcih] rp-at-wedge` present and unchanged in shape on BOTH. Read with `awk 'index($0,"[pcih]")'`, never bare `grep`. Every outcome and what it means for A1: `docs/dev/OS/08_VIDEO/PCIE-RP-RECOVERY.md` §11.3. Three of them settle an argument without P5 at all — `dis=1` in the `cto` line, `d_secsta=0000` at `n=1`, and `d_lnksta` carrying bits [15:14]
· A5  shell window tears under storm (`torn=111`) — the x86 twin of the Orin BEAM fix: Kepler's VERT/rgpos register as the beam source
· A6  `[clickroute] … -> FAIL` deterministic on metal, green in QEMU
· A3 / A4  reboot verb's witnesses never reach the wire; unattended reboots need the card as default startup volume (⌥ picker)
· NEW  RBTDRAIN-FLY  score A3 at the cable: with the FTDI on `/dev/ttyUSB0` and a boot up, TYPE `reboot` at the shell and read the LAST lines the wire carries. Expect, in order and as the final bytes before the machine goes, EXACTLY THREE lines — measured on the QEMU fixture, not predicted: `[pwrreboot] reboot verb invoked — dispatching the platform mechanism`, `[pwrreboot] x86 mechanism: FADT RESET_REG ladder (acpi_power::reboot)`, `[pwrreboot] ftdi flushed bytes=B transfers=T exhausted=0`. ⚠ `[pwrreboot] ring drained lines=N bytes=M` will NOT be among them and its absence is not a failure: `power_drain` writes both the staged lines and its own tally through `arch::serial::raw_write_str`, which on x86 is the 16550 at 0x3F8 alone — it never enters the FTDI mirror ring, so on this laptop PWRDRAIN's whole output goes to a port that is not there. RESIDUAL GAP, found by this fixture and NOT fixed by RBTDRAIN: a line DEFERRED into the staging ring on a contended rMBP boot is consumed by `power_drain` into that dead port before the FTDI flush can ever see it. Closing it means giving `power_drain` a sink that is `_print`'s set and not one port (`serial_ring.rs`, outside this brief). `exhausted=1` does NOT close the row — it converts it into "the flush ran and the budget bound first", and the number to read next is `bytes=`. The FADT ladder's own `raw_witness` lines (`FADT RESET_REG space=… — writing`) will NOT appear on the cable and their absence is not a failure: `raw_witness` is `raw_write_str`, the 16550 this laptop does not have — that fact is A3 itself, and `reset_report`'s boot-time line (BOOTFADT, flown F7) is where those facts already are. Same verb from the desktop's Restart closes nothing here (x86 has no `crystal_restart`; the menu routes through `power::reboot`). Score `shutdown` on the same sitting, and the desktop's Shut Down too: the fold is on BOTH x86 power ports (`acpi_power::poweroff`'s signature is `s5_ring_flush(); ftdi_flush_sync(…)`), so `video/crystal.rs` and `video/instgui.rs`, which call `poweroff` DIRECTLY, must also end with `[pwrshutoff] ftdi flushed bytes=B transfers=T exhausted=0` as the cable's last line before the board goes dark. A dark board with NO such line is a red, not a known gap. `docs/dev/OS/02_KERNEL_CORE/serial_transport.md` §RBTDRAIN
· A7  gmux switch to the iGPU does not persist — ⚠ RESTATED by the register: it does not switch AT ALL (GMUX-1 below); the gate is FIXED-UNFLOWN as of GMUX-2 — score it with the row below
· NEW  GMUX-2 FLIGHT — the pre-switch gate's verdict, on the `UNAOS_GMUX_IGD=1` line (already on it; no new knob). TWO LINES decide it, in order. (1) `:: igpu-dpy: pre-switch state DDC=0x.. SW_DISP=0x.. SW_EXT=0x.. SW_EXT_ST=0x.. DISP=0x.. EXT=0x.. sw_ext_state=.. ext_state=.. gate=.. ::` — the EXPECTED shape on this machine is `DDC=0x02 SW_EXT=0x01 SW_EXT_ST=0x21 DISP=0x03 EXT=0x21 … gate=ACCEPT`, i.e. **`SW_EXT` still reads `0x01` and the gate accepts anyway** — that pairing IS the fix, not a contradiction. `gate=REFUSE:<port>@<idx>` names which STATUS port reports a state this rung was not written for; `gate=UNREADABLE:<port>@<idx>` says the gmux did not answer (0x00 / 0xFF / 0xFFFFFFFF) and sends the question back to G3's handshake, NOT to this row. (2) the switch verdict: `:: igpu-dpy: rung=00 name=census ok=1 … ::` and then the ladder past `highest=00/10`, or the honest refusal `:: igpu: [GMUX] REFUSED: pre-switch-not-accepted … ::` → `gmux=UNTOUCHED`. Also expected on an accepted boot, and NOT a defect: `:: igpu-dpy: restore ext=SKIPPED (write-target port, no state read) ::` at the gate, `:: igpu: [GMUX] EXTERNAL restore=SKIPPED … ::` at the revert, and EXTERNAL abstaining from the `gmux=MATCH|FAILED` vote — which means **the external mux is left on IGD until power-cycle** on the first run that reaches G7. Read with `awk 'index($0,"igpu-dpy")'`, never bare `grep`. Read-back the LADDER line whatever happens: a boot with no `igpu-dpy` lines at all is G3 failing upstream
· A8  Bluetooth inquiry deafness is boot-scoped
· B8  Broadcom 0x14e4 NIC has no driver (e1000 is QEMU-only)
✓ B10  shut-out register for the rMBP GPU ladders (R19) — COMPILED: `docs/dev/OS/08_VIDEO/SHUTOUT-REGISTER.md`; the jobs it found are the GPU LADDERS section below
· B89 / B91  the "boot dumb" falsifier the tree fails; the installer guards HOME, not the stranger (Catalina)
· NEW  AHCIFLY  score the READ-ONLY AHCI driver on the rMBP's own metal — `UNAOS_AHCI=1` on the flight line, one boot, three lines to read. EXPECTED WIRE: `[ahci] bdf 0:31.2 8086:1e03 bar5=0x… cmd=0x…` (⚠ BAR5, not the `bar0=0x3080` the census prints — that is the legacy IDE I/O window), then `:: AHCI: port=<p> model="APPLE SSD SM…" sectors=<n> lba48=1 ::` naming the internal SSD, then `:: AHCI: port=<p> sector0 sig=0xaa55 kind=GPT ::` (Catalina's disk is GPT), then `:: AHCI: selfcheck port=<p> identify=ok sector0=GPT -> PASS ::`. B89's second disk should raise a second port block. Confirm `ahci` in the `⚡ kernel features:` banner AND by `LC_ALL=C grep -a -o -F ':: AHCI: port='` on the artifact, never by the banner alone (s42's INSTGUI lesson, LAWS §5). READ-ONLY by construction — the image carries no ATA write opcode — so this boot cannot touch Catalina. Doc: `docs/dev/OS/07_USB_STORAGE/ahci.md`; ledger B89
· NEW  AHCIWRITE  the SATA write arc + the partition-mode installer, and B91 FIXES THE ORDER: stranger guard FIRST. Three parts, in this sequence. (a) `BlockHandle::Ahci` + `fs::fat::BlockSource::Ahci` so `fs/bootdisk.rs` can bind a root by content on a SATA volume — costs FIVE files the read arc was forbidden to touch: `install/mod.rs` (2 arms), `wifi/firmware.rs` (1), `fs/unafs.rs` (4), `fs/fat.rs` (variant + name + write_veto + 4 dispatch arms), `fs/bootdisk.rs` (the presence row + the walk's admit); the block-layer seam they dispatch to is already landed (`ahci_info_ix`, `read_block_ahci_ix`, `read_blocks_ahci_ix`, `write_block_ahci`). (b) B91's stranger guard — the installer's only guard protects HOME, and the moment `install/mod.rs` gains an `Ahci` arm the internal SSD becomes a legitimate candidate by construction. (c) `WRITE DMA EXT` (0x35) behind its own knob, refuse-by-default in the shape `sdw` established for the SD path, and a partition-mode installer that targets a FREE partition and can never name the volume Catalina lives on. Do not start (c) before (b) lands: that is B91's ruling, not a preference. Ledger B89 / B91
· FLIGHT 8+ on the current tree — the x86 track's last metal flight predates its own landing
· B51 / B56  XHCINTD + DUPGUARD are REVIEWED and ACCEPTED on `origin/exec-orin17-dupguard` (`0019ec7a`, `28899d5c`, `e390721f` — all fetchable since Peter's push) and FOLD NOWHERE until the rMBP flies the completion path: `KBD_INFLIGHT`, `kbd_retire` and `kbd_guard_verdict` are 0 hits in this tree. B45 established the rMBP is the only place in the fleet that can score it. Shared half: trunk queue §2 A41/B44
· B90  the FRIEND-INVARIANCE positive control, and it is one unplug plus a diff: boot the same image twice, once with a friend UnaOS disk present and once without, and diff the wire modulo the mount witnesses (normalization PRE-REGISTERED: counters, timestamps, addresses and mount witnesses only; no line drops, order kept). The Orin flew the falsifier and it was NOT FALSIFIED — but vector 1 declined to trigger, so the leg that matters is FRIEND PRESENT **and** ROOT REFUSING WRITES, the only configuration in which the capture ladder's rung 2 can stain. B109's witness field (`Shot.vol`, `source`, `serial`) is landed, so the result is now readable

## GPU LADDERS — the B10 shut-out register's job list (R19)
One row per rung whose failure conditions a next flight (or, for the first three, no flight at all) can change.
Every row cites `docs/dev/OS/08_VIDEO/SHUTOUT-REGISTER.md` by section; read the row there before acting.
✓ DONE  GMUX-1  d429c4a6 — read it. One boot in the capture; `SW_EXT=0x01` is the single failing term of the four at `igpu.rs:1241-1246`, and it is the read of `GMUX_SWITCH_EXTERNAL` (0x40), the WRITE-TARGET port. Every register that reports mux state read its accepted value (DDC 0x02 DIS, READ_DISPLAY 0x03 DIS, READ_EXTERNAL 0x21 kepler-owned). A wrong READ, ours, not firmware state and not a wrong write (`gmux=UNTOUCHED`, `elapsed_ms=1`). Finding: `docs/dev/evidence/rmbp-0915/GMUX-1-PRESWITCH.md`; A7's evidence cell re-cited to it. Next rung: GMUX-2 below
✓ DONE  SHUTRESTORE  all seven rungs' code is back in the tree, each behind a default-OFF feature of its own inside the existing `nvidia-kepler` cfg region, restored from our own history: `USERD_SNOOP` (`nvidia-kepler-userdsnoop`, from 7124e4e1), `PFIFO_FLUSH`/`flush-executed` (`nvidia-kepler-pfifoflush`, 7420e06f), `CTRL_ADDR` (`nvidia-kepler-ctrladdr`, 3620c7d5), the 0x6101E0 `repoint` (`nvidia-kepler-repoint`, 896faee0), the EVO latch arm+UPDATE (`nvidia-kepler-latcharm`, bfeedd94 — `update_reg` is live again under that knob), `lin-step`/`bwpg` (`nvidia-kepler-pitchladder`, eee60395/04b494be), `gop-overlap` (`nvidia-kepler-gopoverlap`, bfeedd94). All seven mapped in `arroyo` AND read by `builder/src/main.rs`, and named on the literal `x86-all` leg so their ARMED bodies are type-checked. Register §7 is now the record of the restore; the §1/§2 rung rows carry `code: restored … behind <knob>`; the flight lines are in `KEPLER-METAL-LOG.md`. TWO carve-outs below. Register §7
· NEW  SHUTRESTORE-CTRLADDR-BRINGUP  KF9's audit is restored; its APPARATUS is not. The s13 original (3620c7d5) re-ran the whole channel bringup — PFIFO_CHAN bind for CHIDs 1/2/3/7, the `sched-status` bracket, the `PFIFO_CHAN[1]` witness and the runlist submit — INSIDE the `for target in 0..4` loop, so each of the twelve TARGET encodings got its own validation attempt. That surrounding code no longer exists in that shape: today's `kepler::init` binds channel 1 once, far below the pre-init point where the audit sits, and re-entering it twelve times from inside a knob-gated rung would need the bringup factored out of `init` into a callable seam. The missing seam is exactly that: a `fn try_bind_and_witness(bar0, inst_off, userd_off, bar1) -> bool` carved out of `kepler::init` between the PBDMA bind and the `PFIFO_CHAN[1] pre-submit` read, which the audit could call per TARGET. Without it the restored rung answers "is CTRL_ADDR[1:0] writable" (it is, 12/12) but not "does any TARGET encoding change the strip" (s13's actual question). Register §7, §2 KF9
· NEW  SHUTRESTORE-KNOBLESS  the two knobless hard-codes §7 names, which were never deleted and so were never SHUTRESTORE's subject: `run_recon` (`kepler_display.rs`) is a literal `false` and `do_takeover` a literal `true`. Code kept, knob absent — neither can be re-armed from a media build without an edit, which is the same R19 exposure one step short of deletion. Give each a default-OFF / default-ON feature respectively, mapped in `arroyo` and `builder/src/main.rs` like the seven. Register §7
✓ DONE  GEN7DOC  d429c4a6 — `gen7.md` §2 table (R2/R5/R6/R7 cells), §2.2, §2.5, §2.6, §2.7, §3.1 and §4 corrected against flight 4's wire, both boots, quoted with their boot and line (L1483/L10385, L1565/L10467). Second finding, a misreading of our own code: `write_ok()` (`gen7.rs:1351`) is `GtWake::write_ok()`, R3's wake verdict — not a launch gate; flight 4's `r5/r6/r7 begin` lines all read `write_ok=1`. Register §4
✓ DONE  GMUX-2  `exec-rmbp-gmux2` — both halves, in `igpu.rs` only. GATE: `gmux_preswitch_decode(ddc, read_display, read_external)`, a pure `const fn` at the file tail, scores the STATUS ports only and **structurally cannot** score `SWITCH_EXTERNAL` again — 0x40 is not one of its arguments; it is still read and still printed (`SW_EXT=`, now beside its pair-mate `SW_EXT_ST=`), and it is no longer a verdict, exactly as `SW_DISP` already was. It returns Accept / Refuse(port) / **Unreadable(port)**: 0x00, 0xFF and the 0xFFFFFFFF timeout sentinel are not mux states, and folding them into "not accepted" would let a boot that learned NOTHING look like a boot that learned the mux was in the wrong place. RESTORE: `ext_restore_target(read_external)` maps the accepted STATUS read back to a write encoding **only where the map is cited** — `0x03` → `GMUX_EXTERNAL_DIS`, the one value that is the same named constant on both sides — and returns `None` otherwise, so on this machine (`READ_EXTERNAL=0x21`, no write encoding named anywhere in tree) the unwind pushes no EXTERNAL entry at all and prints `restore ext=SKIPPED (write-target port, no state read)`. Nothing writes `0x01` anywhere any more. The `why` token is unchanged (`pre-switch-not-accepted`) so flight 5's capture and register §5's G6 row still compare; the new discrimination is the `gate=` field. Decode pinned by `const _` asserts, NOT `#[cfg(test)]` — `cargo test` never runs on this `no_std` crate and `./arroyo check` is the gate (`kepler.rs:3906-3918`). Knob-off holds by construction: every new item carries the existing `gmux_igd` cfg and sits below the gmux harness, which is `igpu.rs`'s last item, so no knob-OFF line number moves (B94). Ledger A7 → `fixed-unflown`; register §5 "what would change the verdict" rewritten to the flight. Unblocks G5, G7, G9 — none of which is a fact until the flight below
· NEW  CEFLY  first CE-LADDER flight: `UNAOS_KEPLER_CE=1` decides R1/R2/R2b/R3/R5 in one boot (`CE-LADDER end r1_ptop=…`). Confirm `nvidia-kepler-ce` in the `⚡ kernel features:` banner AND by `LC_ALL=C grep -a -o -F` on the artifact (s42's INSTGUI lesson). Falsification story pre-written. Register §3
· NEW  BAR1UC  fly `UNAOS_BAR1EXP=uc` (`:: x86 bar1exp: UC arm ARMED`, `arch/x86_64/memory.rs:3729`) on a boot scored **wedge/no-wedge**, not throughput — the M1-vs-{M2,M3} discriminator for ledger A1. The ~6.8x UC slowdown that disqualified it from flight 5's power numbers is irrelevant to this question. The only coded, never-flown power experiment. Register §6. ⚠ **UPDATED by BAR1WEDGE: this row is no longer flyable on its own and never should have been.** A UC boot yields ONE BIT, and the same 6.8x slowdown that makes the throughput objection irrelevant also explains a quiet boot without the theory being true — so a no-wedge under UC would have been recorded as a conviction it could not carry. It now flies as the pair `UNAOS_BAR1EXP=uc UNAOS_BAR1WEDGE=1` with a WC control boot, as the METAL row above specifies. Register §6 P5 + P6
· NEW  BAR1WEDGE  **landed this session** (`exec-rmbp-bar1wedge`, knob `UNAOS_BAR1WEDGE=1`, default OFF, `drivers/gpu/pcihealth.rs` file tail): the FIRST-STALL register block — the falsifier BAR1UC needs. Root port only, endpoint never touched; the sampler half is READ-ONLY and keeps the module's two refusals (root port only, ECAM only). Prints LNKCTL, which the existing sampler loads and DISCARDS, so no capture has ever carried the ASPM state in force at a wedge; prints the completion-timeout configuration (Device Capabilities/Control 2), which nothing in this tree had ever read and which decides whether §3.2's sacrificial probe can survive at all; and W1C-clears the three sticky latches at arm time so every later reading is a delta — rung 1 of `docs/dev/OS/08_VIDEO/PCIE-RP-RECOVERY.md` §10, widened from the one latch that document noticed to three. **Not a fact until the flight above.** Register §6 P6; ladder, decode and score card at PCIE-RP-RECOVERY §11
· NEW  GEN7R2  re-score gen7 R2 (`gt-still-dark`): it failed on an instrument flight 4 proved blind — `battery_moved=0/17` through a **verified** 1 KiB engine DMA — and on a poll that passed at iteration zero on a gated window. Re-run with a behavioural witness (ring arm / sentinel), not the 17-register battery. Register §4
· NEW  GEN7R8  R7's own `next=`: wire `bring_up_blt_ring` to the held wake and fix `blitter_copy_rect`'s DW0 client field. The BCS copies pixels on metal; this is engineering, not physics. Must NOT gate on a forcewake ack decode (`fw_evidence=blind` while the BCS executed). Register §4
· NEW  GEN7TLB  decide the R6/R7 reclaim: pin an alternative GGTT-invalidation witness, or promote the bounded 7-page / 28 672 B per-armed-boot leak to documented-accepted and delete `reclaim=freed` from the falsifier — it is structurally unreachable as coded (`tlb-flush-write-silent`). Register §4
· NEW  KDHEAD  re-run the per-head EVO/CRTC decode (KD3) with KD4's `head[0] stat underflow=` as the control bracket and the per-head stride re-derived for the 917D class. It failed with no control read, and KD4 proves head 0 scans. Register §1
· NEW  KFBIND  re-run the CHAN_CUR/CHAN_NEXT bind (KF18) and the post-bind strip (KF19) **with FECS context microcode resident and running** — the one condition never varied across ten eliminations. Carry KF6/KF8/KF9 (once SHUTRESTORE lands) as first steps of the same boot: each was refuted only under a host that had never run FECS ucode. Register §2
· NEW  KFUNWEDGE  the un-wedge experiment, still UNEXERCISED after ten sittings: a boot where the `0x409504` poison **deliberately** fires, then a PRING observe/clear and a `cpuctl` re-read in the same boot. KF21 proved the write is harmless; it is the read that poisons. Register §2. ⚠ **NOT TOUCHED BY BAR1WEDGE, and the distinction matters because the two rungs share a word.** KFUNWEDGE is the FIFO/PBDMA ladder's un-wedge (register §2, a GK107 PRI poison recovered inside the endpoint); BAR1WEDGE is the PCIe ladder's wedge INSTRUMENT (register §6, root-port config only, which is the one thing the wedge sampler is allowed to read). Nothing in this session's change reaches `0x409504`, `cpuctl`, or any endpoint register — by the module's own refusal, not by omission. KFUNWEDGE is still owed and still unexercised

## X86-ONLY FILES — QEMU q35 gates them
· B1  LOCKFIX gap at `arch/x86_64/syscall.rs` `click_pointer_pos` (`WRITER.lock()` in an input band)
· B3  five x86 features uncovered by any board leg (`nvidia-kepler-kdisp-hold rtpi rtwit selfhost vugras`)
· B4  two `unsafe` warnings in the bootloader under `unaos_ivb`
· B45  the x86 gate does not exercise the xHCI keyboard path at all (decides where the N-TD fix is proven)
· B85  six FORBIDs keyed across a token junction in the x86 specs
· B95  x86 spec files run by no `arroyo` verb (trunk-queue row 5; the x86 verb wiring is here)
· B9  `[ptrdead] … fpop3=1 -> FAIL` in `UNAOS_WC=1 ./arroyo test-fat sf 200` — the foreign-drain flake in `arch/x86_64/syscall.rs`, 2 reds in 5 WC runs, both at load >= 24 (load-correlated, not random); `0d509431` / `badc8732` did not close it. Costs a re-run per proof on an unrelated leg
· B83  the CITATION half: rmbp-ledger B58 and B60 cite `dc683c40` / `1aae3459`, which resolve on local `exec-*` branches only, while their CONTENT is on `main` as `18af05ab` / `c8153b4e`. A re-cite against the landed shas, not a rescue. (The metal-flight half closed when Peter pushed `exec-orin17-dupguard`.) Lane rule adopted: every cross-lane cherry-pick uses `-x`, so a dangling citation resolves by `git log --grep=<old sha>`

## THE GRAVEYARD SWEEP (an rmbp job, one sitting)
· 60-odd B-rows (B18-B88) are GRANT and REVIEW records of orin arcs that have since landed or been folded; each is closed-by-landing or re-homed as a job, never deleted. The sweep is briefed with this file and reports only what is still a job.

## SHARED, ROUTED TO THE TRUNK QUEUE (listed so the cut is visible)
· SR13 strict trigger, R39 battery selector, S6 board names in shared files (50), SR1/SR9/SR10/SR11/SR12 gate classes, SR2 print-screen wedge, orin-ledger A41 / B44 keyboard report loss — all trunk-queue rows
