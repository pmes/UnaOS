# Pi ledger — every finding, ticked once

> Arch ledger for the Raspberry Pi 4 track (`hw-pi4`). Cross-arch and shared-seam items live in
> `docs/dev/LEDGER.md` (ids `S<n>`, `P<n>`, and the seat-prefixed `SO`/`SP`/`SR`); rows below that
> cross reference it by id. Ids here are `PI<n>` — orin's arch ledger uses A-D and rmbp's uses
> A, B, E, so a distinct prefix keeps the three files mergeable without renumbering.

> Created 2026-09-11 by pi 11, closing `LEDGER.md` **S24**, which had stood open since 2026-09-05
> because its creation was deferred to pi's focus turn. **The deferral is what produced the defect
> it was meant to avoid:** with no arch ledger in the tree, pi's owed work accumulated in
> `~/.claude/plans/unaos/queue/pi-focus-queue.md` — 2830 lines, outside version control, ungated,
> invisible to `ledger-check.sh` — while orin's ledger held 101 rows and rmbp's 137 and the gate
> scanned three files instead of four. That file is archived verbatim at
> `docs/dev/evidence/pi11/FOCUS-QUEUE.md`; this is where its work now lives. Row **SP18** on the
> shared ledger records the process half.

> Rule (Peter, 2026-09-05): **an arc that fixes, flies, or drops a ledger item ticks it here in the
> same commit.** Every audit folds its findings into this table the turn it lands; brief every audit
> with this file and report only what is NEW or CHANGED. A finding re-derived because it was not
> here is the waste.

Status: **open** · **fixed-unflown** (in tree, not on metal) · **flown** (scored on the wire) ·
**landed** · **dropped** (ruled out, with the ruling).
Owner is the lane that fixes it. `flies-on` is the bench that scores it: **QEMU raspi4b** is the
`kernel8-test` gate, **Pi bench** is attended metal — and the two are not interchangeable evidence
(a QEMU green certifies pi's QEMU execution only).

⚠ **Every number in this table is per-tree and decays.** Floors, line numbers and shas are true in
the tree and at the moment each row names; re-derive before acting. Pi's spec floor derives from
`parse_spec`, never from another tree's quoted value.

## PI. Pi 4 lane — spec, emmc2/SD, the aarch64 bare-metal boot path

| id | item | owner | flies-on | status | evidence | closed by |
|---|---|---|---|---|---|---|
| PI1 | **`drivers/emmc2.rs`'s header states a CAPABILITY THAT EXISTS AS ABSENT — it says the Pi's SD backend takes no writes, and `drivers/block.rs` routes SD writes straight to it.** Header lines 6-7: *"PIO only, single-block CMD17 reads, fully polled (IRPT_EN = 0), no DMA, and NO writes (the seam in `drivers::block` refuses a write on this backend)"*. Both halves false since U9: `block.rs:1197-1199` is `#[cfg(all(aarch64, baremetal))] if BACKEND == BACKEND_SD { return emmc2::write_block_512(lba, buf); }`, and `emmc2::write_block_512` is real at `emmc2.rs:798`. Found by orin 17, verified here 2026-09-06. **Why this is worse than ordinary stale prose (S17/S28): those make a reader hunt for something that is gone; this makes a reader believe a capability is ABSENT when it is present** — the same asymmetry as believing a gate is stricter than it is, and the expensive direction, because nobody looks again at a door they have been told is locked. **Concrete cost path, and it is live on pi's own queue: S14's closer needs a WRITABLE card** (`selftest_once()` parks in its no-writable-volume arm on QEMU raspi4b; the fix is to fly with a card mounted). A seat consulting this header would conclude the Pi's SD backend cannot write and that Print Screen to the card is impossible — abandoning a line of work that is available. Fix = one header line, no code **Lifted from `LEDGER.md` SP3 2026-09-11 when pi's arch ledger was created (S24); affects Pi.** | pi | Pi bench | open — queued by pi 7, NOT started (support seat, no-executors order); one-line header correction when the file is next touched | orin 17 2026-09-06; verified pi 7 at `f25f1601`: `emmc2.rs:6-7`, `block.rs:1197`, `emmc2.rs:798` | — |
| PI2 | **The registry-full `FORBID` for `pi4-regression.spec` is READY and UNWRITTEN — one line, precondition measured, and it has been sitting in an unversioned queue file instead of a ledger.** The window registry refuses a registration when it is full; nothing in pi's spec forbids that refusal, so a boot that silently loses windows to a full registry passes the gate. Precondition MET and measured by orin: `[wm] winid-register REFUSED` = **0** across two `UNAOS_PIDESK=1 kernel8-test` runs, 6 holders plus fixture against a ceiling of 8 — so the FORBID cannot red on current behaviour and is safe to add. ⚠ Adding it moves the derived floor by one (120 → 121 at the time of measurement; the floor is 126 as of `2461094b`) — **RECOUNT from `parse_spec` at the tree you land on, never quote a floor across trees.** Migrated out of `pi-focus-queue.md` 2026-09-11 so the queue can be deleted without losing it. **Lifted from `LEDGER.md` SP7 2026-09-11 when pi's arch ledger was created (S24); affects pi.** | pi | QEMU raspi4b | open — one line, not written | orin's two-run measurement; floor re-derived per tree, not inherited | — |
| PI3 | **`[wc-d] moved=` is emitted, never consulted, and tightening it FIRST manufactures a red with no defect behind it — BLOCKED ON PURPOSE.** The verifier already emits `moved=` (`wm.rs:6682`; `wm.rs:6192` defines it as the reference moving underneath the sample) and `pi4-regression.spec` ignores the field entirely — both the REQUIRE and the FORBID that could read it do not. So the spec has a field available to it that means something and is unread. **The order matters and is the whole content of this row: tighten ONLY after the fixture emits a distinct verdict (`-> RESAMPLE`) for `moved != 0`.** Tighten first and every legitimate resample reds a gate that has found nothing — wrong-strict, which this lane holds to be worse than wrong-lenient. Mechanism is SO7's. Migrated out of `pi-focus-queue.md` 2026-09-11. **Lifted from `LEDGER.md` SP8 2026-09-11 when pi's arch ledger was created (S24); affects pi.** | pi | QEMU raspi4b | open — blocked deliberately, not stalled; the block is the finding | `wm.rs:6682` emitter, `wm.rs:6192` definition, `pi4-regression.spec` REQUIRE/FORBID pair that ignore it | — |
| PI4 | **`/volumes` and `/usb` are DOCUMENTED-AND-UNGATED on pi, and that is stronger evidence than plain absence.** Measured at `1b54c03f`: `grep -c -e '^REQUIRE' -e '^COUNT' -e '^FORBID' -e '^OPTIONAL' -e '^PENDING' -e '^COMPLETE' <spec>` intersected with `-e /usb -e /volumes` over BOTH scored specs returns **0**, while `pi4-regression.spec:2429` and `:2432` discuss them in comments — `:2432` spelling out the exact boundary case, that a `/usbfoo` sibling must not be mistaken for `/usb`. Control that the pattern can fire at all: the same directive-kind filter with `-e K3` on the same file -> **5**, so the zero is about the data. The property is written down where nothing consumes it — the mirror of `wcg.rs`, where the contract sits exactly where its consumer relies on it. ⚠ **CONSEQUENCE: after LABELMOUNT or any later mount-namespace change, a green "no pi row moved" is a TRUE statement about a gate that never looked** — blindness, not safety, and it has now been measured three times. `fs/vfs.rs` calls the Usb BOT WRITE(10) path "verified" in its own comment, so this is a live, deliberate capability with zero coverage on either board **Lifted from `LEDGER.md` SP10 2026-09-11 when pi's arch ledger was created (S24); affects Pi.** | pi | QEMU raspi4b | open — owed: a REQUIRE naming the `/usb` write posture, observed before written | migrated out of `pi-focus-queue.md` 2026-09-11; counts re-derived at `1b54c03f`, not inherited (the queue's `:2335`/`:2338` are 94 commits stale) | — |
| PI5 | **`[pstrip]` is a class-2 fragile FORBID: the gate depends on four of the emitter's fields staying adjacent and NOTHING at the emitter says so — while the sibling family `[wc-g]`/`[wc-h]` carries exactly that contract.** Re-located by content at `1b54c03f` (the line numbers moved with the fold, the emitter is `unaos/crates/kernel/src/ui_status.rs`, NOT `video/ui_status.rs`): `pi4-regression.spec:1549` `FORBID \[pstrip\] rollup samples=... redraws=... skipped=0 srcdelta=0` — four adjacent fields, three bare-space junctions, no `.*` to absorb an inserted field; `:1484` `\[pstrip\] armed .*leds=[0-9] led=` — one junction; `:1132` `\[wc-h\] .*presspread=[0-9] presspop=<alternation> .*-> AT-RISK` — one junction. ⛔ **`:1549` IS ACUTE AND THE COLLISION IS SCHEDULED: the spec's own comment one line above it, `:1548`, says the emitter-side `paced=yes/no` "remains the real fix and is still owed"** — and the emitter at `ui_status.rs:1245` prints `samples= redraws= skipped= srcdelta= rate= srate= gapmax= lat_max_ms= period= decay=`, so landing `paced=` anywhere among the first four silently greens the guard. **CONTRACT ASYMMETRY, proven with a control:** the same grep set (`grep -n -i -e pi4-regression -e 'may be INSERTED' -e 'KEY ORDER IS LOAD-BEARING'`) returns **6 hits in `video/wcg.rs`** (`:1626` *"KEY ORDER IS LOAD-BEARING ACROSS SEATS"*, `:4068` quoting the pi4 pattern and permitting insertion) and **0 in `ui_status.rs`**, which does discuss the gate at `:110`, `:997`, `:1022`, `:1230` but never states what may be inserted, renamed or reordered. So the absence is about the file, not the pattern. ⇒ **ORDER IS THE WHOLE CONTENT: write the `[pstrip]` contract at the emitter FIRST, in `wcg.rs:1626`'s shape, THEN re-key `:1484`/`:1549`.** Re-keying alone leaves the next author unable to learn the rule they are about to break, and tail-append is the safe form for the same reason it is for byte-identity: position is load-bearing and nothing warns you when you move it **Lifted from `LEDGER.md` SP12 2026-09-11 when pi's arch ledger was created (S24); affects Pi.** | pi | QEMU raspi4b | open — contract unwritten; three FORBIDs un-re-keyed; `:1549` will go silently green when `paced=` lands | line numbers re-derived by content at `1b54c03f`; emitter read at `ui_status.rs:1245`; `wcg.rs` control run the same turn | — |
| PI6 | **`pi4-regression.spec:36-40`'s metal CAPSTONE caveat PRE-EXCUSES the exact misses a regression would produce.** The block says some real-Pi boots bring up only 3 of 4 cores, CAPSTONE self-skips, *"on such a boot the CAPSTONE directives below report as misses"*, and a power-cycle usually restores it. The directives it excuses are `:286` `COUNT 6 CAPSTONE \w+: PASS` and `:287` `REQUIRE CAPSTONE COMPLETE`. A scheduler regression that stops secondaries joining produces a miss on exactly those rows and arrives pre-explained as bench variance. **Not a defect to delete — the variance is real and the caveat is honest — but it is a standing instruction to read a red as noise**, which is the one thing a gate must never teach. Conditional retirement: when orin's APS work makes secondaries join reliably on the Pi, the five lines come out and the rows become load-bearing again; until then a post-change pi run must be read with them in view, and the count must be taken from `CAPSTONE \w+: PASS` on the run's own wire rather than from the MBENCH total **Lifted from `LEDGER.md` SP13 2026-09-11 when pi's arch ledger was created (S24); affects Pi.** | pi | Pi bench | open — conditional on orin's APS result; blocked on a measurement, not on a decision | `unaos/scripts/specs/pi4-regression.spec:36-40` and `:286`/`:287` located at `1b54c03f`; migrated out of `pi-focus-queue.md` 2026-09-11 | — |
| PI7 | **`pi4-regression.spec:359` `OPTIONAL K1-atr` is SILENTLY UNFAILABLE and has become load-bearing under it.** `grep -n '^OPTIONAL'` over both scored specs returns exactly one row, `:359 OPTIONAL K1-atr:.*codec PASS`, sitting between two REQUIREs of the same K1 block. `mbench.py` `failed()` returns False and `satisfied()` returns True for anything that is not REQUIRE/COUNT/FORBID, so OPTIONAL cannot fail in the program `arroyo` actually gates with — the hole is in the GATE, not only in the `orin-specscore.py` auditor where it was first sourced. What it guards is the `UNAFS.ATR` owner/grants codec, and the ACL rows that landed with orin 19's eight directives READ that codec (`native_write_authz` does `ino.attributes.get("owner")` and `grants:<principal>`). ⇒ **codec regresses, `K1-atr` goes quiet, run stays green, and the ACL rows evaluate attributes that silently stopped decoding.** Observe-first is already satisfied: `K1-atr` fires once per capture, same as `K1-persist` and `K1-corrupt` — it has been passing, not skipping; it simply could never have said otherwise. ⚠ **`COMPLETE` (`:99`, `:100`) IS A FALSE LEAD — do NOT promote it**: `mbench.py` makes COMPLETE non-failable deliberately so an absent marker is the TRUNCATED verdict, and promoting it converts a truncation signal into a regression signal. Action: promote `:359` to REQUIRE, red-proof first, with a provenance comment recording why it was optional (attributes were a side feature) and why it is not any more (the ACL rows depend on it); recount the floor from `parse_spec` on the tree it lands on **Lifted from `LEDGER.md` SP14 2026-09-11 when pi's arch ledger was created (S24); affects Pi.** | pi | QEMU raspi4b | open — grant #9, offered to orin 20 and correctly declined for fleet-wide floor churn; pi's to land | `grep -n '^OPTIONAL'` at `1b54c03f`; `mbench.py` satisfied()/failed() read this turn; migrated out of `pi-focus-queue.md` | — |
| PI8 | **The Pi issues CMD2 ALL_SEND_CID, latches the 136-bit CID into the response registers, and throws it away on the next line — so the board's block registry can discriminate one card from another only by SIZE.** Re-derived at `1b54c03f`: `drivers/emmc2.rs:556-557` sends `cmd(2)` OR-ed with `CMD_RESP_136` and `CMD_CRCCHK`, and `:559` is CMD3, which overwrites RESP0..RESP3. Population enumerated rather than asserted — `grep -n -e read_resp -e RESP0 -e RESP1 -e RESP2 -e RESP3 unaos/crates/kernel/src/drivers/emmc2.rs` returns 14 occurrences and **none falls between `:557` and `:559`**; the contrast is six lines down, where CMD9 SEND_CSD does `let resp = read_resp(base);` and parses capacity — so both the 136-bit helper and the bit-field extractor already exist in this file and are applied to the CSD only. **Control, same tree, other aarch64 SD driver:** `arch/aarch64/sdmmc_tegra.rs:547-549` does `cmd_step(... CMD2 ALL_SEND_CID ...)` then `let cid = read_resp(base); print_cid(&cid);`, decoding MID, OID, PNM, PRV, **PSN (the 32-bit serial)** and MDT. This is a divergence between two drivers in one tree, not a missing capability. Consequence: the Pi's sole `BlockDeviceInfo` construction site fills four of five fields with literals, so `num_blocks` is the only value that varies with the card, and INSTALL-SEL's durable identity triple reduces to the card's size on this board. ⚠ **The alarming reading is the WRONG one, recorded so nobody re-raises it**: the selfguard's verdict keys on FAT volume serials, not geometry, so a byte-clone of the boot card classifies as BootDevice and is REFUSED — fail-safe. ⚠ **OPEN HALF, NOT ESTABLISHED: whether any card re-probe or hot-plug path exists on this board.** `emmc2::probe()` runs once on the BSP at boot; with no re-probe the registry cannot notice a swap at all, which is a larger statement than a size collision. Enumerate that population before anything is written down as a finding. Property, not mechanism: whether anything SHOULD read the CID, and whether shared `BlockDeviceInfo` grows a field, is the consuming lane's call **Lifted from `LEDGER.md` SP16 2026-09-11 when pi's arch ledger was created (S24); affects Pi.** | pi | Pi bench | open — measurement complete, consumer question open, re-probe population not yet enumerated | pi 10's M1-M4 re-derived line-by-line at `1b54c03f` this turn; `emmc2.rs:556-565`, `sdmmc_tegra.rs:546-549`, `:611-628` | — |
