# UnaOS Security — model, threat framing, hardening ledger

> Engineering companion to the design vision in
> [`dev/OS/04_SECURITY_IMMUNITY/`](dev/OS/04_SECURITY_IMMUNITY) — the
> permission model there ("no root; apps start with zero permissions; the OS
> passes single file handles, not folder access") is the direction; this
> document tracks the concrete threat model, the implementation chain, and the
> per-arch hardening state. Arc sequencing lives in [`ROADMAP.md`](ROADMAP.md).
>
> Rule: **an arc that lands a ledger item checks it off here in the same
> commit.**

## Threat framing

UnaOS is being built to hold *direct authority over physical hardware*:
G-code streams to a 3-D printer, PWM to a vehicle's throttle, GPIO, SDR. A
compromise is not just data loss — it has physical consequences. Security is
therefore foundation work, not a retrofit (BeOS deferred multi-user and paid
for it; we won't repeat that).

Exposure classes, honestly stated:

1. **Ring-0-resident parsers.** The network stack (Ethernet/ARP/IP/TCP/DHCP),
   USB descriptor parsing, the FAT reader, and (since UNAFS-K3, aarch64) the
   unafs mount parser (partition table / superblock / inode / catalog decode
   in `fs/unafs.rs` + `unaos/libs/fs/unafs`) all parse attacker-influenceable bytes with
   full kernel privilege. As of UNAFS-K4 the unafs mount is read-WRITE (the
   kernel commits blocks to the medium through it — see the K4 write-path scope
   at the bottom of this ledger). Until the privilege boundary exists everywhere, these
   are the practical attack surface, and they remain kernel-resident even after.
   The unafs parser joined on the same basis FAT shipped on, and is now the
   first of the four hardened against a crafted volume (BEFS-HARDEN,
   2026-07-13): every disk-derived size/count/offset is bounded against the
   volume span at the parse boundary (`Superblock::validate`), disk-sized
   allocations are fallible (`try_reserve` → graceful `Err`, never a capacity
   panic or OOM abort), extent arithmetic is `checked_*`
   (profile-independent), hole-fill/free walks are bounded, and record decodes
   carry bincode byte budgets so a crafted length prefix fails BEFORE
   allocating. Residual, honestly stated: for extent-backed records the decode
   pre-allocation is capped (4 MiB record ceiling — the panel lowered it from 64 MiB, which exceeded the 48 MiB kernel heap and so was itself an abort vector; a passing bincode claim allocates infallibly, so the ceiling IS the max forced allocation), not eliminated. The FAT
   reader's equivalent audit is still open — see the checklist below.
2. **Privilege boundary not yet load-bearing (both arches).** The ring-3/EL0
   boundary now exists on BOTH architectures (x86 U1a/U1b: ring-3 round-trip +
   per-page perms + fault→task-kill; aarch64 M6a/M6b, the pioneer). x86 U2 runs
   the FIRST real program loaded FROM DISK in it (a flat ring-3 binary read off
   the FAT volume) — **metal-confirmed on the real 2012 rMBP (2026-07-03): a
   Realtek USB3 SD reader → FAT16 card → `HELLO.BIN` (72 B) read + executed in
   ring 3 (`hello from disk` -> PASS)**. But that program is still trusted only by
   ring-3 isolation (no signature/allowlist check yet — U2's untrusted-loader note
   below), and the kernel's own subsystems (drivers, FS, net) remain ring-0.
3. **No identity or authorization layer yet** — no principals, no grants;
   any code that runs can do anything. This is what the U-chain builds.

## The model: capabilities first, POSIX-layerable

Decision (2026-07-02): UnaOS targets a **single primary human with
capability-isolated software principals**, not classical login/uid multi-user.

- Every vessel, handler, and agent is a **principal** with an explicit grant
  set (e.g. Comscan: `serial:acm0`, `gpio:*`; Vug: `fs:/models rw`; an
  untrusted download: nothing).
- **Handles are capabilities.** The per-process handle table is the enforcement
  point: every syscall that touches a resource goes through it. Grant, attenuate,
  revoke are first-class operations; handle transfer over the bandy bus is
  unforgeable. *(Status: arc **U4** built the table STRUCTURE — child-process
  handles, keyed by ASID, with reaping owner-scoped by construction — on aarch64;
  arc **U5** (2026-07-05, aarch64) turned it into the CHECK — handles now carry a
  rights bitmask, checked at the single lookup point (`handle_resolve`); grant/
  attenuate/revoke are first-class (`SYS_CAP`, with the monotonic-decrease
  attenuation invariant enforced); `sys_write` routes through the table (a
  `CONSOLE` handle with `CAP_WRITE`, endowed at spawn — no ambient stdout); and a
  process's handles are cleared on ASID teardown. Arc **U6a** (2026-07-06, aarch64)
  generalized the descriptor into a **general object table** — a handle is now
  `(kind, target, rights)` with the kind (`Child`/`Console`/`File`/`Socket`) in a
  parallel sidecar, first-free-allocated for ALL kinds; the fixed `CONSOLE_FD` pin
  is retired (the reserved-index allocator closes the print-AND-spawn collision).
  The x86 twin **U6x** (2026-07-06) mirrors it (keyed by address-space SLOT rather
  than ASID). Arc **U6b** (2026-07-06, aarch64) made the `File` kind REAL —
  `SYS_OPEN`/`SYS_READ` route a disk read through a `File` handle gated by
  `CAP_READ` (the first resource syscall on a non-Console object, backed by a
  per-task open-file table; the hook UnaFS `grants:*` enforcement will ride).
  Its x86 twin **U6bx** (2026-07-07) brings the arches to File-handle parity,
  serving reads from a **BSP-staged source** (the IF-masked x86 SYSCALL path
  cannot drive the `hlt()`-ing xHCI read; the staged-set constraint was
  RETIRED by the STOR-1 S1–S7 arc — the interrupt-driven storage service task —
  which routes reads live and, at S7, resolves an open of ANY on-disk file).
  Arc **U7** (2026-07-07, aarch64) landed the cross-process CORE: an
  inbox-mediated `SYS_XFER`/`SYS_RECV` (attenuated, owner-scoped, the
  single-writer-per-row invariant preserved by construction) + a sender-owned
  single-level revoke that reaches the received cap at its next resolve.
  Its x86 twin **U7x** (2026-07-07) restores full parity (keyed by
  address-space SLOT; the shared kernel window is refused as a transfer
  endpoint) and folds both U6bx ledger notes (atomic offset claim;
  File-revoke descriptor release).
  Arc **U8** (2026-07-07, aarch64) closed U7's two documented escapes:
  revocation TREES (a derivation ledger — revoke chases re-grants and
  re-transfers, and `CAP_REVOKE` gained its real subtree semantics) and
  generation-tagged inboxes (the sys_xfer TOCTOU residue).
  Arc **U9** (2026-07-07, aarch64) gave `CAP_WRITE` on a `File` real meaning —
  EMMC2 sector writes, an in-place-only `fat::write_at`, `SYS_SEEK`, and a
  File+`CAP_WRITE`-routed `sys_write` (the same `handle_resolve` CHECK, so a
  revoked write-cap is `-EACCES` for free) — the last rung before UnaFS
  `grants:*` on open. Its x86 twin **U8x** (2026-07-07) restores full parity (a
  slot-keyed derivation ledger + per-slot inbox generations; the shared kernel
  window stays refused as a transfer endpoint), so the whole capability chain —
  U4→U8/U8x — is now arch-symmetric through revocation trees.
  Arc **U10** (2026-07-08, aarch64) gave the FAT writer an ALLOCATOR: file
  **growth** (`fat::write_grow` — allocate + zero-fill + chain new clusters, dir
  `size` bumped LAST), **create** (`SYS_OPEN` `O_CREAT` → `fat::create_in_root`),
  and **delete** (`SYS_UNLINK` → `fat::delete_located`). Grow, create, AND delete
  all ride `sys_write`'s ONE `handle_resolve(asid, fd, CAP_WRITE)` CHECK (create
  via an O_CREAT open that endows the write cap), so an RO-opened / U8-revoked /
  wrong-kind handle can never mutate the volume — no new enforcement code. The
  FAT-safety invariants are load-bearing: every FAT mutation writes ALL `num_fats`
  copies; the free search is bounded and returns `-ENOSPC` when full; a new cluster
  is **zero-filled before it joins a chain** (no stale-byte disclosure into a grown
  region); the directory `size` (the reader's truth) is written last so a crash
  mid-grow leaves a consistent smaller file; and delete marks the dir entry `0xE5`
  BEFORE freeing the chain, so a crash can never leave a live entry aliasing freed
  clusters. Additive on the shared `fat.rs` — x86 never calls the writers.
  Its x86 counterpart **U9x** (2026-07-08) brings the arches to File-WRITE parity:
  `SYS_SEEK`, an RW `SYS_OPEN` mode bit, and a File+`CAP_WRITE`-routed `sys_write`
  (the same single `handle_resolve` CHECK, so a revoked write-cap is `-EACCES` for
  free). Because the IF-masked x86 SYSCALL handler cannot drive the `hlt()`-ing
  xHCI write pump, the write is **STAGED** — an in-place overwrite of a
  per-descriptor in-memory buffer a read-back witnesses through the same cap (the
  write twin of U6bx's staged read); M1 is in-memory only, the BSP-side disk
  write-back is M2. Its ALLOCATOR twin **U10x** (2026-07-08) then landed
  grow/create/delete on x86 — every FAT mutation DEFERRED out of the IF-masked
  handler to a launcher IF=1 drain (a separate op-queue; the shared `fat.rs` writers
  reused VERBATIM, x86 adds only a read-only `cluster_size()` accessor). It rides the
  SAME single `handle_resolve(row, fd, CAP_WRITE)` CHECK (grow and unlink are
  mutations; create via an O_CREAT open that endows the write cap), plus a scaffold
  guard that refuses `SYS_UNLINK` on an immutable STAGED file (HELLO.BIN is EL0 code —
  only a runtime-created descriptor is unlinkable, a flag reset on every slot free) and
  a per-descriptor `FILE_OPNAME` binding so a deferred op ALWAYS names the descriptor's
  OWN file (no confused-deputy: a RW SCRATCH.BIN holder can never mint a GROW.BIN op).
  x86 divergences are ledgered honestly: the deferred DELETE is a launcher-side replay
  (weaker causal fidelity than pi4's in-handler unlink), its sibling-`-EACCES` proves
  gen-invalidation not a freed-chain aliasing fail-safe (no on-disk chain exists
  pre-drain), and grow is one-page-bounded. **U11x M2** (2026-07-10) then brought the
  arches to unlink-defers-free parity: a GLOBAL per-name open refcount defers the
  deleted file's op to the LAST close ACROSS processes (teardown counts as close),
  so the delete now FIRES at the correct lifecycle point even though the disk half
  remains a replay (the causal-fidelity gap closes at the lifecycle layer). UnaFS `owner`/`grants:*` enforcement on the
  by-name namespace — the last cap gap — landed on the **aarch64** track as **U6**
  (owned-by-default at `SYS_OPEN` + `SYS_FGRANT` delegation, an in-kernel seam the
  on-disk attributes will feed; see the aarch64 ledger); the **x86 twin (U6x)**, the
  bandy Ring-3 delegation wrapper, subdirs/LFN/rename, and write-back caching remain
  future. See both ledgers below.)*
- **UnaFS stores the metadata natively**: `owner` and `grants:*` are ordinary
  typed attributes — queryable ("show every file the midi-agent can write"),
  and no on-disk format change is ever needed.
- **POSIX hedge**: if human multi-user is ever wanted, a "user" becomes a
  named bundle of capabilities layered on the same attributes. Nothing in the
  chain below has to be redone.

Implementation chain: **U1a → U1b → U2 → U3 → U4 → U5 → U6** (x86 leads, Pi
ports, Jetson joins — full table in `ROADMAP.md` §1).

## Hardening ledger

### x86_64
- [x] Ring-3 boundary: user GDT segments (DPL-3 code/data, SYSRET-compatible STAR), `TSS.RSP0`, SYSCALL/SYSRET path (`EFER.SCE`, `LSTAR`, `STAR`, `FMASK`) (U1a, 2026-07-02, **QEMU-verified; metal pending**)
- [x] EFER.NXE enabled; NX on all user data/stack pages; user **code page W^X enforced across cores** — mapped ring3-RX **read-only from the start** at `USER_BASE` (the blob is copied through the identity alias, never through `USER_BASE`), so no core can ever cache a writable mapping of it; supersedes the U1a single-core `invlpg` flip (U1a/**U1b B4**, 2026-07-02, **QEMU-verified + metal-confirmed** on the real 2012 rMBP: `u1b-code-write` faulted with `err=0x7` (Write bit set) on the real page tables)
- [x] User fault → task kill, never kernel halt — a ring-3 fault (`CS.RPL == 3`) on #PF/#GP/#UD/#SS/#NP/#DE/#BR/#AC `swapgs`es, logs `(task, vector, cr2)`, and kills the task via `sched::exit()`; a CPL-0 fault stays fatal (kernel bug never hidden). (**U1b A**, 2026-07-02, **QEMU-verified + metal-confirmed** on the real 2012 rMBP: 3 kills at `vec=14 err=0x7/0x7/0x15`, `exited=1 killed=3 -> PASS`, **kernel continued past all three kills** — proving `swapgs`/GS restore work on silicon)
- [x] SYSRET boundary hardening: caller-saved GPR scrub (`rdi/rsi/rdx/r8/r9/r10` zeroed before `sysretq` — no kernel-dispatcher pointer leaks to ring 3) + canonical-`rcx` guard (a non-canonical return RIP — the CVE-2012-0217 shape — is refused and the task killed, never `sysret`ed with the user rsp loaded) (**U1b B1/B2**, 2026-07-02, **QEMU-verified + metal-confirmed for the pass-through** — the ring-3 round-trip returns cleanly through the scrub+guard tail on real silicon. The guard's *refusal* logic is now **logic-tested** (U2 Part-0c, 2026-07-02: the callable twin `rcx_canonical` refuses `0x8000_0000_0000_0000`, QEMU-verified — `:: U2-0c: canonical-rcx guard refuses … -> PASS ::`); the end-to-end refusal — ring-3 code actually returning to a non-canonical RIP, which is unreachable from our 1 TiB window — is still future)
- [x] NMI on a dedicated IST stack — an NMI landing in the pre-`swapgs`/pre-stack-switch window of `unaos_syscall_entry` switches to a kernel stack unconditionally, so its frame can never be pushed onto the ring-3 stack; the handler stays GS-free (**U1b B3** + **U2 Part-0c**, 2026-07-02, **QEMU-verified, fire path now exercised**: a real self-NMI — `apic::send_ipi(<own apic id>, 0x4400)`, level-assert | NMI delivery mode — is confirmed **taken on the NMI IST stack** (the handler compares its RSP against the IST bounds): `:: U2-0c: self-NMI taken on IST -> PASS ::`. This positively demonstrates the unconditional IST switch, upgrading the earlier "slot installed" claim. Still future: metal re-confirmation of the self-NMI, and a fixture that lands the NMI *specifically* mid-syscall-entry — the IST switch is CPL-independent, so the self-NMI is representative, but the exact window has not been targeted)
- [x] CR4.SMEP set — code gates it on `CPUID.7:EBX.SMEP` and sets it when present; **metal-confirmed active on the real 2012 rMBP** (`:: SMEP on ::` photographed 2026-07-02 — TCG `qemu64` does **not** expose SMEP, logging `SMEP unsupported`, so this is the one line QEMU cannot show). A SMEP-*violation* functional test (supervisor fetch from a ring-3 page → #PF) is still future. **SMAP unavailable pre-Broadwell** — compensate with explicit `copy_from_user` discipline
- [x] #DB (vector 1) + #MC (vector 18) each on a dedicated IST stack (**U2 Part-0a**, 2026-07-02). #DB closes a user-triggerable DoS: `RFLAGS.TF` is writable at any CPL (`popfq`), so ring 3 can arm single-step then `SYSCALL`; the pending trap lands on the first `LSTAR` instruction at CPL 0 with GS/RSP still ring-3, and with #DB absent from the IDT that escalated to a same-CPL #NP whose frame landed on the user-writable stack → misread as a kernel fault → halt. The GS-free #DB handler kills a ring-3 #DB, and for a CPL-0 #DB whose RIP is inside the `unaos_syscall_entry` stub clears TF and `iretq`s (resumes the syscall); any other CPL-0 #DB is fatal. #MC is fatal on its own IST. **QEMU-verified**: installed + boot-verified, `:: U2-0a: TF+SYSCALL survived -> PASS ::` (kernel not halted). **Fire path of the #DB *resume* branch is metal-only** — QEMU TCG does not model the TF-on-`SYSCALL` trap (`db_resumed=0`; the fixture exits cleanly instead), so the resume itself is exercised only on real silicon; the #MC fire path is untested (no machine-check fixture). **DR7 is now cleared per-CPU at syscall init (U2.5 Part 0-ii, 2026-07-03, QEMU-verified)** — `mov dr7, 0` beside the SMEP write, logged once as `:: U2.5-0: DR7 cleared ::` — so a firmware-left hardware breakpoint can no longer raise a #DB inside the entry stub: the resume policy's "TF is the only #DB source there" assumption is now *enforced*, not merely assumed
- [x] First-entry GPR scrub (`user_task_trampoline`) — every GPR but `rsp` is zeroed after the five `iretq`-frame words are pushed and before `iretq`, so no live kernel value (the Task Box pointer, kernel-stack top, entry VA) reaches ring 3 in a register at first entry. The x86 twin of the aarch64 M6d first-`eret` scrub; complements the U1b SYSRET scrub, which covered only the return half (**U2 Part-0b**, 2026-07-02, **QEMU-verified**: U1a/U1b/U2 ring-3 entry all still function — the scrub does not disturb the entry ABI, which takes no register arguments)
- [ ] W^X audit of kernel mappings (no page both writable and executable) — note: `CR0.WP` is briefly cleared around the U1a/U1b/U2 page-table edits (firmware maps its tables read-only) and restored; scope is the mapper only. (The U2 loader writes the loaded program through the code frame's identity alias, NOT through `USER_BASE`, and needs no `CR0.WP` toggle — the frame is ordinary RW heap; the ring-3 mapping stays read-only, so W^X across cores holds by construction.)
- [ ] Validated user-pointer access (`copy_from_user`/`copy_to_user`)
- [x] **Per-process address spaces (U3, 2026-07-04, QEMU-verified; metal-pending) — the x86 mirror of aarch64 M6d.** Each user process now runs in its OWN top-level page table (its own CR3): a static 8-slot pool (`memory.rs` `SLOT_PML4/PDPT/PD/PT` + backing + `SLOT_USED`), each slot SHARING the kernel half — `build_slot` copies every live kernel PML4 entry EXCEPT PML4[2], so the kernel mappings (identity map incl. high MMIO like the xHCI BAR at 768 GiB, heap, kernel code/stacks, IST/percpu/GDT) are shared by pointing at the SAME kernel next-level tables — while owning a PRIVATE PML4[2] user window at `USER_BASE`. `user_task_trampoline` installs the task's CR3 (`Task.user_cr3`) before the `iretq`; `exit` restores the kernel CR3 then frees the slot. Two processes can map the same VA to different frames. **Evidence:** a deterministic kernel isolation probe (two slots, same VA, distinct sentinels, swap CR3 and read — `:: U3: per-process CR3 isolation … distinct -> PASS ::`) plus two ring-3 tasks each in their OWN CR3 each reading their slot-private sentinel (`:: U3: 2 ring-3 tasks each in a private CR3 read their own sentinel … -> PASS ::`); U1a/U1b/U2/U2.5 byte-identical (the `user_cr3 == 0` shared-window path is untouched). **Scope is honest:** plain `mov cr3` (full non-global-TLB flush) — **no PCID** (the x86 analogue of M6d's ASID) yet; a **static 8-slot pool** (hard cap = STOP tripwire) — no dynamic user-memory allocator yet; **per-slot table freeze** (a kernel PML4-*entry* added post-boot would need mirroring into live slots — the kernel never adds one; edits BELOW the PML4 propagate via the shared tables); ring 3 still **cooperative** (the CR3 switch lives in the trampoline/`exit`, not a preemptive dispatch path — that moves when preemptible ring 3 lands). The probe's supervisor read of a user page relies on **no SMAP** (correct on the Ivy Bridge target; a SMAP-capable port must use an AC-toggle or validated copy). **Metal-pending** (rides the next reflash / FTDI cable day).
- [x] **Preemptible ring 3 (U3.5, x86 twin of aarch64 M6e; 2026-07-04, QEMU-verified + ★ METAL-CONFIRMED real 2012 rMBP).** A ring-3 task can now be dropped **preemptible** (`Task.preemptible` → `user_task_trampoline` sets `RFLAGS.IF` in the `iretq` frame), so the local-APIC timer evicts it and other work shares its core — closing the one-core DoS a never-syscalling program (`jmp $`) was. The timer **and reschedule-IPI** ISRs — the two maskable handlers that touch per-CPU GS — conditionally `swapgs` on a CPL-3 entry (like the U1b fault handler) so `this_cpu()`/the scheduler resolve the kernel per-CPU block, and swap back before `iretq` (the xHCI/NIC MSI handlers and `eoi()` touch no GS, so they need no swap); the per-process **CR3 install moved from the trampoline into the scheduler DISPATCH path** (`run()`), so it now covers a resumed-after-preemption task (which never re-enters the trampoline) as well as first entry. The full user register file is preserved across preempt/resume by the existing `x86-interrupt` save + `switch_context` machinery (the same that already preempts kernel tasks). **Evidence:** `:: U3.5: ring-3 preemption — IRQs-at-ring3=<n>, co-task ran, spinner resumed -> PASS ::` — a preemptible spinner (increments a private-CR3 counter, never syscalls) is preempted (`IRQs-at-ring3 > 0` — cooperative ring 3 can never show this), a kernel co-task on the SAME core runs to completion (the DoS fix), the spinner's counter keeps climbing across preemptions (correct resume through the CR3-at-dispatch path), and a watchdog reaps it via a scheduler `KillSwitch`; U1a/U1b/U2/U2.5/U3 byte-identical (they stay `preemptible=false` — IF clear, cooperative FIFO, never preempted). **Scope is honest:** a **per-task opt-in** (only the U3.5 spinner is preemptible); **FP/SIMD is NOT saved across preemption** (no FXSAVE/FXRSTOR context switch — see the FP item below); only **one user task per core** at a time (TSS.RSP0/`syscall_kernel_rsp` are set at first entry only — a second concurrent ring-3 task per core would need RSP0-at-dispatch, an M7-twin arc); plain `mov cr3` (no PCID). **★ METAL-CONFIRMED on the real 2012 rMBP (2026-07-04, bootlog photo):** `:: SMEP on ::` (real supervisor-execute protection ACTIVE while the preemptible spinner ran — the metal-only win TCG can't show) then `:: U3.5: ring-3 preemption — IRQs-at-ring3=156, co-task ran, spinner resumed -> PASS ::`; the real timer preempted the ring-3 spinner 156× and the swapgs-on-ring3-timer + CR3-at-dispatch + reap all ran correctly on Ivy Bridge, with every prior fixture PASS (U1a/U1b/U2-0a/U3, byte-consistent) and 0 unexpected faults. The same boot also **metal-confirmed the U2.5 APIC ms-clock fix** (`initcnt=6236 [1 kHz calibrated]`, `ms-clock 999 Hz` — the old ~119 Hz bug gone). (Item surfaced by the U2 integrator review, 2026-07-02; closed by U3.5.)
- [x] **Process model — `sys_spawn`/`sys_wait` + per-process handle table (U4x, x86 twin of aarch64 M7/U4; 2026-07-05, QEMU-verified; metal-pending).** The x86 process abstraction U3/U3.5 began now closes: a parent loads a child program into its OWN address space, runs it ring-3, and reaps it by an owner-scoped HANDLE. **Part 0 (the enabler):** the `TSS.RSP0` + per-CPU `syscall_kernel_rsp` install MOVED from `user_task_trampoline` into the scheduler DISPATCH path (`run()`), beside U3.5's CR3-at-dispatch — so a task RESUMED after a block/preempt (which never re-enters the trampoline) gets ITS OWN kernel stack installed. This closes a use-after-free a second concurrent user task per core would otherwise hit: task B's syscall/fault would land on task A's kernel stack (installed at A's first entry and never updated on B's resume) — and if A had exited and its `Box` kernel stack been freed, on a freed/reused stack. Concretely: parent blocks in `sys_wait`, child A (first entry) sets `syscall_kernel_rsp = A_kstack`, A exits and frees it, child B runs+exits, the parent RESUMES (no trampoline) and its next SYSCALL does `mov rsp, gs:[syscall_kernel_rsp]` — which is `parent_kstack` only because dispatch re-installs it. This CLOSES U3.5's "one user task per core" scope note. **Part A:** `SYS_SPAWN=8` returns a small HANDLE index into the caller's per-process handle table (`HANDLES`, keyed by the caller's address-space SLOT — the x86 stand-in for aarch64's ASID, read from the live CR3 via `current_slot`); `SYS_WAIT=9` blocks on the child's done-semaphore, returns its exit status, and CONSUMES the handle (a second wait ⇒ `-ECHILD`). `PROCS` (pid-keyed control blocks: `status`/`state`/`done`) and `HANDLES` (slot-keyed ownership namespace) are separate, static, const-init — no heap — mirroring the reviewed aarch64 U4. Reaping is structurally owner-scoped: a task can only wait a handle in its OWN slot's row. **Part B:** a parent spawns TWO children and reaps both by distinct handles; a separate orphan's `sys_wait(0)` returns `-ECHILD` (same index 0, different slot row — proving the tables are per-process). **Evidence:** `:: U4x: x86 process model — parent reaped 2 children by handle, non-child sys_wait -ECHILD -> PASS ::` under `UNAOS_FATIMG=1` / `test-fat part` / `test-fat sf`; U1a/U1b/U2/U2.5/U3/U3.5 **byte-identical** (proven by a pre-change baseline diff — the RSP0-at-dispatch move does not disturb the cooperative single-user-task path); `./arroyo check` both arches, **zero aarch64 files touched**. **Load path — an honest x86 divergence from the aarch64 twin:** aarch64's `sys_spawn` reads `HELLO.BIN` off the SD card INSIDE the SVC handler because its EMMC2 driver is PIO (no interrupts). x86 storage is USB-mass-storage over xHCI, whose BOT read pump `hlt()`s to await the async completion — and `hlt` with IF=0 hangs forever, while the SYSCALL handler runs IF-masked (SFMASK clears IF). So the FAT read is hoisted OUT of the syscall: `HELLO.BIN` is pre-staged off FAT ONCE by `stage_hello` on the BSP main loop (IF=1 — the proven U2 read path), and `sys_spawn` instantiates each child by a pure memcpy of the staged bytes into a fresh slot (IF=0-safe). The child still runs storage-loaded bytes gated on storage being present — the observable behavior is the aarch64 twin's. The untrusted-loader posture is unchanged (size-bounded to one code page; contained by ring-3 + NX + W^X + SMEP + the U1b fault-kill net; no signature gate — the shared code-signing item). **Scope is honest:** the fixtures are **register-only** and **cooperative** (IF clear — never preempted in ring 3); a `MAX_PROCS = 4` cap and the static 8-slot pool (STOP tripwires); no PCID; no `copy_from_user` (program-by-name `sys_spawn` with a validated user name is a later arc — a STOP tripwire here); handle rows are NOT cleared on slot teardown (harmless today — reapers consume their handles and the demo holds distinct slots — but a lifecycle concern once processes churn slots: that + the capability CHECK + grant/attenuate/revoke is U5, exactly the aarch64 U4→U5 boundary). Two independent adversarial reviews (an 8-lens sweep + a 3-lens deep pass on the RSP0-at-dispatch use-after-free, the spawn/wait concurrency+leak+hang surface, and the blob's cross-syscall register ABI) each returned **0 findings**. **Metal-pending** (rides the next reflash / FTDI cable day).
- [x] **Handles as capabilities — the enforcement CHECK + grant/attenuate/revoke + routed `sys_write` + teardown-clear (U5x, x86 twin of aarch64 U5; 2026-07-06, QEMU-verified; no metal this arc — pure syscall logic, and the child loads ride U2/U4x's already-metal-confirmed FAT path).** Turns U4x's handle STRUCTURE into a real capability, mirroring the reviewed aarch64 U5 with one substitution: aarch64 keys the tables by ASID; x86 keys by the address-space **SLOT** index (`memory::current_slot()`, read from the live CR3). A handle now carries **rights** (a `CAP_READ|CAP_WRITE|CAP_EXEC|CAP_GRANT|CAP_REVOKE` bitmask, held in a **sidecar** `HANDLE_RIGHTS[[AtomicU32; 8]; USER_SLOTS+1]` keyed identically to `HANDLES`, so U4x's `0`/`RESERVING` value-word sentinel logic is byte-unperturbed) and names a **target** beyond "child pid" (a well-known `HANDLE_CONSOLE = u64::MAX-1` token — kept to two kinds, `Child(pid)` and `Console`, NOT a general object table, which is U6). **The CHECK** is a single `handle_resolve(row, idx, req_rights)` at the one lookup point every handle-consuming path goes through: out-of-range/Empty/`RESERVING` ⇒ the caller's own errno (`sys_wait` → `-ECHILD`, preserving U4x structural ownership; `sys_write`/`SYS_CAP` → `-EACCES`), present-but-missing-a-right ⇒ `-EACCES`. **grant/attenuate/revoke** are first-class via `SYS_CAP = 10` (sub-op selector): GRANT mints a new handle to the same target carrying a rights mask that must be a **subset** of the granter's rights on the source (the **monotonic-decrease / attenuation invariant** — `req & !src_rights != 0` ⇒ `-EACCES`; a grant can never amplify), requiring `CAP_GRANT` on the source; REVOKE drops a handle the caller owns (`-EACCES`/`-ECHILD` on subsequent use). **`sys_write` now routes through the table** — `fd` is a handle index that must resolve to a `Console` handle with `CAP_WRITE` (subsuming U1a's `fd==1 → -EBADF`); there is no ambient stdout. Every printing process is **endowed** a `Console`+`CAP_WRITE` cap at the conventional index `CONSOLE_FD = 1`: the **shared kernel window** (U1a/U1b/U2, which run with `user_cr3 == 0` so `current_slot()` is `None`) is given a dedicated `SHARED_ROW` (index `USER_SLOTS` — the x86 twin of aarch64 ASID 0, since x86 has no ASID-0 row) endowed in `setup()`, and each spawned child in `sys_spawn`; the existing `USER_BASE` range-check + all-or-nothing `-EFAULT` semantics are byte-identical, so a hostile pointer still yields `-EFAULT`, not `-EACCES`. **Teardown-clear** (folds U4x's one deferred lifecycle note): `memory::free_user_space_by_cr3` now wipes the whole `HANDLES[slot]` row + its rights **before** releasing the slot's used-flag (not after — a post-release clear could race a concurrent `alloc_user_space` on another core that reclaims the same slot), and because BOTH teardown paths (normal `exit` and the KillSwitch reap of a never-exiting preemptible task) funnel through that one function, the clear rides both. Evidence — the inline `u5x-cap` fixture proves the four ring-3-observable behaviours against its own slot's table (conveying a 4-bit witness bitmask as its `sys_exit` status, routed by task **name** — x86 needs no `SYS_REPORT`) and the launcher proves the fifth kernel-side: `:: U5x: x86 capabilities — write-cap OK, no-cap -EACCES, attenuated grant bounded, revoke enforced, teardown-clear clean -> PASS ::` under `UNAOS_FATIMG=1` / `test-fat part` / `test-fat sf`, with every U1a/U1b/U2/U2.5/U3/U3.5/U4x verdict byte-identical (all prior `hello from ring 3`/`hello from disk` prints land — their processes hold the endowed console cap); `./arroyo check` both arches, **zero aarch64 files touched**. Still deferred to **U6** (the x86 twin of pi4 U6): bandy handle-transfer between principals, a general object table (routing fs/net syscalls through handles, first-free `Console`), and cross-process revocation trees (`CAP_REVOKE` defined but reserved); PCID and `copy_from_user`/`copy_to_user` (the M6f twin) remain separately deferred. **Metal-pending** (pure syscall logic; the child loads ride U2's metal-confirmed FAT path).
- [x] **General object table — `(kind, target, rights)` descriptors, first-free for ALL kinds, the `CONSOLE_FD` collision closed (U6x, x86 twin of aarch64 U6a; 2026-07-06, QEMU-verified; no metal this arc — pure syscall logic, and the demo's children ride U2/U4x's already-metal-confirmed FAT load path).** Generalizes U5x's fixed-shape handle into a general object descriptor, mirroring the reviewed aarch64 U6a with the one x86 substitution (the tables are keyed by the address-space **SLOT**/`row`, not the ASID). **The kind rides in a parallel sidecar** `HANDLE_KIND[[AtomicU8; 8]; USER_SLOTS+1]` (keyed identically to `HANDLES`/`HANDLE_RIGHTS`), so the value word keeps U4x/U5x's sentinels **byte-identical** — `0` = Empty (the lock-free allocator's free marker), `u64::MAX` = `RESERVING` (an in-flight claim) — and nothing else is reserved. A handle now names one of four kinds — `Child(pid)` (U4x), `Console` (U5x), and the **scaffolds** `File(id)` / `Socket(id)` (defined + resolvable via `handle_resolve`, though no fs/net syscall routes through them yet — they prove the table is genuinely general). The sidecar choice is deliberate: it makes the STOP-tripwire sentinel-collision reasoning *structurally impossible* (a `File`/`Socket` id may be any non-`0`/non-`u64::MAX` word with no high-bit masking), and mirrors the rights sidecar 1:1 (same publish-kind-and-rights-before-the-live-value / observe-after discipline; single-writer-per-slot is the backstop). **The `CONSOLE_FD` collision is closed** — the raison d'être of the arc. U5x pinned the console cap at a fixed index `CONSOLE_FD = 1` via an unconditional store while `handle_install`'s first-free scan allocated children from index 0, so a process that both PRINTED and SPAWNED 2+ children could have a child auto-allocated onto index 1 and then clobbered by the console install (harmless only because no such process existed on x86). U6x makes `CONSOLE_FD` a **reserved index the first-free allocator SKIPS**: the console lives there by the `fd=1`/stdout convention (keeping every prior blob's `sys_write(fd=1)` byte-identical), and auto-allocated child/object handles fill `{0, 2, 3, ..}` — so a console cap and N child/object caps coexist with **zero index collision, for any interleaving of installs**. Every existing consumer is behaviour-preserved: `handle_resolve` dispatches on the kind sidecar (`sys_wait` requires `Child`, `sys_write` requires `Console`+`CAP_WRITE`, `sys_cap` grant/revoke on any kind); the **attenuation invariant is unchanged** and the mint now copies the source handle's kind (a grant attenuates rights, never re-kinds the object) and publishes the value LAST; `handle_clear`/`clear_handle_row`/`handle_row_is_clear` (the U5x teardown-clear) also clear/verify the kind. Evidence — the `u6x-spawn` fixture is the printing spawner U5x could not serve: it prints, spawns 2 children (distinct auto-allocated handles, both off the reserved index), prints AGAIN (proving the console cap survived the spawns), and reaps both by handle (conveying a 4-bit witness bitmask as its `sys_exit` status, routed by task **name** — x86 needs no `SYS_REPORT`); the launcher additionally proves kernel-side that the `File`/`Socket` kinds resolve to their kind with the required right (and `Denied` without) and that the exact U5x-breaking console-vs-two-children interleaving no longer collides: `:: U6x: x86 general object table — printing spawner + 2 children, no index collision, File/Socket kinds resolve -> PASS ::` under `UNAOS_FATIMG=1` / `test-fat part` / `test-fat sf`, with every U1a/U1b/U2/U2.5/U3/U3.5/U4x/U5x verdict **byte-identical** (all prior `hello from ring 3`/`hello from disk`/`u5x: cap write` prints land); default no-FAT `./arroyo test` stays MISSION SUCCESS with U6x skipping cleanly (like U4x — its children need the staged program); `./arroyo check` both arches, **zero aarch64 files touched**. **Honest scope — this is the OBJECT TABLE only.** Deferred to **U7**: cross-process handle-transfer (`SYS_XFER` — a cross-slot write discipline that breaks the single-writer invariant) + revocation trees (`CAP_REVOKE`, still reserved) + the bandy Ring-3 delegation wrapper; and to a later arc: real `File`/`Socket` fs/net syscalls routing through these kinds. PCID and `copy_from_user`/`copy_to_user` (the M6f twin) remain separately deferred. **Metal-pending** (pure syscall logic; the child loads ride U2's metal-confirmed FAT path).
- [x] **Real `File` handles — `SYS_OPEN`/`SYS_READ` routed through the object table via `File` + `CAP_READ`, served from the BSP-staged source (U6bx, x86 twin of aarch64 U6b; 2026-07-07, QEMU-verified; no metal this arc — pure syscall logic, and the staged source rides U2/U4x's already-metal-confirmed FAT path).** Makes U6x's `File` scaffold real: the first resource syscall routed through a **non-Console** object on x86, bringing the arches to File-handle parity. **The honest x86 divergence (the U4x pattern):** pi4's U6b reads the FAT volume INSIDE the SVC handler (EMMC2 is PIO); x86 CANNOT — storage is USB-mass-storage over xHCI, whose BOT read pump `hlt()`s awaiting the async completion, and the SYSCALL handler runs IF-masked — so **`SYS_OPEN = 11`** opens a file from the **BSP-staged set** (files the BSP pre-read at IF=1 over the proven U2 FAT path; HELLO.BIN today, the same `stage_hello` buffer `sys_spawn` instantiates children from) and **`SYS_READ = 12`** serves the staged bytes. The CAPABILITY layer is byte-for-byte the pi4 twin; only the SOURCE of the bytes differs. `SYS_OPEN(name_ptr, name_len)` validates the bounded name against the user window (overflow rejected; `-EINVAL`/`-EFAULT`), looks it up in the staged set (`-ENOENT`), records a per-task **open-file descriptor** and installs a `File` handle carrying `CAP_READ` (first-free, skipping the reserved `CONSOLE_FD`); resources are claimed **LAST** with the `sys_spawn` reserve/unwind discipline — a full handle table after the descriptor claim frees the descriptor then returns `-EAGAIN`, so **no failure path leaks a FILES slot or a handle** (the pi4-U6b lesson, applied by construction). **`SYS_READ(handle, buf, len)` is the enforcement point** — `handle_resolve(row, handle, CAP_READ)` must yield a `File`; a missing right (`Denied`), a non-File kind (`Console`/`Child`/`Socket`), or no handle (Empty/oob) **all return `-EACCES`** (the exact twin of `sys_write`'s Console+`CAP_WRITE`). It clamps the request to `min(len, size − offset)`, validates the **whole** destination up front as WRITABLE user memory — inside the window AND past the read-only code page, so a bad buffer is `-EFAULT` with **no copy and no offset advance** — serves the bytes from the staged source, and advances the offset by exactly the count delivered (`0` = EOF; **sequential, no seek**). Descriptors live in per-task FILES sidecars — `FILE_USED`/`FILE_STAGED`/`FILE_SIZE`/`FILE_OFFSET` keyed `[row][idx]` (`NFILE = 4`), the same lock-free CAS-claim + Release-publish/Acquire-resolve shape as `HANDLE_RIGHTS`/`HANDLE_KIND` — and the `File` value word carries **file-id = descriptor index + 1** (structurally clear of the `0`/`u64::MAX` sentinels). **Teardown-clear** extends the U5x lifecycle rule to files: `clear_handle_row` (riding `free_user_space_by_cr3` — both the normal exit and the KillSwitch reap) now also clears the FILES row, so a reused slot starts with no stale file, no leaked offset, no aliasable descriptor. Evidence — the `u6bx-file` fixture opens `HELLO.BIN`, reads 16 bytes through the `File` capability and verifies them against the kernel-planted staged prefix, then proves the CHECK denies both a **present File lacking `CAP_READ`** (the rights arm, backed by a real descriptor) and a **`Socket` carrying `CAP_READ`** (the kind arm) with `-EACCES` — witness `0x1F`; the launcher proves the FILES-row teardown-clear kernel-side: `:: U6bx: x86 real File handles — open+read via a File capability OK, no-CAP_READ -EACCES, wrong-kind -EACCES -> PASS ::` under `test-fat sf`, with every U1a/U1b/U2/U2.5/U3/U3.5/U4x/U5x/U6x verdict **byte-identical** (stash-free scratch-worktree baseline diff — pure append of the U6bx lines in both modes); default no-FAT `./arroyo test` stays MISSION SUCCESS with U6bx skipping cleanly; `./arroyo check` both arches, **zero aarch64 files touched**. **Honest scope:** open serves the **pre-staged set only** — arbitrary-runtime-file open awaits an interrupt-driven / IF-safe x86 storage path (deferred; that arc retires the divergence entirely and is the natural deep-x86 follow-on); read-only, flat namespace, no write/seek/dirs. Deferred: **x86 U7** (cross-process transfer, the pi4 U7 twin); real `Socket` fs/net routing; PCID; `copy_from_user`/`copy_to_user` (the M6f twin). **Two review notes ledgered** (five-lens adversarial review + refuter panel, 0 must-fix; both shapes are shared with the merged pi4 U6b twin): (1) the `FILE_OFFSET` advance is a non-atomic load→store — correct under the single-writer-per-private-slot discipline, but ill-defined for two concurrent SHARED_ROW tasks reading the same descriptor (memory-safe either way: every copy stays clamped to the staged source and the window); (2) `SYS_CAP` REVOKE of a `File` handle clears the handle but not its FILES descriptor (unreachable today — File caps carry no `CAP_GRANT` and no fixture revokes one; a repeat open→revoke loop would exhaust the row to `-EMFILE`). Both fold into the U7/U7x revoke+transfer work on both arches. **Metal-pending** (pure syscall logic; the staged source rides U2's metal-confirmed FAT path). *Both notes CLOSED by U7x (2026-07-07) — see the U7x entry below.*
- [x] **Cross-process capability transfer — inbox-mediated `SYS_XFER`/`SYS_RECV` + sender-owned single-level revoke, the single-writer invariant preserved (U7x, x86 twin of aarch64 U7; 2026-07-07, QEMU-verified; no metal expected — pure syscall logic; Campaign 2, Fable-executed).** Ports the reviewed, merged pi4 U7 design line-for-line, restoring full arch parity on the capability chain. **The invariant:** every `HANDLES[row]` row keeps exactly ONE writer (U4x's lock-free foundation) — a sender NEVER writes the recipient's row. **`SYS_XFER = 13`**`(dest, src, req_rights)` deposits an **attenuated** `(kind, target, rights)` descriptor into the recipient's per-SLOT transfer **INBOX** (`XFER_SLOT_*`, `NXFER = 4` — the one deliberately cross-slot surface, every claim/consume/retract/teardown a **tx-exact CAS** over a `0`/`RESERVING`/unique-transfer-id state word); **`SYS_RECV = 14`** pulls it into the CALLER's OWN row. Delegation is **owner-scoped** (`dest` must be a `Child` handle in the SENDER's own table; pid→row through a new **`Proc.slot`** field, +1-biased because slot 0 is valid — the x86 substitution for pi4's `Proc.asid`); the source must carry **`CAP_GRANT`**; and **attenuation crosses processes intact** — `req & !src_rights != 0` ⇒ `-EACCES`. **Revocation:** `SYS_CAP` **XREVOKE** flips `XFER_REVOKED_BIT` (bit 63) **inside the sender-owned record's TX word** (`XFER_REC_*`, `MAX_XFERS = 8`) — tx-exact like every other transition (the pi4 review-confirmed shape, inherited here, so a stale revoke can never land on a reclaimed record); the received cap goes **stale at its next `handle_resolve`** via the recipient-written `HANDLE_XFER_REC` sidecar, a still-pending revoked transfer is **discarded at RECV**, and **post-revoke laundering** (grant OR re-transfer from a revoked received cap) is refused. Post-check + retract closes the deposit-vs-exit race sender-side; the same residual TOCTOU as pi4 (exit + slot-recycle + new-tenant consume between the live-store and the post-check) is documented and accepted — generation-tagged inboxes ride the revocation-tree arc. Teardown extends `clear_handle_row`: received-cap records freed with their handles, the inbox swept (claim-to-clear), and the dying sender's records **DISOWNED** (owner-exact CAS to `u64::MAX` — revoke authority dies with the sender). Payloads: **`Console`/`Socket` only** (`File`/`Child` refused, as pi4). **The x86 divergences, both from slot keying:** (a) `Proc.slot` as above; (b) the **SHARED_ROW is refused as a transfer endpoint** — `-EACCES` from both XFER and RECV for a shared-window caller (the shared kernel window is immortal and multi-tenant; a transfer into it could never be revoked-by-teardown; pi4 had no such row). **Folds both ledgered U6bx notes:** (1) the `FILE_OFFSET` advance is now a **tx-exact CAS claim** — two SHARED_ROW tasks racing one descriptor claim disjoint byte ranges (well-defined concurrent reads; destination validated before the claim, so `-EFAULT` still moves no offset; private slots' single-writer discipline untouched); (2) `SYS_CAP` REVOKE of a `File` handle now **frees its FILES descriptor** (repeat open→revoke no longer exhausts the row to `-EMFILE`; honest scope: a GRANT-minted duplicate File handle shares the descriptor, so revoking either frees it and the survivor's reads fail CLOSED at the `FILE_USED` re-check — per-descriptor refcounts ride the tree arc). Evidence — a launcher-orchestrated two-fixture script (`u7x-parent`/`u7x-child`; the sequencing divergence from pi4: x86 ring 3 is IF-masked/cooperative with **no yield syscall**, so each fixture runs on its OWN dedicated AP — 3 online APs required, fewer skips cleanly — with bounded-spin GO/RECV polls; witnesses ride the name-routed `sys_exit` status, x86 having no SYS_REPORT, and the child's mid-run USE cue is a store to its own RW page the launcher reads through the slot backing): over-rights transfer **refused**; the **single-writer witness** (the child's handle row — including the `HANDLE_XFER_REC` sidecar — byte-clear while the t1 deposit sits in its inbox, the child provably parked pre-RECV on its GO word); the child RECVs and **prints through the transferred cap** (`u7x: child prints via the transferred cap`); use-then-revoke ordering enforced by the launcher (parent's GO released only after the child's USE word); the revoked cap's next write `-EACCES`; teardown leaves both rows, both inboxes, and the record ledger **fully clear**: `:: U7x: cross-process transfer — SYS_XFER attenuated, child received + used the cap, revoke enforced, single-writer intact -> PASS ::` under `test-fat sf`, with every U1a→U6bx verdict **byte-identical** (sorted scratch-worktree baseline diff vs `225ae48` — pure append of the U7x lines); default no-FAT `./arroyo test` stays MISSION SUCCESS (U7x needs no FAT file, so — like U5x — it runs and PASSes there too); `./arroyo check` both arches, **zero aarch64 files touched**. **Honest scope:** single-LEVEL revoke — re-granted/re-transferred copies escape it; revocation TREES (derivation records, real `CAP_REVOKE`) stay deferred to the pi4-led U8. Deferred: trees; the bandy Ring-3 wrapper; arbitrary recipients; `File` payload migration; generation-tagged inboxes. **Metal-pending** (pure syscall logic).
- [x] **Revocation TREES + generation-tagged inboxes — revoke chases derived capabilities, real `CAP_REVOKE` semantics, the sys_xfer TOCTOU closed (U8x, x86 twin of aarch64 U8; 2026-07-07, QEMU-verified; no metal expected — pure syscall logic).** Ports the reviewed, merged pi4 U8 design line-for-line onto U7x, restoring FULL arch parity on the capability chain (U4→U8 now symmetric). Closes both escapes U7x's entry documents, slot-keyed throughout. **(1) The derivation ledger.** A bounded static ledger of derivation NODES (`MAX_DERIV = 16` — the U7x static-atomic-array discipline: no heap, state-exact CAS over a `0`/`RESERVING`/unique-node-id word, Release-publish/Acquire-read) records an edge **child → parent at every derive**: `sys_cap_grant` (a local mint) and `SYS_XFER`/`SYS_RECV` (a delivered transfer — the node rides the inbox slot and is remembered by the sender-owned record). Handles installed by spawn/open/endow are ROOTS; a root gets its node LAZILY at its first use as a grant/transfer source. **Revoke = mark one node; staleness is discovered at USE:** `handle_resolve` walks child → root (bounded by the ledger size; cycles impossible — a parent is claimed strictly before its child and edges never change) and fails `Denied` if ANY ancestor is revoked — keeping U7x's load-bearing invariant, **no revoke path ever writes another row** (the x86 single-writer foundation), with revoke O(1) and resolve paying the bounded walk. **`CAP_REVOKE` is real now:** `SYS_CAP` REVOKE of a handle CARRYING it marks its node before the row clear, killing the whole derivation subtree — every re-grant and every re-transfer whose chain passes through it, however deep, exactly once, idempotently (a second revoke of the same index is an honest `-ECHILD`); WITHOUT the right the drop keeps U5x's ownership semantics (row-local; derived copies survive). **XREVOKE composes:** the transfer record captures its derivation node (+ the node's publish-time id — an ABA guard: a node dropped-and-reclaimed before the revoke lands no longer matches and is left alone; it had no children, so nothing escapes), and a won tx-exact XREVOKE marks it — so revoking the ROOT transfer kills the received cap AND everything the recipient re-granted or re-transferred onward (the U7x escape #1, closed); post-revoke laundering stays refused, now tree-deep (grant/xfer check ancestor staleness, not just the direct record). **Node lifetime (the documented choice):** a node frees when its owning handle/pending-slot drops AND it has no live children; dropped-with-children = a **TOMBSTONE** (still walkable, bit intact) until the subtree drains, then the free CASCADES up through drained tombstoned ancestors — every free arbitrated by a state-exact CAS on the id word (racing freers resolve to one winner), every walkable node PINNED (a live handle pins its own node; a live child holds `KIDS > 0` on its parent), so no walk reads a reclaimed node. Ledger exhaustion is `-EAGAIN` at mint/transfer time with reserve/unwind on every failure path (the U4x discipline). **(2) Generation-tagged inboxes.** A per-slot generation word (`SLOT_GEN` — the x86 twin of pi4's `ASID_GEN`) bumps at the TOP of `clear_handle_row`, strictly before the U7x inbox sweep, so it covers even a deposit that lands after the sweep passed its slot. `sys_xfer` stamps the recipient's current generation into the deposit; `sys_recv` delivers only on an exact match (a stale stamp = a deposit aimed at a PREVIOUS tenant: discarded, record freed, node dropped, never delivered); the sender's post-check re-reads the word and retracts on any change. The U7x-documented residual TOCTOU (recipient exit + slot recycle + new-tenant consume inside the deposit-live→post-check window) is now **structurally closed from both sides**. **The x86 divergences (both carried from U7x's slot keying):** rows are keyed by address-space SLOT (`current_slot()`, `Proc.slot`); the SHARED_ROW is refused as a transfer endpoint; and — x86 has no `SYS_REPORT` — the fixture conveys its witness as its `sys_exit` STATUS routed by task name (the u5x idiom; the aarch64 twin uses SYS_REPORT only because its sentinel exit status is reserved for demo routing). Evidence — the `u8x-tree` fixture (single slot, register-only; witness `0xF`): a grant→re-grant chain carries authority pre-revoke; the parent revoke (with `CAP_REVOKE`) returns 0, its double revoke exactly `-ECHILD`, and BOTH descendant copies are `-EACCES` at their next use; a right-less revoke stays local (its derived copy still writes) with its own double-revoke errno. `u8_kernel_check` drives the REAL `sys_xfer_from`/`sys_recv_for` code paths (the syscall bodies, parameterized on ROW — no duplicate logic) over scratch rows 5/6/7 (all private, `< USER_SLOTS`, none the refused SHARED_ROW): an S→R1→R2 re-transfer chain where the root XREVOKE makes R1 AND R2 stale + refuses the laundering mint (the assertion is non-vacuous — the re-transferred cap CARRIES `CAP_GRANT`, so its `-EACCES` provably comes from the tree-deep staleness check, not the missing-right gate); a deposit stamped before the generation bump is never consumable by the recycled row's tenant (its record frees; a late XREVOKE gets `-ENOENT`); and afterwards the handle rows, inboxes, transfer records AND the derivation ledger are all provably clear (the teardown/tombstone-cascade proof): `:: U8x: revocation trees — parent revoke kills re-grant + re-transfer, generation-tagged inbox, ledger clean -> PASS ::` under `test-fat sf`, with every U1a→U7x verdict **byte-identical** (sorted scratch-worktree baseline diff vs `e8db35c` — pure append of the U8x lines; the U7x setup *banner* is a best-effort-console drop under peak 3-AP contention, not a regression — its PASS verdict, printed on the same branch-free path, lands byte-identical); default no-FAT `./arroyo test` stays MISSION SUCCESS (U8x needs no FAT file, so — like U7x — it runs and PASSes there too); `./arroyo check` both arches, **zero aarch64 files touched**. **Of U7x's deferred list this closes exactly: revocation trees, generation-tagged inboxes, real `CAP_REVOKE` semantics** — bringing x86 to full parity with the pi4 lead. **Carried from the aarch64 U8 review** (the merged reference): the `deriv_drop` free-CAS RETRIES past a racing `deriv_revoke_if` (returning on the lost CAS would leak the node + its tombstoned ancestors, exhausting the ledger to `-EAGAIN` under SMP); the laundering witness is non-vacuous (the re-transferred cap carries `CAP_GRANT`). **Still deferred:** the bandy Ring-3 delegation wrapper (a Ring-3 arc); arbitrary-recipient transfer; `File` payloads (descriptor migration, with writes/seek/dirs); real Socket net syscalls; UnaFS `grants:*` on `SYS_OPEN` (K2/K3); IF-safe interrupt-driven x86 storage (retires the U6bx staged-source divergence). **Metal-pending** (pure syscall logic). **Post-merge hardening:** the `deriv_drop` tombstone/free handshake is `SeqCst` on both arches (store-buffering-safe) — see the aarch64 U8 entry.
- [x] **File WRITES + SEEK — `CAP_WRITE` on a `File` made real on x86 as a STAGED WRITE-BACK: `SYS_SEEK`, an RW `SYS_OPEN` mode bit, and a File+`CAP_WRITE`-routed `sys_write` into a per-descriptor in-memory writable buffer (U9x M1, x86 twin of aarch64 U9; 2026-07-08, QEMU-verified; no metal expected — the xHCI storage-enumeration blocker means the block device is absent on the real rMBP, so this whole demo SKIPS on metal exactly as U8x does).** Gives U6bx's read-only `File`+`CAP_READ` its write half — the x86 mirror of the pi4 U9 lead, structurally faithful to it at the CAPABILITY layer and diverging ONLY where the hardware forces it. **The load-bearing divergence (the same one U6bx's read side pays):** aarch64 U9 writes straight to the SD card INSIDE the SVC handler because its EMMC2 driver is PIO (polled MMIO). x86 CANNOT — storage is USB-over-xHCI whose BOT pump `hlt()`s awaiting the async completion, and the SYSCALL handler runs IF-masked (SFMASK clears IF), so a `hlt` at IF=0 never wakes: **no disk I/O can run in-handler.** So the write is **STAGED** — the write twin of the staged read. **The mechanism.** (1) **`SYS_SEEK = 15`**`(handle, offset)` — a near-verbatim mirror of the aarch64 `sys_seek`: the CHECK requires a `File` carrying ANY of `CAP_READ|CAP_WRITE` (resolve-for-READ-else-WRITE over `handle_resolve`'s all-bits semantics), an offset PAST `size` is `-EINVAL` (seeking TO `size`, the EOF position, is legal), and it sets the U6bx `FILE_OFFSET` sidecar (now settable, no longer forward-only) — a pure descriptor-state update, so it is IF-masked-handler-safe. (2) **RW `SYS_OPEN`** — `SYS_OPEN` gains a **mode bit** in `a2` (`0` = RO/`CAP_READ`, byte-identical to U6bx; `1` = RW/`CAP_READ|CAP_WRITE`); backward-safe because the x86 first-entry GPR scrub zeroes `rdx`, so the pre-existing U6bx open reads mode `0`. A RW open claims a per-descriptor **writable staging buffer** from a small fixed pool (`NWSTAGE`, one page each, per-slot single-writer, `static mut` + raw-pointer access mirroring `HELLO_BYTES`), **SEEDED** from the file's staged content, tracked by a new `+1`-biased `FILE_WSTAGE` sidecar; reserve/unwind is claim-last on every failure path (writable slot → descriptor → handle, each unwinding the prior — no leaked slot on `-EMFILE`/`-EAGAIN`). (3) **Routed writes** — `sys_write` is now KIND-DISPATCHED at its single `handle_resolve(row, fd, CAP_WRITE)` CHECK: a `Console` streams to serial (byte-identical), a `File` with `CAP_WRITE` overwrites its writable buffer IN PLACE at the descriptor offset via `sys_write_file` — a pure **memcpy** (IF-masked-handler-safe: no disk touched), clamped to `min(len, size-offset)` so it **never grows the file** (a write at/after EOF is a clean 0-byte no-op), the whole source validated up front (a bad buffer is `-EFAULT` with no copy and no offset move), advancing the offset by exactly the count written — the exact write twin of `sys_read`'s clamp/validate/offset discipline. (4) **Read-back through the SAME cap** — `sys_read` now serves a RW descriptor from its writable buffer (a RO descriptor from the read-only staged source), so a seek-back-and-read WITNESSES the write in memory. **The CHECK is the enforcement point, and it inherits U7x/U8x for free:** because the derivation/revocation walk lives INSIDE `handle_resolve`, a File-write cap whose ancestor was revoked is `-EACCES` at the write with no new code. **Three denials proven:** a **File opened RO** written to (missing `CAP_WRITE` — the rights arm), a **non-File handle** carrying `CAP_WRITE` (a `Socket` — the kind arm), and a **U8x-revoked File-write cap** all return `-EACCES`. **Honest scope — M1: IN-MEMORY ONLY, NO DISK WRITE-BACK.** `SCRATCH.BIN` is staged as a const in-memory seed (1 KiB of `0xEE`, always present regardless of disk), NOT read from FAT; the write lands in the in-memory buffer and the read-back is the write witness — there is NO on-disk "sector changed" check (that, plus capturing the FAT cluster chain at stage time + a per-descriptor dirty flag + a BSP-side write-back flush pump + planting `SCRATCH.BIN` on the x86 FAT image, is **M2** — deferred, a complete honest capability milestone landed without half-landing the flush). Also still deferred: file GROWTH/create/delete/truncate; directory mutation; IF-safe interrupt-driven x86 storage (retires the staged-buffer divergence for BOTH read and write); UnaFS `grants:*` on `SYS_OPEN` (the RW mode bit is the local precursor; K2/K3). Evidence — the `u9x-write` fixture (single slot, register-only apart from the read-back dest, witness `0x1F`) opens `SCRATCH.BIN` RW, seeks to a partial-sector offset (520), overwrites a 16-byte sentinel IN PLACE, seeks back and reads it through the SAME cap (the read-back witness), and proves the RO-write + wrong-kind denials; `u9x_check_revoked_write` stages the U8x-revoked-File-write denial over scratch row 5 (a File+`CAP_WRITE` cap backed by a real wstage-seeded descriptor whose granted ancestor is revoked; `sys_write`'s only File gate is `handle_resolve`, so proving that resolve `Err`s IS the `-EACCES`), and demands the handle/file/writable-pool/derivation ledgers all clear afterward (no node/descriptor/buffer leaked): `:: U9x: real File writes — open-RW+seek+write+readback OK, RO-write/wrong-kind/revoked-cap all -EACCES (staged in-place, no disk write-back this milestone) -> PASS ::` under `test-fat sf`, with every U1a→U8x verdict **byte-identical** (sorted scratch-worktree baseline diff vs `ca1b765` — pure append of the U9x lines); default no-FAT `./arroyo test` stays MISSION SUCCESS AND U9x **runs and PASSes** there too (its in-memory scratch needs no FAT volume — this DIVERGES from the HELLO.BIN-dependent demos U2/U4x/U6x/U6bx, which skip cleanly without a FAT); `./arroyo check` both arches, **zero aarch64 files touched**. **Lane:** `arch/x86_64/syscall.rs` only — the write stack (`fat.rs`/`block.rs`/`drivers/xhci`) is untouched (M1 stages in memory; M2 will reuse the existing shell-proven `fat::write_at`→`block::write_block` write path from the BSP main loop). **Metal:** CANNOT be metal-confirmed until the xHCI enumeration blocker (`task_47291f90`) clears — QEMU-green is the ceiling, and the gate is NOT relaxed to compensate.
- [x] **File WRITE-BACK to disk — the staged write is PERSISTED to the FAT, completing the x86 File-write twin (U9x M2, 2026-07-08, QEMU-verified; no metal expected — same xHCI storage-enumeration blocker as M1).** M1 proved the whole capability surface with an in-memory per-descriptor `wstage` buffer (a read-back through the same cap was the write witness) and deliberately did NOT half-land the flush; M2 lands it. **The IF-masked-handler constraint is unchanged and is NOT fought:** the x86 SYSCALL handler runs IF-masked (SFMASK clears IF) and the xHCI BOT pump `hlt()`s awaiting an async transfer event, so a `hlt` at IF=0 never wakes — **no disk I/O can run in-handler** (the same divergence U6bx's read side and M1's write side pay). So the write STAYS staged in the handler (a pure memcpy into `wstage`) and is FLUSHED to disk OUT of the handler, at IF=1, over the shell-proven `fat::write_at`→`block::write_block` path — which on x86 is USB-over-xHCI, and (the load-bearing new fact) the BOT pump is **self-servicing and CPU-agnostic**: it drains its own DMA'd event ring and acks the interrupter via RAW MMIO (IMAN/ERDP/EINT), and its `hlt` is woken by the per-CPU timer tick, so it drives from ANY core, not only the BSP that owns the xHCI MSI. **THE crux this arc solves (why M1 stopped):** the dirty `wstage` buffer is FREED at the fixture's teardown (`clear_files_row`, IF=0), but the flush needs IF=1 disk I/O — a naive "flush at teardown" is impossible. **The chosen protocol — a flush queue that survives teardown, drained at IF=1.** Teardown COPIES each dirty descriptor's bytes + its `(cluster, size, dirty [lo,hi))` into a small static flush queue (`flush_enqueue`) BEFORE freeing the `wstage` slot; the copy makes each entry SELF-CONTAINED, so a fixture that faults mid-flow can never strand a queue entry pointing at a freed buffer (the M2 robustness requirement). The demo launcher (running on the demo AP at IF=1) then DRAINS the queue (`flush_drain_one`→`fat::write_at`) and raw-re-reads the sector. Keeping the copy-then-free order means `wstage_all_free()` is still TRUE at teardown, so M1's reviewed teardown-clear proof is undisturbed; the launcher gates its drain on observing that teardown (`cleared`), whose `wstage_all_free()` Acquire pairs with `wstage_free`'s Release to make the enqueue's stores visible (no drain-before-populate race, no stranded late enqueue). **Revoke ordering (a revoked dirty cap must NOT persist stale bytes):** a whole-task TEARDOWN (`clear_files_row`) ENQUEUES dirty; a per-handle REVOKE / open-unwind (`files_free`) DISCARDS dirty (frees the buffer without enqueuing) — so a `CAP_REVOKE` on a dirty File cap repudiates the staged write and it never reaches disk. **The flush is IN-PLACE ONLY — the same discipline as pi4 U9's `write_at`:** clamped to `size`, it never grows/allocates/writes-a-FAT-entry/touches-a-directory, and it RMWs exactly the sectors the dirty `[lo,hi)` span overlaps (for the demo's single contiguous write, exactly one sector — the partial-sector case, other bytes preserved). The dirty range is SET fresh on the first write (not min/max'd against a 0 init), so the flushed span can never start at offset 0 and clobber an un-dirtied sector. **The flush cannot hang:** the BOT pump is BOUNDED (a 2000-iter timeout → `BlockError::Io`), so if the concurrent AP-side drive ever failed it would be a LOUD verdict FAIL (`flushed=false`), never a wedged core — `test-fat sf` is the first empirical proof of concurrent-AP xHCI BOT I/O in the tree. **The M1 capability surface is otherwise byte-identical** (the `u9x-write` fixture blob is UNCHANGED — M2 is entirely kernel-side capture/dirty/enqueue/flush); `SCRATCH.BIN`'s on-disk chain head is captured at the launcher pre-flight (IF=1, gated on `HELLO_STAGED`) and published to `SCRATCH_CLUSTER` before the fixture opens (the x86 stand-in for pi4 capturing `FILE_CLUSTER` in-handler). **Two M1 review notes folded:** `sys_write_file`'s offset advance is now a tx-exact `compare_exchange` claim (CAS-symmetric with `sys_read`, closing the load/store asymmetry before any shared writable descriptor exists), and a writable (`CAP_WRITE`) `SYS_OPEN` on `SHARED_ROW` is refused (a shared multi-tenant writable descriptor could race the unsynchronized staging memcpy). **DUAL-MODE evidence** — disk-backed (`UNAOS_FATIMG=sf ./arroyo test-fat sf 300`): the launcher raw-re-reads the flushed sector from a fresh mount and confirms it CHANGED (to the 16-byte pattern at offset 520, differing from the `0xEE` pre-image) while the directory SIZE did NOT (in place, never grew) — `:: U9x: real File writes — open-RW+seek+write+readback OK, RO-write/wrong-kind/revoked-cap all -EACCES, staged write FLUSHED to FAT (on-disk sector changed + size unchanged) -> PASS ::`, with all 16 prior U1a→U8x verdicts BYTE-IDENTICAL (sorted scratch-worktree baseline diff vs `ddefc40` — the only verdict delta is the U9x line evolving M1→M2; two non-verdict setup banners show best-effort-console-drop jitter, present in the baseline run too — the same artifact the U8x entry documents). The revoke-discard proof is NON-VACUOUS — `u9x_check_revoked_write` first shows (positive control) that a dirty descriptor run through `clear_files_row` DOES enqueue a flush, then that the revoke path leaves the queue empty; only the contrast proves discard. Default no-FAT `./arroyo test 25` stays MISSION SUCCESS AND U9x PASSes its IN-MEMORY core (`in-memory core; no FAT volume, flush is a no-op`) with ZERO AP disk I/O (the pre-flight is `HELLO_STAGED`-gated). `./arroyo check` both arches, **zero aarch64 files touched**. Adversarial 4-lens code review (atomics/ordering + cross-CPU visibility, lifecycle/leaks/double-free, flush correctness + FAT safety, revoke-discard proof + dual-mode verdict), each finding skeptic-verified: 0 confirmed. **Lane:** `arch/x86_64/syscall.rs` + `make-fat-img.sh` (the `SCRATCH.BIN` plant) — the write stack (`fat.rs`/`block.rs`/`drivers/xhci`) is REUSED unchanged via its existing API. **Out of scope, tracked:** generation-tagged file-ids (a bare descriptor index lets a revoke+reopen alias a reused slot to a new file's write — real but not ring-3-reachable this arc; the x86 twin of pi4 U11, lands as **U11x**, kept arch-symmetric). **Metal:** CANNOT be metal-confirmed — the xHCI enumeration blocker (`task_47291f90`) leaves `block::info()` None on the real rMBP, so the whole storage-gated demo SKIPS there exactly as M1/U8x do; QEMU-green is the ceiling, the gate is NOT relaxed. (pi4 U9 is metal-confirmed on real EMMC2; x86 U9x waits on the xHCI fix.)
- [x] **Open-file lifecycle — `SYS_CLOSE` + generation-tagged file-ids close the revoke+reopen descriptor-aliasing gap (U11x, x86 twin of aarch64 U11 M1 `714daad`; 2026-07-08, QEMU-verified; no metal expected — storage-gated like the rest of the chain).** Closes the hazard U9x M2 ledgered: a per-task FILES descriptor was named to ring 3 by a bare `+1`-biased slot index, so when a File `sys_cap_revoke` freed a slot and a later `sys_open` first-fit-reused it, a lingering sibling handle carrying the old index would silently **re-bind** to the different file now living in that slot (the `FILE_USED` guard lapsed the instant the slot went live again — a use-after-free / descriptor-aliasing disclosure). **Fix = the standard slotmap/generation-index:** a per-slot `FILE_GEN[[AtomicU32; NFILE]; USER_SLOTS+1]` counter bumped **LAST** on EVERY free (`files_free` — the ONE primitive `SYS_CLOSE`, the File-revoke drop, and `sys_open`'s reserve/unwind all route through — and `clear_files_row` at teardown, per slot); File handle value words now pack `(gen << 32) | (idx + 1)` (the low `+1` half keeps the word clear of the `0`=Empty / `u64::MAX`=RESERVING sentinels for any index and gen; **gen 0 packs to exactly `idx+1`, byte-identical to the pre-U11x file-id**, so every prior scaffold/kernel check that reads the value word is unperturbed); and **`file_desc_validate` is the SINGLE seam** — range + `FILE_USED` + gen-match — that EVERY File consumer funnels through: `sys_read`, `sys_write_file`, `sys_seek`, the new `sys_close`, AND `sys_cap_revoke`'s File-descriptor drop (which previously decoded the value word with a bare `checked_sub(1)` — now REQUIRED to route through the seam, since a gen-tagged word's high bits would otherwise land in the index). A stale handle to a reused slot is `-EACCES` (a gen mismatch), never a rebind. **`SYS_CLOSE = 17`** frees the caller's descriptor (bumping its gen) + clears the handle word; it requires NO right (`handle_resolve(row, handle, 0)` — a process may always close a handle it holds; kind + descriptor identity still enforced); a non-File kind is `-EINVAL` (object table UNTOUCHED — never corrupt it by freeing a File slot it does not own), and an unresolvable / already-closed / stale-slot handle is `-EBADF` (new errno `-9`) so a double-close returns cleanly and a use-after-close is denied. **x86 divergence from pi4:** `files_free` DISCARDS un-flushed dirty bytes (the staged write-back is a whole-task-teardown event, `clear_files_row`), so an explicit close of a dirty RW descriptor drops the write exactly as a revoke does — the demo closes RO handles; a future arc can make close enqueue the flush. **The gap is revoke+reopen, not unlink** (x86 has no U10 create/unlink) and it is **NOT ring-3-reachable at U9x** (a revoke clears the only handle, and `sys_open` endows no `CAP_GRANT` to duplicate one — no way for a ring-3 program to hold a stale file-id across a free), so the airtight proof is kernel-side: `u11x_check_gen_rebind` (scratch row 6) claims a descriptor slot + mints its `(gen, idx)`, frees it (gen bumps), first-fit-RE-claims the SAME slot at the bumped gen (a "different file"), and proves the OLD file-id is rejected (`file_desc_validate` → `None`, a gen mismatch) EVEN THOUGH the slot is live again (`FILE_USED` true), while a FRESH file-id resolves — "no silent rebind on slot reuse", isolated from disk/EL0 timing, the row left clean. The ring-3 `u11x-close` fixture proves the SYS_CLOSE contract end-to-end over the immutable staged `SCRATCH.BIN` (witness `0x1F`): open RO + read the 16-byte seed; `SYS_CLOSE` → 0; double-close → `-EBADF`; a read through the closed handle → `-EACCES` (use-after-close denied); reopen (a fresh handle first-fit-reusing the freed slot) + read the seed again. **Evidence:** `:: U11x: open-file lifecycle — open+read/close/double-close(-EBADF)/use-after-close(-EACCES)/reopen+read OK, gen-tagged file-id rejects a stale sibling to a reused slot -> PASS ::` under `UNAOS_FATIMG=sf ./arroyo test-fat sf 300`, with all 17 prior U1a→U9x verdicts **byte-identical**; chained off `u9x_launcher`, storage-gated (chain-inherited) but needing no disk itself, so default no-FAT `./arroyo test 30` stays MISSION SUCCESS AND U11x PASSes there too; `./arroyo check` both arches green, **zero aarch64 files touched** (`arch/x86_64/syscall.rs` only). Every File value word backing a real descriptor is now gen-tagged (`sys_open`, the U6bx no-cap plant, the U9x revoke-check plant); the two opaque scaffold ids (`0x100`/`0x200`) are untouched and resolve identically (idx ≥ `NFILE` → rejected as before). **Still deferred:** cross-process open refcounts + `unlink`-defers-free (pi4 U11 M2); file growth/create/delete (U10 twin); IF-safe interrupt-driven x86 storage. **Metal:** storage-gated like the rest of the capability chain (the xHCI enumeration blocker `task_47291f90` keeps `block::info()` None on the real rMBP), so the demo SKIPS on metal — the gen-tag mechanism itself is always-on in the syscall path, only the demo is gated. **Metal-pending.**
- [x] **No-storage capability-chain visibility — the INLINE console-cap demos (U5x/U7x/U8x) run on the no-storage / metal path so the chain is confirmable over the FTDI console (scoped gate relaxation + `UNAOS_NOSTORAGE` knob; 2026-07-08, QEMU-verified; the direct enabler of the metal FTDI bench).** Until now every U-arc demo gated on `block::info().is_none()` and SKIPPED with no block device — so on the serial-less-until-now metal rMBP (where the SD reader never enumerates over xHCI, `task_47291f90`, leaving `block::info()` None) the ENTIRE capability chain was invisible. But U5x/U7x/U8x are **inline console-cap blobs that need NO storage** (verified: no `SYS_OPEN`, no `staged_bytes`, no disk touch — their fixtures transfer/revoke `Console` capabilities, and `sys_write` routes to the serial console); their `block::info()` gate was **control-path DISCIPLINE** (keep the no-storage boot log demo-line-free), NOT a functional dependency, and the code said so at each site. With the FTDI cable making the no-storage metal path finally observable, that discipline is **deliberately relaxed FOR THOSE THREE DEMOS ONLY**: their probe/launcher/run gates are removed so they run + print regardless of storage, surfacing the U5→U8 capability slice (capabilities → cross-process transfer → revocation trees) over the metal console **without waiting for the xHCI storage fix**. **Scope is surgical and the security posture is unchanged:** the storage-GATED arcs (U2 load, U4x, U6x, U6bx, U9x, U11x — all of which genuinely read/write FAT or run HELLO.BIN off disk) **keep their gates** and still skip on no-storage; NO protection is touched (SMEP/NXE/W^X/page-perms/checksums are orthogonal — this is a demo-visibility gate, not a boundary); and the demos run are the already-adversarially-reviewed U5x/U7x/U8x fixtures on an ADDITIONAL path, not new attack surface. The relative demo ORDER is preserved by the existing `*_LAUNCH_DONE` bounded waits (not the block gate), so nothing runs out of sequence. **`UNAOS_NOSTORAGE=1`** (builder knob) omits the QEMU `usb-storage` device so the kernel enumerates no block device — the QEMU analog of the metal no-storage path, used to verify. **Evidence:** `UNAOS_NOSTORAGE=1 ./arroyo test 90` → `xHCI: ... note='no mass-storage device enumerated'` (block absent) with `:: U5x: ... -> PASS ::`, `:: U7x: ... -> PASS ::`, `:: U8x: ... -> PASS ::` AND every storage-gated arc (U2 load/U4x/U6x/U6bx/U9x/U11x) ABSENT (cleanly skipped) while the storage-INDEPENDENT arcs (U1a/U1b/U2-0/U3/U3.5) still PASS — exactly what the metal FTDI console will replay. **No regression on the block-present paths** (the relaxation is a no-op there — the deleted gates never fired when block was present, and the builder attaches `usb-storage` identically when the knob is unset): `./arroyo test 30` stays MISSION SUCCESS with the full applicable chain PASS, and `UNAOS_FATIMG=sf ./arroyo test-fat sf 300` runs the whole U1a→U11x chain with **0 FAIL**. `./arroyo check` both arches green, **zero aarch64 files touched** (`arch/x86_64/syscall.rs` + `builder/src/main.rs`). **Metal:** this IS the metal enabler — the U5→U8 console-cap slice becomes metal-confirmable over the FTDI cable at the Peter-attended bench (the storage-gated arcs still wait on `task_47291f90`). **Metal-pending (bench).**
- [x] **Cross-process unlink-defers-free — a GLOBAL open-file refcount defers the DELETE to the last close across processes (U11x M2, x86 twin of aarch64 U11 M2/M2b `b88d2ba`/`303e271`; 2026-07-10, QEMU-verified; metal storage-gated).** Closes the U10-ledgered cross-process gap: unlinking a file another process holds open no longer completes the delete at unlink — the deferred `CreateGrowDelete` op is enqueued **HELD** (`U10_HELD`) and released by the LAST descriptor release anywhere: explicit `SYS_CLOSE`, a revoke, or whole-task TEARDOWN (`clear_files_row` — the pi4 M2b exit-without-close orphan; the release is one atomic store, IF=0-legal, so x86 needs no reaper task — the launcher's IF=1 drain is the reaper). **Mechanism:** the created-file identity space is the static `U10_NAMES` table, so the refcount table (`OPENF_REFS`/`OPENF_PENDING`/`OPENF_HELDSLOT`) is indexed directly by name-id — pure atomics, no lock, and NONE of pi4's recycled-`(dir_lba,dir_off)`-key aliasing class (`b863304`) **by construction**: an O_CREAT of a delete-pending name is refused with the new **`-EBUSY`** until the delete completes (drain, or last-close release in no-FAT mode), so one name never denotes two live files. The per-row `DYN_DELETED` overlay became GLOBAL (`DYN_DELETED_G`): the name vanishes for EVERY process in-handler at unlink — checked FIRST in the dynamic-open path, BEFORE the any-row descriptor scan, else a live cross-row descriptor would let any process re-open (and incref) an unlinked file by name; a second unlink of the same name is claimed atomically (`swap`) → `-ENOENT`, never a double-enqueue. Cross-process opens: `sys_open` now resolves a live created file in ANY private row (`created_desc_any_row`); every created-descriptor claim increfs strictly BEFORE `install_file_handle`, so its `-EAGAIN` unwind (through `files_free`, THE single decref seam) pairs exactly once — an underflow would strand a held op + a permanently-`-EBUSY` name. Teardown of a delete-pending file's dirty holder SUPPRESSES the usual `CreateGrow` enqueue (else: `NU10 == 1` overflow + the file resurrected on disk after its delete drained). **Evidence:** two-phase `u11m2_launcher` — the launcher plays "process S" on a REAL allocated scratch slot, held across the phase (a hardcoded row a fixture could claim would hand a live RW File handle to ring 3 — reviewed out) + the EL0 `u11m2-unlink` fixture (witness `0x3F`: cross-row open / read the OTHER process's bytes / unlink → 0 / invalidated-sibling read `-EACCES` / re-open `-ENOENT` / re-create `-EBUSY`); C1 proves the DEFER after fixture teardown (op HELD + refcount 1 + pending + read-after-unlink through the surviving descriptor); phase-1 release = the SYS_CLOSE core, phase-2 = the REAL teardown funnel (`free_user_space_by_cr3`); C2 proves the release (drainable / refcount 0 / pending consumed); then a drain of exactly ONE op + on-disk gone / chain freed in all FAT copies / cluster re-allocatable / name re-creatable. Gates: `./arroyo test 40` MISSION SUCCESS 17 PASS 0 FAIL (in-memory core); `test-fat sf` + `p16` **21 PASS 0 FAIL** each (the U11m2 on-disk PASS appended to the 20 priors, all unchanged); `UNAOS_NOSTORAGE=1 test 90` clean skip; `check` both arches, zero aarch64 files. Adversarial PRE-CODE design review folded 5 must-fixes before coding (enqueue-suppression, deleted-flag-first ordering, incref placement, allocated scratch row, no-FAT flag clear). **Ledgered residuals:** (1) the disk half of DELETE remains the U10 launcher-side replay — but it now FIRES at the correct lifecycle point (the U10 causal-fidelity gap closes at the lifecycle layer); (2) `-EBUSY`-on-recreate is an x86 semantic divergence (pi4 allows an immediate re-create as a new file) — safe-by-refusal, revisit if userland needs it; (3) the cross-row sibling open SNAPSHOT-copies the source wstage — a concurrent writer in the source row could torn-copy; worse, a cross-core TEARDOWN of the source row between the scan and the copy could free + let a third open re-claim the source wstage slot, so the seed would copy ANOTHER file's bytes (cross-file disclosure; in the self-alias case the copy would even overlap itself) — and a sibling that writes then exits would enqueue a second op against `NU10 == 1` (all demo-sequenced today; the product fix is the pi4 SpinMutex table + real shared backing — the IF-safe interrupt-driven-storage arc retires this whole staged-buffer family); (4) the open-vs-unlink TOCTOU (an incref racing a concurrent last-close release on another core) is excluded in the demos by launcher sequencing — same product fix; (5) `SYS_CLOSE` of a dirty RW descriptor still DISCARDS the un-flushed write (unchanged since U11x, now explicitly re-ledgered here — teardown, the only acknowledged-persistence path, is unchanged; a future arc may make close enqueue the flush); (6) **cross-process open/unlink is GRANT-FREE pending U6x** — this arc deliberately introduces the pre-U6 pi4 model on x86: any process can plain-open (auto-RW) and even UNLINK any other process's created file by name, no grant — arch-DIVERGENT from pi4's now-merged secure-by-default U6 (owned-at-O_CREAT + F1 owner-only unlink). The `-EBUSY` gate blocks the pi4 F1 recreate-steal only WHILE the delete is pending; after the drain the name is freely re-creatable. **This residual is the U6x arc's ledger anchor** (fold pi4's F1 owner-only-unlink from the start). **Metal:** storage-gated; the xHCI fix (`3bee9d6`) is metal-confirmed, so the next attended bench can metal-confirm this directly off the FAT card (the DEFER.BIN pre-flight self-heal keeps re-runs honest). **Metal-pending.**
- [x] **UnaFS owner/grants — the by-NAME namespace ACL at `SYS_OPEN` (owned-by-default) + `SYS_FGRANT` delegation + F1 owner-only unlink (U6gx, x86 twin of the aarch64 U6 `arch/aarch64/syscall.rs`; 2026-07-10, QEMU-verified; metal storage-gated).** Closes the U11x M2 ledger-anchor residual #6: on x86 the by-name namespace was GRANT-FREE — any process could open/create/unlink any other process's created file by name. This makes it secure-by-DEFAULT and brings the arches to owner/grants parity (naming: pi4's owner/grants is "U6"; x86 already spent "U6x" on the earlier general-object-table arc, so this twin is **U6gx**). **Owned-by-default:** an `O_CREAT` of a NEW name records the creating principal as the file's OWNER (PRIVATE); the new `O_PUBLIC` mode bit (bit2) opts a create OUT into world-access (the U11x cross-process `DEFER.BIN` fixture tags its shared file `O_PUBLIC`, one bit — U11x stays byte-identical). An open of an EXISTING owned file is admitted only for the owner or a granted principal, else `-EACCES`; a file with NO owner row (a STAGED file — HELLO.BIN/SCRATCH.BIN/GROW.BIN carry no created-name identity — or an `O_PUBLIC` create, or one whose owner has torn down) is PUBLIC, byte-identical to pre-U6gx. **KEY vs PRINCIPAL — the x86 substitutions:** the ACL is keyed by the created file's `U10_NAMES` **name-id** (a direct index — x86's created-file identity space IS the static name table, the same key `DYN_DELETED_G`/`OPENF_*` use), NOT pi4's on-disk `(dir_lba,dir_off)` — so there is no recycled-key aliasing class by construction (U11x M2's reasoning) and no bounded-owner-table "full" case, hence `owned_set_owner` is infallible and a private create has **no fail-closed `-ENOSPC` path** (a clean divergence from pi4). The PRINCIPAL is the address-space **SLOT** fenced by its `SLOT_GEN` incarnation (the x86 `(ASID, ASID_GEN)` analogue, bumped at the top of `clear_handle_row`): a stale owner/grant whose gen no longer matches never authorizes a recycled slot's next tenant. **The store:** an in-kernel `OWNED_FILES` — a `SpinMutex<[OwnedFile; N_U10_NAMES]>` (owner slot+gen, up to 4 grants), taken IRQ-masked via `without_interrupts` on every access (the SYSCALL handler already masks IF; the teardown path does not — the mask makes them symmetric, the pi4 `IrqGuard` discipline). Cleared at `sys_unlink` (the name is gone) and at OWNER teardown (`owned_clear_owner_slot` in `clear_handle_row` — the file reverts to PUBLIC + the table self-cleans). **`SYS_FGRANT = 18`:** the owner grants (a `CAP_READ|CAP_WRITE` subset) or revokes (`rights == 0`) access to a principal named OWNER-SCOPED by a `Child` handle it holds (the `SYS_XFER` idiom — ring 3 never supplies a raw pid/slot); ownership is checked BEFORE the grantee handle is resolved (a non-owner is `-EACCES` whatever it passes, and never learns whether that handle was valid), a nonzero request naming only unsupported bits is `-EINVAL`, a full grant list `-ENOSPC`. The grant is an ACL edge on the FILE — nothing is delivered to the grantee's table; it opens the name and the ACL admits it, and a handle it ALREADY holds survives a revoke (the ACL gates ACQUISITION, not held caps). **F1 — folded from the START** (pi4 learned it post-hoc): `SYS_UNLINK` is OWNER-only for an owned file — the `CAP_WRITE` CHECK admits both the owner AND a WRITE-grantee, but a content grantee must NOT be able to `unlink` + `O_CREAT` to STEAL ownership; a PUBLIC file keeps the prior `CAP_WRITE`-gated unlink. **Evidence** — a TWO-process `u6gx_launcher` choreographs `u6gx-owner` (A) + `u6gx-grantee` (B, its own address space, a launcher-planted Proc entry so A's `Child` handle names it) over per-slot GO/SIG words; A owns `OWNED.BIN` (a 5th `U10_NAMES` member). A (`0x3F`): create PRIVATE + write; grant B R|W → 0; revoke → 0; owner re-open after revoke; **owner unlink while B still holds the file open → 0 (deferred — the U11x M2 combined path)**; O_CREAT re-create → `-EBUSY`. B (`0x1F`): pre-grant open → `-EACCES`; granted RW open + read-back matches A's bytes (content crossed processes); non-owner `SYS_FGRANT` → `-EACCES`; grantee `SYS_UNLINK` → `-EACCES` (F1); post-revoke open → `-EACCES` (its already-held handle survives). B closes at the last step, releasing A's deferred delete; the launcher then proves ownership DIED at the last close (refcount 0, not pending, name re-creatable, `OWNED_FILES` row PUBLIC) and drains the queued delete (disk mode). Verdict `:: U6gx: UnaFS owner/grants — non-owner open -EACCES, owner grant admits R|W (content crossed), non-owner grant -EACCES, grantee unlink -EACCES (delete owner-only), revoke re-denies, owner unlink defers while grantee holds + re-create -EBUSY, ownership dies at last close -> PASS ::`. Gates: `./arroyo test 40`/`90` (in-memory core) 17 PASS + U6gx; `test-fat sf` **22 PASS 0 FAIL** (the 21 priors byte-identical + U6gx — note `grep 'PASS ::'` undercounts by one, U2-0a's verdict ends `PASS (...)`); `p16` 0 FAIL (the documented intermittent best-effort console one-line drop can show 21); `UNAOS_NOSTORAGE=1 test 90` clean skip; `check` both arches, **zero aarch64 files**. **Lane:** additive on `arch/x86_64/syscall.rs` + docs; the pattern data lives in-blob. **Flagged, deferred (honest scope, mirroring pi4's):** the owner/grants store is in-kernel, VOLATILE, boot-scoped (no persistent principals yet — the model's own gap), so ownership is meaningful only within a boot and only while the owner lives; a persistent form + retroactive owners for host files awaits on-disk UnaFS attributes. The U11x M2 residuals it inherits (snapshot torn-copy / `SYS_CLOSE` discards dirty / open-vs-unlink TOCTOU) are unchanged — the product fix is the same IF-safe interrupt-driven-storage arc. A `SYS_FGRANT` that endows the grantee with a directly-usable handle (vs. an open-by-name) remains future. **Metal:** pure syscall logic, storage-gated (metal-pending like U8x/U11x; flash-and-watch at the next attended bench). **Metal-pending.**
- [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall, live in-place reads + write-through behind the `irqstorage` knob (STOR-1 S1–S3, 2026-07-10, QEMU-verified + ✅ core mechanism METAL-CONFIRMED on the real 2012 rMBP; off by default).** Retires the staged-buffer divergence for the READ source and the in-place WRITE, giving x86 the aarch64 *semantics* (a syscall that blocks on a real transfer completion and reads/writes the live volume in place) via a different *mechanism* (interrupt completion + a sleeping syscall, since x86 storage is async DMA not synchronous PIO). **The mechanism (S1).** A scheduled kernel task — the STORAGE SERVICE TASK — owns the xHCI BOT pump; it runs at IF=1 (like the U9x/U10x launcher drains already do), so the pump's `hlt` wakes on the storage MSI-X. A storage syscall builds a `BlockRequest` on its OWN kernel stack, pushes it on `REQ_QUEUE`, wakes the service task, and blocks on the request's own `Semaphore`; `Semaphore::wait` restores the caller's IF snapshot across the switch, so the handler SLEEPS WITH IF=1 SEMANTICS though it entered IF-masked (SFMASK) — the crux that makes it IF-safe. The task is off every run queue while blocked (`STATE_BLOCKED`), so nothing runs the half-finished syscall on another core, and the frame (hence the request + its semaphore) stays pinned and alive for the whole transfer (the `Condvar::wait`/`done_sem` lifetime discipline). The service task runs the transaction and `post`s the submitter — ALL wakeups at IF=1, never in interrupt context, so the wake-only MSI-X handler stays lock-free (the `sched.rs:28` invariant — handlers never `make_ready` — is preserved by construction), and there is exactly one BOT submitter (no self-deadlock). A bounded wall-clock pump timeout surfaces a wedged transfer as `BlockError::Io` to the sleeping submitter — never a hang; endpoint recovery beyond that is explicitly out of scope. Witness: `bx-blockreq`, a kernel self-test that reads LBA 0 both polled and through a `BlockRequest` and compares → PASS. **Live reads (S2).** A staged descriptor's `SYS_READ` resolves the file BY NAME on the live volume (`find_located` + `read_at`), retiring the in-memory staged source as the READ source (the U6bx "staged-set constraint retires when an IF-safe storage path lands" — closed-when-on). The read BOUNCES through a kernel-stack buffer: the ring-3 destination lives in the submitter's private CR3 (PML4[2]), unreachable by the service task under the kernel CR3, but the submitter's kernel stack is in the shared kernel half, which the service task can reach — the handler copies the fetched bytes out to the (already-validated) user destination on resume, under its own re-installed CR3. **Live write-through (S3).** A non-growable staged descriptor's `SYS_WRITE` (SCRATCH.BIN, `FILE_OPNAME == 0`) writes THROUGH in place via `write_at` — synchronously, out of the IF-masked handler, with nothing staged or marked dirty. This retires the U9x wstage-as-backing + FLUSH queue for the write and CLOSES the close-discards-dirty residual (a `SYS_CLOSE`/teardown of the written descriptor discards nothing — the write is already on the volume). The matching read broadens to `FILE_OPNAME == 0` (RO opens AND non-growable RW opens), so SCRATCH.BIN's write-then-read-back reads the just-written disk bytes. **The gate is per-descriptor and coherent:** the live path fires only for staged descriptors that are in-place-only (`FILE_OPNAME == 0`), with a FAT volume mounted (`HELLO_STAGED`) and the service task up (`service_ready()`); GROWABLE / created descriptors (`FILE_OPNAME` set) and the no-FAT in-memory core fall through to the staged path unchanged. **Everything is behind the `irqstorage` cargo feature (`UNAOS_IRQSTORAGE=1`; mapped in `builder` + `arroyo`), x86_64 only** — the default build never links `drivers/xhci/irqstorage.rs`, so the whole staged path is BYTE-IDENTICAL. Evidence — knob-ON `test 40` (non-FAT) = 18 PASS + MISSION + `:: bx-blockreq: PASS ::` (the live path falls through where there is no volume); knob-ON `UNAOS_FATIMG=sf test` = 23 PASS with `u6bx-file`'s HELLO.BIN read served LIVE, `u9x-write`'s write+read-back served LIVE, and a new witness `:: S3: synchronous write-through — SYS_WRITE persisted to FAT in-syscall (disk held the write pre-drain: true, no deferred flush: true), close-discards-dirty residual retired -> PASS ::`; knob-OFF `test 40` = 18 PASS + MISSION and `UNAOS_FATIMG=sf test` = 22 PASS, BYTE-IDENTICAL (no STOR-1 lines); `UNAOS_NOSTORAGE` clean both knob states; `./arroyo check` both arches (knob on + off), **zero aarch64 files touched**. **Lane:** a new `drivers/xhci/irqstorage.rs` (the service task + `BlockRequest` + `submit`/`submit_read_file`/`submit_write_file`) + `arch/x86_64/syscall.rs` (the routing) + `builder`/`arroyo` (the knob) + `main.rs` (spawn + self-test) — the `fat.rs`/`block.rs` writers are REUSED unchanged via their existing API. **Metal:** the 2012 rMBP Panther Point xHCI transfer-IRQ behaviour is UNPROVEN for I/O (enumeration is metal-confirmed; transfer-event-driven I/O has never been exercised on that controller) — so this stays OFF BY DEFAULT until an attended bench confirms; QEMU-green is the ceiling, and the knob is the metal caveat, not a relaxed gate. **STOR-1 S4 — synchronous grow/create/delete in-syscall (LANDED 2026-07-10 knob-on; QEMU-green, metal-pending):** grow/create/delete now run SYNCHRONOUSLY in the syscall via the storage service task (out of the IF-masked handler), retiring the U10x deferred op-queue + its launcher-replay causal-fidelity gap WHEN THE KNOB IS ON. CONSERVATIVE-HYBRID — only the DISK OPS became synchronous (`BlockOp::{Create,Grow,Delete}` + `submit_create`/`submit_grow`/`submit_delete`); the created-file wstage-for-reads, the snapshot-sibling model, and the U11x M2 defer LOGIC (`DYN_DELETED_G`/`OPENF_*`) are UNCHANGED (shared-backing cross-process READS are S5). **S4a** `open_create_new` submits `Create` (idempotent `find_located` → `create_in_root`) FIRST — the file is on disk in-syscall. **S4b** `sys_write_grow` submits `Grow` disk-FIRST, then mirrors the extend into wstage + `FILE_SIZE` (reads still serve wstage this arc); no `mark_dirty` (already durable, so the teardown flush + `clear_files_row` enqueue are skipped knob-on). **S4c** `sys_unlink` enqueues NO held op — the deferred DELETE runs synchronously at the LAST close (`openf_release`). **The teardown-safety crux:** the last-close release runs from a syscall handler (`files_free`) or the launcher's non-self teardown (both BLOCKING-SAFE), OR an `exit`/reap SELF-teardown (IF=0 mid-death, or the reaper with no current task — MUST NOT block: blocking would resume a task whose CR3 is being freed, or `submit` off a scheduler). So `openf_decref` stays pure atomics and SIGNALS the caller; `clear_files_row` detects blocking-safety at runtime via `current_user_cr3() != slot_cr3(slot)` (a launcher is a kernel task, `user_cr3 == 0`; `exit` of the slot has `user_cr3 ==` the slot's CR3; the reaper has no current → both `false`). Every DEMO delete-trigger is blocking-safe (the unlinking fixtures sweep their own descriptors at unlink, and the cross-process holders — u6gx's B, u11m2's launcher — close/tear-down from a blocking-safe context). **The unsafe-teardown last-close IS reachable** (adversarial-review catch): a process that opened a created file CROSS-PROCESS (a U6 grantee / an `O_PUBLIC` sharer) and became its LAST holder, then `exit`s/faults WITHOUT an explicit close, reaches `clear_files_row` with `can_block == false` — where blocking would corrupt (its CR3 is being freed / no current task). That branch therefore DEFERS the delete the IF=0-safe way (the knob-off mechanism): it enqueues a `U10OP_DELETE` op (the file is on disk; no bytes) that a launcher's IF=1 drain completes, leaving `DYN_DELETED_G` SET meanwhile so no re-create aliases the undeleted file — best-effort completion, fail-safe if it strands (name blocked + a chkdsk-reclaimable orphan, no corruption). The clean fix (shared on-disk backing so reads/deletes need no per-holder blocking) is S5. **Created-file in-place writes also write through** (review catch): an in-place SYS_WRITE (within EOF) to a created/growable descriptor persists synchronously via `write_at` + mirrors wstage — else knob-on it would land only in wstage and the created-file teardown flush (disabled knob-on) would drop the acknowledged write on remount. **S4d** the launcher verdicts require the U10 op-queue drained NOTHING knob-on (`count == 0`, `u10_drain_verdict`), reading the state the synchronous ops already wrote (`u11m2` also grows DEFER.BIN on disk so its freed-cluster proof stays non-vacuous); a new `:: S4: grow/create/delete SYNCHRONOUS … op-queue drained NOTHING … ::` witness. (Ledgered residual: a knob-on `open_create_new` whose in-memory claim fails AFTER the synchronous `create_in_root` leaves a harmless 0-length on-disk orphan — the idempotent re-submit + the launcher pre-flight self-heal reconcile it.) Evidence — `./arroyo check` both arches (on+off); knob-ON `test 40` = 18 PASS + MISSION (S4 inert without a FAT volume); knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 150` = **24 PASS** (22 fixtures + the S3 + S4 witnesses) with u10x-grow / u10cx-create / u10dx-delete / u11m2-unlink / u6gx SYNCHRONOUS and zero `U10_OVERFLOW`; knob-OFF `test 40` = 18 PASS + MISSION and `UNAOS_FATIMG=sf test` = 22 PASS **byte-identical** (no S4/synchronous lines); `UNAOS_NOSTORAGE` clean both knob states; **zero aarch64 files** (`fat.rs`/`block.rs` reused unchanged; new `arch/x86_64/sched.rs::current_user_cr3`). The N1 offset-over-advance note (ledgered at S3) is addressed for the grow: S4b advances `FILE_OFFSET` by the count actually persisted. **Metal (S4):** the cross-process delete-at-last-close races (u11m2/u6gx) are metal-only-validatable (QEMU-TCG will not interleave; design risk 3) — rides the next attended rMBP bench. Also future: real shared backing for cross-process opens (S5, retires the U11x M2 torn-copy/TOCTOU residuals); activating `FAT_MUTATION` on x86 (S6); retiring the staged OPEN (S7). **Security-arc review (2 must-fix + 3 notes, adversarial refuter panel, all folded before merge):** **MF1** — every `REQ_QUEUE` acquisition is now IRQ-masked (`without_interrupts`, scoped to the O(1) push/pop, mask ENDS before the pump's `hlt`) at all three sites (submit push, service pop, start reserve): the service task pops at IF=1 and is timer-preemptible, and run queues are per-CPU with no steal, so a plain spinlock hold preempted mid-flight would deadlock a same-core IF=0 submitter — masking BOTH sides closes it (masking only the service task swaps the trap onto IF=1 kernel-task submitters). **MF2** — `sys_open` refuses a WRITABLE open of HELLO.BIN (staged index 0, immutable EL0 code): `rw && sidx == 0 -> -EACCES`, the ROOT-CAUSE close for the S3 live write-through (which resolves the target BY NAME and would otherwise overwrite the on-disk executable in place, surviving reboot, defeating the default staged path's `staged_cluster(0) == 0` never-flush) AND the pre-S3 in-memory-dirty leg, in one place; SCRATCH.BIN/GROW.BIN keep their write-through. **Severity, honest:** a feature-gated regression that turned a read-only code object into a survives-reboot RW overwrite of the boot-critical image — NOT a live ring-3 escalation (x86 has no arbitrary-blob loader; `sys_spawn` only re-instantiates the pre-staged HELLO.BIN; fixtures are kernel-authored), and no fixture opens HELLO.BIN RW so the fix regresses no verdict. This fix applies in BOTH knob states (immutable code is read-only regardless of the knob). **N1** (ledgered, deferred to S4): a live short-read/EIO leaves the CAS-advanced offset ahead of the bytes delivered → a sequential stream would skip the tail; BENIGN today (live size == staged size, so the delivered count always equals the claim), the advance-by-actual clamp rides S4 where a live grow diverges the sizes. **N2**: `submit()` now panics on an off-scheduler `wait() == false` rather than fall through (no dead-frame DMA; unreachable today). **✅ Core mechanism METAL-CONFIRMED (2026-07-10 attended bench, real 2012 rMBP):** a clean full-chain usbdebug boot captured over FTDI serial — `:: bx-blockreq: PASS ::` (a raw LBA-0 read driven through the SERVICE TASK matched the polled read → transfer-IRQ I/O + the submit/block/complete handshake WORK on the real Panther Point xHCI, RESOLVING design risk 1, the one genuinely unproven thing), `:: U6bx … PASS ::` (HELLO.BIN read served LIVE, S2), `:: S3: synchronous write-through … PASS ::` + `:: U9x … on-disk sector changed … PASS ::` (SCRATCH.BIN written+read-back LIVE + synchronous on the real FAT16 card, S3), and the ENTIRE prior chain U1a→U6gx re-confirmed knob-ON incl. the deferred U10 on-disk grow/create/delete proofs; zero faults / timeouts / deadlock, HELLO.BIN intact. The bench ran the PRE-fix code, but the two review fixes are behaviorally transparent to every witnessed path (they touch REQ_QUEUE lock timing and HELLO.BIN RW-open, which the witnesses do not exercise), so the metal-confirm STANDS; the fixed code re-benches at the S4 attended bench. **STOR-1 S5 — real shared backing for cross-process created-file reads (LANDED 2026-07-11 knob-on; QEMU-green, metal-pending):** a CREATED-file descriptor's `SYS_READ` now reads the LIVE shared on-disk volume BY NAME (`created_read_live` → `submit_read_file`), retiring the private wstage snapshot as the read SOURCE — so a cross-process (or same-process sibling) opener READS a peer's writes (shared backing) instead of a stale open-time copy. Three coupled changes, all knob-on (`s4_sync_storage()` = knob + FAT + service): **(C2)** `sys_read`'s created branch (gated `FILE_CREATED && FILE_OPNAME != 0 && HELLO_STAGED && service_ready`) resolves `U10_NAMES[nameid]` and bounces the live read through a kernel-stack buffer (GROW.BIN — a growable STAGED file, `FILE_CREATED == false` — keeps its wstage serve, byte-identical); **(C3)** `open_created_sibling` seeds the sibling wstage EMPTY (no snapshot COPY — the ledgered torn-copy is closed BY CONSTRUCTION, there is no copy to tear) and stamps the sibling's identity from the CALLER-verified `nameid` (never a re-read of the source slot) after RE-VALIDATING the source still names it (`FILE_USED && FILE_CREATED && FILE_OPNAME == nameid+1`, else `-ENOENT`); **(C4)** `sys_open_dynamic` re-checks `DYN_DELETED_G[nameid]` AFTER `created_desc_any_row` resolves (fail-closed `-EBUSY`/`-ENOENT`). **Residuals 3 (torn-copy/disclosure) + 4 (open-vs-unlink TOCTOU) are CLOSED-WHEN-ON,** with one honest residual: the source read + the C3 re-check are not atomic, so a metal-only recycle-to-a-DIFFERENT-name window is NARROWED (the sibling can only ever name the caller-verified nameid, so a recycle to another name is caught fail-closed) but not eliminated — the airtight fix is the S6 namespace lock (`with_fat_lock`, out-of-lane `fat.rs`). No corruption / freed-chain / wrong-file LIVE read is possible: the single service task serializes read-vs-delete, and the sibling names the caller-verified nameid. (**Seat-review precision, 2026-07-11:** that claim covers the different-name leg; the same-NAMEID reincarnation leg — an unlink+synchronous-delete+recreate of the SAME name landing entirely inside one IF-masked open window, rebinding a stale ACL vet to a fresh incarnation — is physically near-unrealizable but not excluded by this argument; S6's namespace lock closes it. Also carried, NOT fixed by S5: the pre-existing -EIO-synchronous-delete-clears-`DYN_DELETED_G` wart — a knob-on re-create after a storage error idempotently adopts the stale on-disk entry; on the S6 baton.) **Witness:** `s5_shared_backing_witness` (kernel-side, cross-row, reuses DEFER.BIN, runs first in the u11m2 launcher, SILENT skip off knob-on-FAT, unconditional cleanup) proves NON-VACUOUSLY that the read source is shared/live: a cross-row sibling holds an EMPTY private wstage (`WSTAGE_LEN == 0` — the discriminator; the old snapshot model would hold 16 bytes) YET `created_read_live` returns a peer's POST-OPEN overwrite (P2) — the private buffer never saw it, only the live read did. The u11m2/u6gx EL0 fixtures supply the production-faithful DISPATCH proof (a real `SYS_READ` of a cross-process sibling returns the correct 16-byte pattern from an EMPTY-wstage sibling → the read can only be live). **NO concurrent stress + no metal-race framing:** read/write file ops serialize through the single service task, so tearing is impossible by construction (architectural, not a QEMU-TCG artifact). **⚠ SCHEDULING DEADLOCK found + fixed (QEMU-reproducible, NOT metal-only):** routing created reads through the single service task makes a read BLOCK on it; a NON-preemptible ring-3 fixture busy-spinning (IF=0, `spawn_user_in_space`) on the service task's core (u6gx's owner A parked on its GO word; A and the service task both take `online.first()` = cpu 1) makes the core unschedulable, so the service task never runs and u6gx's grantee B's cross-core created read blocks forever (deadlock: launcher waits B → B waits the service task → the service task waits behind A's IF=0 spin → A waits the launcher). No priority preempts an IF=0 task. Fixed by (a) spawning the service task `PRIO_HIGH` (a system service other tasks block on must out-rank a spinning user task — a same-priority cross-core wake only "waits its turn", `sched::poke_for` preempts only `prio > running`), and (b) making u6gx's cooperative-spin fixtures PREEMPTIBLE knob-on (`spawn_user_preemptible`, gated on `s4_sync_storage()` so knob-off keeps the byte-identical non-preemptible spawn; u6gx is register-only so the ledgered unsaved-FP-across-preemptible-switch gap is not reachable) — the timer then evicts A and the `PRIO_HIGH` service task runs. **Metal relevance (honest):** a well-behaved program blocks/yields; a MALICIOUS busy-spinner on the service core could still DoS created reads on this single-service-task design — a broader scheduler-fairness concern (a yield syscall / a dedicated service core / per-core service tasks) noted as future work, out of S5's scope. **Evidence** — `./arroyo check` both arches (knob on+off); knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test` = full chain 0 FAIL with `:: S3:`/`:: S4:`/`:: S5:` witnesses + u10cx/u11m2/u6gx created reads served LIVE + u6gx PASS (owner/grantee choreography completes — the deadlock fix); knob-ON `test 40` = MISSION + `bx-blockreq` with NO `:: S5:` line (silent skip, no FAT); knob-OFF `test 40` = MISSION and `UNAOS_FATIMG=sf test` = BYTE-IDENTICAL (no `:: S3:`/`:: S4:`/`:: S5:`/`PRIO_HIGH` lines; u6gx passes non-preemptibly); `UNAOS_NOSTORAGE` clean both knob states; the documented best-effort console drops one verdict line under multi-AP contention (0 FAIL every run). **Lane:** `arch/x86_64/syscall.rs` (the read routing + `open_created_sibling` + `sys_open_dynamic` + the witness + the u6gx preemptible spawn) + `drivers/xhci/irqstorage.rs` (the service task `PRIO_HIGH`); `fat.rs`/`block.rs` reused unchanged; **zero aarch64.** **Metal (S5):** the shared-backing correctness is deterministic (proven), but S5 rides the same attended rMBP bench as S4 (transfer-IRQ I/O + create/grow/delete + the cross-process read/write + fsck) — ✅ **METAL-CONFIRMED 2026-07-11 (round-6 attended rMBP bench, Boot 1 on a pristine card + the pristine re-confirm boot 3′):** `./arroyo mbench` PASS 29/29 required witnesses + 0 forbidden + 0 FAIL/EXCEPTION/panic on the real 2012 rMBP over FTDI serial (knob-ON usbdebug videobench media, kernel.elf sha 872f3e4a; live UNAOSRW FAT16 card). `:: S5: … cross-process read serves LIVE shared backing … -> PASS ::` fired FIRST-EVER on metal; **`:: U6gx: … -> PASS ::` is the DEADLOCK-CLOSURE witness under real SMP** — u6gx's grantee completes its LIVE cross-core read, which is possible ONLY via the PRIO_HIGH wake + the per-core timer evicting the preemptible spinner, so the scheduler deadlock fix WORKS on silicon (the metal proof the security-tier review deferred; re-confirmed a 3rd time on the pristine boot 3′). `:: STOR-1: storage service task live on cpu 1 (irqstorage, PRIO_HIGH) ::` + S4-first-metal (`:: S4: … SYNCHRONOUS … -> PASS ::`) + `:: bx-blockreq: PASS ::` (transfer-IRQ I/O re-confirm on the MF1/MF2-fixed code) + the full U-chain all PASS; HELLO.BIN intact. **Honest artifact (ledgered, NOT a regression):** a later reboot on a NON-re-prepped card showed a `witness=0x0` false-FAIL across the WHOLE created-file family (U11x/U11m2/U6gx, `done=2` = fixtures EXITED, not the deadlock's `done=0` hang) — the documented stateful-fixture staleness; forensics on the stale card (archived) showed created-family leftovers (a 0-byte create-then-cut `DELME.BIN` + `FRESH.BIN` + a grown `GROW.BIN`) that a pristine card never carries, and a pristine re-prep re-confirmed all-PASS on the SAME kernel (identical storage code), proving it a card-state artifact orthogonal to the mechanism. **STOR-1 S6 — the syscall-layer NAMESPACE lock (LANDED 2026-07-11 knob-on; QEMU-green + ✅ METAL-CONFIRMED 2026-07-12 rMBP): CLOSES S5 residual 1 AIRTIGHT.** S5 narrowed the created-file recycle / open-vs-unlink windows to fail-SAFE (a sibling can only ever name the caller-verified name-id) but left the source-resolve (`created_desc_any_row`) + the re-validate (`== nameid+1`) NON-atomic — a metal-only recycle-to-a-different-name / same-name-reincarnation window. S6 adds an IRQ-masked `SpinMutex` (`NAMESPACE`/`ns_lock`, `arch/x86_64/syscall.rs`, the pi4 F3 `NAMESPACE` twin) that makes the THREE created-file name sequences MUTUALLY ATOMIC: the sibling-open decision (`sys_open_dynamic`: deleted-check → `created_desc_any_row` → ACL → `open_created_sibling`), the fresh create (`open_create_new`, with a new ACL-checked idempotent-sibling fallback closing the create-races-create ownership-theft window), and the `sys_unlink` claim + descriptor sweep. No unlink/create can interleave inside another sequence's resolve→claim, so the recycle/reincarnation legs are closed by exclusion. **⚠ The lock is held for the O(1) IN-MEMORY namespace decision ONLY, NEVER across a `submit`/BOT pump — the S5 deadlock lesson binds:** the two blocking disk ops are lifted OUT of the locked region — the idempotent `submit_create` runs BEFORE the lock (`open_create_new`), and the last-close `submit_delete` runs AFTER it (`sys_unlink` performs it post-`drop(ns)`; `files_free` split into `files_free_clear` [atomic clears, lock-safe] + `openf_release` [blocking, lock-free]). The disk create/delete are idempotent AND serialized by the single-service-task BOT writer, so lifting them out re-introduces no disk-level race. `sys_close`/`files_free` need NOT take the lock: completing a recycle requires a `files_alloc` REUSE, which happens only inside a lock-held open sequence; a bare close mid-resolve makes the re-validate fail closed (`-ENOENT`, fail-safe). **§6 decision 2 RESOLVED:** `FAT_MUTATION` is NOT activated on x86 — VACUOUS under the single-service-task-writer invariant (the pi4 FAT lost-update RMW race is unreachable with exactly one BOT writer); the real gap was syscall-layer namespace atomicity, not the FAT RMW. `fat.rs` (shared kernel-core) stays untouched. **Carry-overs folded (seat fold-ins):** the `-EIO` synchronous-delete now gates its `DYN_DELETED_G` clear on delete SUCCESS (`openf_perform_delete`) — a wedged delete leaves the name blocked (fail-SAFE) rather than a knob-on re-create adopting a stale on-disk entry; + three uncounted mbench witnesses (MF2 immutable-code RW refusal `S4-mf2`; the last-close synchronous-delete outcome `S4-race`; the S6 lock-holds `S6-witness`) + a u6gx drain-verdict tighten (`count==0` knob-on). **Witness `s6_witness_launcher` (`:: S6-witness: … witness OK ::`, uncounted, knob-on FAT):** a cross-core in-RAM RMW on the SAME `ns_lock` — LOCKED reaches `2*N` intact; the UNLOCKED control loses under contention (QEMU actually interleaved: locked 240000/240000, unlocked lost ~119800/240000, proving the lock closes a REAL race — the positive proof; true-parallelism is otherwise metal-latent per risk 3). **Evidence:** `./arroyo check` both arches (knob on+off, zero aarch64); knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test` = full chain 0 FAIL with `:: S3:`/`:: S4:`/`:: S5:` + `:: S4-mf2:`/`:: S4-race:`/`:: S6-witness:` witnesses + u10c/u11m2/u6gx created reads served LIVE + u6gx PASS; knob-ON `test 40` = MISSION + `bx-blockreq` (no FAT witnesses, silent skip); knob-OFF `test 40` = MISSION and `UNAOS_FATIMG=sf test` = BYTE-IDENTICAL (no `:: S3/S4/S5/S6:`/witness lines; the NAMESPACE lock is uncontended-transparent knob-off); `UNAOS_NOSTORAGE` clean both. **Lane:** `arch/x86_64/syscall.rs` only (`fat.rs`/`block.rs` reused unchanged); zero aarch64. **Metal (S6): ✅ METAL-CONFIRMED 2026-07-12 (round-9 attended rMBP bench, Boot 1, TWO clean boots).** The true-parallelism proof landed on silicon: `:: S6-witness: NAMESPACE cross-core RMW (worker on cpu 1) — locked 240000/240000 intact (0 lost); unlocked lost 119996/240000 -> the created-file open/create/unlink sequence lock serializes under real contention -> witness OK ::` — the UNLOCKED control lost ~50% of its increments to REAL cross-core parallelism while the LOCKED path stayed 0-lost (the metal proof risk 3 deferred; QEMU also interleaved here, metal confirms it on true SMP). The two seat-fold-in witnesses fired clean too: `:: S4-mf2: RW open of staged code (HELLO.BIN) refused -EACCES … witness OK ::` and both `:: S4-race(close-release): … ::` / `:: S4-race(teardown-release): last close took the SYNCHRONOUS in-syscall delete path (no held op) … witness OK ::` (the S4 cross-process delete-at-last-close races — QEMU-TCG-uninterleavable, design risk 3 — took the synchronous path on real SMP). Knob-ON usbdebug+videobench media (kernel.elf sha `3083b467`); `./arroyo mbench` PASS 37/37 required + 0 forbidden + 6/6 pending matched, 0 fault; log `~/unaos-bench/rmbp-serial-2026-07-12-185626.log`. **STOR-1 S7 — retire the U6bx staged-open constraint (LANDED 2026-07-12 knob-on; QEMU-green + ✅ METAL-CONFIRMED 2026-07-12 rMBP): `sys_open` resolves an open of ANY on-disk file.** The read machinery already resolved arbitrary names live (`service_read_file` → `find_located`); only the OPEN layer was gated to the staged/`U10_NAMES` name tables, so a pre-existing file that was neither staged nor a U10 name was `-ENOENT` at open. S7 adds a DYNAMIC fallthrough: knob-on, when a name is neither staged nor a U10 name, `sys_open_dynamic` calls `open_dynamic_ondisk`, which sizes the file via a new `BlockOp::Stat` (`submit_stat` → `find_located`, the one fact the IF-masked handler cannot resolve itself) and opens a READ-ONLY descriptor whose reads route live BY the stored NAME through `submit_read_file` (`sys_read`'s new `FILE_DYNLEN != 0` branch, filled a page at a time so an arbitrary file may exceed the one-page staged bound). **Three security properties hold by construction:** (1) **READ-ONLY — MF2 generalized:** a writable dynamic open is refused `-EACCES`, so S7 opens NO write path to arbitrary files (the S3 in-place write-through resolves by name and could otherwise overwrite any resolvable file — the exact hazard MF2 closed for HELLO.BIN, here generalized to every non-staged file). (2) **ACL surface unchanged:** a dynamic on-disk file has no `U10_NAMES` id, so it can never enter `OWNED_FILES` and is inherently PUBLIC — no new policy (invariant: mirror created files' ACL). (3) **Staged/U10 names EXCLUDED — CASE-INSENSITIVELY** (the security-critical step, security-review CONFIRMED-and-FIXED before merge): the dynamic fallthrough first CANONICALIZES the ring-3 name to 8.3 UPPERCASE (`make_ascii_uppercase`), then excludes it if the canonical form is a staged name (`staged_lookup`) OR a U10 name (`u10_name_id`). This is load-bearing because the on-disk resolver the dynamic path submits to (`find_located` → `DirEntry::eq_name`) matches CASE-INSENSITIVELY (equal length + `eq_ignore_ascii_case`), whereas `staged_lookup`/`u10_name_id` compare byte-EXACT against uppercase constant tables — so WITHOUT canonicalization a case-variant of an owned/created U10 name (e.g. `owned.bin`) would miss both exact-match exclusions yet resolve on disk to the OWNED file, bypassing the U6gx owner ACL and reading a private file (a straight confidentiality break). Because `eq_name` requires EQUAL LENGTH, the only queries that resolve to a U10/staged name are exactly its case-variants, every one of which uppercases back to the exact table entry — so canonicalization is a PROVABLY COMPLETE defense (no trailing-space/padding gap). A closed created file thus stays `-ENOENT` in any casing, never re-resolved from disk as PUBLIC; created-file + owner-ACL semantics preserved exactly. The `s7_openany_witness` includes a NEGATIVE regression-lock leg: `sys_open_dynamic("owned.bin")` MUST be refused (a name-table decision, so deterministic regardless of on-disk state). **No `ns_lock`:** a dynamic file is outside the U10 mutation namespace (it can't be created/grown/unlinked via the syscall API), so its resolve races no create/unlink and the `Stat` block never runs under the namespace lock (the S5 deadlock class stays closed). **The pre-stage buffer is RETAINED** — knob-off it still backs every staged read, and `HELLO_BYTES` still serves `sys_spawn`'s program-image copy in BOTH knob states (no live alternative off the IF-masked path) — but knob-on it is no longer the file-open boundary: all reads (staged S2/S3, created S5, arbitrary on-disk S7) serve the live volume. **Seat fold (S6 fold-in 1):** `install_file_handle`'s handle-table-full unwind now documents the non-local invariant (pending ⟹ deleted ⟹ the open was already `-EBUSY`) that makes its `openf_release` safe against a concurrent last-close; a dynamic descriptor has no name-id so its unwind skips `openf_release` (vacuous). **Witness `s7_openany_witness` (`:: S7-openany: … resolved dynamically + read its live content off the pre-stage set … witness OK ::`, uncounted, knob-on FAT):** drives the REAL dispatcher (`sys_open_dynamic`) on a scratch row for README.TXT (a non-staged/non-U10 file every FAT image carries; pre-S7 this exact open was `-ENOENT`), proving a conjunction — the open minted a DYNAMIC descriptor (`FILE_DYNLEN != 0`) stamped with the resolved name, sized from the LIVE volume (`FILE_SIZE == 57` on sf), and a read BY THE STORED NAME returned the file's known content prefix. **Evidence:** `./arroyo check` both arches (knob on+off, zero aarch64, zero new warnings); knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` = full chain 0 FAIL with `:: S3/S4/S5:` + `:: S4-mf2/S4-race/S6-witness:` witnesses intact + the new `:: S7-openany: … witness OK ::` + u6gx/u11m2 PASS; knob-OFF `test 25` = MISSION and `UNAOS_FATIMG=sf test 200` = BYTE-IDENTICAL (all S7 code `irqstorage`-gated or comment-only — no S7/dynamic lines); `UNAOS_NOSTORAGE` clean both. **Lane:** `arch/x86_64/syscall.rs` + `drivers/xhci/irqstorage.rs` (both x86/`irqstorage`-gated); `fat.rs`/`block.rs` reused unchanged; **zero aarch64.** **Residuals (ledgered, out of scope):** arbitrary-file open is READ-ONLY (a writable arbitrary-file path is future work); files > 2 GiB are not openable (the `i32` stat channel); GROW.BIN reads still serve wstage knob-on. **Metal (S7): ✅ METAL-CONFIRMED 2026-07-12 (round-9 attended rMBP bench, Boot 1, TWO clean boots).** `:: S7-openany: a non-staged on-disk file (README.TXT, 57 bytes) resolved dynamically + read its live content off the pre-stage set, and a case-variant of an owned name ("owned.bin") was refused (owner ACL not bypassed) == expected — witness OK ::` fired on the real 2012 rMBP over FTDI serial (pristine UNAOSRW FAT16 card carrying README.TXT + HELLO/SCRATCH/GROW, created names absent). The arbitrary-file open resolves + reads the live volume BY NAME on silicon, AND the security-critical case-insensitive owner-ACL exclusion HOLDS on metal (`owned.bin` lowercase refused — the U6gx private OWNED.BIN not disclosed): the confidentiality regression-lock the security review confirmed-and-fixed is now metal-proven. Rode the knob-ON usbdebug+videobench media (kernel.elf sha `3083b467`); `./arroyo mbench` PASS 37/37 + 0 forbidden + 6/6 pending matched, 0 fault.
- [x] **Ring-3 FP/SIMD hygiene (x86) — first-entry scrub (U2.5 Part 0-i, 2026-07-03, QEMU-verified; metal-pending FTDI cable day).** `user_task_trampoline` now runs `FNINIT` then eight `FLDZ` + eight `FSTP ST(0)` before the GPR scrub, so all eight *physical* x87/MMX registers hold 0.0 at first ring-3 entry — `FNINIT` alone only empties the tags, and an MMX move reads the aliased mantissa bits regardless of tag, hence the `FLDZ`/`FSTP` pass. The x86 twin of the aarch64 M6f FP/SIMD scrub. **Scope is honest — first entry only:** there is still NO FXSAVE/FXRSTOR context switching, so cross-*task* FP residue once multiple programs share an AP is NOT yet covered (rides the x86 context-switch work), and there is no negative residue-read fixture — this is exercised transitively (U1a/U1b/U2 still enter ring 3 and pass). **U3.5 (preemptible ring 3) makes this a live gap, not just a future one:** a preempted ring-3 task's FP/SIMD state is NOT saved/restored across the switch (only its GPRs are), so a preemptible program that used FP would see its FP file clobbered by whatever ran in between. The U3.5 spinner fixture is **register-only** (it touches no FP/SIMD), so it is safe today; a preemptible program that uses FP must wait for the FXSAVE/FXRSTOR context-switch work. **U4x (the process model) widens this further:** Part 0 (RSP0-at-dispatch) enables >1 user task per core, so FP state is now unsaved across ANY ring-3 context switch between two user tasks (e.g. a parent that blocks in `sys_wait` while a child runs), not only across a timer preemption. The U4x parent/orphan/child fixtures are ALL register-only (GPR only), so the gap is reachable but not triggered — it is deliberately ledgered here rather than closed this arc. The `[ ]` FP context-switch item stays open (now with two consumers: U3.5 preemption and U4x multi-task-per-core). The x87 mnemonics assembled cleanly under the `+soft-float` kernel target, so no `FNINIT`-only fallback (and no MMX-alias residue caveat) was needed. SSE stays contained separately by `OSFXSR=0` (#UD → kill), so only x87/MMX is at risk. (First step surfaced by the U2 integrator review, 2026-07-02.)
- [ ] Self-vs-non-self loader check for U2-loaded programs — **not yet.** The U2 FAT loader treats `HELLO.BIN` as UNTRUSTED INPUT: it is bounded only by size (must fit one 4 KiB ring-3 code page; oversize/missing → clean skip, no panic) and is contained solely by ring-3 + NX + W^X + SMEP + the U1b fault-kill net (a malformed program cannot corrupt kernel state or escalate privilege — though it *can* still spin forever and wedge its AP; see the preemptible-ring-3 ledger item below). The loader now **zeros the ENTIRE ring-3 window** (all four pages — code + data + two stack) through each page's identity alias before copying the program in (U2.5 Part 0-iii, 2026-07-03), not just the code page, so a second loaded program cannot read the previous program's / the U1a/U1b fixtures' residue from the data or stack pages; the writes go through the identity aliases, never `USER_BASE`, so the code mapping stays read-only (W^X across cores holds). No signature or allowlist gate exists yet — that is the U-chain's code-signing item (design: `dev/OS/04_SECURITY_IMMUNITY/intrusion_detection.md`), the first consumer of which is exactly this loader. **The aarch64 M6g loader (2026-07-03) is the exact twin of this posture** (size-bounded only, contained by EL0 + per-page perms + the M6b fault-kill net — see the aarch64 ledger below); the code-signing gate, when it lands, must cover BOTH loaders

- [x] **Sockets as capabilities — the UDP socket syscall family, ring 3's first network reach (SOCK-2, 2026-07-12, QEMU-verified; metal-pending — x86-only, behind `UNAOS_SMOLNET`).** Four new syscalls give ring 3 datagram send/recv over the persistent smoltcp `SocketSet`: `sys_socket = 19` (mints a socket), `sys_bind = 20`, `sys_sendto = 21`, `sys_recvfrom = 22`. **A socket is a capability, enforced at the SAME single `handle_resolve` CHECK the File syscalls use:** it is a new object-table kind `KIND_SOCKET` (already scaffolded as the U6bx/U9x kind-negative) whose value word is the persistent-set id (+1-biased off the 0/`u64::MAX` sentinels); `sys_socket` mints `CAP_READ|CAP_WRITE`, **send requires `CAP_WRITE`, recv requires `CAP_READ`** — so `SYS_CAP` GRANT attenuates a socket cap to send-only / recv-only and `SYS_XFER` hands one to another process, for free, with no new enforcement code. A wrong-kind / missing-right / no-handle all fail closed `-EACCES`. **What ring 3 gets, and ONLY that:** datagram send/recv on a UDP socket it created and bound — and nothing else. NO raw-frame access (the `raw_rx`/`raw_tx` Device accessors are kernel-only, `cfg`-gated), NO interface configuration (IP/routes/neighbor cache are kernel-set at init), NO promiscuous receive (a socket receives only what smoltcp demuxes to its bound port). The peer address is DATA the kernel copies through the ring-3-window bound check (the `sys_write`/`sys_open` pointer discipline — the whole `msg = [ip[4]][port u16 LE][pad] + payload` is proven in-window before any deref; full `copy_from_user` is still deferred). **`recvfrom` cannot block the IF-masked handler:** it drives a BOUNDED poll pump and returns `-EAGAIN` when empty — a ring-3 caller can never wedge the core waiting on a datagram (the U3.5 preemptible-ring-3 DoS class stays closed). **A dying task's sockets are freed at teardown** (`clear_handle_row` → `smolnet::free_row_sockets` removes the smoltcp socket + reclaims its static buffers), so a reused address-space slot inherits no live socket. **Fully static** (no heap: the socket set, per-socket packet buffers, and device scratch are all BSS `&'static mut`), and the ~5-7 KiB smolnet frames stay off AP scheduler stacks (device scratch in BSS ⇒ only smoltcp's ~2 KiB poll frames touch the BSP-witness / IF-masked-syscall stacks). **Evidence** — a ring-3 fixture (`sock2-udp`) makes all four syscalls and completes an end-to-end datagram round-trip to slirp's DNS server (`10.0.2.3:53`), `:: SOCK-2: ring-3 udp round-trip — socket/bind/sendto OK, recvfrom returned a datagram FROM 10.0.2.3:53, socket teardown clean -> PASS ::`, plus a kernel-side witness `:: SOCK-2: smoltcp udp dns query 10.0.2.3:53 -> 64 bytes back — witness OK ::`; `UNAOS_SMOLNET=1 ./arroyo test 60` MISSION SUCCESS + both lines; knob-off `./arroyo test`/`test-arm` MISSION with NO SOCK-2 lines (all code `#[cfg(all(feature = "smolnet", target_arch = "x86_64"))]` — byte-identical both arches); `check` both arches on+off, zero new warnings. **Lane:** `arch/x86_64/syscall.rs` + `smolnet.rs` + `drivers/e1000.rs` (additive) + `Cargo.toml` (`socket-udp`); `crates/net`/`fat.rs`/`sched.rs` untouched; **zero aarch64.** **Residuals (ledgered):** UDP only (TCP is SOCK-3); full `copy_from_user` deferred (the cheap window check holds today); the SOCK-1 frame-stealing residual persists cross-CPU (the ring-3 fixture runs on an AP while the BSP's hand-rolled `service_net` also drains the NIC), handled by the fixture's sendto+recvfrom retry loop — a bounded liveness cost, never a safety issue. **The socket value word is only `+1`-biased, with NO generation tag** (unlike the U11x file-id's packed `(gen << 32) | (idx+1)`): safe TODAY because sockets are never transferable (they mint `CAP_READ|CAP_WRITE`, no `CAP_GRANT`, so `SYS_CAP`/`SYS_XFER` cannot move one across rows), but **the moment a future arc makes a socket transferable this is a recycled-slot UAF** — a stale cross-row socket handle would rebind to whatever socket next takes the freed registry slot. SOCK-3 (or the transferable-socket arc) MUST pack a slot-generation into the value word first (the U11x fd discipline). **Metal-pending** (pure syscall logic + slirp witness; joins the accrued x86 attended-bench batch).
- [x] **TCP client sockets — ring 3 gets a byte stream (SOCK-3, 2026-07-12, QEMU-verified; metal-pending — x86-only, behind `UNAOS_SMOLNET`).** Three more syscalls give ring 3 a connected TCP byte stream on the same persistent smoltcp stack: `sys_connect = 23`, `sys_send = 24`, `sys_sock_recv = 25` (named so — `SYS_RECV = 14` is the capability-transfer inbox recv). A TCP socket is minted by `sys_socket` with `type = SOCK_STREAM(1)`; it is the **same `KIND_SOCKET` capability** enforced at the same `handle_resolve` CHECK — **connect + send require `CAP_WRITE`, recv requires `CAP_READ`**, so `SYS_CAP` GRANT still attenuates. **What stream reach adds over UDP, and ONLY that:** a ring-3 program can now active-open a TCP connection to a peer address it names and exchange a byte stream on it — and nothing else new. Still **NO raw-frame access, NO listen/accept (client only this arc), NO interface configuration, NO promiscuous receive**; the peer address is the same 8-byte `[ip[4]][port u16 LE][pad]` header the kernel copies through the ring-3-window bound check (connect: read-source `>= USER_BASE`; send: read-source; recv: write-dest `>= USER_BASE + PAGE_SIZE`, past the RO code page — the `sys_sendto`/`sys_recvfrom` disciplines). **A UDP handle routed to a stream syscall (or a TCP handle to a datagram syscall) fails closed `-EACCES`** on the `SockKind` registry tag, **before** smoltcp's typed accessor can panic — a kernel-integrity guard on a ring-3-chosen syscall/handle pairing. **Nothing blocks the IF-masked handler:** `connect` is non-blocking with a ring-3 poll model (`0` established / `-EINPROGRESS` still handshaking — ring 3 re-drives / `-ECONNREFUSED` reset), issuing the SYN only from state Closed and pumping a BOUNDED loop; `send`/`recv` drive bounded pumps and return `-EAGAIN` / `0`-EOF rather than waiting (the U3.5 preemptible-ring-3 DoS class stays closed). **The recycled-slot UAF the SOCK-2 review flagged is now CLOSED for all socket kinds:** the socket handle value word is the **gen-fenced** `(gen << 32) | (sid + 1)` (`sock_id_pack`), and `socket_id_of` validates the packed generation against the live registry (`smolnet::sock_valid`: present, owner-matched, generation-matched — the U11x `file_desc_validate` discipline). `SOCK_GEN[sid]` bumps on every free (`stack_close` / `free_row_sockets`), so a stale handle to a freed-and-reused slot resolves `-EACCES`, no rebind — making a future transferable-socket arc safe by construction. **No global-lock busy-wait:** every TCP pump (`tcp_pump_chunked`) releases the global `STACK` lock between `TCP_CHUNK`-sized chunks and re-validates the reg slot on re-acquire, so a cross-CPU socket syscall never spins on `STACK.lock()` for a full ~400 k-iteration pump, and a concurrent teardown is seen as the connection vanishing, never a UAF. **A dying task's TCP sockets are freed at teardown** exactly like UDP (`free_row_sockets` removes the smoltcp socket + reclaims its 2 KiB×2 static stream rings + bumps the gen). **Fully static** (the TCP stream ring buffers are BSS `&'static mut`, borrowed once when a slot is built). **Evidence** — a ring-3 fixture (`sock3-tcp`) makes all three stream syscalls and completes an end-to-end byte-stream round-trip to slirp's DNS resolver over **DNS-over-TCP** (a hermetic 3-way handshake + stream reply under the default slirp backend — no injector, no netdev change), `:: SOCK-3: ring-3 tcp round-trip — socket/connect/send OK, recv returned a byte stream FROM 10.0.2.3:53, socket teardown clean -> PASS ::`, plus a kernel-side witness `:: SOCK-3: smoltcp tcp connect 10.0.2.3:53 established, 64 bytes back — witness OK ::`; `UNAOS_SMOLNET=1 ./arroyo test 90` MISSION SUCCESS + both lines; knob-off `./arroyo test`/`test-arm` MISSION with NO SOCK-3 lines (all code `#[cfg(all(feature = "smolnet", target_arch = "x86_64"))]` — byte-identical both arches); `check` both arches on+off. **Lane:** `arch/x86_64/syscall.rs` + `smolnet.rs` + `drivers/e1000.rs` (additive) + `Cargo.toml` (`socket-tcp`); `crates/net`/`fat.rs`/`sched.rs` untouched; **zero aarch64.** **Residuals (ledgered):** TCP **client** only (no listen/accept yet — SOCK-4); full `copy_from_user` still deferred (the cheap window check holds); the SOCK-1 cross-CPU frame-stealing residual persists (the AP fixture retries via the non-blocking poll model); sockets remain non-transferable (they mint `CAP_READ|CAP_WRITE`, no `CAP_GRANT`) but the gen fence is now IN PLACE so a transferable-socket arc no longer needs to add it. **Metal-pending** (pure syscall logic + slirp witness; SOCK has no metal leg — no wired NIC on any current board — so QEMU slirp is the honest gate).
- [x] **Transferable sockets — a socket capability moves across processes (SOCK-4 scope B, 2026-07-14, QEMU-verified; metal-pending — x86-only, behind `UNAOS_SMOLNET`).** A socket cap is now movable to another principal, the socket analogue of the U7x/U8x console-cap transfer — with **no new syscall** (a socket rides the existing `SYS_XFER`(13)/`SYS_RECV`(14) inbox machinery, which already special-cased `KIND_SOCKET`). Two changes make it real, and safe: **(1) `sys_socket` now mints `CAP_READ|CAP_WRITE|CAP_GRANT`.** SOCK-2/3 minted no `CAP_GRANT`, so `SYS_XFER` (which demands `CAP_GRANT` on the source) could never move a socket; the mint endows it because a socket is the owning process's own resource and `CAP_GRANT` cannot be self-added later (rights only attenuate). Send still needs `CAP_WRITE`, recv `CAP_READ`, so a transfer/grant still **attenuates** to send-only/recv-only, and a transfer that drops `CAP_GRANT` (single-level) keeps the grantee from re-delegating. **(2) `sys_recv` MIGRATES socket ownership.** The persistent registry slot records an owner row, and `sock_valid` requires `owner == caller_row`; a transferred handle carries the same gen-fenced value word but its registry owner is still the grantor's row, so without migration it would fail `sock_valid` at the grantee (`-EACCES`). When `sys_recv` installs a received `KIND_SOCKET` cap it calls `smolnet::reassign_owner(sid, gen, new_row)` (`xfer_socket_migrate`): under the `STACK` lock, iff slot `sid` is still present at the SAME generation, ownership moves to the receiving row — only the owner field changes, the smoltcp socket + its buffers are untouched (a MOVE, not a re-open; a bound port / in-flight connection survives). **Single-owner, gen-fenced, safe by construction:** a socket has exactly ONE owner at any instant — after the move the grantor's original handle is owner-mismatched (`sys_send`/`recv` through it is `-EACCES`, the **cross-row stale-handle rejection**), `free_row_sockets` frees a socket only for its current owner (the grantee's exit reclaims it; the grantor's exit reclaims nothing), and once the owner frees the slot the SOCK-3 **generation fence** (`SOCK_GEN[sid]` bump) makes a stale old-gen handle `-EACCES` against BOTH the freed slot and whatever socket first-fit-reuses it — **no rebind** (the U11x `file_desc_validate` discipline, socket edition). A stale deposit (socket freed+reused between XFER and RECV) fails `reassign_owner`'s gen check, so the received handle is dead-on-arrival rather than stealing a different tenant's socket. **The SOCK-2 review's explicit warning — "the moment a future arc makes a socket transferable this is a recycled-slot UAF" — is thereby DISCHARGED:** the gen fence SOCK-3 pre-installed + this arc's owner migration close it by construction. **Evidence** — a two-fixture ring-3 demo (`sock4-grantor`/`sock4-grantee`, the U7x idiom on dedicated APs): the grantor mints a UDP socket, proves cross-process attenuation (an over-rights `SYS_XFER` is `-EACCES`) and transfers it (dropping `CAP_GRANT`); the grantee `SYS_RECV`s it and completes a datagram round-trip to slirp's resolver **on the moved socket** (`bind`/`sendto`/`recvfrom` FROM `10.0.2.3:53`); then the grantor's post-transfer `SYS_SENDTO` through its migrated-away handle is `-EACCES`; single-writer snapshot + teardown-clear hold. Plus a kernel-side check (`sock4_kernel_check`) that folds the **U11x gen-rebind proof** (grantee frees, a fresh socket reuses the slot at the new gen, the old-gen handle stays `-EACCES`). `:: SOCK-4: transferable sockets — grantee received + round-tripped the moved socket, grantor's migrated-away handle -EACCES, gen-rebind rejected, teardown clean -> PASS ::`; `UNAOS_SMOLNET=1 ./arroyo test 90` MISSION SUCCESS + the line (3/3 deterministic); knob-off `./arroyo test`/`test-arm` MISSION with NO SOCK-4 lines (all code `#[cfg(all(feature = "smolnet", target_arch = "x86_64"))]` — byte-identical both arches); `check` both arches on+off. **Lane:** `arch/x86_64/syscall.rs` + `smolnet.rs`; `crates/net`/`fat.rs`/`sched.rs`/`Cargo.toml` untouched (reuses the existing socket features); **zero aarch64.** **Residuals (ledgered):** it is a MOVE not a copy (the safe choice for a stateful owner-scoped resource); a double transfer is last-recv-wins (no UAF/leak — single owner throughout); single-level (a transferred socket drops `CAP_GRANT`, no re-delegation — socket revocation trees deferred); the demo round-trips UDP but a TCP socket transfers identically (the kind-agnostic kernel check covers it). **Metal-pending** (pure syscall logic + slirp witness; SOCK has no metal leg — QEMU slirp is the honest gate).
- [x] **DHCP via smoltcp — the persistent stack leases its own address (SOCK-5 scope B, 2026-07-14, QEMU-verified; metal-pending — x86-only, behind `UNAOS_SMOLNET`).** Knob-on, the persistent smoltcp interface now obtains its IPv4 address with its **own** `dhcpv4::Socket` instead of copying the static lease the hand-rolled `crates/net` DHCP set (which `hw_addr` returns). **This adds NO new syscall (next free stays 26) and NO new ring-3 surface** — DHCP is kernel-internal interface configuration, so the SOCK-2 invariant "ring 3 gets NO interface configuration (IP/routes/neighbor cache are kernel-set at init)" is **preserved, not weakened**: ring 3 still cannot set an address, add a route, or drive the DHCP socket; it is a kernel socket in a reserved storage slot (`NSOCK+1`) that **never appears in `reg`**, so no `sys_socket`/`handle_resolve` path can ever resolve to it, and no `stack_open*`/`free_row_sockets` touches it. The acquisition (`configure_via_dhcp`) is a **bounded, clock-free poll pump** run once at first stack build from a large-stack context (never a ring-3 syscall stack), and it **falls back to the static `hw_addr` lease** on a silent server so the interface is never left unconfigured (SOCK-1/2/3/4 never regress). Fully static/BSS (the DHCP socket's storage is smoltcp-internal fixed; no heap). **Evidence** — a kernel witness `:: SOCK-5: smoltcp dhcpv4 lease 10.0.2.20/24 gw 10.0.2.2 — witness OK ::` (the leased `.20` ≠ the `.15` static default the interface used to hard-code, so the address demonstrably came from smoltcp's DHCP), and the SOCK-2/3/4 round-trips all still PASS on that address; `UNAOS_SMOLNET=1 ./arroyo test 90` MISSION SUCCESS + the SOCK-5 line, SOCK-1/2/3/4 intact; knob-off `./arroyo test`/`test-arm` MISSION with NO SOCK-5 line (all code `#[cfg(all(feature = "smolnet", target_arch = "x86_64"))]` — byte-identical both arches); `check` both arches on+off. **Lane:** `smolnet.rs` + `Cargo.toml` (`socket-dhcpv4`) + docs; `arch/x86_64/syscall.rs`/`drivers/e1000.rs`/`crates/net`/`fat.rs`/`sched.rs` untouched; **zero aarch64.** **Residuals (ledgered):** one-shot acquisition, no lease renewal (adequate under slirp's effectively-infinite lease; renewal is a future `service_net`-pump arc); the hand-rolled DHCP still runs in the driver (live stack knob-off, and knob-on still leases the driver's own `hw_addr`). **Metal-pending** (no wired NIC on any current board — QEMU slirp is the honest gate).

### aarch64
- [x] EL0/EL1 privilege split (M6a-0/M6a, 2026-07-01, metal-confirmed)
- [x] Per-page user permissions: user code EL0-RX, stacks RW-XN; UXN/PXN on user pages (M6b)
- [x] EL0 fault → task kill with syndrome-matched accounting (M6b)
- [x] Preemptible EL0 — SP_EL0 banked in the IRQ frame; `spawn_user` starts I-unmasked (M6e, 2026-07-02, QEMU-verified + **metal-confirmed** real Pi 4: `IRQs-taken-at-EL0=18`, spinner resumed correctly, no faults). W^X map **unchanged**: the shared user stack is retained (no EL0 program writes its stack), so no new user page and no permission change — the audit table below still holds.
- [x] Kernel W^X / WXN audit (SCTLR_EL1.WXN feasibility) — audit complete; WXN enable pending review (M6c/H1; table below)
- [ ] PAN (Privileged Access Never) where silicon supports it
- [x] Validated user-pointer access — `copy_from_user`/`copy_to_user` (M6f, 2026-07-02, QEMU-verified; ★ metal-confirmed real Pi 4 2026-07-04 on the M6g reflash) — every user pointer crossing the syscall boundary is range-checked before any deref: `copy_from_user` requires `[va, va+len)` fully inside the caller's EL0 window with a non-wrapping length (`checked_add`); `copy_to_user` additionally excludes the read-only code page from its writable range, so a write aimed there is refused with `-EFAULT` **before** the store (never the faulting EL1 write a code-page target would otherwise take). SYS_WRITE now streams through `copy_from_user` (validate-whole-range-then-chunk, so a bad pointer produces `-EFAULT` with no partial output). A bad pointer is an **error return, not a task-kill** (kills stay for hardware-raised faults). Evidence: the M6f hostile fixture issues four bad pointers — `sys_write`(kernel RAM VA), `sys_write`(unmapped window-adjacent VA), `sys_write`(wrapping length), `sys_getinfo`(RO code-page target) — and the kernel refused all four with `-EFAULT` and **0 kills, 0 kernel halts** (`:: M6f: 4 hostile pointers refused (EFAULT), 0 kills -> PASS ::`), while the well-behaved `sys_getinfo` round-trip (`copy_to_user` then EL0 read-back matching `SYS_GETPID`) passed. Scope is honest: A72 is Armv8.0 with **no FEAT_PAN**, so this is pure software validation — the direct EL1 access is sound only because a syscall executes with the caller's TTBR0/ASID live (M6d), so an in-window VA can reach only that task's frames; a PAN-capable port (Jetson A78) must switch to an unprivileged (LDTR) or validated-copy path. **Metal-confirmed (real Pi 4, 2026-07-04):** all four hostile pointers refused with 0 kills / 0 kernel halts and the getinfo `copy_to_user` round-trip passed on silicon (`:: M6f: 4 hostile pointers refused (EFAULT), 0 kills -> PASS ::`, `getinfo/copy_to_user round-trip -> PASS`).
- [x] Per-task address-space isolation via ASIDs (M6d, 2026-07-02, QEMU-verified + **★ metal-confirmed** real Pi 4) — each EL0 task runs in its own translation-table branch with its own 16 KiB backing mapped at the same VAs; user-window leaves are non-global (`nG=1`, ASID-tagged 1..8), kernel leaves stay Global; `TTBR0_EL1` is switched (root + ASID) on dispatch and the slot is broadcast-`TLBI ASIDE1IS`'d and repointed off at exit. A task can no longer see or corrupt another task's user memory at a shared VA. **Metal proof (2026-07-02, EL=1/54 MHz — the class of thing QEMU cannot test):** `same-VA isolation A=0xa5a5…1 B=0x5a5a…2 distinct -> PASS` on real A72 TLB/caches (backed by a deterministic kernel-side TTBR0-swap `nG` probe), EL0 stack write/readback + SP-sentinel `-> PASS`, and with EL0 preemption live in the same boot (aggregate `IRQs-taken-at-EL0=21`, QEMU=0 — a demo-wide counter with no per-task attribution) all four slot tasks reported correct sentinel/stack values across interleaved per-task `TTBR0`/ASID dispatches; M6b `exited=1 killed=3 -> PASS` unchanged, 0 unexpected faults.
- [x] EL0 first-entry GPR scrub (M6d, 2026-07-02) — `user_task_trampoline` zeroes x0–x30 before the first `eret` to EL0, so no live kernel value (the raw `Task` pointer, kernel x29/x30, ...) reaches EL0 **in the general-purpose file** at entry. The FP/SIMD file is NOT yet covered (next item). The aarch64 twin of the x86 SYSRET GPR scrub (merged with U1b).
- [x] Loadable program from storage — the untrusted-loader path (M6g, 2026-07-03, QEMU-verified; ★ metal-confirmed real Pi 4 2026-07-04) — the block layer dispatches over a registered backend so a read-only BCM2711 EMMC2/SDHCI microSD driver (PIO, single-block CMD17, polled, no writes; every wait CNTPCT-bounded) sits beside the untouched xHCI path (x86 read/write byte-identical — the seam is cfg-gated to `aarch64+baremetal`). The M6g loader reads `HELLO.BIN` off the card's FAT volume, **rejects it up-front from the on-disk directory size** if it does not fit one 4 KiB code page (the U2 truncation lesson: `read_file` caps at `min(size, cap)`, so a post-read check could never SEE an oversize file), copies it into a fresh M6d per-task slot, and protects the page EL0-RX/EL1-RO **before the task exists** (so the untrusted bytes are never writable-and-executable). **The loaded bytes are UNTRUSTED and bounded ONLY by size** — contained by EL0 + per-page perms + the M6b fault-kill net, exactly the x86 U2 posture; there is **no signature or allowlist gate yet** (see the shared code-signing item below). Same residual DoS as U2/x86: a program that never `svc`s can spin its EL0 core (M6e makes EL0 preemptible on metal, so the timer *can* evict it there; QEMU delivers no timer IRQ). QEMU proves the seam, the whole SDHCI ladder against the legacy base, FAT mount off SD, and the EL0 load + run + name-routed exit accounting; the EMMC2 base actually carrying the card is metal-only — and it **ran on silicon (2026-07-04):** no fallback line, `SD card @0xfe340000 identified — 31116288 blocks (15193 MiB, CSD v2)` (a real ~16 GB SDHC card, block-addressed — vs QEMU's 64 MiB/CSD v1 legacy fallback), then FAT mount + `HELLO.BIN loaded from SD (51 bytes) -> EL0` + `disk-loaded EL0 program exited ok -> PASS`, with 0 unexpected faults.
- [x] Minimal process model — `sys_spawn` + `sys_wait` (M7, 2026-07-04, QEMU-verified; ★ metal-confirmed real Pi 4 2026-07-04) — an EL0 program can spawn a child program (`SYS_SPAWN = 8` loads the fixed on-disk `HELLO.BIN` into a fresh per-task slot and runs it at EL0, returning its pid) and reap it (`SYS_WAIT = 9` blocks on a per-child counting `Semaphore` the child posts once — on its normal exit **or** on a fault-kill, so a killed child can never hang its parent). **The security scope is deliberately narrow and stated honestly: there is NO capability or permission check on spawn yet** — any EL0 program that reaches `SYS_SPAWN` gets a child. What contains the child is the SAME chain as every prior EL0 program, nothing weaker: it runs on its own fresh slot (own ASID, per-page EL0-RX/EL1-RO code protected *before the task exists*), **inherits nothing** from the parent (no shared memory, no shared handles — there is no handle table yet), and is bounded by EL0 + per-page perms + the M6b fault-kill net. The child bytes are the same size-bounded **untrusted** on-disk program as M6g (no signature/allowlist — the shared code-signing item below gates BOTH the child load and the M6g loader). Resource bounds are fixed, not grown under load: a 4-entry process table and the 8-slot pool both return `-EAGAIN`/`-errno` on exhaustion rather than expanding. Residual DoS is unchanged from M6g/U2 — a child that never `svc`s can spin its EL0 core (the timer evicts it on metal, M6e; QEMU delivers none). **`sys_spawn` takes no name/path argument** — the program is fixed to `HELLO.BIN`; arbitrary program-by-name is M8 and needs a validated `copy_from_user` name (a spawn argument is a new attack surface, held off deliberately). The capability model — *which* principals may spawn *what*, plus inheritance/grant semantics — is U5/U6. QEMU exercises the whole reap round-trip end to end (the waker is a scheduler post, not the timer): `:: M7: parent spawned child pid=<p>, waited, child exited status 0 -> PASS ::`, with every M6b/M6d/M6e/M6f/M6g verdict byte-identical (the child-exit accounting is routed by pid through the process table, never through the M6b/sentinel counters). **Metal-confirmed (real Pi 4, 2026-07-04):** `:: M7: parent spawned child pid=41, waited, child exited status 0 -> PASS ::` on silicon — the child loaded off the real card via the EMMC2-first path QEMU cannot exercise (`SD card @0xfe340000 — 15193 MiB, CSD v2`), and the parent's blocking `sys_wait` was woken by the child's scheduler post and reaped it under a live timer (EL0 preemption live, `IRQs-taken-at-EL0=23`), with 0 unexpected faults / 0 FAIL lines and the whole prior battery green (M6b `exited=1 killed=3 PASS`, M6d ×3, M6f ×3, M6g PASS, CAPSTONE 6/6, EL=1/54 MHz).
- [x] Per-process handle table + owner-scoped reaping (U4, 2026-07-04, QEMU-verified; no metal this arc — fully QEMU-verifiable scheduler/syscall logic) — evolves M7 into the ownership substrate. `SYS_SPAWN = 8` now installs the child into the *spawner's* per-process handle table and returns a small **handle index** (not the raw pid); `SYS_WAIT = 9` takes that **handle**. The table is a static, const-init `HANDLES[[AtomicU64; 8]; USER_SLOTS+1]` keyed by the caller's **ASID** (read from `TTBR0_EL1[63:48]` synchronously in the SVC handler — the caller's root is live there); `PROCS` stays keyed by pid (exit-accounting control blocks) while `HANDLES` is keyed by ASID (the spawner's private namespace of child capabilities). **Honest scope — this is STRUCTURE, not yet a CHECK:** the per-process handle table now holds child-process handles and **reaping is owner-scoped BY CONSTRUCTION** — a task can only reap children whose handles are in ITS table, so the M7 review note (any EL0 task could `sys_wait` any pid) is folded structurally — **but there is NO capability CHECK yet.** The capability model proper — handles-as-capabilities with grant/attenuate/revoke, routing resource syscalls (e.g. `sys_write`) through the table, and bandy handle-transfer — is **U5**, which adds its check at *exactly the handle lookup this arc builds*. `sys_write` is deliberately **left as the `fd==1` path** (routing it through the table now would force seeding a table for every existing EL0 writer for no capability benefit — a STOP tripwire). No new syscall number, no name/path argument to `sys_spawn` (that is still M8), no new scheduler primitive; the child bytes remain the same size-bounded **untrusted** on-disk program as M6g/M7 (no signature/allowlist — the shared code-signing item still gates the load). Resource bounds stay fixed (a 4-entry process table + the 8-slot pool return `-EAGAIN`; the 8-slot-per-process handle table likewise). Evidence — the demo proves the isolation the structure gives: a parent (`el0-u4parent`) `sys_spawn`s two children and reaps **both by handle**; an ownership negative (`el0-u4orphan`, its own slot/ASID) calls `sys_wait(0)` on an Empty handle in its OWN table and gets `-ECHILD` — same handle *index* 0, different tables, different outcomes: `:: U4: process model — parent reaped 2 children by handle, non-child sys_wait -ECHILD (per-process handle tables) -> PASS ::`, with every M6b/M6d/M6e/M6f/M6g verdict byte-identical (child-exit accounting stays pid-keyed through `PROCS`, never the handle or the sentinel counters).
- [x] **Handles as capabilities — the enforcement CHECK + grant/attenuate/revoke + routed `sys_write` + teardown-clear (U5, 2026-07-05, QEMU-verified; no metal this arc — pure syscall logic, and the child loads ride U4/M7's already-metal-confirmed EMMC2 path).** Turns U4's handle STRUCTURE into a real capability. A handle now carries **rights** (a `u8`/`u16`-scale bitmask `CAP_READ|CAP_WRITE|CAP_EXEC|CAP_GRANT|CAP_REVOKE`, held in a **sidecar** `HANDLE_RIGHTS[[AtomicU32; 8]; USER_SLOTS+1]` keyed identically to `HANDLES`, so U4's `0`/`RESERVING` value-word sentinel logic is byte-unperturbed) and names a **target** beyond "child pid" (a well-known `HANDLE_CONSOLE = u64::MAX-1` token — kept to two kinds, `CHILD(pid)` and `CONSOLE`, NOT a general object table, which is U6). **The CHECK** is a single `handle_resolve(asid, idx, req_rights)` at the one lookup point every handle-consuming path goes through: out-of-range/Empty ⇒ the caller's own errno (`sys_wait` → `-ECHILD`, preserving U4 structural ownership; `sys_write`/`SYS_CAP` → `-EACCES`), present-but-missing-a-right ⇒ `-EACCES`. **grant/attenuate/revoke** are first-class via `SYS_CAP = 10` (sub-op selector): GRANT mints a new handle to the same target carrying a rights mask that must be a **subset** of the granter's rights on the source (the **monotonic-decrease / attenuation invariant** — `req & !src_rights != 0` ⇒ `-EACCES`; a grant can never amplify), requiring `CAP_GRANT` on the source; REVOKE drops a handle the caller owns (`-EACCES`/`-ECHILD` on subsequent use). **`sys_write` now routes through the table** — `fd` is a handle index that must resolve to a `CONSOLE` handle with `CAP_WRITE`; there is no ambient stdout. Every EL0 process that prints is **endowed** a `CONSOLE`+`CAP_WRITE` cap at the conventional index `CONSOLE_FD = 1` at spawn/launch (the shared window ASID 0 for `el0-hello`; each M6f/M6g/U4-child slot); the `copy_from_user` validation and all-or-nothing `-EFAULT` semantics are byte-identical, so the M6f hostile fixture (which holds the cap) still gets `-EFAULT`, not `-EACCES`. **Teardown-clear** (folds U4's one deferred lifecycle note): `boot::teardown_user_slot` now wipes the whole `HANDLES[asid]` row + its rights **before** releasing the slot's used-flag (not after — a post-release clear could race a concurrent `alloc_user_slot` on another core that reclaims the same ASID), so no stale capability outlives its owning ASID. Evidence — the `el0-u5cap` fixture proves the four EL0-observable behaviours against its own table and the launcher proves the fifth kernel-side: `:: U5: capabilities — write-cap OK, no-cap -EACCES, attenuated grant bounded, revoke enforced, teardown-clear clean -> PASS ::`, with every M6b/M6d/M6e/M6f/M6g/U4 verdict byte-identical (all four prior `hello from EL0` prints land — their processes hold the endowed console cap — and CAPSTONE 6/6). Still deferred to **U6**: bandy handle-transfer between principals, a general object table (routing fs/net syscalls through handles), and cross-process revocation trees (`CAP_REVOKE` as a right is defined but reserved).
- [x] **General object table — `(kind, target, rights)` descriptors, first-free for ALL kinds, the `CONSOLE_FD` collision closed (U6a, 2026-07-06, QEMU-verified; no metal this arc — pure syscall logic, and the demo's children ride U4/M7's already-metal-confirmed EMMC2 load path).** Generalizes U5's fixed-shape handle into a general object descriptor. **The kind rides in a parallel sidecar** `HANDLE_KIND[[AtomicU8; 8]; USER_SLOTS+1]` (keyed identically to `HANDLES`/`HANDLE_RIGHTS`), so the value word keeps U4/U5's sentinels **byte-identical** — `0` = Empty (the lock-free allocator's free marker), `u64::MAX` = `RESERVING` (an in-flight claim) — and nothing else is reserved. A handle now names one of four kinds — `Child(pid)` (U4), `Console` (U5), and the **scaffolds** `File(id)` / `Socket(id)` (defined + resolvable via `handle_resolve`, though no fs/net syscall routes through them yet — they prove the table is genuinely general). The sidecar choice is deliberate: it makes the STOP-tripwire sentinel-collision reasoning *structurally impossible* (a `File`/`Socket` id may be any non-`0`/non-`u64::MAX` word with no high-bit masking), and mirrors the rights sidecar 1:1 (same publish-kind-and-rights-before-the-live-value / observe-after discipline; single-writer-per-ASID is the backstop). **The `CONSOLE_FD` collision is closed** — the raison d'être of the arc. U5 pinned the console cap at a fixed index `CONSOLE_FD = 1` via an unconditional store while `handle_install`'s first-free scan allocated children from index 0, so a process that both PRINTED and SPAWNED 2+ children could have a child auto-allocated onto index 1 and then clobbered by the console install (harmless only because no such process existed). U6 makes `CONSOLE_FD` a **reserved index the first-free allocator SKIPS**: the console lives there by the `fd=1`/stdout convention (keeping every prior blob's `sys_write(fd=1)` byte-identical), and auto-allocated child/object handles fill `{0, 2, 3, ..}` — so a console cap and N child/object caps coexist with **zero index collision, for any interleaving of installs** (rather than allocating the console through the shared allocator, which would break the stable stdout index). Every existing consumer is behaviour-preserved: `handle_resolve` dispatches on the kind sidecar (`sys_wait` requires `Child`, `sys_write` requires `Console`+`CAP_WRITE`, `sys_cap` grant/revoke on any kind); the **attenuation invariant is unchanged** and the mint now copies the source handle's kind (a grant attenuates rights, never re-kinds the object); `handle_clear`/`clear_handle_row`/`handle_row_is_clear` (the U5 teardown-clear) also clear/verify the kind. Evidence — the `el0-u6spawn` fixture is the printing spawner U5 could not serve: it prints, spawns 2 children (distinct auto-allocated handles, both off the reserved index), prints AGAIN (proving the console cap survived the spawns), and reaps both by handle; the launcher additionally proves kernel-side that the `File`/`Socket` kinds resolve to their kind with the required right (and `-EACCES`-equivalent without) and that the exact U5-breaking console-vs-two-children interleaving no longer collides: `:: U6: general object table — printing spawner + 2 children, no index collision, File/Socket kinds resolve -> PASS ::`, with every M6b/M6d/M6e/M6f/M6g/U4/U5 verdict **byte-identical** and CAPSTONE 6/6. **Honest scope — this is (a) the OBJECT TABLE only.** Deferred to **U7**: cross-process handle-transfer (`SYS_XFER` — a cross-ASID write discipline that breaks the single-writer invariant) + revocation trees (`CAP_REVOKE`, still reserved) + the bandy Ring-3 delegation wrapper; and to a later arc: real `File`/`Socket` fs/net syscalls routing through these kinds, plus UnaFS `owner`/`grants:*` enforcement on `SYS_OPEN` (rides the kernel UnaFS mount, K2/K3). Six-lens adversarial self-review (no-collision under every interleaving, kind sentinel-safety + ordering, attenuation, consumer preservation, demo correctness, holistic).
- [x] **Real `File` handles — `SYS_OPEN`/`SYS_READ` routed through the object table via `File` + `CAP_READ` (U6b, 2026-07-06, QEMU-verified + ✅ METAL-CONFIRMED on the real Pi 4 — the fixture opened `HELLO.BIN` and read it through a `File` capability off the real EMMC2/SD card, both `-EACCES` denials holding, full battery green).** Makes U6a's `File` scaffold real: the first resource syscall routed through a **non-Console** object, and the direct precursor to UnaFS grants. **`SYS_OPEN = 11`**`(name_ptr, name_len)` `copy_from_user`s the bounded 8.3 name, mounts the single **read-only** FAT volume, finds the top-level entry (a directory ⇒ `-EISDIR`), records a per-task **open-file descriptor**, and installs a `File` handle carrying `CAP_READ` (first-free, skipping the reserved `CONSOLE_FD`). **`SYS_READ = 12`**`(handle, buf, len)` is the enforcement point — `handle_resolve(asid, handle, CAP_READ)` must yield a `File`; a missing right (`Denied`), a non-File kind (`Console`/`Child`/`Socket`), or no handle (Empty/oob) **all return `-EACCES`**, the exact twin of `sys_write`'s Console+`CAP_WRITE`. It then clamps the request to `min(len, size − offset)`, validates the **whole** destination up front (`user_range_ok(buf, want, writable=true)` — the RO code page excluded — so a bad buffer is `-EFAULT` with no disk read and **no offset advance**), reads `want` bytes from the current offset via a new read-only **offset-aware** FAT reader (`fat::read_at`, walking the chain and skipping to `start`; `read_file` left byte-identical for its M6g/U4 caller), `copy_to_user`s, and advances the offset by exactly the count delivered (`0` = EOF; **sequential, no seek**). The descriptor lives in a small **per-task FILES table** — parallel atomic arrays `FILE_USED`/`FILE_CLUSTER`/`FILE_SIZE`/`FILE_OFFSET` keyed `[asid][idx]` (`NFILE = 4`), the same lock-free sidecar shape and Release-publish/Acquire-resolve discipline as `HANDLE_RIGHTS`/`HANDLE_KIND`; the `File` handle's value word carries the **file-id = descriptor index + 1** (the `+1` bias keeps it clear of the value word's `0`/`u64::MAX` sentinels, **structurally**, so a `File` id never aliases Empty/RESERVING). `sys_open` claims resources **last** and mirrors `sys_spawn`'s reserve/unwind: a full handle table after a descriptor was claimed frees the descriptor then returns `-EAGAIN`, so no failure path leaks a FILES slot or a handle. **Teardown-clear** extends U5's lifecycle rule to files — `clear_handle_row` (called from `boot::teardown_user_slot` before the slot's used-flag is released) now also clears the FILES row, so a reused ASID starts with no stale file, no leaked offset, no aliasable descriptor. Evidence — the `el0-u6bfile` fixture opens `HELLO.BIN`, reads its first bytes through the `File` capability and verifies they equal the kernel-planted on-disk prefix, then proves the CHECK denies both a **present File lacking `CAP_READ`** (the rights arm) and a **`Socket` carrying `CAP_READ`** (the kind arm) with `-EACCES` — witness `0x1F`; the launcher proves the file-row teardown-clear kernel-side: `:: U6b: real File handles — open+read via a File capability OK, no-CAP_READ -EACCES, wrong-kind -EACCES -> PASS ::`, with every M6b/M6d/M6e/M6f/M6g/U4/U5/U6 verdict **byte-identical** (the shared FAT mount does not regress M6g/U4) and CAPSTONE 6/6. **Honest scope:** read-only, flat root, one FAT volume — no write/create/delete, no seek, no directory ops, no second mount. Deferred: file writes/dirs/seek; real `Socket` fs/net syscalls (the fs twin); **UnaFS `owner`/`grants:*` checked on `SYS_OPEN`** (this arc's `SYS_OPEN` is the hook), riding the kernel UnaFS mount (K2/K3). Five-lens adversarial self-review (reserve/unwind + leaks, the SYS_READ CHECK for bypasses, offset/EOF + `read_at` correctness, teardown-clear + atomic ordering, regression + fixture ABI).
- [x] **Cross-process capability transfer — inbox-mediated `SYS_XFER`/`SYS_RECV` + sender-owned single-level revoke, the single-writer invariant preserved (U7, 2026-07-07, QEMU-verified; no metal expected — pure syscall logic; Campaign 1, Fable-executed).** The first CROSS-process operation on the object table — the desktop's delegation primitive (a shell handing an editor a console/file cap). **The invariant the design exists to preserve:** every `HANDLES[asid]` row keeps exactly ONE writer (U4's lock-free foundation). A sender therefore NEVER writes the recipient's row: **`SYS_XFER = 13`**`(dest, src, req_rights)` deposits an **attenuated** `(kind, target, rights)` descriptor into the recipient's per-ASID transfer **INBOX** (`XFER_SLOT_*`, `NXFER = 4` — the one deliberately cross-ASID surface, where every claim/consume/retract/teardown is a **tx-exact CAS** over a `0`/`RESERVING`/unique-transfer-id state word, with the publish-Release/observe-Acquire discipline of the handle sidecars), and **`SYS_RECV = 14`** pulls it into the CALLER's OWN row — the recipient writes only itself. Delegation is **owner-scoped**: `dest` must be a `Child` handle in the SENDER's own table (pid→ASID through a new `Proc.asid` field — no global process namespace); the source must carry **`CAP_GRANT`** (the same delegation right `sys_cap` GRANT demands); and **attenuation crosses processes intact** — `req & !src_rights != 0` ⇒ `-EACCES`, a transfer can never amplify. **Revocation is one-way too:** `SYS_CAP` **XREVOKE**`(transfer id)` flips a bit in the **sender-owned transfer RECORD** (`XFER_REC_*`, `MAX_XFERS = 8`, sender-only — `-EACCES` for anyone else; transfer ids are globally unique, so a stale id can never alias a new transfer); the recipient's next `handle_resolve` of the received cap reads that bit through a recipient-written sidecar (`HANDLE_XFER_REC`) and fails `Denied` — **stale at use** — while a still-PENDING revoked transfer is discarded at RECV, never delivered. Record lifetime = transfer lifetime: freed by exactly one of received-handle drop / pending-discard / sender retract / recipient teardown. A **post-check + retract** closes the deposit-vs-recipient-exit race from the sender's side (re-read the recipient's Proc entry after the deposit goes live; a tx-exact CAS undoes only our own deposit); the one residual TOCTOU (recipient exit + ASID recycle + new-tenant consume, all between the live-store and the post-check) is documented and accepted this arc — generation-tagged inboxes ride the revocation-tree arc. **Teardown** extends `clear_handle_row`: received-cap records are freed with their handles and the inbox swept (claim-to-clear per slot), so a recycled ASID inherits no stale pending cap and no dangling record. Payload kinds are **`Console`/`Socket` only** — `File` is refused (`-EACCES`: a file-id indexes the SENDER's per-ASID FILES row, so cross-ASID File transfer needs descriptor migration; deferred with writes/seek), `Child` is refused (delegating reap rights is a process-model question, not a transfer one). Evidence — a launcher-orchestrated two-fixture script (`el0-u7parent`/`el0-u7child`, cooperative SYS_YIELD polling with kernel-written GO words for deterministic sequencing): the parent's **over-rights transfer is refused**; t1 lands and — with the child provably parked pre-RECV — the launcher verifies the child's handle row is **byte-clear while the deposit sits in its inbox** (the single-writer witness); the child RECVs the cap into its own row and **prints through it** (`u7: child prints via the transferred cap` — a real console write authorized by a transferred capability); the parent revokes **after** the use, and the child's next write through the same handle is `-EACCES`; teardown leaves both handle rows, both inboxes, and the record ledger **fully clear**: `:: U7: cross-process transfer — SYS_XFER attenuated, child received + used the cap, revoke enforced, single-writer intact -> PASS ::`, with every prior M6b/M6d/M6e/M6f/M6g/U4/U5/U6/U6b verdict **byte-identical** (sorted scratch-worktree baseline diff — only the U7 lines and the binary-growth VBAR shift differ) and CAPSTONE 6/6, 0 faults. **Honest scope:** revoke is single-LEVEL — a cap the recipient **re-granted (a local mint does not carry the transfer reference) or re-transferred onward escapes it**; both are derived capabilities, and chasing them is exactly the revocation-TREE arc (per-cap derivation records + real `CAP_REVOKE` semantics). Deferred: revocation trees; the bandy Ring-3 delegation wrapper (a Ring-3 arc); arbitrary-recipient transfer; `File` payloads (descriptor migration); UnaFS `grants:*` on open (K2/K3). **Adversarial review (five lenses + refuter panels, 29 agents): 2 distinct CONFIRMED must-fixes, both fixed in-arc:** (1) the original XREVOKE was the arc's one non-tx-exact transition (find-then-store on a reusable record — a stale revoke could land on a freed-and-reclaimed record and kill an unrelated sender's fresh transfer, or mint one born-revoked); fixed by moving the revoked flag INTO the record's TX word (`XFER_REVOKED_BIT`, bit 63) so the revoke is a tx-exact CAS like every other transition, and the separate flag word (with its born-revoked window) no longer exists. (2) The demo's GO words read never-scrubbed recycled slot backings — U6b deterministically plants nonzero bytes at exactly +0x3000 — so the single-writer witness was a race, not a proof; fixed by full-window scrub in `u7_build` (the x86 U2.5 residue discipline). **Review notes closed in-arc:** post-revoke LAUNDERING (grant/re-transfer from a revoked received cap now `-EACCES` — only pre-revoke copies remain the tree arc's scope); XREVOKE authority no longer survives sender-ASID recycling (teardown DISOWNS the dying sender's records via an owner-exact CAS — orphaned transfers become irrevocable until the tree arc); `handle_row_is_clear` now covers the `HANDLE_XFER_REC` sidecar (the snapshot + teardown proofs are complete); the fixtures' name-routed exit/kill arms now mark the planted Proc entry EXITED (the pid→ASID map never vouches for a dead recipient). **Ledgered (not closed):** the RESERVING/post-check ordering argument leans on aarch64 STLR/LDAR RCsc codegen rather than the Rust memory model alone — revisit if the toolchain ever emits LDAPR here.
- [x] **Revocation TREES + generation-tagged inboxes — revoke chases derived capabilities, real `CAP_REVOKE` semantics, the sys_xfer TOCTOU closed (U8, 2026-07-07, QEMU-verified; no metal expected — pure syscall logic; Campaign 2, Fable-executed).** Closes both escapes U7's entry documents. **(1) The derivation ledger.** A bounded static ledger of derivation NODES (`MAX_DERIV = 16` — the U7 static-atomic-array discipline: no heap, state-exact CAS transitions over a `0`/`RESERVING`/unique-node-id word, Release-publish/Acquire-read) records an edge **child → parent at every derive**: `sys_cap_grant` (a local mint) and `SYS_XFER`/`SYS_RECV` (a delivered transfer — the node rides the inbox slot and is remembered by the sender-owned record). Handles installed by spawn/open/endow are ROOTS; a root gets its node LAZILY at its first use as a grant/transfer source. **Revoke = mark one node; staleness is discovered at USE:** `handle_resolve` walks child → root (bounded by the ledger size; cycles impossible — a parent is claimed strictly before its child and edges never change) and fails `Denied` if ANY ancestor is revoked. This keeps U7's load-bearing invariant — **no revoke path ever writes another ASID's row** — with revoke O(1) and resolve paying the bounded walk. **`CAP_REVOKE` is real now:** `SYS_CAP` REVOKE of a handle CARRYING it marks its node before the row clear, killing the whole derivation subtree — every re-grant and every re-transfer whose chain passes through it, however deep, exactly once, idempotently (a second revoke of the same index is an honest `-ECHILD`); WITHOUT the right the drop keeps U5's ownership semantics (row-local; derived copies survive). **XREVOKE composes:** the transfer record captures its derivation node (+ the node's publish-time id — an ABA guard: a node dropped-and-reclaimed before the revoke lands no longer matches and is left alone; it had no children, so nothing escapes), and a won tx-exact XREVOKE marks it — so revoking the ROOT transfer kills the received cap AND everything the recipient re-granted or re-transferred onward (the U7 escape, closed); post-revoke laundering stays refused, now tree-deep (grant/xfer check ancestor staleness, not just the direct record). **Node lifetime (the documented choice):** a node frees when its owning handle/pending-slot drops AND it has no live children; dropped-with-children = a **TOMBSTONE** (still walkable, bit intact) until the subtree drains, then the free CASCADES up through drained tombstoned ancestors — every free is arbitrated by a state-exact CAS on the id word (racing freers resolve to one winner), and every walkable node is PINNED (a live handle pins its own node; a live child holds `KIDS > 0` on its parent), so no walk reads a reclaimed node. Ledger exhaustion is `-EAGAIN` at mint/transfer time with reserve/unwind on every failure path (the U4 discipline). **(2) Generation-tagged inboxes.** A per-ASID generation word (`ASID_GEN`) bumps at the TOP of `clear_handle_row` — strictly before the U7 inbox sweep — so it covers even a deposit that lands after the sweep passed its slot. `sys_xfer` stamps the recipient's current generation into the deposit; `sys_recv` delivers only on an exact match (a stale stamp = a deposit aimed at a PREVIOUS tenant: discarded, record freed, node dropped, never delivered); the sender's post-check re-reads the word and retracts on any change. The U7-documented residual TOCTOU (recipient exit + ASID recycle + new-tenant consume inside the deposit-live→post-check window) is now **structurally closed from both sides**. Evidence — the `el0-u8tree` fixture (single slot, register-only; witness `0xF`): a grant→re-grant chain carries authority pre-revoke; the parent revoke (with `CAP_REVOKE`) returns 0, its double revoke exactly `-ECHILD`, and BOTH descendant copies are `-EACCES` at their next use; a right-less revoke stays local (its derived copy still writes) with its own double-revoke errno. `u8_kernel_check` drives the REAL `sys_xfer_from`/`sys_recv_for` code paths (the syscall bodies, parameterized on ASID — no duplicate logic) over scratch ASID rows: an S→R1→R2 re-transfer chain where the root XREVOKE makes R1 AND R2 stale + refuses the laundering mint; a deposit stamped before the generation bump is never consumable by the recycled ASID's tenant (its record frees; a late XREVOKE gets `-ENOENT`); and afterwards the handle rows, inboxes, transfer records AND the derivation ledger are all provably clear (the teardown/tombstone-cascade proof): `:: U8: revocation trees — parent revoke kills re-grant + re-transfer, generation-tagged inbox, ledger clean -> PASS ::`, with every prior verdict **byte-identical** (sorted scratch-worktree baseline diff vs `225ae48` — only the U8 lines and the binary-growth VBAR/ELR shift differ), 19/19 prior PASS + U8, CAPSTONE 6/6, 0 faults; `./arroyo test-arm 22` byte-identical; `./arroyo check` both arches, **zero x86 files touched**. **Of U7's deferred list this closes exactly: revocation trees, generation-tagged inboxes, real `CAP_REVOKE` semantics.** **Still deferred:** the bandy Ring-3 delegation wrapper (a Ring-3 arc); arbitrary-recipient transfer; `File` payloads (descriptor migration, with writes/seek/dirs); real Socket net syscalls; UnaFS `grants:*` on `SYS_OPEN` (K2/K3); the x86 U7x/U8x twins. The U7 RCsc-codegen footnote is respected, not fixed: the new ledger keeps the same STLR/LDAR ordering discipline. **Post-merge hardening (`deriv_drop` SeqCst fence, both arches):** the tombstone/free handshake — DROPPED store + KIDS load (parent-drop side), KIDS fetch_sub + DROPPED load (child-drop side) — was a store-buffering/Dekker pair under Release/Acquire; a concurrent parent-vs-child drop could store-buffer both sides into declining the free, leaking a ledger node (fail-closed → `-EAGAIN`, no UAF). The four ops are promoted to `SeqCst` (one total order forbids the double-stale outcome; the both-free case stays arbitrated by the `DERIV_ID` CAS). Surfaced by the U8x pre-merge concurrency lens.
- [x] **File WRITES + SEEK — `CAP_WRITE` on a `File` made real: EMMC2 sector write, `fat::write_at` (in-place), `SYS_SEEK`, a File+`CAP_WRITE`-routed `sys_write` (U9, 2026-07-07, QEMU-verified + ✅ METAL-CONFIRMED on the real Pi 4 — the fixture opened `SCRATCH.BIN` RW off the real EMMC2 card `@0xfe340000 15193 MiB CSD v2`, overwrote it IN PLACE with a polled CMD24, read it back through the same cap, and the launcher's raw re-read confirmed the on-disk sector CHANGED while the file size did NOT; all three `-EACCES` denials held; full battery green — 15/15 PASS incl. U9, CAPSTONE 6/6, 0 unexpected faults. **The first write the EMMC2 driver has ever issued to a card — the metal-risk write path is now proven on silicon.**).** Gives U6b's read-only `File`+`CAP_READ` its write half — the last rung before UnaFS `grants:*` on open. **The mechanism, bottom-up.** (1) **EMMC2 sector write** — `emmc2::write_block_512` is the exact mirror of the polled CMD17 read path with three deltas: `WRITE_SINGLE_BLOCK` (`cmd(24)`, host→card so `CMD_DAT_DIR_READ` is OMITTED), the Buffer-**Write**-Ready status bit (`INT_WRITE_RDY = 1<<4`, distinct from the read path's bit 5), and a FIFO that PUSHES 128 LE words (a short buffer zero-pads to the full 512-byte block — the controller demands exactly one sector or it stalls); same `send_command`, same bounded CNTPCT deadlines, same W1C/error discipline. (2) **The block seam** — `drivers::block::write_block` now routes the SD backend to `write_block_512` (it previously refused all SD writes); the change sits inside the existing `#[cfg(aarch64, baremetal)]` arm, so the x86 xHCI write path is **byte-identical**. (3) **`fat::write_at`** — the write twin of `read_at`, structurally identical: it walks the file's existing cluster chain, skips to `start`, and **read-modify-writes** only the touched data sectors. It is BOUNDED BY CONSTRUCTION — clamped to `min(size, start+len)` so it **never grows a file** (a write at/after EOF is a clean 0-byte no-op), it visits only clusters already in the chain so it **never allocates/frees a cluster or writes a FAT entry**, and it **never touches a directory** (the on-disk size + chain head are unchanged). A chain that ends before `size` yields a short write, never a write outside the chain. The module's read-only-by-design invariant is narrowed honestly (its doc now names `write_at` as the one mutating entry point, still structurally unable to corrupt volume layout or any file but the one addressed, and only within its current bytes). (4) **`SYS_SEEK = 15`**`(handle, offset)` — absolute seek: the CHECK requires a `File` carrying ANY of `CAP_READ|CAP_WRITE` (expressed as resolve-for-READ-else-WRITE over `handle_resolve`'s all-bits semantics), an offset PAST `size` is `-EINVAL` (seeking TO `size`, the EOF position, is legal), and it sets the U6b `FILE_OFFSET` sidecar (now settable, no longer forward-only). (5) **Routed writes** — `SYS_OPEN` gains a **mode bit** in `a2` (`0` = RO/`CAP_READ` as before; `1` = RW/`CAP_READ|CAP_WRITE`); `sys_write` is now KIND-DISPATCHED at its single `handle_resolve(asid, fd, CAP_WRITE)` CHECK — a `Console` streams to serial (byte-identical), a `File` with `CAP_WRITE` overwrites in place at the descriptor offset via `sys_write_file`→`fat::write_at` (advancing the offset by the count written), the exact write twin of `sys_read`'s clamp/validate/offset discipline (a bad buffer is `-EFAULT` with no I/O and no offset move). **The CHECK is the enforcement point, and it inherits U7/U8 for free:** because the derivation/revocation walk lives INSIDE `handle_resolve`, a File-write cap whose ancestor was revoked is `-EACCES` at the write with no new code. **Three denials proven:** a **File opened RO** written to (missing `CAP_WRITE` — the rights arm), a **non-File handle** carrying `CAP_WRITE` (a `Socket` — the kind arm), and a **U8-revoked File-write cap** all return `-EACCES`. Evidence — the `el0-u9write` fixture (single slot, register-only, witness `0x1F`) opens a DEDICATED scratch file (`SCRATCH.BIN`, 1 KiB of `0xEE` planted by the launcher's FAT image — never `HELLO.BIN`, which other fixtures load as EL0 code) RW, seeks to a partial-sector offset, overwrites a 16-byte sentinel IN PLACE, seeks back and reads it through the SAME cap (the read-back witness), and proves the RO-write + wrong-kind denials; the launcher folds the kernel-side proofs — it re-reads the raw on-disk sector from a fresh mount and confirms it **changed** (to the sentinel, differing from the pre-image) while the directory **size did NOT** (in-place, never grew), and stages the U8-revoked-File-write denial over a scratch ASID (a File+`CAP_WRITE` cap whose granted ancestor is revoked; `sys_write`'s only gate is `handle_resolve`, so proving that resolve `Err`s IS the `-EACCES`): `:: U9: real File writes — open-RW+seek+write+readback OK, RO-write/wrong-kind/revoked-cap all -EACCES, on-disk sector changed + size unchanged -> PASS ::`, with every prior verdict **byte-identical** (sorted scratch-worktree baseline diff vs `e8db35c` — only the U9 lines + the binary-growth VBAR/ELR shift differ), all prior PASS + U9, CAPSTONE 6/6, 0 faults; `./arroyo test-arm 22` byte-identical (the module is baremetal-gated); `./arroyo check` both arches, **zero x86 files touched**. **Honest scope — IN-PLACE OVERWRITE ONLY.** Deferred (each needs an allocation-policy design pass): file GROWTH / cluster allocation / create / delete / truncate; directory mutation; write-back caching (every write is a synchronous RMW straight to the card); and **UnaFS `owner`/`grants:*` enforcement on `SYS_OPEN`** (this arc's RW mode bit is the local precursor; the real ACL check rides the kernel UnaFS mount, K2/K3). **Lane note:** `fat.rs` and `drivers/block.rs` are shared cross-arch files and the FAT image builder is shared infra — the integrator seat widened the pi4 lane for this arc after the executor stopped and reported; the `fat::write_at` addition is purely additive (x86 never calls it → x86 byte-identical) and the block seam + image plant are cfg-/pi-scoped. Adversarial multi-lens review (0 findings): EMMC2 CMD24 polling discipline, `write_at` RMW + no-growth bound, the three-denial CHECK enforcement, and byte-identical Console/prior-PASS. **✅ METAL-CONFIRMED on the real Pi 4 (2026-07-07): the CMD24 buffer-write-ready + FIFO-push + transfer-complete/DAT0-programming-busy sequence works on real silicon — the write landed on the real card and a fresh-mount re-read proved it, with every prior verdict + CAPSTONE 6/6 byte-consistent with QEMU. QEMU's generic-sdhci had modeled the PIO write FIFO but not the silicon timing; that gap is now closed.**
- [x] **UnaFS owner/grants — the by-NAME namespace ACL, enforced at `SYS_OPEN` (U6, 2026-07-08, QEMU-verified; ✅ METAL-CONFIRMED 2026-07-10 attended bench — pure syscall + in-kernel metadata, rides the next Pi 4 boundary).** Closes the LAST documented capability gap: `SYS_OPEN`/`O_CREAT`/`SYS_UNLINK` were gated on the HANDLE capability, but the by-NAME namespace itself was not ACL'd — any process could open/create/unlink any name. **Secure-by-DEFAULT (owned-by-default):** an `O_CREAT` of a NEW name records the creating principal as the file's OWNER (the file is PRIVATE); the new `O_PUBLIC` mode bit (bit2) opts a create OUT to world-access. An open of an EXISTING owned file is admitted only for the owner or a principal the owner GRANTED; everyone else is `-EACCES`. A file with NO owner row (pre-existing / host-created / `O_PUBLIC`) is PUBLIC — byte-identical to the pre-U6 behaviour (the whole prior regression is preserved: the two cross-process U11 fixtures tag their shared file `O_PUBLIC`, one mode bit each). **M1 — the `SYS_OPEN` ACL:** an in-kernel `OWNED_FILES` table (a bounded `SpinMutex<[_;16]>` keyed by the FAT file-identity `(dir_lba, dir_off)`, mirroring `OPEN_FILES`; owner + up to 4 grants, ALL fenced by the `(ASID, ASID_GEN)` incarnation — the recycle fence the file-id/xfer/derivation machinery already uses) checked in `sys_open`'s "nothing-claimed-yet" window (a denial is a clean `-EACCES`); a private create that cannot record an owner FAILS CLOSED (`-ENOSPC`, undoing the fresh dir entry). The row is cleared at unlink (FAT may recycle the slot) and at OWNER teardown (the file reverts to PUBLIC — no persistent principal keeps owning it — which also keeps the bounded table self-cleaning); its lock is IRQ-masked via the M2b `IrqGuard` (taken in BOTH syscall and teardown contexts). **M2 — `SYS_FGRANT` delegation:** the owner grants (a `CAP_READ|CAP_WRITE` subset) or revokes (`rights = 0`) access to another principal named OWNER-SCOPED by a `Child` handle it holds (the `SYS_XFER` idiom — no raw pid/ASID from EL0); ownership is checked BEFORE the grantee handle is resolved (a non-owner is `-EACCES` regardless of its argument). The grant is an ACL edge on the FILE — nothing is delivered to the grantee's table; it opens the name and the ACL admits it, and a handle it ALREADY holds survives a revoke (the ACL gates ACQUISITION, not held caps). **F1 — delete is OWNER-only (adversarial-review fix):** `SYS_UNLINK` is now gated on OWNERSHIP, not merely the handle's `CAP_WRITE` — a content `CAP_WRITE` grantee (which legitimately opened the file RW) must NOT be able to `unlink` it, else it could `unlink` + `O_CREAT` the name to STEAL ownership and lock the real owner out; a PUBLIC file (no owner row) keeps the prior `CAP_WRITE`-gated unlink. This is the in-kernel enforcement SEAM the on-disk UnaFS `owner`/`grants:*` attributes will feed once K2/K3/K4 land (no on-disk owner format exists yet, and `fat.rs` is shared/off-lane). Verdict `:: U6-grants: … non-owner -EACCES, owner grant admits R|W, grantee unlink -EACCES (delete owner-only), non-owner grant -EACCES, revoke re-denies -> PASS ::` (23 PASS, CAPSTONE 6/6, `test-arm` MISSION SUCCESS). **Lane:** additive on `arch/aarch64/syscall.rs`; zero x86 files touched. **Flagged, deferred (not a defect — the honest scope):** the owner/grants store is in-kernel, VOLATILE, boot-scoped (there are no persistent principals yet — the model's own documented gap), so ownership is meaningful only within a boot and only while the owner process lives; a persistent form (and retroactively assigning owners to pre-existing/host files) awaits the on-disk UnaFS attributes (K2/K3/K4). The x86 twin (U6x) and a `SYS_FGRANT` that endows the grantee with a directly-usable handle (vs. an open-by-name) remain future. 🔬 QEMU-green, metal-pending.
- [x] **Teardown-last-close REAPER — a deferred-free queue + a kernel reaper task closes M2a's honest-scope leak (U11 M2b / U12b, 2026-07-08, QEMU-verified; ✅ METAL-CONFIRMED 2026-07-10 attended bench — the reaper freed a real teardown-orphaned chain on the card).** Closes the ONE honest-scope gap M2a left open. M2a proved cross-process unlink-defers-free for the EXPLICIT-close path (the chain frees at the last `SYS_CLOSE`, in syscall context), but a program that EXITS holding the last cross-process open of an `unlink_pending` file cannot free its chain at teardown — that runs **IRQ-masked, on the dying task's own kernel stack, TTBR0 already on the boot root, immediately before `switch_context`**, where a multi-sector polled SD write is illegal. M2a therefore LOGGED that as a transient lost-cluster leak (benign — refcount stays exact, the chain stays allocated and is never handed out, NO UAF/disclosure — but not reclaimed until reboot). **M2b actually frees it, in a block-I/O-legal context, without weakening the teardown discipline.** **(1) An I/O-free deferred-free QUEUE** — `DEFERRED_FREE`, a bounded (`NDEFERFREE = 16`) fixed ring of chain heads guarded by its OWN `SpinMutex` (SEPARATE from `OPEN_FILES`, so the teardown push never contends with live open/close on the refcount table). `clear_files_row`'s last-close-of-`unlink_pending` branch now PUSHES the chain head (`deferred_free_push` — lock + array write + unlock: **I/O-free, the SAFE twin of M2a's teardown decrement**) instead of logging the leak. A FULL queue degrades honestly to the M2a behavior (log the leak + leave the clusters allocated) — the push **never blocks, spins, or does I/O**. **(2) A kernel REAPER task** — `orphan_reaper`, a forever service task spawned at BOOT via the EXISTING `sched::spawn(name, fn, arg, cpu)` service-task API (in `main.rs`'s aarch64-baremetal service block, alongside `input`/`render`; **NO `sched.rs` change — the scheduler hook the M1/M2 notes feared turned out to be unnecessary**, and a lazily-spawned reaper is impossible anyway: a spawn allocates a heap `Box<Task>` and takes `RUN_QUEUES`, both illegal from the IRQ-masked teardown push, so the reaper must pre-exist). It drains the queue: **pop one head UNDER the lock, RELEASE the lock, THEN `free_orphan_chain`** (mount + all-FAT-copies free — block I/O, legal here: EL1, IRQs enabled, its own stack, never teardown context); it `yield_now`s when empty so it never hogs its core under QEMU's cooperative scheduler, and is co-located with the demo VERDICT core so the launcher's bounded `yield_now` poll cedes it CPU to drain deterministically. **The invariants the design holds:** a teardown-orphaned chain reaches the queue ONLY via `openfile_decref_at` returning `Some(fc)` (last-close-of-pending, the row already cleared to EMPTY), so it is queued ONCE and freed ONCE by the reaper; the explicit-close path frees inline and NEVER queues (no chain both freed inline and queued); no I/O runs under any lock and the queue lock is never held across `mount()` (so a teardown push can never spin IRQ-masked on it — bounded); SMP-safe (a teardown on core X pushes, the reaper on core Y drains — one `SpinMutex`, no torn reads, no lost/duplicated entries); M2a's explicit-close + same-process paths stay byte-identical PASS. Evidence — a two-process fixture (`el0-u11reap-a`/`-b`, the u11defer choreography): A creates + writes + holds `DEFER2.BIN` open; B opens + `SYS_UNLINK`s it (deferred — A holds it); A reads its ORIGINAL bytes back (chain alive), then **EXITS WITHOUT CLOSING** — so teardown is the last close. The launcher proves the same three checkpoints as u11defer, but CHECKPOINT-3 is a bounded `yield_now` poll of the FAT until the REAPER has freed the chain (all FAT copies) + it is re-allocatable (`first_free == f0`); the serial shows `U11-defer: reaper freed teardown-orphaned chain @cluster N` and the M2a `"teardown … left allocated (leak)"` line does NOT appear for `DEFER2.BIN` (reaped, not leaked): `:: U11-reap: teardown-last-close reaper — A exits holding the unlinked file open, its chain freed by the reaper (all FAT copies) + re-allocatable, no teardown leak -> PASS ::`; **22 PASS** (21 prior byte-identical + U11-reap — sorted scratch-worktree baseline diff vs `d57520f`, a single appended line), **CAPSTONE 6/6**, only the 3 expected M6b kills, 0 unexpected faults. `./arroyo test-arm 22` green (baremetal-gated); `./arroyo check` both arches, `./arroyo kernel8` compiles; **zero x86 files**. **Lane:** aarch64 `syscall.rs` (the queue + reaper + the `clear_files_row` push + the reap fixture/launcher) + the aarch64-baremetal service-spawn block of `main.rs` (additive, cfg-scoped) + docs; zero x86 files; `SpinMutex` reused from `sched.rs`; **NO `sched.rs`/`fat.rs` change** (the reaper calls M2a's existing `free_chain`). 🔬 Metal-pending (QEMU proves the reaper in full; rides the next Pi 4 boundary alongside U10/U11-M1/M2). **Seat coalesce-review fix (2-lens: double-free/leak · SMP/lock discipline, + refuters):** the queue machinery is freed-exactly-once and torn-read-free, but one CONFIRMED should-fix — `DEFERRED_FREE` is a bare `spin::Mutex` held PREEMPTIBLY by the reaper task (I-unmasked, `SCHED_ACTIVE` on) yet taken IRQ-MASKED by the teardown push, so a timer preempt of the reaper mid-hold + a same-core teardown push spinning on it deadlocks that core (dormant in a healthy ≥2-AP boot where reaper and EL0 fixtures sit on distinct cores; **live in the single-AP fallback** the pi4's 3/4-core boot variance can produce). Fixed in-lane by a local RAII `IrqGuard` masking IRQs across both `deferred_free_push`/`pop` (a proper IRQ-safe spinlock at any core count) — **still no `sched.rs` change**; re-gated 22 PASS byte-transparent. **Flagged, deferred to a future SMP-hardening arc (out of this lane):** the reaper is a permanent second cross-core FAT writer, and `fat::set_fat_entry`'s read-modify-write has no FS-mutation lock spanning the sector RMW — a latent lost-update / **cluster-aliasing (information-disclosure)** race should UnaOS ever run uncoordinated concurrent FAT writers (not live today: the reaper's free is await-verdict-sequenced strictly after all writers exit). Its fix touches `fat.rs`/the block layer and also covers the pre-existing U9/U10 multi-core-syscall FAT-write path.
- [x] **Cross-process unlink-defers-free — a GLOBAL open-file refcount + deferred chain-free (U11 M2 / U12, 2026-07-08, QEMU-verified; ✅ METAL-CONFIRMED 2026-07-10 attended bench).** Closes the SECOND (and last) of U10's two review notes — **cross-process unlink-while-open** (POSIX unlink-defers-free). U10 freed a file's cluster chain the instant `sys_unlink` ran; if ANOTHER process held the file open, its live descriptor kept pointing at a chain a later `create_in_root` + `write_grow` could first-fit-reuse — a cross-file read/write + **information disclosure**. U10 was the first arc to free a chain under a live descriptor, so this is U10-new. **The fix defers the chain-free to the file's LAST close across ALL processes.** (1) **A global (cross-ASID) open-file refcount table** — `OPEN_FILES`, a fixed `NOPENFILE = 16`-row table, one row shared by every open of a file across every ASID (`refcount` = its live descriptor count), guarded by a single **`SpinMutex`** (the scheduler's low-level spinlock reused from `sched.rs`, NOT the sleeping `Mutex`, which YIELDS — illegal in the IRQ-masked teardown decrement path). The lock is held ONLY across the short table mutation (±refcount, read the stashed chain head); the `mount()`/`free_chain` block I/O is ALWAYS the caller's, after the lock drops — **never under the lock, never in teardown context.** (2) **`sys_open` increments, `files_free` decrements** — the increment is done BEFORE `files_alloc`, so every increment pairs with EXACTLY one decrement (the descriptor's `files_free` at close/teardown, or a one-line `openfile_decref_at` on the `files_alloc`-full unwind), with no path where a decrement lands on a row this open never incremented (which the after-`files_alloc` ordering could race a concurrent same-file open into, on SMP); a full table on a NEW file identity is a clean **`-ENFILE` (`-23`)** with reserve/unwind claim-last on every failure path. (3) **`sys_unlink` marks + defers** — `fat::mark_dir_deleted` writes the directory entry `0xE5` FIRST (the NAME is gone immediately — a re-open is `-ENOENT`; a failure here changes nothing → `-EIO`), then the refcount row is marked `unlink_pending` + the chain head stashed, then `files_free_by_dir` drops all the caller's descriptors naming the file (each decrementing). If the caller is the SOLE opener the last decrement reaches 0 and frees the chain NOW (all FAT copies, in syscall context) — **byte-identical to U10's immediate delete, so `el0-u10delete` stays byte-identical PASS**; if ANOTHER process still holds it open the refcount stays > 0 and NOTHING is freed — that process keeps reading its ORIGINAL bytes until its last close. (4) **`sys_close` frees at the last close** — the decrement that drops an `unlink_pending` file's refcount to 0 returns the stashed chain head, freed via `fat::free_chain` (all FAT copies) AFTER the lock drops (block I/O legal in syscall context). (5) **`fat::delete_located` split** into `mark_dir_deleted` (the `0xE5` write) + `free_chain` (the all-FAT-copies chain free), reusing the existing `set_fat_entry`/`chain_clusters` writers; `delete_located` remains their pre-validated composition for the immediate path (the U10 "bad chain → nothing changed" contract holds byte-for-byte, and the dir-`0xE5`-FIRST-then-free-chain order is preserved). **Every U10 FAT-safety invariant holds when the deferred free finally runs** (all-FAT-copies, dir-first order, bounded free-search); the deferral loses no clusters and frees none twice (the row is cleared to EMPTY the instant refcount hits 0, so a second decrement can't re-return it) — on the same-process, cross-process, AND FAT-slot-recycle sweep paths (the last of these fixed post-landing by the seat coalesce review; see the review note below). Evidence — a **two-process** fixture (`el0-u11defer-a`/`-b`, the U7 GO-word choreography with the launcher as the single sequencer): A creates + writes + reads `DEFER.BIN`; B opens + `SYS_UNLINK`s it while A holds it open; the launcher re-mounts the FAT at THREE checkpoints — after B's unlink the NAME is gone but the chain (cluster `f0`) is STILL allocated in all FAT copies; A seeks + reads its ORIGINAL 16 bytes back (the deferred chain is alive); after A's last `SYS_CLOSE` the chain is FREED in all FAT copies + re-allocatable (`first_free == f0`). `:: U11-defer: cross-process unlink-defers-free — name gone at unlink, reader keeps original bytes, chain freed (all FAT copies) + re-allocatable at last close -> PASS ::`; **20 PASS** (19 prior byte-identical + U11-defer — sorted scratch-worktree baseline diff vs `d0d12ef`, only the appended line + the binary-growth VBAR/ELR diagnostic shift differ), **CAPSTONE 6/6**, only the 3 expected M6b kills, 0 unexpected faults, no leak lines. `./arroyo test-arm 22` green (baremetal-gated); `./arroyo check` both arches, **zero x86 files** (the `fat.rs` split is additive — x86 never calls the new writers). **Adversarial 3-lens review (refcount pairing / double-free+leak+FAT-safety / SMP+teardown lock discipline) with per-finding refuters: lock-across-I/O, teardown-I/O, SMP incref ordering, and deadlock lenses all clean; 1 real should-fix caught + fixed in-arc** — the refcount row was originally keyed only on `(dir_lba, dir_off)`, a directory SLOT which FAT recycles for a new file the moment a delete `0xE5`'s it, so a file created in an unlinked-but-still-open file's reused slot could conflate the two files' refcount + deferred free (a lost-cluster leak / mis-triggered free — **not** a UAF/disclosure: refcount stayed exact, so refcount-0 always means no live reader). This is exactly the M1 descriptor-slot-reuse hole ONE LEVEL UP, which generation tags do not cover. Fixed by (a) excluding `unlink_pending` rows from the open-time join (a `0xE5`'d file cannot be re-opened by name, so a key match on a pending row is a DIFFERENT file that reused the slot → claim a fresh row) and (b) recording each descriptor's row INDEX in a `FILE_OPENROW` sidecar so every decrement/mark hits that EXACT row, never a re-searchable key; a targeted re-verify confirmed the fix closes it with no new leak/double-free/stale-index path. **A subsequent SEAT COALESCE-REVIEW caught a SECOND, distinct slot-recycle bug on the EXPLICIT `sys_unlink` SWEEP side (refuted 0/3), fixed in a follow-up commit:** the above fixed the incref/JOIN side, but `files_free_by_dir` still matched descriptors by the recyclable `(dir_lba, dir_off)` — so when ONE process held an unlinked file F open AND created a new file G in F's recycled `0xE5` slot, a `SYS_UNLINK` of G swept BOTH descriptors and both reached `refcount == 0`, but the loop kept only the LAST orphan chain head (`orphan = Some(fc)` overwriting) → the earlier chain leaked. A benign lost-cluster leak on the EXPLICIT path (refcount stayed exact; no UAF/double-free/disclosure), DISTINCT from the teardown-only honest-scope leak below (that one is M2b's job). Fixed by freeing EVERY orphan head the sweep produces — `files_free_by_dir` is `sys_unlink`-only (a block-I/O-legal context; teardown uses `files_free` directly + logs), so it frees each head inline via `free_orphan_chain`, with the `OPEN_FILES` lock never held across the I/O. Proven by a deterministic kernel-side check (`u11defer_check_double_orphan`, the `u11_check_gen_rebind` style): it physically recycles a slot (create F, `0xE5`, create G — asserting G reused F's EXACT slot), reproduces the two-descriptor/two-pending-row state on a scratch ASID, runs the real `files_free_by_dir` sweep, and a fresh mount confirms BOTH chains free in all FAT copies — `:: U11-reuse: sys_unlink slot-recycle — two files sharing a recycled dir slot BOTH free their chains (all FAT copies) -> PASS ::`. Now **21 PASS** (the prior 20 byte-identical + U11-reuse), CAPSTONE 6/6, `./arroyo check` both arches, zero x86 files. **Honest scope — M2a (explicit-close path) landed; the teardown-last-close gap is now CLOSED by M2b (above).** The teardown DECREMENT is done (a short, I/O-free `SpinMutex` critical section, safe IRQ-masked), but a program that EXITS holding the LAST `unlink_pending` open cannot free its chain in teardown (a multi-sector polled SD write on the IRQ-masked, about-to-be-freed teardown stack is unsafe), so M2a LOGGED it as a transient lost-cluster leak; **M2b now reaps it** — `clear_files_row` pushes the chain head onto an I/O-free `SpinMutex` deferred-free queue and an `orphan_reaper` kernel service task frees it in a block-I/O-legal context (it needed NO `sched.rs` hook — the existing `sched::spawn` service-task API sufficed). **Lane:** aarch64 `syscall.rs` + `fs/fat.rs` (the split — additive) + docs; zero x86 files; `SpinMutex` reused (not reimplemented); no `sched.rs` change. 🔬 Metal-pending (the refcount + deferred-free are QEMU-provable in full).
- [x] **Open-file LIFECYCLE — `SYS_CLOSE` + GENERATION-tagged file-ids (U11 M1, 2026-07-08, QEMU-verified; ✅ METAL-CONFIRMED 2026-07-10 attended bench).** Gives a `File` descriptor a real end-of-life and a stable identity across slot reuse, closing the FIRST of U10's two review notes: the **same-process sibling-handle rebind on slot reuse**. The hole: U10's per-task FILES row was a bare `+1`-biased slot index with NO generation, so after `files_free_by_dir` freed a slot (an unlink invalidating a multiply-opened file's descriptors), a later `sys_open` first-fit-REUSED that slot and a lingering sibling handle — still `KIND_FILE`, still `CAP_WRITE`, still carrying the old file-id — resolved to the SAME slot and silently REBOUND to the different file (U10's immediate `FILE_USED=false` guard lapsed the moment the slot went live again). **The fix, standard slotmap/generation-index:** a per-slot `FILE_GEN[asid][idx]` counter; the `File` handle's value word now packs `(gen << 32) | (idx + 1)`; a SINGLE seam `file_desc_validate(asid, file_id) -> Option<idx>` decodes it and enforces range + `FILE_USED` + **gen == the slot's current gen**, and EVERY File consumer (`sys_read`/`sys_write_file`/`sys_seek`/`sys_unlink`/`sys_close`) funnels through it (replacing five open-coded `idx = id-1; if idx>=NFILE || !FILE_USED` checks with one) — so a stale handle to a reused slot is `-EACCES` (a gen mismatch, EVEN when `FILE_USED` is true again for the different file), never a rebind. `files_free` (the one free primitive — `SYS_CLOSE`, `sys_unlink`'s `files_free_by_dir`, and `sys_open`'s reserve/unwind all route through it) and `clear_files_row` (teardown) **bump the gen on every free**, so the very next reuse lands on a fresh generation. **`SYS_CLOSE = 17`** gives the descriptor its end-of-life: it resolves the handle for NO right (`handle_resolve(asid, handle, 0)` — a close is always permitted on a handle you hold), frees the descriptor slot (bumping the gen → any lingering sibling to that slot goes stale) and clears the handle → `0`; a `Console`/`Socket`/`Child` kind is `-EINVAL` with the object table UNTOUCHED (not closeable this arc — never corrupted); an unresolvable / already-closed / stale-slot handle is `-EBADF` (`-9`) — so a double-close returns cleanly and a use-after-close is denied. **`handle_resolve`'s contract is UNCHANGED** — it still returns `File(raw value word)`, so the scaffold File handles resolved directly by the U6 (`File(0x100)`) and U9 (`File(fid+1)`, derived caps) kernel checks are byte-identically unaffected; the gen validation lives one layer out, in `file_desc_validate`, which only the real File-I/O consumers call. And because a FRESH open at generation 0 packs to exactly `idx + 1` (the pre-U11 bare file-id), nothing observable changes for programs that never `SYS_CLOSE`: open→read/write→(implicit teardown) is byte-identical to U10, and teardown drops the descriptor exactly as a close would. **Evidence** — one register-only fixture (`el0-u11close`, witness `0x1F`) drives the ops through the syscall surface: create + grow-write `A11.BIN`, open it a SECOND time (a sibling descriptor), `SYS_UNLINK` via the first handle (frees BOTH A11 descriptors + clears the first, leaving the sibling lingering on a freed slot), REUSE the freed slots by opening + writing a DIFFERENT file `B11.BIN`, then a read through the STALE sibling is `-EACCES` (the slot is LIVE again for B11, so the only possible denial is the stale generation — NOT a rebind onto B11's bytes), and `SYS_CLOSE` → `0` / double-close → `-EBADF` / close→re-open→read round-trips B11's content. A kernel-side `u11_check_gen_rebind` gives the mechanistic proof in isolation on a scratch ASID row (claim slot → mint file-id → free (bump gen) → first-fit RE-claim the same slot → the OLD file-id is rejected while the slot is `FILE_USED`-live, the FRESH one resolves), and a fresh `mount()` confirms `A11.BIN` GONE + `B11.BIN` present with its written content. `:: U11: open-file lifecycle … -> PASS ::`; **19 PASS** (18 prior + U11), **CAPSTONE 6/6**, only the 3 expected M6b kills, 0 unexpected faults; every prior verdict **byte-identical** (sorted scratch-worktree baseline diff vs `ddefc40` — a single appended U11 line, no VBAR/USER_REGION shift observable in any verdict). `./arroyo test-arm 22` green (baremetal-gated); `./arroyo check` both arches, **zero x86 files** (the design is the pi4 lead; its x86 twin U11x is a later rmbp arc). Adversarial 3-lens review (correctness / byte-identical-regression / SMP-ordering) with per-finding refuters. **Honest scope — M1 (generation tags + `SYS_CLOSE`) lands and closes hole 1.** The SECOND U10 note — **cross-process unlink-while-open** (a file open in ANOTHER process when unlinked leaves that process's descriptor stale; POSIX unlink-defers-free) — **stays DEFERRED to the next arc (U11 M2 / U12)**, a global cross-ASID `(dir_lba, dir_off)`-keyed open-file refcount + a `fat::delete_located` split (mark-`0xE5`-only / free-chain-only). It was NOT half-landed here by design: its key constraint is that a program which exits WITHOUT closing must still trigger the deferred chain-free at TEARDOWN, and teardown (`exit` → `teardown_user_slot` → `clear_handle_row`) runs **IRQ-masked, on the dying task's own kernel stack, with TTBR0 already repointed to the boot root, immediately before the context-switch away** — doing multi-sector polled SD I/O there (plus a new global Mutex-guarded refcount table needing its own SMP-safety proof) is arc-deep and most likely wants a reaper rather than inline teardown I/O. The `O_CREAT` ambient-namespace gap (open/create/unlink are handle-cap-gated but the by-NAME namespace is not ACL'd) remains the future UnaFS `owner`/`grants:*` arc, NOT this one. **Lane:** aarch64 `syscall.rs` only (no `fat.rs`/`block.rs`/`arroyo` change — the fixture creates its own files); zero x86 files. 🔬 Metal-pending (pure syscall lifecycle; the generation tag + `SYS_CLOSE` are QEMU-provable in full).
- [x] **File GROWTH + CREATE + DELETE — the FAT writer gets an ALLOCATOR (`fat::write_grow`/`create_in_root`/`delete_located`, `SYS_OPEN` `O_CREAT`, `SYS_UNLINK`) (U10, 2026-07-08, QEMU-verified; ✅ METAL-CONFIRMED 2026-07-10 attended bench — real cluster allocation, both-FAT mirror + directory RMW on the card).** Lifts the three restrictions U9's `write_at` kept, in rising order of risk, so `CAP_WRITE` can **extend and create** files. **The FAT-safety invariants are the whole game, and each is load-bearing:** (1) **every FAT mutation writes ALL `num_fats` copies** — `set_fat_entry` RMWs each copy at `fat_start + f*fat_sz + sec` (neighbours preserved; FAT32 reserved high-nibble preserved), so the mirror can never diverge on a successful op; a one-FAT write would be a corrupt volume and is structurally impossible. (2) **The free search is bounded** — `alloc_cluster`/`first_free_cluster` scan `[2, count+2)` reading each FAT sector at most once, return `-ENOSPC` (new `FatError::NoSpace`) when full, and never return a reserved/bad/out-of-range cluster (only one whose entry reads `0`). (3) **No stale-byte disclosure** — a newly allocated cluster is **zero-filled BEFORE it joins a chain**, so no bytes from a previously-freed file can leak into a grown/created region; partial-sector RMWs preserve their untouched bytes, and every newly-readable byte (`[size, end)`) is covered by the caller's data (the seek gate keeps `start <= size`, so there are no sparse holes). (4) **Crash-consistency on grow** — `write_grow` allocates + zero-fills + links the FAT + writes the data FIRST, then bumps the directory `size` (offset 28) / `first_cluster` (20 hi, 26 lo) **LAST**; the dir `size` is the reader's source of truth, so a crash mid-grow leaves a consistent smaller file, never a size claiming unwritten clusters (a partially-linked new cluster is at worst a benign lost cluster). (5) **Crash-consistency on delete** — `delete_located` marks the directory entry `0xE5` FIRST, then frees the whole chain (all copies); a crash after the mark only orphans clusters (benign), and can NEVER leave a live entry pointing at freed — and possibly re-allocated — clusters (which would alias another file's data). **The enforcement point is unchanged and inherited for free:** grow, create, AND delete are all reachable ONLY through `sys_write`'s single `handle_resolve(asid, fd, CAP_WRITE)` CHECK (create via an `O_CREAT` open that endows the write cap; `sys_unlink` resolves the same CAP_WRITE), so an RO-opened File, a U8-revoked cap (the derivation/revocation walk lives INSIDE `handle_resolve`), a wrong-kind handle, or an absent handle is `-EACCES` and can never mutate the volume — no new enforcement code. `sys_write_file` splits on `len <= size - offset`: the in-place branch is **byte-identical to U9**; the grow branch (capped `GROW_WRITE_MAX = 8 KiB`) routes to `write_grow`. The read path stays byte-identical: `fat_entry` now delegates to the new `pub fat_entry_copy` (one FAT-offset site), and `scan_dir_sector` shares its 8.3 parse with the new locator via `classify_dir_slot`/`DirSlot` (single source of truth). **Evidence** — three register-only single-slot fixtures (`el0-u10{grow,create,delete}`, witnesses `0x1F`/`0xF`/`0x1F`) drive the ops through the syscall surface; each launcher folds the kernel-side proof via a **fresh `mount()` re-read**: growth shows the on-disk `size` grew + the appended bytes on the card + the original cluster intact + both FAT copies consistent along the 2-cluster chain; create shows `FRESH.BIN` present with the right size + content + exactly ONE entry (idempotent `O_CREAT`, no duplicate); delete shows `DELME.BIN` GONE + its data cluster free in ALL FAT copies + the first-free cluster unchanged from before the run (re-allocatable), and — opening the file TWICE then unlinking via one handle — a read through the SIBLING handle returns `-EACCES` (no stale in-kernel reference to the freed chain). An independent on-disk re-parse of the image confirmed all three. `:: U10: file growth … -> PASS ::`, `:: U10-create: … -> PASS ::`, `:: U10-delete: … -> PASS ::`; every prior verdict **byte-identical** (sorted scratch-worktree baseline diff vs `ca1b765` — only the U10 lines differ, plus the binary-growth `VBAR_EL1` shift `0xad800`→`0xb1000` and the identity-mapped `USER_REGION` address it drags along, which shift the M6b EL0-fault diagnostic ELR/FAR; the M6b **verdict** line is byte-identical), 18 PASS, CAPSTONE 6/6, only the 3 expected M6b kills. `./arroyo test-arm 22` green (baremetal-gated); `./arroyo check` both arches, **zero x86 files** (the writers are additive — x86 never calls them). Adversarial multi-lens review (FAT-copy mirroring, allocation bounds/zero-fill, grow write-order/overflow, delete crash-safety, cap-gating, dir-parse, x86-neutral/panic-freedom). **Honest scope — grow + create + delete land.** Deferred: subdirectories, LFN (long names), rename, write-back caching (every mutation is a synchronous RMW to the card), root-directory-chain extension (root-dir full → `-ENOSPC`), and UnaFS `owner`/`grants:*` on the by-NAME namespace ops — open/create/unlink are cap-gated on the *handle* (read/write/delete authority) but the by-name namespace itself is not ACL'd yet (the future UnaFS layer). Deletion invalidates ALL of the caller's descriptors naming the file (a same-process double-open leaves the sibling fail-safe `-EACCES`, verified by the delete fixture); the remaining gap is CROSS-process — a file open in another process when unlinked here leaves that process's descriptor stale, which needs a global open-file refcount (POSIX unlink-defers-free) deferred with `SYS_CLOSE`. An `O_CREAT` that then fails to claim a kernel handle leaves a harmless 0-length entry (no kernel leak); names are strict 8.3 (trailing-dot / multi-dot / non-representable → `-EINVAL`). **Lane note:** the U9 widening stands (`fat.rs` shared + the FAT image builder in `arroyo`; `block.rs` untouched this arc); all FAT writers are aarch64-only by call site. 🔬 **This is the first arc that ALLOCATES + MUTATES the FAT and directory on a real card — the metal-risk beyond U9's single in-place sector (multi-sector zero-fill, both-FAT mirrored writes, directory RMW). The launchers' fresh-mount re-read is the proof; the boundary re-copies pristine `GROW.BIN` between runs.**
- [x] **EMMC2 R1-status check — the card's own error verdict enforced on CMD17/CMD24 (2026-07-10, QEMU-verified; ✅ healthy-path METAL-CONFIRMED 2026-07-10 attended bench — the check rode the full 23-PASS battery on the real card with zero false positives; the error leg remains metal-only hardening, unexercised until a real card complains — QEMU's SD model returns clean R1).** The polled single-block read (CMD17) and write (CMD24) paths in `drivers/emmc2.rs` previously checked only the CONTROLLER's interrupt error bits (link-level: CRC/timeout/index) and ignored the R1 card-status word in RESP0 — so a card-reported failure (OUT_OF_RANGE, ADDRESS_ERROR, WP_VIOLATION, CARD_ECC_FAILED, CARD_IS_LOCKED, the general ERROR bit, …) was silently swallowed and a rejected transfer looked like success: a fabricated read (stale buffer bytes treated as disk data) or a phantom write (FAT metadata believed durable that the card refused). Both paths now check RESP0 against the SD Physical Layer §4.10.1 error mask (`R1_ERROR_MASK = 0xFFF9_8008`) immediately after `send_command` and before the PIO FIFO phase, symmetrically (the U9 mirror discipline), logging one diagnostic line and returning `BlockError::Io` — so the FAT layer above (whose U9→U11/U6 integrity proofs assume the block layer tells the truth) sees the failure instead of corrupt success. Gate: 23 PASS + CAPSTONE 6/6 (healthy path un-regressed), `check` both arches, `test-arm` MISSION SUCCESS.
- [x] **EMMC2 CMD13 post-write status check — programming-phase card errors no longer swallowed (2026-07-10, QEMU-verified; error leg metal-only, same honest scope as the R1 arc — QEMU's SD model never fails programming).** The R1 arc above checks the card's verdict on CMD24 *acceptance*, but a single-block write has a SECOND failure window: after the transfer completes at the link layer the card goes busy programming flash (DAT0 low), and a failure DURING programming (CARD_ECC_FAILED, the general ERROR bit, WP_ERASE_SKIP) is reported only in a subsequent SEND_STATUS — so a write the card ultimately discarded still returned Ok. `write_block_512` now, after transfer-complete: waits out programming-busy by polling `ST_DAT_INHIBIT` clear under a dedicated `PROG_BUSY_TIMEOUT_MS = 500` bound (2× the spec's 250 ms single-block write timeout, §4.6.2.2 — `send_command`'s own 100 ms DAT-inhibit wait would misclassify a slow-but-legal write as an error), then issues CMD13 SEND_STATUS (`rca_arg` captured at CMD3, now carried on `SdCard`) and runs the same `r1_check` against `R1_ERROR_MASK` — a post-programming error logs one `:: M6g: CMD13 R1 error status 0x… ::` line and surfaces `BlockError::Io`, and a busy-timeout logs its own line. Read path (CMD17) unchanged — reads have no programming phase. `drivers/emmc2.rs` only. Gate: 23 PASS + CAPSTONE 6/6 (healthy path un-regressed — QEMU deasserts busy instantly, CMD13 returns clean R1 on every write in the full FAT-mutating battery), `check` both arches, `test-arm` MISSION SUCCESS. Deferred (unchanged, seat-brief only): multi-block CMD18/CMD25.
- [x] **FAT-mutation SMP-hardening — the FAT-table RMW serialized + the `sys_fgrant` grantee TOCTOU closed, with the residual concurrent-FS-mutation surface audited and ledgered (F2, 2026-07-10, QEMU-verified + ✅ METAL-CONFIRMED 2026-07-10 attended bench — the full 23-PASS battery ran through the serialized `set_fat_entry` RMW on the real EMMC2 card, CAPSTONE 6/6, zero R1/CMD13 errors, and the F2-witness held on real 4-core silicon: LOCKED 240000/240000 intact, UNLOCKED control lost 119998/240000 (~50%) under true parallelism + preemption).** The U11-M2b reaper made a SECOND cross-core FAT writer permanent, but `fat::set_fat_entry`'s read-modify-write of a FAT sector was unserialized — two cores mutating entries in the same sector (or the two mirrored FAT copies) could lose an update. Not live today (the reaper's free is await-verdict-sequenced strictly after all writers exit); closed before SMP scheduling widens.
    - **M1 — lost-update CLOSED.** A new aarch64-only `FAT_MUTATION` spinlock (`fs/fat.rs`), acquired IRQ-masked via `arch::without_interrupts` (non-preemptible → a proper IRQ-safe spinlock at any core count, the reaper's `IrqGuard` discipline), serializes the whole all-copies `set_fat_entry` RMW. The lock span is bounded to a single FAT-sector RMW — never a free-search, a data-cluster loop, or `mount()` — which is safe because the aarch64 storage path is polled (no scheduler yield). aarch64-only on purpose: x86's FAT path `hlt`s awaiting an async xHCI transfer event, so masking IRQs across it would hang (`with_fat_lock` is a zero-cost passthrough there — x86 behaviour byte-identical).
    - **M2 — `sys_fgrant` grantee (asid,gen) TOCTOU CLOSED.** The grant captured the grantee identity as two separate atomic loads (`PROCS[pi].asid`, then `ASID_GEN[asid]`); a child exiting + ASID-recycling between them could bind the grant to the recycled incarnation (misdelegation → privilege escalation / disclosure). Now re-validates `state==PRUNNING && pid && asid` after the gen read (an ASID's gen bumps only at teardown, which flips those), returning `-ECHILD` on any mismatch.
    - **M3 — cross-core witness (with teeth).** An in-RAM stress on the SAME lock (no on-disk FAT touched, zero volume risk): on 4-core QEMU raspi4b (round-robin TCG) the UNLOCKED control raced away ~48% (116001 / 240000) of its increments while the LOCKED path lost NONE — a genuine cross-core demonstration that the race is real AND the lock closes it, not honest-scope prose. ✅ Re-ran on real 4-core silicon (2026-07-10 bench): UNLOCKED lost 119998/240000 (~50% — more contended than QEMU, as true parallelism + preemption predict), LOCKED 240000/240000 intact.
    - **Audited SOUND (verified cross-core-safe on 4-core metal, no change):** the U11 open-file refcount lifecycle (incref/decref/mark-pending are lock-serialized; decrement-by-recorded-row-index is robust to FAT directory-slot recycling; the `unlink_pending`-exclusion invariant keeps a re-open off a freeing row; last-close-frees-exactly-once; single-writer-per-ASID), and the U6 owner path (`owned_grant` re-checks the owner UNDER its lock; the `(asid,gen)` fence makes stale OWNER rows harmless). Two windows are BENIGN-BY-DESIGN (a revoke gates ACQUISITION not held caps; owner-teardown-reverts-to-public).
    - **Ledgered — remaining, metal-latent, EXCLUDED_BY_SEQUENCING today (a 23-agent adversarial audit confirmed each on 4-core metal), with identified fixes → ✅ ALL SIX CLOSED by the F3 arc (2026-07-10, below):** (1) `alloc_cluster` free-search-then-claim **cluster-aliasing** — the F2 flag's other named leg — CLOSED (F3-M1 compare-and-claim under `FAT_MUTATION`); (2) directory-sector RMW lost-update (`write_dir_entry_fields`/`mark_dir_deleted`/`create_in_root`) — CLOSED (F3-M2 `DIR_MUTATION` twin); (3) `create_in_root`/`find_free_root_slot` slot-claim + duplicate-name — CLOSED (F3-M3 namespace lock); (4) `sys_open` name-lookup→`openfile_incref` racing a sole-opener `sys_unlink`'s free — a stale-chain use-after-free / cross-file disclosure — CLOSED (F3-M3); (5) `owned_clear` racing `owned_set_owner` on a recycled directory slot (private→public / ownership theft) — CLOSED (F3-M3); (6) the `sys_unlink` `0xE5`-before-mark-pending window — CLOSED (F3-M3 closes the open-vs-unlink half; the LAST-CLOSE half was already guarded by U11's mechanism — mark-pending precedes the descriptor drops and both run under the `OPEN_FILES` lock's serialization, and `sys_close` takes no namespace lock — so credit for that half is U11's, not ns). Lane: `fs/fat.rs` + `arch/aarch64/syscall.rs` only; **zero x86 behavioural change.** Gate: `kernel8-test` 23 PASS + CAPSTONE 6/6 (byte-equivalent) + the F2-witness line, zero R1/CMD13 errors; `check` both arches; `test-arm` MISSION SUCCESS.
- [x] **UnaFS namespace / FS-metadata locking — every F2-ledgered concurrent-FS-mutation race CLOSED (F3, 2026-07-10, QEMU-verified; ✅ METAL-CONFIRMED 2026-07-10 attended bench — the full 23-PASS battery ran through every F3-serialized path on the real EMMC2 card at post-merge tip `8757b27`, CAPSTONE 6/6, zero R1/CMD13, no stall on the ns-span latency watch, and the F3-witness held under TRUE parallelism: LOCKED 240000/240000 intact, UNLOCKED control lost 120000/240000 (exactly 50%); the two-cores-mid-syscall disk-sequence interleave remains honest-scope until multiple EL0 cores run FS syscalls concurrently).** The F2 arc serialized the single FAT-sector RMW and ledgered six residual races (all EXCLUDED_BY_SEQUENCING today — no uncoordinated concurrent FS writers run yet); F3 closes all six before SMP scheduling widens, with the F2 discipline: behaviorally-transparent single-core (23-PASS battery byte-equivalent), cfg-gated aarch64 locks (**zero x86 OBSERVABLE-behaviour change** — x86's FAT path `hlt`-waits, masking IRQs across it would hang; honesty note: the shared `alloc_cluster` body's on-disk write ORDERING did change on x86 too — the claim now precedes the zero-fill, plus one extra in-lock FAT-sector re-read — end-state identical and single-writer-safe, but the crash/power-loss window ordering differs from pre-F3).
    - **M1 — `alloc_cluster` cluster-aliasing CLOSED (compare-and-claim).** The free SEARCH stays unlocked, but the CLAIM re-reads the candidate's FAT entry under `FAT_MUTATION` and sets EOC only if STILL free (the loser rescans — a bounded retry via the new lock-free `set_fat_entry_inner`, factored so the claim composes inside one lock hold). Two cores can never both claim the same cluster → no two files aliasing one cluster (disclosure/corruption). ORDER REORDERED: the claim now precedes the zero-fill (zeroing an unclaimed cluster could scribble a winner's linked data); the disclosure invariant holds — the cluster is EOC-reserved but UNLINKED during the fill, so no reader path can walk onto stale bytes; a zero-fail after claim orphans the cluster (benign lost cluster, chkdsk-reclaimable), never aliases.
    - **M2 — directory-sector RMW lost-update CLOSED (`DIR_MUTATION`).** `write_dir_entry_fields` / `mark_dir_deleted` / `create_in_root`'s slot write each did a bare read-modify-write of a directory sector; two cores RMW-ing sibling slots in the same sector could lose an update (a grow's size-publish resurrecting a racing delete's `0xE5`, or vice versa). `DIR_MUTATION` is the aarch64-only IRQ-masked twin of `FAT_MUTATION` (same shape, same x86 passthrough, same tight span: one sector RMW, never a scan). FAT + directory locks guard disjoint sectors and never nest.
    - **M3 — the UnaFS NAMESPACE lock (the four sequence races CLOSED).** One per-mount IRQ-masked `NAMESPACE` SpinMutex (`arch/aarch64/syscall.rs`, RAII `NsGuard` — release-lock-then-restore-DAIF drop order) makes the coupled multi-step name sequences mutually atomic: `sys_open`'s `find_located → ACL → openfile_incref/files_alloc` (incl. the `create_in_root → owned_set_owner` create path) and `sys_unlink`'s `mark_dir_deleted → owned_clear → mark-pending → descriptor-drop`. Closes: open-races-unlink stale-chain UAF/disclosure; `owned_clear`-vs-`owned_set_owner` recycled-slot private→public/ownership-theft; the create scan-then-claim duplicate-slot/name race; the `0xE5`-before-mark-pending window's open-vs-unlink half (its last-close half was already U11's — see ledger item (6) above). SPAN (relaxed BY DESIGN vs the tight FAT/DIR spans): held across the bounded polled directory block I/O inside the sequences, but NEVER across `mount()` or a chain free — `files_free_by_dir` now COLLECTS orphan chain heads (`#[must_use]`) and `sys_unlink` frees them after the guard drops. STRICT lock order: `NAMESPACE ⊃ {FAT_MUTATION, DIR_MUTATION, OPEN_FILES, OWNED_FILES, DEFERRED_FREE}` — inner locks taken freely under it, NAMESPACE never acquired while an inner is held (no inner critical section calls `ns_lock`).
    - **M4 — cross-core witness (the F2 pattern) + honest scope.** An in-RAM stress of the EXACT `ns_lock` guard on a non-atomic counter, two cores: the LOCKED pass loses nothing while the UNLOCKED control races away increments under QEMU RR-TCG — proving the namespace lock is engaged and serializes cross-core. (The stress holds ns around a bare counter with no inner lock taken, so nesting safety against the inner locks rests on the reviewed lock-order discipline — NAMESPACE outermost, never acquired while an inner is held — not on this witness.) Emits an `F3-witness:` line (not a `-> PASS` line — fixture count stays byte-equivalent). HONEST SCOPE: the full open-vs-unlink DISK-sequence interleave (two cores mid-syscall in `find_located`/`mark_dir_deleted`) is not provokable from the single-EL0-core QEMU battery — that leg is metal-latent and rides the attended bench.
    - Gate: `kernel8-test` 23 PASS byte-equivalent + CAPSTONE 6/6 + the F2-witness AND F3-witness lines, zero R1/CMD13 errors; `check` both arches; `test-arm` MISSION SUCCESS. Lane: `fs/fat.rs` (aarch64 path, seat-granted) + `arch/aarch64/syscall.rs`; zero x86 behavioural change.
- [x] **FAT directory create/remove — `create_dir` / `remove_dir`, the lock-correct directory-mutation seam (FATDIRS, 2026-07-12, QEMU-verified; the attended money-shot rides JD7's Orin panel bench).** JD7 (the Orin panel's `mkdir`/`rmdir`) needs directory-mutation logic beyond JD6's thin additive wrappers, and a dir-mutation bug in arch-shared `fat.rs` hits all three platforms — so the seam lands as a dedicated pi4-lane arc (the F2/F3/K* lock lineage is this track's expertise), which JD7 consumes call-never-edit. Two new public `fat.rs` methods next to JD6's dir-aware twins, with **ZERO edits to any existing fn** (the JD6 additive-exception pattern): `create_dir(parent_first_cluster, name)` and `remove_dir(parent_first_cluster, name)`, plus one private `init_subdir_cluster`. They COMPOSE the reviewed F2/F3-locked primitives (`alloc_cluster` compare-and-claim under `FAT_MUTATION`; `create_in_dir`/`locate_in_dir`; `write_dir_entry_fields`/`mark_dir_deleted`/`free_chain` under `DIR_MUTATION`/`FAT_MUTATION`).
    - **Crash ordering (fail-safe, invariant 2).** `create_dir` fully allocates + `.`/`..`-initializes the child cluster BEFORE it links the parent, and the parent link itself is two writes — `create_in_dir` writes a 0-cluster DIR entry, then `write_dir_entry_fields` publishes the child cluster (the last write). Every crash window fails toward a leaked (chkdsk-reclaimable) cluster or the JD6-ledgered `FstClus==0` corner (a 0-cluster dir entry — malformed, `cd` into it lists the root), NEVER toward a live entry pointing at a cluster that later gets freed/aliased. `remove_dir` marks the parent entry `0xE5` FIRST, then frees the child chain (the U10/`delete_located` order) — a crash leaves the directory gone with a benign leaked cluster, never a live entry over a freed chain. The child-init write needs no lock (the cluster is a zero-filled UNLINKED orphan, unreachable until publish — the `alloc_cluster` zero-fill discipline).
    - **Locking span + honest residual (invariant 3 + 5).** Every SECTOR mutation is SMP-atomic via the existing per-RMW locks, and `DIR_MUTATION` is NEVER widened past its documented single-sector span (holding it across a directory SCAN or across `free_chain`'s block I/O would break its IRQ-masked-non-preemptible span rule). The internal locking is therefore sound WITHOUT the syscall-layer `NAMESPACE` lock — required, because EL1 shell callers (the panel) reach `fat.rs` directly, not through `sys_open`/`sys_unlink`. That leaves ONE honest-scope residual, the same class as F3's item (4) / the two-cores-mid-syscall interleave: **`remove_dir`'s emptiness-scan → `delete_located` is not atomic against a concurrent `create_in_dir` INTO the same target directory** (a file linked between the scan and the free would be orphaned in a freed chain). **EXCLUDED_BY_SEQUENCING today** — the only FS mutators are the single-threaded EL1 panel shell, EL0 syscalls (serialized by the syscall `NAMESPACE` lock), and the await-verdict-sequenced reaper; none races another FS mutator. The fix when concurrent EL1 FS mutators appear is a `fat.rs`-level namespace lock spanning both the emptiness-scan→delete and `create_in_dir`'s scan→write — a future seam change that WOULD have to touch `create_in_dir`, so it is out of this additive grant (flagged to the seat). **JD7 caller update (2026-07-12, jetson lane):** with the Orin panel now landing an EL1 `create_dir` CALLER (`mkdir`) as well as `remove_dir`, this residual is symmetric — an EL1-panel-vs-EL0 create/create INTO the same directory (two `create_in_dir` scan→write sequences racing for the same free slot) is the SAME EXCLUDED_BY_SEQUENCING class, closed by the SAME future namespace lock (the ledger above framed it `remove_dir`-only because at FATDIRS-authoring time no EL1 create caller existed yet). Still excluded today: the panel shell is the only EL1 FS mutator and runs single-threaded; EL0 creates ride the syscall `NAMESPACE` lock; the two do not run as concurrent FS mutators on the tegra panel.
    - **Error-fidelity note (flagged for JD7, jetson lane).** `create_dir`/`remove_dir` reuse EXISTING `FatError` variants: adding a variant would break shell.rs's exhaustive `fat_errno` match (kernel-core, the jetson lane) — out of this arc's lane. So `-ENOTDIR` (a file target) and a root-like `first_cluster==0` target both map to `Unsupported`, and `-ENOTEMPTY` maps to `IsDirectory`. Enriching `FatError` for full POSIX-errno fidelity is a future jetson-lane seam change JD7 can carry (it owns the errno map). `NotFound`/`NoSpace`/`Io`/`BadChain`/`NoDisk` map exactly. `create_dir` mirrors `create_in_dir`'s no-de-dup contract (the caller confirms absence first).
    - **Proof + gate.** `fatdirs_check`/`fatdirs_launcher` (the `k1_atr` disk-selftest idiom, LAST in the U7 chain so its disk I/O can never perturb the battery) drive the live volume end to end and are FULLY self-cleaning — an uncounted `:: FATDIRS: … PASS [w=0xff] ::` (8 assertions: `.`/`..` well-formed, file-in-dir, non-empty refused, empty rmdir frees+reuses the freed cluster, root-like + file targets refused). Gate: `kernel8-test` 23 PASS byte-equivalent + CAPSTONE 6/6 + all prior witnesses intact + the FATDIRS line, zero FAIL; `check` both arches + `UNAOS_TEGRA=1 check`; `test-arm 22` MISSION SUCCESS. Lane: `fs/fat.rs` (additive only) + `arch/aarch64/syscall.rs` (selftest wiring); **zero x86 behavioural change** (the new methods compile on x86 but have no x86 caller). Commit `cdfe25b`.
- [x] **FAT directory-entry rename/move — `rename_entry` / `move_entry`, the lock-correct rename+cross-directory-move seam (FATMOVE, 2026-07-12, QEMU-verified; the attended money-shot rides a future jetson `mv` (JD10) Orin panel bench).** `mv` is the last unimplemented classic file-manager verb on the Orin panel, and its move-across-directories needs a genuinely-new `fat.rs` mutation — "unlink the source directory entry WITHOUT freeing the chain" — with no existing public twin; a mutation bug in arch-shared `fat.rs` hits all three platforms, so the seam lands as a dedicated pi4-lane arc (the F2/F3/K*/FATDIRS lock lineage is this track's expertise), which the jetson `mv` arc consumes call-never-edit (the FATDIRS/JD7 split). Two new public `fat.rs` methods next to the FATDIRS block, with **ZERO edits to any existing fn** (the FATDIRS additive-exception pattern): `rename_entry(parent_first_cluster, old_leaf, new_leaf)` (rewrite the 8.3 name in place — a SINGLE dir-sector RMW; `first_cluster`/`size`/`attr` unchanged) and `move_entry(src_parent, leaf, dst_parent, new_leaf)` (relink a file entry into a new parent over the SAME `first_cluster`/`size`, then `0xE5` the source WITHOUT freeing the chain), plus one private single-sector `write_dir_entry_name`. They COMPOSE the reviewed F2/F3-locked primitives (`locate_in_dir`/`create_in_dir`; `write_dir_entry_fields`/`mark_dir_deleted` under `DIR_MUTATION`) — no `alloc_cluster`, no `free_chain`: the data clusters move BY REFERENCE.
    - **Crash ordering (fail-safe, invariant 2 — NEVER lose the chain).** `move_entry` publishes the DESTINATION entry FIRST (`create_in_dir` writes a 0-cluster entry, then `write_dir_entry_fields` publishes the shared chain head + size), and only THEN marks the source entry `0xE5`. A crash/power-loss between the two leaves a benign **DUPLICATE**: two directory entries pointing at the SAME chain (the source's original name and the destination's new name). Both are readable; the operator removes the unwanted one BY ITS ENTRY (`rm OLDNAME`). The reverse order (`0xE5` the source first) could ORPHAN the chain if the crash landed before the destination published it — forbidden. If `write_dir_entry_fields` fails after `create_in_dir` succeeds, the destination is left a benign 0-length file and the source chain is intact; if the final `mark_dir_deleted` fails, the benign duplicate stands — every window fails toward a leaked/duplicate name, NEVER a lost or aliased chain. FAT is not journaled, so the window is fundamental; the bias is fail-SAFE. `rename_entry` is a single dir-sector name-field RMW — no cross-write window at all.
    - **Directories (invariant 3).** `rename_entry` renames a directory IN PLACE (allowed): only the parent's entry name field changes; the directory's own `.` and its children's `..` point at `first_cluster` values a rename does NOT touch, so they stay correct. `move_entry` of a directory ACROSS parents is REFUSED (`IsDirectory`) — it would require rewriting the moved directory's `..` entry to the new parent, out of this additive arc's scope.
    - **Locking span + honest residual (invariants 4 + 5).** Every SECTOR mutation is SMP-atomic via the existing per-RMW `DIR_MUTATION` span, and `DIR_MUTATION` is NEVER widened to span both of `move_entry`'s two dir-sector RMWs at once (its documented single-sector contract; cross-sector atomicity of the destination-publish and the source-`0xE5` is EXCLUDED_BY_SEQUENCING for EL1 callers). The internal locking is therefore sound WITHOUT the syscall-layer `NAMESPACE` lock — required, because EL1 shell callers (the panel) reach `fat.rs` directly. The residual is the SAME class as FATDIRS's `remove_dir` TOCTOU / F3 item (4): the composite `locate → mutate` sequences are not held under one lock, so a concurrent FS mutator into the same directory could race. **EXCLUDED_BY_SEQUENCING today** — the only FS mutators are the single-threaded EL1 panel shell, EL0 syscalls (serialized by the syscall `NAMESPACE` lock), and the await-verdict-sequenced reaper; none races another FS mutator. The fix when concurrent EL1 FS mutators appear is the SAME future `fat.rs`-level namespace lock FATDIRS flagged — spanning `create_in_dir`'s scan→write — so it is out of this additive grant (flagged to the seat).
    - **ACL re-key note (invariant 5, flagged for the jetson `mv` arc + the K-line).** `fat.rs` is ACL-blind by layering — the `OWNED_FILES` ACL keys a private file by its directory-entry location `(dir_lba, dir_off)` (aarch64 `syscall.rs`). A rename/move CHANGES that key: `rename_entry` moves the name within the same slot (key preserved — an in-place rename does NOT strand the ACL), but `move_entry` writes a NEW slot and `0xE5`s the old, so the ACL row keyed on the old `(dir_lba, dir_off)` would be STRANDED (the moved file silently reverts to public) if an OWNED file were moved via a future EL0 path. This arc builds NO EL0 plumbing; the EL1 panel runs as ASID 0 (the PUBLIC principal), so a panel-driven rename/move touches no ACL row and the witness proves only the public-file reality. A future EL0 rename/move syscall MUST re-key the `OWNED_FILES` row (and the persisted `UNAFS.ATR` `dir_lba`/`dir_off` fields) to the new location, or refuse an owned target — the SAME class of key-follows-the-entry work the K-line already does for persist/unlink. Ledgered here + carried into the jetson `mv` (JD10) brief.
    - **Error-fidelity note (flagged for the jetson `mv` arc, jetson lane).** Like FATDIRS, `rename_entry`/`move_entry` reuse EXISTING `FatError` variants (adding one would break shell.rs's exhaustive `fat_errno` match — kernel-core, the jetson lane). So the DEST-EXISTS refusal maps to `Unsupported` (shared with a bad 8.3 name); the CALLER confirms the destination is absent first (as `fs_touch` does for create) and surfaces `-EEXIST` locally, so the seam's `Unsupported`-on-exists is a defensive backstop that never writes a duplicate. The move-a-directory refusal uses `IsDirectory` (the `-EISDIR`-equivalent). `NotFound` (source absent) → `-ENOENT`, `NoSpace` (dst full) → `-ENOSPC`, `Io`/`BadChain`/`NoDisk` map exactly. Enriching `FatError` for full POSIX-errno fidelity remains a future jetson-lane seam change (it owns the errno map).
    - **Proof + gate.** `fatmove_check`/`fatmove_launcher` (the `k1_atr`/FATDIRS disk-selftest idiom, LAST in the U7 chain so its disk I/O can never perturb the battery) drive the live volume end to end and are FULLY self-cleaning — an uncounted `:: FATMOVE: … PASS [w=0x1ff] ::` (9 assertions: rename name-swap returns same head+size, old name gone / new name = same chain, 700-byte content intact through the rename, rename-onto-existing refused; cross-dir move by reference, content intact at the new location, move-onto-existing refused, directory move refused, empty 0-cluster file relinked). Gate: `kernel8-test` 23 PASS byte-equivalent + CAPSTONE 6/6 + all prior witnesses intact (F2/F3 locked 240000/240000, K1-atr/persist/corrupt, K2-liveenf, K3-revoke, IMG-SIG, FATDIRS, K4-ready) + the FATMOVE line, zero FAIL, zero R1/CMD13; `check` both arches; `test-arm 22` MISSION SUCCESS. Lane: `fs/fat.rs` (additive only) + `arch/aarch64/syscall.rs` (selftest wiring); **zero x86 behavioural change** (the new methods compile on x86 but have no x86 caller). Commit on `hw-pi4` (the seat assigns the integration hash at merge).
- [x] **On-disk UnaFS owner/grants — `UNAFS.ATR` persistence LANDED + cross-reboot enforcement now LIVE (K1 M1–M4 + K2, 2026-07-10/11, QEMU-verified; true power-cycle survival metal-pending).** U6's `OWNED_FILES` ACL is in-RAM and BOOT-SCOPED — the owner is an `(asid, gen)` incarnation, so a power-cycle reverts every private file to PUBLIC. K1 makes ownership SURVIVE REBOOT by persisting owner + grants as on-disk attributes inside the FAT volume; **K2 turns the proven-but-gated mechanism LIVE** (see the K2 bullet). **M1 landed the FORMAT + (de)serializers + reserved-file helpers + a round-trip self-test (enforcement-inert). M2.2/M2.3/M2.4 landed the real volume fingerprint, the write-through persist, and the mount-rebuild + gated cross-reboot admission; M3 proves the whole survive-reboot round-trip and M4 the fail-closed torn-row rule. The persistent-principal model — a TCB-level policy decision ABOVE this aarch64 lane — was PROPOSED at M1 and RATIFIED by Peter + the orchestrator seat (2026-07-10) before M2 proceeded.**
    - **PRINCIPAL-IDENTITY (the arc's load-bearing decision — RATIFIED, then implemented).** A persisted owner cannot be an `(asid, gen)` (boot-scoped; `asid = slot+1` is reassigned every boot). It must be a PERSISTENT PRINCIPAL, and UnaOS has none: EL0 programs are UNTRUSTED blobs loaded BY NAME with no code-signing / manifest / image hash, there is no RTC / wall-clock and no uid registry — nothing durable to project ownership onto. **RATIFIED model (Peter + orchestrator seat, 2026-07-10; launcher policy v1 is seat-owned):** a launcher-assigned, KERNEL-STAMPED principal — identity is stamped onto an ASID at spawn by trusted launcher/boot policy and NEVER self-asserted by a blob (no syscall says "I am principal K"). ONE principal per 8.3 program name, stamped by the kernel from the name the loader RESOLVED (`load_program_into_slot` → `SLOT_PPID[asid]`); no EL0 path may set or change it. Name collisions (two blobs, same name) were BY DESIGN the same principal under PROGRAM_NAME — that residual is **CLOSED by IMG-SIG (2026-07-11): the loader now mints `kind = IMAGE_SHA256`** (see the IMG-SIG bullet below). Recorded on disk as a 32-byte kind-tagged, string-projectable `PrincipalRecord` (`kind` u8, `len` u8, `value[30]`); `kind = PROGRAM_NAME` (`"prog:<name>"`) remains valid (kernel-internal manual stamps still use it), `kind = IMAGE_SHA256` is the loader's mint since IMG-SIG, `KERNEL_PID` is still reserved; `kind = NONE` = anonymous / public-only. The string projection maps 1:1 onto native unafs `owner` / `grants:<name>` keys at K4. **Honest scope of the claim:** this protects a DIFFERENT on-box software principal reading/writing/deleting another's private file ACROSS A REBOOT (the real U6 threat, extended over the power-cycle) and blocks runtime identity spoofing (identity is kernel-stamped, not blob-chosen); it does NOT and cannot claim tamper-resistance against an OFFLINE writer of the unencrypted card (FAT is plaintext — forging a CRC'd row buys nothing that `cat`-ing the sector doesn't), NOR image-substitution without code-signing, NOR rollback/replay of the attr file (no monotonic anti-replay counter is defensible without protected storage the Pi 4 lacks). No HMAC is added and none is claimed — the CRC is integrity/torn-write detection, NOT authentication.
    - **FORMAT (`UNAFS.ATR`, a hidden|system root file — attr `0x06`, so foreign OSes hide/skip it; internal magic, not the name, is the identity).** A 512-byte header (magic `UNAATR1\0` — deliberately NOT the native unafs superblock magic, so a future kernel tells the FAT bridge apart and migrates-then-deletes at K4; `version`, self-describing `header_len`/`row_stride`/`row_count`, a `committed` flag, a volume binding, `header_crc32` over `[0..508)`) followed by 16 fixed 256-byte rows (2 per sector, none straddling → a single-row update is exactly ONE device-sector RMW). **16 on-disk rows MIRROR the 16-row in-RAM `OWNED_FILES` bound — no unbounded growth.** Each row: the 8.3 name (the DURABLE identity key, re-resolved at mount), `first_cluster`/`size` (corroborating validators), a `(dir_lba, dir_off)` runtime hint, the 32-byte owner `PrincipalRecord`, 4 grants (`{PrincipalRecord, rights u32}` — matches `NFGRANT`), and a `row_crc32` over `[0..252)` covering owner AND grants TOGETHER. Two independent CRCs (header, per-row) and DELIBERATELY no whole-file CRC — so a single-row grant change never has to rewrite (and torn) the header sector. **In-place single-row `write_at` (one sector) for every steady-state mutation; a whole-file `write_grow` ONLY at creation, where a torn write is benign (no ACL exists yet).**
    - **FAIL-CLOSED (the read-side rule M2's mount-rebuild inherits).** On ANY doubt about a row — bad magic / version / geometry / volume binding / header CRC / row CRC / (M2) a name that no longer resolves — revert that file (or the whole volume) to PUBLIC, the well-defined pre-U6 baseline. **The asymmetry "fail-closed on grants, fail-open on ownership is not a thing" is STRUCTURALLY foreclosed:** owner and all its grants live in ONE row under ONE `row_crc32` and install-or-drop ATOMICALLY, so a row is never half-trusted and an owner/grant is never forged from garbage. A wrong volume binding (a FOREIGN volume or a REFORMAT — a different `BS_VolID`/cluster count) → all-public, so its rows never attach to THIS volume's directory slots. (A full byte-for-byte clone preserves the fingerprint and is NOT rejected — offline tampering is out of scope, per the honest-scope claim above.) HONEST residual (accepted this arc): fail-closed-to-PUBLIC of a torn OWNER row is a confidentiality downgrade on crash (a private file becomes world-readable); the old-row-survives write-commit-order mitigation is an M2 write-path concern. What is forbidden — fail-OPEN, admitting a specific unauthorized principal from a torn/garbage row — never happens.
    - **What M1 BUILT (gate-green, in-lane in `arch/aarch64/syscall.rs`, ZERO `fat.rs`/x86 edit):** the format consts + `PrincipalRecord`/`AtrRow`/`AtrGrant` types + `atr_crc32` + row/header (de)serializers with the fail-closed rules; reserved-file helpers (`atr_ensure`/`atr_write` steady-state row / read-all) composed ENTIRELY from `fat.rs`'s EXISTING public API (`find_located`/`create_in_root`/`write_grow`/`write_at`/`read_at`); and `k1_atr_selftest` — an in-RAM codec round-trip (populated owner+multi-grant row byte- and field-equal; a CRC-corrupt row → dropped-to-public; wrong magic / CRC / binding / uncommitted headers → rejected) PLUS an on-disk round-trip (create the empty image, write one SYNTHETIC row via `write_at` UNDER the F3 `NAMESPACE` lock — the exact M2 steady-state seam — read it back byte-equal, then clear it, leaving `UNAFS.ATR` a valid EMPTY / all-public image). It runs LAST in the demo chain and emits `:: K1-atr: … codec PASS … on-disk helpers disk PASS … ENFORCEMENT-INERT … ::` — NOT a `-> PASS` line, so the 23-fixture count stays byte-equivalent. **The round-trip uses SYNTHETIC principals ON PURPOSE — the live owner `(asid, gen)` is the one thing that must NOT be persisted.**
    - **Lock discipline.** The one-time file CREATE (`create_in_root` + a multi-cluster `write_grow`) runs WITHOUT the namespace lock (file setup, not an ACL mutation — holding `ns` across a multi-cluster grow would extend the IRQ-masked span, the F3 ns-latency concern). The steady-state single-row `write_at` (one sector, no inner lock) runs UNDER `ns_lock` — respecting the order `NAMESPACE ⊃ {FAT_MUTATION, DIR_MUTATION, OPEN_FILES, OWNED_FILES, DEFERRED_FREE}`; never held across `mount()`.
    - **What M2.2/M2.3/M2.4 BUILT (the enforcement model, in-lane in `arch/aarch64/syscall.rs` + one read-only `fs/fat.rs` accessor).** **M2.1** (behaviour-neutral, prior commit): a by-name `load_program_into_slot(name)` + the `SLOT_PPID[asid]` kernel principal-STAMP from the loader-resolved name (`current_principal()`/`slot_ppid_of()` read it), cleared at teardown. **M2.2:** `fs::fat::volume_fingerprint() -> (BS_VolID, count_of_clusters)` (read-only; parsed at mount, offset `0x27` FAT16 / `0x43` FAT32), the real volume binding replacing M1's `cluster_size`/`num_fats` placeholder. **M2.3 (write-through persist):** `OwnedFile` gains `owner_ppid`, `FileGrant` gains `ppid`, captured at create (`owned_set_owner`) and grant (`owned_grant`) from principals snapshotted BEFORE any `OWNED_FILES`/`ns` hold; the syscall handler persists AFTER the in-RAM update (`sys_open`-create → `atr_persist_row`, `sys_fgrant` → `atr_persist_grants`, `sys_unlink` → `atr_clear_row`), gated on `owner_ppid.kind != NONE` so an anonymous owner does ZERO disk I/O; `atr_ensure` now self-heals a stale/foreign-binding header (regrow). **M2.4 (mount-rebuild + gated admission):** `atr_rebuild_into_owned` reads + binding-validates `UNAFS.ATR` and, for each committed named-owner row, RE-RESOLVES the file BY NAME (`find_located` — defeats the recycled-slot hazard), cross-checks `first_cluster`, and installs an `OwnedFile` with the NO-LIVE-OWNER sentinel (`owner_asid = u64::MAX`) + the persisted principals; `owned_access_ok`/`owned_is_owner`/`owned_unlink_permitted` gain an ADDITIVE branch admitting a NAMED caller-principal (owner-by-name = full authority incl. delete/re-grant; grantee-by-name = rights-checked) AFTER the unchanged `(asid, gen)` checks — purely structural (a NONE principal never matches), so anonymous callers are inert. **The real-boot rebuild is GATED on `by_name_spawn_multivalued()` (FALSE today — one launchable named program), because a persisted owner that could never be re-acquired but denies everyone would only BRICK the file; enforcement goes live the instant a second launchable named program lands.** The `owned_access_ok` ppid branch itself is NOT gated on that global, so it is inert in the battery yet directly testable.
    - **What M3/M4 PROVE (kernel-side, the `u11_check_gen_rebind` idiom — no EL0 fixture; each emits its own UNCOUNTED `<noun> PASS` witness line, so the 23-fixture count is byte-equivalent).** **M3 (`k1_persist_launcher`, `:: K1-persist: … PASS [w=0x3fff] ::`):** stamp two scratch principals via the REAL `slot_ppid_stamp`/`slot_ppid_of` machinery, persist an owned+granted file (both `atr_persist_row` and `atr_persist_grants`, persisting `first_cluster = 0` exactly as `sys_open`-create does), simulate a reboot (drop the in-RAM ACL), remount, `atr_rebuild_into_owned`, then a 14-assertion witness: owner-by-name admitted RW, impostor + anonymous DENIED, grantee admitted R / denied W, public stays public, owner-by-name recognized as owner + has delete authority (`owned_unlink_permitted`) + CAN re-grant AND re-REVOKE a rebuilt grantee by-name (`owned_grant`, F1+F2), impostor denied both delete and grant, and a NAMED owner's row SURVIVES its owner's teardown as a sentinel (F2 MF2). **M4 (`k1_corrupt_launcher`, `:: K1-corrupt: … fails closed to PUBLIC … PASS ::`):** persist a named owner, TEAR one byte of its on-disk row (busting the row CRC), remount + rebuild — the torn row is DROPPED so an ANONYMOUS caller is ADMITTED (the file is public — the fail-closed baseline, never a forged owner). Both fixtures are FULLY self-cleaning (delete_located frees the grown chain; UNAFS.ATR restored to empty) so the stateful metal card accumulates nothing. **M4 also denies EL0 `SYS_OPEN` of `UNAFS.ATR` outright** (case-insensitive, before any lookup) — the kernel owns the ACL store; no blob may read every principal or forge a row.
    - **F1 (adversarial-review catches, folded into this arc):** (1) `owned_grant` had a live-`(asid, gen)`-only owner gate, so a rebuilt-row (by-name) owner was admitted by `owned_is_owner`/`owned_unlink_permitted` but DENIED re-grant/revoke by `owned_grant` (`-EACCES`) — fixed by threading the caller's principal into `owned_grant` and adding the same by-name owner branch; M3 now exercises it (bits 10/11). (2) `atr_ensure` regrew (WIPING all rows) on any `false` from the header check, conflating a transient block-READ error with a genuine binding MISMATCH — split into `atr_header_status` (tri-state), so `atr_ensure` regrows ONLY on a real mismatch and BAILS the (non-fatal) persist on a read error, preserving the rows. (3) M3 now persists `first_cluster = 0` to FAITHFULLY match production.
    - **F2 (seat security-tier review, 6/6-refuter must-fixes — all fail-OPEN, latent while the gate is off, fixed before K2 flips it):** (MF1/MF3) `owned_grant`'s REVOKE + GRANT/UPDATE arms matched a grantee slot ONLY by live `(asid, gen)`, so a post-reboot revoke/update of a REBUILT grantee (slot `asid == OWNER_ASID_PERSISTED`, addressed by a live Child asid 1..=8) matched nothing — REVOKE returned success while the grant stayed admitting AND was re-persisted (irrevocable), UPDATE claimed a SECOND slot per principal (order-dependent rights). Fixed: both arms also match by `g.ppid == grantee_ppid` for a rebuilt slot (structural — a NONE grantee never matches; the update re-binds the slot to the live grantee). M3 now witnesses revoke-after-rebuild (bit 12). (MF2) owner teardown (`owned_clear_owner_asid`) WIPED the in-RAM row but touched no disk, so `sys_unlink`'s `owned_owner_ppid` gate read NONE and SKIPPED the disk clear — a stale owner row survived that a FUTURE same-name file would adopt at mount (prog:X creates N → X exits → anyone unlinks N → victim creates N → reboot → prog:X owns victim's file). Fixed: teardown of a NAMED owner CONVERTS `owner_asid` to the sentinel (keeps `owner_ppid` — the file persists by principal, RAM/disk stay consistent, the unlink-time `atr_clear_row` fires); an ANONYMOUS owner still wipes-to-public (battery byte-identical). M3 witnesses it (bit 13). Also folded: `sys_unlink` now clears the ATR row BEFORE the `0xE5` name delete, so a crash between them fails toward PUBLIC instead of stranding a stale owner row.
    - **K2 (make-enforcement-LIVE — 2026-07-11, QEMU-verified + ✅ METAL-CONFIRMED on real Pi 4 silicon). Turns K1's proven-but-gated cross-reboot ACL into LIVE enforcement, end-to-end through REAL disk-loaded programs.** Four milestones, all in `arch/aarch64/syscall.rs` (+ the seat-granted build lane) with ZERO x86 change: **M(a)** landed the honest precondition — TWO more launchable named programs on the card (`K2OWN.BIN`, a private-`O_CREAT` owner; `K2IMP.BIN`, a non-owner) built as extra `[[bin]]`s of `crates/user-blob` and carried by `arroyo kernel8`; three distinct 8.3 names now mint three distinct `prog:<NAME>` principals via the sole mint path. **M(b)** added `atr_persist_grow` (the grow twin of `atr_persist_grants`): on a GROW in `sys_write` of a NAMED-owner file, re-persist the row's real `first_cluster`/`size`, so the rebuild's identity cross-check ENGAGES instead of the create-time-0 name-primary path — a defense-in-depth strengthening of the offline-tamper / non-`sys_unlink`-delete corroboration; NO-OP (zero disk I/O) for anonymous owners (the whole battery), so byte-equivalent. **M(c)** flipped `by_name_spawn_multivalued()` → `true`, so `atr_maybe_boot_rebuild` (at the head of `u7_launcher`) reinstalls persisted rows at REAL boot — the bricking hazard is gone now that a persisted owner can be re-acquired by re-spawning its named program. On QEMU (fresh FAT) `UNAFS.ATR` is absent at that rebuild, so it is a no-op and the battery stays byte-equivalent; the flip's live effect is metal (a real power-cycle where a prior boot left a real row). **M(d)** `k2_liveenf_launcher` PROVES it end-to-end through real programs (the last demo in the U7 chain): spawn `K2OWN.BIN` (auto-stamped) → it privately creates+owns+grows `K2PRIV.BIN` at EL0 (create persists, M(b) grow-repersists the real cluster); SIMULATE a reboot (`owned_clear` + remount + `atr_rebuild_into_owned` → the row is reconstructed purely from disk with `owner_asid = sentinel`); re-spawn `K2OWN.BIN` → a FRESH incarnation is re-ADMITTED purely BY NAME (the sentinel can never match a live `(asid,gen)`); spawn `K2IMP.BIN` (a distinct real principal) → REFUSED `-EACCES`. A 7-bit witness (`:: K2-liveenf: … rebuild+enforce PASS [w=0x7f] ::`, uncounted) covers create+stamp / in-RAM owner / disk-survive+rebuild / rebuilt-owned-by-name / owner-re-admitted-by-name / impostor-denied / grow-repersist-landed. Fully self-cleaning (delete `K2PRIV.BIN` + clear the attr row) so the STATEFUL metal card leaves no bricking row. **F2 (13-agent adversarial review, 1/9 confirmed):** the pre-flight "programs absent" skip returned BEFORE the stale-`K2PRIV.BIN` cleanup, so with the gate live a reinstalled row could be stranded if a program went missing after an interrupted run — fixed by cleaning a stale `K2PRIV.BIN` unconditionally first (bounded: nothing outside the demo opens it). **Honest residual:** name-collision = same principal until `IMAGE_SHA256`. **✅ METAL-CONFIRMED (2026-07-11 attended bench, real Pi 4, this-session-driven, Peter physical): Part A one-boot** — `MBENCH PASS 25/25` required witnesses, 0 forbidden hits (23 PASS + CAPSTONE 6/6 + `K2-liveenf … PASS [w=0x7f]` + K1-persist/corrupt + F2/F3 locked 240000/240000; zero R1/CMD13/AARCH64-EXCEPTION; zero A72 EC=0 heal lines). **Part B genuine two-boot power-cycle** (the `UNAOS_K2_LEAVE` knob, M(e)): boot-1 left `K2PRIV.BIN` persisted (owner `prog:K2OWN.BIN`, fc=0x12) on the card → REAL power-cut → boot-2 the LIVE `atr_maybe_boot_rebuild` reinstalled the row from the card's `UNAFS.ATR`, owner re-admitted BY NAME, impostor refused `-EACCES`, self-cleaned — `:: K2-metal: BOOT-2 … SURVIVED a real power-cycle … PASS [w=0x07] ::`. So the true cross-reboot power-cycle survival is no longer simulate-only — it holds on real silicon (logs `~/unaos-bench/pi-serial-2026-07-11-112106.log` [Part A] + `…-112529.log` [Part B]).
    - **K3 (revoke-persist commit-ordering — 2026-07-11, QEMU-verified; the fail-OPEN revoke residual RETIRED).** The revoke leg was previously best-effort in the FAIL-OPEN direction: after the in-RAM revoke succeeded, a crash or a swallowed disk error during the `atr_persist_grants` re-persist left the OLD grant on disk, so the revoked grantee was re-admitted at the next mount. K3 reverses the commit order for a REVOKE of a NAMED grantee on a NAMED-owner file: `sys_fgrant_revoke_2phase` computes the POST-revoke grant set from the current in-RAM snapshot, writes that NARROWED row to disk FIRST (via the extracted `atr_write_grant_row`), and commits the in-RAM removal ONLY if the durable write held. A persist failure is FAIL-CLOSED — the in-RAM grant is left intact (still enforced this boot) and the caller gets `-EIO`/`-ENODEV`, so RAM and disk never silently diverge in the fail-open direction (no false success). **Scope (seat 3-lens review, 2026-07-11): this closes the SINGLE-ACTOR crash/swallowed-error fail-open.** A pre-existing SMP window remains (unchanged by K3, same class as the S6-lock residual): a concurrent full-row re-persist (`atr_persist_grow` from a writer core, or a second owner-incarnation grant) that snapshots RAM between the revoke's disk-narrow and its in-RAM commit can resurrect the revoked grant on disk — closable only by spanning snapshot→disk→commit under one lock; ledgered, deferred. The order is unchanged (in-RAM, then best-effort persist) for a WIDEN (grant/update — a lost persist just drops the new grant, already fail-closed) and for an ANONYMOUS (NONE) grantee (it persists as an inert NONE-principal row a rebuild never re-admits), so the whole 23-fixture battery is byte-equivalent. Proven by `k3_revoke_check` (kernel-side, the `k1_persist_check` idiom): a named-owner file with two grantees, one revoked through the production two-phase path, survives a simulated reboot (revoked grantee DENIED, kept grantee still admitted) and a FORCED persist failure fails closed (`-EIO`, in-RAM grant intact) — witnessed `:: K3-revoke: … PASS [w=0x7f] ::`. **Folded K2-review family items:** `sys_unlink` now aborts (`-EIO`) BEFORE the `0xE5` name delete if the durable `atr_clear_row` failed (a swallowed clear can no longer strand a stale owner row — the same commit-ordering discipline, fail-closed); the K2 launchers' `cleaned` probe matches `NotFound` specifically rather than `.is_err()` (a transient read error no longer overclaims self-cleaned). **STILL DEFERRED (post-K3):** the torn-row write-commit residual (a torn OWNER row is a crash-window confidentiality downgrade to PUBLIC — accepted honest residual, demonstrated fail-safe by M4; **K2 seat-review addendum 2026-07-11:** `atr_persist_grow` re-exercises it once per NAMED-OWNER grow, so the torn-row window and the snapshot-vs-write TOCTOU recur per grow rather than only at grant time — both degrade toward PUBLIC, the fail-safe direction); (K4) migrate-then-delete onto native unafs attributes. The first-CREATE of `UNAFS.ATR` (`atr_ensure`) is not `ns`-serialized (the F3 span rule forbids `ns` across a multi-cluster grow) — a benign latent SMP first-create race, untriggered today (single EL0 core; no named persist in the battery). **DONE by K2 (moved off this list):** flipping `by_name_spawn_multivalued()` true (M(c)); re-persisting a row's `first_cluster`/`size` on GROW (M(b)). **DONE by K3:** the fail-OPEN revoke leg. **DONE by IMG-SIG:** code-signing → `kind=IMAGE_SHA256` (the name-collision residual, closed).
    - **IMG-SIG (code-signing — 2026-07-11, QEMU-verified; the name-collision residual RETIRED).** Graduates the persistent principal from PROGRAM_NAME ("same 8.3 name = same principal", the last honest identity residual K1/K2/K3 carried) to the program's IMAGE: `load_program_into_slot` — the SOLE mint path — now stamps `PrincipalRecord::image_of(&bytes)` = `kind = IMAGE_SHA256` over the SHA-256 of the loaded image, not `program_name(name)`. Two byte-identical images share a principal (a re-spawn is re-admitted; a grown/persisted owner is re-acquired across a reboot), two DIFFERENT images under the same 8.3 name do NOT (a swapped/trojaned blob installed under a trusted name is refused `-EACCES`). **Implementation, in-lane (`arch/aarch64/syscall.rs` only), no new crate:** a no_std FIPS 180-4 SHA-256 (`sha256`/`sha256_compress`, bitwise, no heap, only the 64 round constants); `PrincipalRecord::image_sha256` stores the 30-byte digest PREFIX in the fixed `value[30]` field — 240-bit identity, collision-infeasible, and NO format bump (widening `value` would be one), so the on-disk row layout, CRC, and codec are byte-transparent (the raw digest bytes ride the same path the name string did; the native `owner` string projection is the 240-bit PREFIX form `sha256:` + 60 hex (67 chars) — NOT a 71-char full digest, which cannot be rebuilt from disk — derived by the K4-ready codec below). The K2 launcher/metal fixtures now compute the expected owner via `image_principal_of_file` (mount+read+hash, no slot) instead of a name literal. **Enforcement is principal-agnostic** — `owned_access_ok`/`owned_is_owner`/… compare `PrincipalRecord` by value, so the whole ACL machinery, the K1/K2/K3 fixtures (which MANUALLY stamp PROGRAM_NAME on scratch ASIDs and so are unaffected), and the 23-fixture battery are byte-equivalent. **Honest scope:** this is image IDENTITY, NOT authentication — SHA-256 is a plain digest, so it does NOT resist an OFFLINE attacker who recomputes the digest of a substituted image and rewrites the plaintext `UNAFS.ATR` row (the FAT card is unencrypted; no HMAC/signature is added or claimed — that needs a key in protected storage the Pi 4 lacks). What it closes is the ONLINE / same-boot substitution and the cross-reboot "same-name-different-code" adoption: a program the kernel loads and hashes gets an identity bound to its actual bytes. The K3 SMP concurrent-repersist window and the torn-row confidentiality-downgrade residual are UNCHANGED (IMG-SIG touches identity, not commit-ordering). **Cross-version migration (seat 3-lens review, 2026-07-11): legacy PROGRAM_NAME persisted rows are NOT migrated.** A card whose `UNAFS.ATR` was written by a pre-IMG-SIG kernel rebuilds its rows verbatim, but no spawn under this kernel can ever mint a PROGRAM_NAME principal — the owner is permanently un-re-acquirable (fail-CLOSED: owner-inaccessible AND owner-undeletable) until the card is re-prepped off-card (delete `UNAFS.ATR` + the owned file). Correct-direction failure (deny, never adopt); no production rows exist — only bench fixtures. Proven by `image_sig_selftest` (last in the U7 chain, read-only): FIPS KATs for `""`/`"abc"`/a 56-byte padding-overflow message; constant-buffer discrimination (byte-identical → equal principal, one-byte-different → distinct, IMAGE ≠ PROGRAM_NAME by kind); and real-image discrimination (`K2OWN.BIN` vs `K2IMP.BIN` hash to distinct stable IMAGE_SHA256 principals) — witnessed `:: IMG-SIG: … residual closed) PASS [w=0x7f/0x7f] ::` (uncounted).
    - **K4-ready (native-attribute projection codec — 2026-07-12, QEMU-verified; the IN-LANE slice of K4, landed ahead of the native unafs mount).** K4 proper ("migrate-then-delete onto native unafs attributes") reads each committed `UNAFS.ATR` row and re-stores its owner/grants as NATIVE unafs typed attributes (`owner` = the principal string, `grants:<grantee>` = the rights), then deletes the FAT-bridge sidecar. That migration is gated on a native unafs FILESYSTEM existing in the kernel — the ROADMAP §2 "BeFS" kernel-convergence chain (no_std port → block adapter → read-only mount → journaled writes + a minimal VFS for FAT/UnaFS coexistence), NONE of which is in-tree yet (`fs/mod.rs` mounts only FAT; `unaos/libs/fs/unafs` is a std Ring-3 crate). So K4-ready lands the part that does NOT need the mount: the deterministic 1:1 CODEC the migration will use, PINNED + KAT'd now. **What landed (all in `arch/aarch64/syscall.rs`, zero x86):** `principal_native_string` (a `PrincipalRecord` → its native `owner` string: `PROGRAM_NAME` → its stored `prog:<name>` verbatim; `IMAGE_SHA256` → `sha256:` + 60 lowercase hex of the 30-byte digest PREFIX; `NONE`/reserved → `None`, fail-closed); `grant_native_key` (`grants:<grantee>`); `rights_native_value` (`rw`/`r`/`w`/`-`); a no-heap `hex_lower_into`; and `classify_volume_magic` — the "tell the FAT-bridge sidecar (`UNAATR1\0`) from a real native unafs volume (`UNAFS`, mirrored from `unaos/libs/fs/unafs/src/superblock.rs`) from anything else (a FAT boot sector)" primitive the FORMAT bullet names. **THE 240-BIT-PREFIX RULE (load-bearing correctness):** an `IMAGE_SHA256` principal stores only the 30-byte (240-bit) digest PREFIX (`value[30]` is a hard cap), enforcement compares those 30 bytes, and `image_of` mints the same prefix — so the native `owner` string MUST be the 67-char prefix form (`sha256:` + 60 hex), NOT the 71-char full digest (unreconstructable from disk). Projecting a full digest would make a MIGRATED owner mismatch a FRESH mint of the same program → un-re-acquirable; the prefix form keeps migrated == fresh-mint byte-for-byte. (This CORRECTS the earlier IMG-SIG note that called the canonical form "71 chars, derived at K4".) **Proof:** `k4_ready_selftest` (last in the U7 chain, read-only, in-RAM, SYNTHETIC principals — no card, no disk, so byte-equivalent): an 8-bit witness over each projection + the discriminator + THE MIGRATION LANDMINE (a row round-tripped through the REAL `atr_serialize_row`/`atr_parse_row` codec projects IDENTICALLY to fresh mints) → `:: K4-ready: … PASS [w=0xff] ::` (uncounted). **STILL DEFERRED (K4 proper):** the native mount + the migrate-then-delete pass itself, gated on the BeFS chain above; the ratified policy already stands (legacy `PROGRAM_NAME` rows are not migrated — only `IMAGE_SHA256` rows the kernel can re-mint). The torn-row and SMP concurrent-repersist residuals are UNCHANGED (K4-ready touches projection, not commit-ordering).
    - Gate (K1+K2+K3+IMG-SIG+K4-ready): `kernel8`/`kernel8-test` 23 PASS byte-equivalent (the UNCOUNTED witness lines `:: K1-atr: … ::`, `:: K1-persist: … PASS ::`, `:: K1-corrupt: … PASS ::`, `:: K2-liveenf: … rebuild+enforce PASS [w=0x7f] ::`, `:: K3-revoke: … PASS [w=0x7f] ::`, `:: IMG-SIG: … PASS [w=0x7f/0x7f] ::`, and `:: K4-ready: … PASS [w=0xff] ::` are the only additions — PASS/FAIL space-flanked so the count stays 23) + CAPSTONE 6/6 + F2/F3 witnesses locked 240000/240000, zero R1/CMD13; `check` BOTH arches (x86 unchanged); `test-arm` MISSION SUCCESS. Lane: `arch/aarch64/syscall.rs` (+ K2's seat-granted build lane: `arroyo`/`crates/user-blob`); zero x86 behavioural change. 🔬 QEMU-green — the true cross-reboot power-cycle survival (a real prog:X owner file surviving a power-cycle, admitted-by-name on the next boot, impostor denied) is metal-attended (QEMU same-boot SIMULATEs the reboot). K2 metal-verify rides the next attended bench: one boot on the 16 GB `UNAOS` card should show 23 PASS + CAPSTONE 6/6 + `K2-liveenf … PASS [w=0x7f]` + the K1 lines, zero R1/CMD13; a pristine card (delete stale `UNAFS.ATR`) is the pre-condition for the power-cycle money-shot.
- [x] EL0 first-entry FP/SIMD scrub (M6f Part 0, 2026-07-02, QEMU-verified; exercised transitively on metal 2026-07-04 — every M6f fixture entered through the scrubbed trampoline and ran correctly on silicon) — `user_task_trampoline` zeroes all of `v0-v31` (32× `movi vN.2d,#0`), resets `FPSR`/`FPCR`, and zero-inits `TPIDR_EL0`/`TPIDRRO_EL0`, immediately after the x0–x30 scrub and before the first `eret`. `CPACR_EL1.FPEN=0b11` makes the whole FP file EL0-readable and the `+neon` kernel autovectorizes, so this closes the leak the GPR-only scrub left. Scope is honest: this covers **first entry only** — the preempt-RESUME path restores each task's own saved `v0-v31`/FPSR/FPCR from its `__vec_irq` frame, so no cross-task leak there either. Exercised transitively (every M6f EL0 fixture enters through the scrubbed trampoline and runs correctly); there is no negative FP-residue read test yet, so "no kernel FP value is architecturally readable at entry" rests on the code path, not a fixture that reads the file.

#### aarch64 W^X map audit (H1, M6c — report only; WXN not enabled)

Walk of the EL1&0 boot page tables as built in `arch/aarch64/boot.rs` (`build_l1`,
`protect_user_code`; TTBR0_EL1 → one L1 of 1 GiB blocks, with L1[0] demoted to L2_USER/L3_USER for
the 4 KiB EL0 window). `AP[7:6]`: `0b00` = EL1-RW/EL0-none, `0b01` = EL1+EL0 RW, `0b11` = RO at both
ELs. `UXN` = bit 54 (EL0 execute-never), `PXN` = bit 53 (EL1 execute-never).

| Region (VA == PA) | Descriptor | Attr | AP | EL1 | EL0 | UXN/PXN | W∧X? |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 0–1 GiB general RAM (L2_USER blocks + L3_USER pages outside `USER_REGION`) | 2 MiB block / 4 KiB page | Normal | `0b00` | RW+X | — | 0/0 | **yes (EL1 RWX)** |
| `USER_REGION` code page `[0x100000,0x101000)` after `protect_user_code` | 4 KiB page | Normal | `0b11` | RO, no-X | R+X | 0/1 | no |
| `USER_REGION` data/stack `[0x101000,0x104000)` | 4 KiB page | Normal | `0b01` | RW, no-X | RW, no-X | 1/1 | no |
| 1–2 GiB RAM (L1[1]) | 1 GiB block | Normal | `0b00` | RW+X | — | 0/0 | **yes (EL1 RWX)** |
| 2–3 GiB RAM (L1[2]) | 1 GiB block | Normal | `0b00` | RW+X | — | 0/0 | **yes (EL1 RWX)** |
| 3–4 GiB Device (L1[3]) | 1 GiB block | Device | `0b00` | RW, no-X | — | 1/1 | no |

Transient: the code page is a data page (`0b01`, UXN=PXN=1, RW no-X) during the blob copy, then
`protect_user_code` flips it to the row above — so it is never W∧X, at either EL, at any point.

Since M6d the two `USER_REGION` rows are also **non-global (`nG=1`)** and replicated per task: the shared
window is the ASID-0 boot context, and up to 8 per-task slots (`boot::alloc_user_slot`, ASID 1..8) map the
same two VAs to their own frames with identical AP/UXN/PXN. The W^X properties of both rows are unchanged
(nG affects only TLB ASID-tagging, not access permission); the VAs shown are illustrative and shift with
the kernel image size (the pool adds ~224 KiB BSS). Kernel rows stay Global (`nG=0`).

**Every EL0-reachable page already satisfies W^X**: the user code page is R+X but read-only, and the
data/stack pages are RW but execute-never. The only W∧X coincidences are in the **kernel's own coarse
RWX identity RAM** (0–3 GiB, `AP=0b00`, PXN=0): the kernel image `.text` (which must be executable)
shares one RWX attribute set with the heap/stack/BSS/data (which must be writable).

**SCTLR_EL1.WXN feasibility.** With `WXN=1` the EL1&0 regime forces "writable ⇒ execute-never":
EL1-writable regions become PXN, EL0-writable regions become UXN. The EL0 window is unaffected (the
code page is not writable at either EL; data/stack are already XN). But the kernel executes from the
`AP=0b00`, PXN=0 identity RAM above — under WXN that RAM becomes EL1-non-executable and the next
kernel instruction fetch faults, so the kernel cannot boot. Enabling WXN therefore requires FIRST
splitting the identity map into a read-only-executable kernel text/rodata region and RW-execute-never
data/heap/stack/BSS regions (a linker-symbol-driven map, mirroring the 4 KiB EL0 code/data split M6b
already performs). That refactor is a separate, review-gated change; **WXN is not enabled in M6c.**

### Ring-0 parser audits (today's practical attack surface)
- [ ] Network stack: header-length/bounds audit (Ethernet/ARP/IP/TCP options/DHCP options)
- [ ] USB: descriptor parsing bounds (config/interface/endpoint/HID report walks; hub paths)
- [ ] FAT: BPB/dirent/FAT-chain bounds and loop guards (partially hardened during the read-only arc — re-verify and record)
- [x] unafs RO mount (UNAFS-K3): bound every disk-derived size/count/offset vs volume geometry (superblock `bitmap_blocks`, inode `size`, extent arithmetic, codec lengths; `try_reserve` not `with_capacity`, size-limited decode) — **DONE (BEFS-HARDEN, 2026-07-13)**: `Superblock::validate` at the parse boundary; `try_reserve` on the bitmap load and `read_from_extents`; `checked_*` extent arithmetic + past-volume extent rejection; bounded bulk hole fill and free walks; checked spilled-extent sums on the query/get_attribute paths (the QSIM-flagged sites); two-tier bincode decode budgets (8 KiB block records / 4 MiB extent-backed records, pinned under the kernel heap by a regression test). 23 hostile-volume fixtures assert `Err`-not-panic (`unaos/libs/fs/unafs/tests/hostile_volume.rs`); golden format KATs byte-identical

#### unafs kernel WRITES — the K4 write-path honest scope (UNAFS-K4, 2026-07-14; ✅ METAL-CONFIRMED 2026-07-14 attended Pi 4 bench — `K4-write PASS [w=0x7f]` on 3 cold boots with a GENUINE power-cut between each, boot-2 remount CLEAN: the journaled write was durable across real power loss)

The UNAFS-K3 mount became read-WRITE at K4: `fs/unafs.rs`'s `write_sector` routes to
`drivers::block::write_block` (emmc2 CMD24 + R1/CMD13 status checks). Stated honestly:

- **Coherence.** Writes make a per-call mount a corruption hazard — two live mounts hold two
  independent in-RAM allocation bitmaps + journal cursors, so a block one frees the other can
  re-hand-out. Closed structurally: exactly ONE process-wide, IRQ-masked mount (`with_unafs`,
  the F3 `NAMESPACE`-lock discipline) is the sole path for read AND write, so there is one
  authoritative in-RAM bitmap/journal and every operation is serialized. A pure read never
  mutates, so it never triggers the crate's `Drop`-time metadata write-back — reads stay
  genuinely read-only, even against a dirty volume.
- **Torn-write safety is by ORDERING, not by journal replay.** The `unaos/libs/fs/unafs` WAL is
  **intent-logging only**: it records BeginOp/EndOp markers to DETECT a torn transaction on the
  next mount (a dirty warning), it does NOT store undo/redo data and there is NO replay or
  rollback ("Log only for now"). Crash safety comes from the crate's per-mutation ordering
  (new-extents-first, single-block metadata swap, free-last), so the failure mode of an ill-timed
  power cut is a LEAK (allocated-but-unreachable blocks, reclaimable by a future fsck/scavenger
  arc), never a dangling reference to a freed block. Recovery on a dirty mount does NOT write —
  it warns — so a read-only consumer can still mount-and-warn safely.
- **The 4096↔512 atomicity gap (residual, ledgered).** The crate's "single-block metadata swap
  is the atomic point" holds only at its 4096 B block granularity. The medium is 512 B sectors,
  and `BlockAdapter::write_block` is eight sequential, individually-non-atomic `write_sector`s,
  so a power cut mid-block tears the 4096 B block across sector boundaries. The swap is therefore
  truly atomic ONLY when the live record fits the FIRST 512 B sector (inode ≤ 512 B, directory
  data ≤ 512 B) — true for small files/directories (the K4-write witness's case), NOT guaranteed
  for a large inode or directory that spans multiple sectors. Closing this needs sector-level
  journaling or a format change — deferred; the frozen on-disk format + KATs are LOCKED and were
  NOT touched (the journal is partial *by design*, not a gap this arc fixes).
- **Proof.** `:: K4-write: … PASS [w=0x7f] ::` (uncounted) — create + write a scratch file, force
  a genuine remount (fresh in-RAM bitmap/journal re-read from disk), byte-verify the durable
  write, delete, remount (delete durable), negative path, clean journal. Self-cleaning. Proven
  under `kernel8-test` via `if=sd` write-back (the K2 M(e) same-image technique). The metal
  write→REAL power-cycle→boot-2 byte-verify rides the next attended Pi bench (not attempted here).

### Process & supply chain
- [ ] Adversarial review before metal and before merge on every arc (standing rule, `CLAUDE.md`)
- [ ] Code-signing / "self vs non-self" loader check (design: `dev/OS/04_SECURITY_IMMUNITY/intrusion_detection.md`) — **U2 (x86, 2026-07-02) and M6g (aarch64, 2026-07-03) both landed the loadable-program path but NOT this check**: on both arches the FAT-loaded program is bounded only by size and contained by hardware isolation (ring-3/EL0 + per-page perms + the fault-kill net — see each arch ledger's untrusted-loader item). The gate must cover both loaders. Simple allowlist first, signatures when entropy + crypto land
