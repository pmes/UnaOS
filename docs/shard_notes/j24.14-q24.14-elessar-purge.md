## YYYY-MM-DD - Dynamic Payload Truncation
**Anomaly:** MatrixScanner's semantic_dag included headers that caused .contains() checks to fail on subsequent clicks, freezing the UI payload and refusing new topologies.
**Resolution:** Replaced boolean checks with .find() and .truncate() to surgically slice off stale context headers before appending fresh topology, enabling zero-latency hot-swaps.