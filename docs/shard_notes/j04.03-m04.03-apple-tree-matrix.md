## 2026-10-27 - [Architectural Audit & Blueprint]
 **Structural Anomalies Found:** AppKit layout routing requires dynamic responsive constraint toggling to shift from staggered chat (matrix) to single-column (mobile/shrunk view), bounded around a 450pt threshold.
 **The Woz Blueprint:** Build `UnaFlippedDocumentView` with a `setFrameSize:` override (calling `msg_send![super(self, NSView::class()), setFrameSize: new_size]`). Manage state arrays of `NSLayoutConstraint` for X-axis positioning inside an `NSStackView` acting as the Y-axis engine. Toggle constraints dynamically.