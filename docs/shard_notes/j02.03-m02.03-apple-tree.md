## 2026-03-30 - [Architectural Audit & Blueprint: Rebuilding macOS Native UI from J02.02]
 **Structural Anomalies Found:** J02.02 failed catastrophically due to mixing Objective-C block memory management (via `block2::RcBlock`) with standard `dispatch_async` routing in `spline.rs`, resulting in memory leak traps. Further, J02.02 violated the `objc2` v0.6.4 API syntax boundaries, failed to explicitly mark delegates with `MainThreadOnly` (via inheriting `NSResponder`), failed to implement `NSObjectProtocol` for custom classes, incorrectly used mutable struct `define_class!` definitions rather than unit structs with `Ivars`, and attempted to rely on GTK layout terminology rather than native `NSLayoutConstraint` arrays with `translatesAutoresizingMaskIntoConstraints = NO`.

 **The Woz Blueprint:** Apply strict Can-Am rules to rebuild `libs/quartzite/src/platforms/macos`. Replace all `block2` usage in `spline.rs` cross-thread dispatch boundaries with `dispatch2::DispatchQueue::main().exec_async()`. Subclass all custom app/workspace delegates from `NSResponder` instead of `NSObject` to properly inherit `MainThreadOnly` requirements. Use explicit `Ivars` attached to unit structs via `#[ivars = StructIvars]` for storing `Retained<T>` references wrapped in `RefCell`s to tie UI component lifecycles properly to the delegate objects. Use `objc2::sel::Sel` for selectors, `msg_send!`, and explicit `NSArray::from_slice` for fully wired programmatic `NSLayoutConstraint` assemblies spanning the split view (`sidebar.rs` and `comms.rs`).

## 2026-03-30 - [Post-Commit Execution Audit & Re-Calibration]
 **Structural Anomalies Found:** Several syntax violations, incomplete UI bindings, runtime panics, and unresolved path boundaries required subsequent intervention after the initial pass.
 **The Woz Blueprint (Subsequent Fixes Applied):**
1. **Removed `msg_send_id!` Macro Violation:** Purged all usages across `toolbar.rs`, `sidebar.rs`, `comms.rs`, and `mod.rs`, replacing them with `msg_send!`.
2. **Restored AppKit Constants:** Stripped custom `NSString` constant generators and reverted to using the native static `NSToolbarFlexibleSpaceItemIdentifier` and `NSToolbarToggleSidebarItemIdentifier`.
3. **Implemented Missing OutlineView Delegate:** Completed `outlineView:viewForTableColumn:item:` in `sidebar.rs` to render a native `NSTableCellView` housing an explicitly constrained `NSTextField`.
4. **Wired `SMessage::StateInvalidated` (No Stubs):** Removed the `TODO` in `spline.rs` and implemented `NSNotificationCenter` dispatch to ping an observer in the workspace to safely trigger `.reloadData` on the main thread.
5. **Wired Comms Input Dispatch (No Stubs):** Implemented string extraction from `NSTextView` in `comms.rs` and cleared the buffer correctly upon dispatching the `insertNewline:` event.
6. **Fixed Tokio Runtime Panic:** Removed an illegal nested `let rt = Handle::current();` execution within a detached OS thread in `spline.rs`, relying safely on the `tokio::sync::broadcast` channel's synchronous `.blocking_recv()`.
7. **Fixed Array Type Mismatches:** Cast dynamically allocated `Retained<NSString>` constants to `&NSToolbarItemIdentifier` via explicit `&*var` syntax to satisfy strict uniform type bounds within `NSArray::from_slice`.
8. **Fixed `null_mut` Option Pointer Mismatches:** Updated all raw pointer passing (`core::ptr::null_mut::<AnyObject>()`) to `objc2`'s strictly-typed `None::<&AnyObject>` mapping inside message send contexts.
9. **Enabled Cross-Crate Features:** Fixed the global build blocker by exposing the `macos` feature within `libs/quartzite/Cargo.toml` and correctly mapping it inside `apps/lumen/Cargo.toml`.
10. **Refactored `NSString` Memory Allocations:** Eliminated unsafe, malformed manual C-string allocations `msg_send![sel!(initWithUTF8String:), b"...\0".as_ptr()]` and updated them to the safe `objc2_foundation` wrapper: `NSString::from_str("...")`.
11. **Fixed Strict `objc2` Scope Paths:** Relocated `MainThreadOnly` to `objc2::MainThreadOnly`, moved `Sel` to `objc2::runtime::Sel`, and strictly imported geometry structures (`NSRect`, `NSSize`, `NSPoint`) directly from `objc2_foundation`.
12. **Implemented AppKit Super-Protocols:** Defined empty `unsafe impl` blocks for undocumented parent protocol requirements on delegates, notably `NSControlTextEditingDelegate` (for OutlineView) and `NSTextDelegate` (for TextView).
13. **Exported `MacOSSpline` Architecture:** Wrote the cross-platform constructor signature (`bootstrap()`) and exported the correct type to fulfill `libs/quartzite/src/spline.rs` architectural imports.
14. **Stripped Unauthorized `anyhow` Dependency:** Refactored `mod.rs` to use standard `Result<(), String>` rather than pulling the external `anyhow` crate inside the platform UI code.
15. **Fixed `Sel` Path Binding:** Changed raw pointer `null_mut::<objc2::sel::Sel>()` initializations directly to `null_mut::<Sel>()` corresponding with updated macro scopes.
16. **Removed Orphaned `foundation::` Prefixes:** Cleared the final module resolution error by referencing `NSRect::ZERO` and `NSRect { ... }` explicitly without the invalid namespace wrapper.
