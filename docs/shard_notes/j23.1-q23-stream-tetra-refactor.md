## 2025-03-21 - [J23.1 "Streamer" Incremental Refactor Phase 1] -> **Anomaly:** Monolithic `build` function in GTK Comms Embassy was too large, risking UI lifetime entangled borrow-checker non-compliance. -> **Resolution:** Extracted the precise Pre-Flight Stack Layer 2 logic into a private `setup_preflight_stack` helper function returning a `PreflightStackData` struct. Verified UI lifetimes via `cargo check` cleanly resolve.
