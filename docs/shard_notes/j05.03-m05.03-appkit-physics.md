## 2026-05-03 - [J05.03 "Caliper": AppKit Physics & Geometry Overhaul]
**Anomaly:** `NSBox` CoreAnimation rendering pipeline out-of-order execution, `toggle_expansion` cache invalidation failing for multiline text, and hardcoded `setPreferredMaxLayoutWidth` bounding box creating rigid layouts instead of flowy iMessage-style staggering.
**Resolution:** [CAN-AM Rule Applied] Geometry constraint bounds compounding updated: AppKit constraints correctly set dynamically. Bound `LESS_THAN_OR_EQUAL` with `.75` multiplier. Configured leading and trailing inequality bounds to `NSLayoutPriorityRequired`. Corrected `NSBox` `wantsLayer` to activate before rendering commands. Refactored `toggle_expansion` to directly query dequeued cell instances via `viewAtColumn:row:makeIfNecessary:` to trigger native height re-calculation on embedded textfields via `invalidateIntrinsicContentSize`. Code explicitly reviewed through native workspace verification checking AST without relying strictly on external crates.
