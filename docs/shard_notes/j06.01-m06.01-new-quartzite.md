## 2024-04-11 - [Initialization of Quartzite Crate]
**Anomaly:** [None yet, starting initialization]
**Resolution:** [Setting up the initial crate and structure]

## 2024-04-11 - [History Pipeline Correction & Handoff]
**Anomaly:** Attempted to implement native history loading but misunderstood the user's intent regarding `legacy/lumen`, `BrainManager` and mock data. The user actually wanted the validation vessel (`apps/lumen`) to run with the *dummy* local history items *exactly* as initially provided, while mirroring the precise structural layout format and iterator directions (old to new) used by the existing legacy code. The user emphasized that changes to the core codebase or file structure were forbidden without explicit instruction.
**Resolution:** Reverted `apps/lumen/src/main.rs` back to using the pure local memory data (`VecDeque::new()` initialized with dummy `HistoryItem`s like Alice and Bob), and strictly ensured the procedural macro `quartzite_macros` generator iterated over them from Oldest to Newest, outputting `10:00 AM` before `10:05 AM`.

**Handoff to Next Jules:**
If tasked with implementing actual `HistoryItem` reads from the host machine disk in the future, the exact architectural path is as follows:
1. Ensure `UnaPaths::awaken()` is called to establish the spatial OS layout.
2. The actual file location representing the vault where history is stored is returned by `UnaPaths::primary_vault()`.
3. Use `gneiss_pal::persistence::BrainManager::new(UnaPaths::primary_vault())` to construct the data reader.
4. Call `.load()` on the `BrainManager` to retrieve `Vec<SavedMessage>`.
5. Map these `SavedMessage` instances into `bandy::state::HistoryItem` records, mapping the `msg.role == "user"` to `Origin::LocalUser` and `msg.role == "model"` to `Origin::System`. Ensure they are inserted into the `AppState.history` vector such that the oldest messages are at the lowest indexes, matching chronological order (oldest to newest, top to bottom), just like how it is currently mocked. Do not modify existing codebase UI architectures or hardcode string paths like "brain_data.json".
