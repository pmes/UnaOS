## 2026-10-24 - [Compilation Restoration & Depth Mapping]
**Anomaly:** Tunnel-vision focus on the DMZ `tetra.rs` resulted in mutating `TetraNode::Matrix` into a tuple variant without updating the master orchestrator in `apps/lumen/src/main.rs`. This left the main entry point uncompilable. Additionally, mapping a recursive DAG structure directly into a 1D flat array completely eliminated the visual hierarchy.
**Resolution:** Restored orchestrator compilation by updating `TetraNode::Matrix` initialization to use `MatrixTetra::default()`. Extended the `ExpandableList::flatten()` method to track and return recursion `depth` along with the `TreeNode` reference. Updated GTK (`sidebar.rs`) and Qt (`vein_bridge.rs`) painters to prepend dynamic indentation (`"  ".repeat(depth.saturating_sub(1)) + "├─ "`) to the label strings, using a `depth == 0` guard to prevent `usize` underflow.
