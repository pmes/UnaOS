## 2026-04-02 - [Fix define_class! Macro Panic and Memory Review]
**Anomaly:** The `define_class!` macro caused a compiler panic due to standard Rust methods (such as `append_stream_token`) being placed inside the macro invocation for `CommsDelegate`. The macro syntax strictly rejects standard Rust `impl` logic.
**Resolution:** Extracted `append_stream_token` out of the `define_class!` macro and placed it into a separate, standard `impl CommsDelegate { ... }` block. Additionally, verified that `Retained<AnyObject>` and `AnyObject` are handled properly with `.clone()` across the FFI boundaries and `tokio::spawn` dispatches correctly using `MainThreadMarker` internally. Dead code related to previous UI implementations (`matrix_bubble.rs`) was purged.