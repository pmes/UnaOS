## 2026-03-31 - [Architectural Audit & Blueprint]
**Structural Anomalies Found:** J02.03 and predecessors failed due to memory leaks from `block2`, incorrect type casting (`Retained::cast`), raw pointers in Objective-C APIs, missing `unsafe` blocks for methods, and missing `MainThreadOnly` markers by subclassing `NSObject` instead of `NSResponder`.
**The Woz Blueprint:** The J02.04 shard has re-engineered `libs/quartzite/src/platforms/macos` from the ground up:
- Mapped `AppDelegate` and `MainThreadMarker` correctly via `NSResponder` for `MainThreadOnly` traits.
- Replaced `block2` routing with `dispatch2::DispatchQueue::main().exec_async` in `spline.rs` and `sidebar.rs`.
- Enforced two-step `alloc`/`init` object initialization.
- Mapped explicit, strongly-typed `NSLayoutConstraint` arrays for the `comms.rs` right pane layout.
- Stubbed the Data source in `comms.rs` and `toolbar.rs` per the directive amendment, while `sidebar.rs` ingested `SMessage::Matrix` payloads correctly.
