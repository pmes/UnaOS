# UNAOS Environment Knobs Inventory

This document is automatically generated by `tools/extract-env-knobs.py`. It lists every `UNAOS_` environment variable found in the source tree.

## `UNAOS_BAREMETAL`

- **CLAUDE.md:14**
  ```
  Env knobs: `UNAOS_PI`, `UNAOS_BAREMETAL`, `UNAOS_SKIP_XHCI`, `UNAOS_BOOTLOG`,
  ```
- **unaos/crates/kernel/Cargo.toml:39**
  ```
  # builds a BootInfo, and calls kernel_main. Implies `pi`. Enable via `UNAOS_BAREMETAL=1`.
  ```

## `UNAOS_BOOTDIAG`

- **docs/dev/DEBUGGING.md:129**
  ```
  `UNAOS_BOOTDIAG=1` on the first boot of a new build to dump the firmware id,
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3212**
  ```
  Bootloader Started`). With `UNAOS_BOOTDIAG=1` it first prints the `BOOTDIAG:`
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3251**
  ```
  ### `BOOTDIAG` block (`UNAOS_BOOTDIAG=1`)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3282**
  ```
  The Orin booted the `UNAOS_BOOTDIAG=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` media
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3381**
  ```
  attempt used `UNAOS_BOOTDIAG=1` media; with the corrected GUID, BOOTDIAG now reaches
  ```
- **unaos/arroyo:288**
  ```
  # UNAOS_BOOTDIAG=1 turns on the BOOTLOADER's `bootdiag` feature — an additive UEFI boot-time
  ```
- **unaos/arroyo:295**
  ```
  [ -n "${UNAOS_BOOTDIAG:-}" ] && _bfeats="${_bfeats}bootdiag,"
  ```
- **unaos/crates/bootloader/Cargo.toml:13**
  ```
  # non-bootdiag build is byte-identical. Enabled by arroyo via UNAOS_BOOTDIAG=1.
  ```
- **unaos/crates/bootloader/src/main.rs:98**
  ```
  /// UEFI boot-time diagnostics (`bootdiag` feature; enabled via `UNAOS_BOOTDIAG=1`). For the Jetson
  ```
- **unaos/crates/bootloader/src/main.rs:282**
  ```
  // Boot-time diagnostics (UNAOS_BOOTDIAG=1 → `bootdiag` feature). Additive and OFF by default
  ```
- **unaos/crates/kernel/src/arch/aarch64/fdt_tegra.rs:14**
  ```
  // UNAOS_BOOTDIAG fdt panic, JM2 era). This walker is a bounded big-endian token scan — no
  ```
- **unaos/scripts/jetson-serial-bridge.py:30**
  ```
  # After launch, expect (with UNAOS_BOOTDIAG=1): the `BOOTDIAG:` block, the bootloader proceeding ...
  ```

## `UNAOS_BOOTLOG`

- **CLAUDE.md:14**
  ```
  Env knobs: `UNAOS_PI`, `UNAOS_BAREMETAL`, `UNAOS_SKIP_XHCI`, `UNAOS_BOOTLOG`,
  ```
- **docs/dev/DEBUGGING.md:56**
  ```
  - **Knobs:** `UNAOS_USBDEBUG=1` (debug console view), `UNAOS_BOOTLOG=1`
  ```
- **docs/dev/OS/08_VIDEO/framebuffer.md:111**
  ```
  | `UNAOS_BOOTLOG=1` | Hold the fbcon boot log on screen + print the EDID/mode-selection readout, ...
  ```
- **unaos/HARDWARE-BRINGUP.md:78**
  ```
  of the picture. Add `UNAOS_BOOTLOG=1` to hold the boot log on screen and read the EDID/mode line.
  ```
- **unaos/HARDWARE-BRINGUP.md:93**
  ```
  EDID parser here (it didn't on Apple EFI); a `UNAOS_BOOTLOG=1` build will show `EDID read: source...
  ```
- **unaos/arroyo:45**
  ```
  # UNAOS_BOOTLOG=1 holds the boot log on screen instead of handing off to the GUI;
  ```
- **unaos/arroyo:60**
  ```
  [ -n "${UNAOS_BOOTLOG:-}" ] && _feats="${_feats}bootlog,"
  ```
- **unaos/builder/src/main.rs:38**
  ```
  // UNAOS_BOOTLOG=1 (hold the boot log on screen instead of the GUI), UNAOS_USBDEBUG=1 (run the
  ```
- **unaos/builder/src/main.rs:50**
  ```
  if std::env::var("UNAOS_BOOTLOG").is_ok() { feats.push("bootlog"); }
  ```
- **unaos/crates/kernel/Cargo.toml:30**
  ```
  # whole boot log can be photographed on serial-less hardware. Enable via `UNAOS_BOOTLOG=1`.
  ```

## `UNAOS_CORE3PROBE`

- **unaos/arroyo:821**
  ```
  # CORE3-PROBE: UNAOS_CORE3PROBE=1 adds the `core3probe` feature — the aarch64 secondary entry
  ```
- **unaos/arroyo:824**
  ```
  if [ -n "${UNAOS_CORE3PROBE:-}" ]; then
  ```
- **unaos/arroyo:826**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_CORE3PROBE: secondary stub raw-dumps MPIDR/EL/x0 to PL011 (see ...
  ```
- **unaos/crates/kernel/Cargo.toml:316**
  ```
  # OFF => the secondary stub is byte-identical. Enable via `UNAOS_CORE3PROBE=1 UNAOS_PI=1 ./arroyo
  ```
- **unaos/crates/kernel/src/arch/aarch64/smp.rs:83**
  ```
  // CORE3-PROBE (UNAOS_CORE3PROBE=1). Identical to the stub above, but with a raw-PL011 dump prologue
  ```
- **unaos/scripts/core3-probe-bench.md:44**
  ```
  UNAOS_CORE3PROBE=1 UNAOS_PI=1 ./arroyo kernel8
  ```

## `UNAOS_CPU`

- **docs/dev/OS/02_KERNEL_CORE/scheduler.md:25**
  ```
  launched with `+x2apic`; `UNAOS_CPU=qemu64` forces the xAPIC fallback path for
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1213**
  ```
  **Gates (RMBP-FIX).** `./arroyo check` green both arches. `./arroyo test 22` + `UNAOS_CPU=qemu64 ...
  ```
- **review/unaos-default-quiet-LANDING.md:83**
  ```
  UNAOS_CPU=qemu64 ./arroyo test 22 → MISSION SUCCESS · 21 -> PASS   (xAPIC path)
  ```
- **review/unaos-gui-witness-LANDING.md:87**
  ```
  - `UNAOS_CPU=qemu64 ./arroyo test 22` — `✅` complete; MISSION SUCCESS; ring landed (4).
  ```
- **review/unaos-install-core-LANDING.md:65**
  ```
  - `UNAOS_CPU=qemu64 ./arroyo test 22` (knob OFF, xAPIC fallback): FAIL scan = 0.
  ```
- **review/unaos-kernel-clock-LANDING.md:68**
  ```
  - `UNAOS_CPU=qemu64 ./arroyo test 22` (xAPIC, MISSION) — `xAPIC software-enabled` (ids 0–3),
  ```
- **review/unaos-kernel-clock-LANDING.md:76**
  ```
  - `UNAOS_CPU=qemu64 UNAOS_SCHED_DEMO=1 ./arroyo test 45` (xAPIC demo, bonus) —
  ```
- **review/unaos-rmbp-fix-LANDING.md:90**
  ```
  - `UNAOS_CPU=qemu64 ./arroyo test 22` — MISSION SUCCESS, **0 FAIL**.
  ```
- **review/unaos-sched-polish-LANDING.md:91**
  ```
  - `UNAOS_CPU=qemu64 ./arroyo test 22` (xAPIC) — `MISSION SUCCESS`.
  ```
- **review/unaos-sched-polish-LANDING.md:96**
  ```
  - `UNAOS_CPU=qemu64 UNAOS_SCHED_DEMO=1 ./arroyo test 30` (xAPIC) — same five PASS.
  ```
- **review/unaos-x-net-phy-LANDING.md:73**
  ```
  | `UNAOS_CPU=qemu64 ./arroyo test 22` (x86) | `MISSION SUCCESS`; `SOCK-1..3/5` witnesses **OK** |
  ```
- **unaos/builder/src/main.rs:309**
  ```
  // Override with UNAOS_CPU — e.g. `UNAOS_CPU=qemu64` drops x2APIC to test the xAPIC fallback.
  ```
- **unaos/builder/src/main.rs:309**
  ```
  // Override with UNAOS_CPU — e.g. `UNAOS_CPU=qemu64` drops x2APIC to test the xAPIC fallback.
  ```
- **unaos/builder/src/main.rs:310**
  ```
  let cpu = std::env::var("UNAOS_CPU").unwrap_or_else(|_| "qemu64,+x2apic".into());
  ```

## `UNAOS_EHCICONFIG`

- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:718**
  ```
  feature (`UNAOS_EHCICONFIG=1`, mapped in both `arroyo` and `builder/src/main.rs`;
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:766**
  ```
  **QEMU result (knob-on, `UNAOS_EHCICONFIG=1 UNAOS_EHCISCOUT=1`).** The harness `usb-ehci`
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:926**
  ```
  (no census spam, no redundant pre-init wake). `UNAOS_EHCISCOUT=1`/`UNAOS_EHCICONFIG=1` still
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:932**
  ```
  `UNAOS_EHCISCOUT=1 UNAOS_EHCICONFIG=1`, and `UNAOS_NOEHCIHID=1 UNAOS_EHCICONFIG=1`
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:932**
  ```
  `UNAOS_EHCISCOUT=1 UNAOS_EHCICONFIG=1`, and `UNAOS_NOEHCIHID=1 UNAOS_EHCICONFIG=1`
  ```
- **unaos/arroyo:89**
  ```
  [ -n "${UNAOS_EHCICONFIG:-}" ] && _feats="${_feats}ehciconfig_run,"
  ```
- **unaos/builder/src/main.rs:75**
  ```
  // EHCI-2 configure-and-relook scout: UNAOS_EHCICONFIG=1 fires a knob-gated minimal EHCI wake
  ```
- **unaos/builder/src/main.rs:80**
  ```
  if std::env::var("UNAOS_EHCICONFIG").is_ok() { feats.push("ehciconfig_run"); }
  ```
- **unaos/builder/src/main.rs:360**
  ```
  if std::env::var("UNAOS_EHCISCOUT").is_ok() || std::env::var("UNAOS_EHCICONFIG").is_ok() || ehcih...
  ```
- **unaos/builder/src/main.rs:364**
  ```
  } else if std::env::var("UNAOS_EHCICONFIG").is_ok() {
  ```
- **unaos/builder/src/main.rs:365**
  ```
  println!("   UNAOS_EHCICONFIG: usb-ehci controller attached — EHCI configure-and-relook scout tar...
  ```
- **unaos/crates/kernel/Cargo.toml:95**
  ```
  # =1 / UNAOS_EHCICONFIG=1 arm the probes; pair with UNAOS_NOEHCIHID=1 for probe-without-driver.
  ```
- **unaos/crates/kernel/Cargo.toml:98**
  ```
  # EHCI-2 configure-and-relook scout: UNAOS_EHCICONFIG=1 arms a knob-gated, minimal EHCI WAKE sequ...
  ```
- **unaos/crates/kernel/src/arch/x86_64/pci.rs:210**
  ```
  // EHCI-2 configure-and-relook (opt-in probe, UNAOS_EHCICONFIG=1 -> `ehciconfig_run`): after the ...
  ```
- **unaos/crates/kernel/src/drivers/ehci_scout.rs:291**
  ```
  // EHCI-2 — configure-and-relook mode (UNAOS_EHCICONFIG=1, feature `ehciconfig`).
  ```

## `UNAOS_EHCIHID`

- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:805**
  ```
  `qh.rs` schedule structures), armed by `UNAOS_EHCIHID=1` (feature `ehcihid`, implies
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:873**
  ```
  at the sitting by the M1 witness line. `UNAOS_EHCIHID` stays **default-OFF until the
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:938**
  ```
  default** now (was `UNAOS_EHCIHID`-gated); `UNAOS_NOEHCIHID=1` restores the pre-fold harness
  ```
- **unaos/crates/kernel/Cargo.toml:107**
  ```
  # EHCI-3 driver: UNAOS_EHCIHID=1 arms the minimal EHCI HID driver — the shared EHCI-2 wake, port
  ```
- **unaos/crates/kernel/src/drivers/mod.rs:7**
  ```
  // EHCI-3: the minimal EHCI HID driver (UNAOS_EHCIHID=1) — the 2012 rMBP internal keyboard/
  ```

## `UNAOS_EHCISCOUT`

- **docs/MILESTONES.md:690**
  ```
  **Knob-gated, default-OFF** (`UNAOS_EHCISCOUT=1`, mapped in `arroyo` + `builder/src/main.rs`); kn...
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:684**
  ```
  (`UNAOS_EHCISCOUT=1`, mapped in both `arroyo` and `builder/src/main.rs`). Knob-off: the module
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:766**
  ```
  **QEMU result (knob-on, `UNAOS_EHCICONFIG=1 UNAOS_EHCISCOUT=1`).** The harness `usb-ehci`
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:926**
  ```
  (no census spam, no redundant pre-init wake). `UNAOS_EHCISCOUT=1`/`UNAOS_EHCICONFIG=1` still
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:932**
  ```
  `UNAOS_EHCISCOUT=1 UNAOS_EHCICONFIG=1`, and `UNAOS_NOEHCIHID=1 UNAOS_EHCICONFIG=1`
  ```
- **unaos/arroyo:82**
  ```
  # EHCI-1 scout: read-only EHCI reconnaissance probe (x86_64). UNAOS_EHCISCOUT=1 fires the read-only
  ```
- **unaos/arroyo:85**
  ```
  [ -n "${UNAOS_EHCISCOUT:-}" ] && _feats="${_feats}ehciscout_run,"
  ```
- **unaos/builder/src/main.rs:69**
  ```
  // EHCI-1 scout: UNAOS_EHCISCOUT=1 fires the STRICTLY READ-ONLY EHCI reconnaissance census probe
  ```
- **unaos/builder/src/main.rs:74**
  ```
  if std::env::var("UNAOS_EHCISCOUT").is_ok() { feats.push("ehciscout_run"); }
  ```
- **unaos/builder/src/main.rs:352**
  ```
  // EHCI-1 scout (UNAOS_EHCISCOUT=1): give the read-only EHCI probe a QEMU target. q35's default
  ```
- **unaos/builder/src/main.rs:360**
  ```
  if std::env::var("UNAOS_EHCISCOUT").is_ok() || std::env::var("UNAOS_EHCICONFIG").is_ok() || ehcih...
  ```
- **unaos/builder/src/main.rs:367**
  ```
  println!("   UNAOS_EHCISCOUT: usb-ehci controller attached — read-only EHCI scout target");
  ```
- **unaos/crates/kernel/Cargo.toml:85**
  ```
  # EHCI-1 scout: UNAOS_EHCISCOUT=1 arms a STRICTLY READ-ONLY EHCI reconnaissance probe (PCI-config +
  ```
- **unaos/crates/kernel/Cargo.toml:89**
  ```
  # default OFF => module unlinked, media byte-identical. Enable via `UNAOS_EHCISCOUT=1`.
  ```
- **unaos/crates/kernel/Cargo.toml:94**
  ```
  # NEITHER the read-only census NOR the configure-and-relook evidence — only the driver. UNAOS_EHC...
  ```
- **unaos/crates/kernel/src/arch/x86_64/pci.rs:202**
  ```
  // EHCI-1 scout (opt-in probe, UNAOS_EHCISCOUT=1 -> `ehciscout_run`): STRICTLY READ-ONLY EHCI rec...
  ```
- **unaos/crates/kernel/src/drivers/ehci_scout.rs:4**
  ```
  //! EHCI-1 scout — read-only EHCI reconnaissance (UNAOS_EHCISCOUT=1).
  ```
- **unaos/crates/kernel/src/drivers/mod.rs:3**
  ```
  // EHCI-1 scout: read-only EHCI reconnaissance probe (UNAOS_EHCISCOUT=1). x86_64-only; the whole
  ```

## `UNAOS_EHCITABLET`

- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:991**
  ```
  **QEMU gate (honest).** `UNAOS_EHCITABLET=1` moves the harness `usb-tablet` onto the EHCI bus;
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1016**
  ```
  **Gates (M2).** Knob-matrix `./arroyo check` green both arches. `UNAOS_EHCITABLET=1 test 40`:
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1042**
  ```
  it (proven in QEMU: `UNAOS_EHCITABLET=1` still arms the `usb-tablet` with the unchanged layout
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1118**
  ```
  unregressed. `UNAOS_EHCITABLET=1 test 40`: the standard `usb-tablet` report-pointer still arms and
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1155**
  ```
  `test 22` and `UNAOS_EHCITABLET=1 test 22` both MISSION SUCCESS with **no** `bcm5974 GET_REPORT` /
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1170**
  ```
  `UNAOS_EHCITABLET=1 test 22` MISSION SUCCESS, `usb-tablet` report-pointer path unchanged. Metal
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1214**
  ```
  MISSION SUCCESS, keyboard + vendor self-test unregressed, 0 FAIL. `UNAOS_EHCITABLET=1 test 22` MI...
  ```
- **review/unaos-ehci-trackpad-LANDING.md:58**
  ```
  - `UNAOS_EHCITABLET=1 ./arroyo test 22` — MISSION SUCCESS; `[0] M2 armed report-pointer addr=2
  ```
- **review/unaos-ehci5-fix-LANDING.md:50**
  ```
  - `UNAOS_EHCITABLET=1 ./arroyo test 40` — MISSION SUCCESS; standard-pointer path unregressed:
  ```
- **review/unaos-rmbp-fix-LANDING.md:91**
  ```
  - `UNAOS_EHCITABLET=1 ./arroyo test 22` — MISSION SUCCESS, **0 FAIL**; `[0] M2 armed report-pointer
  ```
- **unaos/builder/src/main.rs:392**
  ```
  // EHCI-4 M2 gate (UNAOS_EHCITABLET=1): move the usb-tablet onto the EHCI bus so the driver's
  ```
- **unaos/builder/src/main.rs:398**
  ```
  let ehci_tablet = ehcihid && std::env::var("UNAOS_EHCITABLET").is_ok();
  ```
- **unaos/builder/src/main.rs:401**
  ```
  println!("   UNAOS_EHCITABLET: usb-tablet on the EHCI bus — EHCI-4 M2 report-pointer path target");
  ```

## `UNAOS_FATIMG`

- **docs/MILESTONES.md:104**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` = 0 FAIL / 23 PASS +
  ```
- **docs/MILESTONES.md:168**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` = 0 FAIL / 23 PASS +
  ```
- **docs/MILESTONES.md:623**
  ```
  Knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 240`: new `S9-grow … witness OK`
  ```
- **docs/MILESTONES.md:699**
  ```
  — the honest QEMU baseline (no downstream device attached). `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf t...
  ```
- **docs/MILESTONES.md:780**
  ```
  stays frozen)`; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` storage chain **0 FAIL**, S8-write ...
  ```
- **docs/MILESTONES.md:824**
  ```
  MISSION SUCCESS with **zero PORTSW lines / zero register writes**; `UNAOS_FATIMG=sf test 200` sto...
  ```
- **docs/MILESTONES.md:828**
  ```
  (before == after: no write issued, inert as expected on QEMU); `UNAOS_FATIMG=sf test 200` storage...
  ```
- **docs/MILESTONES.md:951**
  ```
  slot intact; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` full storage chain 0 FAIL, S8-write wi...
  ```
- **docs/MILESTONES.md:980**
  ```
  MOUSE-1 fires, proto=0 absolute + proto=2 relative); `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200...
  ```
- **docs/MILESTONES.md:1141**
  ```
  files touched; knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` 25 PASS 0 FAIL (S8-write int...
  ```
- **docs/MILESTONES.md:1145**
  ```
  `test 25` 18 PASS / `UNAOS_FATIMG=sf test 200` 22 PASS byte-identical apart from the witness; `te...
  ```
- **docs/MILESTONES.md:2316**
  ```
  `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` → the same line with **`bl...
  ```
- **docs/MILESTONES.md:2353**
  ```
  `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` (hosts-format `BLOCK.TXT` ...
  ```
- **docs/MILESTONES.md:2928**
  ```
  PASS + MISSION + `bx-blockreq` PASS. Knob-ON `UNAOS_FATIMG=sf ./arroyo test 150` = **23 PASS** (22
  ```
- **docs/MILESTONES.md:2930**
  ```
  served LIVE. Knob-OFF `test 40` = 18 PASS + MISSION, `UNAOS_FATIMG=sf test` = 22 PASS,
  ```
- **docs/MILESTONES.md:2952**
  ```
  isn't). `UNAOS_FATIMG=sf ./arroyo test 150` (env at script start) is the RELIABLE FAT form.
  ```
- **docs/MILESTONES.md:2983**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 150` = **24 PASS** (22 fixtures + the S3 + S4 witnesses)...
  ```
- **docs/MILESTONES.md:2985**
  ```
  NOTHING; zero `U10_OVERFLOW`). Knob-OFF `test 40` = 18 PASS + MISSION, `UNAOS_FATIMG=sf test` = 2...
  ```
- **docs/MILESTONES.md:3022**
  ```
  - **Gate:** `./arroyo check` both arches (on+off); knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf te...
  ```
- **docs/MILESTONES.md:3025**
  ```
  knob-OFF `test 40` = MISSION and `UNAOS_FATIMG=sf test` = BYTE-IDENTICAL (no S3/S4/S5/PRIO_HIGH l...
  ```
- **docs/MILESTONES.md:3068**
  ```
  - **Gate:** `./arroyo check` both arches (on+off, zero aarch64); knob-ON `UNAOS_IRQSTORAGE=1 UNAO...
  ```
- **docs/MILESTONES.md:3071**
  ```
  knob-OFF `test 40` = MISSION and `UNAOS_FATIMG=sf test` = BYTE-IDENTICAL (no S3/S4/S5/S6/witness ...
  ```
- **docs/MILESTONES.md:3122**
  ```
  UNAOS_FATIMG=sf test 200` = full chain 0 FAIL (S3/S4/S5/S6 + u6gx + u11m2 witnesses intact) + the...
  ```
- **docs/MILESTONES.md:3123**
  ```
  `:: S7-openany: … witness OK ::`; knob-OFF `test 25` = MISSION and `UNAOS_FATIMG=sf test 200` = B...
  ```
- **docs/MILESTONES.md:3164**
  ```
  UNAOS_FATIMG=sf test 200` = full prior chain 0 FAIL (S3/S4/S5/S6/S7 + u6gx + u11m2 intact) + the new
  ```
- **docs/MILESTONES.md:3165**
  ```
  `:: S8-write: … witness OK ::`; knob-OFF `test 25` = MISSION and `UNAOS_FATIMG=sf test 200` = BYT...
  ```
- **docs/MILESTONES.md:3388**
  ```
  `UNAOS_FATIMG=sf` the storage checks PASS too (`12 pass 0 fail 0 skip (+19 boot)`), all six sync
  ```
- **docs/MILESTONES.md:3978**
  ```
  - **Tested — QEMU:** `UNAOS_FATIMG=sf ./arroyo test-fat sf 300` → after the U8x PASS: the U9x set...
  ```
- **docs/MILESTONES.md:4028**
  ```
  - **Tested — QEMU:** `UNAOS_FATIMG=sf ./arroyo test-fat sf 300` → the U9x line now proves the on-...
  ```
- **docs/MILESTONES.md:4084**
  ```
  - **Tested — QEMU:** `UNAOS_FATIMG=sf ./arroyo test-fat sf 300` → `:: U11x: open-file lifecycle —
  ```
- **docs/MILESTONES.md:4115**
  ```
  SUCCESS with the applicable chain PASS; `UNAOS_FATIMG=sf ./arroyo test-fat sf 300` runs U1a→U11x ...
  ```
- **docs/MILESTONES.md:4157**
  ```
  chains the next on ALL paths, so a skip never strands U11x). `UNAOS_FATIMG=sf ./arroyo test-fat s...
  ```
- **docs/MILESTONES.md:4210**
  ```
  FAIL (U11m2 in-memory core PASS, all priors unchanged); `UNAOS_FATIMG=sf ./arroyo test-fat sf 300...
  ```
- **docs/MILESTONES.md:4211**
  ```
  0 FAIL** (U11m2 on-disk PASS appended, 20 priors); `UNAOS_FATIMG=p16 ./arroyo test-fat p16 300` 2...
  ```
- **docs/MILESTONES.md:4512**
  ```
  - **Tested — QEMU:** `UNAOS_FATIMG=sf ./arroyo test-fat sf 260` → after the U7x PASS line: the U8...
  ```
- **docs/MILESTONES.md:4570**
  ```
  - **Tested — QEMU:** `UNAOS_FATIMG=sf ./arroyo test-fat sf 180` → after the U6bx PASS line: the U...
  ```
- **docs/MILESTONES.md:4798**
  ```
  - **Tested — QEMU:** `UNAOS_FATIMG=1 ./arroyo test 30` (and `test-fat part`/`sf`) →
  ```
- **docs/MILESTONES.md:4872**
  ```
  size and 2 new U3.5 lines differ); coexists with the U2 disk loader (`UNAOS_FATIMG=1` →
  ```
- **docs/MILESTONES.md:5054**
  ```
  `UNAOS_FATIMG=1 test`) functionally byte-identical through the U-lines (seam is
  ```
- **docs/MILESTONES.md:5084**
  ```
  unexpected faults. x86 (`test` + `UNAOS_FATIMG=1 test`) functionally byte-identical
  ```
- **docs/MILESTONES.md:5115**
  ```
  (`UNAOS_FATIMG=1`), the U2.5 FTDI console (`UNAOS_USBSERIAL=1`), and the FAT
  ```
- **docs/MILESTONES.md:5141**
  ```
  line. x86 (`test` + `UNAOS_FATIMG=1 test`) byte-identical (seam inert); `check` both
  ```
- **docs/MILESTONES.md:5167**
  ```
  loader (`UNAOS_USBSERIAL=1 UNAOS_FATIMG=1` → both PASS) and the usbdebug view.
  ```
- **docs/MILESTONES.md:5183**
  ```
  - **Tested — QEMU:** `UNAOS_FATIMG=1 ./arroyo test 25` across all four FAT
  ```
- **docs/SECURITY.md:173**
  ```
  - [x] **Validated user-pointer access — the single kernel/user copy seam `copy_from_user`/`copy_t...
  ```
- **docs/SECURITY.md:173**
  ```
  - [x] **Validated user-pointer access — the single kernel/user copy seam `copy_from_user`/`copy_t...
  ```
- **docs/SECURITY.md:177**
  ```
  - [x] **Process model — `sys_spawn`/`sys_wait` + per-process handle table (U4x, x86 twin of aarch...
  ```
- **docs/SECURITY.md:178**
  ```
  - [x] **Handles as capabilities — the enforcement CHECK + grant/attenuate/revoke + routed `sys_wr...
  ```
- **docs/SECURITY.md:179**
  ```
  - [x] **General object table — `(kind, target, rights)` descriptors, first-free for ALL kinds, th...
  ```
- **docs/SECURITY.md:184**
  ```
  - [x] **File WRITE-BACK to disk — the staged write is PERSISTED to the FAT, completing the x86 Fi...
  ```
- **docs/SECURITY.md:185**
  ```
  - [x] **Open-file lifecycle — `SYS_CLOSE` + generation-tagged file-ids close the revoke+reopen de...
  ```
- **docs/SECURITY.md:186**
  ```
  - [x] **No-storage capability-chain visibility — the INLINE console-cap demos (U5x/U7x/U8x) run o...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:200**
  ```
  - [x] **The resolver now parses a HOSTILE FILE, not just a hostile packet — real blocklist ingest...
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:524**
  ```
  UNAOS_FATIMG=sf test 200` → S-chain 0 FAIL; `test 40` → MISSION SUCCESS; `UNAOS_NOSTORAGE=1`
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:585**
  ```
  changed apply path; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` → 0 FAIL; `test 40` →
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:934**
  ```
  boot; `UNAOS_NOSTORAGE=1` clean; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` 0 FAIL / 0
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1021**
  ```
  UNAOS_FATIMG=sf test 200` 0 FAIL / 0 PANIC with the M2 code compiled in. The 256-byte control
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1120**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` 0 FAIL / 0 PANIC with the EHCI-5 code compiled in.
  ```
- **review/unaos-batmon-LANDING.md:54**
  ```
  - `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` → **0 FAIL, 24 PASS**, all S/U witnesses
  ```
- **review/unaos-flight-recorder-LANDING.md:73**
  ```
  - `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` — **0 FAIL, 24 PASS**,
  ```
- **review/unaos-smolnet-default-LANDING.md:81**
  ```
  - **Full x86 net regression under the new default** (`UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo...
  ```
- **review/unaos-smolnet-default-LANDING.md:120**
  ```
  - **Stateful-FAT trap (documented, caught):** `./arroyo test` with `UNAOS_FATIMG=sf` reuses `buil...
  ```
- **review/unaos-xenum3-LANDING.md:51**
  ```
  - `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` — 25 PASS / **0 FAIL**; S-chain tags
  ```
- **review/unaos-xenum3-LANDING.md:77**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` — 25 PASS / 0 FAIL, `storage_slot=1 note='ready'`;
  ```
- **review/unaos-xenum4-LANDING.md:42**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200`:
  ```
- **review/unaos-zeolite2-LANDING.md:51**
  ```
  - Full composition `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` (hosts-...
  ```
- **unaos/arroyo:334**
  ```
  # UNAOS_FATIMG swaps the usb-storage backing to a FAT image (scripts/make-fat-img.sh) so the
  ```
- **unaos/arroyo:338**
  ```
  case "${UNAOS_FATIMG:-}" in
  ```
- **unaos/arroyo:344**
  ```
  *) STICK_IMG="${UNAOS_FATIMG}";;
  ```
- **unaos/arroyo:652**
  ```
  # (MBR@LBA0 -> BPB) and a superfloppy (BPB@LBA0). Test with `UNAOS_FATIMG=1 ./arroyo test`.
  ```
- **unaos/arroyo:678**
  ```
  export UNAOS_FATIMG="$layout"
  ```
- **unaos/arroyo:1179**
  ```
  UNAOS_FATIMG=sf "$0" test 200
  ```
- **unaos/builder/src/main.rs:246**
  ```
  // UNAOS_FATIMG selects a FAT filesystem image (built by scripts/make-fat-img.sh) as the
  ```
- **unaos/builder/src/main.rs:252**
  ```
  let stick_image = match std::env::var("UNAOS_FATIMG").ok().as_deref() {
  ```
- **unaos/builder/src/main.rs:264**
  ```
  // the engine writes ONLY this scratch disk. This OVERRIDES UNAOS_FATIMG (the two are exclusive:
  ```
- **unaos/builder/src/main.rs:280**
  ```
  panic!("UNAOS_FATIMG set but {} is missing — run `./arroyo fat-img` first",
  ```
- **unaos/builder/src/main.rs:320**
  ```
  // Boot the ESP first, explicitly (bootindex=0). When UNAOS_FATIMG points the usb-storage at a
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:581**
  ```
  NET). The S7 path needs `UNAOS_IRQSTORAGE=1` + a mounted FAT (`UNAOS_FATIMG=sf`); with **no** FAT...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:612**
  ```
  `./arroyo test-fat sf` which rebuilds it), then `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:710**
  ```
  composition (hosts-format blocklist from FAT): `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:124**
  ```
  the separate `ide-hd`, so the engine writes ONLY this scratch disk. This overrides `UNAOS_FATIMG`
  ```
- **unaos/scripts/make-fat-img.sh:4**
  ```
  # Produces a raw disk image the QEMU usb-storage device can back (see the UNAOS_FATIMG
  ```
- **unaos/scripts/make-fat-img.sh:177**
  ```
  echo "    Test in QEMU:  UNAOS_FATIMG=1 ./arroyo test 25   (partitioned)"
  ```
- **unaos/scripts/make-fat-img.sh:178**
  ```
  echo "                   UNAOS_FATIMG=sf ./arroyo test 25  (superfloppy)"
  ```
- **unaos/scripts/specs/x86-fat.spec:2**
  ```
  #   QEMU gate:  UNAOS_FATIMG=sf ./arroyo test 150   → unaos/target/serial.log
  ```

## `UNAOS_GENET`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6139**
  ```
  `UNAOS_GENET=1` (the `genet` cargo feature, default OFF) arms `arch/aarch64/genet.rs`: it DTB-res...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6216**
  ```
  knob-on `UNAOS_GENET=1 ./arroyo kernel8-test` = 0 FAIL with the honest DTB-gated skip (BSP surviv...
  ```
- **review/unaos-pi-genet-LANDING.md:75**
  ```
  - `UNAOS_GENET=1 ./arroyo check` (knob-on): x86_64 OK, aarch64 OK (witness half only — `genet` do...
  ```
- **review/unaos-pi-genet-LANDING.md:83**
  ```
  - `UNAOS_GENET=1 ./arroyo kernel8-test` (knob-on): 0 FAIL. Honest DTB-gated skip
  ```
- **unaos/arroyo:250**
  ```
  # PI-GENET: UNAOS_GENET=1 arms the BCM2711 on-board Gigabit Ethernet (Broadcom GENET v5) driver +
  ```
- **unaos/arroyo:260**
  ```
  [ -n "${UNAOS_GENET:-}" ] && _feats="${_feats}genet,"
  ```
- **unaos/arroyo:854**
  ```
  # PI-GENET: UNAOS_GENET=1 adds the `genet` feature — the BCM2711 on-board Gigabit Ethernet (Broadcom
  ```
- **unaos/arroyo:861**
  ```
  if [ -n "${UNAOS_GENET:-}" ]; then
  ```
- **unaos/arroyo:863**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_GENET: BCM2711 GENET v5 GbE + smoltcp bind (M1 probe classifies...
  ```
- **unaos/crates/kernel/Cargo.toml:351**
  ```
  # so the kernel8 image is byte-identical to baseline. Enable via `UNAOS_GENET=1 UNAOS_PI=1 ./arroyo
  ```
- **unaos/crates/kernel/src/arch/aarch64/genet.rs:78**
  ```
  "{} BCM GENET v5 driver compiled; no BCM2711 on this build — bring-up is metal-only (Pi 4, UNAOS_...
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:145**
  ```
  // PI-GENET (`UNAOS_GENET=1`): the BCM2711 on-board Gigabit Ethernet (Broadcom GENET v5) driver +
  ```
- **unaos/scripts/pi-genet-bench.md:14**
  ```
  UNAOS_GENET=1 UNAOS_PI=1 ./arroyo kernel8
  ```

## `UNAOS_GICV3`

- **docs/MILESTONES.md:66**
  ```
  MISSION SUCCESS; `UNAOS_GICV3=1 ./arroyo test-arm` CAPSTONE 6/6 + idle + busy heartbeat
  ```
- **docs/MILESTONES.md:196**
  ```
  **How it was tested.** `UNAOS_GICV3=1 ./arroyo test-arm` — the `virt` path runs the *same*
  ```
- **docs/MILESTONES.md:1700**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `kernel8-test` **0 FAIL**; `UNAOS_HUBSTORAGE=1 test 25`...
  ```
- **docs/MILESTONES.md:1730**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `kernel8-test` **0 FAIL**; `UNAOS_HUBSTORAGE=1 test 25`...
  ```
- **docs/MILESTONES.md:1773**
  ```
  MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `kernel8-test` **0 FAIL** (protects the shared...
  ```
- **docs/MILESTONES.md:1846**
  ```
  `shutdown` double-`hlt_loop`); `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6;
  ```
- **docs/MILESTONES.md:1881**
  ```
  `shutdown` double-`hlt_loop`); `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6;
  ```
- **docs/MILESTONES.md:1917**
  ```
  `shutdown` double-`hlt_loop`); `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6;
  ```
- **docs/MILESTONES.md:1953**
  ```
  MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `...
  ```
- **docs/MILESTONES.md:1990**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2038**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2083**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2120**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2572**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2752**
  ```
  both arches; `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `kernel8` +
  ```
- **docs/MILESTONES.md:2783**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`
  ```
- **docs/MILESTONES.md:2855**
  ```
  MISSION (shared `shell.rs` guard); `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6;
  ```
- **docs/MILESTONES.md:3344**
  ```
  MISSION (shared shell/fat guard); `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6;
  ```
- **docs/MILESTONES.md:3447**
  ```
  SUCCESS** (aarch64 BOT-pump guard); `UNAOS_GICV3=1 ./arroyo test-arm 40` → **CAPSTONE 6/6**;
  ```
- **docs/MILESTONES.md:4833**
  ```
  (`UNAOS_GICV3=1 test-arm 45`) SMP 3/3 + JC3 drop + `VBAR_EL1 = 0x7c38c000` + CAPSTONE 6/6 —
  ```
- **docs/MILESTONES.md:5245**
  ```
  - **Tested — QEMU:** `UNAOS_GICV3=1 ./arroyo test-arm 30` → 3 cores online +
  ```
- **docs/dev/ORCHESTRATION.md:153**
  ```
  UNAOS_GICV3=1 ./arroyo test-arm 22   # aarch64 virt GICv3: self-SGI v3 + timer live
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:319**
  ```
  core. Captured by `UNAOS_GICV3=1 ./arroyo test-arm 40`.
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:344**
  ```
  (`run_capstone_boot_core`, captured by `UNAOS_GICV3=1 ./arroyo test-arm 40`) and into the Pi
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:352**
  ```
  `UNAOS_GICV3=1 ./arroyo {arm,test-arm}` appends `-machine gic-version=3` to the
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:429**
  ```
  **Evidence** (`UNAOS_GICV3=1 ./arroyo test-arm 45`):
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:556**
  ```
  both legs + `esp-jetson` link. virt (`UNAOS_GICV3=1 test-arm 45`): SMP 3/3 + the JC3 drop +
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1939**
  ```
  timeout); `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` →
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2027**
  ```
  `shell.rs`/`fat.rs`); `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2122**
  ```
  test-arm 22` → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → `CAPSTONE COMPLETE` 6/6;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2209**
  ```
  `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → `CAPSTONE COMP...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2317**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2384**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2464**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2539**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2599**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2667**
  ```
  `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2765**
  ```
  the pre-existing `shutdown` double-`hlt_loop`); `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAO...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2841**
  ```
  the pre-existing `shutdown` double-`hlt_loop`); `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAO...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2904**
  ```
  the pre-existing `shutdown` double-`hlt_loop`); `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAO...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2970**
  ```
  `UNAOS_TEGRA=1 check` both arches; `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3131**
  ```
  `./arroyo test-arm 22` → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3152**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3168**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3349**
  ```
  code is `tegra`-gated), `UNAOS_GICV3=1 test-arm` (JC2 SMP 3/3, modulo the known
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4755**
  ```
  `tegra:` 109 unchanged); `test-arm 22` MISSION SUCCESS; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6 ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4854**
  ```
  warnings on every combo); `test-arm 22` MISSION SUCCESS; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4961**
  ```
  warnings on every combo); `test-arm 22` MISSION SUCCESS; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6 +
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5032**
  ```
  warnings), `net4`/virt + default-off both arches; `test-arm 22` MISSION SUCCESS; `UNAOS_GICV3=1 t...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5093**
  ```
  warnings); `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_NET4=1 UNAOS_GICV3=1 test-arm 40` `PC...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5093**
  ```
  warnings); `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_NET4=1 UNAOS_GICV3=1 test-arm 40` `PC...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5134**
  ```
  park, so `UNAOS_GICV3=1 ./arroyo test-arm` proves the fix: after `3/3 secondaries online`, the BSP
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5143**
  ```
  **Gates green:** `./arroyo check` both arches × {knob-off, `UNAOS_TEGRA=1`}; `UNAOS_GICV3=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5177**
  ```
  **The QEMU gate.** `UNAOS_GICV3=1 ./arroyo test-arm` now emits, per online AP,
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5206**
  ```
  **Gates green:** `./arroyo check` both arches; `UNAOS_GICV3=1 ./arroyo test-arm 40` = 3/3 seconda...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5254**
  ```
  **Gates green:** `./arroyo check` both arches × {knob-off, `UNAOS_TEGRA=1`}; `UNAOS_GICV3=1 ./arroyo
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5293**
  ```
  board); `UNAOS_GICV3=1 ./arroyo test-arm` CAPSTONE 6/6 + idle + busy heartbeat PASS + VUG-HONESTY
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5397**
  ```
  arches; knob-off `UNAOS_GICV3=1 ./arroyo test-arm 40` CAPSTONE 6/6, `./arroyo test-arm 22` + `./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5398**
  ```
  22` + `./arroyo kernel8-test` 0 FAIL; knob-on `UNAOS_SDMMC=1 UNAOS_GICV3=1 ./arroyo test-arm 40` ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5663**
  ```
  **How to run.** `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm`. The knob adds both the `vnet` ker...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5667**
  ```
  MISSION SUCCESS + `UNAOS_GICV3=1 ./arroyo test-arm` CAPSTONE 6/6 + priority+aging PASS + VUG-HONE...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5668**
  ```
  (unregressed); knob-on `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm` fires the `AARCH64 VNET … =...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5714**
  ```
  `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm` still fires `AARCH64 VNET … => PASS` (4/4 slirp re...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5754**
  ```
  Gate of record: `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` — DHCP lease PASS + ping 4/4 `[...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6043**
  ```
  `UNAOS_GICV3=1 ./arroyo test-arm 40`, `./arroyo test 22` all unregressed. Positive verification (...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6131**
  ```
  QEMU exactly as rung 1 does); `./arroyo test-arm 22`, `UNAOS_GICV3=1 ./arroyo test-arm 40`, `./ar...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6217**
  ```
  the shell, CAPSTONE 6/6, no exception); `./arroyo test-arm 22`, `UNAOS_GICV3=1 ./arroyo test-arm ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6297**
  ```
  `./arroyo test-arm 22`, `UNAOS_GICV3=1 ./arroyo test-arm 40`, `./arroyo test 22`, and the `UNAOS_...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6348**
  ```
  `./arroyo check` and `UNAOS_NET4=1 UNAOS_SDMMC=1 UNAOS_TEGRA=1 UNAOS_GICV3=1 ./arroyo check` — both
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6349**
  ```
  arches green. `./arroyo test-arm 30` 0 FAIL; `UNAOS_GICV3=1 ./arroyo test-arm 30` CAPSTONE COMPLETE,
  ```
- **review/unaos-aarch64-prio-LANDING.md:53**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40`:
  ```
- **review/unaos-default-quiet-LANDING.md:85**
  ```
  UNAOS_GICV3=1 ./arroyo test-arm 40→ CAPSTONE COMPLETE · 6/6 primitives PASS · prio-mix witness PASS
  ```
- **review/unaos-install-pi-LANDING.md:85**
  ```
  - `./arroyo test-arm 22` (exit 0) + `UNAOS_GICV3=1 ./arroyo test-arm 40` — **0 FAIL**, CAPSTONE 6/6.
  ```
- **review/unaos-install1-LANDING.md:78**
  ```
  - **GICv3 CAPSTONE 6/6** (`UNAOS_GICV3=1 ./arroyo test-arm`) — `CAPSTONE COMPLETE — all 6 sync pr...
  ```
- **review/unaos-install1-LANDING.md:80**
  ```
  - **test-arm** (plain + `UNAOS_INSTALL_TARGET_SD=1 UNAOS_GICV3=1`) — **0 FAIL**; the install_targ...
  ```
- **review/unaos-install2-LANDING.md:103**
  ```
  - **`UNAOS_GICV3=1 ./arroyo test-arm` (CAPSTONE):** `CAPSTONE COMPLETE — all 6 sync primitives ve...
  ```
- **review/unaos-install2-LANDING.md:105**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 UNAOS_GICV3=1 ./arroyo test-arm`:** the three SDMMC/INSTALL compil...
  ```
- **review/unaos-net-dhcp-LANDING.md:49**
  ```
  - `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` — DHCP lease PASS + ping 4/4 `[dhcp]` PASS + ...
  ```
- **review/unaos-net-dhcp-LANDING.md:50**
  ```
  - Knob-off regressions, all **0 FAIL**: `UNAOS_GICV3=1 ./arroyo test-arm 40`, `./arroyo test-arm ...
  ```
- **review/unaos-net-phy-LANDING.md:57**
  ```
  | `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | `:: AARCH64 VNET: ping 10.0.2.2 RTT 4730 us...
  ```
- **review/unaos-net-phy-LANDING.md:58**
  ```
  | knob-off `UNAOS_GICV3=1 ./arroyo test-arm 40` | CAPSTONE COMPLETE 6/6; **no VNET lines** (compi...
  ```
- **review/unaos-orin-net3-LANDING.md:48**
  ```
  | `UNAOS_GICV3=1 test-arm 40` (knob-off) | CAPSTONE COMPLETE 6/6 |
  ```
- **review/unaos-orin-net3-LANDING.md:49**
  ```
  | `UNAOS_PCIE3=1 UNAOS_GICV3=1 test-arm 40` | **PS-widen witness PASS** (ECAM REACHABLE; refusal ...
  ```
- **review/unaos-orin-net4-LANDING.md:52**
  ```
  | `UNAOS_GICV3=1 ./arroyo test-arm 40` (knob-off) | **CAPSTONE COMPLETE 6/6** + VUG-HONESTY PASS ...
  ```
- **review/unaos-orin-net4-LANDING.md:53**
  ```
  | `UNAOS_NET4=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | the `PCIE4` witness line fires (`RTL8168 dr...
  ```
- **review/unaos-orin-net4b-LANDING.md:80**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40` — CAPSTONE 6/6 + per-core idle/busy heartbeat PASS + VUG-H...
  ```
- **review/unaos-orin-net4b-LANDING.md:82**
  ```
  - `UNAOS_NET4=1 UNAOS_GICV3=1 ./arroyo test-arm 40` — `PCIE4` witness line present + CAPSTONE COM...
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:75**
  ```
  - knob-off `UNAOS_GICV3=1 ./arroyo test-arm 40`: CAPSTONE 6/6 PASS (Semaphore/Mutex/Channel/Condv...
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:82**
  ```
  - knob-on virt `UNAOS_SDMMC=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: the witness line
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:63**
  ```
  - **Serial identity.** Knob-off `UNAOS_GICV3=1 ./arroyo test-arm 40`: 0 `SDMMC` strings, 0 FAIL. ...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:64**
  ```
  `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: exactly the two virt-witnes...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:80**
  ```
  - knob-off `UNAOS_GICV3=1 ./arroyo test-arm 40`: `CAPSTONE COMPLETE — all 6 sync primitives verif...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:85**
  ```
  - armed virt witness `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: prints
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:83**
  ```
  - **`UNAOS_GICV3=1 ./arroyo test-arm 22` (CAPSTONE):** `:: CAPSTONE COMPLETE — all 6 sync primiti...
  ```
- **review/unaos-pi-genet-LANDING.md:87**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40`: unregressed (3/3 secondaries online GICv3, CAPSTONE 6/6, ...
  ```
- **review/unaos-pi-usb1-LANDING.md:73**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40` — **0 FAIL**, `CAPSTONE COMPLETE — all 6 sync primitives`.
  ```
- **review/unaos-pi-usb2-LANDING.md:103**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40` — **0 FAIL**, `CAPSTONE COMPLETE — all 6 sync primitives`.
  ```
- **review/unaos-prio-mix-LANDING.md:51**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40`:
  ```
- **review/unaos-rast-tegra-LANDING.md:52**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40` — ✅ CAPSTONE 6/6 COMPLETE.
  ```
- **review/unaos-schednext-LANDING.md:35**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40`:
  ```
- **review/unaos-schednext-LANDING.md:98**
  ```
  - Idle host, `UNAOS_GICV3=1 ./arroyo test-arm 40` ×3 — every run: AP {1,2,3} `pulse (busy=8, idle...
  ```
- **review/unaos-v3d3-LANDING.md:80**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40`: `✅ aarch64 test complete`, `CAPSTONE COMPLETE — all 6 sync
  ```
- **review/unaos-videonext-LANDING.md:50**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40`: `timer heartbeat live`, 3 online APs idle-heartbeat PASS,
  ```
- **review/unaos-vnet-LANDING.md:49**
  ```
  | knob-off `UNAOS_GICV3=1 ./arroyo test-arm 40` | CAPSTONE COMPLETE 6/6 + `priority+aging PASS` +...
  ```
- **review/unaos-vnet-LANDING.md:50**
  ```
  | knob-on `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | the `AARCH64 VNET … RTL … 4/4 repli...
  ```
- **review/unaos-vug-honesty-LANDING.md:65**
  ```
  - `UNAOS_GICV3=1 ./arroyo test-arm 40`: ✅ — `3/3 secondaries online`; `AP 1/2/3 pulse (busy=8,
  ```
- **review/unaos-x-net-phy-LANDING.md:68**
  ```
  | `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | `:: AARCH64 VNET: ping 10.0.2.2 RTT 5368 us...
  ```
- **review/unaos-x-net-phy-LANDING.md:69**
  ```
  | knob-off `UNAOS_GICV3=1 ./arroyo test-arm 40` | `CAPSTONE COMPLETE` 6/6; **no VNET lines** (com...
  ```
- **unaos/arroyo:177**
  ```
  # (UNAOS_GICV3=1 test-arm — witnesses the graceful skip: virt has a generic ecam, no Tegra234 RC).
  ```
- **unaos/arroyo:199**
  ```
  # decision + graceful skip (UNAOS_GICV3=1 test-arm). Default OFF => module blocks + call sites va...
  ```
- **unaos/arroyo:246**
  ```
  # it runs on the plain virt GICv3 path (UNAOS_VNET=1 UNAOS_GICV3=1 test-arm). The knob ALSO adds the
  ```
- **unaos/arroyo:305**
  ```
  # UNAOS_GICV3=1 selects the GICv3 model on the aarch64 `virt` board. A trailing `-machine
  ```
- **unaos/arroyo:311**
  ```
  if [ -n "${UNAOS_GICV3:-}" ]; then
  ```
- **unaos/arroyo:1211**
  ```
  _step "arm virt v3 (CAPSTONE 6/6)" "CAPSTONE COMPLETE"   "$blog_dir/test-arm-v3.log"  env UNAOS_G...
  ```
- **unaos/crates/kernel/Cargo.toml:207**
  ```
  # GICv3 path (so `UNAOS_PCIEPROBE=1 UNAOS_GICV3=1 ./arroyo test-arm 40` witnesses the graceful skip
  ```
- **unaos/crates/kernel/Cargo.toml:308**
  ```
  # `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm`. See arch_arm64.md §AARCH64-VNET.
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:118**
  ```
  The virt witness (no metal, for reference — `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arro...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:178**
  ```
  **Virt witness** (no metal — `UNAOS_INSTALL_TARGET_SD=1 UNAOS_GICV3=1 ./arroyo test-arm`), after ...
  ```
- **unaos/scripts/orin-smp-bench.md:18**
  ```
  - **`UNAOS_GICV3=1 ./arroyo test-arm`** — the only path that actually runs `__secondary_rust_virt`:
  ```

## `UNAOS_HUBSTORAGE`

- **docs/MILESTONES.md:851**
  ```
  no-behavior-change proof for the `smp_virt.rs` amendment); `kernel8-test` 0 FAIL; `UNAOS_HUBSTORAGE`
  ```
- **docs/MILESTONES.md:879**
  ```
  `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS; `esp-jetson` links, `tegra:` 109 (unchanged — no new prin...
  ```
- **docs/MILESTONES.md:946**
  ```
  aarch64 diffs; default `test 25` MISSION SUCCESS + MOUSE-1 (usb-tablet enumerates); `UNAOS_HUBSTO...
  ```
- **docs/MILESTONES.md:981**
  ```
  storage chain **PASS 25 / FAIL 0**, S8-write witness intact; `UNAOS_HUBSTORAGE=1 test 90` brings ...
  ```
- **docs/MILESTONES.md:1224**
  ```
  byte-untouched); `kernel8-test` 0-FAIL (34 PASS); `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS. Four ar...
  ```
- **docs/MILESTONES.md:1262**
  ```
  `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS. **Knob-off byte-identity proven:** two default `esp-jetson`
  ```
- **docs/MILESTONES.md:1302**
  ```
  (the shared `smp_virt` path byte-untouched); `kernel8-test` 0-FAIL; `UNAOS_HUBSTORAGE` x86 MISSION
  ```
- **docs/MILESTONES.md:1349**
  ```
  PASS; `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS; `esp-jetson` links. **Knob-off byte-identity proven:**
  ```
- **docs/MILESTONES.md:1700**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `kernel8-test` **0 FAIL**; `UNAOS_HUBSTORAGE=1 test 25`...
  ```
- **docs/MILESTONES.md:1730**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `kernel8-test` **0 FAIL**; `UNAOS_HUBSTORAGE=1 test 25`...
  ```
- **docs/MILESTONES.md:1774**
  ```
  `UNAOS_HUBSTORAGE=1 test 25` MISSION (x86, shared shell/fat guard). Wall-clock stamp not headless...
  ```
- **docs/MILESTONES.md:1847**
  ```
  `UNAOS_HUBSTORAGE=1 test 25` MISSION. Zero x86 behavioural change. No `kernel8-test` on the jetso...
  ```
- **docs/MILESTONES.md:1882**
  ```
  `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs` guard); `esp-jetson` links, **109 `tegra:...
  ```
- **docs/MILESTONES.md:1918**
  ```
  `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs` guard); `esp-jetson` links, **109 `tegra:...
  ```
- **docs/MILESTONES.md:1953**
  ```
  MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `...
  ```
- **docs/MILESTONES.md:1990**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2038**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2083**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2120**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2572**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`...
  ```
- **docs/MILESTONES.md:2783**
  ```
  `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 test 25` MISSION (shared `shell.rs`
  ```
- **docs/MILESTONES.md:2854**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `UNAOS_HUBSTORAGE=1 test 25`
  ```
- **docs/MILESTONES.md:3343**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `UNAOS_HUBSTORAGE=1 test 25`
  ```
- **docs/MILESTONES.md:3445**
  ```
  `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → **MISSION SUCCESS** (`storage_slot=2 note='ready'`, no BOT
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1911**
  ```
  x86 `UNAOS_HUBSTORAGE=1 test` and aarch64 `test-arm 22` MISSION regressions (both exercise the BOT
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1938**
  ```
  `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS` (`storage_slot=2 note='ready'`, no BOT
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2026**
  ```
  `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS` (run because M1 touches the shared
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2121**
  ```
  `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS` (shared `shell.rs` guard); `./arroyo
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2210**
  ```
  6/6; `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS` (shared `shell.rs` guard); `esp-je...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2317**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2384**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2464**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2539**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2599**
  ```
  → `MISSION SUCCESS`; `UNAOS_GICV3=1 ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2668**
  ```
  `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS` (the shared `shell.rs` guard); `esp-jetson
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2766**
  ```
  ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS` (...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2842**
  ```
  ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS`;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2905**
  ```
  ./arroyo test-arm 40` → CAPSTONE 6/6; `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS`. ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3133**
  ```
  `UNAOS_HUBSTORAGE=1 ./arroyo test 25` → `MISSION SUCCESS` (x86, shared shell/fat guard). The wall...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3152**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3168**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3757**
  ```
  `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS; `esp-jetson` links, `tegra:` count 142 armed / 109 off
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3829**
  ```
  (the shared `smp_virt` path is byte-untouched); `kernel8-test` 0-FAIL; `UNAOS_HUBSTORAGE` x86 MIS...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3901**
  ```
  (34 PASS); `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS; knob-off byte-identity proven (two default bui...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3939**
  ```
  3/3 secondaries (shared `smp_virt` byte-untouched); `kernel8-test` 0-FAIL (34 PASS); `UNAOS_HUBST...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4016**
  ```
  0-FAIL (34 PASS); `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS; `esp-jetson` links, `tegra:` 109 (uncha...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4062**
  ```
  `smp_virt.rs` amendment); `kernel8-test` 0-FAIL; `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS; 3 armed leg
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4647**
  ```
  esp-jetson links); `UNAOS_HUBSTORAGE` x86 MISSION SUCCESS. Bench runbook: `scripts/orin-smp8-benc...
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:116**
  ```
  bring-up as a root-port device. QEMU reproduction: `UNAOS_HUBSTORAGE=1` attaches
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:522**
  ```
  QEMU gates (no-regression): `UNAOS_HUBSTORAGE=1 test 60` → MISSION SUCCESS + full U-arc
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:582**
  ```
  **QEMU gates (no-regression + direct functional):** `UNAOS_HUBSTORAGE=1 test 60` → the hubbed
  ```
- **review/unaos-xenum3-LANDING.md:47**
  ```
  - `UNAOS_HUBSTORAGE=1 ./arroyo test 60` — `>>> MISSION SUCCESS (BOT + CSW). TARGET ACQUIRED. <<<`
  ```
- **review/unaos-xenum3-LANDING.md:76**
  ```
  `UNAOS_HUBSTORAGE=1 test 60` — MPS0-learn line fired + `MISSION SUCCESS`;
  ```
- **review/unaos-xenum4-LANDING.md:33**
  ```
  `UNAOS_HUBSTORAGE=1 ./arroyo test 60`:
  ```
- **unaos/builder/src/main.rs:342**
  ```
  // UNAOS_HUBSTORAGE=1 attaches the usb-storage BEHIND a usb-hub instead of on a root port —
  ```
- **unaos/builder/src/main.rs:347**
  ```
  let hubstorage = std::env::var("UNAOS_HUBSTORAGE").is_ok();
  ```
- **unaos/builder/src/main.rs:373**
  ```
  println!("   UNAOS_HUBSTORAGE: usb-storage attached behind a usb-hub (hub-downstream MSC path)");
  ```

## `UNAOS_INSTALLDEMO`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5443**
  ```
  the GPT writer + FAT32 formatter + sha extent-verify proven pre-silicon by `UNAOS_INSTALLDEMO`, see
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6297**
  ```
  `./arroyo test-arm 22`, `UNAOS_GICV3=1 ./arroyo test-arm 40`, `./arroyo test 22`, and the `UNAOS_...
  ```
- **review/unaos-install-core-LANDING.md:3**
  ```
  Branch: `us-installcore` (off `main` 088a06a). Knob: `UNAOS_INSTALLDEMO=1` (feature `installdemo`),
  ```
- **review/unaos-install-core-LANDING.md:60**
  ```
  - `UNAOS_INSTALLDEMO=1 ./arroyo check` (both arches, knob ON): both `Finished` (engine compiles on
  ```
- **review/unaos-install-core-LANDING.md:62**
  ```
  - `UNAOS_INSTALLDEMO=1 ./arroyo test 22`: the 11-line INSTALL witness incl. the negative test, en...
  ```
- **review/unaos-install-pi-LANDING.md:87**
  ```
  - `UNAOS_INSTALLDEMO=1 ./arroyo test 22` — engine witness `:: INSTALL: gpt+fat32+copy verify => P...
  ```
- **review/unaos-install1-LANDING.md:76**
  ```
  `UNAOS_SDMMC_ARM`, `UNAOS_INSTALL_TARGET_SD` (each × virt and × `UNAOS_TEGRA`), plus `UNAOS_INSTA...
  ```
- **review/unaos-install1-LANDING.md:83**
  ```
  - **`UNAOS_INSTALLDEMO=1 ./arroyo test`** — the engine witness still runs end-to-end:
  ```
- **review/unaos-install2-LANDING.md:88**
  ```
  do zero MMIO; the deferred call site is tegra-gated (absent on virt). The x86 `UNAOS_INSTALLDEMO`...
  ```
- **review/unaos-install2-LANDING.md:98**
  ```
  - **`UNAOS_INSTALLDEMO=1 ./arroyo check`:** 0 errors (the `TreeWriter` additions compile in the e...
  ```
- **review/unaos-install2-LANDING.md:99**
  ```
  - **`UNAOS_INSTALLDEMO=1 ./arroyo test` (x86 engine):** engine end-to-end PASS —
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:64**
  ```
  The x86 `UNAOS_INSTALLDEMO` witness proves it: after the existing engine run it re-establishes the
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:73**
  ```
  - **`UNAOS_INSTALLDEMO=1 ./arroyo check` (both arches):** ✅ x86_64 OK, ✅ aarch64 OK; 0 errors, no...
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:78**
  ```
  - **`UNAOS_INSTALLDEMO=1 ./arroyo test 22` (x86 engine):** engine end-to-end PASS, including the ...
  ```
- **unaos/arroyo:102**
  ```
  # INSTALL-CORE: UNAOS_INSTALLDEMO=1 arms the installer engine + its x86 boot witness (GPT writer +
  ```
- **unaos/arroyo:110**
  ```
  [ -n "${UNAOS_INSTALLDEMO:-}" ] && _feats="${_feats}installdemo,"
  ```
- **unaos/builder/src/main.rs:92**
  ```
  // INSTALL-CORE: UNAOS_INSTALLDEMO=1 arms the installer engine + its x86 boot witness (GPT writer
  ```
- **unaos/builder/src/main.rs:96**
  ```
  if std::env::var("UNAOS_INSTALLDEMO").is_ok() { feats.push("installdemo"); }
  ```
- **unaos/builder/src/main.rs:260**
  ```
  // INSTALL-CORE (UNAOS_INSTALLDEMO=1): the installer engine's target is a DEDICATED BLANK scratch
  ```
- **unaos/builder/src/main.rs:268**
  ```
  let installdemo = std::env::var("UNAOS_INSTALLDEMO").is_ok();
  ```
- **unaos/builder/src/main.rs:274**
  ```
  println!("   UNAOS_INSTALLDEMO: fresh BLANK 128 MiB scratch disk -> {}", scratch.display());
  ```
- **unaos/crates/kernel/Cargo.toml:385**
  ```
  # INSTALL-CORE: the `UNAOS_INSTALLDEMO=1` storage-agnostic installer engine + its boot witness
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:88**
  ```
  // do zero MMIO. The engine itself is proven on x86 (the UNAOS_INSTALLDEMO witness); the full SD ...
  ```
- **unaos/crates/kernel/src/install/mod.rs:14**
  ```
  //     the `UNAOS_INSTALLDEMO=1` knob, and the target is ONLY a dedicated blank scratch disk the ...
  ```
- **unaos/crates/kernel/src/install/mod.rs:80**
  ```
  /// device is the QEMU usb-storage scratch disk the builder attaches under `UNAOS_INSTALLDEMO=1`.
  ```
- **unaos/crates/kernel/src/lib.rs:51**
  ```
  // compiles on both arches under `installdemo` (UNAOS_INSTALLDEMO=1); its witness `run_demo` is i...
  ```
- **unaos/crates/kernel/src/main.rs:1078**
  ```
  // the builder attaches under UNAOS_INSTALLDEMO=1) is present, run the installer engine
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:7**
  ```
  Knob: `UNAOS_INSTALLDEMO=1` (feature `installdemo`), default OFF. Module: `crates/kernel/src/inst...
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:29**
  ```
  `installdemo` feature + the `UNAOS_INSTALLDEMO=1` knob, and the target is ONLY a dedicated blank
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:121**
  ```
  `UNAOS_INSTALLDEMO=1`:
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:135**
  ```
  `UNAOS_INSTALLDEMO=1 ./arroyo test 22` boots x86_64 headless and runs the engine end-to-end, emit...
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:183**
  ```
  `UNAOS_INSTALLDEMO` witness above still covers the engine end-to-end. The tegra flow's **first ex...
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:220**
  ```
  **Witness.** Virt: one honest metal-only line (both arches). x86 `UNAOS_INSTALLDEMO` still covers...
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:243**
  ```
  **Witness (x86 `UNAOS_INSTALLDEMO`).** A final step builds a `SUB/` directory of 20 files (22 slo...
  ```

## `UNAOS_INSTALL_TARGET_SD`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5439**
  ```
  #### ORIN-INSTALL-1 — the first real installer flow (`UNAOS_INSTALL_TARGET_SD`, the THIRD destruc...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5457**
  ```
  | 3 | `install_target` / `UNAOS_INSTALL_TARGET_SD` | the explicit **destructive-confirmation** ga...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5498**
  ```
  #### ORIN-INSTALL-2 — the self-clone: the installer copies the running system's real boot payload...
  ```
- **review/unaos-install1-LANDING.md:29**
  ```
  - `unaos/arroyo` — `UNAOS_INSTALL_TARGET_SD=1` → `install_target,sdmmc_arm,sdmmc`.
  ```
- **review/unaos-install1-LANDING.md:44**
  ```
  | 3 | `install_target` / `UNAOS_INSTALL_TARGET_SD` | the explicit **destructive-confirmation** ga...
  ```
- **review/unaos-install1-LANDING.md:76**
  ```
  `UNAOS_SDMMC_ARM`, `UNAOS_INSTALL_TARGET_SD` (each × virt and × `UNAOS_TEGRA`), plus `UNAOS_INSTA...
  ```
- **review/unaos-install1-LANDING.md:80**
  ```
  - **test-arm** (plain + `UNAOS_INSTALL_TARGET_SD=1 UNAOS_GICV3=1`) — **0 FAIL**; the install_targ...
  ```
- **review/unaos-install1-LANDING.md:95**
  ```
  The full flow's **first execution is the attended Orin sitting**: `UNAOS_INSTALL_TARGET_SD=1 UNAO...
  ```
- **review/unaos-install2-LANDING.md:4**
  ```
  the installer line, on top of INSTALL-1 (`review/unaos-install1-LANDING.md`). Same `UNAOS_INSTALL...
  ```
- **review/unaos-install2-LANDING.md:95**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 ./arroyo check` — virt (both arches):** 0 errors.
  ```
- **review/unaos-install2-LANDING.md:96**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo check` — tegra (both arches):** 0 errors, n...
  ```
- **review/unaos-install2-LANDING.md:105**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 UNAOS_GICV3=1 ./arroyo test-arm`:** the three SDMMC/INSTALL compil...
  ```
- **review/unaos-install2-LANDING.md:118**
  ```
  `UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` on a card the operator is willing t...
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:5**
  ```
  single-cluster directories (the >16-entry bound). Same `UNAOS_INSTALL_TARGET_SD=1` gate.
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:75**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 ./arroyo check` — virt (both arches):** ✅ both OK.
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:76**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo check` — tegra (both arches):** ✅ both OK, ...
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:88**
  ```
  **rebuilds byte-for-byte identical** (sha256 `68599c33…9581` twice). The `UNAOS_INSTALL_TARGET_SD...
  ```
- **unaos/arroyo:232**
  ```
  # ORIN-INSTALL-1: UNAOS_INSTALL_TARGET_SD=1 is the THIRD (destructive-confirmation) gate on top o...
  ```
- **unaos/arroyo:241**
  ```
  [ -n "${UNAOS_INSTALL_TARGET_SD:-}" ] && _feats="${_feats}install_target,sdmmc_arm,sdmmc,"
  ```
- **unaos/crates/kernel/Cargo.toml:284**
  ```
  # ORIN-INSTALL-1: the `UNAOS_INSTALL_TARGET_SD=1` THIRD (destructive-confirmation) gate. On top of
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:92**
  ```
  "{} ORIN-INSTALL-2 third gate (UNAOS_INSTALL_TARGET_SD=1) compiled-present but metal-only — no Te...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:864**
  ```
  // unless UNAOS_INSTALL_TARGET_SD=1, so a plain `sdmmc`/`sdmmc_arm` build is byte-identical to th...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:1165**
  ```
  "{} ORIN-INSTALL-2 THIRD GATE (UNAOS_INSTALL_TARGET_SD=1) — cloning the running boot payload onto...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:1703**
  ```
  // ── ORIN-INSTALL-2: the self-clone installer flow (only with UNAOS_INSTALL_TARGET_SD=1, the third
  ```
- **unaos/crates/kernel/src/main.rs:1591**
  ```
  // ORIN-INSTALL-2 (`UNAOS_INSTALL_TARGET_SD=1`): the self-clone install runs HERE — AFTER the JB2b
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:6**
  ```
  (§INSTALL-1 / §INSTALL-2 / §ORIN-SDMMC-3 below; `UNAOS_INSTALL_TARGET_SD=1`).
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:158**
  ```
  **Landed 2026-07-18** (aarch64/tegra; metal-pending). `UNAOS_INSTALL_TARGET_SD=1` (feature `insta...
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:189**
  ```
  **Landed 2026-07-18** (aarch64/tegra; metal-pending). Same `UNAOS_INSTALL_TARGET_SD=1` gate. Glue:
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:227**
  ```
  `installdemo` witness). Same `UNAOS_INSTALL_TARGET_SD=1` gate. Glue: `sdmmc_tegra.rs` (multi-bloc...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:131**
  ```
  - **Image:** `UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` (the knob pulls `insta...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:151**
  ```
  :: SDMMC: ORIN-INSTALL-2 THIRD GATE (UNAOS_INSTALL_TARGET_SD=1) — cloning the running boot payloa...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:178**
  ```
  **Virt witness** (no metal — `UNAOS_INSTALL_TARGET_SD=1 UNAOS_GICV3=1 ./arroyo test-arm`), after ...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:182**
  ```
  :: SDMMC: ORIN-INSTALL-2 third gate (UNAOS_INSTALL_TARGET_SD=1) compiled-present but metal-only —...
  ```

## `UNAOS_IRQSTORAGE`

- **docs/MILESTONES.md:104**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` = 0 FAIL / 23 PASS +
  ```
- **docs/MILESTONES.md:168**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` = 0 FAIL / 23 PASS +
  ```
- **docs/MILESTONES.md:623**
  ```
  Knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 240`: new `S9-grow … witness OK`
  ```
- **docs/MILESTONES.md:699**
  ```
  — the honest QEMU baseline (no downstream device attached). `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf t...
  ```
- **docs/MILESTONES.md:780**
  ```
  stays frozen)`; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` storage chain **0 FAIL**, S8-write ...
  ```
- **docs/MILESTONES.md:951**
  ```
  slot intact; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` full storage chain 0 FAIL, S8-write wi...
  ```
- **docs/MILESTONES.md:980**
  ```
  MOUSE-1 fires, proto=0 absolute + proto=2 relative); `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200...
  ```
- **docs/MILESTONES.md:1141**
  ```
  files touched; knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` 25 PASS 0 FAIL (S8-write int...
  ```
- **docs/MILESTONES.md:2302**
  ```
  path — needs `UNAOS_IRQSTORAGE=1` + a mounted FAT). No FAT ⇒ a builtin fallback list + honest `bu...
  ```
- **docs/MILESTONES.md:2316**
  ```
  `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` → the same line with **`bl...
  ```
- **docs/MILESTONES.md:2353**
  ```
  `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` (hosts-format `BLOCK.TXT` ...
  ```
- **docs/MILESTONES.md:2922**
  ```
  - **Knob:** everything behind the `irqstorage` cargo feature (`UNAOS_IRQSTORAGE=1`; mapped in
  ```
- **docs/MILESTONES.md:2983**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 150` = **24 PASS** (22 fixtures + the S3 + S4 witnesses)...
  ```
- **docs/MILESTONES.md:3022**
  ```
  - **Gate:** `./arroyo check` both arches (on+off); knob-ON `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf te...
  ```
- **docs/MILESTONES.md:3068**
  ```
  - **Gate:** `./arroyo check` both arches (on+off, zero aarch64); knob-ON `UNAOS_IRQSTORAGE=1 UNAO...
  ```
- **docs/MILESTONES.md:3121**
  ```
  - **Gate:** `./arroyo check` both arches (on+off, zero aarch64, zero new warnings); knob-ON `UNAO...
  ```
- **docs/MILESTONES.md:3163**
  ```
  - **Gate:** `./arroyo check` both arches (on+off, zero aarch64, zero new warnings); knob-ON `UNAO...
  ```
- **docs/SECURITY.md:173**
  ```
  - [x] **Validated user-pointer access — the single kernel/user copy seam `copy_from_user`/`copy_t...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:200**
  ```
  - [x] **The resolver now parses a HOSTILE FILE, not just a hostile packet — real blocklist ingest...
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:523**
  ```
  chain off the hubbed disk (MPS0-learn re-address exercised); `UNAOS_IRQSTORAGE=1
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:585**
  ```
  changed apply path; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` → 0 FAIL; `test 40` →
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:934**
  ```
  boot; `UNAOS_NOSTORAGE=1` clean; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` 0 FAIL / 0
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1020**
  ```
  `test 40` (no tablet) MISSION SUCCESS with the trackpad path dormant; `UNAOS_IRQSTORAGE=1
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:1120**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` 0 FAIL / 0 PANIC with the EHCI-5 code compiled in.
  ```
- **review/unaos-batmon-LANDING.md:54**
  ```
  - `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` → **0 FAIL, 24 PASS**, all S/U witnesses
  ```
- **review/unaos-flight-recorder-LANDING.md:73**
  ```
  - `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` — **0 FAIL, 24 PASS**,
  ```
- **review/unaos-smolnet-default-LANDING.md:81**
  ```
  - **Full x86 net regression under the new default** (`UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo...
  ```
- **review/unaos-xenum3-LANDING.md:51**
  ```
  - `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` — 25 PASS / **0 FAIL**; S-chain tags
  ```
- **review/unaos-xenum3-LANDING.md:77**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` — 25 PASS / 0 FAIL, `storage_slot=1 note='ready'`;
  ```
- **review/unaos-xenum4-LANDING.md:42**
  ```
  `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200`:
  ```
- **review/unaos-zeolite2-LANDING.md:51**
  ```
  - Full composition `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` (hosts-...
  ```
- **unaos/arroyo:66**
  ```
  [ -n "${UNAOS_IRQSTORAGE:-}" ] && _feats="${_feats}irqstorage,"
  ```
- **unaos/builder/src/main.rs:54**
  ```
  // UNAOS_IRQSTORAGE=1 routes x86 storage syscalls through the interrupt-driven storage service task
  ```
- **unaos/builder/src/main.rs:57**
  ```
  if std::env::var("UNAOS_IRQSTORAGE").is_ok() { feats.push("irqstorage"); }
  ```
- **unaos/crates/kernel/Cargo.toml:63**
  ```
  # `UNAOS_IRQSTORAGE=1`. See unaos/docs/dev/OS/07_USB_STORAGE/x86_interrupt_storage.md.
  ```
- **unaos/docs/dev/OS/07_USB_STORAGE/x86_interrupt_storage.md:334**
  ```
  behind a knob (e.g. `UNAOS_IRQSTORAGE=1`) that falls back to the current staged path until an att...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:581**
  ```
  NET). The S7 path needs `UNAOS_IRQSTORAGE=1` + a mounted FAT (`UNAOS_FATIMG=sf`); with **no** FAT...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:612**
  ```
  `./arroyo test-fat sf` which rebuilds it), then `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:710**
  ```
  composition (hosts-format blocklist from FAT): `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo
  ```

## `UNAOS_IVB`

- **unaos/arroyo:126**
  ```
  [ -n "${UNAOS_IVB:-}" ] && _feats="${_feats}intel-ivb,"
  ```

## `UNAOS_JB8_LEVER`

- **docs/MILESTONES.md:4373**
  ```
  (`UNAOS_JB8_LEVER=1`) forces `Disconnect`+`ConnectController` on the `Usb2Hc` handles — a fresh
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1490**
  ```
  (`UNAOS_JB8_LEVER=1 ./arroyo esp-jetson`): `DisconnectController`+`ConnectController(recursive)` ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1503**
  ```
  esp-jetson` links (`kernel.elf` 254,536 B tegra, 120 `tegra:` strings) with and without `UNAOS_JB...
  ```
- **unaos/arroyo:296**
  ```
  # UNAOS_JB8_LEVER=1 turns on the bootloader's `jb8lever` feature — the JB8 pre-EBS Usb2Hc
  ```
- **unaos/arroyo:299**
  ```
  [ -n "${UNAOS_JB8_LEVER:-}" ] && _bfeats="${_bfeats}jb8lever,"
  ```
- **unaos/crates/bootloader/Cargo.toml:17**
  ```
  # risk media — OFF by default; enabled by arroyo via UNAOS_JB8_LEVER=1. aarch64-only code paths.
  ```
- **unaos/crates/bootloader/src/main.rs:1016**
  ```
  /// JB8 lever (feature `jb8lever`, arroyo `UNAOS_JB8_LEVER=1` — separate media, only flashed when
  ```

## `UNAOS_K2_LEAVE`

- **docs/MILESTONES.md:3245**
  ```
  heal lines). **Part B genuine two-boot power-cycle** (the M(e) `UNAOS_K2_LEAVE` knob): boot-1 left
  ```
- **docs/MILESTONES.md:3253**
  ```
  - **M(e) — the metal money-shot knob (`UNAOS_K2_LEAVE`, attended Pi bench only; follow-on commit ...
  ```
- **docs/MILESTONES.md:3256**
  ```
  `UNAOS_K2_LEAVE=1`; OFF ⇒ the normal same-boot battery, byte-identical) swaps `k2_liveenf_launche...
  ```
- **docs/ROADMAP.md:75**
  ```
  | **K1+K2** | Reboot-surviving ACL: the U6 owner/grants persist to an on-disk `UNAFS.ATR` file (k...
  ```
- **docs/SECURITY.md:262**
  ```
  - **K2 (make-enforcement-LIVE — 2026-07-11, QEMU-verified + ✅ METAL-CONFIRMED on real Pi 4 silico...
  ```
- **unaos/arroyo:806**
  ```
  # K2 metal money-shot: UNAOS_K2_LEAVE=1 adds the `k2_leave` feature — k2_liveenf becomes a two-boot
  ```
- **unaos/arroyo:817**
  ```
  if [ -n "${UNAOS_K2_LEAVE:-}" ]; then
  ```
- **unaos/arroyo:819**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_K2_LEAVE: k2_liveenf → two-boot money-shot mode (boot-1 leaves ...
  ```
- **unaos/crates/kernel/Cargo.toml:45**
  ```
  # only. Enable via `UNAOS_K2_LEAVE=1 ./arroyo kernel8`. OFF => the normal same-boot proof (batter...
  ```
- **unaos/scripts/k2-metal-bench.md:10**
  ```
  `UNAOS_K2_LEAVE` knob, K2 M(e)).
  ```
- **unaos/scripts/k2-metal-bench.md:44**
  ```
  1. `UNAOS_K2_LEAVE=1 ./arroyo kernel8`. Flash the LEAVE image onto a **pristine** card. (`arroyo`...
  ```
- **unaos/scripts/k2-metal-bench.md:45**
  ```
  `• UNAOS_K2_LEAVE: k2_liveenf → two-boot money-shot mode`.)
  ```

## `UNAOS_KEPLER`

- **unaos/arroyo:125**
  ```
  [ -n "${UNAOS_KEPLER:-}" ] && _feats="${_feats}nvidia-kepler,"
  ```

## `UNAOS_METER_SNAPSHOT`

- **libs/quartzite/src/platforms/macos/meter.rs:339**
  ```
  // Dev affordance: `UNAOS_METER_SNAPSHOT=/path.png` writes a pixel-true PNG
  ```
- **libs/quartzite/src/platforms/macos/meter.rs:344**
  ```
  if let Ok(snap_path) = std::env::var("UNAOS_METER_SNAPSHOT") {
  ```
- **vessels/pulse/README.md:55**
  ```
  Dev affordance: `UNAOS_METER_SNAPSHOT=/path.png` (with optional
  ```

## `UNAOS_METER_SNAPSHOT_DELAY_MS`

- **libs/quartzite/src/platforms/macos/meter.rs:341**
  ```
  // involved) after `UNAOS_METER_SNAPSHOT_DELAY_MS` (default 1500) — long
  ```
- **libs/quartzite/src/platforms/macos/meter.rs:345**
  ```
  let delay_ms = std::env::var("UNAOS_METER_SNAPSHOT_DELAY_MS")
  ```
- **vessels/pulse/README.md:56**
  ```
  `UNAOS_METER_SNAPSHOT_DELAY_MS`, default 1500) makes the meter write a
  ```

## `UNAOS_NET`

- **docs/MILESTONES.md:2224**
  ```
  `UNAOS_NET=socket` builder mode SOCK-1 already wired (**no builder change**): `scripts/net-inject...
  ```
- **docs/MILESTONES.md:2232**
  ```
  witnesses intact; server round-trip `witness OK` via `UNAOS_NET=socket UNAOS_SMOLNET=1` builder +...
  ```
- **docs/MILESTONES.md:2278**
  ```
  `UNAOS_NET=socket UNAOS_SMOLNET=1 ./arroyo test 90` + `net-inject.py sock7` → both SOCK-6 and SOC...
  ```
- **docs/MILESTONES.md:2307**
  ```
  the **serve leg needs the `UNAOS_NET=socket` injector** (`net-inject.py dns`), reading PENDING he...
  ```
- **docs/MILESTONES.md:2318**
  ```
  proven across runs): `UNAOS_NET=socket UNAOS_SMOLNET=1 ./arroyo test` + `net-inject.py dns` →
  ```
- **docs/ROADMAP.md:98**
  ```
  | SOCK-6 ✅ 🔬 | **Landed (round 15, `net-sock1`).** Scope A — **TCP server/listen sockets**: ring ...
  ```
- **docs/SECURITY.md:197**
  ```
  - [x] **TCP server / listen sockets — ring 3 accepts inbound TCP (SOCK-6 scope A, 2026-07-14, QEM...
  ```
- **docs/SECURITY.md:199**
  ```
  - [x] **A ring-3 service now ANSWERS network queries — the DNS resolver / sinkhole (SINKHOLE-1 "z...
  ```
- **review/unaos-zeolite2-LANDING.md:50**
  ```
  `:: zeolite: resolver bound :53 — awaiting an inbound query (UNAOS_NET=socket net-inject dns) — w...
  ```
- **unaos/builder/src/main.rs:286**
  ```
  // comes up, but the host cannot arping/ping the guest (it's NAT). UNAOS_NET=vmnet
  ```
- **unaos/builder/src/main.rs:289**
  ```
  let net_mode = std::env::var("UNAOS_NET").unwrap_or_default();
  ```
- **unaos/crates/kernel/src/arch/x86_64/syscall.rs:10550**
  ```
  //   (3) SERVE (needs the UNAOS_NET=socket `dns` injector): binds UDP :53, and for a bounded window
  ```
- **unaos/crates/kernel/src/arch/x86_64/syscall.rs:11239**
  ```
  // The forward leg's upstream is unreachable on this medium (the UNAOS_NET=socket injector has
  ```
- **unaos/crates/kernel/src/arch/x86_64/syscall.rs:11259**
  ```
  // Leg 2: the over-the-wire sinkhole serve — needs the UNAOS_NET=socket `dns` injector.
  ```
- **unaos/crates/kernel/src/arch/x86_64/syscall.rs:11266**
  ```
  ":: zeolite: resolver bound :53 — awaiting an inbound query (UNAOS_NET=socket net-inject dns) — w...
  ```
- **unaos/crates/kernel/src/drivers/e1000.rs:1204**
  ```
  // across passes). Awaits an inbound connect from scripts/net-inject.py under UNAOS_NET=socket;
  ```
- **unaos/crates/kernel/src/smolnet.rs:330**
  ```
  /// running under `UNAOS_NET=socket`.)
  ```
- **unaos/crates/kernel/src/smolnet.rs:1396**
  ```
  // gateway-side active-opens under `UNAOS_NET=socket`. Under the default (hermetic) slirp backend...
  ```
- **unaos/crates/kernel/src/smolnet.rs:1463**
  ```
  ":: SOCK-6: smoltcp tcp listen :{} armed — awaiting inbound connect (UNAOS_NET=socket injector) —...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:180**
  ```
  `UNAOS_NET=socket`). Two witnesses fire knob-on:
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:436**
  ```
  ### The witness (net-inject, `UNAOS_NET=socket`)
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:440**
  ```
  `scripts/net-inject.py sock6` (against `-netdev socket,listen=127.0.0.1:5555`, the `UNAOS_NET=soc...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:450**
  ```
  :: SOCK-6: smoltcp tcp listen :8080 armed — awaiting inbound connect (UNAOS_NET=socket injector) ...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:453**
  ```
  Under `UNAOS_NET=socket` with the injector it completes and emits:
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:461**
  ```
  `UNAOS_NET=socket UNAOS_SERIAL_LOG=<log> UNAOS_TEST_SECS=60` and, concurrently,
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:514**
  ```
  ### The witness (net-inject `sock7`, `UNAOS_NET=socket`)
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:525**
  ```
  :: SOCK-6: smoltcp tcp listen :8080 armed — awaiting inbound connect (UNAOS_NET=socket injector) ...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:529**
  ```
  Under `UNAOS_NET=socket` with the `sock7` injector it completes and emits **both** OK lines — the...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:539**
  ```
  `UNAOS_NET=socket UNAOS_TEST_SECS=85 ./arroyo test 90` and, concurrently,
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:601**
  ```
  - **The SERVE (sinkhole-over-the-wire) leg needs the `UNAOS_NET=socket` injector.** Slirp's NAT w...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:607**
  ```
  `:: zeolite: resolver bound :53 — awaiting an inbound query (UNAOS_NET=socket net-inject dns) — w...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:615**
  ```
  Over-the-wire sinkhole: launch `UNAOS_NET=socket UNAOS_TEST_SECS=150 ./arroyo test 155` and concu...
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:722**
  ```
  | SOCK-6 | TCP server/listen sockets (`sys_listen`/`sys_accept`, #26–27) — ring 3 accepts inbound...
  ```
- **unaos/scripts/net-inject.py:6**
  ```
  # `UNAOS_NET=socket` mode (QEMU `-netdev socket,listen=127.0.0.1:5555`): QEMU listens,
  ```

## `UNAOS_NET4`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4965**
  ```
  ### ORIN-NET-4 — RTL8168/8111 GbE driver + smoltcp bind (`UNAOS_NET4`, knob-gated; the Orin's fir...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5019**
  ```
  :: PCIE4: ORIN-NET-4 RTL8168 driver compiled; no Tegra234 RC on this build (QEMU virt) — bring-up...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5022**
  ```
  On metal (`UNAOS_NET4=1 UNAOS_TEGRA=1`) the full claim → rings → bind sequence runs; live ICMP/AR...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5026**
  ```
  **Byte-identity (knob-off).** `net4` is default-OFF and armed only by `UNAOS_NET4=1`; with it off...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5031**
  ```
  **Gates green (pre-metal).** `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` both arches (zero net-new
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5092**
  ```
  `./arroyo check` default + `UNAOS_NET4=1 UNAOS_TEGRA=1` + `UNAOS_VNET=1` both arches (zero net-new
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5093**
  ```
  warnings); `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6; `UNAOS_NET4=1 UNAOS_GICV3=1 test-arm 40` `PC...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6348**
  ```
  `./arroyo check` and `UNAOS_NET4=1 UNAOS_SDMMC=1 UNAOS_TEGRA=1 UNAOS_GICV3=1 ./arroyo check` — both
  ```
- **review/unaos-net-dhcp-LANDING.md:47**
  ```
  - `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both arches) — **Finished** (green).
  ```
- **review/unaos-net-phy-LANDING.md:4**
  ```
  `any(feature = "net4", feature = "vnet")`; exercised via `UNAOS_NET4`/`UNAOS_TEGRA` and `UNAOS_VN...
  ```
- **review/unaos-net-phy-LANDING.md:54**
  ```
  | `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **review/unaos-orin-net4-LANDING.md:3**
  ```
  **Branch:** `hw-jetson` (base `main` @ `32e6c34`). **Knob:** `UNAOS_NET4=1` (feature `net4 = ["pc...
  ```
- **review/unaos-orin-net4-LANDING.md:24**
  ```
  | M4 | (this commit) | arch_arm64.md §ORIN-NET-4 (design + the r21c/NET-3 metal-facts fold: RTL81...
  ```
- **review/unaos-orin-net4-LANDING.md:48**
  ```
  | `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both arches) | OK, **zero net-new warnings** (aarc...
  ```
- **review/unaos-orin-net4-LANDING.md:49**
  ```
  | `UNAOS_NET4=1 ./arroyo check` (virt, both arches) | OK (aarch64 = 15 = default/pcie3 baseline) |
  ```
- **review/unaos-orin-net4-LANDING.md:53**
  ```
  | `UNAOS_NET4=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | the `PCIE4` witness line fires (`RTL8168 dr...
  ```
- **review/unaos-orin-net4-LANDING.md:57**
  ```
  `net4` is default-OFF, armed only by `UNAOS_NET4=1`, and NOT stripped by `arm_features` (it is a ...
  ```
- **review/unaos-orin-net4-LANDING.md:67**
  ```
  `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`; stage to `~/unaos-bench/flash/orin/` per the fl...
  ```
- **review/unaos-orin-net4b-LANDING.md:78**
  ```
  - `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` — x86_64 OK + aarch64 OK (no rtl8168 warnings).
  ```
- **review/unaos-orin-net4b-LANDING.md:82**
  ```
  - `UNAOS_NET4=1 UNAOS_GICV3=1 ./arroyo test-arm 40` — `PCIE4` witness line present + CAPSTONE COM...
  ```
- **review/unaos-x-net-phy-LANDING.md:64**
  ```
  | `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **review/unaos-x-net-phy-LANDING.md:66**
  ```
  | `UNAOS_NET4=1 UNAOS_VNET=1 UNAOS_TEGRA=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **unaos/arroyo:202**
  ```
  # ORIN-NET-4: UNAOS_NET4=1 arms the Realtek RTL8168/8111 GbE driver + smoltcp bind — the Orin's F...
  ```
- **unaos/arroyo:211**
  ```
  [ -n "${UNAOS_NET4:-}" ] && _feats="${_feats}net4,pcie3,pcie2,"
  ```
- **unaos/crates/kernel/Cargo.toml:245**
  ```
  # ORIN-NET-4: the `UNAOS_NET4=1` Realtek RTL8168/8111 GbE driver + smoltcp bind — the Orin's FIRST
  ```
- **unaos/crates/kernel/Cargo.toml:254**
  ```
  # only prints one honest witness line; the metal build combines `UNAOS_NET4=1 UNAOS_TEGRA=1`. Every
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:112**
  ```
  // ORIN-NET-4 (`UNAOS_NET4=1`, implies `pcie3`): the Realtek RTL8168/8111 GbE driver + smoltcp bi...
  ```
- **unaos/crates/kernel/src/arch/aarch64/rtl8168_tegra.rs:21**
  ```
  // (`net4_witness_virt`); only `UNAOS_NET4=1 UNAOS_TEGRA=1` on real Orin silicon exercises the ri...
  ```
- **unaos/crates/kernel/src/arch/aarch64/rtl8168_tegra.rs:66**
  ```
  "{} ORIN-NET-4 RTL8168 driver compiled; no Tegra234 RC on this build (QEMU virt) — bring-up is me...
  ```
- **unaos/crates/kernel/src/main.rs:226**
  ```
  // ORIN-NET-4 (QEMU witness, `UNAOS_NET4=1` without `UNAOS_TEGRA=1`): QEMU virt models no
  ```
- **unaos/crates/kernel/src/main.rs:1448**
  ```
  // ORIN-NET-4 (RTL8168/8111 GbE driver + smoltcp bind, `UNAOS_NET4=1`): with `net4` armed (implies
  ```
- **unaos/scripts/orin-net4-bench.md:26**
  ```
  - **`UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the driver image. `net4` implies `pcie3`...
  ```

## `UNAOS_NET4_DHCP_MS`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6341**
  ```
  - **Knob-tunable window**: `UNAOS_NET4_DHCP_MS=<millis>` at build time; unset default stays 5000.
  ```
- **unaos/crates/kernel/src/arch/aarch64/rtl8168_tegra.rs:1081**
  ```
  /// NET-4c: the discover window is knob-tunable — `UNAOS_NET4_DHCP_MS=<millis>` at build time
  ```
- **unaos/crates/kernel/src/arch/aarch64/rtl8168_tegra.rs:1085**
  ```
  if let Some(s) = option_env!("UNAOS_NET4_DHCP_MS") {
  ```

## `UNAOS_NOEHCIHID`

- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:905**
  ```
  the EHCI HID driver now runs **by default**; `UNAOS_NOEHCIHID=1` **opts out**.
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:914**
  ```
  `UNAOS_NOEHCIHID=1`. Every existing `#[cfg(feature = "ehcihid" | "ehciconfig" | "ehciscout")]`
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:927**
  ```
  arm the probes; pair either with `UNAOS_NOEHCIHID=1` for probe-without-driver (the old
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:931**
  ```
  **Gates (M1).** Knob-matrix `./arroyo check` green both arches for default, `UNAOS_NOEHCIHID=1`,
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:932**
  ```
  `UNAOS_EHCISCOUT=1 UNAOS_EHCICONFIG=1`, and `UNAOS_NOEHCIHID=1 UNAOS_EHCICONFIG=1`
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:938**
  ```
  default** now (was `UNAOS_EHCIHID`-gated); `UNAOS_NOEHCIHID=1` restores the pre-fold harness
  ```
- **unaos/arroyo:88**
  ```
  # the driver). Pair with UNAOS_NOEHCIHID=1 for pure evidence (no driver). Kept in sync with builder.
  ```
- **unaos/arroyo:92**
  ```
  # compiled by default via `ehcihid` (implies ehciconfig->ehciscout). UNAOS_NOEHCIHID=1 OPTS OUT =>
  ```
- **unaos/arroyo:95**
  ```
  [ -z "${UNAOS_NOEHCIHID:-}" ] && _feats="${_feats}ehcihid,"
  ```
- **unaos/builder/src/main.rs:79**
  ```
  // UNAOS_NOEHCIHID=1 for pure evidence (no driver). x86_64-only. Kept in sync with arroyo.
  ```
- **unaos/builder/src/main.rs:83**
  ```
  // the ACPI-root retention it uses) UNLESS opted out with UNAOS_NOEHCIHID=1, which unlinks the
  ```
- **unaos/builder/src/main.rs:87**
  ```
  if std::env::var("UNAOS_NOEHCIHID").is_err() { feats.push("ehcihid"); }
  ```
- **unaos/builder/src/main.rs:357**
  ```
  // routing ride by default; UNAOS_NOEHCIHID=1 restores the pre-fold harness (kbd on xHCI, no EHCI
  ```
- **unaos/builder/src/main.rs:359**
  ```
  let ehcihid = std::env::var("UNAOS_NOEHCIHID").is_err();
  ```
- **unaos/builder/src/main.rs:363**
  ```
  println!("   EHCI-HID (default-on): usb-ehci controller attached — EHCI HID driver target (usb-kb...
  ```
- **unaos/crates/kernel/Cargo.toml:95**
  ```
  # =1 / UNAOS_EHCICONFIG=1 arm the probes; pair with UNAOS_NOEHCIHID=1 for probe-without-driver.
  ```
- **unaos/crates/kernel/Cargo.toml:115**
  ```
  # suppresses it under UNAOS_NOEHCIHID=1 (the PORTSW-1 negative-knob policy). Opt-out => this feature
  ```
- **unaos/crates/kernel/src/arch/x86_64/pci.rs:220**
  ```
  // EHCI-3 driver (EHCI-4 M1: DEFAULT-ON, opt out with UNAOS_NOEHCIHID=1): the minimal EHCI HID
  ```
- **unaos/crates/kernel/src/drivers/ehci/mod.rs:5**
  ```
  //! on x86 (the internal keyboard is metal-proven to type; usb_xhci.md §10a); `UNAOS_NOEHCIHID=1`
  ```

## `UNAOS_NOPORTSW`

- **docs/MILESTONES.md:657**
  ```
  feature / `UNAOS_PORTSW` knob replaced by an opt-OUT `noportsw` / `UNAOS_NOPORTSW` (for
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:403**
  ```
  `noportsw` Cargo feature (`UNAOS_NOPORTSW=1`, mapped in both `arroyo` and
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:644**
  ```
  `UNAOS_NOPORTSW=1`, metal-gated policy) routes the 2012 rMBP shared USB2/USB3 ports onto
  ```
- **unaos/arroyo:79**
  ```
  # by default. UNAOS_NOPORTSW=1 OPTS OUT (never-run no-routing experiment) => zero config-space wr...
  ```
- **unaos/arroyo:81**
  ```
  [ -n "${UNAOS_NOPORTSW:-}" ] && _feats="${_feats}noportsw,"
  ```
- **unaos/builder/src/main.rs:65**
  ```
  // 2026-07-16: the no-routing boot dropped ALL external USB on the 2012 rMBP). UNAOS_NOPORTSW=1
  ```
- **unaos/builder/src/main.rs:68**
  ```
  if std::env::var("UNAOS_NOPORTSW").is_ok() { feats.push("noportsw"); }
  ```
- **unaos/crates/kernel/Cargo.toml:81**
  ```
  # feature (UNAOS_NOPORTSW=1) SUPPRESSES the flip for the never-run no-routing topology experiment =>
  ```
- **unaos/crates/kernel/src/arch/x86_64/pci.rs:100**
  ```
  /// DEFAULT-ON here). Suppressed ONLY under the `noportsw` opt-out feature (UNAOS_NOPORTSW=1) for...
  ```
- **unaos/crates/kernel/src/arch/x86_64/pci.rs:271**
  ```
  // PORTSW-1 (DEFAULT-ON, opt out with UNAOS_NOPORTSW=1): route USB2/USB3 ports from the EHCI
  ```
- **unaos/crates/kernel/src/arch/x86_64/pci.rs:275**
  ```
  // routing runs by default. Opt out (UNAOS_NOPORTSW=1) => this call does not exist and no
  ```

## `UNAOS_NOSMOLNET`

- **docs/ROADMAP.md:89**
  ```
  `03105f0` — opt-out knob `UNAOS_NOSMOLNET`); the knob-on framing above is historical.
  ```
- **docs/dev/OS/06_NETWORK_STACK/network_stack.md:15**
  ```
  > `UNAOS_NOSMOLNET=1` — **available for resumption** if we choose to resume
  ```
- **docs/dev/OS/06_NETWORK_STACK/network_stack.md:16**
  ```
  > hand-rolling our own. Build with `UNAOS_NOSMOLNET=1` to run everything below as
  ```
- **review/unaos-smolnet-default-LANDING.md:16**
  ```
  **Default-ON positive cargo feature `smolnet` + `UNAOS_NOSMOLNET` opt-out ENV, plus an aarch64
  ```
- **review/unaos-smolnet-default-LANDING.md:21**
  ```
  `UNAOS_SMOLNET` to opt-OUT `UNAOS_NOSMOLNET`, mirroring the established PORTSW-1/EHCI-4 default-ON +
  ```
- **review/unaos-smolnet-default-LANDING.md:52**
  ```
  still the whole opt-out stack under `UNAOS_NOSMOLNET=1`.
  ```
- **review/unaos-smolnet-default-LANDING.md:56**
  ```
  (smoltcp default, `UNAOS_NOSMOLNET` opt-out, arm_features strip + byte-identity), added a "retired
  ```
- **review/unaos-smolnet-default-LANDING.md:58**
  ```
  - **Resume hand-rolling our own** = build `UNAOS_NOSMOLNET=1`. Proven to work (opt-out gate below).
  ```
- **review/unaos-smolnet-default-LANDING.md:71**
  ```
  - `UNAOS_NOSMOLNET=1` → `⚡ kernel features: ehcihid` → `✅ x86_64 OK` / `✅ aarch64 OK`
  ```
- **review/unaos-smolnet-default-LANDING.md:85**
  ```
  - **Opt-out `UNAOS_NOSMOLNET=1 ./arroyo test 40`:** `⚡ kernel features: ehcihid` → `MISSION SUCCE...
  ```
- **review/unaos-smolnet-default-LANDING.md:99**
  ```
  2. **Opt-out path still builds + works** — VERIFIED. `UNAOS_NOSMOLNET=1` check green both arches;...
  ```
- **review/unaos-smolnet-default-LANDING.md:104**
  ```
  proven `UNAOS_NOSMOLNET=1` build.
  ```
- **unaos/arroyo:114**
  ```
  # — it still backs connect/fetch/udpsend, the TCP echo listener, and driver DHCP). UNAOS_NOSMOLNET=1
  ```
- **unaos/arroyo:120**
  ```
  [ -z "${UNAOS_NOSMOLNET:-}" ] && _feats="${_feats}smolnet,"
  ```
- **unaos/builder/src/main.rs:100**
  ```
  // ping/arp/netinfo + socket syscalls + boot witnesses) UNLESS opted out with UNAOS_NOSMOLNET=1,
  ```
- **unaos/builder/src/main.rs:104**
  ```
  if std::env::var("UNAOS_NOSMOLNET").is_err() { feats.push("smolnet"); }
  ```
- **unaos/crates/kernel/Cargo.toml:124**
  ```
  # UNAOS_NOSMOLNET=1 opts out (the feature is not pushed) => the hand-rolled `net` crate is the whole
  ```
- **unaos/crates/kernel/src/arch/x86_64/syscall.rs:124**
  ```
  // `smolnet` feature (DEFAULT-ON since SMOLNET-DEFAULT; dropped under `UNAOS_NOSMOLNET=1`); aarch...
  ```
- **unaos/crates/kernel/src/smolnet.rs:21**
  ```
  // `UNAOS_NOSMOLNET=1` (the opt-out to the hand-rolled stack). The hand-rolled `net` engines stay
  ```
- **unaos/crates/net/README.md:27**
  ```
  `UNAOS_NOSMOLNET=1` (e.g. `UNAOS_NOSMOLNET=1 ./arroyo test 40`). That drops the
  ```
- **unaos/crates/net/README.md:27**
  ```
  `UNAOS_NOSMOLNET=1` (e.g. `UNAOS_NOSMOLNET=1 ./arroyo test 40`). That drops the
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:17**
  ```
  | | Default (smoltcp) | Opt-out (`UNAOS_NOSMOLNET=1`) |
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:35**
  ```
  It is **pushed by default** on x86; `UNAOS_NOSMOLNET=1` opts out (the negative-knob idiom mirroring
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:39**
  ```
  - **Opt-out (`UNAOS_NOSMOLNET=1`), x86** → the feature is dropped, smoltcp is never pulled, and the
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:52**
  ```
  `unaos/arroyo` (the feature map — pushes `smolnet` unless `UNAOS_NOSMOLNET` is set; strips it for
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:54**
  ```
  before launching QEMU, so it maps `UNAOS_NOSMOLNET` → drop `smolnet` independently; it produces only
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:65**
  ```
  `UNAOS_NOSMOLNET=1`; that opt-out x86 build is byte-identical to the pre-flip default.
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:407**
  ```
  - **The hand-rolled DHCP still runs in the driver** (it is the live stack under `UNAOS_NOSMOLNET=1`)
  ```

## `UNAOS_NOSTORAGE`

- **docs/MILESTONES.md:106**
  ```
  usb.img → flush self-skips silently); `UNAOS_NOSTORAGE=1 ./arroyo test 40` 11 PASS
  ```
- **docs/MILESTONES.md:170**
  ```
  usb.img → flush self-skips silently); `UNAOS_NOSTORAGE=1 ./arroyo test 40` 11 PASS
  ```
- **docs/MILESTONES.md:628**
  ```
  (0 S9 lines, 0 FAIL). `UNAOS_NOSTORAGE=1 test 90` clean. `test-arm 22` MISSION SUCCESS.
  ```
- **docs/MILESTONES.md:781**
  ```
  OK; `test-arm 22` MISSION SUCCESS (0 FAIL, aarch64 clock untouched); `UNAOS_NOSTORAGE=1 test 90` ...
  ```
- **docs/MILESTONES.md:825**
  ```
  chain 0 FAIL (U11m2 PASS); `test-arm 22` MISSION SUCCESS; `UNAOS_NOSTORAGE=1` clean (U1b PASS, ke...
  ```
- **docs/MILESTONES.md:2931**
  ```
  **byte-identical** (no STOR-1 lines). `UNAOS_NOSTORAGE` clean both knob states. Lane: new
  ```
- **docs/MILESTONES.md:2986**
  ```
  **byte-identical** (no S4/synchronous lines — the deferred path verbatim). `UNAOS_NOSTORAGE` clea...
  ```
- **docs/MILESTONES.md:3026**
  ```
  u6gx passes non-preemptibly); `UNAOS_NOSTORAGE` clean both. Lane: `arch/x86_64/syscall.rs` +
  ```
- **docs/MILESTONES.md:3072**
  ```
  NAMESPACE lock is uncontended-transparent knob-off); `UNAOS_NOSTORAGE` clean both. Lane: `arch/x8...
  ```
- **docs/MILESTONES.md:3124**
  ```
  (all S7 code `irqstorage`-gated or comment-only — no S7/dynamic lines); `UNAOS_NOSTORAGE` clean b...
  ```
- **docs/MILESTONES.md:3166**
  ```
  (all S8 code `irqstorage`-gated + `FILE_DYNLEN`-keyed — no S8/dynamic lines); `UNAOS_NOSTORAGE` c...
  ```
- **docs/MILESTONES.md:3488**
  ```
  format); `UNAOS_NOSTORAGE=1 test 90` clean skip. Metal: pure syscall logic, storage-gated
  ```
- **docs/MILESTONES.md:4096**
  ```
  ### No-storage capability-chain visibility — inline console-cap demos (U5x/U7x/U8x) run on the me...
  ```
- **docs/MILESTONES.md:4110**
  ```
  gate. New builder knob **`UNAOS_NOSTORAGE=1`** omits the QEMU `usb-storage` device (block absent)...
  ```
- **docs/MILESTONES.md:4112**
  ```
  - **Tested — QEMU:** `UNAOS_NOSTORAGE=1 ./arroyo test 90` → `note='no mass-storage device enumera...
  ```
- **docs/MILESTONES.md:4164**
  ```
  stranded op). `UNAOS_NOSTORAGE=1 ./arroyo test 90` skips the storage-gated arcs cleanly (console-...
  ```
- **docs/MILESTONES.md:4212**
  ```
  FAIL (FAT16); `UNAOS_NOSTORAGE=1 ./arroyo test 90` clean skip (storage-gated, chain-inherited).
  ```
- **docs/SECURITY.md:186**
  ```
  - [x] **No-storage capability-chain visibility — the INLINE console-cap demos (U5x/U7x/U8x) run o...
  ```
- **docs/SECURITY.md:186**
  ```
  - [x] **No-storage capability-chain visibility — the INLINE console-cap demos (U5x/U7x/U8x) run o...
  ```
- **docs/SECURITY.md:186**
  ```
  - [x] **No-storage capability-chain visibility — the INLINE console-cap demos (U5x/U7x/U8x) run o...
  ```
- **docs/SECURITY.md:187**
  ```
  - [x] **Cross-process unlink-defers-free — a GLOBAL open-file refcount defers the DELETE to the l...
  ```
- **docs/SECURITY.md:188**
  ```
  - [x] **UnaFS owner/grants — the by-NAME namespace ACL at `SYS_OPEN` (owned-by-default) + `SYS_FG...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/SECURITY.md:189**
  ```
  - [x] **Interrupt-driven x86 storage — the storage service task + a submit/block/complete syscall...
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:524**
  ```
  UNAOS_FATIMG=sf test 200` → S-chain 0 FAIL; `test 40` → MISSION SUCCESS; `UNAOS_NOSTORAGE=1`
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:586**
  ```
  MISSION SUCCESS; `UNAOS_NOSTORAGE=1` clean.
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:934**
  ```
  boot; `UNAOS_NOSTORAGE=1` clean; `UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf test 200` 0 FAIL / 0
  ```
- **review/unaos-batmon-LANDING.md:57**
  ```
  - `UNAOS_NOSTORAGE=1 ./arroyo test 40` → 0 FAIL, 11 PASS (storage-independent chain), clean.
  ```
- **review/unaos-flight-recorder-LANDING.md:77**
  ```
  - `UNAOS_NOSTORAGE=1 ./arroyo test 40` — 11 PASS, 0 FAIL, 0 panic; service self-skips (no
  ```
- **review/unaos-xenum3-LANDING.md:55**
  ```
  - `UNAOS_NOSTORAGE=1 ./arroyo test 40` — clean, 0 FAIL (U1b/U2-0a/U3/U3.5/U5x/U7x/U8x PASS).
  ```
- **review/unaos-xenum3-LANDING.md:78**
  ```
  `test 40` — `MISSION SUCCESS`; `UNAOS_NOSTORAGE=1 test 40` — 11 PASS / 0 FAIL.
  ```
- **review/unaos-xenum4-LANDING.md:53**
  ```
  `UNAOS_NOSTORAGE=1 ./arroyo test 40`:
  ```
- **unaos/builder/src/main.rs:335**
  ```
  // UNAOS_NOSTORAGE=1 omits the usb-storage device entirely, so the kernel enumerates NO block device
  ```
- **unaos/builder/src/main.rs:340**
  ```
  // shows. NOTE: `is_ok()` treats an EMPTY value as SET (the known knob trap) — `UNAOS_NOSTORAGE=`...
  ```
- **unaos/builder/src/main.rs:341**
  ```
  let nostorage = std::env::var("UNAOS_NOSTORAGE").is_ok();
  ```
- **unaos/builder/src/main.rs:371**
  ```
  println!("   UNAOS_NOSTORAGE: usb-storage omitted — kernel sees no block device (metal-like no-st...
  ```

## `UNAOS_NOTEGRASMP`

- **docs/MILESTONES.md:49**
  ```
  ### ORIN-SMP-DEFAULT — tegrasmp default-on, `UNAOS_NOTEGRASMP` opt-out
  ```
- **docs/MILESTONES.md:53**
  ```
  `UNAOS_TEGRASMP=1` to the tegra **default**, with a new opt-*out* `UNAOS_NOTEGRASMP=1`
  ```
- **docs/MILESTONES.md:58**
  ```
  `UNAOS_NOTEGRASMP=1`; `unaos/builder/src/main.rs` maps the explicit knob for parity
  ```
- **docs/MILESTONES.md:61**
  ```
  **How it was tested.** `./arroyo check` both arches × {default, `UNAOS_NOTEGRASMP=1`,
  ```
- **docs/MILESTONES.md:64**
  ```
  `UNAOS_NOTEGRASMP=1` is the distinct boot-core-only baseline (`cb7df0a4…`, `tegra:` 109,
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5261**
  ```
  ### ORIN-SMP-DEFAULT — the 6-core bring-up is now the tegra DEFAULT (`UNAOS_NOTEGRASMP` opt-out)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5275**
  ```
  `tegrasmp` unless opted out. `UNAOS_NOTEGRASMP=1` suppresses the push → the tegra image runs
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5280**
  ```
  sync-completeness; `UNAOS_NOTEGRASMP` is a no-op there.
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5286**
  ```
  the `UNAOS_NOTEGRASMP=1` opt-out is byte-identical to the pre-arc default. Verified in-tree at this
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5288**
  ```
  (`sha256 bde59b4f…`); the `UNAOS_NOTEGRASMP=1` opt-out is the distinct boot-core-only baseline
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5291**
  ```
  **Gates green:** `./arroyo check` both arches × {default, `UNAOS_NOTEGRASMP=1`, `UNAOS_TEGRA=1`};
  ```
- **unaos/arroyo:139**
  ```
  # `tegrasmp` UNLESS opted out with UNAOS_NOTEGRASMP=1, which drops the kick-off => the tegra imag...
  ```
- **unaos/arroyo:144**
  ```
  [ -n "${UNAOS_TEGRA:-}" ] && [ -z "${UNAOS_NOTEGRASMP:-}" ] && _feats="${_feats}tegrasmp,tegra,"
  ```
- **unaos/arroyo:701**
  ```
  # UNAOS_NOTEGRASMP=1 => boot-core-only, byte-identical to the pre-flip baseline). Idempotent.
  ```
- **unaos/arroyo:702**
  ```
  if [ -z "${UNAOS_NOTEGRASMP:-}" ]; then
  ```
- **unaos/builder/src/main.rs:113**
  ```
  // UNAOS_NOTEGRASMP=1). `tegrasmp` implies the aarch64 `tegra` board feature; this x86_64 builder
  ```
- **unaos/builder/src/main.rs:115**
  ```
  // maps the EXPLICIT UNAOS_TEGRASMP=1 knob for parity only. UNAOS_NOTEGRASMP is a no-op here (not...
  ```
- **unaos/crates/kernel/Cargo.toml:147**
  ```
  # UNAOS_NOTEGRASMP=1 opts out) — metal-proven across SMP-1..8. With the feature OFF (UNAOS_NOTEGR...
  ```
- **unaos/crates/kernel/Cargo.toml:147**
  ```
  # UNAOS_NOTEGRASMP=1 opts out) — metal-proven across SMP-1..8. With the feature OFF (UNAOS_NOTEGR...
  ```

## `UNAOS_NSSPAN`

- **docs/MILESTONES.md:296**
  ```
  `UNAOS_NSSPAN=1` knob (TCG — NOT the verdict; the new `NS-SPAN-K9` line carries worst flips/block...
  ```
- **docs/MILESTONES.md:1103**
  ```
  (`UNAOS_NSSPAN=1` → cargo feature `nsspan`) CNTPCT instrument measures the worst-case IRQ-masked ...
  ```
- **docs/SECURITY.md:268**
  ```
  - **K7 (wholesale legacy-sidecar ENFORCEMENT removal — 2026-07-15, ✅✅ METAL-CONFIRMED same day; t...
  ```
- **review/unaos-k9-maskcut-LANDING.md:105**
  ```
  ## M2 — before/after (QEMU, `UNAOS_NSSPAN=1`; TCG, NOT the verdict)
  ```
- **unaos/arroyo:828**
  ```
  # NS-SPAN (K7 WATCH rider): UNAOS_NSSPAN=1 adds the `nsspan` feature — instrument the IRQ-masked
  ```
- **unaos/arroyo:832**
  ```
  if [ -n "${UNAOS_NSSPAN:-}" ]; then
  ```
- **unaos/arroyo:834**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_NSSPAN: instrument the ns-lock hold at revoke/grants/grow, emit...
  ```
- **unaos/crates/kernel/Cargo.toml:362**
  ```
  # rider is `#[cfg(feature = "nsspan")]`-gated; the core3probe idiom). Enable via `UNAOS_NSSPAN=1 ...
  ```

## `UNAOS_PACKAGE_ONLY`

- **review/unaos-vmimage-LANDING.md:29**
  ```
  right before the existing `UNAOS_PACKAGE_ONLY` block. It fires AFTER the ESP
  ```
- **unaos/arroyo:605**
  ```
  UNAOS_PACKAGE_ONLY=1 cargo +nightly run --release
  ```
- **unaos/builder/src/main.rs:190**
  ```
  if std::env::var("UNAOS_PACKAGE_ONLY").is_ok() {
  ```

## `UNAOS_PANEL_SNAPSHOT`

- **libs/quartzite/src/platforms/macos/tone_panel.rs:365**
  ```
  /// Dev affordance: `UNAOS_PANEL_SNAPSHOT=/path.png` writes a pixel-true PNG
  ```
- **libs/quartzite/src/platforms/macos/tone_panel.rs:387**
  ```
  if let Ok(snap_path) = std::env::var("UNAOS_PANEL_SNAPSHOT") {
  ```
- **vessels/phonolite/README.md:54**
  ```
  Dev affordance: `UNAOS_PANEL_SNAPSHOT=/path.png` (with optional
  ```

## `UNAOS_PANEL_SNAPSHOT_DELAY_MS`

- **libs/quartzite/src/platforms/macos/tone_panel.rs:366**
  ```
  /// of the panel after `UNAOS_PANEL_SNAPSHOT_DELAY_MS` (default 1500) — the
  ```
- **libs/quartzite/src/platforms/macos/tone_panel.rs:388**
  ```
  let delay_ms = std::env::var("UNAOS_PANEL_SNAPSHOT_DELAY_MS")
  ```
- **vessels/phonolite/README.md:55**
  ```
  `UNAOS_PANEL_SNAPSHOT_DELAY_MS`, default 1500) makes the panel write a
  ```

## `UNAOS_PCIE2`

- **docs/MILESTONES.md:15**
  ```
  ### ORIN-NET-2 — controller-0 link + device recon via the DBI aperture (`UNAOS_PCIE2`) 🔬
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4759**
  ```
  ### ORIN-NET-2 — controller-0 link + device enumeration (`UNAOS_PCIE2`, knob-gated; the last reco...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4768**
  ```
  All code extends `arch/aarch64/pcie_probe.rs` behind the `pcie2` cargo feature (`UNAOS_PCIE2=1`),
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4856**
  ```
  witnesses the graceful skip); `kernel8-test` 0-FAIL; `esp-jetson` (+`UNAOS_PCIE2=1`) links. Bench
  ```
- **unaos/arroyo:182**
  ```
  # ORIN-NET-2: UNAOS_PCIE2=1 arms the controller-0 link + device-enumeration recon
  ```
- **unaos/arroyo:190**
  ```
  [ -n "${UNAOS_PCIE2:-}" ] && _feats="${_feats}pcie2,"
  ```
- **unaos/crates/kernel/Cargo.toml:217**
  ```
  # ORIN-NET-2: the `UNAOS_PCIE2=1` controller-0 link + device-enumeration recon
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:101**
  ```
  // ORIN-NET-1 (`UNAOS_PCIEPROBE=1`) + ORIN-NET-2 (`UNAOS_PCIE2=1`): read-only PCIe root-complex +...
  ```
- **unaos/crates/kernel/src/main.rs:206**
  ```
  // ORIN-NET-2 (QEMU graceful-skip WITNESS, `UNAOS_PCIE2=1`): same read-only recon on the virt
  ```
- **unaos/crates/kernel/src/main.rs:1431**
  ```
  // ORIN-NET-2 (controller-0 link + device recon, `UNAOS_PCIE2=1`): with the `pcie2` feature armed,
  ```
- **unaos/scripts/orin-net2-bench.md:17**
  ```
  - **`UNAOS_PCIE2=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the recon image. The `pcie2` feature is
  ```

## `UNAOS_PCIE3`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4859**
  ```
  ### ORIN-NET-3 — PS widen + controller-0 link bring-up + device enumeration (`UNAOS_PCIE3`, knob-...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4878**
  ```
  behind the `pcie3` cargo feature (`UNAOS_PCIE3=1`), which **implies `pcie2`** (it builds on NET-2's
  ```
- **review/unaos-orin-net3-LANDING.md:3**
  ```
  **Branch:** `us-orinnet3` (base `main` @ `3fe218a`). **Knob:** `UNAOS_PCIE3=1` (feature `pcie3 = ...
  ```
- **review/unaos-orin-net3-LANDING.md:28**
  ```
  runbook `scripts/orin-net3-bench.md`. Cargo/arroyo `UNAOS_PCIE3` wiring.
  ```
- **review/unaos-orin-net3-LANDING.md:49**
  ```
  | `UNAOS_PCIE3=1 UNAOS_GICV3=1 test-arm 40` | **PS-widen witness PASS** (ECAM REACHABLE; refusal ...
  ```
- **unaos/arroyo:191**
  ```
  # ORIN-NET-3: UNAOS_PCIE3=1 arms the PS-widen + controller-0 link bring-up + device-enumeration r...
  ```
- **unaos/arroyo:201**
  ```
  [ -n "${UNAOS_PCIE3:-}" ] && _feats="${_feats}pcie3,pcie2,"
  ```
- **unaos/crates/kernel/Cargo.toml:231**
  ```
  # ORIN-NET-3: the `UNAOS_PCIE3=1` PS-widen + controller-0 link bring-up + device enumeration recon
  ```
- **unaos/crates/kernel/src/main.rs:217**
  ```
  // ORIN-NET-3 (QEMU PS-widen mapping WITNESS, `UNAOS_PCIE3=1`): the metal TCR widen is only
  ```
- **unaos/crates/kernel/src/main.rs:1438**
  ```
  // ORIN-NET-3 (`UNAOS_PCIE3=1`, implies `pcie2`) extends this SAME `census2` call in place: the M...
  ```
- **unaos/scripts/orin-net3-bench.md:17**
  ```
  - **`UNAOS_PCIE3=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the recon image. `pcie3` implies `pcie2`
  ```

## `UNAOS_PCIEPROBE`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4679**
  ```
  ### ORIN-NET-1 — read-only PCIe root-complex + NIC recon (`UNAOS_PCIEPROBE`, knob-gated; census-b...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4690**
  ```
  (`UNAOS_PCIEPROBE=1`). The feature is **standalone** (it does NOT imply `tegra`, mirroring the
  ```
- **unaos/arroyo:174**
  ```
  # ORIN-NET-1: UNAOS_PCIEPROBE=1 arms the read-only PCIe root-complex + NIC recon census
  ```
- **unaos/arroyo:181**
  ```
  [ -n "${UNAOS_PCIEPROBE:-}" ] && _feats="${_feats}pcieprobe,"
  ```
- **unaos/crates/kernel/Cargo.toml:205**
  ```
  # ORIN-NET-1: the `UNAOS_PCIEPROBE=1` read-only PCIe root-complex + NIC recon census
  ```
- **unaos/crates/kernel/Cargo.toml:207**
  ```
  # GICv3 path (so `UNAOS_PCIEPROBE=1 UNAOS_GICV3=1 ./arroyo test-arm 40` witnesses the graceful skip
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:101**
  ```
  // ORIN-NET-1 (`UNAOS_PCIEPROBE=1`) + ORIN-NET-2 (`UNAOS_PCIE2=1`): read-only PCIe root-complex +...
  ```
- **unaos/crates/kernel/src/main.rs:193**
  ```
  // ORIN-NET-1 (QEMU graceful-skip WITNESS, `UNAOS_PCIEPROBE=1`): run the SAME read-only PCIe
  ```
- **unaos/crates/kernel/src/main.rs:1417**
  ```
  // ORIN-NET-1 (read-only PCIe/NIC census, `UNAOS_PCIEPROBE=1`): with the `pcieprobe` feature
  ```
- **unaos/scripts/orin-net1-bench.md:16**
  ```
  - **`UNAOS_PCIEPROBE=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the census image. The `pcieprobe` f...
  ```

## `UNAOS_PI`

- **CLAUDE.md:14**
  ```
  Env knobs: `UNAOS_PI`, `UNAOS_BAREMETAL`, `UNAOS_SKIP_XHCI`, `UNAOS_BOOTLOG`,
  ```
- **docs/dev/OS/08_VIDEO/framebuffer.md:112**
  ```
  | `UNAOS_PI=1` | aarch64: don't touch the QEMU-address UART (it is RAM on a real Pi). |
  ```
- **review/unaos-pi-usb1-LANDING.md:66**
  ```
  - knob-off `UNAOS_PI=1 ./arroyo kernel8-test` — **0 FAIL**; `kernel8.img` **byte-identical to bas...
  ```
- **review/unaos-pi-usb1-LANDING.md:69**
  ```
  - knob-on `UNAOS_PIUSB=1 UNAOS_PI=1 ./arroyo kernel8-test` — **0 FAIL**, DTB-census graceful skip,
  ```
- **unaos/arroyo:46**
  ```
  # UNAOS_PI=1 (Raspberry Pi 4) and UNAOS_TEGRA=1 (Jetson Orin Nano / Tegra234) select the aarch64
  ```
- **unaos/arroyo:61**
  ```
  [ -n "${UNAOS_PI:-}" ] && _feats="${_feats}pi,"
  ```
- **unaos/builder/src/main.rs:39**
  ```
  // USB main loop but keep the boot log on screen + print input events), UNAOS_PI=1 (Pi 4,
  ```
- **unaos/builder/src/main.rs:51**
  ```
  if std::env::var("UNAOS_PI").is_ok() { feats.push("pi"); }
  ```
- **unaos/crates/kernel/Cargo.toml:34**
  ```
  # output, like the Mac. Enable via `UNAOS_PI=1`.
  ```
- **unaos/crates/kernel/Cargo.toml:316**
  ```
  # OFF => the secondary stub is byte-identical. Enable via `UNAOS_CORE3PROBE=1 UNAOS_PI=1 ./arroyo
  ```
- **unaos/crates/kernel/Cargo.toml:325**
  ```
  # probe detects absence and degrades gracefully. Enable via `UNAOS_V3D=1 UNAOS_PI=1 ./arroyo
  ```
- **unaos/crates/kernel/Cargo.toml:337**
  ```
  # UNAOS_PI=1 ./arroyo kernel8`. See arch_arm64.md §PI-USB and scripts/pi-usb1-bench.md.
  ```
- **unaos/crates/kernel/Cargo.toml:351**
  ```
  # so the kernel8 image is byte-identical to baseline. Enable via `UNAOS_GENET=1 UNAOS_PI=1 ./arroyo
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:88**
  ```
  // verification is attended metal. Enable via `UNAOS_V3D=1 UNAOS_PI=1 ./arroyo kernel8`.
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:97**
  ```
  // `UNAOS_PIUSB=1 UNAOS_PI=1 ./arroyo kernel8`. See arch_arm64.md §PI-USB.
  ```
- **unaos/scripts/core3-probe-bench.md:44**
  ```
  UNAOS_CORE3PROBE=1 UNAOS_PI=1 ./arroyo kernel8
  ```
- **unaos/scripts/pi-genet-bench.md:14**
  ```
  UNAOS_GENET=1 UNAOS_PI=1 ./arroyo kernel8
  ```
- **unaos/scripts/pi-usb1-bench.md:18**
  ```
  UNAOS_PIUSB=1 UNAOS_PI=1 ./arroyo kernel8
  ```
- **unaos/scripts/pi-v3d-bench.md:12**
  ```
  UNAOS_V3D=1 UNAOS_PI=1 ./arroyo kernel8
  ```

## `UNAOS_PIINSTALL`

- **review/unaos-install-pi-LANDING.md:93**
  ```
  lines, arroyo run-mode wiring + `UNAOS_PIINSTALL` knob family, named docs. The emmc2 driver's rea...
  ```
- **unaos/arroyo:865**
  ```
  # INSTALL-PI: the UNAOS_PIINSTALL family — the installer engine's first LIVE end-to-end execution...
  ```
- **unaos/arroyo:870**
  ```
  if [ -n "${UNAOS_PIINSTALL:-}" ]; then
  ```
- **unaos/arroyo:872**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_PIINSTALL: Gate 1 — emmc2 microSD read-only census + announce${...
  ```
- **unaos/arroyo:1025**
  ```
  export UNAOS_PIINSTALL=1 UNAOS_PIINSTALL_ARM=1 UNAOS_PIINSTALL_CONFIRM=1
  ```
- **unaos/crates/kernel/Cargo.toml:396**
  ```
  # INSTALL-PI: the `UNAOS_PIINSTALL` family — the installer engine's first LIVE end-to-end executi...
  ```
- **unaos/crates/kernel/src/install/pi.rs:189**
  ```
  "{} Gate 1 only (UNAOS_PIINSTALL) — read-only census; the write path + install are gated (UNAOS_P...
  ```
- **unaos/crates/kernel/src/lib.rs:60**
  ```
  // UNAOS_PIINSTALL family and reached from the aarch64 bare-metal boot path (no x86 witness call ...
  ```

## `UNAOS_PIINSTALL_ARM`

- **review/unaos-install-pi-LANDING.md:53**
  ```
  :: PIINSTALL: Gate 2 ARMED (UNAOS_PIINSTALL_ARM) — non-destructive scratch write/verify/restore l...
  ```
- **unaos/arroyo:874**
  ```
  if [ -n "${UNAOS_PIINSTALL_ARM:-}" ]; then
  ```
- **unaos/arroyo:876**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_PIINSTALL_ARM: Gate 2 — non-destructive scratch write/verify/re...
  ```
- **unaos/arroyo:1025**
  ```
  export UNAOS_PIINSTALL=1 UNAOS_PIINSTALL_ARM=1 UNAOS_PIINSTALL_CONFIRM=1
  ```
- **unaos/crates/kernel/src/install/pi.rs:189**
  ```
  "{} Gate 1 only (UNAOS_PIINSTALL) — read-only census; the write path + install are gated (UNAOS_P...
  ```
- **unaos/crates/kernel/src/install/pi.rs:214**
  ```
  "{} Gate 2 ARMED (UNAOS_PIINSTALL_ARM) — non-destructive scratch write/verify/restore ladder on t...
  ```

## `UNAOS_PIINSTALL_CONFIRM`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6222**
  ```
  `UNAOS_PIINSTALL_CONFIRM=1` (feature `piinstall_confirm` ⇒ `piinstall_arm` ⇒ `piinstall` ⇒ `barem...
  ```
- **review/unaos-install-pi-LANDING.md:55**
  ```
  :: PIINSTALL: Gate 3 THIRD GATE (UNAOS_PIINSTALL_CONFIRM) — installing UnaOS onto the SEATED Pi m...
  ```
- **unaos/arroyo:878**
  ```
  if [ -n "${UNAOS_PIINSTALL_CONFIRM:-}" ]; then
  ```
- **unaos/arroyo:880**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_PIINSTALL_CONFIRM: Gate 3 — DESTRUCTIVE install (GPT→FAT32→payl...
  ```
- **unaos/arroyo:1014**
  ```
  # Builds kernel8.img with the three install gates armed (UNAOS_PIINSTALL_CONFIRM ⇒ arm ⇒ piinstall),
  ```
- **unaos/arroyo:1025**
  ```
  export UNAOS_PIINSTALL=1 UNAOS_PIINSTALL_ARM=1 UNAOS_PIINSTALL_CONFIRM=1
  ```
- **unaos/crates/kernel/Cargo.toml:408**
  ```
  # `UNAOS_PIINSTALL_CONFIRM=1 ./arroyo kernel8-install` against a DEDICATED BLANK scratch image (n...
  ```
- **unaos/crates/kernel/src/install/pi.rs:287**
  ```
  "{} Gate 3 THIRD GATE (UNAOS_PIINSTALL_CONFIRM) — installing UnaOS onto the SEATED Pi microSD ::",
  ```
- **unaos/docs/dev/OS/10_INSTALL/installer_engine.md:259**
  ```
  **Landed 2026-07-18** (aarch64/Pi bare-metal; QEMU-live). `UNAOS_PIINSTALL_CONFIRM=1` (feature
  ```

## `UNAOS_PIINSTALL_MIB`

- **review/unaos-install-pi-LANDING.md:34**
  ```
  all-zero `target/pi-install-scratch.img` (128 MiB, `UNAOS_PIINSTALL_MIB` overridable) and attache...
  ```
- **unaos/arroyo:1028**
  ```
  local scratch_mib="${UNAOS_PIINSTALL_MIB:-128}"
  ```

## `UNAOS_PIUSB`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6041**
  ```
  byte-identical to baseline** (sha256 in the landing report); knob-on `UNAOS_PIUSB=1 ./arroyo kern...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6129**
  ```
  byte-identity); knob-on `UNAOS_PIUSB=1 ./arroyo kernel8-test` = 0 FAIL with **both** graceful cen...
  ```
- **review/unaos-pi-usb1-LANDING.md:49**
  ```
  **`arroyo`:** `UNAOS_PIUSB=1` → `,piusb`. **Docs:** `docs/.../arch_arm64.md` new §PI-USB;
  ```
- **review/unaos-pi-usb1-LANDING.md:69**
  ```
  - knob-on `UNAOS_PIUSB=1 UNAOS_PI=1 ./arroyo kernel8-test` — **0 FAIL**, DTB-census graceful skip,
  ```
- **review/unaos-pi-usb2-LANDING.md:92**
  ```
  - `./arroyo check` — **x86_64 OK, aarch64 OK**, knob-off **and** knob-on (`UNAOS_PIUSB=1`).
  ```
- **review/unaos-pi-usb2-LANDING.md:98**
  ```
  - knob-on `UNAOS_PIUSB=1 ./arroyo kernel8-test 35` — **0 FAIL** (194 lines), **both** graceful ce...
  ```
- **unaos/arroyo:844**
  ```
  # PI-USB-1: UNAOS_PIUSB=1 adds the `piusb` feature — the BCM2711 PCIe root complex + VL805 xHCI a...
  ```
- **unaos/arroyo:850**
  ```
  if [ -n "${UNAOS_PIUSB:-}" ]; then
  ```
- **unaos/arroyo:852**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_PIUSB: BCM2711 PCIe RC + VL805 xHCI attach (attended-metal for ...
  ```
- **unaos/crates/kernel/Cargo.toml:336**
  ```
  # absence and degrades gracefully; positive verification is attended metal. Enable via `UNAOS_PIU...
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:97**
  ```
  // `UNAOS_PIUSB=1 UNAOS_PI=1 ./arroyo kernel8`. See arch_arm64.md §PI-USB.
  ```
- **unaos/scripts/pi-usb1-bench.md:18**
  ```
  UNAOS_PIUSB=1 UNAOS_PI=1 ./arroyo kernel8
  ```

## `UNAOS_PORTSW`

- **docs/MILESTONES.md:657**
  ```
  feature / `UNAOS_PORTSW` knob replaced by an opt-OUT `noportsw` / `UNAOS_NOPORTSW` (for
  ```
- **docs/MILESTONES.md:812**
  ```
  compiled only under the `portsw` feature (`UNAOS_PORTSW=1`, mapped in `arroyo` + `builder/src/mai...
  ```

## `UNAOS_QEMU_DEBUG_LOG`

- **unaos/builder/src/main.rs:501**
  ```
  if let Ok(dbg_path) = std::env::var("UNAOS_QEMU_DEBUG_LOG") {
  ```

## `UNAOS_QEMU_EXTRA`

- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:168**
  ```
  (via `UNAOS_QEMU_EXTRA`) for the relative path. Inject motion over QMP
  ```
- **review/unaos-videonext-LANDING.md:42**
  ```
  - Headless x86 (`UNAOS_QEMU_EXTRA="-qmp …" ./arroyo test 45`, `tste` driven via `qmp_type.py`):
  ```
- **unaos/arroyo:495**
  ```
  ${UNAOS_QEMU_EXTRA:-}
  ```
- **unaos/arroyo:517**
  ```
  ${UNAOS_QEMU_EXTRA:-} &
  ```
- **unaos/arroyo:553**
  ```
  export UNAOS_QEMU_EXTRA="${UNAOS_QEMU_EXTRA:-} -qmp tcp:127.0.0.1:${qmp_port},server,nowait"
  ```
- **unaos/arroyo:553**
  ```
  export UNAOS_QEMU_EXTRA="${UNAOS_QEMU_EXTRA:-} -qmp tcp:127.0.0.1:${qmp_port},server,nowait"
  ```
- **unaos/arroyo:978**
  ```
  ${UNAOS_QEMU_EXTRA:-}
  ```
- **unaos/arroyo:1005**
  ```
  ${UNAOS_QEMU_EXTRA:-} &
  ```
- **unaos/arroyo:1049**
  ```
  ${UNAOS_QEMU_EXTRA:-} &
  ```
- **unaos/builder/src/main.rs:443**
  ```
  // For slirp wire debugging add UNAOS_QEMU_EXTRA="-object filter-dump,id=d0,netdev=n0,file=target...
  ```
- **unaos/builder/src/main.rs:458**
  ```
  // DIAGNOSTIC: append arbitrary QEMU args from UNAOS_QEMU_EXTRA (whitespace-split), e.g.
  ```
- **unaos/builder/src/main.rs:461**
  ```
  let qemu_extra = std::env::var("UNAOS_QEMU_EXTRA").unwrap_or_default();
  ```
- **unaos/scripts/qmp_shoot.py:14**
  ```
  # and the fresh-OVMF-vars copy. Launch QEMU headless with a QMP port (via UNAOS_QEMU_EXTRA on
  ```
- **unaos/scripts/qmp_type.py:13**
  ```
  # (via UNAOS_QEMU_EXTRA on x86, or arroyo's arm paths), then point this at that port.
  ```

## `UNAOS_QMP_PORT`

- **unaos/arroyo:545**
  ```
  # busy listeners so parallel worktree sessions never collide (UNAOS_QMP_PORT overrides the
  ```
- **unaos/arroyo:551**
  ```
  local qmp_port="${UNAOS_QMP_PORT:-4464}"
  ```
- **unaos/arroyo:995**
  ```
  # QMP port: default 4463, overridable (UNAOS_QMP_PORT) and auto-bumped past ports already in
  ```
- **unaos/arroyo:997**
  ```
  local qmp_port="${UNAOS_QMP_PORT:-4463}"
  ```
- **unaos/arroyo:1041**
  ```
  local qmp_port="${UNAOS_QMP_PORT:-4465}"
  ```

## `UNAOS_QMP_SHOT`

- **docs/dev/OS/08_VIDEO/engine.md:242**
  ```
  final screen → QMP screendump compares (`UNAOS_QMP_SHOT=<png> ./arroyo test 40`,
  ```
- **unaos/arroyo:543**
  ```
  # VPERF: UNAOS_QMP_SHOT=<png path> arms a QMP framebuffer screendump of the headless x86 run
  ```
- **unaos/arroyo:550**
  ```
  if [ -n "${UNAOS_QMP_SHOT:-}" ]; then
  ```
- **unaos/arroyo:555**
  ```
  --wait "${UNAOS_QMP_WAIT:-$((secs - 3))}" --out "${UNAOS_QMP_SHOT}" 2>/dev/null &
  ```
- **unaos/arroyo:557**
  ```
  echo -e "${COLOR_YELLOW}⚡ QMP screendump armed: port ${qmp_port} -> ${UNAOS_QMP_SHOT}${COLOR_RESET}"
  ```

## `UNAOS_QMP_WAIT`

- **unaos/arroyo:547**
  ```
  # compiles, waits UNAOS_QMP_WAIT (default secs-3) after QEMU appears, then dumps — inside the
  ```
- **unaos/arroyo:555**
  ```
  --wait "${UNAOS_QMP_WAIT:-$((secs - 3))}" --out "${UNAOS_QMP_SHOT}" 2>/dev/null &
  ```

## `UNAOS_RAST`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3172**
  ```
  ### RAST-TEGRA — first 3D pixels on the Orin panel (`UNAOS_RAST=1`, ⏳ METAL-PENDING)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3192**
  ```
  `UNAOS_RAST=1 ./arroyo test-arm` prints `:: RAST: … spinning cube … ::` + the fps line, and the boot
  ```
- **docs/dev/OS/08_VIDEO/rasterizer.md:107**
  ```
  ## 4. The demo (`UNAOS_RAST=1`)
  ```
- **docs/dev/OS/08_VIDEO/rasterizer.md:142**
  ```
  witnesses the demo — `UNAOS_RAST=1 ./arroyo test-arm` prints the two `RAST:` lines
  ```
- **review/unaos-rast-pace-LANDING.md:46**
  ```
  - `UNAOS_RAST=1 ./arroyo check` **knob-on** — ✅ x86_64 OK, ✅ aarch64 OK.
  ```
- **review/unaos-rast-pace-LANDING.md:56**
  ```
  - **Paced fps witness** (`UNAOS_RAST=1 ./arroyo test-arm 30`, aarch64/virt ramfb), serial:
  ```
- **review/unaos-rast-pace-LANDING.md:64**
  ```
  - **x86 knob-on unregressed** (`UNAOS_RAST=1 ./arroyo test 30`), serial:
  ```
- **review/unaos-rast-pace-LANDING.md:79**
  ```
  - knob-ON `UNAOS_RAST=1` + `tegra`; effective aarch64 features
  ```
- **review/unaos-rast-tegra-LANDING.md:20**
  ```
  `UNAOS_RAST=1 ./arroyo test-arm` witnesses the identical arch-neutral render under
  ```
- **review/unaos-rast-tegra-LANDING.md:48**
  ```
  - `UNAOS_RAST=1 ./arroyo check` **knob-on** — ✅ x86_64 OK, ✅ aarch64 OK.
  ```
- **review/unaos-rast-tegra-LANDING.md:55**
  ```
  - **Knob-on virt witness** (`UNAOS_RAST=1 ./arroyo test-arm 30`), serial:
  ```
- **review/unaos-rast-tegra-LANDING.md:87**
  ```
  - knob-ON `UNAOS_RAST=1` + `tegra`; effective aarch64 features `rast,ehcihid,tegra`
  ```
- **review/unaos-rast1-LANDING.md:4**
  ```
  crate + one knob-gated x86/virt demo wire-in (`UNAOS_RAST=1`). No Pi/BCM2711 or
  ```
- **review/unaos-rast1-LANDING.md:33**
  ```
  - `./arroyo check` **knob-on** (`UNAOS_RAST=1`) — ✅ x86_64 OK, ✅ aarch64 OK.
  ```
- **review/unaos-rast1-LANDING.md:37**
  ```
  - **Knob-on virt demo witnessed** (`UNAOS_RAST=1 ./arroyo test`), serial:
  ```
- **review/unaos-rast1-LANDING.md:68**
  ```
  - `unaos/arroyo` — one `UNAOS_RAST → rast` `_feats` line.
  ```
- **review/unaos-rast1-LANDING.md:69**
  ```
  - `unaos/builder/src/main.rs` — one `UNAOS_RAST` push line.
  ```
- **unaos/arroyo:71**
  ```
  # RAST-1/RAST-TEGRA: software-rasterizer spinning-cube demo through the panel path (UNAOS_RAST=1).
  ```
- **unaos/arroyo:74**
  ```
  [ -n "${UNAOS_RAST:-}" ] && _feats="${_feats}rast,"
  ```
- **unaos/builder/src/main.rs:63**
  ```
  if std::env::var("UNAOS_RAST").is_ok() { feats.push("rast"); }
  ```
- **unaos/crates/kernel/Cargo.toml:376**
  ```
  # RAST-1 / RAST-TEGRA: the `rast` software-rasterizer demo (UNAOS_RAST=1). Pulls in the
  ```
- **unaos/crates/kernel/src/lib.rs:72**
  ```
  // the public `Screen` API — so it is `rast`-feature-gated (UNAOS_RAST=1) but NOT arch-gated: x86...
  ```
- **unaos/crates/kernel/src/main.rs:1727**
  ```
  tegra_rast_demo_maybe(); unaos_kernel::arch::sched::run_capstone_boot_core(0); // RAST-TEGRA demo...
  ```
- **unaos/crates/kernel/src/rast_demo.rs:7**
  ```
  //! This is the knob-gated (`UNAOS_RAST=1` → `rast` feature) x86/virt wire-in of
  ```

## `UNAOS_SCHED_DEMO`

- **CLAUDE.md:15**
  ```
  `UNAOS_SCHED_DEMO`, `UNAOS_USBDEBUG`.
  ```
- **docs/dev/DEBUGGING.md:58**
  ```
  `UNAOS_SCHED_DEMO=1`, `UNAOS_USBSERIAL=1` (attach an emulated FTDI console —
  ```
- **review/unaos-gui-witness-LANDING.md:89**
  ```
  - `UNAOS_SCHED_DEMO=1 ./arroyo test 30` — `✅` complete; MISSION SUCCESS; sched/demo witnesses
  ```
- **review/unaos-kernel-clock-LANDING.md:70**
  ```
  - `UNAOS_SCHED_DEMO=1 ./arroyo test 30` (x2APIC demo) — witnesses:
  ```
- **review/unaos-kernel-clock-LANDING.md:76**
  ```
  - `UNAOS_CPU=qemu64 UNAOS_SCHED_DEMO=1 ./arroyo test 45` (xAPIC demo, bonus) —
  ```
- **review/unaos-sched-polish-LANDING.md:92**
  ```
  - `UNAOS_SCHED_DEMO=1 ./arroyo test 30` (x2APIC):
  ```
- **review/unaos-sched-polish-LANDING.md:96**
  ```
  - `UNAOS_CPU=qemu64 UNAOS_SCHED_DEMO=1 ./arroyo test 30` (xAPIC) — same five PASS.
  ```
- **unaos/arroyo:63**
  ```
  [ -n "${UNAOS_SCHED_DEMO:-}" ] && _feats="${_feats}sched_demo,"
  ```
- **unaos/builder/src/main.rs:53**
  ```
  if std::env::var("UNAOS_SCHED_DEMO").is_ok() { feats.push("sched_demo"); }
  ```
- **unaos/crates/kernel/Cargo.toml:48**
  ```
  # torn-read check). Opt-in via `UNAOS_SCHED_DEMO=1`: it's a QEMU-verified smoke test whose tick-b...
  ```
- **unaos/crates/kernel/src/main.rs:692**
  ```
  // (UNAOS_SCHED_DEMO=1 -> `sched_demo` feature); by default the scheduler initializes but no
  ```

## `UNAOS_SDIMG`

- **docs/MILESTONES.md:5140**
  ```
  `UNAOS_SDIMG=0` no-SD control adds exactly the two no-card lines + the loader-skipped
  ```
- **docs/dev/OS/02_KERNEL_CORE/userspace.md:139**
  ```
  all unchanged and 0 unexpected faults; the `UNAOS_SDIMG=0` control adds exactly the
  ```
- **unaos/arroyo:328**
  ```
  # UNAOS_SDIMG (kernel8-run / kernel8-test): the microSD image QEMU raspi4b attaches via `if=sd`.
  ```
- **unaos/arroyo:330**
  ```
  # root — so QEMU's card IS the boot image). UNAOS_SDIMG=0 omits the drive (the M6g no-SD control);
  ```
- **unaos/arroyo:968**
  ```
  # QEMU's card IS the boot image, HELLO.BIN in its FAT32 root). UNAOS_SDIMG=0 omits it (regression
  ```
- **unaos/arroyo:970**
  ```
  local SDIMG="${UNAOS_SDIMG:-${WORKSPACE_DIR}/target/UnaOS-pi4-baremetal.img}"
  ```
- **unaos/arroyo:990**
  ```
  # M6g: attach the flashable image as the SD card (UNAOS_SDIMG=0 omits it for the no-SD control; any
  ```
- **unaos/arroyo:992**
  ```
  local SDIMG="${UNAOS_SDIMG:-${WORKSPACE_DIR}/target/UnaOS-pi4-baremetal.img}"
  ```

## `UNAOS_SDMMC`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5303**
  ```
  ### ORIN-SDMMC — Tegra234 microSD-slot SDMMC controller READ-ONLY recon (`UNAOS_SDMMC`, knob-gate...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5358**
  ```
  `UNAOS_SDMMC=1 UNAOS_TEGRA=1` on real Orin silicon touches the controller. Correctness off-metal ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5394**
  ```
  both the metal tegra build (`UNAOS_SDMMC=1 UNAOS_TEGRA=1`) and the virt witness build (`UNAOS_SDM...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5394**
  ```
  both the metal tegra build (`UNAOS_SDMMC=1 UNAOS_TEGRA=1`) and the virt witness build (`UNAOS_SDM...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5396**
  ```
  **Gates green.** `./arroyo check` default + `UNAOS_SDMMC=1 UNAOS_TEGRA=1` + `UNAOS_SDMMC=1` (virt...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5396**
  ```
  **Gates green.** `./arroyo check` default + `UNAOS_SDMMC=1 UNAOS_TEGRA=1` + `UNAOS_SDMMC=1` (virt...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5398**
  ```
  22` + `./arroyo kernel8-test` 0 FAIL; knob-on `UNAOS_SDMMC=1 UNAOS_GICV3=1 ./arroyo test-arm 40` ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5401**
  ```
  #### ORIN-SDMMC-2 — the write path behind the paranoia ladder (`UNAOS_SDMMC_ARM`, a SEPARATE arm ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5405**
  ```
  `UNAOS_SDMMC_ARM=1`. A card write happens only when BOTH are present — the recon knob (`UNAOS_SDM...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5406**
  ```
  never writes the card. **Every line of the write path is `sdmmc_arm`-gated**, so a plain `UNAOS_S...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5455**
  ```
  | 1 | `sdmmc` / `UNAOS_SDMMC` | the controller is up and the card **census succeeded** (we hold a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6348**
  ```
  `./arroyo check` and `UNAOS_NET4=1 UNAOS_SDMMC=1 UNAOS_TEGRA=1 UNAOS_GICV3=1 ./arroyo check` — both
  ```
- **review/unaos-install1-LANDING.md:42**
  ```
  | 1 | `sdmmc` / `UNAOS_SDMMC` | controller up and the card **census succeeded** (we hold a `Card`) |
  ```
- **review/unaos-install1-LANDING.md:75**
  ```
  - **`./arroyo check` across the knob matrix** — green both arches (x86_64 + aarch64) for: default...
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:37**
  ```
  - `unaos/arroyo` — `[ -n "${UNAOS_SDMMC:-}" ] && _feats="${_feats}sdmmc,"`.
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:73**
  ```
  - `UNAOS_SDMMC=1 UNAOS_TEGRA=1 ./arroyo check`: `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:74**
  ```
  - `UNAOS_SDMMC=1 ./arroyo check` (virt): `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:82**
  ```
  - knob-on virt `UNAOS_SDMMC=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: the witness line
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:84**
  ```
  recon is metal-only (UNAOS_SDMMC=1 UNAOS_TEGRA=1) ::` printed, and CAPSTONE 6/6 intact.
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:103**
  ```
  `sdmmc: ORIN-SDMMC-1 — Tegra234 microSD READ-ONLY census (UNAOS_SDMMC)` on `hw-jetson`. Not merge...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:64**
  ```
  `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: exactly the two virt-witnes...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:75**
  ```
  - `UNAOS_SDMMC=1 ./arroyo check` (virt): `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:76**
  ```
  - `UNAOS_SDMMC=1 UNAOS_TEGRA=1 ./arroyo check`: `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:77**
  ```
  - `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 ./arroyo check` (armed virt): `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:78**
  ```
  - `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_TEGRA=1 ./arroyo check` (armed tegra): `✅ x86_64 OK` /
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:85**
  ```
  - armed virt witness `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: prints
  ```
- **unaos/arroyo:212**
  ```
  # ORIN-SDMMC-1: UNAOS_SDMMC=1 arms the Tegra234 microSD-slot SDMMC controller READ-ONLY recon — the
  ```
- **unaos/arroyo:222**
  ```
  [ -n "${UNAOS_SDMMC:-}" ] && _feats="${_feats}sdmmc,"
  ```
- **unaos/arroyo:223**
  ```
  # ORIN-SDMMC-2: UNAOS_SDMMC_ARM=1 is the SEPARATE explicit write ARM on top of UNAOS_SDMMC (doubl...
  ```
- **unaos/arroyo:229**
  ```
  # the read census. UNAOS_SDMMC=1 WITHOUT this is byte-identical to the ORIN-SDMMC-1 recon. See
  ```
- **unaos/crates/kernel/Cargo.toml:260**
  ```
  # ORIN-SDMMC-1: the `UNAOS_SDMMC=1` Tegra234 microSD-slot SDMMC controller READ-ONLY recon
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:126**
  ```
  // ORIN-SDMMC-1 (`UNAOS_SDMMC=1`): the Tegra234 microSD-slot SDMMC controller READ-ONLY recon
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:30**
  ```
  // witness line (`sdmmc_census` below); only `UNAOS_SDMMC=1 UNAOS_TEGRA=1` on real Orin silicon t...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:74**
  ```
  "{} ORIN-SDMMC-1 Tegra234 microSD recon compiled; no Tegra234 SDMMC on this build (QEMU virt) — r...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:1699**
  ```
  //    a plain UNAOS_SDMMC=1 build ends exactly here, byte-identical to the merged recon). ──
  ```
- **unaos/crates/kernel/src/main.rs:234**
  ```
  // ORIN-SDMMC-1 (QEMU witness, `UNAOS_SDMMC=1` without `UNAOS_TEGRA=1`): QEMU models no Tegra234
  ```
- **unaos/crates/kernel/src/main.rs:1457**
  ```
  // ORIN-SDMMC-1 (`UNAOS_SDMMC=1`): the Tegra234 microSD-slot READ-ONLY recon on the metal Orin — the
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:6**
  ```
  - **UNARMED (ORIN-SDMMC-1, `UNAOS_SDMMC=1`)** — the NET-1 house pattern: **read-only census befor...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:10**
  ```
  - **ARMED (ORIN-SDMMC-2, `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1`)** — the write path behind a **paranoi...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:21**
  ```
  - **`UNAOS_SDMMC=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the **unarmed recon** image. Read-only ...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:23**
  ```
  - **`UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the **armed ladder** ...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:26**
  ```
  `UNAOS_SDMMC=1` **without** `UNAOS_SDMMC_ARM=1` is byte-identical in behavior to the merged ORIN-...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:118**
  ```
  The virt witness (no metal, for reference — `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arro...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:121**
  ```
  :: SDMMC: ORIN-SDMMC-1 Tegra234 microSD recon compiled; no Tegra234 SDMMC on this build (QEMU vir...
  ```

## `UNAOS_SDMMC_ARM`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5401**
  ```
  #### ORIN-SDMMC-2 — the write path behind the paranoia ladder (`UNAOS_SDMMC_ARM`, a SEPARATE arm ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5405**
  ```
  `UNAOS_SDMMC_ARM=1`. A card write happens only when BOTH are present — the recon knob (`UNAOS_SDM...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5456**
  ```
  | 2 | `sdmmc_arm` / `UNAOS_SDMMC_ARM` | the rung-2 armed CMD24 write path is compiled in |
  ```
- **review/unaos-install1-LANDING.md:43**
  ```
  | 2 | `sdmmc_arm` / `UNAOS_SDMMC_ARM` | the rung-2 armed CMD24 write path is compiled in |
  ```
- **review/unaos-install1-LANDING.md:76**
  ```
  `UNAOS_SDMMC_ARM`, `UNAOS_INSTALL_TARGET_SD` (each × virt and × `UNAOS_TEGRA`), plus `UNAOS_INSTA...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:12**
  ```
  (requires `sdmmc`) wired to `UNAOS_SDMMC_ARM=1`, plus its doc refresh. A card write happens only ...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:31**
  ```
  - `unaos/arroyo` — `[ -n "${UNAOS_SDMMC_ARM:-}" ] && _feats="${_feats}sdmmc_arm,sdmmc,"`.
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:58**
  ```
  `UNAOS_SDMMC_ARM=1`. Concretely:
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:64**
  ```
  `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: exactly the two virt-witnes...
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:77**
  ```
  - `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 ./arroyo check` (armed virt): `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:78**
  ```
  - `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_TEGRA=1 ./arroyo check` (armed tegra): `✅ x86_64 OK` /
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:85**
  ```
  - armed virt witness `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arroyo test-arm 40`: prints
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:87**
  ```
  (UNAOS_SDMMC_ARM=1) but metal-only … zero card writes here ::`; CAPSTONE 6/6 intact.
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:86**
  ```
  - **String-identity (esp-jetson builds, established method):** the unarmed `UNAOS_SDMMC_ARM=1` je...
  ```
- **unaos/arroyo:223**
  ```
  # ORIN-SDMMC-2: UNAOS_SDMMC_ARM=1 is the SEPARATE explicit write ARM on top of UNAOS_SDMMC (doubl...
  ```
- **unaos/arroyo:225**
  ```
  # the `sdmmc_arm` feature (which requires `sdmmc`, added here for a lone UNAOS_SDMMC_ARM=1) that ...
  ```
- **unaos/arroyo:231**
  ```
  [ -n "${UNAOS_SDMMC_ARM:-}" ] && _feats="${_feats}sdmmc_arm,sdmmc,"
  ```
- **unaos/arroyo:239**
  ```
  # full install after the read census + rung-2 ladder. UNAOS_SDMMC_ARM=1 WITHOUT this is byte-iden...
  ```
- **unaos/crates/kernel/Cargo.toml:274**
  ```
  # ORIN-SDMMC-2: the `UNAOS_SDMMC_ARM=1` write-path ARM. A SEPARATE explicit gate on top of `sdmmc`
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:82**
  ```
  "{} ORIN-SDMMC-2 write ladder ARMED (UNAOS_SDMMC_ARM=1) but metal-only — no Tegra234 SDMMC on thi...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:574**
  ```
  // marker is compiled out unless UNAOS_SDMMC_ARM=1, keeping a plain `sdmmc` build byte-identical ...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:743**
  ```
  "{} ORIN-SDMMC-2 ARMED (UNAOS_SDMMC_ARM=1) — paranoia write ladder on the SEATED card (scratch re...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:1698**
  ```
  // ── ORIN-SDMMC-2: the paranoia write ladder (only with UNAOS_SDMMC_ARM=1; compiled out otherwis...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:1704**
  ```
  //    destructive gate; compiled out otherwise, so a plain UNAOS_SDMMC_ARM=1 build ends at the la...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:10**
  ```
  - **ARMED (ORIN-SDMMC-2, `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1`)** — the write path behind a **paranoi...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:23**
  ```
  - **`UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the **armed ladder** ...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:26**
  ```
  `UNAOS_SDMMC=1` **without** `UNAOS_SDMMC_ARM=1` is byte-identical in behavior to the merged ORIN-...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:96**
  ```
  :: SDMMC: ORIN-SDMMC-2 ARMED (UNAOS_SDMMC_ARM=1) — paranoia write ladder on the SEATED card (scra...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:118**
  ```
  The virt witness (no metal, for reference — `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_GICV3=1 ./arro...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:122**
  ```
  :: SDMMC: ORIN-SDMMC-2 write ladder ARMED (UNAOS_SDMMC_ARM=1) but metal-only — no Tegra234 SDMMC ...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:132**
  ```
  ⇒ `sdmmc_arm` ⇒ `sdmmc`). A plain `UNAOS_SDMMC_ARM=1` image (the armed-ladder image above) does *...
  ```

## `UNAOS_SERIAL_LOG`

- **unaos/arroyo:563**
  ```
  UNAOS_SERIAL_LOG="$SERIAL_LOG" UNAOS_TEST_SECS="$secs" cargo +nightly run --release
  ```
- **unaos/builder/src/main.rs:470**
  ```
  let log_path = std::env::var("UNAOS_SERIAL_LOG")
  ```
- **unaos/builder/src/main.rs:491**
  ```
  // Test mode: set UNAOS_SERIAL_LOG to run headless, redirect serial to that file, and
  ```
- **unaos/builder/src/main.rs:494**
  ```
  if let Ok(log_path) = std::env::var("UNAOS_SERIAL_LOG") {
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:461**
  ```
  `UNAOS_NET=socket UNAOS_SERIAL_LOG=<log> UNAOS_TEST_SECS=60` and, concurrently,
  ```

## `UNAOS_SKIP_XHCI`

- **CLAUDE.md:14**
  ```
  Env knobs: `UNAOS_PI`, `UNAOS_BAREMETAL`, `UNAOS_SKIP_XHCI`, `UNAOS_BOOTLOG`,
  ```
- **docs/MILESTONES.md:1048**
  ```
  report can't double-decode (double cursor motion) or over-arm the ring. **M3** — `UNAOS_SKIP_XHCI...
  ```
- **docs/MILESTONES.md:1056**
  ```
  MISSION SUCCESS; `UNAOS_SKIP_XHCI=1` → zero MOUSE lines, skip banner, no panic; default `test` → ...
  ```
- **docs/dev/DEBUGGING.md:57**
  ```
  (verbose boot), `UNAOS_SKIP_XHCI=1` (skip USB bring-up when isolating),
  ```
- **docs/dev/OS/05_USER_EXPERIENCE/shell_philosophy.md:107**
  ```
  `rdtsc`-bounded; `UNAOS_SKIP_XHCI=1` is the documented escape hatch), and nothing else on the def...
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:166**
  ```
  `UNAOS_SKIP_XHCI` never reach the arm site). QEMU exercises both decode paths:
  ```
- **docs/dev/OS/07_USB_STORAGE/usb_xhci.md:649**
  ```
  support. The `skip_xhci` Cargo feature (`UNAOS_SKIP_XHCI=1`) disables USB bring-up
  ```
- **docs/dev/OS/08_VIDEO/framebuffer.md:110**
  ```
  | `UNAOS_SKIP_XHCI=1` | Video only; skip USB/xHCI bring-up (firmware may still own the controller...
  ```
- **unaos/HARDWARE-BRINGUP.md:74**
  ```
  UNAOS_SKIP_XHCI=1 ./arroyo esp-arm   # packages target/aarch64_esp/ (BOOTAA64.EFI + kernel.elf)
  ```
- **unaos/arroyo:43**
  ```
  # Optional kernel cargo features (composable). UNAOS_SKIP_XHCI=1 disables xHCI/USB bring-up
  ```
- **unaos/arroyo:59**
  ```
  [ -n "${UNAOS_SKIP_XHCI:-}" ] && _feats="${_feats}skip_xhci,"
  ```
- **unaos/builder/src/main.rs:37**
  ```
  // Optional kernel features from env knobs: UNAOS_SKIP_XHCI=1 (disable xHCI/USB bring-up),
  ```
- **unaos/builder/src/main.rs:49**
  ```
  if std::env::var("UNAOS_SKIP_XHCI").is_ok() { feats.push("skip_xhci"); }
  ```
- **unaos/crates/kernel/Cargo.toml:26**
  ```
  # immediately; USB is simply absent (no keyboard). Enable via `UNAOS_SKIP_XHCI=1 ./arroyo ...`.
  ```
- **unaos/crates/kernel/src/main.rs:711**
  ```
  //    Skippable via the `skip_xhci` Cargo feature (UNAOS_SKIP_XHCI=1) so the video stack still
  ```

## `UNAOS_SMC`

- **docs/dev/OS/05_USER_EXPERIENCE/shell_philosophy.md:123**
  ```
  `UNAOS_SMC=1` only); every other target compiles an honest fallback that says so. Under QEMU's
  ```
- **review/unaos-batmon-LANDING.md:8**
  ```
  - **`037917e` smc: BATMON-1 — Apple SMC battery monitor (x86, UNAOS_SMC=1)** — the whole arc:
  ```
- **review/unaos-batmon-LANDING.md:15**
  ```
  QEMU `isa-applesmc` attach under `UNAOS_SMC=1`).
  ```
- **review/unaos-batmon-LANDING.md:35**
  ```
  - `UNAOS_SMC=1`: `⚡ kernel features: ehcihid,smc` → `✅ x86_64 OK` / `✅ aarch64 OK`, zero smc warn...
  ```
- **review/unaos-batmon-LANDING.md:42**
  ```
  - **Knob-ON QEMU witnesses (`UNAOS_SMC=1 ./arroyo test 40`):**
  ```
- **review/unaos-batmon-LANDING.md:51**
  ```
  - Confirmed too under `UNAOS_SMC=1 UNAOS_USBDEBUG=1 ./arroyo test 25` (the metal on-screen path):
  ```
- **review/unaos-batmon-LANDING.md:83**
  ```
  - **Built-from:** hw-rmbp@037917e, knobs `UNAOS_SMC=1 UNAOS_USBDEBUG=1` (MANIFEST line appended).
  ```
- **review/unaos-batmon-LANDING.md:89**
  ```
  `UNAOS_SMC=1 UNAOS_USBDEBUG=1` media, on the 2012 rMBP over FTDI serial (and on-screen via fbcon ...
  ```
- **unaos/arroyo:96**
  ```
  # BATMON-1: the Apple SMC battery monitor (x86_64). UNAOS_SMC=1 arms the polled SMC key/value driver
  ```
- **unaos/arroyo:101**
  ```
  [ -n "${UNAOS_SMC:-}" ] && _feats="${_feats}smc,"
  ```
- **unaos/builder/src/main.rs:88**
  ```
  // BATMON-1: the Apple SMC battery monitor (x86_64). UNAOS_SMC=1 arms the polled SMC key/value
  ```
- **unaos/builder/src/main.rs:91**
  ```
  if std::env::var("UNAOS_SMC").is_ok() { feats.push("smc"); }
  ```
- **unaos/builder/src/main.rs:405**
  ```
  // BATMON-1 (UNAOS_SMC=1): attach QEMU's ISA AppleSMC so the SMC driver has a protocol target.
  ```
- **unaos/builder/src/main.rs:412**
  ```
  if std::env::var("UNAOS_SMC").is_ok() {
  ```
- **unaos/builder/src/main.rs:416**
  ```
  println!("   UNAOS_SMC: isa-applesmc attached (iobase 0x300) — SMC protocol/read-key target");
  ```
- **unaos/crates/kernel/Cargo.toml:366**
  ```
  # BATMON-1: the Apple SMC battery monitor (UNAOS_SMC=1). Arms the polled SMC key/value driver
  ```
- **unaos/crates/kernel/src/arch/x86_64/pci.rs:229**
  ```
  // BATMON-1 (UNAOS_SMC=1): fire the Apple SMC key-inventory scout once, here at the early x86
  ```
- **unaos/crates/kernel/src/drivers/mod.rs:11**
  ```
  // BATMON-1: Apple SMC polled key/value driver (UNAOS_SMC=1) — the 2012 rMBP battery monitor.
  ```
- **unaos/crates/kernel/src/drivers/smc.rs:4**
  ```
  //! BATMON-1 — Apple SMC (System Management Controller) polled key/value driver (UNAOS_SMC=1).
  ```
- **unaos/crates/kernel/src/drivers/smc.rs:14**
  ```
  //! `smc` feature (`UNAOS_SMC=1`). During a READ transaction the driver writes the 4 key-name bytes
  ```
- **unaos/crates/kernel/src/shell.rs:2026**
  ```
  console.println("POWER:    batmon (SMC battery snapshot; x86 UNAOS_SMC=1 only)");
  ```
- **unaos/crates/kernel/src/shell.rs:2809**
  ```
  console.println("batmon: SMC battery monitor is x86 UNAOS_SMC=1 only");
  ```
- **unaos/docs/dev/OS/09_PLATFORM/smc_battery.md:1**
  ```
  # BATMON-1 — Apple SMC battery monitor (x86, `UNAOS_SMC=1`)
  ```
- **unaos/docs/dev/OS/09_PLATFORM/smc_battery.md:24**
  ```
  only and compiled solely under the `smc` cargo feature (`UNAOS_SMC=1`). Knob off ⇒ the module and
  ```
- **unaos/docs/dev/OS/09_PLATFORM/smc_battery.md:58**
  ```
  Port I/O is confined to **{0x300, 0x304}**, and only under `UNAOS_SMC=1`. During a READ the driver
  ```
- **unaos/docs/dev/OS/09_PLATFORM/smc_battery.md:101**
  ```
  QEMU's `isa-applesmc` (attached by the builder under `UNAOS_SMC=1`) implements only `READ_CMD` over
  ```
- **unaos/docs/dev/OS/09_PLATFORM/smc_battery.md:159**
  ```
  Assertable at the attended 2012 rMBP sitting (`UNAOS_SMC=1` media). Items 1–5 were the original s...
  ```

## `UNAOS_SMOLNET`

- **docs/MILESTONES.md:1143**
  ```
  no side effect, through the real dispatcher); knob-ON `UNAOS_SMOLNET=1 test 90` MISSION + SOCK-1....
  ```
- **docs/MILESTONES.md:2212**
  ```
  `sys_accept(handle)` (**#27**, poll for an inbound connection, `CAP_READ`). Same `UNAOS_SMOLNET` ...
  ```
- **docs/MILESTONES.md:2231**
  ```
  - **Gates:** hermetic `UNAOS_SMOLNET=1 ./arroyo test 90` MISSION SUCCESS + the SOCK-6 PENDING lin...
  ```
- **docs/MILESTONES.md:2232**
  ```
  witnesses intact; server round-trip `witness OK` via `UNAOS_NET=socket UNAOS_SMOLNET=1` builder +...
  ```
- **docs/MILESTONES.md:2252**
  ```
  listening. Same `UNAOS_SMOLNET` knob, x86-only, byte-identical knob-off / aarch64. **No new sysca...
  ```
- **docs/MILESTONES.md:2274**
  ```
  `UNAOS_SMOLNET=1 ./arroyo test 90` prints both honest PENDING lines, MISSION SUCCESS, SOCK-1/2/3/...
  ```
- **docs/MILESTONES.md:2277**
  ```
  `UNAOS_SMOLNET=1 ./arroyo test 90` MISSION + both PENDING lines + SOCK-1/2/3/5 intact; knob-on
  ```
- **docs/MILESTONES.md:2278**
  ```
  `UNAOS_NET=socket UNAOS_SMOLNET=1 ./arroyo test 90` + `net-inject.py sock7` → both SOCK-6 and SOC...
  ```
- **docs/MILESTONES.md:2296**
  ```
  forwards the rest to the upstream resolver (`10.0.2.3:53`). Same `UNAOS_SMOLNET` knob, x86-only, ...
  ```
- **docs/MILESTONES.md:2313**
  ```
  - **Evidence:** hermetic `UNAOS_SMOLNET=1 ./arroyo test 90` →
  ```
- **docs/MILESTONES.md:2316**
  ```
  `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` → the same line with **`bl...
  ```
- **docs/MILESTONES.md:2318**
  ```
  proven across runs): `UNAOS_NET=socket UNAOS_SMOLNET=1 ./arroyo test` + `net-inject.py dns` →
  ```
- **docs/MILESTONES.md:2352**
  ```
  - **Evidence:** hermetic `UNAOS_SMOLNET=1 ./arroyo test 90` (builtin hosts-format list) + full co...
  ```
- **docs/MILESTONES.md:2353**
  ```
  `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` (hosts-format `BLOCK.TXT` ...
  ```
- **docs/MILESTONES.md:2374**
  ```
  the lease itself. Same `UNAOS_SMOLNET` knob, x86-only, byte-identical knob-off / aarch64. **No ne...
  ```
- **docs/MILESTONES.md:2388**
  ```
  - **Gates:** `UNAOS_SMOLNET=1 ./arroyo test 90` MISSION SUCCESS + the SOCK-5 witness OK line, SOC...
  ```
- **docs/MILESTONES.md:2408**
  ```
  Same `UNAOS_SMOLNET` knob, x86-only, byte-identical knob-off / aarch64.
  ```
- **docs/MILESTONES.md:2433**
  ```
  - **Gates:** `UNAOS_SMOLNET=1 ./arroyo test 90` MISSION SUCCESS + the SOCK-4 PASS line (3/3 deter...
  ```
- **docs/MILESTONES.md:2451**
  ```
  capability-transfer inbox recv). Same `UNAOS_SMOLNET` knob, x86-only, byte-identical knob-off / a...
  ```
- **docs/MILESTONES.md:2476**
  ```
  SOCK-3 lines, byte-identical). Knob-on — `UNAOS_SMOLNET=1 check` green both arches, `UNAOS_SMOLNE...
  ```
- **docs/MILESTONES.md:2476**
  ```
  SOCK-3 lines, byte-identical). Knob-on — `UNAOS_SMOLNET=1 check` green both arches, `UNAOS_SMOLNE...
  ```
- **docs/MILESTONES.md:2492**
  ```
  local port, `sys_sendto`(21)/`sys_recvfrom`(22) move datagrams. Same `UNAOS_SMOLNET` knob, x86-only,
  ```
- **docs/MILESTONES.md:2516**
  ```
  (no SOCK-2 lines, byte-identical). Knob-on — `UNAOS_SMOLNET=1 check` green both arches,
  ```
- **docs/MILESTONES.md:2517**
  ```
  `UNAOS_SMOLNET=1 test 60` MISSION + both witness lines above; SOCK-1's ICMP witness intact; zero new
  ```
- **docs/MILESTONES.md:2527**
  ```
  (`UNAOS_SMOLNET=1`) the shell's `ping` / `arp` / `netinfo` route through smoltcp; the hand-rolled
  ```
- **docs/MILESTONES.md:2543**
  ```
  (zero aarch64 impact). Knob-on — `UNAOS_SMOLNET=1 check` green, `UNAOS_SMOLNET=1 test 60` MISSION +
  ```
- **docs/MILESTONES.md:2543**
  ```
  (zero aarch64 impact). Knob-on — `UNAOS_SMOLNET=1 check` green, `UNAOS_SMOLNET=1 test 60` MISSION +
  ```
- **docs/ROADMAP.md:93**
  ```
  | SOCK-1 ✅ 🔬 | **Landed (round 9, `net-sock1`).** smoltcp 0.13.1 (0BSD, `no_std`, static buffers)...
  ```
- **docs/ROADMAP.md:94**
  ```
  | SOCK-2 ✅ 🔬 | **Landed (round 9, `net-sock1`).** The UDP socket syscall family over a persistent...
  ```
- **docs/ROADMAP.md:95**
  ```
  | SOCK-3 ✅ 🔬 | **Landed (round 10, `net-sock1`).** TCP **client** sockets over the same persisten...
  ```
- **docs/ROADMAP.md:96**
  ```
  | SOCK-4 ✅ 🔬 | **Landed (round 13, `net-sock1`).** Scope B — **transferable sockets**: a socket c...
  ```
- **docs/ROADMAP.md:97**
  ```
  | SOCK-5 ✅ 🔬 | **Landed (round 14, `net-sock1`).** Scope B — **DHCP via smoltcp**: the persistent...
  ```
- **docs/ROADMAP.md:98**
  ```
  | SOCK-6 ✅ 🔬 | **Landed (round 15, `net-sock1`).** Scope A — **TCP server/listen sockets**: ring ...
  ```
- **docs/SECURITY.md:173**
  ```
  - [x] **Validated user-pointer access — the single kernel/user copy seam `copy_from_user`/`copy_t...
  ```
- **docs/SECURITY.md:193**
  ```
  - [x] **Sockets as capabilities — the UDP socket syscall family, ring 3's first network reach (SO...
  ```
- **docs/SECURITY.md:193**
  ```
  - [x] **Sockets as capabilities — the UDP socket syscall family, ring 3's first network reach (SO...
  ```
- **docs/SECURITY.md:194**
  ```
  - [x] **TCP client sockets — ring 3 gets a byte stream (SOCK-3, 2026-07-12, QEMU-verified; metal-...
  ```
- **docs/SECURITY.md:194**
  ```
  - [x] **TCP client sockets — ring 3 gets a byte stream (SOCK-3, 2026-07-12, QEMU-verified; metal-...
  ```
- **docs/SECURITY.md:195**
  ```
  - [x] **Transferable sockets — a socket capability moves across processes (SOCK-4 scope B, 2026-0...
  ```
- **docs/SECURITY.md:195**
  ```
  - [x] **Transferable sockets — a socket capability moves across processes (SOCK-4 scope B, 2026-0...
  ```
- **docs/SECURITY.md:196**
  ```
  - [x] **DHCP via smoltcp — the persistent stack leases its own address (SOCK-5 scope B, 2026-07-1...
  ```
- **docs/SECURITY.md:196**
  ```
  - [x] **DHCP via smoltcp — the persistent stack leases its own address (SOCK-5 scope B, 2026-07-1...
  ```
- **docs/SECURITY.md:197**
  ```
  - [x] **TCP server / listen sockets — ring 3 accepts inbound TCP (SOCK-6 scope A, 2026-07-14, QEM...
  ```
- **docs/SECURITY.md:197**
  ```
  - [x] **TCP server / listen sockets — ring 3 accepts inbound TCP (SOCK-6 scope A, 2026-07-14, QEM...
  ```
- **docs/SECURITY.md:198**
  ```
  - [x] **Persistent-listener acceptor pool — the listener survives accepts (SOCK-7, 2026-07-15, QE...
  ```
- **docs/SECURITY.md:198**
  ```
  - [x] **Persistent-listener acceptor pool — the listener survives accepts (SOCK-7, 2026-07-15, QE...
  ```
- **docs/SECURITY.md:199**
  ```
  - [x] **A ring-3 service now ANSWERS network queries — the DNS resolver / sinkhole (SINKHOLE-1 "z...
  ```
- **docs/SECURITY.md:199**
  ```
  - [x] **A ring-3 service now ANSWERS network queries — the DNS resolver / sinkhole (SINKHOLE-1 "z...
  ```
- **docs/SECURITY.md:200**
  ```
  - [x] **The resolver now parses a HOSTILE FILE, not just a hostile packet — real blocklist ingest...
  ```
- **docs/SECURITY.md:200**
  ```
  - [x] **The resolver now parses a HOSTILE FILE, not just a hostile packet — real blocklist ingest...
  ```
- **docs/SECURITY.md:200**
  ```
  - [x] **The resolver now parses a HOSTILE FILE, not just a hostile packet — real blocklist ingest...
  ```
- **review/unaos-smolnet-default-LANDING.md:21**
  ```
  `UNAOS_SMOLNET` to opt-OUT `UNAOS_NOSMOLNET`, mirroring the established PORTSW-1/EHCI-4 default-ON +
  ```
- **review/unaos-smolnet-default-LANDING.md:22**
  ```
  negative-knob idiom (precedent `arroyo:73` ehcihid). No legacy `UNAOS_SMOLNET` alias kept — do-it...
  ```
- **review/unaos-smolnet-default-LANDING.md:57**
  ```
  hand-rolled line (resumable)" section, migrated every reproduce command off `UNAOS_SMOLNET` (0 st...
  ```
- **review/unaos-smolnet-default-LANDING.md:77**
  ```
  ACQUIRED. <<<`. Witness the default IS smoltcp (all with NO `UNAOS_SMOLNET`): `:: SOCK-1: … witne...
  ```
- **review/unaos-smolnet-default-LANDING.md:116**
  ```
  - **Shared docs carry stale `UNAOS_SMOLNET` prose (OUT of the named doc lane — integrator reconci...
  ```
- **review/unaos-smolnet-default-LANDING.md:118**
  ```
  mention `UNAOS_SMOLNET`. These are descriptive (not broken reproduce commands), but should migrat...
  ```
- **review/unaos-zeolite2-LANDING.md:47**
  ```
  - Hermetic `UNAOS_SMOLNET=1 ./arroyo test 90` (builtin hosts-format list), 0 FAIL:
  ```
- **review/unaos-zeolite2-LANDING.md:51**
  ```
  - Full composition `UNAOS_SMOLNET=1 UNAOS_IRQSTORAGE=1 UNAOS_FATIMG=sf ./arroyo test 200` (hosts-...
  ```

## `UNAOS_SMP`

- **unaos/builder/src/main.rs:435**
  ```
  // count with UNAOS_SMP (e.g. `UNAOS_SMP=1` to force uniprocessor). The BSP still
  ```
- **unaos/builder/src/main.rs:435**
  ```
  // count with UNAOS_SMP (e.g. `UNAOS_SMP=1` to force uniprocessor). The BSP still
  ```
- **unaos/builder/src/main.rs:437**
  ```
  let smp = std::env::var("UNAOS_SMP").unwrap_or_else(|_| "4".into());
  ```

## `UNAOS_SMPPROBE`

- **docs/MILESTONES.md:742**
  ```
  ## aarch64 SMP — ORIN-SMP-6: the LAST-DIFFERENCES legs (`UNAOS_SMPPROBE=21..23`) — 2026-07-16 ⚡ B...
  ```
- **docs/MILESTONES.md:834**
  ```
  ## aarch64 SMP — ORIN-SMP-6: the LAST-DIFFERENCES legs (`UNAOS_SMPPROBE=21..23`) — 2026-07-16 🔬 Q...
  ```
- **docs/MILESTONES.md:1191**
  ```
  ## aarch64 SMP — ORIN-SMP-5: the RESIDUE legs (`UNAOS_SMPPROBE=17..20`) — 2026-07-16 ⚡ BENCHED SA...
  ```
- **docs/MILESTONES.md:1219**
  ```
  **How it was tested:** `./arroyo check` green both arches (knob-off) + `UNAOS_TEGRA=1 UNAOS_SMPPR...
  ```
- **docs/MILESTONES.md:1231**
  ```
  ## aarch64 SMP — ORIN-SMP-4: the woken core's EXECUTION BISECT (`UNAOS_SMPPROBE=10..16`) — 2026-0...
  ```
- **docs/MILESTONES.md:1246**
  ```
  ADDR `0x8000000000000200`). Extends `UNAOS_SMPPROBE` (`arch/aarch64/smpprobe.rs`) with leg family
  ```
- **docs/MILESTONES.md:1260**
  ```
  UNAOS_SMPPROBE=15` + `UNAOS_TEGRA=1 UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `test...
  ```
- **docs/MILESTONES.md:1311**
  ```
  ## aarch64 SMP — ORIN-SMP-2: the JM5 `CPU_ON` firmware-wall INVESTIGATION probe (`UNAOS_SMPPROBE`...
  ```
- **docs/MILESTONES.md:1337**
  ```
  every PSCI *query* works. `UNAOS_SMPPROBE=<n>` (tegra + `smpprobe` gated, default OFF) selects ONE
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3711**
  ```
  ### ORIN-SMP-2 — the JM5 `CPU_ON` firmware-wall INVESTIGATION probe (`UNAOS_SMPPROBE`, code-only;...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3720**
  ```
  **The knob (`arch/aarch64/smpprobe.rs`, `tegra`+`smpprobe` gated).** `UNAOS_SMPPROBE=<n>` selects...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3727**
  ```
  `option_env!("UNAOS_SMPPROBE")`, so each armed value is a **distinct image** (distinct hashes; cargo
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3833**
  ```
  ### ORIN-SMP-4 — the woken core's EXECUTION BISECT (`UNAOS_SMPPROBE=10..16`, knob-gated; bench Pe...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3843**
  ```
  **Mechanism (`arch/aarch64/smpprobe.rs`, extends the `UNAOS_SMPPROBE` probe).** Each leg 10..16 w...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3895**
  ```
  distinct kernels (`UNAOS_SMPPROBE` is a compile-time const); validate an armed image by its disti...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3899**
  ```
  UNAOS_SMPPROBE=<n>` + `UNAOS_TEGRA=1 UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `tes...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3905**
  ```
  ### ORIN-SMP-5 — the RESIDUE legs (`UNAOS_SMPPROBE=17..20`, knob-gated; bench Peter-attended)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3935**
  ```
  UNAOS_SMPPROBE=17..20` all compile; knob-off byte-identity proven (two default `esp-jetson` build...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4020**
  ```
  ### ORIN-SMP-6 — the LAST-DIFFERENCES legs (`UNAOS_SMPPROBE=21..23`, knob-gated; bench Peter-atte...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4059**
  ```
  UNAOS_SMPPROBE=21/22/23` all compile; knob-off byte-identity re-proven post-amendment (two default
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4370**
  ```
  knobs (`UNAOS_SMPPROBE=23` era) PLUS `UNAOS_XCARVE_CRCRQ=1` (+ census `UNAOS_XCARVE=1` so the boot
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4389**
  ```
  ### ORIN-SMP-7 — the BOOT-STATE-CONTEXT bisect (`UNAOS_SMPPROBE=24..27`, knob-gated; bench Peter-...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4454**
  ```
  UNAOS_SMPPROBE=24/25` compile; `test-arm 22` MISSION SUCCESS; GICv3 `test-arm 40` CAPSTONE 6/6 + 3/3
  ```
- **unaos/arroyo:129**
  ```
  # ORIN-SMP-2: UNAOS_SMPPROBE=<n> arms the JM5 CPU_ON firmware-wall investigation probe (tegra-only;
  ```
- **unaos/arroyo:131**
  ```
  # option_env!("UNAOS_SMPPROBE") (cargo passes the env to rustc), so each armed value is a distinct
  ```
- **unaos/arroyo:134**
  ```
  [ -n "${UNAOS_SMPPROBE:-}" ] && _feats="${_feats}smpprobe,"
  ```
- **unaos/builder/src/main.rs:108**
  ```
  // ORIN-SMP-2: UNAOS_SMPPROBE=<n> arms the JM5 CPU_ON firmware-wall investigation probe (tegra-only
  ```
- **unaos/builder/src/main.rs:111**
  ```
  if std::env::var("UNAOS_SMPPROBE").is_ok() { feats.push("smpprobe"); }
  ```
- **unaos/crates/kernel/Cargo.toml:136**
  ```
  # ORIN-SMP-2: the `UNAOS_SMPPROBE=<n>` JM5 `CPU_ON` firmware-wall INVESTIGATION probe
  ```
- **unaos/crates/kernel/Cargo.toml:139**
  ```
  # byte-identical to baseline. WHICH experiment runs is `option_env!("UNAOS_SMPPROBE")` (compile-time
  ```
- **unaos/crates/kernel/Cargo.toml:140**
  ```
  # const). Enable via `UNAOS_SMPPROBE=<n> UNAOS_TEGRA=1 ./arroyo esp-jetson`.
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:78**
  ```
  // ORIN-SMP-2 (JM5 `CPU_ON` firmware-wall INVESTIGATION): the `UNAOS_SMPPROBE=<n>` boot-time probe.
  ```
- **unaos/crates/kernel/src/arch/aarch64/smpprobe.rs:11**
  ```
  // This module adds a boot-time, serial-recorded probe selected by `UNAOS_SMPPROBE=<n>` (one
  ```
- **unaos/crates/kernel/src/arch/aarch64/smpprobe.rs:26**
  ```
  // `SEL` is a compile-time const parsed from `option_env!("UNAOS_SMPPROBE")` (default 0). Each armed
  ```
- **unaos/crates/kernel/src/arch/aarch64/smpprobe.rs:60**
  ```
  /// The experiment selected at compile time from `UNAOS_SMPPROBE` (default 0 = the safe control).
  ```
- **unaos/crates/kernel/src/arch/aarch64/smpprobe.rs:61**
  ```
  pub const SEL: u32 = match option_env!("UNAOS_SMPPROBE") {
  ```
- **unaos/crates/kernel/src/arch/aarch64/smpprobe.rs:474**
  ```
  // ORIN-SMP-4 — the woken core's EXECUTION BISECT (UNAOS_SMPPROBE = 10..16)
  ```
- **unaos/crates/kernel/src/arch/aarch64/smpprobe.rs:1366**
  ```
  // ORIN-SMP-6 — the LAST-DIFFERENCES legs (UNAOS_SMPPROBE = 21..23)
  ```
- **unaos/crates/kernel/src/main.rs:1615**
  ```
  // ORIN-SMP-2 (JM5 `CPU_ON` firmware-wall INVESTIGATION): if `UNAOS_SMPPROBE` armed this image
  ```
- **unaos/scripts/orin-smp2-bench.md:6**
  ```
  (`AFFINITY_INFO`) works on silicon (§JM5-result). This runbook drives the `UNAOS_SMPPROBE` probe
  ```
- **unaos/scripts/orin-smp2-bench.md:40**
  ```
  Each armed value is a **distinct image** (`option_env!("UNAOS_SMPPROBE")` const), so rebuild+restage
  ```
- **unaos/scripts/orin-smp2-bench.md:44**
  ```
  UNAOS_TEGRA=1 UNAOS_SMPPROBE=<n> ./arroyo esp-jetson
  ```
- **unaos/scripts/orin-smp4-bench.md:8**
  ```
  `UNAOS_SMPPROBE=10..16` bisect (`arch/aarch64/smpprobe.rs`, §ORIN-SMP-4) to bracket which access is
  ```
- **unaos/scripts/orin-smp4-bench.md:48**
  ```
  | leg | `UNAOS_SMPPROBE=` | variable added over the previous leg | predicted BSP serial | predict...
  ```
- **unaos/scripts/orin-smp4-bench.md:95**
  ```
  trusting the boot (`UNAOS_SMPPROBE` is compile-time — one image per leg).
  ```
- **unaos/scripts/orin-smp5-bench.md:13**
  ```
  (`UNAOS_SMPPROBE=17..20`; `arch/aarch64/smpprobe.rs`, §ORIN-SMP-5). One leg per boot.
  ```
- **unaos/scripts/orin-smp5-bench.md:17**
  ```
  1. **RIDER 1 — a leg-16 re-confirm runs FIRST every sitting.** Re-flash `UNAOS_SMPPROBE=16` and c...
  ```
- **unaos/scripts/orin-smp5-bench.md:55**
  ```
  | leg | `UNAOS_SMPPROBE=` | residue element restored over leg 16 | predicted BSP serial | predict...
  ```
- **unaos/scripts/orin-smp5-bench.md:83**
  ```
  1. Re-flash `UNAOS_SMPPROBE=16` (RIDER 1), boot, assert firmware precondition, confirm
  ```
- **unaos/scripts/orin-smp5-bench.md:102**
  ```
  flashed BEFORE trusting the boot (`UNAOS_SMPPROBE` is compile-time — one image per leg). The DEFAULT
  ```
- **unaos/scripts/orin-smp6-bench.md:16**
  ```
  ORIN-SMP-6 takes them ONE VARIABLE PER LEG (`UNAOS_SMPPROBE=21..23`; `arch/aarch64/smpprobe.rs`
  ```
- **unaos/scripts/orin-smp6-bench.md:23**
  ```
  1. **RIDER 1 — a leg-16 re-confirm runs FIRST every sitting.** Re-flash `UNAOS_SMPPROBE=16` and c...
  ```
- **unaos/scripts/orin-smp6-bench.md:65**
  ```
  | leg | `UNAOS_SMPPROBE=` | the ONE variable | predicted BSP serial | predicted box behavior |
  ```
- **unaos/scripts/orin-smp6-bench.md:86**
  ```
  1. Flash `UNAOS_SMPPROBE=16` (RIDER 1), boot, assert firmware precondition, confirm
  ```
- **unaos/scripts/orin-smp7-bench.md:21**
  ```
  - **Leg 24 (`UNAOS_SMPPROBE=24`)** — the REPRO CONTROL: the wake at the **POST-xHCI-takeover** site
  ```
- **unaos/scripts/orin-smp7-bench.md:23**
  ```
  - **Leg 25 (`UNAOS_SMPPROBE=25`)** — the SAME wake at the **PRE-xHCI-takeover** site
  ```
- **unaos/scripts/orin-smp7-bench.md:36**
  ```
  `UNAOS_SMPPROBE=23` and confirm `RAPID REAL-ENTRY SEQUENCE DONE — 5/5` + `AP -> BSP SGI OK` (or, if
  ```
- **unaos/scripts/orin-smp7-bench.md:74**
  ```
  | leg | `UNAOS_SMPPROBE=` | the ONE variable (position) | predicted BSP serial | predicted box be...
  ```
- **unaos/scripts/orin-smp7-bench.md:99**
  ```
  1. Flash `UNAOS_SMPPROBE=23` (RIDER 1), boot, assert firmware precondition, confirm
  ```
- **unaos/scripts/orin-xcarve-bench.md:39**
  ```
  | **relinked-leg23** | `UNAOS_XCARVE_RELINK=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1` | the 4/4-faulting...
  ```
- **unaos/scripts/orin-xcarve-bench.md:97**
  ```
  | **censusv2-scrub-leg23** | `UNAOS_XCARVE=1 UNAOS_XCARVE_SCRUB=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1...
  ```
- **unaos/scripts/orin-xcarve-bench.md:145**
  ```
  | **crcrq-leg23** | `UNAOS_XCARVE_CRCRQ=1 UNAOS_XCARVE=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1` | the h...
  ```

## `UNAOS_TEGRA`

- **docs/MILESTONES.md:57**
  ```
  for any tegra build (`esp-jetson` force-adds it; `UNAOS_TEGRA=1` adds it) unless
  ```
- **docs/MILESTONES.md:62**
  ```
  `UNAOS_TEGRA=1`}. **Byte-identity proven:** default `esp-jetson` ≡ explicit
  ```
- **docs/MILESTONES.md:877**
  ```
  **How it was tested:** `./arroyo check` green both arches, knob-off AND `UNAOS_TEGRA=1`; `test-ar...
  ```
- **docs/MILESTONES.md:1219**
  ```
  **How it was tested:** `./arroyo check` green both arches (knob-off) + `UNAOS_TEGRA=1 UNAOS_SMPPR...
  ```
- **docs/MILESTONES.md:1259**
  ```
  **How it was tested:** `./arroyo check` green both arches + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/MILESTONES.md:1259**
  ```
  **How it was tested:** `./arroyo check` green both arches + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/MILESTONES.md:1260**
  ```
  UNAOS_SMPPROBE=15` + `UNAOS_TEGRA=1 UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `test...
  ```
- **docs/MILESTONES.md:1300**
  ```
  **How it was tested:** `./arroyo check` green both arches + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/MILESTONES.md:1300**
  ```
  **How it was tested:** `./arroyo check` green both arches + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/MILESTONES.md:1699**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings); `test-a...
  ```
- **docs/MILESTONES.md:1729**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings); `test-a...
  ```
- **docs/MILESTONES.md:1772**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings); `test-a...
  ```
- **docs/MILESTONES.md:1811**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings); `test-a...
  ```
- **docs/MILESTONES.md:1845**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings — only th...
  ```
- **docs/MILESTONES.md:1880**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings — only th...
  ```
- **docs/MILESTONES.md:1916**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings — only th...
  ```
- **docs/MILESTONES.md:1952**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches (no new warnings); `test-a...
  ```
- **docs/MILESTONES.md:1989**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `test-arm 22` MISSION;
  ```
- **docs/MILESTONES.md:2037**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `test-arm 22` MISSION;
  ```
- **docs/MILESTONES.md:2082**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `test-arm 22` MISSION;
  ```
- **docs/MILESTONES.md:2119**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `test-arm 22` MISSION;
  ```
- **docs/MILESTONES.md:2571**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `test-arm 22` MISSION;
  ```
- **docs/MILESTONES.md:2723**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `kernel8` compiles;
  ```
- **docs/MILESTONES.md:2751**
  ```
  - **Tested (QEMU, byte-equivalent — the heal never fires there):** `check` + `UNAOS_TEGRA=1 check`
  ```
- **docs/MILESTONES.md:2782**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `test-arm 22` MISSION;
  ```
- **docs/MILESTONES.md:2822**
  ```
  - **How tested (QEMU):** per milestone: `./arroyo check` + `UNAOS_TEGRA=1` check green (both
  ```
- **docs/MILESTONES.md:2854**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `UNAOS_HUBSTORAGE=1 test 25`
  ```
- **docs/MILESTONES.md:3343**
  ```
  - **Tested (QEMU):** `check` + `UNAOS_TEGRA=1 check` green both arches; `UNAOS_HUBSTORAGE=1 test 25`
  ```
- **docs/MILESTONES.md:3384**
  ```
  - **Tested — QEMU:** `./arroyo check` (x86 + aarch64) and `UNAOS_TEGRA=1 ./arroyo check` green;
  ```
- **docs/MILESTONES.md:3413**
  ```
  - **Tested — QEMU:** `./arroyo check` (x86 + aarch64) and `UNAOS_TEGRA=1 ./arroyo check` green;
  ```
- **docs/MILESTONES.md:3444**
  ```
  - **Tested — QEMU:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches;
  ```
- **docs/MILESTONES.md:4233**
  ```
  - **Gate:** `UNAOS_TEGRA=1 ./arroyo check` both arches + `./arroyo test` + `test-arm` green (tegr...
  ```
- **docs/MILESTONES.md:4271**
  ```
  - **Tested:** `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo test` (x86) + `./arroyo...
  ```
- **docs/MILESTONES.md:4305**
  ```
  - **Tested:** `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo test` (x86) + `./arroyo
  ```
- **docs/MILESTONES.md:4345**
  ```
  - **Tested:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; `esp-jetson`
  ```
- **docs/MILESTONES.md:4375**
  ```
  - **Tested:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; `esp-jetson` l...
  ```
- **docs/MILESTONES.md:4404**
  ```
  - **Tested — QEMU (the DONE gate):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` both arche...
  ```
- **docs/MILESTONES.md:4405**
  ```
  (probes compile clean, zero new warnings); `UNAOS_TEGRA=1 ./arroyo esp-jetson` links (`kernel.elf`
  ```
- **docs/MILESTONES.md:4427**
  ```
  - **Tested — QEMU (the DONE gate):** `./arroyo check` both arches green; `UNAOS_TEGRA=1 ./arroyo
  ```
- **docs/MILESTONES.md:4832**
  ```
  `UNAOS_TEGRA=1 ./arroyo check` both legs, no new warnings. Non-regression: virt
  ```
- **docs/SECURITY.md:244**
  ```
  - **Proof + gate.** `fatdirs_check`/`fatdirs_launcher` (the `k1_atr` disk-selftest idiom, LAST in...
  ```
- **docs/dev/DEBUGGING.md:127**
  ```
  - **Build boot media:** `UNAOS_TEGRA=1 ./arroyo esp-jetson` writes a single
  ```
- **docs/dev/DEBUGGING.md:136**
  ```
  - **Tegra build stops early by design:** with `UNAOS_TEGRA=1` the kernel
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:445**
  ```
  **Regression bar (all held).** `./arroyo check` both arches; `UNAOS_TEGRA=1 ./arroyo build`
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:462**
  ```
  machine), so this lands **QEMU-green** — it compiles under `UNAOS_TEGRA` and the drop MECHANISM
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:555**
  ```
  **Regression bar (all held, QEMU).** `./arroyo check` both arches; `UNAOS_TEGRA=1 ./arroyo check`
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:756**
  ```
  Gate results (2026-07-06, re-run after the review fixes): `./arroyo check` + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:963**
  ```
  **Gate (2026-07-06):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; virt
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1011**
  ```
  Gate (probe milestone): `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1051**
  ```
  Gate (arc close): `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; every
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1296**
  ```
  Gate: `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green; x86 `test` MISSION SUCCESS; virt G...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1364**
  ```
  **Gate:** `./arroyo check` both arches green; `UNAOS_TEGRA=1 ./arroyo esp-jetson` links
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1370**
  ```
  `prepare_aarch64` **without** `UNAOS_TEGRA=1`, so it rebuilds `kernel.elf` as the **non-tegra
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1373**
  ```
  media. **Always rebuild `UNAOS_TEGRA=1 ./arroyo esp-jetson` after any `test-arm`, and size-check
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1446**
  ```
  **Gate:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` both arches green (probes compile cle...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1447**
  ```
  warnings); `UNAOS_TEGRA=1 ./arroyo esp-jetson` links, `kernel.elf` 254,536 B tegra (120 `tegra:` ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1502**
  ```
  **Gate:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` both arches green; `UNAOS_TEGRA=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1502**
  ```
  **Gate:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` both arches green; `UNAOS_TEGRA=1 ./a...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1589**
  ```
  **Gate:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` both arches green; `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1589**
  ```
  **Gate:** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` both arches green; `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1811**
  ```
  before the console takes the screen; set it to 0 to skip. `UNAOS_TEGRA=1 ./arroyo check` green bo...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1846**
  ```
  **Gate:** `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo test` (x86) + `test-arm`
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:1937**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2025**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2120**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2208**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2262**
  ```
  **Tested (QEMU):** `check` both arches + `UNAOS_TEGRA=1 check` green; `kernel8-test 30` = 23 PASS
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2316**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo t...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2383**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo t...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2463**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo t...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2538**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo t...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2598**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches; `./arroyo t...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2666**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2764**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2840**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2903**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:2970**
  ```
  `UNAOS_TEGRA=1 check` both arches; `test-arm 22` MISSION; `UNAOS_GICV3=1 test-arm 40` CAPSTONE 6/6;
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3062**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3130**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3152**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3152**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3168**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3168**
  ```
  **Gate (QEMU):** `./arroyo check` + `UNAOS_TEGRA=1 ./arroyo check` green both arches (no new warn...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3206**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` builds a single-FAT32 USB stick
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3282**
  ```
  The Orin booted the `UNAOS_BOOTDIAG=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` media
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3343**
  ```
  - `./arroyo check` green both arches; `UNAOS_TEGRA=1 ./arroyo build` green both legs
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3421**
  ```
  `UNAOS_TEGRA=1 build` both legs; `esp-jetson` links. The GIC change is tegra-gated or
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3552**
  ```
  `check` both arches; `UNAOS_TEGRA=1 build` both legs; `esp-jetson` links.
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3827**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches) + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3827**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches) + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3898**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches) + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3898**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches) + `UNAOS_TEGRA=1` + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3899**
  ```
  UNAOS_SMPPROBE=<n>` + `UNAOS_TEGRA=1 UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `tes...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3934**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches, knob-off) + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4014**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches, knob-off AND `UNAOS_TEGRA=1`);
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4058**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches, knob-off) + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4453**
  ```
  **Gate (this executor).** `./arroyo check` green (both arches, knob-off) + `UNAOS_TEGRA=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4692**
  ```
  path — the metal build combines it with `UNAOS_TEGRA=1`. Knob-off, the module and its two call sites
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4770**
  ```
  (`+UNAOS_TEGRA=1`) and the QEMU `virt` GICv3 witness. Two knob-gated call sites (`census2`) sit e...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5019**
  ```
  :: PCIE4: ORIN-NET-4 RTL8168 driver compiled; no Tegra234 RC on this build (QEMU virt) — bring-up...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5022**
  ```
  On metal (`UNAOS_NET4=1 UNAOS_TEGRA=1`) the full claim → rings → bind sequence runs; live ICMP/AR...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5031**
  ```
  **Gates green (pre-metal).** `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` both arches (zero net-new
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5092**
  ```
  `./arroyo check` default + `UNAOS_NET4=1 UNAOS_TEGRA=1` + `UNAOS_VNET=1` both arches (zero net-new
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5143**
  ```
  **Gates green:** `./arroyo check` both arches × {knob-off, `UNAOS_TEGRA=1`}; `UNAOS_GICV3=1
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5254**
  ```
  **Gates green:** `./arroyo check` both arches × {knob-off, `UNAOS_TEGRA=1`}; `UNAOS_GICV3=1 ./arroyo
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5274**
  ```
  force-adds `tegra`) also force-adds `tegrasmp` unless opted out, and a `UNAOS_TEGRA=1` build adds
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5291**
  ```
  **Gates green:** `./arroyo check` both arches × {default, `UNAOS_NOTEGRASMP=1`, `UNAOS_TEGRA=1`};
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5358**
  ```
  `UNAOS_SDMMC=1 UNAOS_TEGRA=1` on real Orin silicon touches the controller. Correctness off-metal ...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5394**
  ```
  both the metal tegra build (`UNAOS_SDMMC=1 UNAOS_TEGRA=1`) and the virt witness build (`UNAOS_SDM...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5396**
  ```
  **Gates green.** `./arroyo check` default + `UNAOS_SDMMC=1 UNAOS_TEGRA=1` + `UNAOS_SDMMC=1` (virt...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6348**
  ```
  `./arroyo check` and `UNAOS_NET4=1 UNAOS_SDMMC=1 UNAOS_TEGRA=1 UNAOS_GICV3=1 ./arroyo check` — both
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:6351**
  ```
  brief's `UNAOS_TEGRA=1 … test-arm` combination wedges at the UEFI→kernel handoff on QEMU virt at ...
  ```
- **review/unaos-aarch64-prio-LANDING.md:51**
  ```
  - `UNAOS_TEGRA=1 ./arroyo check` — `✅ x86_64 OK` / `✅ aarch64 OK` (the Orin `run_capstone_boot_core`
  ```
- **review/unaos-install1-LANDING.md:76**
  ```
  `UNAOS_SDMMC_ARM`, `UNAOS_INSTALL_TARGET_SD` (each × virt and × `UNAOS_TEGRA`), plus `UNAOS_INSTA...
  ```
- **review/unaos-install1-LANDING.md:95**
  ```
  The full flow's **first execution is the attended Orin sitting**: `UNAOS_INSTALL_TARGET_SD=1 UNAO...
  ```
- **review/unaos-install2-LANDING.md:96**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo check` — tegra (both arches):** 0 errors, n...
  ```
- **review/unaos-install2-LANDING.md:118**
  ```
  `UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` on a card the operator is willing t...
  ```
- **review/unaos-net-dhcp-LANDING.md:47**
  ```
  - `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both arches) — **Finished** (green).
  ```
- **review/unaos-net-phy-LANDING.md:4**
  ```
  `any(feature = "net4", feature = "vnet")`; exercised via `UNAOS_NET4`/`UNAOS_TEGRA` and `UNAOS_VN...
  ```
- **review/unaos-net-phy-LANDING.md:54**
  ```
  | `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **review/unaos-orin-net4-LANDING.md:48**
  ```
  | `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both arches) | OK, **zero net-new warnings** (aarc...
  ```
- **review/unaos-orin-net4-LANDING.md:67**
  ```
  `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`; stage to `~/unaos-bench/flash/orin/` per the fl...
  ```
- **review/unaos-orin-net4b-LANDING.md:78**
  ```
  - `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` — x86_64 OK + aarch64 OK (no rtl8168 warnings).
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:73**
  ```
  - `UNAOS_SDMMC=1 UNAOS_TEGRA=1 ./arroyo check`: `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc1-LANDING.md:84**
  ```
  recon is metal-only (UNAOS_SDMMC=1 UNAOS_TEGRA=1) ::` printed, and CAPSTONE 6/6 intact.
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:76**
  ```
  - `UNAOS_SDMMC=1 UNAOS_TEGRA=1 ./arroyo check`: `✅ x86_64 OK` / `✅ aarch64 OK`.
  ```
- **review/unaos-orin-sdmmc2-LANDING.md:78**
  ```
  - `UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_TEGRA=1 ./arroyo check` (armed tegra): `✅ x86_64 OK` /
  ```
- **review/unaos-orin-sdmmc3-LANDING.md:76**
  ```
  - **`UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo check` — tegra (both arches):** ✅ both OK, ...
  ```
- **review/unaos-schednext-LANDING.md:108**
  ```
  **Full gate (R20):** `./arroyo check` both arches OK; `UNAOS_TEGRA=1 ./arroyo check` both arches OK;
  ```
- **review/unaos-vug-honesty-LANDING.md:64**
  ```
  - `UNAOS_TEGRA=1 ./arroyo check` both arches: ✅.
  ```
- **review/unaos-x-net-phy-LANDING.md:64**
  ```
  | `UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **review/unaos-x-net-phy-LANDING.md:66**
  ```
  | `UNAOS_NET4=1 UNAOS_VNET=1 UNAOS_TEGRA=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **unaos/arroyo:46**
  ```
  # UNAOS_PI=1 (Raspberry Pi 4) and UNAOS_TEGRA=1 (Jetson Orin Nano / Tegra234) select the aarch64
  ```
- **unaos/arroyo:128**
  ```
  [ -n "${UNAOS_TEGRA:-}" ] && _feats="${_feats}tegra,"
  ```
- **unaos/arroyo:138**
  ```
  # determinism). Any tegra build (UNAOS_TEGRA=1, or the `esp-jetson` target which forces tegra) arms
  ```
- **unaos/arroyo:144**
  ```
  [ -n "${UNAOS_TEGRA:-}" ] && [ -z "${UNAOS_NOTEGRASMP:-}" ] && _feats="${_feats}tegrasmp,tegra,"
  ```
- **unaos/arroyo:176**
  ```
  # census runs on BOTH the metal tegra path (combine with UNAOS_TEGRA=1) and the QEMU virt GICv3 path
  ```
- **unaos/arroyo:187**
  ```
  # (combine with UNAOS_TEGRA=1) and the QEMU virt GICv3 path (graceful skip — no Tegra234 RC). Rea...
  ```
- **unaos/arroyo:209**
  ```
  # UNAOS_TEGRA=1. Default OFF => module + call sites vanish, smoltcp not pulled, byte-identical to
  ```
- **unaos/arroyo:218**
  ```
  # pcieprobe): the recon runs on BOTH the metal tegra path (combine with UNAOS_TEGRA=1) and the QE...
  ```
- **unaos/arroyo:228**
  ```
  # metal-only ARMED witness line (zero MMIO); on metal (combine with UNAOS_TEGRA=1) it runs the la...
  ```
- **unaos/arroyo:238**
  ```
  # honest compiled-present-metal-only line (zero MMIO); on metal (combine with UNAOS_TEGRA=1) it r...
  ```
- **unaos/arroyo:691**
  ```
  # `tegra` UART feature forced on (= UNAOS_TEGRA=1), so the kernel drives the Tegra 16550 at UARTC
  ```
- **unaos/arroyo:1207**
  ```
  _step "check (tegra)"              ""                    "$blog_dir/check-tegra.log"  env UNAOS_T...
  ```
- **unaos/arroyo:1213**
  ```
  _step "esp-jetson (links)"         ""                    "$blog_dir/esp-jetson.log"   env UNAOS_T...
  ```
- **unaos/builder/src/main.rs:40**
  ```
  // fbcon-only serial) and UNAOS_TEGRA=1 (Jetson Orin / Tegra234 UART). Composable. NOTE: keep
  ```
- **unaos/builder/src/main.rs:107**
  ```
  if std::env::var("UNAOS_TEGRA").is_ok() { feats.push("tegra"); }
  ```
- **unaos/crates/kernel/Cargo.toml:132**
  ```
  # with `pi`. Enable via `UNAOS_TEGRA=1`. See arch/aarch64/serial.rs for the base address (TO VERIFY
  ```
- **unaos/crates/kernel/Cargo.toml:140**
  ```
  # const). Enable via `UNAOS_SMPPROBE=<n> UNAOS_TEGRA=1 ./arroyo esp-jetson`.
  ```
- **unaos/crates/kernel/Cargo.toml:160**
  ```
  # compare hashes; zero `JBXC` strings knob-off). Enable via `UNAOS_XCARVE=1 UNAOS_TEGRA=1 ./arroyo
  ```
- **unaos/crates/kernel/Cargo.toml:174**
  ```
  # `JBXC-SCRUB` strings). Enable via `UNAOS_XCARVE_SCRUB=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`
  ```
- **unaos/crates/kernel/Cargo.toml:193**
  ```
  # `UNAOS_XCARVE_CRCRQ=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` (combine with `UNAOS_XCARVE=1` to see the
  ```
- **unaos/crates/kernel/Cargo.toml:202**
  ```
  # byte-identical to baseline. Enable via `UNAOS_XCARVE_RELINK=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`.
  ```
- **unaos/crates/kernel/Cargo.toml:210**
  ```
  # builds; the metal build combines it with `UNAOS_TEGRA=1`. Read-only: DTB census + a
  ```
- **unaos/crates/kernel/Cargo.toml:224**
  ```
  # `pcieprobe`) so the recon runs on BOTH the metal tegra build (combine with `UNAOS_TEGRA=1`) and...
  ```
- **unaos/crates/kernel/Cargo.toml:254**
  ```
  # only prints one honest witness line; the metal build combines `UNAOS_NET4=1 UNAOS_TEGRA=1`. Every
  ```
- **unaos/crates/kernel/Cargo.toml:267**
  ```
  # BOTH the metal tegra build (combine with `UNAOS_TEGRA=1`) and the virt witness build; the driver's
  ```
- **unaos/crates/kernel/src/arch/aarch64/rtl8168_tegra.rs:21**
  ```
  // (`net4_witness_virt`); only `UNAOS_NET4=1 UNAOS_TEGRA=1` on real Orin silicon exercises the ri...
  ```
- **unaos/crates/kernel/src/arch/aarch64/rtl8168_tegra.rs:66**
  ```
  "{} ORIN-NET-4 RTL8168 driver compiled; no Tegra234 RC on this build (QEMU virt) — bring-up is me...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:30**
  ```
  // witness line (`sdmmc_census` below); only `UNAOS_SDMMC=1 UNAOS_TEGRA=1` on real Orin silicon t...
  ```
- **unaos/crates/kernel/src/arch/aarch64/sdmmc_tegra.rs:74**
  ```
  "{} ORIN-SDMMC-1 Tegra234 microSD recon compiled; no Tegra234 SDMMC on this build (QEMU virt) — r...
  ```
- **unaos/crates/kernel/src/main.rs:226**
  ```
  // ORIN-NET-4 (QEMU witness, `UNAOS_NET4=1` without `UNAOS_TEGRA=1`): QEMU virt models no
  ```
- **unaos/crates/kernel/src/main.rs:234**
  ```
  // ORIN-SDMMC-1 (QEMU witness, `UNAOS_SDMMC=1` without `UNAOS_TEGRA=1`): QEMU models no Tegra234
  ```
- **unaos/scripts/jd10-bench.md:14**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd11-bench.md:15**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd12-bench.md:11**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd13-bench.md:12**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd14-bench.md:12**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd15-bench.md:14**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd16-bench.md:14**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd17-bench.md:15**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd18-bench.md:13**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd19-bench.md:13**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd5-bench.md:8**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd6-bench.md:9**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd7-bench.md:12**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd8-bench.md:13**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/jd9-bench.md:14**
  ```
  `UNAOS_TEGRA=1 ./arroyo esp-jetson` → `strings target/aarch64_esp/kernel.elf | grep -c 'tegra:'`
  ```
- **unaos/scripts/orin-net1-bench.md:16**
  ```
  - **`UNAOS_PCIEPROBE=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the census image. The `pcieprobe` f...
  ```
- **unaos/scripts/orin-net1-bench.md:17**
  ```
  is standalone (does not imply `tegra`); the metal build combines it with `UNAOS_TEGRA=1`. Knob-off,
  ```
- **unaos/scripts/orin-net2-bench.md:17**
  ```
  - **`UNAOS_PCIE2=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the recon image. The `pcie2` feature is
  ```
- **unaos/scripts/orin-net2-bench.md:18**
  ```
  standalone (does not imply `tegra`); the metal build combines it with `UNAOS_TEGRA=1`. Knob-off, the
  ```
- **unaos/scripts/orin-net3-bench.md:17**
  ```
  - **`UNAOS_PCIE3=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the recon image. `pcie3` implies `pcie2`
  ```
- **unaos/scripts/orin-net4-bench.md:26**
  ```
  - **`UNAOS_NET4=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the driver image. `net4` implies `pcie3`...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:21**
  ```
  - **`UNAOS_SDMMC=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the **unarmed recon** image. Read-only ...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:23**
  ```
  - **`UNAOS_SDMMC=1 UNAOS_SDMMC_ARM=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`** — the **armed ladder** ...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:121**
  ```
  :: SDMMC: ORIN-SDMMC-1 Tegra234 microSD recon compiled; no Tegra234 SDMMC on this build (QEMU vir...
  ```
- **unaos/scripts/orin-sdmmc1-bench.md:131**
  ```
  - **Image:** `UNAOS_INSTALL_TARGET_SD=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` (the knob pulls `insta...
  ```
- **unaos/scripts/orin-smp-bench.md:17**
  ```
  - **`./arroyo check`** — green both arches, with and without `UNAOS_TEGRA=1`.
  ```
- **unaos/scripts/orin-smp-bench.md:26**
  ```
  The bring-up is already born fixed. To bench on Orin then: build `UNAOS_TEGRA=1 ./arroyo esp-jets...
  ```
- **unaos/scripts/orin-smp2-bench.md:44**
  ```
  UNAOS_TEGRA=1 UNAOS_SMPPROBE=<n> ./arroyo esp-jetson
  ```
- **unaos/scripts/orin-smp3-bench.md:34**
  ```
  UNAOS_TEGRA=1 UNAOS_TEGRASMP=1 ./arroyo esp-jetson
  ```
- **unaos/scripts/orin-xcarve-bench.md:38**
  ```
  | **instrumented** | `UNAOS_XCARVE=1 UNAOS_TEGRA=1` | default layout + the inherited-pointer cens...
  ```
- **unaos/scripts/orin-xcarve-bench.md:39**
  ```
  | **relinked-leg23** | `UNAOS_XCARVE_RELINK=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1` | the 4/4-faulting...
  ```
- **unaos/scripts/orin-xcarve-bench.md:40**
  ```
  | **relinked-default** | `UNAOS_XCARVE_RELINK=1 UNAOS_TEGRA=1` | default, relinked — layout cross...
  ```
- **unaos/scripts/orin-xcarve-bench.md:41**
  ```
  | **knob-off default** | `UNAOS_TEGRA=1` | byte-identical-to-baseline control |
  ```
- **unaos/scripts/orin-xcarve-bench.md:95**
  ```
  | **censusv2** | `UNAOS_XCARVE=1 UNAOS_TEGRA=1` | default layout + census **v2** — endpoint conte...
  ```
- **unaos/scripts/orin-xcarve-bench.md:96**
  ```
  | **censusv2-scrub** | `UNAOS_XCARVE=1 UNAOS_XCARVE_SCRUB=1 UNAOS_TEGRA=1` | census v2 + the aime...
  ```
- **unaos/scripts/orin-xcarve-bench.md:97**
  ```
  | **censusv2-scrub-leg23** | `UNAOS_XCARVE=1 UNAOS_XCARVE_SCRUB=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1...
  ```
- **unaos/scripts/orin-xcarve-bench.md:98**
  ```
  | **knoboff-default** | `UNAOS_TEGRA=1` | byte-identical-to-baseline control (knob-off restore) |
  ```
- **unaos/scripts/orin-xcarve-bench.md:145**
  ```
  | **crcrq-leg23** | `UNAOS_XCARVE_CRCRQ=1 UNAOS_XCARVE=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1` | the h...
  ```
- **unaos/scripts/orin-xcarve-bench.md:146**
  ```
  | **crcrq-default** | `UNAOS_XCARVE_CRCRQ=1 UNAOS_XCARVE=1 UNAOS_TEGRA=1` | default layout + cens...
  ```
- **unaos/scripts/orin-xcarve-bench.md:147**
  ```
  | **knoboff-default** | `UNAOS_TEGRA=1` | byte-identical-to-baseline control (zero `JBXC` / `JBXC...
  ```

## `UNAOS_TEGRASMP`

- **docs/MILESTONES.md:53**
  ```
  `UNAOS_TEGRASMP=1` to the tegra **default**, with a new opt-*out* `UNAOS_NOTEGRASMP=1`
  ```
- **docs/MILESTONES.md:63**
  ```
  `UNAOS_TEGRASMP=1` `esp-jetson` (`kernel.elf` sha256 `bde59b4f…`, both tegrasmp-on);
  ```
- **docs/MILESTONES.md:1260**
  ```
  UNAOS_SMPPROBE=15` + `UNAOS_TEGRA=1 UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `test...
  ```
- **docs/MILESTONES.md:1271**
  ```
  ## aarch64 SMP — ORIN-SMP-3: the real 6-core Orin bring-up (`UNAOS_TEGRASMP`) — 2026-07-15 ⛔ META...
  ```
- **docs/MILESTONES.md:1288**
  ```
  that ORIN-SMP-2's bench proved the JM5 `CPU_ON` wall is gone on current firmware. `UNAOS_TEGRASMP=1`
  ```
- **docs/MILESTONES.md:1301**
  ```
  UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `test-arm 40` CAPSTONE 6/6 + 3/3 secondaries
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3760**
  ```
  ### ORIN-SMP-3 — the real 6-core Orin bring-up (`UNAOS_TEGRASMP`, knob-gated; bench Peter-attended)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3765**
  ```
  default-OFF `UNAOS_TEGRASMP=1` build-time knob that, when armed, brings every real Orin core online
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3828**
  ```
  UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `test-arm 40` CAPSTONE 6/6 + 3/3 secondaries
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:3899**
  ```
  UNAOS_SMPPROBE=<n>` + `UNAOS_TEGRA=1 UNAOS_TEGRASMP=1`; `test-arm 22` MISSION SUCCESS; GICv3 `tes...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4395**
  ```
  `0x800000027767dc80`) fired from the real `UNAOS_TEGRASMP=1` kick-off at its production position,
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4579**
  ```
  ### ORIN-SMP-8 — the tegrasmp RELINK (the layout-axis close-out; BUILD-ONLY, `UNAOS_TEGRASMP` + `...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4593**
  ```
  `arch/aarch64/xusb_tegra.rs`) is composed onto the REAL `UNAOS_TEGRASMP=1` image — the exact SMP-3
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4596**
  ```
  de-dups (compose verified: `./arroyo check` green both arches for `UNAOS_TEGRASMP=1` and
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4597**
  ```
  `UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1`; feature-echo confirms both active). No code changed — the
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5266**
  ```
  `UNAOS_TEGRASMP=1`. This arc promotes the kick-off to the tegra **default** and turns the knob in...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5276**
  ```
  boot-core-only, byte-identical to the pre-flip baseline. `UNAOS_TEGRASMP=1` still arms it explicitly
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5279**
  ```
  tegra media (arroyo's `esp-jetson` does), so it maps the explicit `UNAOS_TEGRASMP=1` knob for
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5285**
  ```
  source is untouched, the new default's ELF is byte-identical to a pre-arc `UNAOS_TEGRASMP=1` buil...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5287**
  ```
  arc: default `esp-jetson` and explicit `UNAOS_TEGRASMP=1 esp-jetson` produce the same `kernel.elf`
  ```
- **unaos/arroyo:140**
  ```
  # workers on the boot core only, byte-identical to the pre-flip baseline. UNAOS_TEGRASMP=1 still ...
  ```
- **unaos/arroyo:143**
  ```
  [ -n "${UNAOS_TEGRASMP:-}" ] && _feats="${_feats}tegrasmp,tegra,"
  ```
- **unaos/builder/src/main.rs:115**
  ```
  // maps the EXPLICIT UNAOS_TEGRASMP=1 knob for parity only. UNAOS_NOTEGRASMP is a no-op here (not...
  ```
- **unaos/builder/src/main.rs:117**
  ```
  if std::env::var("UNAOS_TEGRASMP").is_ok() { feats.push("tegrasmp"); }
  ```
- **unaos/crates/kernel/src/main.rs:1632**
  ```
  // ORIN-SMP-3 (the real 6-core Orin bring-up, `UNAOS_TEGRASMP=1`): with the `tegrasmp` feature
  ```
- **unaos/scripts/orin-smp3-bench.md:1**
  ```
  # ORIN-SMP-3 bench card — the real 6-core Orin bring-up (`UNAOS_TEGRASMP`, attended)
  ```
- **unaos/scripts/orin-smp3-bench.md:34**
  ```
  UNAOS_TEGRA=1 UNAOS_TEGRASMP=1 ./arroyo esp-jetson
  ```
- **unaos/scripts/orin-smp7-bench.md:7**
  ```
  `0x800000027767dc80`) is BOOT-STATE CONTEXT: the fault fired from the real `UNAOS_TEGRASMP=1` kic...
  ```
- **unaos/scripts/orin-smp8-bench.md:9**
  ```
  `UNAOS_TEGRASMP=1` image — the exact build that RAS-faulted 2/2 on 2026-07-15 — by rebuilding it ...
  ```
- **unaos/scripts/orin-smp8-bench.md:18**
  ```
  Both images carry the IDENTICAL real `start_secondaries_tegra` 6-core kick-off (`UNAOS_TEGRASMP=1`).
  ```
- **unaos/scripts/orin-smp8-bench.md:22**
  ```
  - **tegrasmp-original (`UNAOS_TEGRASMP=1`)** — the 2026-07-15 fault-repro CONTROL. This is the exact
  ```
- **unaos/scripts/orin-smp8-bench.md:26**
  ```
  - **tegrasmp-relinked (`UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1`)** — the SAME SMP kick-off at a
  ```
- **unaos/scripts/orin-smp8-bench.md:80**
  ```
  | **tegrasmp-relinked** | `UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1` | shifted layout (+0x4000) | f...
  ```
- **unaos/scripts/orin-smp8-bench.md:81**
  ```
  | **tegrasmp-original** (optional Boot B) | `UNAOS_TEGRASMP=1` | the historical fault build | may...
  ```
- **unaos/scripts/orin-smp8-bench.md:118**
  ```
  (`UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1`).
  ```
- **unaos/scripts/orin-smp8-bench.md:120**
  ```
  (`UNAOS_TEGRASMP=1`); **EXPECTED to RAS at the SMP kick-off** (`…0200`), or to sample the carveout
  ```

## `UNAOS_TEST_SECS`

- **unaos/arroyo:563**
  ```
  UNAOS_SERIAL_LOG="$SERIAL_LOG" UNAOS_TEST_SECS="$secs" cargo +nightly run --release
  ```
- **unaos/builder/src/main.rs:492**
  ```
  // self-terminate after UNAOS_TEST_SECS (default 20s). Keeps automated boot-log capture
  ```
- **unaos/builder/src/main.rs:495**
  ```
  let secs: u64 = std::env::var("UNAOS_TEST_SECS").ok()
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:461**
  ```
  `UNAOS_NET=socket UNAOS_SERIAL_LOG=<log> UNAOS_TEST_SECS=60` and, concurrently,
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:539**
  ```
  `UNAOS_NET=socket UNAOS_TEST_SECS=85 ./arroyo test 90` and, concurrently,
  ```
- **unaos/docs/dev/OS/08_NET/networking.md:615**
  ```
  Over-the-wire sinkhole: launch `UNAOS_NET=socket UNAOS_TEST_SECS=150 ./arroyo test 155` and concu...
  ```

## `UNAOS_USBDEBUG`

- **CLAUDE.md:15**
  ```
  `UNAOS_SCHED_DEMO`, `UNAOS_USBDEBUG`.
  ```
- **docs/MILESTONES.md:5172**
  ```
  real rMBP on cable day (`UNAOS_USBDEBUG=1` boot, FTDI in a root USB-A port).
  ```
- **docs/dev/DEBUGGING.md:54**
  ```
  fbcon before the main loop, so use `UNAOS_USBDEBUG=1 ./arroyo esp-x86` — it
  ```
- **docs/dev/DEBUGGING.md:56**
  ```
  - **Knobs:** `UNAOS_USBDEBUG=1` (debug console view), `UNAOS_BOOTLOG=1`
  ```
- **docs/dev/DEBUGGING.md:88**
  ```
  cable-day recipe is `UNAOS_USBDEBUG=1 ./arroyo esp-x86` (the usbdebug view keeps
  ```
- **docs/dev/OS/05_USER_EXPERIENCE/shell_philosophy.md:87**
  ```
  rMBP was the `usbdebug` build (`UNAOS_USBDEBUG=1`), which attaches the framebuffer console to the
  ```
- **review/unaos-batmon-LANDING.md:51**
  ```
  - Confirmed too under `UNAOS_SMC=1 UNAOS_USBDEBUG=1 ./arroyo test 25` (the metal on-screen path):
  ```
- **review/unaos-batmon-LANDING.md:83**
  ```
  - **Built-from:** hw-rmbp@037917e, knobs `UNAOS_SMC=1 UNAOS_USBDEBUG=1` (MANIFEST line appended).
  ```
- **review/unaos-batmon-LANDING.md:89**
  ```
  `UNAOS_SMC=1 UNAOS_USBDEBUG=1` media, on the 2012 rMBP over FTDI serial (and on-screen via fbcon ...
  ```
- **unaos/arroyo:62**
  ```
  [ -n "${UNAOS_USBDEBUG:-}" ] && _feats="${_feats}usbdebug,"
  ```
- **unaos/arroyo:1219**
  ```
  echo -e "  ${COLOR_CYAN}⚠ this run clobbered any x86 esp media — rebuild UNAOS_USBDEBUG=1 esp-x86...
  ```
- **unaos/builder/src/main.rs:38**
  ```
  // UNAOS_BOOTLOG=1 (hold the boot log on screen instead of the GUI), UNAOS_USBDEBUG=1 (run the
  ```
- **unaos/builder/src/main.rs:52**
  ```
  if std::env::var("UNAOS_USBDEBUG").is_ok() { feats.push("usbdebug"); }
  ```
- **unaos/crates/kernel/Cargo.toml:55**
  ```
  # mouse deltas are visible and photographable on metal. Enable via `UNAOS_USBDEBUG=1`.
  ```
- **unaos/docs/dev/OS/09_PLATFORM/smc_battery.md:92**
  ```
  serial to the framebuffer — the `SMC-BATT` line is also on-screen in the `UNAOS_USBDEBUG` boot Peter
  ```
- **unaos/scripts/specs/round6-rmbp.spec:6**
  ```
  #   Build:  UNAOS_VIDEOBENCH=1 UNAOS_USBDEBUG=1 ./arroyo esp-x86 (knob-ON, built LAST)
  ```

## `UNAOS_USBSERIAL`

- **docs/MILESTONES.md:4803**
  ```
  cleanly with no FAT volume (default / `UNAOS_USBSERIAL=1`). `./arroyo check` both arches; **0
  ```
- **docs/MILESTONES.md:4873**
  ```
  `hello from disk`), the U2.5 FTDI console (`UNAOS_USBSERIAL=1`), and the FAT regression
  ```
- **docs/MILESTONES.md:5115**
  ```
  (`UNAOS_FATIMG=1`), the U2.5 FTDI console (`UNAOS_USBSERIAL=1`), and the FAT
  ```
- **docs/MILESTONES.md:5164**
  ```
  - **Tested — QEMU:** `UNAOS_USBSERIAL=1 ./arroyo test 25` → `FTDI USB-SERIAL
  ```
- **docs/MILESTONES.md:5167**
  ```
  loader (`UNAOS_USBSERIAL=1 UNAOS_FATIMG=1` → both PASS) and the usbdebug view.
  ```
- **docs/dev/DEBUGGING.md:58**
  ```
  `UNAOS_SCHED_DEMO=1`, `UNAOS_USBSERIAL=1` (attach an emulated FTDI console —
  ```
- **docs/dev/DEBUGGING.md:75**
  ```
  - **QEMU (now):** `UNAOS_USBSERIAL=1 ./arroyo test 25` attaches QEMU's
  ```
- **docs/dev/DEBUGGING.md:86**
  ```
  - **Metal (pending, ~2026-07-08):** boot `UNAOS_USBSERIAL` is a QEMU-only knob —
  ```
- **review/unaos-rmbp-fix-LANDING.md:61**
  ```
  FTDI transport. `ftdi.rs` has no feature gate at all. The builder's `UNAOS_USBSERIAL` is a
  ```
- **unaos/arroyo:48**
  ```
  # UNAOS_USBSERIAL=1 is a BUILDER/QEMU knob (not a kernel feature): the builder attaches an FTDI F...
  ```
- **unaos/builder/src/main.rs:333**
  ```
  let usbserial = std::env::var("UNAOS_USBSERIAL").is_ok();
  ```
- **unaos/builder/src/main.rs:420**
  ```
  // U2.5 (UNAOS_USBSERIAL): attach an FTDI FT232 usb-serial device on the xHCI bus. QEMU's
  ```
- **unaos/builder/src/main.rs:424**
  ```
  // NOTE: `is_ok()` treats an EMPTY value as SET (the known knob trap) — `UNAOS_USBSERIAL=` is ON.
  ```

## `UNAOS_V3D`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5772**
  ```
  `arch/aarch64/v3d.rs` behind the `v3d` cargo feature (`UNAOS_V3D=1`, implies `baremetal` ⇒ `pi`),
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5962**
  ```
  `./arroyo check` both arches (knob on + off); `UNAOS_V3D=1 ./arroyo kernel8-test 43` = **MBENCH PASS
  ```
- **review/unaos-v3d2-LANDING.md:44**
  ```
  - `UNAOS_V3D=1 ./arroyo kernel8-test` → `mbench.py --spec pi4-regression.spec --replay`:
  ```
- **review/unaos-v3d3-LANDING.md:71**
  ```
  - `UNAOS_V3D=1 ./arroyo kernel8-test 35` → `mbench.py --spec scripts/specs/pi4-regression.spec --...
  ```
- **unaos/arroyo:836**
  ```
  # PI-V3D-1: UNAOS_V3D=1 adds the `v3d` feature — the VideoCore VI GPU foundation (power/clock/probe,
  ```
- **unaos/arroyo:840**
  ```
  if [ -n "${UNAOS_V3D:-}" ]; then
  ```
- **unaos/arroyo:842**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_V3D: V3D GPU foundation bring-up (probe/MMU/clear; attended-met...
  ```
- **unaos/crates/kernel/Cargo.toml:325**
  ```
  # probe detects absence and degrades gracefully. Enable via `UNAOS_V3D=1 UNAOS_PI=1 ./arroyo
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:88**
  ```
  // verification is attended metal. Enable via `UNAOS_V3D=1 UNAOS_PI=1 ./arroyo kernel8`.
  ```
- **unaos/scripts/pi-v3d-bench.md:12**
  ```
  UNAOS_V3D=1 UNAOS_PI=1 ./arroyo kernel8
  ```
- **unaos/scripts/pi-v3d-bench.md:21**
  ```
  `73035653d83a4c9a…`; built from `us-v3d1` with `UNAOS_V3D=1`, pristine — not booted in QEMU; see the
  ```

## `UNAOS_VIDEOBENCH`

- **docs/dev/OS/08_VIDEO/engine.md:213**
  ```
  `UNAOS_VIDEOBENCH=1` → feature `videobench` → `video/vperf.rs`: relaxed-atomic
  ```
- **unaos/arroyo:70**
  ```
  [ -n "${UNAOS_VIDEOBENCH:-}" ] && _feats="${_feats}videobench,"
  ```
- **unaos/builder/src/main.rs:60**
  ```
  if std::env::var("UNAOS_VIDEOBENCH").is_ok() { feats.push("videobench"); }
  ```
- **unaos/crates/kernel/Cargo.toml:69**
  ```
  # the screendump gates compare. Enable via `UNAOS_VIDEOBENCH=1`. Default OFF => byte-identical.
  ```
- **unaos/scripts/specs/round6-rmbp.spec:6**
  ```
  #   Build:  UNAOS_VIDEOBENCH=1 UNAOS_USBDEBUG=1 ./arroyo esp-x86 (knob-ON, built LAST)
  ```

## `UNAOS_VIDEOCAP`

- **docs/dev/OS/08_VIDEO/engine.md:234**
  ```
  fb address within 512 MiB). `UNAOS_VIDEOCAP=1` (implies videobench) halves the
  ```
- **unaos/arroyo:122**
  ```
  [ -n "${UNAOS_VIDEOCAP:-}" ] && _feats="${_feats}videocap,"
  ```
- **unaos/builder/src/main.rs:98**
  ```
  if std::env::var("UNAOS_VIDEOCAP").is_ok() { feats.push("videocap"); }
  ```
- **unaos/crates/kernel/Cargo.toml:75**
  ```
  # `UNAOS_VIDEOCAP=1`. Default OFF => byte-identical.
  ```

## `UNAOS_VM_GIT7`

- **review/unaos-vmimage-LANDING.md:24**
  ```
  runs `build_user_hello_x86`, then `UNAOS_VM_IMAGE=1 UNAOS_VM_GIT7=<git7> cargo
  ```
- **unaos/arroyo:622**
  ```
  UNAOS_VM_IMAGE=1 UNAOS_VM_GIT7="$git7" cargo +nightly run --release
  ```
- **unaos/builder/src/main.rs:179**
  ```
  // above; the image builder never rebuilds. UNAOS_VM_GIT7 carries the short git hash (identity
  ```
- **unaos/builder/src/main.rs:182**
  ```
  let git7 = std::env::var("UNAOS_VM_GIT7").unwrap_or_else(|_| "0000000".into());
  ```

## `UNAOS_VM_IMAGE`

- **docs/MILESTONES.md:215**
  ```
  machine**. No parallel build logic: the builder gains a `UNAOS_VM_IMAGE` mode
  ```
- **review/unaos-vmimage-LANDING.md:24**
  ```
  runs `build_user_hello_x86`, then `UNAOS_VM_IMAGE=1 UNAOS_VM_GIT7=<git7> cargo
  ```
- **review/unaos-vmimage-LANDING.md:28**
  ```
  - **`unaos/builder/src/main.rs`** — a `UNAOS_VM_IMAGE` early-return mode added
  ```
- **unaos/arroyo:622**
  ```
  UNAOS_VM_IMAGE=1 UNAOS_VM_GIT7="$git7" cargo +nightly run --release
  ```
- **unaos/builder/src/main.rs:181**
  ```
  if std::env::var("UNAOS_VM_IMAGE").is_ok() {
  ```

## `UNAOS_VNET`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5092**
  ```
  `./arroyo check` default + `UNAOS_NET4=1 UNAOS_TEGRA=1` + `UNAOS_VNET=1` both arches (zero net-new
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5602**
  ```
  ## AARCH64-VNET — virtio-net-mmio driver + smoltcp bind on QEMU `virt` (`UNAOS_VNET`, knob-gated)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5663**
  ```
  **How to run.** `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm`. The knob adds both the `vnet` ker...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5666**
  ```
  **Gates green:** `./arroyo check` both arches × {default, `UNAOS_VNET=1`}; knob-off `./arroyo tes...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5668**
  ```
  (unregressed); knob-on `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm` fires the `AARCH64 VNET … =...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5714**
  ```
  `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm` still fires `AARCH64 VNET … => PASS` (4/4 slirp re...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:5754**
  ```
  Gate of record: `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` — DHCP lease PASS + ping 4/4 `[...
  ```
- **review/unaos-net-dhcp-LANDING.md:48**
  ```
  - `UNAOS_VNET=1 ./arroyo check` (both arches) — **Finished** (green).
  ```
- **review/unaos-net-dhcp-LANDING.md:49**
  ```
  - `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` — DHCP lease PASS + ping 4/4 `[dhcp]` PASS + ...
  ```
- **review/unaos-net-phy-LANDING.md:4**
  ```
  `any(feature = "net4", feature = "vnet")`; exercised via `UNAOS_NET4`/`UNAOS_TEGRA` and `UNAOS_VN...
  ```
- **review/unaos-net-phy-LANDING.md:55**
  ```
  | `UNAOS_VNET=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **review/unaos-net-phy-LANDING.md:57**
  ```
  | `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | `:: AARCH64 VNET: ping 10.0.2.2 RTT 4730 us...
  ```
- **review/unaos-orin-net4b-LANDING.md:79**
  ```
  - `UNAOS_VNET=1 ./arroyo check` — x86_64 OK + aarch64 OK.
  ```
- **review/unaos-vnet-LANDING.md:3**
  ```
  **Branch:** `us-vnet` (base `main` @ `e3fbf3f`). **Knob:** `UNAOS_VNET=1` (feature `vnet = ["dep:...
  ```
- **review/unaos-vnet-LANDING.md:17**
  ```
  | M4 | arch_arm64.md §AARCH64-VNET (purpose + the NET-4 relationship table + the transport/negoti...
  ```
- **review/unaos-vnet-LANDING.md:25**
  ```
  - `arroyo` — `UNAOS_VNET` feature wiring + `VNET_ARG` (`-netdev user -device virtio-net-device`) ...
  ```
- **review/unaos-vnet-LANDING.md:47**
  ```
  | `UNAOS_VNET=1 ./arroyo check` (both arches) | **OK** (zero net-new warnings) |
  ```
- **review/unaos-vnet-LANDING.md:50**
  ```
  | knob-on `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | the `AARCH64 VNET … RTL … 4/4 repli...
  ```
- **review/unaos-vnet-LANDING.md:56**
  ```
  `vnet` is default-OFF and armed only by `UNAOS_VNET=1`. With it off, the module + call site are c...
  ```
- **review/unaos-x-net-phy-LANDING.md:65**
  ```
  | `UNAOS_VNET=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **review/unaos-x-net-phy-LANDING.md:66**
  ```
  | `UNAOS_NET4=1 UNAOS_VNET=1 UNAOS_TEGRA=1 ./arroyo check` (both) | `✅ x86_64 OK` · `✅ aarch64 OK` |
  ```
- **review/unaos-x-net-phy-LANDING.md:68**
  ```
  | `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm 40` | `:: AARCH64 VNET: ping 10.0.2.2 RTT 5368 us...
  ```
- **unaos/arroyo:242**
  ```
  # AARCH64-VNET: UNAOS_VNET=1 arms the virtio-net-mmio driver + smoltcp bind on the QEMU `virt` ma...
  ```
- **unaos/arroyo:246**
  ```
  # it runs on the plain virt GICv3 path (UNAOS_VNET=1 UNAOS_GICV3=1 test-arm). The knob ALSO adds the
  ```
- **unaos/arroyo:249**
  ```
  [ -n "${UNAOS_VNET:-}" ] && _feats="${_feats}vnet,"
  ```
- **unaos/arroyo:316**
  ```
  # AARCH64-VNET: UNAOS_VNET=1 attaches a `virtio-net-device` on the QEMU `virt` virtio-mmio bus wi...
  ```
- **unaos/arroyo:323**
  ```
  if [ -n "${UNAOS_VNET:-}" ]; then
  ```
- **unaos/arroyo:325**
  ```
  echo -e "${COLOR_YELLOW}⚡ aarch64 virt: virtio-net-device + slirp user net (UNAOS_VNET)${COLOR_RE...
  ```
- **unaos/crates/kernel/Cargo.toml:298**
  ```
  # AARCH64-VNET: the `UNAOS_VNET=1` virtio-net-mmio driver on the QEMU `virt` machine + smoltcp bind
  ```
- **unaos/crates/kernel/Cargo.toml:308**
  ```
  # `UNAOS_VNET=1 UNAOS_GICV3=1 ./arroyo test-arm`. See arch_arm64.md §AARCH64-VNET.
  ```
- **unaos/crates/kernel/src/arch/aarch64/mod.rs:135**
  ```
  // AARCH64-VNET (`UNAOS_VNET=1`): a virtio-net-mmio driver on the QEMU `virt` machine + smoltcp bind
  ```
- **unaos/crates/kernel/src/main.rs:241**
  ```
  // AARCH64-VNET (QEMU witness, `UNAOS_VNET=1`): drive a `virtio-net-device` on the `virt`
  ```
- **unaos/crates/kernel/src/main.rs:246**
  ```
  // completes synchronously and emits a self-checking PASS/FAIL line. Needs `UNAOS_VNET=1`'s
  ```

## `UNAOS_WITNESS`

- **docs/SECURITY.md:271**
  ```
  - **K3-BIT5 (K3-mount root-listing assertion made native-store-aware — 2026-07-18, QEMU-reproduce...
  ```
- **docs/dev/DEBUGGING.md:27**
  ```
  > `kernel8-test` export `UNAOS_WITNESS=1` (which arroyo turns into the `witness`
  ```
- **docs/dev/DEBUGGING.md:31**
  ```
  > the whole battery on metal), set `UNAOS_WITNESS=1` explicitly:
  ```
- **docs/dev/DEBUGGING.md:32**
  ```
  > `UNAOS_WITNESS=1 ./arroyo kernel8`. On aarch64 `virt`/`tegra` the `witness`
  ```
- **review/unaos-default-quiet-LANDING.md:65**
  ```
  - **`arroyo`:** a top-of-script `case` auto-**exports** `UNAOS_WITNESS=1` for
  ```
- **review/unaos-default-quiet-LANDING.md:71**
  ```
  - **`builder/src/main.rs`:** `if UNAOS_WITNESS.is_ok() { feats.push("witness") }` (kept in sync w...
  ```
- **review/unaos-default-quiet-LANDING.md:81**
  ```
  UNAOS_WITNESS=1 ./arroyo check    → ✅ x86_64 OK  ✅ aarch64 OK   (features: witness,ehcihid,smolnet)
  ```
- **review/unaos-default-quiet-LANDING.md:86**
  ```
  ./arroyo kernel8-test 35          → "UNAOS_WITNESS: kernel8 carries the M6b..U7 fixture battery" ·
  ```
- **review/unaos-gui-witness-LANDING.md:88**
  ```
  - `UNAOS_WITNESS=1 ./arroyo test 22` — `✅` complete; MISSION SUCCESS.
  ```
- **review/unaos-k3-bit5-LANDING.md:21**
  ```
  - Standard `UNAOS_WITNESS=1 ./arroyo kernel8-test 35` reports `K3-mount PASS [w=0x1ff]`:
  ```
- **review/unaos-k3-bit5-LANDING.md:68**
  ```
  - `UNAOS_WITNESS=1 ./arroyo kernel8-test 35` (clean image) —
  ```
- **review/unaos-k3-bit5-LANDING.md:76**
  ```
  - `UNAOS_WITNESS=1 ./arroyo test 22` — exit 0, no `:: … FAIL` (unregressed).
  ```
- **unaos/arroyo:37**
  ```
  export UNAOS_WITNESS="${UNAOS_WITNESS:-1}" ;;
  ```
- **unaos/arroyo:37**
  ```
  export UNAOS_WITNESS="${UNAOS_WITNESS:-1}" ;;
  ```
- **unaos/arroyo:54**
  ```
  # DEFAULT-QUIET: UNAOS_WITNESS=1 arms the `witness` fixture-battery feature (auto-set above for t...
  ```
- **unaos/arroyo:56**
  ```
  # also flows through UNAOS_WITNESS there; on aarch64 virt/tegra `witness` gates nothing (all its ...
  ```
- **unaos/arroyo:58**
  ```
  [ -n "${UNAOS_WITNESS:-}" ] && _feats="${_feats}witness,"
  ```
- **unaos/arroyo:810**
  ```
  # DEFAULT-QUIET: `kernel8-test` (the battery) arms UNAOS_WITNESS so the Pi bare-metal image carri...
  ```
- **unaos/arroyo:813**
  ```
  if [ -n "${UNAOS_WITNESS:-}" ]; then
  ```
- **unaos/arroyo:815**
  ```
  echo -e "${COLOR_YELLOW}  • UNAOS_WITNESS: kernel8 carries the M6b..U7 fixture battery (CAPSTONE ...
  ```
- **unaos/arroyo:1023**
  ```
  # every gate). Deliberately do NOT arm UNAOS_WITNESS — the scratch card is BLANK, so the fixture
  ```
- **unaos/builder/src/main.rs:44**
  ```
  // DEFAULT-QUIET: UNAOS_WITNESS=1 arms the `witness` fixture-battery feature so a headless `test`...
  ```
- **unaos/builder/src/main.rs:48**
  ```
  if std::env::var("UNAOS_WITNESS").is_ok() { feats.push("witness"); }
  ```
- **unaos/crates/kernel/Cargo.toml:18**
  ```
  # (arroyo/builder push `witness` for `test`/`test-fat`/`test-arm`/`kernel8-test` via UNAOS_WITNES...
  ```

## `UNAOS_XCARVE`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4098**
  ```
  ### JETSON-XCARVE — the xHCI-takeover carveout wall (diagnosis arc; `UNAOS_XCARVE` / `UNAOS_XCARV...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4135**
  ```
  **M2 — the inherited-pointer instrumentation (`UNAOS_XCARVE=1`).** `jb2b_attach` gained a
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4233**
  ```
  **Census v2 (`UNAOS_XCARVE=1`, unchanged feature).** `jbxc_inherited_dump` now walks each Configured
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4370**
  ```
  knobs (`UNAOS_SMPPROBE=23` era) PLUS `UNAOS_XCARVE_CRCRQ=1` (+ census `UNAOS_XCARVE=1` so the boot
  ```
- **unaos/arroyo:145**
  ```
  # JETSON-XCARVE M2: UNAOS_XCARVE=1 arms the inherited-xHCI-pointer instrumentation
  ```
- **unaos/arroyo:150**
  ```
  [ -n "${UNAOS_XCARVE:-}" ] && _feats="${_feats}xcarve,tegra,"
  ```
- **unaos/arroyo:156**
  ```
  # Combine with UNAOS_XCARVE=1 to emit the census + scrub together. See arch_arm64.md §JETSON-XCARVE.
  ```
- **unaos/arroyo:165**
  ```
  # baseline (zero `JBXC-CRCRQ` strings). Combine with UNAOS_XCARVE=1 for census + quiesce together.
  ```
- **unaos/crates/kernel/Cargo.toml:153**
  ```
  # JETSON-XCARVE M2: the `UNAOS_XCARVE=1` inherited-xHCI-pointer instrumentation
  ```
- **unaos/crates/kernel/Cargo.toml:160**
  ```
  # compare hashes; zero `JBXC` strings knob-off). Enable via `UNAOS_XCARVE=1 UNAOS_TEGRA=1 ./arroyo
  ```
- **unaos/crates/kernel/Cargo.toml:175**
  ```
  # (combine with `UNAOS_XCARVE=1` to see the census + scrub together). See arch_arm64.md
  ```
- **unaos/crates/kernel/Cargo.toml:193**
  ```
  # `UNAOS_XCARVE_CRCRQ=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` (combine with `UNAOS_XCARVE=1` to see the
  ```
- **unaos/scripts/orin-xcarve-bench.md:38**
  ```
  | **instrumented** | `UNAOS_XCARVE=1 UNAOS_TEGRA=1` | default layout + the inherited-pointer cens...
  ```
- **unaos/scripts/orin-xcarve-bench.md:95**
  ```
  | **censusv2** | `UNAOS_XCARVE=1 UNAOS_TEGRA=1` | default layout + census **v2** — endpoint conte...
  ```
- **unaos/scripts/orin-xcarve-bench.md:96**
  ```
  | **censusv2-scrub** | `UNAOS_XCARVE=1 UNAOS_XCARVE_SCRUB=1 UNAOS_TEGRA=1` | census v2 + the aime...
  ```
- **unaos/scripts/orin-xcarve-bench.md:97**
  ```
  | **censusv2-scrub-leg23** | `UNAOS_XCARVE=1 UNAOS_XCARVE_SCRUB=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1...
  ```
- **unaos/scripts/orin-xcarve-bench.md:145**
  ```
  | **crcrq-leg23** | `UNAOS_XCARVE_CRCRQ=1 UNAOS_XCARVE=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1` | the h...
  ```
- **unaos/scripts/orin-xcarve-bench.md:146**
  ```
  | **crcrq-default** | `UNAOS_XCARVE_CRCRQ=1 UNAOS_XCARVE=1 UNAOS_TEGRA=1` | default layout + cens...
  ```

## `UNAOS_XCARVE_CRCRQ`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4309**
  ```
  #### CRCR-QUIESCE arc — the command-ring re-seat (`UNAOS_XCARVE_CRCRQ`)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4370**
  ```
  knobs (`UNAOS_SMPPROBE=23` era) PLUS `UNAOS_XCARVE_CRCRQ=1` (+ census `UNAOS_XCARVE=1` so the boot
  ```
- **unaos/arroyo:158**
  ```
  # JETSON-XCARVE-CRCR M1: UNAOS_XCARVE_CRCRQ=1 arms the command-ring quiesce + re-seat
  ```
- **unaos/arroyo:167**
  ```
  [ -n "${UNAOS_XCARVE_CRCRQ:-}" ] && _feats="${_feats}xcarve_crcrq,tegra,"
  ```
- **unaos/crates/kernel/Cargo.toml:179**
  ```
  # JETSON-XCARVE-CRCR M1: the `UNAOS_XCARVE_CRCRQ=1` command-ring quiesce + re-seat
  ```
- **unaos/crates/kernel/Cargo.toml:193**
  ```
  # `UNAOS_XCARVE_CRCRQ=1 UNAOS_TEGRA=1 ./arroyo esp-jetson` (combine with `UNAOS_XCARVE=1` to see the
  ```
- **unaos/crates/kernel/src/arch/aarch64/xusb_tegra.rs:1093**
  ```
  // JETSON-XCARVE-CRCR M1: the command-ring quiesce + re-seat (`UNAOS_XCARVE_CRCRQ=1`).
  ```
- **unaos/scripts/orin-xcarve-bench.md:133**
  ```
  ## CRCR-QUIESCE sitting — the command-ring re-seat (`UNAOS_XCARVE_CRCRQ`; attended; pre-registered)
  ```
- **unaos/scripts/orin-xcarve-bench.md:145**
  ```
  | **crcrq-leg23** | `UNAOS_XCARVE_CRCRQ=1 UNAOS_XCARVE=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1` | the h...
  ```
- **unaos/scripts/orin-xcarve-bench.md:146**
  ```
  | **crcrq-default** | `UNAOS_XCARVE_CRCRQ=1 UNAOS_XCARVE=1 UNAOS_TEGRA=1` | default layout + cens...
  ```

## `UNAOS_XCARVE_RELINK`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4098**
  ```
  ### JETSON-XCARVE — the xHCI-takeover carveout wall (diagnosis arc; `UNAOS_XCARVE` / `UNAOS_XCARV...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4164**
  ```
  **M1 — the relink experiment (`UNAOS_XCARVE_RELINK=1`).** A `#[used]` (never GC'd) inert read-onl...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4579**
  ```
  ### ORIN-SMP-8 — the tegrasmp RELINK (the layout-axis close-out; BUILD-ONLY, `UNAOS_TEGRASMP` + `...
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4597**
  ```
  `UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1`; feature-echo confirms both active). No code changed — the
  ```
- **unaos/arroyo:168**
  ```
  # JETSON-XCARVE M1: UNAOS_XCARVE_RELINK=1 arms the relink-experiment pad (a `#[used]` inert
  ```
- **unaos/arroyo:173**
  ```
  [ -n "${UNAOS_XCARVE_RELINK:-}" ] && _feats="${_feats}xcarve_relink,tegra,"
  ```
- **unaos/crates/kernel/Cargo.toml:197**
  ```
  # JETSON-XCARVE M1: the `UNAOS_XCARVE_RELINK=1` relink-experiment pad — a `#[used]` read-only pad
  ```
- **unaos/crates/kernel/Cargo.toml:202**
  ```
  # byte-identical to baseline. Enable via `UNAOS_XCARVE_RELINK=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`.
  ```
- **unaos/crates/kernel/src/arch/aarch64/xusb_tegra.rs:132**
  ```
  // same). Rebuild the layout-correlated leg-23 image with `UNAOS_XCARVE_RELINK=1` and re-bench: if
  ```
- **unaos/scripts/orin-smp8-bench.md:10**
  ```
  the inert relink pad composed in (`UNAOS_XCARVE_RELINK=1`), which shifts the whole image +0x4000 ...
  ```
- **unaos/scripts/orin-smp8-bench.md:26**
  ```
  - **tegrasmp-relinked (`UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1`)** — the SAME SMP kick-off at a
  ```
- **unaos/scripts/orin-smp8-bench.md:80**
  ```
  | **tegrasmp-relinked** | `UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1` | shifted layout (+0x4000) | f...
  ```
- **unaos/scripts/orin-smp8-bench.md:118**
  ```
  (`UNAOS_TEGRASMP=1 UNAOS_XCARVE_RELINK=1`).
  ```
- **unaos/scripts/orin-xcarve-bench.md:39**
  ```
  | **relinked-leg23** | `UNAOS_XCARVE_RELINK=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1` | the 4/4-faulting...
  ```
- **unaos/scripts/orin-xcarve-bench.md:40**
  ```
  | **relinked-default** | `UNAOS_XCARVE_RELINK=1 UNAOS_TEGRA=1` | default, relinked — layout cross...
  ```

## `UNAOS_XCARVE_SCRUB`

- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4226**
  ```
  #### FIX arc — census v2 (endpoint contexts) + aimed neutralization (`UNAOS_XCARVE_SCRUB`)
  ```
- **docs/dev/OS/01_BOOT_HAL/arch_arm64.md:4245**
  ```
  **The aimed neutralization (`UNAOS_XCARVE_SCRUB=1`, new feature `xcarve_scrub ⇒ tegra`, default O...
  ```
- **unaos/arroyo:151**
  ```
  # JETSON-XCARVE FIX M2: UNAOS_XCARVE_SCRUB=1 arms the aimed neutralization
  ```
- **unaos/arroyo:157**
  ```
  [ -n "${UNAOS_XCARVE_SCRUB:-}" ] && _feats="${_feats}xcarve_scrub,tegra,"
  ```
- **unaos/crates/kernel/Cargo.toml:164**
  ```
  # JETSON-XCARVE FIX M2: the `UNAOS_XCARVE_SCRUB=1` aimed neutralization
  ```
- **unaos/crates/kernel/Cargo.toml:174**
  ```
  # `JBXC-SCRUB` strings). Enable via `UNAOS_XCARVE_SCRUB=1 UNAOS_TEGRA=1 ./arroyo esp-jetson`
  ```
- **unaos/crates/kernel/src/arch/aarch64/xusb_tegra.rs:973**
  ```
  // JETSON-XCARVE FIX M2: the least-mutating neutralization (`UNAOS_XCARVE_SCRUB=1`).
  ```
- **unaos/scripts/orin-xcarve-bench.md:96**
  ```
  | **censusv2-scrub** | `UNAOS_XCARVE=1 UNAOS_XCARVE_SCRUB=1 UNAOS_TEGRA=1` | census v2 + the aime...
  ```
- **unaos/scripts/orin-xcarve-bench.md:97**
  ```
  | **censusv2-scrub-leg23** | `UNAOS_XCARVE=1 UNAOS_XCARVE_SCRUB=1 UNAOS_SMPPROBE=23 UNAOS_TEGRA=1...
  ```

