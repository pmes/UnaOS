# Rust Build Artifacts
target/
**/*.rs.bk

# Large Binary Assets
*.img
*.iso
*.bin

# OS Clutter
.DS_Store
Thumbs.db

# Editor Clutter
.idea/
.vscode/
*.swp
*~

# Agent harness state (session data + agent worktrees; multi-GB, never committed)
.claude/

# Upstream clones fetched for reference, not vendored
# NOTE: nouveau/rnndb register SEMANTICS are banned on the Kepler lanes
# (docs/dev/GEMINI/README.md §5); addresses with citation are fine.
envytools/

# Secrets (Just in case)
secret.txt
evidence.txt
.env
cargo_check.log
jetson-serial.log
pi-serial.log
aether-ledger.txt
