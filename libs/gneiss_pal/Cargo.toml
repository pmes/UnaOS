[package]
name = "gneiss_pal"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = ["dep:winit", "dep:softbuffer", "dep:raw-window-handle"]

[dependencies]
winit = { version = "0.29.15", optional = true }
softbuffer = { version = "0.4.1", optional = true }
raw-window-handle = { version = "0.6.0", optional = true }
ab_glyph = "0.2.23"
