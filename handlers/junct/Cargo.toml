[package]
name = "junct"
version = "0.1.0"
edition = "2024"
license = "LGPL-3.0-or-later"

# Design-stage handler — see README.md. No dependencies yet: the message
# contract (a Bandy `SMessage` Stream producer, symmetric with vein) lands
# with the first implementation.
[dependencies]
